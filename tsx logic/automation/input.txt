```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C4_S1',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C4_S1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1.04, 1.0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Reveal line animation
  const lineW = interpolate(frame, [20, 55], [0, 700], {
    easing: Easing.out(Easing.expo),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Warning pulse
  const warnPulse = Math.sin(frame * 0.12) * 0.4 + 0.6;

  // Text reveal
  const headOp = interpolate(frame, [10, 35], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const subOp = interpolate(frame, [35, 60], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const tagOp = interpolate(frame, [55, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const headY = sp(10) * 0;
  const subSlide = interpolate(frame, [35, 65], [40, 0], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Stick figure
  const figureY = sp(45) * 1;
  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  // Particles
  const particles = Array.from({ length: 28 }, (_, i) => ({
    x: seededRandom(i * 7) * 1920,
    y: seededRandom(i * 13) * 1080,
    r: seededRandom(i * 3) * 3 + 1,
    speed: seededRandom(i * 17) * 0.4 + 0.1,
    op: seededRandom(i * 11) * 0.18 + 0.04,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === DYNAMIC GRADIENT BACKGROUND === */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse 80% 60% at 20% 50%, #2a0a0a 0%, #1C1C1C 55%, #0a0a1a 100%)',
        }} />
        <div style={{
          position: 'absolute', inset: 0,
          background: 'linear-gradient(135deg, rgba(255,0,0,0.07) 0%, transparent 50%, rgba(233,215,0,0.05) 100%)',
        }} />
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      {/* Giant circle top-right */}
      <div style={{
        position: 'absolute',
        top: -260, right: -200,
        width: 720, height: 720,
        borderRadius: '50%',
        border: '1.5px solid rgba(255,0,0,0.06)',
        transform: `rotate(${frame * 0.08}deg) scale(${1 + Math.sin(frame * 0.02) * 0.03})`,
        opacity: 0.5,
      }} />
      {/* Inner circle */}
      <div style={{
        position: 'absolute',
        top: -160, right: -100,
        width: 460, height: 460,
        borderRadius: '50%',
        border: '1px solid rgba(255,0,0,0.04)',
        transform: `rotate(${-frame * 0.05}deg)`,
        opacity: 0.4,
      }} />
      {/* Bottom-left triangle shape */}
      <svg style={{ position: 'absolute', bottom: -60, left: -40, opacity: 0.04 }} width="420" height="420">
        <polygon points="210,0 420,420 0,420" fill="none" stroke="#e9d700" strokeWidth="1.5"
          transform={`rotate(${frame * 0.03}, 210, 210)`} />
      </svg>
      {/* Horizontal slab */}
      <div style={{
        position: 'absolute',
        top: '48%', left: -100,
        width: 2200, height: 1,
        background: 'linear-gradient(90deg, transparent, rgba(255,0,0,0.06), transparent)',
        transform: `translateY(${Math.sin(frame * 0.02) * 8}px)`,
      }} />

      {/* === MOTION TEXTURE: GRID === */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.025 }} width="1920" height="1080">
        {Array.from({ length: 22 }, (_, i) => (
          <line key={`v${i}`} x1={i * 96} y1={0} x2={i * 96} y2={1080} stroke="#F5F5F5" strokeWidth="0.5" />
        ))}
        {Array.from({ length: 13 }, (_, i) => (
          <line key={`h${i}`} x1={0} y1={i * 90} x2={1920} y2={i * 90} stroke="#F5F5F5" strokeWidth="0.5" />
        ))}
      </svg>

      {/* === FLOATING PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x + Math.sin(frame * p.speed + i) * 12,
          top: p.y + Math.cos(frame * p.speed * 0.7 + i) * 8,
          width: p.r * 2, height: p.r * 2,
          borderRadius: '50%',
          backgroundColor: i % 3 === 0 ? '#ff0000' : '#e9d700',
          opacity: p.op,
        }} />
      ))}

      {/* === MAIN CONTENT === */}
      {/* WARNING TAG */}
      <div style={{
        position: 'absolute',
        top: 80, left: 100,
        opacity: tagOp,
        display: 'flex', alignItems: 'center', gap: 14,
      }}>
        <div style={{
          width: 10, height: 10, borderRadius: '50%',
          backgroundColor: '#ff0000',
          opacity: warnPulse,
          boxShadow: '0 0 12px rgba(255,0,0,0.8)',
        }} />
        <span style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 22, color: '#ff0000', fontWeight: 700,
          letterSpacing: 4, textTransform: 'uppercase',
        }}>KAUM JEMAND SPRICHT DARÜBER</span>
      </div>

      {/* MAIN HEADLINE */}
      <div style={{
        position: 'absolute',
        top: 160, left: 100,
        opacity: headOp,
        transform: `translateY(${headY}px)`,
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 120, color: '#F5F5F5',
          lineHeight: 1.0,
          textShadow: '0 4px 40px rgba(0,0,0,0.6)',
        }}>
          STEUERLICHE
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 160, color: '#ff0000',
          lineHeight: 0.9,
          textShadow: '0 0 60px rgba(255,0,0,0.4)',
        }}>
          ASYMMETRIE
        </div>
      </div>

      {/* REVEAL LINE */}
      <div style={{
        position: 'absolute',
        top: 420, left: 100,
        width: lineW, height: 4,
        background: 'linear-gradient(90deg, #ff0000, #e9d700)',
        borderRadius: 2,
      }} />

      {/* SUBHEADLINE */}
      <div style={{
        position: 'absolute',
        top: 450, left: 100, right: 500,
        opacity: subOp,
        transform: `translateY(${subSlide}px)`,
      }}>
        <p style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: 48, color: '#9E9E9E',
          lineHeight: 1.5, margin: 0,
          fontWeight: 400,
        }}>
          Das System behandelt nicht alle Einkommensarten gleich.{' '}
          <span style={{ color: '#F5F5F5', fontWeight: 700 }}>Ganz und gar nicht.</span>
        </p>
      </div>

      {/* CHAPTER BADGE */}
      <div style={{
        position: 'absolute',
        bottom: 100, left: 100,
        opacity: tagOp,
        display: 'flex', alignItems: 'center', gap: 16,
      }}>
        <div style={{
          padding: '10px 22px',
          border: '1px solid rgba(255,0,0,0.4)',
          borderRadius: 4,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 20, color: '#ff0000',
          letterSpacing: 2,
        }}>KAPITEL 4</div>
        <span style={{ fontFamily: 'Open Sans', fontSize: 22, color: '#9E9E9E' }}>
          Wer zahlt am meisten — und wer kaum etwas
        </span>
      </div>

      {/* === STICK FIGURE === */}
      <div style={{
        position: 'absolute',
        right: 160, bottom: 80,
        opacity: sp(45),
        transform: `translateY(${(1 - sp(45)) * 80 + breathe}px)`,
      }}>
        <svg width="110" height="200" viewBox="0 0 110 200">
          {/* Golden glow */}
          <ellipse cx="55" cy="190" rx="38" ry="8" fill="rgba(255,193,7,0.15)" />
          {/* Body */}
          <line x1="55" y1="85" x2="55" y2="150" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          {/* Left arm raised (alert pose) */}
          <line x1="55" y1="105" x2="22" y2="82" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Right arm */}
          <line x1="55" y1="105" x2="88" y2="118" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Left leg */}
          <line x1="55" y1="150" x2="30" y2="185" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Right leg */}
          <line x1="55" y1="150" x2="80" y2="185" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Head */}
          <g transform={`rotate(${headNod}, 55, 60)`}>
            <circle cx="55" cy="55" r="26" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            {/* Golden glasses */}
            <circle cx="44" cy="54" r="9" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <circle cx="66" cy="54" r="9" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="53" y1="54" x2="57" y2="54" stroke="#FFC107" strokeWidth="2" />
            {/* Concerned expression */}
            <path d="M44,68 Q55,64 66,68" stroke="#F5F5F5" strokeWidth="2" fill="none" strokeLinecap="round" />
            <line x1="42" y1="46" x2="48" y2="44" stroke="#F5F5F5" strokeWidth="1.5" />
            <line x1="68" y1="44" x2="62" y2="46" stroke="#F5F5F5" strokeWidth="1.5" />
          </g>
          {/* Exclamation mark held */}
          <text x="14" y="76" fill="#ff0000" fontFamily="Anton" fontSize="28" fontWeight="bold">!</text>
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse 90% 90% at 50% 50%, transparent 50%, rgba(0,0,0,0.65) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene_C4_S1;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C4_S2',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C4_S2: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan left to right
  const camX = interpolate(frame, [0, durationInFrames], [-18, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Tax bar fill animation
  const taxBarW = interpolate(frame, [30, 90], [0, 680], {
    easing: Easing.out(Easing.expo),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Euro flow particles
  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i * 5) * 1920,
    y: seededRandom(i * 11) * 1080,
    size: seededRandom(i * 7) * 3 + 1,
    speed: seededRandom(i * 19) * 0.5 + 0.15,
    op: seededRandom(i * 13) * 0.12 + 0.03,
  }));

  // Stagger reveals
  const label1Op = interpolate(frame, [15, 35], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const bar1Op = interpolate(frame, [25, 50], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const euroOp = interpolate(frame, [60, 85], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const calloutOp = interpolate(frame, [80, 105], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const calloutY = interpolate(frame, [80, 110], [30, 0], {
    easing: Easing.out(Easing.back(1.4)),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === GRADIENT BACKGROUND === */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'linear-gradient(160deg, #1a1000 0%, #1C1C1C 40%, #0d0d0d 100%)',
        }} />
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse 60% 50% at 70% 40%, rgba(233,215,0,0.08) 0%, transparent 70%)',
        }} />
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      {/* Large rectangle slab */}
      <div style={{
        position: 'absolute',
        top: 180, left: -80,
        width: 900, height: 600,
        border: '1px solid rgba(233,215,0,0.04)',
        borderRadius: 8,
        transform: `rotate(${-2 + Math.sin(frame * 0.015) * 1}deg)`,
        opacity: 0.5,
      }} />
      {/* Diagonal stripe */}
      <div style={{
        position: 'absolute',
        top: 0, right: 300,
        width: 2, height: 1080,
        background: 'linear-gradient(180deg, transparent, rgba(233,215,0,0.06), transparent)',
        transform: `rotate(12deg)`,
      }} />
      {/* Circle bottom-right */}
      <div style={{
        position: 'absolute',
        bottom: -180, right: -100,
        width: 500, height: 500,
        borderRadius: '50%',
        border: '1px solid rgba(233,215,0,0.05)',
        transform: `rotate(${frame * 0.04}deg)`,
      }} />

      {/* === MOTION TEXTURE: DIAGONAL LINES === */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.02 }} width="1920" height="1080">
        {Array.from({ length: 30 }, (_, i) => (
          <line key={i}
            x1={i * 70 - 200} y1={0}
            x2={i * 70 + 800} y2={1080}
            stroke="#e9d700" strokeWidth="0.8"
          />
        ))}
      </svg>

      {/* === FLOATING PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x + Math.sin(frame * p.speed + i) * 15,
          top: p.y - frame * p.speed * 0.3,
          width: p.size * 2, height: p.size * 2,
          borderRadius: '50%',
          backgroundColor: '#e9d700',
          opacity: p.op,
        }} />
      ))}

      {/* === MAIN CONTENT === */}

      {/* LEFT COLUMN: label */}
      <div style={{
        position: 'absolute',
        top: 90, left: 100,
        opacity: label1Op,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 18, color: '#e9d700',
          letterSpacing: 5, marginBottom: 12,
          textTransform: 'uppercase',
        }}>Einkommensart → Steuerlast</div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 100, color: '#F5F5F5',
          lineHeight: 1, textTransform: 'uppercase',
        }}>
          ANGESTELLTER
        </div>
        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: 40, color: '#9E9E9E',
          marginTop: 8,
        }}>zahlt Lohnsteuer — direkt vom ersten Euro</div>
      </div>

      {/* TAX BAR VISUALIZATION */}
      <div style={{
        position: 'absolute',
        top: 340, left: 100,
        opacity: bar1Op,
      }}>
        {/* Full bar label */}
        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: 28, color: '#9E9E9E',
          marginBottom: 10, letterSpacing: 1,
        }}>
          Bruttogehalt → 100%
        </div>
        {/* Bar track */}
        <div style={{
          width: 900, height: 60,
          backgroundColor: 'rgba(255,255,255,0.05)',
          borderRadius: 8, overflow: 'hidden',
          border: '1px solid rgba(255,255,255,0.08)',
        }}>
          {/* Tax portion - RED */}
          <div style={{
            width: taxBarW,
            height: '100%',
            background: 'linear-gradient(90deg, #ff0000, #cc0000)',
            borderRadius: '8px 0 0 8px',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            overflow: 'hidden',
          }}>
            <span style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 30, color: '#F5F5F5',
              opacity: taxBarW > 200 ? 1 : 0,
              whiteSpace: 'nowrap',
            }}>STEUERN ~42%</span>
          </div>
        </div>

        {/* Second bar — net */}
        <div style={{ marginTop: 16 }}>
          <div style={{
            fontFamily: 'Open Sans, sans-serif',
            fontSize: 28, color: '#9E9E9E',
            marginBottom: 10,
          }}>Netto → bleibt übrig</div>
          <div style={{
            width: 900, height: 60,
            backgroundColor: 'rgba(255,255,255,0.05)',
            borderRadius: 8, overflow: 'hidden',
            border: '1px solid rgba(255,255,255,0.08)',
          }}>
            <div style={{
              width: interpolate(taxBarW, [0, 680], [0, 220], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              height: '100%',
              background: 'linear-gradient(90deg, #e9d700, #FFC107)',
              borderRadius: '8px 0 0 8px',
            }} />
          </div>
        </div>

        {/* Sozialversicherung rows */}
        {['Krankenversicherung', 'Rentenversicherung', 'Pflegeversicherung'].map((item, idx) => {
          const w = interpolate(frame, [50 + idx * 15, 85 + idx * 15], [0, [180, 160, 100][idx]], {
            easing: Easing.out(Easing.cubic),
            extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
          });
          return (
            <div key={item} style={{ marginTop: 10, display: 'flex', alignItems: 'center', gap: 14 }}>
              <div style={{
                fontFamily: 'JetBrains Mono', fontSize: 20,
                color: '#9E9E9E', width: 280,
              }}>{item}</div>
              <div style={{
                width: 400, height: 22,
                backgroundColor: 'rgba(255,255,255,0.04)',
                borderRadius: 4, overflow: 'hidden',
              }}>
                <div style={{
                  width: w, height: '100%',
                  backgroundColor: 'rgba(255,0,0,0.5)',
                  borderRadius: 4,
                }} />
              </div>
            </div>
          );
        })}
      </div>

      {/* CALLOUT BOX */}
      <div style={{
        position: 'absolute',
        top: 200, right: 80,
        width: 420,
        opacity: calloutOp,
        transform: `translateY(${calloutY}px)`,
        backgroundColor: 'rgba(255,0,0,0.08)',
        border: '1.5px solid rgba(255,0,0,0.35)',
        borderRadius: 12,
        padding: '32px 36px',
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 110, color: '#ff0000',
          lineHeight: 1,
          textAlign: 'center',
          textShadow: '0 0 40px rgba(255,0,0,0.5)',
        }}>~42%</div>
        <div style={{
          fontFamily: 'Open Sans, sans-serif',
          fontSize: 26, color: '#9E9E9E',
          textAlign: 'center', marginTop: 8,
          lineHeight: 1.4,
        }}>weg — bevor du<br />einen Cent siehst</div>
        <div style={{
          marginTop: 20,
          fontFamily: 'JetBrains Mono', fontSize: 18,
          color: '#ff0000', textAlign: 'center',
          letterSpacing: 2, opacity: 0.8,
        }}>HÖCHSTE STEUERKLASSE</div>
      </div>

      {/* EURO FLOW — animated euros falling */}
      <div style={{ position: 'absolute', top: 0, right: 560, opacity: euroOp }}>
        {Array.from({ length: 6 }, (_, i) => {
          const yPos = ((frame * (1.8 + i * 0.3) + i * 160) % 1080);
          return (
            <div key={i} style={{
              position: 'absolute',
              top: yPos, left: i * 22,
              fontFamily: 'Anton', fontSize: 22,
              color: '#e9d700',
              opacity: 0.12 + i * 0.02,
            }}>€</div>
          );
        })}
      </div>

      {/* === STICK FIGURE === */}
      <div style={{
        position: 'absolute',
        right: 60, bottom: 60,
        opacity: sp(50),
        transform: `translateY(${(1 - sp(50)) * 80 + breathe}px)`,
      }}>
        <svg width="100" height="185" viewBox="0 0 100 185">
          <ellipse cx="50" cy="178" rx="32" ry="7" fill="rgba(255,193,7,0.12)" />
          <line x1="50" y1="80" x2="50" y2="138" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          <line x1="50" y1="98" x2="20" y2="118" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="98" x2="80" y2="118" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="138" x2="28" y2="172" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="138" x2="72" y2="172" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <g transform={`rotate(${headNod}, 50, 48)`}>
            <circle cx="50" cy="48" r="24" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <circle cx="40" cy="47" r="8" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <circle cx="60" cy="47" r="8" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="48" y1="47" x2="52" y2="47" stroke="#FFC107" strokeWidth="2" />
            {/* Sad face */}
            <path d="M41,62 Q50,57 59,62" stroke="#F5F5F5" strokeWidth="2" fill="none" strokeLinecap="round" />
          </g>
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse 85% 85% at 50% 50%, transparent 45%, rgba(0,0,0,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene_C4_S2;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C4_S3',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C4_S3: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  // Camera slow zoom out
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const deductions = [
    { label: 'Fahrtkosten', icon: '🚗', amount: '−€ 1.440/Jahr', delay: 25 },
    { label: 'Arbeitsmittel', icon: '💻', amount: '−€ 2.200/Jahr', delay: 45 },
    { label: 'Büroraum', icon: '🏢', amount: '−€ 3.600/Jahr', delay: 65 },
    { label: 'Fortbildungen', icon: '📚', amount: '−€ 900/Jahr', delay: 85 },
  ];

  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 9) * 1920,
    y: seededRandom(i * 3) * 1080,
    r: seededRandom(i * 7) * 2.5 + 0.8,
    speed: seededRandom(i * 13) * 0.4 + 0.1,
    op: seededRandom(i * 17) * 0.1 + 0.03,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === GRADIENT BACKGROUND === */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse 75% 70% at 30% 60%, #001a0a 0%, #1C1C1C 55%, #0d0a00 100%)',
        }} />
        <div style={{
          position: 'absolute', inset: 0,
          background: 'linear-gradient(110deg, rgba(233,215,0,0.06) 0%, transparent 45%)',
        }} />
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      {/* Hexagon-ish shape */}
      <svg style={{ position: 'absolute', top: -100, right: 200, opacity: 0.05 }} width="500" height="500">
        <polygon
          points="250,20 460,130 460,370 250,480 40,370 40,130"
          fill="none" stroke="#e9d700" strokeWidth="1.5"
          transform={`rotate(${frame * 0.02}, 250, 250)`}
        />
      </svg>
      {/* Horizontal divider */}
      <div style={{
        position: 'absolute',
        top: '50%', left: 0, right: 0,
        height: 1,
        background: 'linear-gradient(90deg, transparent, rgba(233,215,0,0.05), transparent)',
      }} />
      {/* Bottom rectangle */}
      <div style={{
        position: 'absolute',
        bottom: -80, left: 100,
        width: 800, height: 300,
        border: '1px solid rgba(233,215,0,0.04)',
        borderRadius: 6,
        transform: `rotate(${-1 + Math.sin(frame * 0.01) * 0.5}deg)`,
      }} />

      {/* === MOTION TEXTURE: DOTS GRID === */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.03 }} width="1920" height="1080">
        {Array.from({ length: 20 }, (_, row) =>
          Array.from({ length: 32 }, (_, col) => (
            <circle key={`${row}-${col}`} cx={col * 64} cy={row * 58} r="1.5" fill="#e9d700" />
          ))
        )}
      </svg>

      {/* === PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x + Math.cos(frame * p.speed + i) * 10,
          top: p.y + Math.sin(frame * p.speed * 0.8 + i) * 8,
          width: p.r * 2, height: p.r * 2,
          borderRadius: '50%',
          backgroundColor: '#e9d700',
          opacity: p.op,
        }} />
      ))}

      {/* === MAIN CONTENT === */}

      {/* TITLE AREA */}
      <div style={{
        position: 'absolute',
        top: 70, left: 100,
        opacity: sp(5),
        transform: `translateY(${(1 - sp(5)) * 30}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 18,
          color: '#e9d700', letterSpacing: 5,
          marginBottom: 10, textTransform: 'uppercase',
        }}>Selbstständiger → Spielraum</div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 90, color: '#F5F5F5',
          lineHeight: 0.95, textTransform: 'uppercase',
        }}>
          BETRIEBSAUSGABEN
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 130, color: '#e9d700',
          lineHeight: 0.9, textTransform: 'uppercase',
          textShadow: '0 0 50px rgba(233,215,0,0.3)',
        }}>ABSETZEN</div>
      </div>

      {/* DEDUCTION CARDS */}
      <div style={{
        position: 'absolute',
        top: 340, left: 100,
        display: 'flex', flexDirection: 'column', gap: 18,
      }}>
        {deductions.map((item, idx) => {
          const cardOp = interpolate(frame, [item.delay, item.delay + 20], [0, 1], {
            extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
          });
          const cardX = interpolate(frame, [item.delay, item.delay + 25], [-60, 0], {
            easing: Easing.out(Easing.back(1.2)),
            extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
          });
          return (
            <div key={item.label} style={{
              display: 'flex', alignItems: 'center', gap: 22,
              opacity: cardOp,
              transform: `translateX(${cardX}px)`,
            }}>
              {/* Check icon */}
              <div style={{
                width: 44, height: 44,
                borderRadius: '50%',
                backgroundColor: 'rgba(233,215,0,0.15)',
                border: '1.5px solid rgba(233,215,0,0.5)',
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                flexShrink: 0,
              }}>
                <svg width="20" height="20" viewBox="0 0 20 20">
                  <polyline points="3,10 8,15 17,5" stroke="#e9d700" strokeWidth="2.5" fill="none" strokeLinecap="round" />
                </svg>
              </div>
              {/* Label */}
              <div style={{
                fontFamily: 'Open Sans, sans-serif',
                fontSize: 38, color: '#F5F5F5',
                fontWeight: 700,
                minWidth: 320,
              }}>{item.label}</div>
              {/* Amount */}
              <div style={{
                fontFamily: 'JetBrains Mono', fontSize: 32,
                color: '#e9d700',
                backgroundColor: 'rgba(233,215,0,0.07)',
                padding: '6px 20px', borderRadius: 6,
                border: '1px solid rgba(233,215,0,0.2)',
              }}>{item.amount}</div>
            </div>
          );
        })}
      </div>

      {/* RIGHT SIDE: Spielraum meter */}
      <div style={{
        position: 'absolute',
        top: 180, right: 120,
        width: 380,
        opacity: interpolate(frame, [70, 95], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        transform: `translateY(${interpolate(frame, [70, 100], [30, 0], {
          easing: Easing.out(Easing.cubic),
          extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
        })}px)`,
      }}>
        <div style={{
          backgroundColor: 'rgba(233,215,0,0.07)',
          border: '1.5px solid rgba(233,215,0,0.3)',
          borderRadius: 16,
          padding: '36px 40px',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 18,
            color: '#9E9E9E', letterSpacing: 3,
            marginBottom: 14, textTransform: 'uppercase',
          }}>Steuerersparnis</div>
          <div style={{
            fontFamily: 'Anton', fontSize: 130,
            color: '#e9d700',
            lineHeight: 1,
            textShadow: '0 0 40px rgba(233,215,0,0.4)',
          }}>
            {Math.round(interpolate(frame, [70, 130], [0, 8140], {
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
            })).toLocaleString('de-DE')}
          </div>
          <div style={{
            fontFamily: 'Open Sans', fontSize: 32,
            color: '#e9d700', marginTop: 4,
          }}>€ / Jahr</div>
          <div style={{
            marginTop: 20,
            fontFamily: 'Open Sans', fontSize: 24,
            color: '#9E9E9E', lineHeight: 1.4,
          }}>durch Abzüge legal<br />gespart</div>
        </div>

        {/* VS Badge */}
        <div style={{
          marginTop: 24,
          backgroundColor: 'rgba(255,0,0,0.08)',
          border: '1px solid rgba(255,0,0,0.25)',
          borderRadius: 8,
          padding: '16px 20px',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Open Sans', fontSize: 22,
            color: '#ff0000',
          }}>Angestellter: kein dieser Abzüge möglich</div>
        </div>
      </div>

      {/* === STICK FIGURE === */}
      <div style={{
        position: 'absolute',
        left: 1480, bottom: 70,
        opacity: sp(55),
        transform: `translateY(${(1 - sp(55)) * 80 + breathe}px)`,
      }}>
        <svg width="105" height="190" viewBox="0 0 105 190">
          <ellipse cx="52" cy="182" rx="34" ry="7" fill="rgba(255,193,7,0.12)" />
          <line x1="52" y1="80" x2="52" y2="142" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          {/* Arms up — happy */}
          <line x1="52" y1="100" x2="18" y2="76" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="52" y1="100" x2="86" y2="76" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="52" y1="142" x2="28" y2="176" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="52" y1="142" x2="76" y2="176" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <g transform={`rotate(${headNod}, 52, 50)`}>
            <circle cx="52" cy="50" r="25" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <circle cx="42" cy="49" r="8.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <circle cx="62" cy="49" r="8.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="50" y1="49" x2="54" y2="49" stroke="#FFC107" strokeWidth="2" />
            {/* Smile */}
            <path d="M40,64 Q52,72 64,64" stroke="#e9d700" strokeWidth="2" fill="none" strokeLinecap="round" />
          </g>
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse 88% 88% at 50% 50%, transparent 48%, rgba(0,0,0,0.65) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene_C4_S3;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C4_S4',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C4_S4: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  // Camera orbit effect
  const camY = interpolate(frame, [0, durationInFrames], [10, -10], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 25 }, (_, i) => ({
    x: seededRandom(i * 11) * 1920,
    y: seededRandom(i * 7) * 1080,
    r: seededRandom(i * 5) * 3 + 0.8,
    speed: seededRandom(i * 3) * 0.3 + 0.1,
    op: seededRandom(i * 17) * 0.15 + 0.03,
    color: i % 4 === 0 ? '#FFC107' : '#e9d700',
  }));

  // Comparison columns
  const col1H = interpolate(frame, [20, 70], [0, 1], {
    easing: Easing.out(Easing.expo),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const col2H = interpolate(frame, [40, 90], [0, 1], {
    easing: Easing.out(Easing.expo),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const col3H = interpolate(frame, [60, 110], [0, 1], {
    easing: Easing.out(Easing.expo),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === GRADIENT BG === */}
      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse 70% 60% at 50% 30%, #0d1a00 0%, #1C1C1C 50%, #000d1a 100%)',
        }} />
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      {/* Large background ring */}
      <div style={{
        position: 'absolute',
        top: '50%', left: '50%',
        width: 900, height: 900,
        borderRadius: '50%',
        border: '1px solid rgba(233,215,0,0.04)',
        transform: `translate(-50%, -50%) rotate(${frame * 0.05}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        top: '50%', left: '50%',
        width: 650, height: 650,
        borderRadius: '50%',
        border: '1px solid rgba(233,215,0,0.03)',
        transform: `translate(-50%, -50%) rotate(${-frame * 0.03}deg)`,
      }} />
      {/* Corner accent */}
      <div style={{
        position: 'absolute', top: 0, left: 0,
        width: 300, height: 300,
        borderBottom: '1px solid rgba(233,215,0,0.05)',
        borderRight: '1px solid rgba(233,215,0,0.05)',
        borderRadius: '0 0 100% 0',
        opacity: 0.6,
      }} />

      {/* === TEXTURE: HORIZONTAL SCAN LINES === */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.015 }} width="1920" height="1080">
        {Array.from({ length: 54 }, (_, i) => (
          <line key={i} x1={0} y1={i * 20} x2={1920} y2={i * 20} stroke="#F5F5F5" strokeWidth="0.5" />
        ))}
      </svg>

      {/* === PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x + Math.sin(frame * p.speed + i) * 14,
          top: p.y + Math.cos(frame * p.speed * 0.6 + i) * 10,
          width: p.r * 2, height: p.r * 2,
          borderRadius: '50%',
          backgroundColor: p.color,
          opacity: p.op,
        }} />
      ))}

      {/* === MAIN CONTENT === */}

      {/* HEADLINE */}
      <div style={{
        position: 'absolute', top: 65, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(5),
        transform: `translateY(${(1 - sp(5)) * 25}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 18,
          color: '#e9d700', letterSpacing: 6,
          textTransform: 'uppercase', marginBottom: 10,
        }}>Steuervergleich — 3 Einkommenstypen</div>
        <div style={{
          fontFamily: 'Anton', fontSize: 100,
          color: '#F5F5F5', lineHeight: 1,
        }}>WER ZAHLT WAS?</div>
      </div>

      {/* THREE COLUMN COMPARISON */}
      <div style={{
        position: 'absolute',
        top: 240, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', gap: 50,
        alignItems: 'flex-end',
        padding: '0 80px',
      }}>

        {/* Column 1: Angestellter */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', width: 340 }}>
          <div style={{
            fontFamily: 'Anton', fontSize: 90, color: '#ff0000',
            textShadow: '0 0 30px rgba(255,0,0,0.4)',
            opacity: col1H,
          }}>~42%</div>
          <div style={{
            width: '100%',
            height: 420 * col1H,
            background: 'linear-gradient(180deg, #ff0000 0%, #8b0000 100%)',
            borderRadius: '10px 10px 0 0',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            transition: 'height 0.1s',
          }}>
            {col1H > 0.3 && (
              <div style={{
                fontFamily: 'JetBrains Mono', fontSize: 20,
                color: 'rgba(255,255,255,0.6)',
                transform: 'rotate(-90deg)',
                whiteSpace: 'nowrap',
              }}>Lohnsteuer + Sozialabgaben</div>
            )}
          </div>
          <div style={{
            width: '100%', height: 2,
            backgroundColor: 'rgba(255,255,255,0.1)',
          }} />
          <div style={{
            marginTop: 16, textAlign: 'center',
            fontFamily: 'Anton', fontSize: 38, color: '#ff0000',
          }}>ANGESTELLTER</div>
          <div style={{
            fontFamily: 'Open Sans', fontSize: 24, color: '#9E9E9E',
            textAlign: 'center', marginTop: 6,
          }}>Lohnsteuer + alle<br />Sozialversicherungen</div>
        </div>

        {/* Column 2: Selbstständiger */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', width: 340 }}>
          <div style={{
            fontFamily: 'Anton', fontSize: 90, color: '#FFC107',
            textShadow: '0 0 30px rgba(255,193,7,0.4)',
            opacity: col2H,
          }}>~28%</div>
          <div style={{
            width: '100%',
            height: 280 * col2H,
            background: 'linear-gradient(180deg, #FFC107 0%, #9a7000 100%)',
            borderRadius: '10px 10px 0 0',
          }} />
          <div style={{ width: '100%', height: 2, backgroundColor: 'rgba(255,255,255,0.1)' }} />
          <div style={{
            marginTop: 16, textAlign: 'center',
            fontFamily: 'Anton', fontSize: 38, color: '#FFC107',
          }}>SELBSTSTÄNDIG</div>
          <div style={{
            fontFamily: 'Open Sans', fontSize: 24, color: '#9E9E9E',
            textAlign: 'center', marginTop: 6,
          }}>nach Betriebsausgaben<br />& Abzügen</div>
        </div>

        {/* Column 3: Kapitaleinkommen */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', width: 340 }}>
          <div style={{
            fontFamily: 'Anton', fontSize: 90, color: '#e9d700',
            textShadow: '0 0 30px rgba(233,215,0,0.5)',
            opacity: col3H,
          }}>~25%</div>
          <div style={{
            width: '100%',
            height: 200 * col3H,
            background: 'linear-gradient(180deg, #e9d700 0%, #7a6d00 100%)',
            borderRadius: '10px 10px 0 0',
          }} />
          <div style={{ width: '100%', height: 2, backgroundColor: 'rgba(255,255,255,0.1)' }} />
          <div style={{
            marginTop: 16, textAlign: 'center',
            fontFamily: 'Anton', fontSize: 38, color: '#e9d700',
          }}>KAPITALEINKOMMEN</div>
          <div style={{
            fontFamily: 'Open Sans', fontSize: 24, color: '#9E9E9E',
            textAlign: 'center', marginTop: 6,
          }}>Dividenden, Kursgewinne<br />keine Sozialversicherung</div>
        </div>

      </div>

      {/* BOTTOM CALLOUT */}
      <div style={{
        position: 'absolute',
        bottom: 60, left: 0, right: 0,
        textAlign: 'center',
        opacity: interpolate(frame, [95, 120], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: 'rgba(233,215,0,0.08)',
          border: '1px solid rgba(233,215,0,0.3)',
          borderRadius: 8,
          padding: '14px 40px',
        }}>
          <span style={{
            fontFamily: 'Open Sans', fontSize: 30,
            color: '#e9d700', fontWeight: 700,
          }}>
            Abgeltungsteuer 25% + Soli — kein Sozialversicherungsabzug
          </span>
        </div>
      </div>

      {/* === STICK FIGURE === */}
      <div style={{
        position: 'absolute',
        right: 50, top: 140,
        opacity: sp(50),
        transform: `translateY(${(1 - sp(50)) * 60 + breathe}px)`,
      }}>
        <svg width="95" height="175" viewBox="0 0 95 175">
          <ellipse cx="47" cy="168" rx="30" ry="6" fill="rgba(255,193,7,0.1)" />
          <line x1="47" y1="74" x2="47" y2="130" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          <line x1="47" y1="92" x2="16" y2="110" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="47" y1="92" x2="78" y2="110" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="47" y1="130" x2="26" y2="162" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="47" y1="130" x2="68" y2="162" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <g transform={`rotate(${headNod}, 47, 44)`}>
            <circle cx="47" cy="44" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <circle cx="38" cy="43" r="7.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <circle cx="56" cy="43" r="7.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="45.5" y1="43" x2="48.5" y2="43" stroke="#FFC107" strokeWidth="2" />
            <path d="M37,57 Q47,64 57,57" stroke="#e9d700" strokeWidth="2" fill="none" strokeLinecap="round" />
          </g>
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse 90% 90% at 50% 50%, transparent 42%, rgba(0,0,0,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene_C4_S4;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C4_S5',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C4_S5: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  // Tax counter counting down
  const taxPercent = interpolate(frame, [40, 130], [25, 7], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 7) * 1920,
    y: seededRandom(i * 11) * 1080,
    r: seededRandom(i * 13) * 2.5 + 0.8,
    speed: seededRandom(i * 3) * 0.35 + 0.1,
    op: seededRandom(i * 19) * 0.12 + 0.03,
  }));

  const strategies = [
    { label: 'Verluste gegenrechnen', tag: 'Verlustverrechnung', delay: 20 },
    { label: 'Sparerpauschbetrag nutzen', tag: '€1.000 / Jahr steuerfrei', delay: 42 },
    { label: '10-Jahresfrist Immobilien', tag: '100% steuerfrei', delay: 64 },
    { label: 'Freibeträge richtig einsetzen', tag: 'Legal & effektiv', delay: 86 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === GRADIENT BG === */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse 80% 70% at 80% 20%, #001200 0%, #1C1C1C 50%, #0a0800 100%)',
      }} />
      <div style={{
        position: 'absolute', inset: 0,
        background: 'linear-gradient(200deg, rgba(233,215,0,0.07) 0%, transparent 40%, rgba(0,255,80,0.03) 100%)',
      }} />

      {/* === SHAPE LAYERS === */}
      {/* Large tilted rectangle */}
      <div style={{
        position: 'absolute',
        top: 80, left: -120,
        width: 780, height: 900,
        border: '1px solid rgba(233,215,0,0.04)',
        borderRadius: 12,
        transform: `rotate(${-8 + Math.sin(frame * 0.015) * 1}deg)`,
      }} />
      {/* Top-right arc */}
      <svg style={{ position: 'absolute', top: -60, right: -80, opacity: 0.05 }} width="400" height="400">
        <circle cx="200" cy="200" r="180" fill="none" stroke="#e9d700" strokeWidth="1"
          transform={`rotate(${frame * 0.04}, 200, 200)`} />
      </svg>

      {/* === TEXTURE: CIRCUIT LINES === */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.025 }} width="1920" height="1080">
        {Array.from({ length: 12 }, (_, i) => (
          <g key={i}>
            <line x1={100 + i * 150} y1={0} x2={100 + i * 150} y2={300} stroke="#e9d700" strokeWidth="0.5" />
            <line x1={100 + i * 150} y1={300} x2={250 + i * 150} y2={300} stroke="#e9d700" strokeWidth="0.5" />
            <circle cx={100 + i * 150} cy={300} r={3} fill="#e9d700" />
          </g>
        ))}
      </svg>

      {/* === PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x + Math.sin(frame * p.speed + i) * 12,
          top: p.y + Math.cos(frame * p.speed * 0.7 + i) * 9,
          width: p.r * 2, height: p.r * 2,
          borderRadius: '50%',
          backgroundColor: '#e9d700',
          opacity: p.op,
        }} />
      ))}

      {/* === MAIN CONTENT === */}

      {/* LEFT: Strategy list */}
      <div style={{
        position: 'absolute',
        top: 80, left: 100,
        opacity: sp(5),
        transform: `translateY(${(1 - sp(5)) * 20}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 18,
          color: '#e9d700', letterSpacing: 5,
          textTransform: 'uppercase', marginBottom: 16,
        }}>Optimierung → Noch weniger zahlen</div>
        <div style={{
          fontFamily: 'Anton', fontSize: 105,
          color: '#F5F5F5', lineHeight: 0.95,
          textTransform: 'uppercase',
        }}>INTELLIGENTE</div>
        <div style={{
          fontFamily: 'Anton', fontSize: 105,
          color: '#e9d700', lineHeight: 0.95,
          textTransform: 'uppercase',
          textShadow: '0 0 40px rgba(233,215,0,0.3)',
        }}>STRUKTUR</div>
      </div>

      {/* Strategy cards */}
      <div style={{
        position: 'absolute',
        top: 340, left: 100,
        display: 'flex', flexDirection: 'column', gap: 20,
      }}>
        {strategies.map((s) => {
          const op = interpolate(frame, [s.delay, s.delay + 18], [0, 1], {
            extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
          });
          const xSlide = interpolate(frame, [s.delay, s.delay + 22], [-50, 0], {
            easing: Easing.out(Easing.back(1.3)),
            extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
          });
          return (
            <div key={s.label} style={{
              display: 'flex', alignItems: 'center', gap: 20,
              opacity: op, transform: `translateX(${xSlide}px)`,
            }}>
              {/* Arrow icon */}
              <svg width="32" height="32" viewBox="0 0 32 32">
                <path d="M4,16 L26,16 M18,8 L26,16 L18,24"
                  stroke="#e9d700" strokeWidth="2.5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
              <div style={{
                fontFamily: 'Open Sans', fontSize: 36,
                color: '#F5F5F5', fontWeight: 700,
                minWidth: 380,
              }}>{s.label}</div>
              <div style={{
                fontFamily: 'JetBrains Mono', fontSize: 26,
                color: '#e9d700',
                backgroundColor: 'rgba(233,215,0,0.08)',
                border: '1px solid rgba(233,215,0,0.25)',
                padding: '6px 18px', borderRadius: 6,
              }}>{s.tag}</div>
            </div>
          );
        })}
      </div>

      {/* RIGHT: Tax dropping counter */}
      <div style={{
        position: 'absolute',
        top: 200, right: 100,
        width: 500,
        opacity: interpolate(frame, [30, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 20,
          color: '#9E9E9E', letterSpacing: 3,
          marginBottom: 16, textTransform: 'uppercase',
        }}>Effektive Steuerlast</div>

        {/* Big counter */}
        <div style={{
          position: 'relative',
          backgroundColor: 'rgba(0,0,0,0.4)',
          border: '2px solid rgba(233,215,0,0.3)',
          borderRadius: 20,
          padding: '40px 60px',
        }}>
          <div style={{
            fontFamily: 'Anton', fontSize: 180,
            color: '#e9d700', lineHeight: 1,
            textShadow: '0 0 60px rgba(233,215,0,0.5)',
          }}>
            {Math.round(taxPercent)}%
          </div>
          <div style={{
            fontFamily: 'Open Sans', fontSize: 28,
            color: '#9E9E9E', marginTop: 10,
          }}>mit richtiger Strukturierung</div>

          {/* Arrow down */}
          <div style={{
            position: 'absolute', top: 20, right: 20,
          }}>
            <svg width="40" height="40" viewBox="0 0 40 40">
              <path d="M20,4 L20,32 M10,22 L20,32 L30,22"
                stroke="#e9d700" strokeWidth="3" fill="none" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </div>
        </div>

        {/* vs badge */}
        <div style={{
          marginTop: 24,
          display: 'flex', justifyContent: 'center', gap: 16, alignItems: 'center',
          opacity: interpolate(frame, [80, 105], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            backgroundColor: 'rgba(255,0,0,0.1)',
            border: '1px solid rgba(255,0,0,0.3)',
            borderRadius: 8, padding: '12px 24px',
          }}>
            <span style={{ fontFamily: 'Anton', fontSize: 36, color: '#ff0000' }}>~42%</span>
            <div style={{ fontFamily: 'Open Sans', fontSize: 18, color: '#9E9E9E' }}>Angestellter</div>
          </div>
          <div style={{
            fontFamily: 'Anton', fontSize: 40, color: '#9E9E9E',
          }}>→</div>
          <div style={{
            backgroundColor: 'rgba(233,215,0,0.1)',
            border: '1px solid rgba(233,215,0,0.3)',
            borderRadius: 8, padding: '12px 24px',
          }}>
            <span style={{ fontFamily: 'Anton', fontSize: 36, color: '#e9d700' }}>~7%</span>
            <div style={{ fontFamily: 'Open Sans', fontSize: 18, color: '#9E9E9E' }}>optimiert</div>
          </div>
        </div>
      </div>

      {/* === STICK FIGURE === */}
      <div style={{
        position: 'absolute',
        right: 80, bottom: 60,
        opacity: sp(55),
        transform: `translateY(${(1 - sp(55)) * 80 + breathe}px)`,
      }}>
        <svg width="100" height="185" viewBox="0 0 100 185">
          <ellipse cx="50" cy="178" rx="32" ry="7" fill="rgba(255,193,7,0.12)" />
          <line x1="50" y1="78" x2="50" y2="138" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          <line x1="50" y1="96" x2="18" y2="72" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="96" x2="82" y2="72" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="138" x2="28" y2="172" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="50" y1="138" x2="72" y2="172" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <g transform={`rotate(${headNod}, 50, 47)`}>
            <circle cx="50" cy="47" r="24" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <circle cx="40" cy="46" r="8" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <circle cx="60" cy="46" r="8" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="48" y1="46" x2="52" y2="46" stroke="#FFC107" strokeWidth="2" />
            {/* Big smile */}
            <path d="M38,62 Q50,72 62,62" stroke="#e9d700" strokeWidth="2.5" fill="none" strokeLinecap="round" />
            {/* Light bulb above head */}
          </g>
          {/* Light bulb */}
          <circle cx="50" cy="14" r="10" fill="none" stroke="#e9d700" strokeWidth="2"
            opacity={0.5 + Math.sin(frame * 0.1) * 0.3} />
          <line x1="46" y1="23" x2="54" y2="23" stroke="#e9d700" strokeWidth="2" opacity="0.6" />
          <line x1="47" y1="27" x2="53" y2="27" stroke="#e9d700" strokeWidth="1.5" opacity="0.4" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse 90% 88% at 50% 50%, transparent 45%, rgba(0,0,0,0.68) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene_C4_S5;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C4_S6',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C4_S6: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  // Scale tilt on the balance scale
  const scaleTilt = interpolate(frame, [40, 100], [0, 22], {
    easing: Easing.out(Easing.elastic(0.8)),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake at impact
  const shakeX = frame >= 95 && frame <= 108
    ? Math.sin(frame * 22) * (5 - (frame - 95) * 0.38)
    : 0;

  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i * 5) * 1920,
    y: seededRandom(i * 9) * 1080,
    r: seededRandom(i * 15) * 3 + 0.8,
    speed: seededRandom(i * 7) * 0.4 + 0.1,
    op: seededRandom(i * 3) * 0.12 + 0.03,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === GRADIENT BG === */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'linear-gradient(145deg, #0f0005 0%, #1C1C1C 45%, #000f00 100%)',
        transform: `translateX(${shakeX}px)`,
      }} />
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse 55% 55% at 50% 50%, rgba(255,0,0,0.04) 0%, transparent 70%)',
      }} />

      {/* === SHAPE LAYERS === */}
      {/* Center divider line */}
      <div style={{
        position: 'absolute',
        top: 0, bottom: 0, left: '50%',
        width: 1,
        background: 'linear-gradient(180deg, transparent, rgba(255,255,255,0.06) 30%, rgba(255,255,255,0.06) 70%, transparent)',
        transform: 'translateX(-50%)',
      }} />
      {/* Diamond shapes */}
      {[0, 1, 2].map(i => (
        <div key={i} style={{
          position: 'absolute',
          top: 100 + i * 300, left: 50,
          width: 30, height: 30,
          border: '1px solid rgba(255,0,0,0.08)',
          transform: `rotate(45deg) scale(${1 + Math.sin(frame * 0.03 + i) * 0.1})`,
        }} />
      ))}
      {[0, 1, 2].map(i => (
        <div key={i} style={{
          position: 'absolute',
          top: 100 + i * 300, right: 50,
          width: 30, height: 30,
          border: '1px solid rgba(233,215,0,0.08)',
          transform: `rotate(45deg) scale(${1 + Math.sin(frame * 0.03 + i + 1) * 0.1})`,
        }} />
      ))}

      {/* === TEXTURE: CROSS HATCH === */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.02 }} width="1920" height="1080">
        {Array.from({ length: 20 }, (_, i) => (
          <line key={`d1-${i}`} x1={i * 100} y1={0} x2={i * 100 + 1080} y2={1080} stroke="#F5F5F5" strokeWidth="0.5" />
        ))}
        {Array.from({ length: 20 }, (_, i) => (
          <line key={`d2-${i}`} x1={i * 100 + 1080} y1={0} x2={i * 100} y2={1080} stroke="#F5F5F5" strokeWidth="0.5" />
        ))}
      </svg>

      {/* === PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x + Math.sin(frame * p.speed + i) * 11,
          top: p.y + Math.cos(frame * p.speed * 0.7 + i) * 8,
          width: p.r * 2, height: p.r * 2,
          borderRadius: '50%',
          backgroundColor: i % 2 === 0 ? '#ff0000' : '#e9d700',
          opacity: p.op,
        }} />
      ))}

      {/* === BALANCE SCALE SVG === */}
      <div style={{
        position: 'absolute',
        top: 60, left: 0, right: 0,
        display: 'flex', justifyContent: 'center',
        opacity: interpolate(frame, [30, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <svg width="600" height="180" viewBox="0 0 600 180">
          {/* Pivot */}
          <circle cx="300" cy="30" r="10" fill="#9E9E9E" />
          <line x1="300" y1="30" x2="300" y2="80" stroke="#9E9E9E" strokeWidth="3" />
          {/* Beam tilted */}
          <line
            x1="80" y1={90 + scaleTilt}
            x2="520" y2={90 - scaleTilt}
            stroke="#9E9E9E" strokeWidth="3"
            style={{ transformOrigin: '300px 80px' }}
          />
          {/* Left pan - Time seller goes down */}
          <line x1="80" y1={90 + scaleTilt} x2="80" y2={120 + scaleTilt} stroke="#ff0000" strokeWidth="2" />
          <ellipse cx="80" cy={128 + scaleTilt} rx="55" ry="12" fill="rgba(255,0,0,0.15)" stroke="#ff0000" strokeWidth="1.5" />
          <text x="80" y={133 + scaleTilt} textAnchor="middle"
            fill="#ff0000" fontFamily="Anton" fontSize="11">ZEIT VERKÄUFER</text>
          {/* Right pan - Capital owner goes up */}
          <line x1="520" y1={90 - scaleTilt} x2="520" y2={120 - scaleTilt} stroke="#e9d700" strokeWidth="2" />
          <ellipse cx="520" cy={128 - scaleTilt} rx="55" ry="12" fill="rgba(233,215,0,0.15)" stroke="#e9d700" strokeWidth="1.5" />
          <text x="520" y={133 - scaleTilt} textAnchor="middle"
            fill="#e9d700" fontFamily="Anton" fontSize="11">KAPITAL EIGENTÜMER</text>
        </svg>
      </div>

      {/* === TWO COLUMNS === */}
      {/* LEFT: Bestraft */}
      <div style={{
        position: 'absolute',
        top: 260, left: 60,
        width: 820,
        opacity: sp(20),
        transform: `translateX(${(1 - sp(20)) * -60}px)`,
      }}>
        {/* Red bar top */}
        <div style={{ height: 6, backgroundColor: '#ff0000', borderRadius: 3, marginBottom: 28, width: '80%' }} />
        <div style={{
          fontFamily: 'Anton', fontSize: 68,
          color: '#ff0000', lineHeight: 1, textTransform: 'uppercase',
          textShadow: '0 0 30px rgba(255,0,0,0.3)',
        }}>BESTRAFT</div>
        <div style={{
          fontFamily: 'Anton', fontSize: 58,
          color: '#F5F5F5', lineHeight: 1, marginTop: 6,
        }}>Zeit verkaufen</div>

        <div style={{ marginTop: 28, display: 'flex', flexDirection: 'column', gap: 16 }}>
          {[
            'Lohnsteuer sofort, direkt, keine Wahl',
            'Sozialabgaben obendrauf',
            'Kein Spielraum, kein Abzug',
            'Körperzeit = Einkommensgrenze',
          ].map((item, idx) => {
            const iOp = interpolate(frame, [30 + idx * 14, 50 + idx * 14], [0, 1], {
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
            });
            return (
              <div key={item} style={{
                display: 'flex', alignItems: 'flex-start', gap: 14, opacity: iOp,
              }}>
                <svg width="22" height="22" viewBox="0 0 22 22" style={{ flexShrink: 0, marginTop: 4 }}>
                  <path d="M5,5 L17,17 M17,5 L5,17" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" />
                </svg>
                <span style={{ fontFamily: 'Open Sans', fontSize: 32, color: '#9E9E9E' }}>{item}</span>
              </div>
            );
          })}
        </div>
      </div>

      {/* RIGHT: Belohnt */}
      <div style={{
        position: 'absolute',
        top: 260, right: 60,
        width: 820,
        opacity: sp(35),
        transform: `translateX(${(1 - sp(35)) * 60}px)`,
        textAlign: 'right',
      }}>
        <div style={{
          height: 6, backgroundColor: '#e9d700', borderRadius: 3,
          marginBottom: 28, marginLeft: 'auto', width: '80%',
        }} />
        <div style={{
          fontFamily: 'Anton', fontSize: 68,
          color: '#e9d700', lineHeight: 1, textTransform: 'uppercase',
          textShadow: '0 0 30px rgba(233,215,0,0.3)',
        }}>BELOHNT</div>
        <div style={{
          fontFamily: 'Anton', fontSize: 58,
          color: '#F5F5F5', lineHeight: 1, marginTop: 6,
        }}>Kapital arbeiten lassen</div>

        <div style={{ marginTop: 28, display: 'flex', flexDirection: 'column', gap: 16, alignItems: 'flex-end' }}>
          {[
            'Abgeltungsteuer — pauschalisiert, niedrig',
            'Keine Sozialversicherungspflicht',
            'Verluste verrechenbar, Freibeträge nutzbar',
            'Einkommen auch im Schlaf',
          ].map((item, idx) => {
            const iOp = interpolate(frame, [45 + idx * 14, 65 + idx * 14], [0, 1], {
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
            });
            return (
              <div key={item} style={{
                display: 'flex', alignItems: 'flex-start',
                gap: 14, opacity: iOp, flexDirection: 'row-reverse',
              }}>
                <svg width="22" height="22" viewBox="0 0 22 22" style={{ flexShrink: 0, marginTop: 4 }}>
                  <polyline points="3,11 9,17 19,5" stroke="#e9d700" strokeWidth="2.5" fill="none" strokeLinecap="round" />
                </svg>
                <span style={{ fontFamily: 'Open Sans', fontSize: 32, color: '#9E9E9E' }}>{item}</span>
              </div>
            );
          })}
        </div>
      </div>

      {/* BOTTOM: System is not neutral */}
      <div style={{
        position: 'absolute',
        bottom: 55, left: 0, right: 0,
        textAlign: 'center',
        opacity: interpolate(frame, [100, 120], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: 'rgba(0,0,0,0.5)',
          border: '2px solid rgba(255,0,0,0.4)',
          borderRadius: 10, padding: '16px 50px',
        }}>
          <span style={{
            fontFamily: 'Anton', fontSize: 50,
            color: '#ff0000', letterSpacing: 4,
            textShadow: '0 0 20px rgba(255,0,0,0.4)',
          }}>DAS SYSTEM IST NICHT NEUTRAL</span>
        </div>
      </div>

      {/* === STICK FIGURE — center === */}
      <div style={{
        position: 'absolute',
        left: '50%', bottom: 55,
        transform: `translateX(-50%) translateY(${(1 - sp(60)) * 80 + breathe}px)`,
        opacity: sp(60),
      }}>
        <svg width="90" height="170" viewBox="0 0 90 170">
          <ellipse cx="45" cy="163" rx="28" ry="6" fill="rgba(255,193,7,0.1)" />
          <line x1="45" y1="70" x2="45" y2="125" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          <line x1="45" y1="88" x2="14" y2="105" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="88" x2="76" y2="105" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="125" x2="24" y2="158" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="125" x2="66" y2="158" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <g transform={`rotate(${headNod}, 45, 42)`}>
            <circle cx="45" cy="42" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <circle cx="36" cy="41" r="7.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <circle cx="54" cy="41" r="7.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="43.5" y1="41" x2="46.5" y2="41" stroke="#FFC107" strokeWidth="2" />
            {/* Thinking expression */}
            <path d="M38,55 Q45,59 52,55" stroke="#F5F5F5" strokeWidth="2" fill="none" strokeLinecap="round" />
          </g>
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse 92% 90% at 50% 50%, transparent 40%, rgba(0,0,0,0.72) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene_C4_S6;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C4_S7',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C4_S7: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  // Matrix rain effect for "code" aesthetic
  const matrixChars = Array.from({ length: 18 }, (_, i) => ({
    x: 60 + i * 100,
    speed: seededRandom(i * 7) * 0.8 + 0.3,
    offset: seededRandom(i * 13) * 300,
    op: seededRandom(i * 3) * 0.08 + 0.02,
    chars: ['0', '1', '%', '€', '§', '#', '&'],
  }));

  const particles = Array.from({ length: 20 }, (_, i) => ({
    x: seededRandom(i * 9) * 1920,
    y: seededRandom(i * 5) * 1080,
    r: seededRandom(i * 11) * 2.5 + 0.8,
    speed: seededRandom(i * 7) * 0.35 + 0.1,
    op: seededRandom(i * 3) * 0.1 + 0.03,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === GRADIENT BG === */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse 70% 60% at 50% 50%, #001408 0%, #1C1C1C 60%, #050014 100%)',
      }} />

      {/* === SHAPE LAYERS === */}
      {/* Code brackets aesthetics */}
      <svg style={{ position: 'absolute', top: 80, left: 60, opacity: 0.04 }} width="200" height="800">
        <path d="M150,0 L80,0 L80,800 L150,800" stroke="#e9d700" strokeWidth="2" fill="none" />
      </svg>
      <svg style={{ position: 'absolute', top: 80, right: 60, opacity: 0.04 }} width="200" height="800">
        <path d="M50,0 L120,0 L120,800 L50,800" stroke="#e9d700" strokeWidth="2" fill="none" />
      </svg>
      {/* Center hexagon */}
      <svg style={{ position: 'absolute', top: '50%', left: '50%', opacity: 0.03, transform: 'translate(-50%,-50%)' }}
        width="700" height="700">
        <polygon points="350,30 630,185 630,515 350,670 70,515 70,185"
          fill="none" stroke="#e9d700" strokeWidth="1.5"
          transform={`rotate(${frame * 0.03}, 350, 350)`} />
      </svg>

      {/* === MATRIX RAIN === */}
      {matrixChars.map((col, ci) => (
        Array.from({ length: 8 }, (_, ri) => {
          const yPos = ((frame * col.speed * 12 + col.offset + ri * 80) % 1180) - 80;
          const charIdx = Math.floor(frame * 0.2 + ri + ci) % col.chars.length;
          return (
            <div key={`${ci}-${ri}`} style={{
              position: 'absolute',
              left: col.x,
              top: yPos,
              fontFamily: 'JetBrains Mono',
              fontSize: 18, color: '#e9d700',
              opacity: col.op * (ri === 0 ? 2.5 : 1),
              pointerEvents: 'none',
            }}>
              {col.chars[charIdx]}
            </div>
          );
        })
      ))}

      {/* === TEXTURE: CIRCUIT BOARD === */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.018 }} width="1920" height="1080">
        {Array.from({ length: 8 }, (_, i) => (
          <g key={i}>
            <rect x={i * 250} y={100 + i * 40} width={120} height={60}
              fill="none" stroke="#e9d700" strokeWidth="0.5" />
            <line x1={i * 250 + 120} y1={130 + i * 40} x2={i * 250 + 220} y2={130 + i * 40}
              stroke="#e9d700" strokeWidth="0.5" />
            <circle cx={i * 250 + 60} cy={130 + i * 40} r={4} fill="none" stroke="#e9d700" strokeWidth="0.5" />
          </g>
        ))}
      </svg>

      {/* === PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x + Math.sin(frame * p.speed + i) * 10,
          top: p.y + Math.cos(frame * p.speed * 0.8 + i) * 8,
          width: p.r * 2, height: p.r * 2,
          borderRadius: '50%',
          backgroundColor: '#e9d700',
          opacity: p.op,
        }} />
      ))}

      {/* === MAIN CONTENT === */}

      {/* FIRST LINE: Nicht böse */}
      <div style={{
        position: 'absolute',
        top: 130, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(5),
        transform: `translateY(${(1 - sp(5)) * 30}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 22,
          color: '#9E9E9E', letterSpacing: 4,
          textTransform: 'uppercase',
        }}>
          {'// kein Verschwörung. kein Böse.'}
        </div>
      </div>

      {/* CODE BOX — THE CODE */}
      <div style={{
        position: 'absolute',
        top: 220, left: 0, right: 0,
        display: 'flex', justifyContent: 'center',
        opacity: interpolate(frame, [15, 40], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          backgroundColor: 'rgba(0,0,0,0.6)',
          border: '2px solid rgba(233,215,0,0.4)',
          borderRadius: 16,
          padding: '36px 60px',
          textAlign: 'center',
          backdropFilter: 'blur(4px)',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 20,
            color: '#9E9E9E', letterSpacing: 3, marginBottom: 16,
          }}>system.rules</div>
          <div style={{
            fontFamily: 'Anton', fontSize: 120,
            color: '#e9d700', lineHeight: 1,
            textShadow: '0 0 60px rgba(233,215,0,0.5)',
            letterSpacing: 8,
          }}>DER CODE</div>
          <div style={{
            fontFamily: 'Open Sans', fontSize: 32,
            color: '#9E9E9E', marginTop: 12,
          }}>
            Nachdem das Spiel gespielt wird
          </div>
        </div>
      </div>

      {/* TWO PATHS */}
      <div style={{
        position: 'absolute',
        top: 550, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', gap: 60,
        opacity: interpolate(frame, [55, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        {/* Wrong side */}
        <div style={{
          width: 420,
          backgroundColor: 'rgba(255,0,0,0.06)',
          border: '1px solid rgba(255,0,0,0.25)',
          borderRadius: 12,
          padding: '28px 36px',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 17,
            color: '#ff0000', letterSpacing: 3, marginBottom: 12,
            textTransform: 'uppercase',
          }}>Code unbekannt</div>
          <div style={{ fontFamily: 'Anton', fontSize: 50, color: '#ff0000', lineHeight: 1 }}>
            FALSCHE SEITE
          </div>
          <div style={{
            fontFamily: 'Open Sans', fontSize: 24, color: '#9E9E9E', marginTop: 10,
          }}>Zeit verkaufen,<br />System verliert nicht</div>
        </div>

        {/* Arrow */}
        <div style={{ display: 'flex', alignItems: 'center' }}>
          <svg width="80" height="40" viewBox="0 0 80 40">
            <path d="M4,20 L64,20 M52,8 L64,20 L52,32"
              stroke="#e9d700" strokeWidth="2.5" fill="none" strokeLinecap="round" />
          </svg>
        </div>

        {/* Right side */}
        <div style={{
          width: 420,
          backgroundColor: 'rgba(233,215,0,0.06)',
          border: '1px solid rgba(233,215,0,0.35)',
          borderRadius: 12,
          padding: '28px 36px',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 17,
            color: '#e9d700', letterSpacing: 3, marginBottom: 12,
            textTransform: 'uppercase',
          }}>Code gelernt</div>
          <div style={{ fontFamily: 'Anton', fontSize: 50, color: '#e9d700', lineHeight: 1 }}>
            RICHTIGE SEITE
          </div>
          <div style={{
            fontFamily: 'Open Sans', fontSize: 24, color: '#9E9E9E', marginTop: 10,
          }}>Kapital arbeiten lassen,<br />System für dich</div>
        </div>
      </div>

      {/* BOTTOM LINE */}
      <div style={{
        position: 'absolute',
        bottom: 55, left: 0, right: 0,
        textAlign: 'center',
        opacity: interpolate(frame, [85, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <span style={{
          fontFamily: 'JetBrains Mono', fontSize: 26,
          color: '#9E9E9E',
        }}>{'// Jetzt weißt du den Code'}</span>
      </div>

      {/* === STICK FIGURE === */}
      <div style={{
        position: 'absolute',
        right: 80, top: 220,
        opacity: sp(50),
        transform: `translateY(${(1 - sp(50)) * 60 + breathe}px)`,
      }}>
        <svg width="95" height="175" viewBox="0 0 95 175">
          <ellipse cx="47" cy="168" rx="30" ry="6" fill="rgba(255,193,7,0.1)" />
          <line x1="47" y1="72" x2="47" y2="132" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          <line x1="47" y1="92" x2="16" y2="108" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="47" y1="92" x2="78" y2="108" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="47" y1="132" x2="26" y2="165" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="47" y1="132" x2="68" y2="165" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <g transform={`rotate(${headNod}, 47, 44)`}>
            <circle cx="47" cy="44" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <circle cx="38" cy="43" r="7.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <circle cx="56" cy="43" r="7.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="45.5" y1="43" x2="48.5" y2="43" stroke="#FFC107" strokeWidth="2" />
            {/* Confident smile */}
            <path d="M38,58 Q47,66 56,58" stroke="#e9d700" strokeWidth="2.5" fill="none" strokeLinecap="round" />
          </g>
          {/* Key in hand */}
          <circle cx="86" cy="108" r="8" fill="none" stroke="#e9d700" strokeWidth="2" />
          <line x1="94" y1="108" x2="106" y2="108" stroke="#e9d700" strokeWidth="2" />
          <line x1="100" y1="104" x2="100" y2="108" stroke="#e9d700" strokeWidth="2" />
          <line x1="104" y1="104" x2="104" y2="108" stroke="#e9d700" strokeWidth="2" />
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse 88% 88% at 50% 50%, transparent 42%, rgba(0,0,0,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene_C4_S7;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C4_S8',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C4_S8: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  // Camera pan upward (price rising)
  const camY = interpolate(frame, [0, durationInFrames], [20, -20], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Price counter
  const priceUp = interpolate(frame, [35, 120], [0, 100], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Chart line
  const chartPoints = Array.from({ length: 20 }, (_, i) => {
    const progress = i / 19;
    const x = 60 + progress * 600;
    const baseY = 280;
    const rise = progress * progress * 220;
    const noise = Math.sin(i * 2.3) * 12;
    const animated = interpolate(frame, [30 + i * 4, 60 + i * 4], [baseY, baseY - rise + noise], {
      extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    });
    return { x, y: animated };
  });

  const chartPath = chartPoints.map((p, i) => `${i === 0 ? 'M' : 'L'}${p.x},${p.y}`).join(' ');
  const fillPath = chartPath + ` L${chartPoints[chartPoints.length - 1].x},320 L60,320 Z`;

  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i * 7) * 1920,
    y: seededRandom(i * 3) * 1080,
    r: seededRandom(i * 11) * 3 + 0.8,
    speed: seededRandom(i * 17) * 0.4 + 0.1,
    op: seededRandom(i * 5) * 0.12 + 0.03,
  }));

  const cities = [
    { name: 'München', pct: '+142%', color: '#ff0000' },
    { name: 'Hamburg', pct: '+98%', color: '#FFC107' },
    { name: 'Frankfurt', pct: '+115%', color: '#e9d700' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === GRADIENT BG === */}
      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        <div style={{
          position: 'absolute', inset: 0,
          background: 'radial-gradient(ellipse 80% 65% at 60% 40%, #1a0a00 0%, #1C1C1C 55%, #000810 100%)',
        }} />
        <div style={{
          position: 'absolute', inset: 0,
          background: 'linear-gradient(180deg, rgba(255,0,0,0.05) 0%, transparent 40%)',
        }} />
      </AbsoluteFill>

      {/* === SHAPE LAYERS === */}
      {/* Arrow shape up */}
      <svg style={{ position: 'absolute', top: 60, left: 100, opacity: 0.05 }} width="300" height="500">
        <path d="M150,480 L150,80 M60,200 L150,80 L240,200"
          stroke="#e9d700" strokeWidth="2" fill="none" strokeLinecap="round"
          transform={`scale(${1 + Math.sin(frame * 0.02) * 0.02})`} />
      </svg>
      {/* Corner grid */}
      <svg style={{ position: 'absolute', bottom: 60, right: 100, opacity: 0.03 }} width="300" height="300">
        {Array.from({ length: 5 }, (_, i) =>
          Array.from({ length: 5 }, (_, j) => (
            <rect key={`${i}-${j}`} x={i * 60} y={j * 60} width={55} height={55}
              fill="none" stroke="#e9d700" strokeWidth="0.5" />
          ))
        )}
      </svg>

      {/* === TEXTURE: WAVY LINES === */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.02 }} width="1920" height="1080">
        {Array.from({ length: 12 }, (_, i) => {
          const y = 90 + i * 90;
          const d = `M0,${y} ` + Array.from({ length: 20 }, (_, j) =>
            `Q${j * 100 + 50},${y + (j % 2 === 0 ? 15 : -15)} ${(j + 1) * 100},${y}`
          ).join(' ');
          return <path key={i} d={d} fill="none" stroke="#F5F5F5" strokeWidth="0.5" />;
        })}
      </svg>

      {/* === PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x + Math.sin(frame * p.speed + i) * 13,
          top: p.y + Math.cos(frame * p.speed * 0.7 + i) * 9,
          width: p.r * 2, height: p.r * 2,
          borderRadius: '50%',
          backgroundColor: i % 3 === 0 ? '#ff0000' : '#e9d700',
          opacity: p.op,
        }} />
      ))}

      {/* === MAIN CONTENT === */}

      {/* HEADLINE */}
      <div style={{
        position: 'absolute',
        top: 70, left: 100,
        opacity: sp(5),
        transform: `translateY(${(1 - sp(5)) * 25}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 18,
          color: '#ff0000', letterSpacing: 5,
          textTransform: 'uppercase', marginBottom: 10,
        }}>Inflation → Wer profitiert zuerst?</div>
        <div style={{
          fontFamily: 'Anton', fontSize: 100,
          color: '#F5F5F5', lineHeight: 0.95,
        }}>VERMÖGENSPREISE</div>
        <div style={{
          fontFamily: 'Anton', fontSize: 140,
          color: '#ff0000', lineHeight: 0.9,
          textShadow: '0 0 50px rgba(255,0,0,0.4)',
        }}>EXPLODIEREN</div>
      </div>

      {/* CHART */}
      <div style={{
        position: 'absolute',
        top: 360, left: 100,
        opacity: interpolate(frame, [25, 45], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 18,
          color: '#9E9E9E', marginBottom: 12,
          letterSpacing: 2,
        }}>Immobilienpreise Deutschland — 10 Jahre</div>
        <svg width="720" height="330" viewBox="0 0 720 330">
          {/* Grid */}
          {[0, 1, 2, 3].map(i => (
            <line key={i} x1={60} y1={40 + i * 70} x2={660} y2={40 + i * 70}
              stroke="rgba(255,255,255,0.05)" strokeWidth="1" />
          ))}
          {/* Fill area */}
          <path d={fillPath} fill="rgba(255,0,0,0.08)" />
          {/* Chart line */}
          <path d={chartPath} fill="none" stroke="#ff0000" strokeWidth="3.5" strokeLinecap="round" />
          {/* Current dot */}
          <circle
            cx={chartPoints[chartPoints.length - 1].x}
            cy={chartPoints[chartPoints.length - 1].y}
            r={8} fill="#ff0000"
            opacity={0.5 + Math.sin(frame * 0.15) * 0.4}
          />
          {/* Labels */}
          <text x="60" y="320" fill="#9E9E9E" fontFamily="JetBrains Mono" fontSize="14">2014</text>
          <text x="620" y="320" fill="#9E9E9E" fontFamily="JetBrains Mono" fontSize="14">2024</text>
        </svg>
      </div>

      {/* RIGHT: Counter + Cities */}
      <div style={{
        position: 'absolute',
        top: 220, right: 80,
        width: 480,
      }}>
        {/* Big % counter */}
        <div style={{
          textAlign: 'center', marginBottom: 30,
          opacity: interpolate(frame, [30, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 20,
            color: '#9E9E9E', letterSpacing: 3, marginBottom: 10,
            textTransform: 'uppercase',
          }}>Preissteigerung</div>
          <div style={{
            fontFamily: 'Anton', fontSize: 160,
            color: '#ff0000', lineHeight: 1,
            textShadow: '0 0 60px rgba(255,0,0,0.5)',
          }}>
            +{Math.round(priceUp)}%
          </div>
          <div style={{
            fontFamily: 'Open Sans', fontSize: 26,
            color: '#9E9E9E',
          }}>in 10 Jahren</div>
        </div>

        {/* City cards */}
        <div style={{ display: 'flex', flexDirection: 'column', gap: 14 }}>
          {cities.map((city, idx) => {
            const cOp = interpolate(frame, [60 + idx * 18, 82 + idx * 18], [0, 1], {
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
            });
            const cX = interpolate(frame, [60 + idx * 18, 82 + idx * 18], [40, 0], {
              easing: Easing.out(Easing.back(1.2)),
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
            });
            return (
              <div key={city.name} style={{
                display: 'flex', alignItems: 'center', justifyContent: 'space-between',
                backgroundColor: 'rgba(255,255,255,0.03)',
                border: `1px solid rgba(255,255,255,0.07)`,
                borderRadius: 10, padding: '14px 24px',
                opacity: cOp, transform: `translateX(${cX}px)`,
              }}>
                <div style={{ fontFamily: 'Open Sans', fontSize: 32, color: '#F5F5F5', fontWeight: 700 }}>
                  {city.name}
                </div>
                <div style={{
                  fontFamily: 'Anton', fontSize: 48, color: city.color,
                  textShadow: `0 0 20px ${city.color}60`,
                }}>
                  {city.pct}
                </div>
              </div>
            );
          })}
        </div>
      </div>

      {/* DAX callout */}
      <div style={{
        position: 'absolute',
        bottom: 65, left: 100,
        opacity: interpolate(frame, [95, 115], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        display: 'flex', alignItems: 'center', gap: 24,
      }}>
        <div style={{
          backgroundColor: 'rgba(233,215,0,0.08)',
          border: '1px solid rgba(233,215,0,0.3)',
          borderRadius: 8, padding: '14px 28px',
        }}>
          <span style={{ fontFamily: 'Open Sans', fontSize: 28, color: '#e9d700', fontWeight: 700 }}>
            DAX: mehr als verdoppelt in 10 Jahren
          </span>
        </div>
        <span style={{ fontFamily: 'Open Sans', fontSize: 26, color: '#9E9E9E' }}>
          Wer hatte Vermögenswerte? — Hat gewonnen.
        </span>
      </div>

      {/* === STICK FIGURE === */}
      <div style={{
        position: 'absolute',
        left: 840, bottom: 55,
        opacity: sp(55),
        transform: `translateY(${(1 - sp(55)) * 80 + breathe}px)`,
      }}>
        <svg width="95" height="175" viewBox="0 0 95 175">
          <ellipse cx="47" cy="168" rx="30" ry="6" fill="rgba(255,193,7,0.1)" />
          <line x1="47" y1="72" x2="47" y2="130" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          {/* Arms out wide — shocked */}
          <line x1="47" y1="90" x2="8" y2="75" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="47" y1="90" x2="86" y2="75" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="47" y1="130" x2="26" y2="162" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="47" y1="130" x2="68" y2="162" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <g transform={`rotate(${headNod}, 47, 44)`}>
            <circle cx="47" cy="44" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <circle cx="38" cy="43" r="7.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <circle cx="56" cy="43" r="7.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="45.5" y1="43" x2="48.5" y2="43" stroke="#FFC107" strokeWidth="2" />
            {/* Open mouth shocked */}
            <ellipse cx="47" cy="60" rx="6" ry="5" fill="none" stroke="#F5F5F5" strokeWidth="2" />
          </g>
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse 90% 88% at 50% 50%, transparent 44%, rgba(0,0,0,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene_C4_S8;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C4_S9',
  durationInSeconds: 23,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C4_S9: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  // Two line chart data
  const years = 12;
  const assetLine = Array.from({ length: years }, (_, i) => ({
    x: 120 + i * 125,
    y: 420 - (i / (years - 1)) * (i / (years - 1)) * 320,
  }));
  const wageLine = Array.from({ length: years }, (_, i) => ({
    x: 120 + i * 125,
    y: 420 - (i / (years - 1)) * 80 + (i > 2 ? Math.sin(i * 0.8) * 10 : 0),
  }));

  const chartReveal = interpolate(frame, [25, 110], [0, 1], {
    easing: Easing.linear,
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const assetPath = assetLine.map((p, i) => `${i === 0 ? 'M' : 'L'}${p.x},${p.y}`).join(' ');
  const wagePath = wageLine.map((p, i) => `${i === 0 ? 'M' : 'L'}${p.x},${p.y}`).join(' ');

  const particles = Array.from({ length: 18 }, (_, i) => ({
    x: seededRandom(i * 13) * 1920,
    y: seededRandom(i * 7) * 1080,
    r: seededRandom(i * 5) * 2.5 + 0.8,
    speed: seededRandom(i * 11) * 0.35 + 0.1,
    op: seededRandom(i * 9) * 0.1 + 0.03,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === GRADIENT BG === */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'linear-gradient(135deg, #080012 0%, #1C1C1C 40%, #000a08 100%)',
      }} />
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse 60% 50% at 70% 30%, rgba(233,215,0,0.05) 0%, transparent 70%)',
      }} />

      {/* === SHAPE LAYERS === */}
      {/* Diagonal cut line */}
      <div style={{
        position: 'absolute',
        top: 0, left: 0, right: 0, bottom: 0,
        overflow: 'hidden',
      }}>
        <div style={{
          position: 'absolute',
          top: -100, left: -100,
          width: 3, height: 2000,
          background: 'linear-gradient(180deg, transparent, rgba(233,215,0,0.08), transparent)',
          transform: `rotate(${30 + Math.sin(frame * 0.01) * 2}deg)`,
          transformOrigin: 'top left',
        }} />
      </div>
      {/* Concentric arcs */}
      {[300, 500, 700].map((r, i) => (
        <div key={r} style={{
          position: 'absolute',
          bottom: -r + 200, right: -r + 200,
          width: r * 2, height: r * 2,
          borderRadius: '50%',
          border: `1px solid rgba(233,215,0,${0.03 - i * 0.008})`,
          transform: `rotate(${frame * (0.02 - i * 0.005)}deg)`,
        }} />
      ))}

      {/* === TEXTURE: DOT MATRIX === */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.025 }} width="1920" height="1080">
        {Array.from({ length: 18 }, (_, row) =>
          Array.from({ length: 30 }, (_, col) => (
            <circle key={`${row}-${col}`}
              cx={col * 68} cy={row * 62} r="1.2" fill="#9E9E9E" />
          ))
        )}
      </svg>

      {/* === PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x + Math.sin(frame * p.speed + i) * 10,
          top: p.y + Math.cos(frame * p.speed * 0.8 + i) * 8,
          width: p.r * 2, height: p.r * 2,
          borderRadius: '50%',
          backgroundColor: '#e9d700',
          opacity: p.op,
        }} />
      ))}

      {/* === MAIN CONTENT === */}

      {/* HEADLINE */}
      <div style={{
        position: 'absolute',
        top: 65, left: 100,
        opacity: sp(5),
        transform: `translateY(${(1 - sp(5)) * 25}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 18,
          color: '#9E9E9E', letterSpacing: 4,
          textTransform: 'uppercase', marginBottom: 10,
        }}>Inflation-Gewinner vs. Zuschauer</div>
        <div style={{
          fontFamily: 'Anton', fontSize: 85,
          color: '#F5F5F5', lineHeight: 0.95,
        }}>VERMÖGEN WÄCHST</div>
        <div style={{
          fontFamily: 'Anton', fontSize: 85,
          color: '#e9d700', lineHeight: 0.95,
          textShadow: '0 0 40px rgba(233,215,0,0.3)',
        }}>LÖHNE HINKEN HINTERHER</div>
      </div>

      {/* CHART */}
      <div style={{
        position: 'absolute',
        top: 290, left: 80,
        opacity: interpolate(frame, [20, 40], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        <svg width="1600" height="450" viewBox="0 0 1600 450" overflow="hidden">
          {/* Axes */}
          <line x1="120" y1="50" x2="120" y2="430" stroke="rgba(255,255,255,0.12)" strokeWidth="1.5" />
          <line x1="120" y1="430" x2="1550" y2="430" stroke="rgba(255,255,255,0.12)" strokeWidth="1.5" />

          {/* Grid lines */}
          {[100, 200, 300, 400].map(y => (
            <line key={y} x1="120" y1={y} x2="1550" y2={y}
              stroke="rgba(255,255,255,0.04)" strokeWidth="1" strokeDasharray="6,6" />
          ))}

          {/* CLIP PATH for reveal */}
          <defs>
            <clipPath id="chartReveal">
              <rect x="0" y="0" width={120 + chartReveal * 1430} height="450" />
            </clipPath>
          </defs>

          {/* Asset line — yellow, goes up steeply */}
          <g clipPath="url(#chartReveal)">
            <path d={assetPath} fill="none" stroke="#e9d700" strokeWidth="4" strokeLinecap="round" />
            {/* Fill under asset */}
            <path d={assetPath + ` L${assetLine[assetLine.length - 1].x},430 L120,430 Z`}
              fill="rgba(233,215,0,0.08)" />
          </g>

          {/* Wage line — grey, flat */}
          <g clipPath="url(#chartReveal)">
            <path d={wagePath} fill="none" stroke="#9E9E9E" strokeWidth="3"
              strokeLinecap="round" strokeDasharray="12,6" />
          </g>

          {/* Legend */}
          <rect x="130" y="60" width="24" height="4" rx="2" fill="#e9d700" />
          <text x="162" y="68" fill="#e9d700" fontFamily="Open Sans" fontSize="20" fontWeight="bold">
            Vermögenspreise (Immobilien / Aktien)
          </text>
          <rect x="130" y="90" width="24" height="4" rx="2" fill="#9E9E9E" />
          <text x="162" y="98" fill="#9E9E9E" fontFamily="Open Sans" fontSize="20">
            Lohnentwicklung (träge, verzögert)
          </text>

          {/* Year labels */}
          {['2014', '2016', '2018', '2020', '2022', '2024'].map((y, i) => (
            <text key={y} x={120 + i * 280} y="448" fill="#9E9E9E" fontFamily="JetBrains Mono" fontSize="16">
              {y}
            </text>
          ))}
        </svg>
      </div>

      {/* RIGHT SIDE: Callout boxes */}
      <div style={{
        position: 'absolute',
        top: 240, right: 60,
        width: 360,
        display: 'flex', flexDirection: 'column', gap: 20,
      }}>
        {/* Winner box */}
        <div style={{
          opacity: interpolate(frame, [70, 95], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `translateX(${interpolate(frame, [70, 95], [40, 0], {
            easing: Easing.out(Easing.back(1.2)),
            extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
          })}px)`,
          backgroundColor: 'rgba(233,215,0,0.08)',
          border: '1.5px solid rgba(233,215,0,0.35)',
          borderRadius: 12, padding: '24px 28px',
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 16, color: '#e9d700', letterSpacing: 3, marginBottom: 8 }}>
            VERMÖGENSBESITZER
          </div>
          <div style={{ fontFamily: 'Anton', fontSize: 72, color: '#e9d700', lineHeight: 1 }}>+%</div>
          <div style={{ fontFamily: 'Open Sans', fontSize: 22, color: '#9E9E9E', marginTop: 8 }}>
            Automatisch profitiert.<br />Ohne einen Finger zu rühren.
          </div>
        </div>

        {/* Loser box */}
        <div style={{
          opacity: interpolate(frame, [88, 113], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          transform: `translateX(${interpolate(frame, [88, 113], [40, 0], {
            easing: Easing.out(Easing.back(1.2)),
            extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
          })}px)`,
          backgroundColor: 'rgba(255,0,0,0.06)',
          border: '1px solid rgba(255,0,0,0.25)',
          borderRadius: 12, padding: '24px 28px',
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 16, color: '#ff0000', letterSpacing: 3, marginBottom: 8 }}>
            GEHALTSEMPFÄNGER
          </div>
          <div style={{ fontFamily: 'Anton', fontSize: 62, color: '#ff0000', lineHeight: 1 }}>HAT ZUGESEHEN</div>
          <div style={{ fontFamily: 'Open Sans', fontSize: 22, color: '#9E9E9E', marginTop: 8 }}>
            Lohnerhöhung? 2 Jahre<br />Verzögerung. Oft zu wenig.
          </div>
        </div>

        {/* Structural note */}
        <div style={{
          opacity: interpolate(frame, [110, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          backgroundColor: 'rgba(255,255,255,0.03)',
          border: '1px solid rgba(255,255,255,0.08)',
          borderRadius: 10, padding: '18px 24px',
        }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 16, color: '#9E9E9E', lineHeight: 1.6 }}>
            {'// Das ist kein Pech.'}<br />
            {'// Das ist ein struktureller Mechanismus.'}
          </div>
        </div>
      </div>

      {/* === STICK FIGURE === */}
      <div style={{
        position: 'absolute',
        right: 75, bottom: 55,
        opacity: sp(55),
        transform: `translateY(${(1 - sp(55)) * 80 + breathe}px)`,
      }}>
        <svg width="95" height="175" viewBox="0 0 95 175">
          <ellipse cx="47" cy="168" rx="30" ry="6" fill="rgba(255,193,7,0.1)" />
          <line x1="47" y1="72" x2="47" y2="130" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          {/* Arms behind back — resigned */}
          <line x1="47" y1="94" x2="16" y2="104" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="47" y1="94" x2="78" y2="104" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="47" y1="130" x2="26" y2="162" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="47" y1="130" x2="68" y2="162" stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <g transform={`rotate(${headNod}, 47, 44)`}>
            <circle cx="47" cy="44" r="22" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            <circle cx="38" cy="43" r="7.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <circle cx="56" cy="43" r="7.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
            <line x1="45.5" y1="43" x2="48.5" y2="43" stroke="#FFC107" strokeWidth="2" />
            {/* Flat / sad expression */}
            <line x1="40" y1="59" x2="54" y2="59" stroke="#9E9E9E" strokeWidth="2" strokeLinecap="round" />
          </g>
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse 92% 90% at 50% 50%, transparent 42%, rgba(0,0,0,0.72) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene_C4_S9;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene_C4_S10',
  durationInSeconds: 27,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene_C4_S10: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const breathe = Math.sin(frame * 0.05) * 2;
  const headNod = Math.sin(frame * 0.04) * 3;

  // Side switch animation
  const switchProgress = interpolate(frame, [50, 130], [0, 1], {
    easing: Easing.out(Easing.cubic),
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Glow pulse
  const glowPulse = 0.5 + Math.sin(frame * 0.08) * 0.3;

  const particles = Array.from({ length: 26 }, (_, i) => ({
    x: seededRandom(i * 9) * 1920,
    y: seededRandom(i * 5) * 1080,
    r: seededRandom(i * 7) * 3 + 0.8,
    speed: seededRandom(i * 3) * 0.4 + 0.1,
    op: seededRandom(i * 13) * 0.14 + 0.03,
    color: i % 3 === 0 ? '#FFC107' : '#e9d700',
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#1C1C1C', overflow: 'hidden', opacity: exitOp }}>

      {/* === GRADIENT BG === */}
      <div style={{
        position: 'absolute', inset: 0,
        background: `radial-gradient(ellipse 80% 70% at ${20 + switchProgress * 60}% 50%, 
          ${switchProgress > 0.5 ? '#001a08' : '#0a0005'} 0%, 
          #1C1C1C 55%, 
          #050010 100%)`,
      }} />
      <div style={{
        position: 'absolute', inset: 0,
        background: `radial-gradient(ellipse 40% 50% at 80% 50%, rgba(233,215,0,${switchProgress * 0.1}) 0%, transparent 70%)`,
      }} />

      {/* === SHAPE LAYERS === */}
      {/* Door/portal shape — switching sides */}
      <svg style={{
        position: 'absolute',
        top: '50%', left: '50%',
        transform: `translate(-50%, -50%) scale(${0.8 + switchProgress * 0.3})`,
        opacity: 0.06,
      }} width="200" height="300">
        <rect x="10" y="10" width="180" height="280" rx="90" fill="none" stroke="#e9d700" strokeWidth="2" />
      </svg>

      {/* Left field color wash */}
      <div style={{
        position: 'absolute', top: 0, left: 0,
        width: '50%', height: '100%',
        background: 'linear-gradient(90deg, rgba(255,0,0,0.03) 0%, transparent 100%)',
      }} />
      {/* Right field color wash */}
      <div style={{
        position: 'absolute', top: 0, right: 0,
        width: '50%', height: '100%',
        background: 'linear-gradient(90deg, transparent 0%, rgba(233,215,0,0.04) 100%)',
      }} />

      {/* Center vertical line */}
      <div style={{
        position: 'absolute',
        top: 0, bottom: 0, left: '50%',
        width: 2,
        background: 'linear-gradient(180deg, transparent, rgba(233,215,0,0.15) 30%, rgba(233,215,0,0.15) 70%, transparent)',
        transform: 'translateX(-50%)',
      }} />

      {/* === TEXTURE: VERTICAL BARS === */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.015 }} width="1920" height="1080">
        {Array.from({ length: 40 }, (_, i) => (
          <rect key={i} x={i * 50} y={0} width={2} height={1080} fill="#F5F5F5" />
        ))}
      </svg>

      {/* === PARTICLES === */}
      {particles.map((p, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: p.x + Math.sin(frame * p.speed + i) * 13,
          top: p.y + Math.cos(frame * p.speed * 0.7 + i) * 9,
          width: p.r * 2, height: p.r * 2,
          borderRadius: '50%',
          backgroundColor: p.color,
          opacity: p.op,
        }} />
      ))}

      {/* === MAIN CONTENT === */}

      {/* TOP BADGE */}
      <div style={{
        position: 'absolute',
        top: 60, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(5),
        transform: `translateY(${(1 - sp(5)) * 20}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: 'rgba(233,215,0,0.08)',
          border: '1px solid rgba(233,215,0,0.3)',
          borderRadius: 8, padding: '12px 40px',
        }}>
          <span style={{
            fontFamily: 'JetBrains Mono', fontSize: 20,
            color: '#e9d700', letterSpacing: 4,
            textTransform: 'uppercase',
          }}>Kapitel 4 — Fazit</span>
        </div>
      </div>

      {/* LEFT SIDE: Falsche Seite */}
      <div style={{
        position: 'absolute',
        top: 180, left: 60,
        width: 820,
        opacity: sp(15),
        transform: `translateX(${(1 - sp(15)) * -40}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 17,
          color: '#ff0000', letterSpacing: 3, marginBottom: 12,
          textTransform: 'uppercase',
        }}>❌ Nicht durch...</div>
        <div style={{
          fontFamily: 'Anton', fontSize: 90,
          color: '#ff0000', lineHeight: 0.95,
          textDecoration: `line-through`,
          opacity: 0.7,
          textShadow: '0 0 30px rgba(255,0,0,0.3)',
        }}>HARTE</div>
        <div style={{
          fontFamily: 'Anton', fontSize: 90,
          color: '#ff0000', lineHeight: 0.95,
          textDecoration: `line-through`,
          opacity: 0.7,
        }}>ARBEIT</div>
        <div style={{
          marginTop: 24,
          fontFamily: 'Open Sans', fontSize: 32,
          color: '#9E9E9E', lineHeight: 1.5,
        }}>
          Mehr Stunden bringen dich<br />
          nur an die Decke — schneller.
        </div>

        {/* Crossed features */}
        <div style={{ marginTop: 24, display: 'flex', flexDirection: 'column', gap: 12 }}>
          {['Mehr arbeiten', 'Länger im Büro bleiben', 'Überstunden'].map((item, idx) => {
            const op = interpolate(frame, [25 + idx * 15, 45 + idx * 15], [0, 1], {
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
            });
            return (
              <div key={item} style={{ display: 'flex', alignItems: 'center', gap: 14, opacity: op }}>
                <svg width="28" height="28" viewBox="0 0 28 28">
                  <line x1="4" y1="4" x2="24" y2="24" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" />
                  <line x1="24" y1="4" x2="4" y2="24" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" />
                </svg>
                <span style={{
                  fontFamily: 'Open Sans', fontSize: 30,
                  color: '#9E9E9E', textDecoration: 'line-through', opacity: 0.6,
                }}>{item}</span>
              </div>
            );
          })}
        </div>
      </div>

      {/* RIGHT SIDE: Richtige Seite */}
      <div style={{
        position: 'absolute',
        top: 180, right: 60,
        width: 820,
        opacity: interpolate(frame, [40, 65], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        transform: `translateX(${interpolate(frame, [40, 65], [60, 0], {
          easing: Easing.out(Easing.back(1.2)),
          extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
        })}px)`,
        textAlign: 'right',
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 17,
          color: '#e9d700', letterSpacing: 3, marginBottom: 12,
          textTransform: 'uppercase',
        }}>✓ Durch...</div>
        <div style={{
          fontFamily: 'Anton', fontSize: 90,
          color: '#e9d700', lineHeight: 0.95,
          textShadow: `0 0 ${40 + glowPulse * 20}px rgba(233,215,0,0.5)`,
        }}>KLÜGER</div>
        <div style={{
          fontFamily: 'Anton', fontSize: 90,
          color: '#e9d700', lineHeight: 0.95,
          textShadow: `0 0 ${40 + glowPulse * 20}px rgba(233,215,0,0.5)`,
        }}>STRUKTURIEREN</div>
        <div style={{
          marginTop: 24,
          fontFamily: 'Open Sans', fontSize: 32,
          color: '#9E9E9E', lineHeight: 1.5,
          textAlign: 'right',
        }}>
          Kapital arbeitet.<br />
          Du entscheidest wo. Und wie.
        </div>

        {/* Right features */}
        <div style={{ marginTop: 24, display: 'flex', flexDirection: 'column', gap: 12, alignItems: 'flex-end' }}>
          {['Kapital strukturieren', 'Steuern optimieren', 'Eigentümer werden'].map((item, idx) => {
            const op = interpolate(frame, [55 + idx * 15, 75 + idx * 15], [0, 1], {
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
            });
            return (
              <div key={item} style={{ display: 'flex', alignItems: 'center', gap: 14, opacity: op }}>
                <span style={{ fontFamily: 'Open Sans', fontSize: 30, color: '#F5F5F5', fontWeight: 700 }}>
                  {item}
                </span>
                <svg width="28" height="28" viewBox="0 0 28 28">
                  <polyline points="4,14 11,21 24,7" stroke="#e9d700" strokeWidth="2.5" fill="none" strokeLinecap="round" />
                </svg>
              </div>
            );
          })}
        </div>
      </div>

      {/* BOTTOM MEGA STATEMENT */}
      <div style={{
        position: 'absolute',
        bottom: 80, left: 0, right: 0,
        textAlign: 'center',
        opacity: interpolate(frame, [130, 155], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        transform: `translateY(${interpolate(frame, [130, 160], [30, 0], {
          easing: Easing.out(Easing.back(1.3)),
          extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
        })}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: 'rgba(233,215,0,0.08)',
          border: '2px solid rgba(233,215,0,0.45)',
          borderRadius: 14,
          padding: '22px 60px',
          boxShadow: `0 0 60px rgba(233,215,0,${glowPulse * 0.15})`,
        }}>
          <span style={{
            fontFamily: 'Anton', fontSize: 60,
            color: '#e9d700', letterSpacing: 4,
            textShadow: '0 0 30px rgba(233,215,0,0.4)',
          }}>
            WECHSEL DIE SEITE — WERDE EIGENTÜMER
          </span>
        </div>
      </div>

      {/* === STICK FIGURE — crossing the center line === */}
      <div style={{
        position: 'absolute',
        bottom: 60,
        left: `${interpolate(frame, [50, 140], [28, 50], {
          easing: Easing.out(Easing.cubic),
          extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
        })}%`,
        opacity: sp(45),
        transform: `translateY(${(1 - sp(45)) * 80 + breathe}px) translateX(-50%)`,
      }}>
        <svg width="110" height="200" viewBox="0 0 110 200">
          {/* Golden glow — grows as he crosses */}
          <ellipse cx="55" cy="192" rx={30 + switchProgress * 20} ry={7 + switchProgress * 5}
            fill={`rgba(255,193,7,${0.1 + switchProgress * 0.2})`} />
          {/* Body */}
          <line x1="55" y1="80" x2="55" y2="148" stroke="#F5F5F5" strokeWidth="3.5" strokeLinecap="round" />
          {/* Arms walking */}
          <line x1="55" y1="100"
            x2={55 + Math.sin(frame * 0.18) * 28}
            y2={118 + Math.cos(frame * 0.18) * 8}
            stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="55" y1="100"
            x2={55 - Math.sin(frame * 0.18) * 28}
            y2={118 - Math.cos(frame * 0.18) * 8}
            stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Legs walking */}
          <line x1="55" y1="148"
            x2={55 + Math.sin(frame * 0.18) * 22}
            y2={182 - Math.abs(Math.sin(frame * 0.18)) * 10}
            stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          <line x1="55" y1="148"
            x2={55 - Math.sin(frame * 0.18) * 22}
            y2={182 - Math.abs(Math.cos(frame * 0.18)) * 10}
            stroke="#F5F5F5" strokeWidth="3" strokeLinecap="round" />
          {/* Head */}
          <g transform={`rotate(${headNod}, 55, 52)`}>
            <circle cx="55" cy="52" r="26" fill="none" stroke="#F5F5F5" strokeWidth="3" />
            {/* Glasses — turning gold as he crosses */}
            <circle cx="44" cy="51" r="9" fill="none"
              stroke={`rgba(255,193,7,${0.5 + switchProgress * 0.5})`} strokeWidth="2.5" />
            <circle cx="66" cy="51" r="9" fill="none"
              stroke={`rgba(255,193,7,${0.5 + switchProgress * 0.5})`} strokeWidth="2.5" />
            <line x1="53" y1="51" x2="57" y2="51"
              stroke={`rgba(255,193,7,${0.5 + switchProgress * 0.5})`} strokeWidth="2" />
            {/* Expression shifts from neutral to smile */}
            <path
              d={`M43,67 Q55,${67 + switchProgress * 10} 67,67`}
              stroke={`rgba(${Math.round(245 - switchProgress * 12)},${Math.round(245 - switchProgress * 30)},${Math.round(245 - switchProgress * 245)},1)`}
              strokeWidth="2.5" fill="none" strokeLinecap="round"
            />
          </g>
        </svg>
      </div>

      {/* === VIGNETTE === */}
      <div style={{
        position: 'absolute', inset: 0,
        background: 'radial-gradient(ellipse 90% 90% at 50% 50%, transparent 40%, rgba(0,0,0,0.72) 100%)',
        pointerEvents: 'none',
      }} />

      {/* === WATERMARK === */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
      }}>
        Kux legt an
      </div>

    </AbsoluteFill>
  );
};

export default Scene_C4_S10;
```

