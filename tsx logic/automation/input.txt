```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1MathFails',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1MathFails: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter animation - 100 lakh crore
  const counterProgress = interpolate(frame, [30, 110], [0, 100], { extrapolateRight: 'clamp' });
  const counterValue = Math.floor(counterProgress);

  // Crack/break animation
  const crackProgress = interpolate(frame, [140, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shakeX = frame > 130 && frame < 180 ? Math.sin(frame * 0.8) * 6 : 0;
  const shakeY = frame > 130 && frame < 180 ? Math.cos(frame * 0.9) * 4 : 0;

  // Equation fail flash
  const failFlash = interpolate(frame, [200, 220, 260, 280], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#D2E3C8', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 20%, rgba(0,21,36,0.08) 0%, transparent 60%), radial-gradient(circle at 80% 90%, rgba(255,193,7,0.1) 0%, transparent 50%)',
      }} />

      {/* Shape Layers - drifting geometric forms */}
      {[0, 1, 2, 3, 4].map((i) => {
        const seed = seededRandom(i + 1);
        const size = 280 + seed * 200;
        const x = seededRandom(i + 10) * 1920;
        const y = seededRandom(i + 20) * 1080;
        const rot = frame * 0.15 + i * 45;
        const scale = 1 + Math.sin(frame * 0.02 + i) * 0.08;
        return (
          <div key={`shape-${i}`} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: '3px solid #001524',
            opacity: 0.06,
            transform: `rotate(${rot}deg) scale(${scale})`,
            borderRadius: i % 2 === 0 ? '50%' : '0%',
          }} />
        );
      })}

      {/* Grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="1920" height="1080">
        <defs>
          <pattern id="grid1" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#001524" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid1)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const seed = seededRandom(i + 100);
        const px = (seededRandom(i + 200) * 1920 + frame * 0.5) % 1920;
        const py = seededRandom(i + 300) * 1080 + Math.sin(frame * 0.03 + i) * 20;
        const psize = 3 + seed * 5;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: psize,
            height: psize,
            backgroundColor: '#001524',
            borderRadius: '50%',
            opacity: 0.3,
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <div style={{
        position: 'absolute',
        inset: 0,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        transform: `translate(${shakeX}px, ${shakeY}px)`,
      }}>
        {/* Top label */}
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 38,
          fontWeight: 700,
          color: '#001524',
          letterSpacing: 8,
          opacity: sp(10) * 0.7,
          marginBottom: 30,
          textTransform: 'uppercase',
        }}>
          // SCENARIO :: SYSTEMIC FAILURE
        </div>

        {/* Big counter */}
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 280,
          fontWeight: 900,
          color: '#001524',
          lineHeight: 1,
          transform: `scale(${sp(20)})`,
          textShadow: failFlash > 0 ? `0 0 ${failFlash * 40}px rgba(255,0,0,0.6)` : 'none',
        }}>
          ₹{counterValue}L Cr
        </div>

        {/* Crack line SVG */}
        {crackProgress > 0 && (
          <svg style={{ position: 'absolute', top: '38%', left: 0, width: '100%', height: 300, pointerEvents: 'none' }} viewBox="0 0 1920 300">
            <path
              d="M 0 150 L 300 140 L 320 160 L 600 145 L 620 170 L 900 150 L 920 130 L 1200 155 L 1220 175 L 1500 145 L 1520 165 L 1920 150"
              stroke="#ff0000"
              strokeWidth="6"
              fill="none"
              strokeDasharray="2000"
              strokeDashoffset={2000 - crackProgress * 2000}
            />
          </svg>
        )}

        {/* Subhead */}
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 72,
          fontWeight: 700,
          color: '#001524',
          marginTop: 50,
          opacity: sp(40),
          transform: `translateY(${interpolate(sp(40), [0, 1], [40, 0])}px)`,
        }}>
          अगर एक साथ निकले...
        </div>

        {/* Equation fail */}
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 64,
          fontWeight: 800,
          color: failFlash > 0.3 ? '#ff0000' : '#001524',
          marginTop: 40,
          opacity: sp(180),
          letterSpacing: 4,
        }}>
          2.29L Cr ÷ 100L Cr = FAIL
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 40%, rgba(0,21,36,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1MathFails;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2NotToScare',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2NotToScare: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const panX = interpolate(frame, [0, durationInFrames], [-20, 20]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#D2E3C8', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(0,21,36,0.05) 0%, transparent 50%, rgba(255,193,7,0.08) 100%)',
      }} />

      {/* Diagonal shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const x = 200 + i * 450;
        const y = 100 + Math.sin(frame * 0.02 + i) * 30;
        const rot = 25 + frame * 0.05 + i * 10;
        return (
          <div key={`tri-${i}`} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: 0,
            height: 0,
            borderLeft: '180px solid transparent',
            borderRight: '180px solid transparent',
            borderBottom: '320px solid #001524',
            opacity: 0.05,
            transform: `rotate(${rot}deg)`,
          }} />
        );
      })}

      {/* Diagonal lines texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="1920" height="1080">
        <defs>
          <pattern id="lines2" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#001524" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#lines2)" />
      </svg>

      {/* Floating dots */}
      {Array.from({ length: 30 }).map((_, i) => {
        const px = (seededRandom(i + 50) * 1920 + frame * 0.3) % 1920;
        const py = seededRandom(i + 60) * 1080;
        return (
          <div key={`d-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py + Math.sin(frame * 0.04 + i) * 15,
            width: 4,
            height: 4,
            backgroundColor: '#001524',
            borderRadius: '50%',
            opacity: 0.25,
          }} />
        );
      })}

      {/* MAIN CONTENT - Split layout */}
      <div style={{
        position: 'absolute',
        inset: 0,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        transform: `translateX(${panX}px)`,
      }}>
        <div style={{ width: '85%', textAlign: 'center' }}>
          {/* Tag */}
          <div style={{
            display: 'inline-block',
            padding: '14px 36px',
            backgroundColor: '#001524',
            color: '#D2E3C8',
            fontFamily: 'Inter',
            fontSize: 32,
            fontWeight: 700,
            letterSpacing: 6,
            marginBottom: 50,
            opacity: sp(10),
            transform: `scale(${sp(10)})`,
          }}>
            एक स्पष्ट संदेश
          </div>

          {/* Strike through "डराना" */}
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 180,
            fontWeight: 900,
            color: '#001524',
            lineHeight: 1.1,
            opacity: sp(30),
            position: 'relative',
            display: 'inline-block',
          }}>
            <span style={{ position: 'relative' }}>
              डराना
              {/* strike line */}
              <div style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                height: 14,
                width: `${interpolate(sp(60), [0, 1], [0, 100])}%`,
                backgroundColor: '#ff0000',
                transform: 'rotate(-3deg)',
              }} />
            </span>
            <span style={{ color: '#001524' }}> नहीं</span>
          </div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 180,
            fontWeight: 900,
            color: '#FFC107',
            lineHeight: 1.1,
            marginTop: 20,
            opacity: sp(100),
            transform: `translateY(${interpolate(sp(100), [0, 1], [60, 0])}px)`,
            textShadow: '4px 4px 0px #001524',
          }}>
            जगाना
          </div>

          {/* Underline accent */}
          <div style={{
            width: interpolate(sp(140), [0, 1], [0, 600]),
            height: 8,
            backgroundColor: '#001524',
            margin: '40px auto 0',
          }} />

          {/* Subhead */}
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 52,
            fontWeight: 600,
            color: '#001524',
            marginTop: 40,
            opacity: sp(170),
            letterSpacing: 1,
          }}>
            वित्तीय जागरूकता ही असली सुरक्षा है
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,21,36,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2NotToScare;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3FirstRule',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3FirstRule: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // zoom
  const zoom = interpolate(frame, [0, durationInFrames], [1.0, 1.05]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#D2E3C8', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 30%, rgba(255,193,7,0.12) 0%, transparent 60%)',
      }} />

      {/* Concentric circles shape layer */}
      {[0, 1, 2, 3, 4].map((i) => {
        const size = 400 + i * 280;
        const breathe = 1 + Math.sin(frame * 0.03 + i * 0.5) * 0.04;
        return (
          <div key={`c-${i}`} style={{
            position: 'absolute',
            left: '50%',
            top: '50%',
            width: size,
            height: size,
            border: '2px dashed #001524',
            borderRadius: '50%',
            transform: `translate(-50%, -50%) scale(${breathe}) rotate(${frame * 0.1 + i * 20}deg)`,
            opacity: 0.07,
          }} />
        );
      })}

      {/* Dot grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="1920" height="1080">
        <defs>
          <pattern id="dots3" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="1.5" fill="#001524" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots3)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const angle = (i / 20) * Math.PI * 2 + frame * 0.005;
        const radius = 500 + Math.sin(frame * 0.02 + i) * 50;
        const cx = 960 + Math.cos(angle) * radius;
        const cy = 540 + Math.sin(angle) * radius;
        return (
          <div key={`orb-${i}`} style={{
            position: 'absolute',
            left: cx,
            top: cy,
            width: 6,
            height: 6,
            backgroundColor: '#FFC107',
            borderRadius: '50%',
            opacity: 0.5,
            boxShadow: '0 0 10px rgba(255,193,7,0.6)',
          }} />
        );
      })}

      {/* MAIN */}
      <div style={{
        position: 'absolute',
        inset: 0,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        transform: `scale(${zoom})`,
      }}>
        {/* Rule number */}
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 56,
          fontWeight: 800,
          color: '#001524',
          opacity: sp(10) * 0.6,
          letterSpacing: 8,
          marginBottom: 20,
        }}>
          RULE • 01
        </div>

        {/* Big "01" circle */}
        <div style={{
          width: 240,
          height: 240,
          borderRadius: '50%',
          backgroundColor: '#001524',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          marginBottom: 50,
          transform: `scale(${sp(20)}) rotate(${interpolate(sp(20), [0, 1], [-180, 0])}deg)`,
          boxShadow: '0 20px 60px rgba(0,21,36,0.3)',
        }}>
          <span style={{
            fontFamily: 'Yantra One',
            fontSize: 160,
            fontWeight: 900,
            color: '#FFC107',
            lineHeight: 1,
          }}>
            1
          </span>
        </div>

        {/* Main statement */}
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 110,
          fontWeight: 900,
          color: '#001524',
          textAlign: 'center',
          lineHeight: 1.15,
          opacity: sp(50),
          maxWidth: '85%',
        }}>
          सिस्टम की कमज़ोरियों को
        </div>

        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 140,
          fontWeight: 900,
          color: '#FFC107',
          textAlign: 'center',
          lineHeight: 1.1,
          marginTop: 10,
          opacity: sp(90),
          transform: `translateY(${interpolate(sp(90), [0, 1], [40, 0])}px)`,
          textShadow: '3px 3px 0 #001524',
        }}>
          समझना
        </div>

        {/* Side bars */}
        <div style={{
          display: 'flex',
          gap: 20,
          marginTop: 50,
          opacity: sp(140),
        }}>
          {[0, 1, 2, 3, 4].map((i) => (
            <div key={`bar-${i}`} style={{
              width: 80,
              height: interpolate(sp(140 + i * 8), [0, 1], [0, 20 + i * 8]),
              backgroundColor: '#001524',
            }} />
          ))}
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 45%, rgba(0,21,36,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3FirstRule;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4DontBeHappy',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4DontBeHappy: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Bank vault fills then warning
  const fillProgress = interpolate(frame, [40, 110], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const warningShake = frame > 180 ? Math.sin(frame * 1.2) * 5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#D2E3C8', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(255,193,7,0.05) 0%, transparent 50%, rgba(255,0,0,0.05) 100%)',
      }} />

      {/* Big rotating square shapes */}
      {[0, 1, 2].map((i) => {
        const size = 500 + i * 200;
        const rot = frame * 0.2 + i * 60;
        return (
          <div key={`sq-${i}`} style={{
            position: 'absolute',
            left: 1500 - i * 100,
            top: -100 + i * 50,
            width: size,
            height: size,
            border: '4px solid #001524',
            opacity: 0.05,
            transform: `rotate(${rot}deg)`,
          }} />
        );
      })}

      {/* Vertical lines texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="1920" height="1080">
        <defs>
          <pattern id="vlines" width="30" height="30" patternUnits="userSpaceOnUse">
            <line x1="15" y1="0" x2="15" y2="30" stroke="#001524" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#vlines)" />
      </svg>

      {/* Particles rising */}
      {Array.from({ length: 25 }).map((_, i) => {
        const py = (1080 - (frame * 2 + i * 50) % 1200);
        const px = seededRandom(i + 1) * 1920;
        return (
          <div key={`pt-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 5,
            height: 5,
            backgroundColor: '#FFC107',
            borderRadius: '50%',
            opacity: 0.4,
          }} />
        );
      })}

      {/* MAIN: Left side bank illustration, Right side text */}
      <div style={{
        position: 'absolute',
        inset: 0,
        display: 'flex',
        alignItems: 'center',
        padding: '0 100px',
        transform: `translateX(${warningShake}px)`,
      }}>
        {/* LEFT - Bank vault SVG */}
        <div style={{
          width: '40%',
          display: 'flex',
          justifyContent: 'center',
          opacity: sp(10),
          transform: `scale(${sp(10)})`,
        }}>
          <svg width="500" height="600" viewBox="0 0 500 600">
            {/* Vault body */}
            <rect x="50" y="100" width="400" height="450" fill="#001524" rx="20" />
            <rect x="80" y="130" width="340" height="390" fill="#D2E3C8" rx="10" />
            {/* Fill bar inside */}
            <rect x="80" y={520 - fillProgress * 3.9} width="340" height={fillProgress * 3.9} fill="#FFC107" opacity="0.7" rx="10" />
            {/* Vault wheel */}
            <circle cx="250" cy="325" r="80" fill="#001524" stroke="#FFC107" strokeWidth="6" />
            <circle cx="250" cy="325" r="60" fill="none" stroke="#FFC107" strokeWidth="3" />
            {/* Wheel spokes */}
            {[0, 60, 120, 180, 240, 300].map((a) => (
              <line
                key={a}
                x1="250"
                y1="325"
                x2={250 + Math.cos((a + frame * 0.5) * Math.PI / 180) * 70}
                y2={325 + Math.sin((a + frame * 0.5) * Math.PI / 180) * 70}
                stroke="#FFC107"
                strokeWidth="4"
              />
            ))}
            <circle cx="250" cy="325" r="12" fill="#FFC107" />
            {/* Top label */}
            <rect x="100" y="60" width="300" height="50" fill="#001524" rx="6" />
            <text x="250" y="95" textAnchor="middle" fill="#FFC107" fontSize="28" fontWeight="800" fontFamily="JetBrains Mono">SINGLE VAULT</text>
            {/* Warning crack appears after frame 180 */}
            {frame > 180 && (
              <path
                d="M 250 130 L 240 200 L 260 280 L 245 360 L 265 450 L 250 520"
                stroke="#ff0000"
                strokeWidth="4"
                fill="none"
              />
            )}
          </svg>
        </div>

        {/* RIGHT - Text content */}
        <div style={{ width: '60%', paddingLeft: 60 }}>
          {/* Warning bar */}
          <div style={{
            display: 'inline-block',
            padding: '12px 28px',
            backgroundColor: '#ff0000',
            color: '#F5F5F5',
            fontFamily: 'Inter',
            fontSize: 28,
            fontWeight: 800,
            letterSpacing: 4,
            marginBottom: 35,
            opacity: sp(30),
          }}>
            ⚠ चेतावनी
          </div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 130,
            fontWeight: 900,
            color: '#001524',
            lineHeight: 1.05,
            opacity: sp(50),
            transform: `translateX(${interpolate(sp(50), [0, 1], [-50, 0])}px)`,
          }}>
            एक ही बैंक
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 130,
            fontWeight: 900,
            color: '#001524',
            lineHeight: 1.05,
            opacity: sp(80),
            transform: `translateX(${interpolate(sp(80), [0, 1], [-50, 0])}px)`,
          }}>
            = एक ही
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 150,
            fontWeight: 900,
            color: '#ff0000',
            lineHeight: 1,
            marginTop: 10,
            opacity: sp(120),
            transform: `scale(${sp(120)})`,
            textShadow: '4px 4px 0 #001524',
          }}>
            जोखिम
          </div>

          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 44,
            fontWeight: 600,
            color: '#001524',
            marginTop: 30,
            opacity: sp(180),
            letterSpacing: 1,
          }}>
            खुश मत होइए — सोचिए
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 40%, rgba(0,21,36,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4DontBeHappy;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5Diversify',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5Diversify: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const assets = [
    { label: 'GOLD', sub: 'सोना', pct: 15, color: '#FFC107' },
    { label: 'REAL ESTATE', sub: 'रियल एस्टेट', pct: 25, color: '#001524' },
    { label: 'EQUITY', sub: 'शेयर', pct: 30, color: '#2D6A4F' },
    { label: 'BONDS', sub: 'बॉन्ड्स', pct: 15, color: '#9E2A2B' },
    { label: 'CASH', sub: 'नकद', pct: 15, color: '#5C4033' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#D2E3C8', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(0,21,36,0.06) 0%, transparent 70%)',
      }} />

      {/* Hex pattern */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="hex5" width="80" height="70" patternUnits="userSpaceOnUse">
            <polygon points="40,5 75,25 75,55 40,75 5,55 5,25" fill="none" stroke="#001524" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hex5)" />
      </svg>

      {/* Floating shapes */}
      {[0, 1, 2, 3].map((i) => {
        const size = 200 + i * 80;
        const x = (200 + i * 500 + Math.sin(frame * 0.02 + i) * 40);
        const y = 100 + Math.cos(frame * 0.025 + i) * 30;
        return (
          <div key={`fl-${i}`} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: size,
            height: size,
            border: '2px solid #001524',
            borderRadius: i % 2 === 0 ? '20%' : '50%',
            opacity: 0.05,
            transform: `rotate(${frame * 0.1 + i * 30}deg)`,
          }} />
        );
      })}

      {/* MAIN */}
      <div style={{
        position: 'absolute',
        inset: 0,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        padding: '60px 100px',
      }}>
        {/* Heading */}
        <div style={{
          fontFamily: 'Inter',
          fontSize: 32,
          fontWeight: 700,
          color: '#001524',
          letterSpacing: 8,
          opacity: sp(10) * 0.7,
          marginBottom: 15,
        }}>
          STRATEGY :: DIVERSIFY
        </div>

        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 130,
          fontWeight: 900,
          color: '#001524',
          opacity: sp(20),
          marginBottom: 60,
          transform: `translateY(${interpolate(sp(20), [0, 1], [-30, 0])}px)`,
        }}>
          एक नहीं, अनेक
        </div>

        {/* Bar chart */}
        <div style={{
          display: 'flex',
          alignItems: 'flex-end',
          gap: 30,
          height: 380,
        }}>
          {assets.map((a, i) => {
            const barSp = sp(40 + i * 15);
            const barHeight = barSp * (a.pct * 11);
            return (
              <div key={`a-${i}`} style={{
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                gap: 12,
              }}>
                {/* Percent label */}
                <div style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 42,
                  fontWeight: 800,
                  color: '#001524',
                  opacity: barSp,
                }}>
                  {a.pct}%
                </div>
                {/* Bar */}
                <div style={{
                  width: 180,
                  height: barHeight,
                  backgroundColor: a.color,
                  borderRadius: '12px 12px 0 0',
                  boxShadow: `0 8px 0 ${a.color === '#001524' ? '#000' : '#001524'}, inset 0 4px 0 rgba(255,255,255,0.15)`,
                  position: 'relative',
                }}>
                  {/* breathing highlight */}
                  <div style={{
                    position: 'absolute',
                    top: 10,
                    left: 10,
                    right: 10,
                    height: 6,
                    backgroundColor: 'rgba(255,255,255,0.3)',
                    opacity: 0.5 + Math.sin(frame * 0.1 + i) * 0.3,
                  }} />
                </div>
                {/* Label */}
                <div style={{
                  fontFamily: 'Inter',
                  fontSize: 22,
                  fontWeight: 800,
                  color: '#001524',
                  letterSpacing: 2,
                  opacity: barSp,
                  marginTop: 8,
                }}>
                  {a.label}
                </div>
                <div style={{
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: 26,
                  fontWeight: 700,
                  color: '#001524',
                  opacity: barSp * 0.8,
                }}>
                  {a.sub}
                </div>
              </div>
            );
          })}
        </div>

        {/* Bottom tagline */}
        <div style={{
          marginTop: 50,
          padding: '16px 40px',
          backgroundColor: '#001524',
          color: '#FFC107',
          fontFamily: 'Yantra One',
          fontSize: 48,
          fontWeight: 800,
          opacity: sp(160),
          transform: `scale(${sp(160)})`,
        }}>
          टोकरी अलग रखो
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,21,36,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene5Diversify;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6Allocation',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6Allocation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pie chart progress
  const pieProgress = interpolate(frame, [30, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const slices = [
    { label: 'GOLD', sub: 'सोना', val: 20, color: '#FFC107' },
    { label: 'REAL ESTATE', sub: 'प्रॉपर्टी', val: 25, color: '#5C4033' },
    { label: 'EQUITY', sub: 'शेयर', val: 30, color: '#001524' },
    { label: 'FD/BONDS', sub: 'बॉन्ड्स', val: 15, color: '#2D6A4F' },
    { label: 'CASH', sub: 'नकद', val: 10, color: '#9E2A2B' },
  ];

  // build SVG arcs
  let cumulative = 0;
  const cx = 300;
  const cy = 300;
  const r = 240;
  const arcs = slices.map((s, i) => {
    const start = (cumulative / 100) * 360;
    const end = ((cumulative + s.val) / 100) * 360 * pieProgress + start * (1 - pieProgress);
    const actualEnd = (cumulative / 100) * 360 + (s.val / 100) * 360 * pieProgress;
    cumulative += s.val;
    const startRad = (start - 90) * Math.PI / 180;
    const endRad = (actualEnd - 90) * Math.PI / 180;
    const x1 = cx + r * Math.cos(startRad);
    const y1 = cy + r * Math.sin(startRad);
    const x2 = cx + r * Math.cos(endRad);
    const y2 = cy + r * Math.sin(endRad);
    const largeArc = (actualEnd - start) > 180 ? 1 : 0;
    const path = `M ${cx} ${cy} L ${x1} ${y1} A ${r} ${r} 0 ${largeArc} 1 ${x2} ${y2} Z`;
    return { path, color: s.color, label: s.label, sub: s.sub, val: s.val };
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#D2E3C8', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(120deg, rgba(255,193,7,0.06) 0%, transparent 50%, rgba(0,21,36,0.06) 100%)',
      }} />

      {/* Orbital ring shapes */}
      {[0, 1, 2].map((i) => {
        const size = 700 + i * 250;
        return (
          <div key={`o-${i}`} style={{
            position: 'absolute',
            left: 200,
            top: 200,
            width: size,
            height: size,
            border: '2px dashed #001524',
            borderRadius: '50%',
            opacity: 0.06,
            transform: `rotate(${frame * (0.15 - i * 0.05)}deg)`,
          }} />
        );
      })}

      {/* Cross-hatch texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="1920" height="1080">
        <defs>
          <pattern id="cross6" width="40" height="40" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="40" y2="40" stroke="#001524" strokeWidth="0.8" />
            <line x1="40" y1="0" x2="0" y2="40" stroke="#001524" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross6)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = seededRandom(i + 1) * 1920;
        const py = (seededRandom(i + 2) * 1080 + frame * 0.4) % 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 4,
            height: 4,
            backgroundColor: '#001524',
            borderRadius: '50%',
            opacity: 0.3,
          }} />
        );
      })}

      <div style={{
        position: 'absolute',
        inset: 0,
        display: 'flex',
        alignItems: 'center',
        padding: '0 100px',
      }}>
        {/* LEFT - Pie chart */}
        <div style={{
          width: '45%',
          display: 'flex',
          justifyContent: 'center',
          transform: `scale(${sp(10)})`,
          opacity: sp(10),
        }}>
          <svg width="600" height="600" viewBox="0 0 600 600">
            {arcs.map((arc, i) => (
              <path key={`arc-${i}`} d={arc.path} fill={arc.color} stroke="#D2E3C8" strokeWidth="6" />
            ))}
            {/* Center circle */}
            <circle cx={cx} cy={cy} r="100" fill="#D2E3C8" />
            <text x={cx} y={cy - 10} textAnchor="middle" fill="#001524" fontSize="32" fontWeight="900" fontFamily="Inter">PORTFOLIO</text>
            <text x={cx} y={cy + 30} textAnchor="middle" fill="#001524" fontSize="48" fontWeight="900" fontFamily="JetBrains Mono">100%</text>
          </svg>
        </div>

        {/* RIGHT - Legend with rows */}
        <div style={{ width: '55%', paddingLeft: 40 }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 90,
            fontWeight: 900,
            color: '#001524',
            marginBottom: 30,
            opacity: sp(20),
          }}>
            स्ट्रक्चर्ड एलोकेशन
          </div>

          {slices.map((s, i) => {
            const rowSp = sp(60 + i * 18);
            return (
              <div key={`row-${i}`} style={{
                display: 'flex',
                alignItems: 'center',
                gap: 25,
                marginBottom: 18,
                opacity: rowSp,
                transform: `translateX(${interpolate(rowSp, [0, 1], [80, 0])}px)`,
              }}>
                {/* Color block */}
                <div style={{
                  width: 60,
                  height: 60,
                  backgroundColor: s.color,
                  border: '3px solid #001524',
                }} />
                {/* Label */}
                <div style={{ flex: 1 }}>
                  <div style={{
                    fontFamily: 'Inter',
                    fontSize: 30,
                    fontWeight: 800,
                    color: '#001524',
                    letterSpacing: 2,
                  }}>
                    {s.label}
                  </div>
                  <div style={{
                    fontFamily: 'Noto Sans Devanagari',
                    fontSize: 28,
                    fontWeight: 600,
                    color: '#001524',
                    opacity: 0.7,
                  }}>
                    {s.sub}
                  </div>
                </div>
                {/* Percent */}
                <div style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 56,
                  fontWeight: 900,
                  color: '#001524',
                  minWidth: 120,
                  textAlign: 'right',
                }}>
                  {s.val}%
                </div>
              </div>
            );
          })}
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,21,36,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene6Allocation;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7Subscribe',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7Subscribe: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // pulse on subscribe
  const pulse = 1 + Math.sin(frame * 0.12) * 0.05;

  return (
    <AbsoluteFill style={{ backgroundColor: '#D2E3C8', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(255,193,7,0.15) 0%, transparent 60%)',
      }} />

      {/* Rotating star burst */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: '50%',
        transform: `translate(-50%, -50%) rotate(${frame * 0.5}deg)`,
        opacity: 0.08,
      }}>
        <svg width="1400" height="1400" viewBox="-700 -700 1400 1400">
          {Array.from({ length: 24 }).map((_, i) => {
            const angle = (i / 24) * 360;
            const rad = angle * Math.PI / 180;
            return (
              <line
                key={`ray-${i}`}
                x1="0"
                y1="0"
                x2={Math.cos(rad) * 700}
                y2={Math.sin(rad) * 700}
                stroke="#001524"
                strokeWidth="3"
              />
            );
          })}
        </svg>
      </div>

      {/* Grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="1920" height="1080">
        <defs>
          <pattern id="g7" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M 50 0 L 0 0 0 50" fill="none" stroke="#001524" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#g7)" />
      </svg>

      {/* Sparkle particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = seededRandom(i + 5);
        const px = seededRandom(i + 10) * 1920;
        const py = seededRandom(i + 20) * 1080;
        const tw = 0.3 + Math.sin(frame * 0.1 + i * 0.5) * 0.4;
        return (
          <div key={`sp-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 4 + seed * 6,
            height: 4 + seed * 6,
            backgroundColor: '#FFC107',
            borderRadius: '50%',
            opacity: tw,
            boxShadow: '0 0 10px rgba(255,193,7,0.5)',
          }} />
        );
      })}

      {/* MAIN */}
      <div style={{
        position: 'absolute',
        inset: 0,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
      }}>
        {/* Channel name */}
        <div style={{
          fontFamily: 'Inter',
          fontSize: 38,
          fontWeight: 700,
          color: '#001524',
          letterSpacing: 10,
          opacity: sp(10) * 0.7,
          marginBottom: 20,
        }}>
          /// JOIN THE CIRCLE
        </div>

        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 180,
          fontWeight: 900,
          color: '#001524',
          opacity: sp(20),
          transform: `scale(${sp(20)})`,
          marginBottom: 30,
          letterSpacing: 2,
        }}>
          ProBuffet HQ
        </div>

        {/* Subscribe button */}
        <div style={{
          padding: '30px 80px',
          backgroundColor: '#ff0000',
          color: '#F5F5F5',
          fontFamily: 'Inter',
          fontSize: 64,
          fontWeight: 900,
          letterSpacing: 4,
          borderRadius: 12,
          opacity: sp(50),
          transform: `scale(${sp(50) * pulse})`,
          boxShadow: '0 12px 0 #001524, 0 16px 30px rgba(0,0,0,0.3)',
          marginBottom: 40,
        }}>
          ▶ SUBSCRIBE
        </div>

        {/* Tag line */}
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 76,
          fontWeight: 800,
          color: '#001524',
          opacity: sp(100),
          textAlign: 'center',
        }}>
          जागरूक निवेशक =
        </div>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 100,
          fontWeight: 900,
          color: '#FFC107',
          opacity: sp(140),
          marginTop: 10,
          textShadow: '3px 3px 0 #001524',
          transform: `translateY(${interpolate(sp(140), [0, 1], [30, 0])}px)`,
        }}>
          सुरक्षित निवेशक
        </div>

        {/* Side stats */}
        <div style={{
          display: 'flex',
          gap: 60,
          marginTop: 50,
          opacity: sp(180),
        }}>
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, fontWeight: 700, color: '#001524', opacity: 0.7 }}>WEEKLY</div>
            <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 800, color: '#001524' }}>FINANCE TRUTHS</div>
          </div>
          <div style={{ width: 2, backgroundColor: '#001524', opacity: 0.3 }} />
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, fontWeight: 700, color: '#001524', opacity: 0.7 }}>ZERO</div>
            <div style={{ fontFamily: 'Inter', fontSize: 32, fontWeight: 800, color: '#001524' }}>SUGAR-COATING</div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 45%, rgba(0,21,36,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene7Subscribe;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8Just72Hours',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8Just72Hours: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Clock progression
  const clockSweep = interpolate(frame, [40, 220], [0, 360], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#D2E3C8', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(0,21,36,0.04) 0%, transparent 50%, rgba(255,0,0,0.06) 100%)',
      }} />

      {/* Big offset circles */}
      {[0, 1, 2].map((i) => (
        <div key={`bg-${i}`} style={{
          position: 'absolute',
          left: -200 + i * 500,
          top: -200 + i * 100,
          width: 800,
          height: 800,
          border: '3px solid #001524',
          borderRadius: '50%',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.05})`,
        }} />
      ))}

      {/* Ticking lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="t8" width="80" height="20" patternUnits="userSpaceOnUse">
            <line x1="0" y1="10" x2="60" y2="10" stroke="#001524" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#t8)" />
      </svg>

      {/* Particles drifting right */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = (seededRandom(i + 1) * 1920 + frame * 1.2) % 1920;
        const py = seededRandom(i + 2) * 1080;
        return (
          <div key={`pa-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 4,
            height: 4,
            backgroundColor: '#ff0000',
            borderRadius: '50%',
            opacity: 0.4,
          }} />
        );
      })}

      <div style={{
        position: 'absolute',
        inset: 0,
        display: 'flex',
        alignItems: 'center',
        padding: '0 100px',
      }}>
        {/* LEFT - Clock visual */}
        <div style={{
          width: '40%',
          display: 'flex',
          justifyContent: 'center',
          opacity: sp(10),
          transform: `scale(${sp(10)})`,
        }}>
          <svg width="500" height="500" viewBox="-250 -250 500 500">
            {/* outer ring */}
            <circle cx="0" cy="0" r="220" fill="none" stroke="#001524" strokeWidth="8" />
            <circle cx="0" cy="0" r="220" fill="#D2E3C8" />
            {/* Hour markers */}
            {Array.from({ length: 12 }).map((_, i) => {
              const a = (i / 12) * Math.PI * 2 - Math.PI / 2;
              return (
                <line
                  key={`hm-${i}`}
                  x1={Math.cos(a) * 200}
                  y1={Math.sin(a) * 200}
                  x2={Math.cos(a) * 220}
                  y2={Math.sin(a) * 220}
                  stroke="#001524"
                  strokeWidth="5"
                />
              );
            })}
            {/* Progress arc */}
            <path
              d={`M 0 -210 A 210 210 0 ${clockSweep > 180 ? 1 : 0} 1 ${Math.cos((clockSweep - 90) * Math.PI / 180) * 210} ${Math.sin((clockSweep - 90) * Math.PI / 180) * 210}`}
              fill="none"
              stroke="#ff0000"
              strokeWidth="14"
              strokeLinecap="round"
            />
            {/* Center text */}
            <text x="0" y="-20" textAnchor="middle" fill="#001524" fontSize="36" fontWeight="700" fontFamily="Inter">PHASE 01</text>
            <text x="0" y="50" textAnchor="middle" fill="#ff0000" fontSize="120" fontWeight="900" fontFamily="JetBrains Mono">72H</text>
            <text x="0" y="100" textAnchor="middle" fill="#001524" fontSize="28" fontWeight="700" fontFamily="Inter">OF MANY</text>
          </svg>
        </div>

        {/* RIGHT - Text */}
        <div style={{ width: '60%', paddingLeft: 60 }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 34,
            fontWeight: 700,
            color: '#ff0000',
            letterSpacing: 8,
            opacity: sp(20),
            marginBottom: 25,
          }}>
            // ABHI TOH SHURUAAT HAI
          </div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 140,
            fontWeight: 900,
            color: '#001524',
            lineHeight: 1,
            opacity: sp(40),
            transform: `translateX(${interpolate(sp(40), [0, 1], [60, 0])}px)`,
          }}>
            72 घंटे
          </div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 90,
            fontWeight: 700,
            color: '#001524',
            opacity: sp(80),
            marginTop: 15,
          }}>
            सिर्फ शुरुआत है
          </div>

          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 110,
            fontWeight: 900,
            color: '#ff0000',
            opacity: sp(130),
            marginTop: 25,
            textShadow: '4px 4px 0 #001524',
            transform: `translateY(${interpolate(sp(130), [0, 1], [40, 0])}px)`,
          }}>
            अंत नहीं
          </div>

          {/* Bottom accent */}
          <div style={{
            display: 'flex',
            gap: 12,
            marginTop: 40,
            opacity: sp(170),
          }}>
            {Array.from({ length: 7 }).map((_, i) => (
              <div key={`bx-${i}`} style={{
                width: 60,
                height: 14,
                backgroundColor: i === 0 ? '#ff0000' : '#001524',
                opacity: i === 0 ? 1 : 0.3,
              }} />
            ))}
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 40%, rgba(0,21,36,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene8Just72Hours;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9SystemBreaks',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9SystemBreaks: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // glitch
  const glitch = frame > 150 ? Math.sin(frame * 2) * 4 : 0;

  // Network connection broken
  const breakProgress = interpolate(frame, [120, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const nodes = [
    { x: 300, y: 250, label: 'BANK', sub: 'बैंक' },
    { x: 960, y: 180, label: 'UPI', sub: 'यूपीआई' },
    { x: 1620, y: 250, label: 'WALLET', sub: 'वॉलेट' },
    { x: 300, y: 700, label: 'EMI', sub: 'ईएमआई' },
    { x: 960, y: 800, label: 'CARDS', sub: 'कार्ड्स' },
    { x: 1620, y: 700, label: 'FASTAG', sub: 'फास्टैग' },
  ];
  const center = { x: 960, y: 490 };

  return (
    <AbsoluteFill style={{ backgroundColor: '#D2E3C8', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(255,0,0,0.08) 0%, transparent 60%)',
      }} />

      {/* Hex floating shapes */}
      {[0, 1, 2, 3].map((i) => {
        const size = 250 + i * 100;
        const x = seededRandom(i + 1) * 1700;
        const y = seededRandom(i + 2) * 900;
        return (
          <div key={`h-${i}`} style={{
            position: 'absolute',
            left: x,
            top: y,
            width: size,
            height: size,
            border: '2px solid #001524',
            opacity: 0.05,
            transform: `rotate(${frame * 0.15 + i * 30}deg)`,
            borderRadius: '15%',
          }} />
        );
      })}

      {/* Network grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="1920" height="1080">
        <defs>
          <pattern id="n9" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#001524" strokeWidth="0.6" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#n9)" />
      </svg>

      {/* MAIN: Network diagram */}
      <div style={{
        position: 'absolute',
        inset: 0,
        transform: `translate(${glitch}px, 0)`,
      }}>
        {/* Title at top */}
        <div style={{
          position: 'absolute',
          top: 40,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: sp(10),
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 30,
            fontWeight: 700,
            color: '#ff0000',
            letterSpacing: 8,
            marginBottom: 8,
          }}>
            // SYSTEM_STATUS :: DOWN
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 80,
            fontWeight: 900,
            color: '#001524',
          }}>
            पूरा सिस्टम टूटता है
          </div>
        </div>

        {/* SVG connections */}
        <svg style={{ position: 'absolute', inset: 0 }} width="1920" height="1080">
          {nodes.map((n, i) => {
            const lineSp = sp(40 + i * 10);
            const x2 = n.x + (center.x - n.x) * lineSp;
            const y2 = n.y + (center.y - n.y) * lineSp;
            const isBroken = breakProgress > 0;
            // Draw two halves for break effect
            const midX = (n.x + center.x) / 2;
            const midY = (n.y + center.y) / 2;
            return (
              <g key={`l-${i}`}>
                {!isBroken ? (
                  <line x1={n.x} y1={n.y} x2={x2} y2={y2} stroke="#001524" strokeWidth="3" />
                ) : (
                  <>
                    <line x1={n.x} y1={n.y} x2={midX - breakProgress * 20} y2={midY - breakProgress * 10} stroke="#001524" strokeWidth="3" strokeDasharray="8 6" opacity={1 - breakProgress * 0.5} />
                    <line x1={midX + breakProgress * 20} y1={midY + breakProgress * 10} x2={center.x} y2={center.y} stroke="#001524" strokeWidth="3" strokeDasharray="8 6" opacity={1 - breakProgress * 0.5} />
                    {/* spark at break point */}
                    <circle cx={midX} cy={midY} r={6 + breakProgress * 8} fill="#ff0000" opacity={breakProgress} />
                  </>
                )}
              </g>
            );
          })}
        </svg>

        {/* Center node */}
        <div style={{
          position: 'absolute',
          left: center.x - 130,
          top: center.y - 130,
          width: 260,
          height: 260,
          borderRadius: '50%',
          backgroundColor: breakProgress > 0.5 ? '#ff0000' : '#001524',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          transform: `scale(${sp(20)})`,
          boxShadow: breakProgress > 0.5 ? '0 0 60px rgba(255,0,0,0.6)' : '0 12px 30px rgba(0,21,36,0.3)',
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 30,
            fontWeight: 800,
            color: '#FFC107',
            letterSpacing: 4,
          }}>
            CORE
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 56,
            fontWeight: 900,
            color: '#FFC107',
            marginTop: 5,
          }}>
            बैंकिंग
          </div>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 26,
            fontWeight: 700,
            color: '#FFC107',
            marginTop: 5,
            opacity: 0.8,
          }}>
            SYSTEM
          </div>
        </div>

        {/* Node boxes */}
        {nodes.map((n, i) => {
          const nodeSp = sp(50 + i * 15);
          return (
            <div key={`n-${i}`} style={{
              position: 'absolute',
              left: n.x - 110,
              top: n.y - 70,
              width: 220,
              height: 140,
              backgroundColor: '#D2E3C8',
              border: `4px solid ${breakProgress > 0.5 ? '#ff0000' : '#001524'}`,
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              transform: `scale(${nodeSp})`,
              boxShadow: '0 8px 0 #001524',
            }}>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 30,
                fontWeight: 900,
                color: '#001524',
                letterSpacing: 2,
              }}>
                {n.label}
              </div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 28,
                fontWeight: 700,
                color: '#001524',
                opacity: 0.75,
              }}>
                {n.sub}
              </div>
              {breakProgress > 0.7 && (
                <div style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 20,
                  fontWeight: 800,
                  color: '#ff0000',
                  marginTop: 4,
                  letterSpacing: 2,
                }}>
                  ✕ OFFLINE
                </div>
              )}
            </div>
          );
        })}
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 40%, rgba(0,21,36,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene9SystemBreaks;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10AllFail',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10AllFail: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const services = [
    { name: 'UPI', sub: 'पेमेंट', delay: 20 },
    { name: 'Google Pay', sub: 'गूगल पे', delay: 35 },
    { name: 'PhonePe', sub: 'फोनपे', delay: 50 },
    { name: 'Paytm', sub: 'पेटीएम', delay: 65 },
    { name: 'FASTag', sub: 'फास्टैग', delay: 80 },
    { name: 'EMI', sub: 'ईएमआई', delay: 95 },
    { name: 'AUTO-DEBIT', sub: 'बिल भुगतान', delay: 110 },
    { name: 'CARDS', sub: 'कार्ड्स', delay: 125 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#D2E3C8', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(255,0,0,0.06) 0%, transparent 50%, rgba(0,21,36,0.06) 100%)',
      }} />

      {/* Big X shape background */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.04 }} width="1920" height="1080">
        <line x1="0" y1="0" x2="1920" y2="1080" stroke="#001524" strokeWidth="80" />
        <line x1="1920" y1="0" x2="0" y2="1080" stroke="#001524" strokeWidth="80" />
      </svg>

      {/* Texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="1920" height="1080">
        <defs>
          <pattern id="b10" width="60" height="60" patternUnits="userSpaceOnUse">
            <rect x="0" y="0" width="60" height="60" fill="none" stroke="#001524" strokeWidth="0.5" />
            <circle cx="30" cy="30" r="2" fill="#001524" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#b10)" />
      </svg>

      {/* Falling particles like static */}
      {Array.from({ length: 40 }).map((_, i) => {
        const seed = seededRandom(i + 1);
        const px = seededRandom(i + 10) * 1920;
        const py = (seededRandom(i + 20) * 1080 + frame * (2 + seed * 3)) % 1080;
        return (
          <div key={`f-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 3,
            height: 8,
            backgroundColor: '#ff0000',
            opacity: 0.3,
          }} />
        );
      })}

      {/* MAIN */}
      <div style={{
        position: 'absolute',
        inset: 0,
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        padding: '60px 80px',
      }}>
        {/* Header */}
        <div style={{
          fontFamily: 'Inter',
          fontSize: 36,
          fontWeight: 800,
          color: '#ff0000',
          letterSpacing: 8,
          opacity: sp(0),
          marginBottom: 15,
        }}>
          // SYSTEM_ALERT
        </div>

        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 130,
          fontWeight: 900,
          color: '#001524',
          opacity: sp(10),
          marginBottom: 8,
          letterSpacing: 2,
        }}>
          सब कुछ ठप्प
        </div>

        <div style={{
          width: interpolate(sp(30), [0, 1], [0, 500]),
          height: 8,
          backgroundColor: '#ff0000',
          marginBottom: 40,
        }} />

        {/* Service grid */}
        <div style={{
          display: 'grid',
          gridTemplateColumns: 'repeat(4, 1fr)',
          gap: 24,
          width: '100%',
          maxWidth: 1600,
        }}>
          {services.map((s, i) => {
            const itemSp = sp(s.delay);
            const failed = frame > s.delay + 25;
            return (
              <div key={`s-${i}`} style={{
                backgroundColor: failed ? '#001524' : '#D2E3C8',
                border: `4px solid ${failed ? '#ff0000' : '#001524'}`,
                padding: '22px 18px',
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                justifyContent: 'center',
                height: 200,
                opacity: itemSp,
                transform: `scale(${itemSp}) rotate(${failed ? Math.sin(frame * 0.3 + i) * 1 : 0}deg)`,
                position: 'relative',
                boxShadow: '0 6px 0 #001524',
              }}>
                <div style={{
                  fontFamily: 'Inter',
                  fontSize: 36,
                  fontWeight: 900,
                  color: failed ? '#ff0000' : '#001524',
                  letterSpacing: 2,
                  textDecoration: failed ? 'line-through' : 'none',
                }}>
                  {s.name}
                </div>
                <div style={{
                  fontFamily: 'Noto Sans Devanagari',
                  fontSize: 26,
                  fontWeight: 700,
                  color: failed ? '#FFC107' : '#001524',
                  opacity: failed ? 0.8 : 0.7,
                  marginTop: 6,
                }}>
                  {s.sub}
                </div>
                <div style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 22,
                  fontWeight: 800,
                  color: failed ? '#ff0000' : '#001524',
                  marginTop: 12,
                  letterSpacing: 3,
                  padding: '4px 12px',
                  border: `2px solid ${failed ? '#ff0000' : '#001524'}`,
                  backgroundColor: failed ? '#D2E3C8' : 'transparent',
                }}>
                  {failed ? '✕ FAILED' : '● ACTIVE'}
                </div>
              </div>
            );
          })}
        </div>

        {/* Bottom warning */}
        <div style={{
          marginTop: 35,
          fontFamily: 'Yantra One',
          fontSize: 56,
          fontWeight: 800,
          color: '#001524',
          opacity: sp(170),
          textAlign: 'center',
        }}>
          एक धागे से बंधी पूरी ज़िंदगी
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 45%, rgba(0,21,36,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10AllFail;
```

