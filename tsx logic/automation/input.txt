```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter4Scene31',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter4Scene31: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const cameraScale = interpolate(frame, [0, durationInFrames], [1.02, 1.08], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const headlineIn = sp(5);
  const terminalIn = sp(18);
  const lineProgress = interpolate(frame, [35, 170], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 26 }, (_, index) => ({
    x: seededRandom(index + 31) * 100,
    y: seededRandom(index + 91) * 100,
    size: 2 + seededRandom(index + 151) * 5,
    drift: Math.sin(frame * 0.025 + index) * 10,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9E20',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 76% 25%, rgba(244,242,242,0.28), transparent 28%), linear-gradient(135deg, #FF9E20 0%, #F1841D 48%, #D85E28 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          width: 600,
          height: 600,
          borderRadius: '50%',
          right: -170,
          top: -170,
          border: '2px solid rgba(244,242,242,0.16)',
          background: 'rgba(244,242,242,0.05)',
          transform: `rotate(${frame * 0.05}deg) scale(${1 + Math.sin(frame * 0.03) * 0.04})`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          width: 430,
          height: 430,
          left: -170,
          bottom: -180,
          border: '2px solid rgba(29,33,40,0.14)',
          transform: `rotate(${-frame * 0.08}deg)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.24,
          backgroundImage:
            'linear-gradient(rgba(29,33,40,0.18) 1px, transparent 1px), linear-gradient(90deg, rgba(29,33,40,0.18) 1px, transparent 1px)',
          backgroundSize: '72px 72px',
          transform: `translateY(${Math.sin(frame * 0.02) * 8}px)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left: `${particle.x}%`,
            top: `${particle.y}%`,
            width: particle.size,
            height: particle.size,
            borderRadius: '50%',
            backgroundColor: '#F4F2F2',
            opacity: 0.18 + seededRandom(index + 201) * 0.35,
            transform: `translateY(${particle.drift}px)`,
          }}
        />
      ))}

      <div
        style={{
          position: 'absolute',
          inset: 0,
          transform: `scale(${cameraScale}) translateX(${Math.sin(frame * 0.018) * 7}px)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 100,
            color: '#1D2128',
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 30,
            fontWeight: 700,
            letterSpacing: 4,
            opacity: interpolate(headlineIn, [0, 1], [0, 1]),
          }}
        >
          KNOWLEDGE ECONOMY / 2026
        </div>

        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 205,
            transform: `translateY(${interpolate(headlineIn, [0, 1], [80, 0])}px)`,
          }}
        >
          <div
            style={{
              color: '#F4F2F2',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 118,
              lineHeight: 0.9,
              letterSpacing: -5,
            }}
          >
            CODING
          </div>
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 82,
              lineHeight: 1,
              letterSpacing: -3,
            }}
          >
            IS NOT THE MOAT
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 505,
            width: 560,
            color: '#1D2128',
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 45,
            lineHeight: 1.16,
            fontWeight: 700,
            opacity: interpolate(headlineIn, [0, 1], [0, 1]),
          }}
        >
          When machines understand language, every digital workflow becomes a target.
        </div>

        <div
          style={{
            position: 'absolute',
            right: 125,
            top: 160,
            width: 650,
            height: 620,
            backgroundColor: '#1D2128',
            borderRadius: 28,
            padding: 34,
            boxShadow: '24px 28px 0 rgba(29,33,40,0.18)',
            transform: `translateX(${interpolate(terminalIn, [0, 1], [180, 0])}px) rotate(${interpolate(terminalIn, [0, 1], [5, 0])}deg)`,
          }}
        >
          <div
            style={{
              display: 'flex',
              justifyContent: 'space-between',
              color: '#F4F2F2',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 28,
              letterSpacing: 2,
            }}
          >
            <span>LANGUAGE_INTERFACE</span>
            <span style={{ color: '#FF9E20' }}>ONLINE</span>
          </div>

          <svg width="580" height="250" viewBox="0 0 580 250" style={{ marginTop: 30 }}>
            <path
              d="M40 190 C130 180 155 125 240 130 S355 60 540 48"
              fill="none"
              stroke="#FF9E20"
              strokeWidth="8"
              strokeLinecap="round"
              strokeDasharray="700"
              strokeDashoffset={700 - lineProgress * 700}
            />
            <circle cx="40" cy="190" r="13" fill="#F4F2F2" />
            <circle cx="240" cy="130" r="13" fill="#F4F2F2" />
            <circle cx="540" cy="48" r="16" fill="#FF9E20" />
            <text x="20" y="230" fill="#F4F2F2" fontSize="24" fontFamily="JetBrains Mono">
              CODE
            </text>
            <text x="198" y="170" fill="#F4F2F2" fontSize="24" fontFamily="JetBrains Mono">
              PROMPT
            </text>
            <text x="455" y="35" fill="#FF9E20" fontSize="24" fontFamily="JetBrains Mono">
              OUTPUT
            </text>
          </svg>

          <div
            style={{
              marginTop: 12,
              color: '#F4F2F2',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 34,
              lineHeight: 1.55,
            }}
          >
            <div>
              <span style={{ color: '#FF9E20' }}>&gt;</span> translate_intent()
            </div>
            <div>
              <span style={{ color: '#FF9E20' }}>&gt;</span> build_solution()
            </div>
            <div>
              <span style={{ color: '#FF9E20' }}>&gt;</span> human_language = command
            </div>
          </div>

          <div
            style={{
              position: 'absolute',
              bottom: 28,
              left: 34,
              right: 34,
              height: 8,
              backgroundColor: 'rgba(244,242,242,0.16)',
              borderRadius: 8,
              overflow: 'hidden',
            }}
          >
            <div
              style={{
                width: `${lineProgress * 100}%`,
                height: '100%',
                backgroundColor: '#FF9E20',
                borderRadius: 8,
              }}
            />
          </div>
        </div>
      </div>

      <div
        style={{
          position: 'absolute',
          inset: 0,
          boxShadow: 'inset 0 0 180px rgba(29,33,40,0.34)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#1D2128',
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: 0.3,
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Chapter4Scene31;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter4Scene32',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter4Scene32: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const architectureIn = sp(8);
  const nodesIn = sp(26);
  const cameraX = interpolate(frame, [0, durationInFrames], [0, -22], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 24 }, (_, index) => ({
    x: seededRandom(index + 302) * 100,
    y: seededRandom(index + 402) * 100,
    size: 2 + seededRandom(index + 502) * 4,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9E20',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'linear-gradient(112deg, #FF9E20 0%, #F79B26 45%, #E46D25 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          width: 720,
          height: 720,
          right: -240,
          top: -250,
          border: '2px solid rgba(244,242,242,0.18)',
          transform: `rotate(${frame * 0.07}deg)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          width: 300,
          height: 300,
          left: 80,
          bottom: -150,
          backgroundColor: 'rgba(29,33,40,0.06)',
          clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
          transform: `rotate(${-frame * 0.12}deg)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.23,
          backgroundImage:
            'repeating-linear-gradient(0deg, transparent 0px, transparent 34px, rgba(29,33,40,0.2) 35px), repeating-linear-gradient(90deg, transparent 0px, transparent 34px, rgba(29,33,40,0.2) 35px)',
          transform: `translateX(${cameraX}px)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left: `${particle.x}%`,
            top: `${particle.y}%`,
            width: particle.size,
            height: particle.size,
            borderRadius: '50%',
            backgroundColor: '#F4F2F2',
            opacity: 0.2 + seededRandom(index + 602) * 0.3,
            transform: `translateY(${Math.sin(frame * 0.03 + index) * 12}px)`,
          }}
        />
      ))}

      <div
        style={{
          position: 'absolute',
          inset: 0,
          transform: `translateX(${cameraX}px) scale(${1 + frame / 5000})`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 130,
            top: 105,
            color: '#1D2128',
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 28,
            fontWeight: 700,
            letterSpacing: 3,
          }}
        >
          MACROECONOMIC ARCHITECTURE / SYSTEM VIEW
        </div>

        <div
          style={{
            position: 'absolute',
            left: 130,
            top: 210,
            width: 610,
            transform: `translateY(${interpolate(architectureIn, [0, 1], [70, 0])}px)`,
            opacity: interpolate(architectureIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#F4F2F2',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 92,
              lineHeight: 0.95,
              letterSpacing: -3,
            }}
          >
            NOT A GLITCH.
          </div>
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 78,
              lineHeight: 1,
              marginTop: 18,
            }}
          >
            A FEATURE.
          </div>
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'Inter, Poppins, sans-serif',
              fontSize: 43,
              lineHeight: 1.2,
              fontWeight: 700,
              marginTop: 42,
            }}
          >
            The system is being rebuilt around fewer workers, more compute, and a calmer public story.
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 110,
            top: 145,
            width: 790,
            height: 750,
            border: '3px solid rgba(29,33,40,0.72)',
            backgroundColor: 'rgba(244,242,242,0.12)',
            borderRadius: 32,
            padding: 34,
            transform: `scale(${interpolate(nodesIn, [0, 1], [0.82, 1])})`,
            opacity: interpolate(nodesIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 28,
              letterSpacing: 2,
              marginBottom: 32,
            }}
          >
            CONTROL LAYER / PUBLIC NARRATIVE
          </div>

          <svg width="720" height="550" viewBox="0 0 720 550">
            <path
              d="M90 110 L360 110 L360 260 L620 260"
              fill="none"
              stroke="#1D2128"
              strokeWidth="5"
              strokeDasharray="12 12"
            />
            <path
              d="M90 420 L360 420 L360 300 L620 300"
              fill="none"
              stroke="#1D2128"
              strokeWidth="5"
              strokeDasharray="12 12"
            />
            <path
              d="M360 110 L360 420"
              fill="none"
              stroke="#1D2128"
              strokeWidth="5"
              strokeDasharray="12 12"
            />

            <rect x="32" y="65" width="180" height="90" rx="16" fill="#1D2128" />
            <rect x="32" y="375" width="180" height="90" rx="16" fill="#1D2128" />
            <rect x="270" y="215" width="180" height="90" rx="16" fill="#FF9E20" stroke="#1D2128" strokeWidth="4" />
            <rect x="515" y="215" width="180" height="90" rx="16" fill="#F4F2F2" stroke="#1D2128" strokeWidth="4" />

            <text x="61" y="103" fill="#F4F2F2" fontSize="21" fontFamily="JetBrains Mono">
              COMPUTE
            </text>
            <text x="62" y="132" fill="#F4F2F2" fontSize="21" fontFamily="JetBrains Mono">
              POWER
            </text>

            <text x="61" y="414" fill="#F4F2F2" fontSize="21" fontFamily="JetBrains Mono">
              LABOR
            </text>
            <text x="61" y="443" fill="#F4F2F2" fontSize="21" fontFamily="JetBrains Mono">
              PRESSURE
            </text>

            <text x="301" y="253" fill="#1D2128" fontSize="21" fontFamily="JetBrains Mono">
              EFFICIENCY
            </text>
            <text x="312" y="282" fill="#1D2128" fontSize="21" fontFamily="JetBrains Mono">
              FILTER
            </text>

            <text x="542" y="253" fill="#1D2128" fontSize="21" fontFamily="JetBrains Mono">
              CALM
            </text>
            <text x="541" y="282" fill="#1D2128" fontSize="21" fontFamily="JetBrains Mono">
              STORY
            </text>

            <circle cx="360" cy="110" r="12" fill="#F4F2F2" />
            <circle cx="360" cy="420" r="12" fill="#F4F2F2" />
            <circle cx="360" cy="260" r="15" fill="#1D2128" />
          </svg>

          <div
            style={{
              color: '#1D2128',
              fontFamily: 'Inter, Poppins, sans-serif',
              fontSize: 32,
              fontWeight: 800,
              borderTop: '2px solid rgba(29,33,40,0.4)',
              paddingTop: 20,
            }}
          >
            The architecture optimizes output — not reassurance.
          </div>
        </div>
      </div>

      <div
        style={{
          position: 'absolute',
          inset: 0,
          boxShadow: 'inset 0 0 190px rgba(29,33,40,0.3)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#1D2128',
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: 0.3,
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Chapter4Scene32;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter4Scene33',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter4Scene33: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const panelIn = sp(6);
  const chartIn = sp(22);
  const tickerX = interpolate(frame, [0, durationInFrames], [0, -420], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const bars = [0.68, 0.75, 0.78, 0.8, 0.82, 0.84];

  const particles = Array.from({ length: 25 }, (_, index) => ({
    x: seededRandom(index + 333) * 100,
    y: seededRandom(index + 433) * 100,
    size: 2 + seededRandom(index + 533) * 4,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9E20',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 20% 20%, rgba(244,242,242,0.2), transparent 30%), linear-gradient(145deg, #FF9E20, #E27723 72%, #C9552A)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          width: 500,
          height: 500,
          right: -220,
          bottom: -210,
          borderRadius: '50%',
          backgroundColor: 'rgba(29,33,40,0.08)',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: 55,
          top: 45,
          width: 170,
          height: 170,
          border: '3px solid rgba(244,242,242,0.25)',
          transform: `rotate(${frame * 0.2}deg)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.2,
          backgroundImage:
            'radial-gradient(rgba(29,33,40,0.35) 1px, transparent 1px)',
          backgroundSize: '24px 24px',
          transform: `translateX(${tickerX * 0.1}px)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left: `${particle.x}%`,
            top: `${particle.y}%`,
            width: particle.size,
            height: particle.size,
            borderRadius: '50%',
            backgroundColor: '#F4F2F2',
            opacity: 0.16 + seededRandom(index + 633) * 0.3,
            transform: `translate(${Math.sin(frame * 0.025 + index) * 10}px, ${Math.cos(frame * 0.02 + index) * 8}px)`,
          }}
        />
      ))}

      <div
        style={{
          position: 'absolute',
          inset: 0,
          transform: `scale(${1.02 + frame / 6000}) translateX(${Math.sin(frame * 0.015) * 6}px)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 110,
            top: 100,
            color: '#1D2128',
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 29,
            letterSpacing: 3,
            fontWeight: 700,
          }}
        >
          DATA DESK / PUBLIC SIGNAL
        </div>

        <div
          style={{
            position: 'absolute',
            left: 110,
            top: 220,
            color: '#F4F2F2',
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 86,
            lineHeight: 0.97,
            letterSpacing: -3,
            transform: `translateY(${interpolate(panelIn, [0, 1], [60, 0])}px)`,
            opacity: interpolate(panelIn, [0, 1], [0, 1]),
          }}
        >
          THE JOBPOCALYPSE?
          <div style={{ color: '#1D2128', fontSize: 76, marginTop: 16 }}>NOT YET, THEY SAY.</div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 110,
            top: 490,
            width: 540,
            color: '#1D2128',
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 42,
            lineHeight: 1.18,
            fontWeight: 700,
            opacity: interpolate(panelIn, [0, 1], [0, 1]),
          }}
        >
          Aggregate employment can look stable while the quality of work quietly deteriorates.
        </div>

        <div
          style={{
            position: 'absolute',
            right: 110,
            top: 145,
            width: 750,
            height: 700,
            padding: 36,
            backgroundColor: '#F4F2F2',
            borderRadius: 28,
            boxShadow: '22px 24px 0 rgba(29,33,40,0.18)',
            transform: `translateY(${interpolate(chartIn, [0, 1], [90, 0])}px)`,
            opacity: interpolate(chartIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 28,
              fontWeight: 700,
              letterSpacing: 2,
            }}
          >
            HEADLINE METRIC
          </div>

          <div
            style={{
              display: 'flex',
              alignItems: 'flex-end',
              gap: 20,
              height: 335,
              marginTop: 45,
              padding: '0 24px 0 18px',
              borderBottom: '4px solid #1D2128',
              borderLeft: '4px solid #1D2128',
            }}
          >
            {bars.map((value, index) => (
              <div
                key={index}
                style={{
                  flex: 1,
                  height: `${value * 100}%`,
                  backgroundColor: index === bars.length - 1 ? '#FF9E20' : '#1D2128',
                  transform: `scaleY(${interpolate(chartIn, [0, 1], [0.05, 1])})`,
                  transformOrigin: 'bottom',
                  transition: 'none',
                }}
              />
            ))}
          </div>

          <div
            style={{
              display: 'flex',
              justifyContent: 'space-between',
              color: '#1D2128',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              marginTop: 18,
            }}
          >
            <span>2019</span>
            <span>2020</span>
            <span>2021</span>
            <span>2022</span>
            <span>2023</span>
            <span>2026</span>
          </div>

          <div
            style={{
              marginTop: 42,
              padding: 24,
              borderRadius: 18,
              backgroundColor: '#1D2128',
              color: '#F4F2F2',
              fontFamily: 'Inter, Poppins, sans-serif',
              fontSize: 34,
              fontWeight: 800,
              lineHeight: 1.1,
            }}
          >
            “Employment is resilient.”
            <div
              style={{
                color: '#FF9E20',
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 22,
                marginTop: 12,
              }}
            >
              REPORT STATUS: PARTIAL VIEW
            </div>
          </div>
        </div>
      </div>

      <div
        style={{
          position: 'absolute',
          inset: 0,
          boxShadow: 'inset 0 0 180px rgba(29,33,40,0.32)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#1D2128',
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: 0.3,
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Chapter4Scene33;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter4Scene34',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter4Scene34: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const titleIn = sp(5);
  const graphIn = sp(16);
  const roleIn = sp(34);
  const camera = interpolate(frame, [0, durationInFrames], [1.03, 1.07], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 28 }, (_, index) => ({
    x: seededRandom(index + 734) * 100,
    y: seededRandom(index + 834) * 100,
    size: 2 + seededRandom(index + 934) * 5,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9E20',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'linear-gradient(125deg, #FF9E20 0%, #F59627 44%, #DE6A25 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: -100,
          top: 120,
          width: 480,
          height: 480,
          borderRadius: '50%',
          border: '3px solid rgba(244,242,242,0.18)',
          transform: `rotate(${frame * 0.06}deg) scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: 90,
          bottom: -130,
          width: 260,
          height: 260,
          backgroundColor: 'rgba(29,33,40,0.08)',
          clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
          transform: `rotate(${-frame * 0.15}deg)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.18,
          backgroundImage:
            'repeating-linear-gradient(45deg, rgba(29,33,40,0.35) 0px, rgba(29,33,40,0.35) 2px, transparent 2px, transparent 28px)',
          transform: `translateX(${Math.sin(frame * 0.02) * 12}px)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left: `${particle.x}%`,
            top: `${particle.y}%`,
            width: particle.size,
            height: particle.size,
            borderRadius: '50%',
            backgroundColor: '#F4F2F2',
            opacity: 0.18 + seededRandom(index + 1034) * 0.3,
            transform: `translateY(${Math.sin(frame * 0.028 + index) * 11}px)`,
          }}
        />
      ))}

      <div
        style={{
          position: 'absolute',
          inset: 0,
          transform: `scale(${camera}) translateX(${Math.sin(frame * 0.016) * 6}px)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 95,
            color: '#1D2128',
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 29,
            fontWeight: 700,
            letterSpacing: 3,
          }}
        >
          THE BALANCED ANGLE
        </div>

        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 190,
            transform: `translateY(${interpolate(titleIn, [0, 1], [80, 0])}px)`,
            opacity: interpolate(titleIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#F4F2F2',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 86,
              letterSpacing: -3,
              lineHeight: 0.98,
            }}
          >
            RESILIENT
          </div>
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 76,
              lineHeight: 1,
            }}
          >
            BUT UNEVEN.
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 430,
            width: 520,
            color: '#1D2128',
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 42,
            lineHeight: 1.2,
            fontWeight: 700,
          }}
        >
          New labels appear on the chart while old pathways disappear beneath it.
        </div>

        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 720,
            display: 'flex',
            gap: 16,
            transform: `translateY(${interpolate(roleIn, [0, 1], [50, 0])}px)`,
            opacity: interpolate(roleIn, [0, 1], [0, 1]),
          }}
        >
          {['PROMPT ENGINEER', 'AI ETHICIST'].map((role, index) => (
            <div
              key={role}
              style={{
                padding: '16px 22px',
                border: '3px solid #1D2128',
                borderRadius: 12,
                color: '#1D2128',
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 21,
                fontWeight: 700,
                backgroundColor: index === 0 ? '#F4F2F2' : '#FFC857',
              }}
            >
              {role}
            </div>
          ))}
        </div>

        <div
          style={{
            position: 'absolute',
            right: 120,
            top: 145,
            width: 770,
            height: 720,
            backgroundColor: 'rgba(244,242,242,0.92)',
            borderRadius: 30,
            padding: 38,
            boxShadow: '20px 24px 0 rgba(29,33,40,0.18)',
            transform: `translateY(${interpolate(graphIn, [0, 1], [100, 0])}px)`,
            opacity: interpolate(graphIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 26,
              letterSpacing: 2,
              fontWeight: 700,
            }}
          >
            JOB CATEGORY EXPANSION
          </div>

          <svg width="690" height="390" viewBox="0 0 690 390" style={{ marginTop: 40 }}>
            <path
              d="M48 320 L160 296 L245 300 L335 240 L430 256 L525 160 L642 105"
              fill="none"
              stroke="#1D2128"
              strokeWidth="6"
              strokeLinecap="round"
              strokeDasharray="850"
              strokeDashoffset={850 - graphIn * 850}
            />
            <path
              d="M48 320 L160 296 L245 300 L335 240 L430 256 L525 160 L642 105"
              fill="none"
              stroke="#FF9E20"
              strokeWidth="16"
              strokeLinecap="round"
              strokeDasharray="850"
              strokeDashoffset={850 - graphIn * 850}
              opacity={0.75}
            />

            {[48, 160, 245, 335, 430, 525, 642].map((x, index) => (
              <circle
                key={x}
                cx={x}
                cy={[320, 296, 300, 240, 256, 160, 105][index]}
                r={index > 4 ? 13 : 9}
                fill={index > 4 ? '#FF9E20' : '#1D2128'}
              />
            ))}

            <line x1="48" y1="340" x2="650" y2="340" stroke="#1D2128" strokeWidth="3" />
            <line x1="48" y1="48" x2="48" y2="340" stroke="#1D2128" strokeWidth="3" />

            <text x="500" y="75" fill="#1D2128" fontSize="22" fontFamily="JetBrains Mono">
              NEW LABELS
            </text>
            <text x="470" y="370" fill="#1D2128" fontSize="20" fontFamily="JetBrains Mono">
              TIME
            </text>
          </svg>

          <div
            style={{
              display: 'grid',
              gridTemplateColumns: '1fr 1fr',
              gap: 18,
              marginTop: 20,
            }}
          >
            <div
              style={{
                padding: 22,
                backgroundColor: '#1D2128',
                borderRadius: 16,
                color: '#F4F2F2',
                fontFamily: 'Inter, Poppins, sans-serif',
                fontSize: 29,
                fontWeight: 800,
              }}
            >
              Trust rises
            </div>
            <div
              style={{
                padding: 22,
                backgroundColor: '#FF9E20',
                borderRadius: 16,
                color: '#1D2128',
                fontFamily: 'Inter, Poppins, sans-serif',
                fontSize: 29,
                fontWeight: 800,
              }}
            >
              Visibility falls
            </div>
          </div>
        </div>
      </div>

      <div
        style={{
          position: 'absolute',
          inset: 0,
          boxShadow: 'inset 0 0 180px rgba(29,33,40,0.3)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#1D2128',
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: 0.3,
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Chapter4Scene34;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter4Scene35',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter4Scene35: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const titleIn = sp(7);
  const waveIn = sp(18);
  const documentIn = sp(32);
  const waveShift = interpolate(frame, [0, durationInFrames], [0, -95], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 22 }, (_, index) => ({
    x: seededRandom(index + 1135) * 100,
    y: seededRandom(index + 1235) * 100,
    size: 2 + seededRandom(index + 1335) * 4,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9E20',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'linear-gradient(180deg, #FF9E20 0%, #E98325 42%, #B64D31 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: -180,
          top: -160,
          width: 500,
          height: 500,
          borderRadius: '50%',
          border: '2px solid rgba(244,242,242,0.17)',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.04})`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.25,
          backgroundImage:
            'repeating-linear-gradient(170deg, transparent 0px, transparent 42px, rgba(29,33,40,0.28) 43px, transparent 45px)',
          transform: `translateY(${waveShift}px)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left: `${particle.x}%`,
            top: `${particle.y}%`,
            width: particle.size,
            height: particle.size,
            borderRadius: '50%',
            backgroundColor: '#F4F2F2',
            opacity: 0.15 + seededRandom(index + 1435) * 0.3,
            transform: `translateY(${Math.sin(frame * 0.035 + index) * 9}px)`,
          }}
        />
      ))}

      <div
        style={{
          position: 'absolute',
          inset: 0,
          transform: `translateX(${Math.sin(frame * 0.018) * 8}px)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 90,
            color: '#1D2128',
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 29,
            fontWeight: 700,
            letterSpacing: 3,
          }}
        >
          SURFACE DATA / HIDDEN CURRENT
        </div>

        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 205,
            transform: `translateY(${interpolate(titleIn, [0, 1], [70, 0])}px)`,
            opacity: interpolate(titleIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#F4F2F2',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 88,
              lineHeight: 0.92,
              letterSpacing: -4,
            }}
          >
            LOOK BELOW
          </div>
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 76,
              marginTop: 18,
            }}
          >
            THE SURFACE.
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 475,
            width: 520,
            color: '#1D2128',
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 42,
            lineHeight: 1.2,
            fontWeight: 700,
          }}
        >
          The headline travels on top. The consequences move underneath.
        </div>

        <div
          style={{
            position: 'absolute',
            left: 70,
            right: 40,
            bottom: -10,
            height: 360,
            transform: `translateY(${interpolate(waveIn, [0, 1], [130, 0])}px)`,
            opacity: interpolate(waveIn, [0, 1], [0, 1]),
          }}
        >
          <svg width="1840" height="350" viewBox="0 0 1840 350">
            <path
              d="M0 110 C130 20 230 215 380 110 S640 25 770 120 S1010 240 1160 115 S1400 20 1530 105 S1720 180 1840 90 L1840 350 L0 350 Z"
              fill="rgba(29,33,40,0.88)"
              transform={`translate(${waveShift}, ${Math.sin(frame * 0.05) * 8})`}
            />
            <path
              d="M0 110 C130 20 230 215 380 110 S640 25 770 120 S1010 240 1160 115 S1400 20 1530 105 S1720 180 1840 90"
              fill="none"
              stroke="#F4F2F2"
              strokeWidth="8"
              opacity="0.8"
            />
            <path
              d="M0 185 C180 105 260 310 450 200 S750 115 900 220 S1190 300 1350 185 S1630 130 1840 215"
              fill="none"
              stroke="#FF9E20"
              strokeWidth="13"
              opacity="0.9"
            />
          </svg>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 125,
            top: 120,
            width: 590,
            height: 260,
            padding: 28,
            backgroundColor: '#F4F2F2',
            borderRadius: 20,
            boxShadow: '18px 20px 0 rgba(29,33,40,0.2)',
            transform: `rotate(${interpolate(documentIn, [0, 1], [-7, -2])}deg) translateY(${interpolate(documentIn, [0, 1], [80, 0])}px)`,
            opacity: interpolate(documentIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 23,
              fontWeight: 700,
              letterSpacing: 1,
            }}
          >
            TERM SHEET / PROJECTION
          </div>
          <div
            style={{
              marginTop: 24,
              height: 3,
              backgroundColor: '#1D2128',
              width: '92%',
            }}
          />
          <div
            style={{
              marginTop: 24,
              display: 'grid',
              gridTemplateColumns: '1fr 1fr',
              gap: 18,
              color: '#1D2128',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 20,
              lineHeight: 1.6,
            }}
          >
            <span>VISIBLE_SIGNAL</span>
            <strong>STABLE</strong>
            <span>HIDDEN_CURRENT</span>
            <strong style={{ color: '#C4472D' }}>ACCELERATING</strong>
          </div>
        </div>
      </div>

      <div
        style={{
          position: 'absolute',
          inset: 0,
          boxShadow: 'inset 0 0 200px rgba(29,33,40,0.4)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#1D2128',
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: 0.3,
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Chapter4Scene35;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter4Scene36',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter4Scene36: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const chartIn = sp(8);
  const labelIn = sp(22);
  const lagPulse = interpolate(frame % 60, [0, 30, 60], [0.75, 1, 0.75], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 26 }, (_, index) => ({
    x: seededRandom(index + 1636) * 100,
    y: seededRandom(index + 1736) * 100,
    size: 2 + seededRandom(index + 1836) * 4,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9E20',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'linear-gradient(135deg, #FF9E20 0%, #F08A23 50%, #D05A2D 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: -170,
          top: -120,
          width: 510,
          height: 510,
          borderRadius: '50%',
          border: '2px solid rgba(244,242,242,0.2)',
          transform: `rotate(${frame * 0.04}deg)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: 90,
          bottom: -100,
          width: 260,
          height: 260,
          backgroundColor: 'rgba(29,33,40,0.08)',
          transform: `rotate(${frame * 0.12}deg)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.18,
          backgroundImage:
            'linear-gradient(90deg, transparent 49%, rgba(29,33,40,0.3) 50%, transparent 51%), linear-gradient(0deg, transparent 49%, rgba(29,33,40,0.3) 50%, transparent 51%)',
          backgroundSize: '58px 58px',
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.025})`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left: `${particle.x}%`,
            top: `${particle.y}%`,
            width: particle.size,
            height: particle.size,
            borderRadius: '50%',
            backgroundColor: '#F4F2F2',
            opacity: 0.15 + seededRandom(index + 1936) * 0.32,
            transform: `translate(${Math.sin(frame * 0.022 + index) * 9}px, ${Math.cos(frame * 0.027 + index) * 8}px)`,
          }}
        />
      ))}

      <div
        style={{
          position: 'absolute',
          inset: 0,
          transform: `translateX(${Math.sin(frame * 0.015) * 7}px) scale(${1.02 + frame / 7000})`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 100,
            color: '#1D2128',
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 29,
            fontWeight: 700,
            letterSpacing: 3,
          }}
        >
          LEADING SIGNAL / LAGGING REPORT
        </div>

        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 195,
            color: '#F4F2F2',
            fontFamily: 'Mont Heavy, sans-serif',
            fontSize: 86,
            lineHeight: 0.95,
            letterSpacing: -4,
          }}
        >
          THE NUMBER
          <div style={{ color: '#1D2128', fontSize: 78, marginTop: 16 }}>ARRIVES LATE.</div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 455,
            width: 480,
            color: '#1D2128',
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 42,
            lineHeight: 1.18,
            fontWeight: 700,
          }}
        >
          A stable unemployment rate can hide a collapsing career ladder.
        </div>

        <div
          style={{
            position: 'absolute',
            right: 110,
            top: 135,
            width: 800,
            height: 720,
            padding: 34,
            borderRadius: 28,
            backgroundColor: '#1D2128',
            boxShadow: '20px 24px 0 rgba(29,33,40,0.2)',
            transform: `translateY(${interpolate(chartIn, [0, 1], [100, 0])}px)`,
            opacity: interpolate(chartIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#F4F2F2',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 26,
              letterSpacing: 2,
            }}
          >
            MARKET DASHBOARD
          </div>

          <svg width="730" height="380" viewBox="0 0 730 380" style={{ marginTop: 32 }}>
            <path
              d="M40 265 C125 248 150 220 235 242 S360 260 430 207 S550 175 690 168"
              fill="none"
              stroke="#F4F2F2"
              strokeWidth="6"
              strokeDasharray="900"
              strokeDashoffset={900 - chartIn * 900}
            />
            <path
              d="M40 310 C140 300 180 275 255 294 S390 300 470 280 S570 245 690 230"
              fill="none"
              stroke="#FF9E20"
              strokeWidth="9"
              strokeDasharray="900"
              strokeDashoffset={900 - chartIn * 900}
            />

            <line x1="40" y1="335" x2="690" y2="335" stroke="#F4F2F2" strokeWidth="2" opacity="0.5" />
            <line x1="40" y1="50" x2="40" y2="335" stroke="#F4F2F2" strokeWidth="2" opacity="0.5" />

            <text x="55" y="82" fill="#F4F2F2" fontSize="21" fontFamily="JetBrains Mono">
              VISIBLE RATE
            </text>
            <text x="55" y="110" fill="#FF9E20" fontSize="21" fontFamily="JetBrains Mono">
              JOB QUALITY
            </text>
            <text x="566" y="365" fill="#F4F2F2" fontSize="20" fontFamily="JetBrains Mono">
              TIME
            </text>

            <line
              x1="480"
              y1="80"
              x2="480"
              y2="335"
              stroke="#FF9E20"
              strokeWidth="4"
              strokeDasharray="10 10"
            />
            <circle cx="480" cy="207" r="14" fill="#FF9E20" />
          </svg>

          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: 24,
              marginTop: 20,
              padding: 22,
              borderRadius: 18,
              backgroundColor: '#FF9E20',
              color: '#1D2128',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 27,
              fontWeight: 700,
              opacity: lagPulse,
            }}
          >
            <span style={{ fontSize: 42 }}>←</span>
            <span>REPORTING LAG: REALITY MOVES FIRST</span>
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            bottom: 95,
            right: 145,
            color: '#F4F2F2',
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 29,
            fontWeight: 800,
            opacity: interpolate(labelIn, [0, 1], [0, 1]),
          }}
        >
          STABLE ≠ SAFE
        </div>
      </div>

      <div
        style={{
          position: 'absolute',
          inset: 0,
          boxShadow: 'inset 0 0 185px rgba(29,33,40,0.34)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#1D2128',
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: 0.3,
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Chapter4Scene36;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter4Scene37',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter4Scene37: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const titleIn = sp(6);
  const stairIn = sp(20);
  const scramble = interpolate(frame, [0, durationInFrames], [0, 24], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 27 }, (_, index) => ({
    x: seededRandom(index + 2037) * 100,
    y: seededRandom(index + 2137) * 100,
    size: 2 + seededRandom(index + 2237) * 5,
  }));

  const steps = [
    { label: 'TECH', value: '$180K', width: 650, color: '#1D2128' },
    { label: 'ADMIN', value: '$72K', width: 490, color: '#F4F2F2' },
    { label: 'GIG', value: 'VARIABLE', width: 330, color: '#FFC857' },
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9E20',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'linear-gradient(132deg, #FF9E20 0%, #F28A24 55%, #D45B2C 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: -160,
          top: -170,
          width: 560,
          height: 560,
          borderRadius: '50%',
          backgroundColor: 'rgba(244,242,242,0.06)',
          border: '2px solid rgba(244,242,242,0.15)',
          transform: `rotate(${frame * 0.05}deg)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: 20,
          bottom: 50,
          width: 240,
          height: 240,
          backgroundColor: 'rgba(29,33,40,0.08)',
          clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
          transform: `rotate(${-frame * 0.1}deg)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.22,
          backgroundImage:
            'repeating-linear-gradient(135deg, transparent 0px, transparent 44px, rgba(29,33,40,0.25) 45px, transparent 47px)',
          transform: `translateX(${Math.sin(frame * 0.018) * 12}px)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left: `${particle.x}%`,
            top: `${particle.y}%`,
            width: particle.size,
            height: particle.size,
            borderRadius: '50%',
            backgroundColor: '#F4F2F2',
            opacity: 0.16 + seededRandom(index + 2337) * 0.3,
            transform: `translate(${Math.sin(frame * 0.024 + index) * 10}px, ${Math.cos(frame * 0.02 + index) * 9}px)`,
          }}
        />
      ))}

      <div
        style={{
          position: 'absolute',
          inset: 0,
          transform: `translateX(${Math.sin(frame * 0.014) * 6}px)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 100,
            color: '#1D2128',
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 29,
            fontWeight: 700,
            letterSpacing: 3,
          }}
        >
          CAREER DISPLACEMENT / DOWNWARD MOBILITY
        </div>

        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 205,
            transform: `translateY(${interpolate(titleIn, [0, 1], [65, 0])}px)`,
            opacity: interpolate(titleIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#F4F2F2',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 84,
              lineHeight: 0.95,
              letterSpacing: -3,
            }}
          >
            THEY SCRAMBLE.
          </div>
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 74,
              marginTop: 18,
            }}
          >
            THEY DOWNGRADE.
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 475,
            width: 485,
            color: '#1D2128',
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 42,
            lineHeight: 1.2,
            fontWeight: 700,
          }}
        >
          A person can remain “employed” while their earning power falls three floors.
        </div>

        <div
          style={{
            position: 'absolute',
            right: 105,
            top: 135,
            width: 820,
            height: 760,
            backgroundColor: '#F4F2F2',
            borderRadius: 30,
            padding: 38,
            boxShadow: '20px 24px 0 rgba(29,33,40,0.18)',
            transform: `translateY(${interpolate(stairIn, [0, 1], [100, 0])}px)`,
            opacity: interpolate(stairIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 27,
              letterSpacing: 2,
              fontWeight: 700,
            }}
          >
            INCOME DESCENT MAP
          </div>

          <div
            style={{
              position: 'relative',
              height: 515,
              marginTop: 46,
              borderLeft: '4px solid #1D2128',
              borderBottom: '4px solid #1D2128',
            }}
          >
            {steps.map((step, index) => (
              <div
                key={step.label}
                style={{
                  position: 'absolute',
                  left: `${index * 55}px`,
                  bottom: `${index * 145}px`,
                  width: `${step.width}px`,
                  height: 112,
                  backgroundColor: step.color,
                  border: '4px solid #1D2128',
                  borderRadius: 16,
                  padding: '22px 28px',
                  color: step.color === '#1D2128' ? '#F4F2F2' : '#1D2128',
                  fontFamily: 'JetBrains Mono, monospace',
                  transform: `translate(${Math.sin(frame * 0.08 + index) * 4}px, ${Math.cos(frame * 0.05 + index) * 3}px)`,
                }}
              >
                <div style={{ fontSize: 25, fontWeight: 700 }}>{step.label}</div>
                <div style={{ fontSize: 35, fontWeight: 800, marginTop: 8 }}>{step.value}</div>
              </div>
            ))}

            <div
              style={{
                position: 'absolute',
                right: 24,
                top: 20,
                color: '#C4472D',
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 24,
                fontWeight: 700,
                transform: `translateX(${Math.sin(frame * 0.08) * scramble}px)`,
              }}
            >
              PAY CUT →
            </div>
          </div>

          <div
            style={{
              marginTop: 30,
              display: 'flex',
              justifyContent: 'space-between',
              alignItems: 'center',
              color: '#1D2128',
              fontFamily: 'Inter, Poppins, sans-serif',
              fontSize: 29,
              fontWeight: 800,
            }}
          >
            <span>TECH / FINANCE</span>
            <span style={{ color: '#C4472D' }}>ADMIN / FREELANCE</span>
          </div>
        </div>
      </div>

      <div
        style={{
          position: 'absolute',
          inset: 0,
          boxShadow: 'inset 0 0 185px rgba(29,33,40,0.32)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#1D2128',
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: 0.3,
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Chapter4Scene37;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter4Scene38',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter4Scene38: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const titleIn = sp(5);
  const gaugeIn = sp(18);
  const routeIn = sp(36);
  const drain = interpolate(frame, [25, 240], [0, 0.78], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 25 }, (_, index) => ({
    x: seededRandom(index + 2438) * 100,
    y: seededRandom(index + 2538) * 100,
    size: 2 + seededRandom(index + 2638) * 4,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9E20',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 72% 36%, rgba(244,242,242,0.2), transparent 25%), linear-gradient(140deg, #FF9E20, #E87B25 58%, #C95431)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: -180,
          top: -140,
          width: 510,
          height: 510,
          borderRadius: '50%',
          border: '2px solid rgba(244,242,242,0.16)',
          transform: `rotate(${frame * 0.06}deg)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: 60,
          bottom: -100,
          width: 250,
          height: 250,
          backgroundColor: 'rgba(29,33,40,0.08)',
          transform: `rotate(${-frame * 0.1}deg)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.2,
          backgroundImage:
            'radial-gradient(circle, rgba(29,33,40,0.4) 1px, transparent 1px)',
          backgroundSize: '34px 34px',
          transform: `translate(${Math.sin(frame * 0.018) * 9}px, ${Math.cos(frame * 0.016) * 7}px)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left: `${particle.x}%`,
            top: `${particle.y}%`,
            width: particle.size,
            height: particle.size,
            borderRadius: '50%',
            backgroundColor: '#F4F2F2',
            opacity: 0.15 + seededRandom(index + 2738) * 0.3,
            transform: `translateY(${Math.sin(frame * 0.03 + index) * 10}px)`,
          }}
        />
      ))}

      <div
        style={{
          position: 'absolute',
          inset: 0,
          transform: `translateX(${Math.sin(frame * 0.015) * 6}px) scale(${1.02 + frame / 7000})`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 100,
            color: '#1D2128',
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 29,
            fontWeight: 700,
            letterSpacing: 3,
          }}
        >
          HOUSE / RETIREMENT / CLASSIFICATION
        </div>

        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 205,
            transform: `translateY(${interpolate(titleIn, [0, 1], [70, 0])}px)`,
            opacity: interpolate(titleIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#F4F2F2',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 82,
              lineHeight: 0.95,
              letterSpacing: -3,
            }}
          >
            EMPLOYED
          </div>
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 74,
              marginTop: 16,
            }}
          >
            ON PAPER.
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 470,
            width: 500,
            color: '#1D2128',
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 42,
            lineHeight: 1.2,
            fontWeight: 700,
          }}
        >
          The label survives. The salary, savings, and security are drained away.
        </div>

        <div
          style={{
            position: 'absolute',
            right: 105,
            top: 140,
            width: 800,
            height: 720,
            backgroundColor: '#F4F2F2',
            borderRadius: 30,
            padding: 38,
            boxShadow: '20px 24px 0 rgba(29,33,40,0.18)',
            transform: `translateY(${interpolate(gaugeIn, [0, 1], [100, 0])}px)`,
            opacity: interpolate(gaugeIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 26,
              letterSpacing: 2,
              fontWeight: 700,
            }}
          >
            FINANCIAL RUNWAY
          </div>

          <div
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: 40,
              marginTop: 42,
            }}
          >
            <svg width="270" height="270" viewBox="0 0 270 270">
              <circle cx="135" cy="135" r="104" fill="none" stroke="#1D2128" strokeWidth="22" opacity="0.14" />
              <circle
                cx="135"
                cy="135"
                r="104"
                fill="none"
                stroke="#C4472D"
                strokeWidth="22"
                strokeLinecap="round"
                strokeDasharray="654"
                strokeDashoffset={654 - drain * 654}
                transform="rotate(-90 135 135)"
              />
              <text x="82" y="128" fill="#1D2128" fontSize="32" fontFamily="JetBrains Mono">
                401K
              </text>
              <text x="90" y="162" fill="#C4472D" fontSize="24" fontFamily="JetBrains Mono">
                DRAIN
              </text>
            </svg>

            <div
              style={{
                color: '#1D2128',
                fontFamily: 'Inter, Poppins, sans-serif',
                fontSize: 34,
                lineHeight: 1.18,
                fontWeight: 800,
                width: 360,
              }}
            >
              Savings become the bridge between income loss and the next bill.
            </div>
          </div>

          <div
            style={{
              marginTop: 40,
              padding: 24,
              borderRadius: 18,
              backgroundColor: '#1D2128',
              color: '#F4F2F2',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 25,
              lineHeight: 1.5,
            }}
          >
            <div>MARKETING DIRECTOR</div>
            <div style={{ color: '#FF9E20' }}>→ DELIVERY DRIVER</div>
            <div style={{ color: '#C4472D' }}>STATUS: EMPLOYED</div>
          </div>

          <div
            style={{
              marginTop: 28,
              height: 150,
              transform: `translateX(${interpolate(routeIn, [0, 1], [100, 0])}px)`,
              opacity: interpolate(routeIn, [0, 1], [0, 1]),
            }}
          >
            <svg width="720" height="150" viewBox="0 0 720 150">
              <path
                d="M25 112 C160 18 270 135 405 68 S575 28 700 92"
                fill="none"
                stroke="#1D2128"
                strokeWidth="5"
                strokeDasharray="12 10"
              />
              <circle cx="25" cy="112" r="11" fill="#1D2128" />
              <circle cx="700" cy="92" r="13" fill="#FF9E20" />
              <text x="12" y="145" fill="#1D2128" fontSize="20" fontFamily="JetBrains Mono">
                OFFICE
              </text>
              <text x="598" y="140" fill="#1D2128" fontSize="20" fontFamily="JetBrains Mono">
                GIG APP
              </text>
            </svg>
          </div>
        </div>
      </div>

      <div
        style={{
          position: 'absolute',
          inset: 0,
          boxShadow: 'inset 0 0 185px rgba(29,33,40,0.33)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#1D2128',
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: 0.3,
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Chapter4Scene38;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter4Scene39',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter4Scene39: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const titleIn = sp(5);
  const engineIn = sp(17);
  const dismantle = interpolate(frame, [80, 245], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 24 }, (_, index) => ({
    x: seededRandom(index + 2939) * 100,
    y: seededRandom(index + 3039) * 100,
    size: 2 + seededRandom(index + 3139) * 5,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9E20',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'linear-gradient(135deg, #FF9E20 0%, #E77A26 54%, #BF4D33 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: -170,
          top: -160,
          width: 500,
          height: 500,
          borderRadius: '50%',
          border: '2px solid rgba(244,242,242,0.17)',
          transform: `rotate(${frame * 0.07}deg)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: 65,
          bottom: -120,
          width: 310,
          height: 310,
          backgroundColor: 'rgba(29,33,40,0.08)',
          transform: `rotate(${-frame * 0.11}deg)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.22,
          backgroundImage:
            'linear-gradient(30deg, transparent 48%, rgba(29,33,40,0.28) 49%, transparent 50%), linear-gradient(150deg, transparent 48%, rgba(29,33,40,0.28) 49%, transparent 50%)',
          backgroundSize: '60px 60px',
          transform: `translateY(${Math.sin(frame * 0.02) * 9}px)`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left: `${particle.x}%`,
            top: `${particle.y}%`,
            width: particle.size,
            height: particle.size,
            borderRadius: '50%',
            backgroundColor: '#F4F2F2',
            opacity: 0.16 + seededRandom(index + 3239) * 0.3,
            transform: `translateY(${Math.sin(frame * 0.026 + index) * 10}px)`,
          }}
        />
      ))}

      <div
        style={{
          position: 'absolute',
          inset: 0,
          transform: `translateX(${Math.sin(frame * 0.016) * 6}px) scale(${1.02 + frame / 7000})`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 100,
            color: '#1D2128',
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 29,
            fontWeight: 700,
            letterSpacing: 3,
          }}
        >
          EMPLOYMENT NARRATIVE / WEALTH ENGINE
        </div>

        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 200,
            transform: `translateY(${interpolate(titleIn, [0, 1], [75, 0])}px)`,
            opacity: interpolate(titleIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#F4F2F2',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 84,
              letterSpacing: -4,
              lineHeight: 0.95,
            }}
          >
            EMPLOYED
          </div>
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 72,
              marginTop: 15,
            }}
          >
            DOES NOT MEAN SECURE.
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 465,
            width: 510,
            color: '#1D2128',
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 42,
            lineHeight: 1.18,
            fontWeight: 700,
          }}
        >
          The dashboard stays green while the middle-class wealth engine loses its parts.
        </div>

        <div
          style={{
            position: 'absolute',
            right: 95,
            top: 130,
            width: 830,
            height: 735,
            backgroundColor: '#1D2128',
            borderRadius: 30,
            padding: 36,
            boxShadow: '20px 24px 0 rgba(29,33,40,0.18)',
            transform: `translateY(${interpolate(engineIn, [0, 1], [100, 0])}px)`,
            opacity: interpolate(engineIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#F4F2F2',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 26,
              letterSpacing: 2,
            }}
          >
            WEALTH ENGINE / LIVE STATUS
          </div>

          <svg width="750" height="430" viewBox="0 0 750 430" style={{ marginTop: 30 }}>
            <g transform={`translate(375 210) rotate(${frame * 0.6})`}>
              <circle cx="0" cy="0" r="110" fill="none" stroke="#FF9E20" strokeWidth="32" strokeDasharray="26 20" />
              <circle cx="0" cy="0" r="70" fill="none" stroke="#F4F2F2" strokeWidth="18" strokeDasharray="12 18" />
              <circle cx="0" cy="0" r="30" fill="#FF9E20" />
            </g>

            <g
              transform={`translate(375 210) rotate(${-frame * 0.9})`}
              opacity={1 - dismantle * 0.45}
            >
              <rect x="-22" y="-175" width="44" height="350" rx="12" fill="#F4F2F2" />
              <rect x="-175" y="-22" width="350" height="44" rx="12" fill="#F4F2F2" />
            </g>

            <path
              d="M100 80 L250 80 L305 125"
              fill="none"
              stroke="#F4F2F2"
              strokeWidth="4"
              strokeDasharray="10 8"
            />
            <path
              d="M650 330 L500 330 L445 285"
              fill="none"
              stroke="#FF9E20"
              strokeWidth="4"
              strokeDasharray="10 8"
            />

            <text x="65" y="68" fill="#F4F2F2" fontSize="22" fontFamily="JetBrains Mono">
              WAGES
            </text>
            <text x="575" y="365" fill="#FF9E20" fontSize="22" fontFamily="JetBrains Mono">
              OWNERSHIP
            </text>
          </svg>

          <div
            style={{
              display: 'flex',
              justifyContent: 'space-between',
              alignItems: 'center',
              borderTop: '2px solid rgba(244,242,242,0.28)',
              paddingTop: 24,
              color: '#F4F2F2',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 24,
            }}
          >
            <span>OUTPUT: STABLE</span>
            <span style={{ color: '#FF9E20' }}>WEALTH SHARE: SHIFTING</span>
          </div>

          <div
            style={{
              marginTop: 22,
              height: 12,
              backgroundColor: 'rgba(244,242,242,0.16)',
              borderRadius: 12,
              overflow: 'hidden',
            }}
          >
            <div
              style={{
                width: `${(0.28 + dismantle * 0.42) * 100}%`,
                height: '100%',
                backgroundColor: '#C4472D',
                borderRadius: 12,
              }}
            />
          </div>
        </div>
      </div>

      <div
        style={{
          position: 'absolute',
          inset: 0,
          boxShadow: 'inset 0 0 185px rgba(29,33,40,0.36)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#1D2128',
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: 0.3,
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Chapter4Scene39;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Chapter4Scene40',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Chapter4Scene40: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const titleIn = sp(5);
  const spiralIn = sp(18);
  const cardIn = sp(36);
  const shake = frame > 190 ? Math.sin(frame * 1.5) * 3 : 0;
  const spiralRotation = interpolate(frame, [0, durationInFrames], [0, 32], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const particles = Array.from({ length: 26 }, (_, index) => ({
    x: seededRandom(index + 3040) * 100,
    y: seededRandom(index + 3140) * 100,
    size: 2 + seededRandom(index + 3240) * 5,
  }));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#FF9E20',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 82% 28%, rgba(244,242,242,0.2), transparent 28%), linear-gradient(140deg, #FF9E20 0%, #E77B25 55%, #B94935 100%)',
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: -170,
          top: -130,
          width: 480,
          height: 480,
          borderRadius: '50%',
          border: '2px solid rgba(244,242,242,0.16)',
          transform: `rotate(${frame * 0.05}deg)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: 55,
          bottom: -160,
          width: 340,
          height: 340,
          backgroundColor: 'rgba(29,33,40,0.1)',
          clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
          transform: `rotate(${-frame * 0.13}deg)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.2,
          backgroundImage:
            'repeating-radial-gradient(circle at 50% 50%, transparent 0px, transparent 42px, rgba(29,33,40,0.3) 44px, transparent 46px)',
          transform: `scale(${1 + Math.sin(frame * 0.025) * 0.03})`,
        }}
      />

      {particles.map((particle, index) => (
        <div
          key={index}
          style={{
            position: 'absolute',
            left: `${particle.x}%`,
            top: `${particle.y}%`,
            width: particle.size,
            height: particle.size,
            borderRadius: '50%',
            backgroundColor: '#F4F2F2',
            opacity: 0.15 + seededRandom(index + 3340) * 0.32,
            transform: `translate(${Math.sin(frame * 0.026 + index) * 10}px, ${Math.cos(frame * 0.024 + index) * 8}px)`,
          }}
        />
      ))}

      <div
        style={{
          position: 'absolute',
          inset: 0,
          transform: `translate(${shake}px, ${shake * 0.4}px) scale(${1.02 + frame / 7000})`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 100,
            color: '#1D2128',
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 29,
            fontWeight: 700,
            letterSpacing: 3,
          }}
        >
          FIXED COSTS / VARIABLE INCOME
        </div>

        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 195,
            transform: `translateY(${interpolate(titleIn, [0, 1], [75, 0])}px)`,
            opacity: interpolate(titleIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#F4F2F2',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 82,
              lineHeight: 0.93,
              letterSpacing: -4,
            }}
          >
            THE DEBT
          </div>
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'Mont Heavy, sans-serif',
              fontSize: 76,
              marginTop: 16,
            }}
          >
            SPIRAL.
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 460,
            width: 505,
            color: '#1D2128',
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 42,
            lineHeight: 1.19,
            fontWeight: 700,
          }}
        >
          When income falls, credit turns survival into a recurring fee.
        </div>

        <div
          style={{
            position: 'absolute',
            right: 95,
            top: 125,
            width: 830,
            height: 760,
            backgroundColor: '#F4F2F2',
            borderRadius: 30,
            padding: 36,
            boxShadow: '20px 24px 0 rgba(29,33,40,0.2)',
            transform: `translateY(${interpolate(spiralIn, [0, 1], [100, 0])}px)`,
            opacity: interpolate(spiralIn, [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              color: '#1D2128',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 26,
              letterSpacing: 2,
              fontWeight: 700,
            }}
          >
            QUIET DESPERATION / CASH FLOW MAP
          </div>

          <svg width="750" height="445" viewBox="0 0 750 445" style={{ marginTop: 28 }}>
            <g transform={`translate(375 215) rotate(${spiralRotation})`}>
              <circle cx="0" cy="0" r="170" fill="none" stroke="#1D2128" strokeWidth="5" strokeDasharray="18 16" opacity="0.28" />
              <circle cx="0" cy="0" r="125" fill="none" stroke="#C4472D" strokeWidth="13" strokeDasharray="80 22" />
              <circle cx="0" cy="0" r="78" fill="none" stroke="#FF9E20" strokeWidth="18" strokeDasharray="46 18" />
              <circle cx="0" cy="0" r="34" fill="#1D2128" />
            </g>

            <path d="M95 80 L245 130" stroke="#1D2128" strokeWidth="4" strokeDasharray="10 8" />
            <path d="M655 86 L510 142" stroke="#1D2128" strokeWidth="4" strokeDasharray="10 8" />
            <path d="M105 355 L250 292" stroke="#1D2128" strokeWidth="4" strokeDasharray="10 8" />
            <path d="M650 356 L505 292" stroke="#1D2128" strokeWidth="4" strokeDasharray="10 8" />

            <text x="40" y="64" fill="#1D2128" fontSize="22" fontFamily="JetBrains Mono">
              INCOME ↓
            </text>
            <text x="590" y="64" fill="#1D2128" fontSize="22" fontFamily="JetBrains Mono">
              CREDIT ↑
            </text>
            <text x="35" y="390" fill="#1D2128" fontSize="22" fontFamily="JetBrains Mono">
              HOME EQUITY
            </text>
            <text x="565" y="390" fill="#C4472D" fontSize="22" fontFamily="JetBrains Mono">
              FEES ↑
            </text>
          </svg>

          <div
            style={{
              display: 'grid',
              gridTemplateColumns: '1fr 1fr 1fr',
              gap: 15,
              marginTop: 18,
              transform: `translateY(${interpolate(cardIn, [0, 1], [40, 0])}px)`,
              opacity: interpolate(cardIn, [0, 1], [0, 1]),
            }}
          >
            {[
              ['CARD', '24% APR'],
              ['HOUSE', 'EQUITY'],
              ['BANK', 'FEE FLOW'],
            ].map(([label, value], index) => (
              <div
                key={label}
                style={{
                  padding: 19,
                  borderRadius: 15,
                  backgroundColor: index === 1 ? '#FF9E20' : '#1D2128',
                  color: index === 1 ? '#1D2128' : '#F4F2F2',
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 21,
                  fontWeight: 700,
                  lineHeight: 1.35,
                }}
              >
                <div>{label}</div>
                <div style={{ fontSize: 25, marginTop: 7 }}>{value}</div>
              </div>
            ))}
          </div>

          <div
            style={{
              marginTop: 26,
              padding: 21,
              backgroundColor: '#C4472D',
              borderRadius: 15,
              color: '#F4F2F2',
              fontFamily: 'Inter, Poppins, sans-serif',
              fontSize: 31,
              fontWeight: 800,
              textAlign: 'center',
              opacity: 0.96,
            }}
          >
            SURVIVAL BECOMES REVENUE
          </div>
        </div>
      </div>

      <div
        style={{
          position: 'absolute',
          inset: 0,
          boxShadow: 'inset 0 0 195px rgba(29,33,40,0.38)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          color: '#1D2128',
          fontFamily: 'Inter, Poppins, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          opacity: 0.3,
        }}
      >
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Chapter4Scene40;
```

