```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31_ReturnBattle',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const BG1 = '#E07B2A';
const BG2 = '#C4611C';
const PAPER = '#FFF1DC';

const Scene31_ReturnBattle: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // spring helper — bouncy physical entrances
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // exit fade — last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // slow camera push-in
  const camScale = interpolate(frame, [0, durationInFrames], [1.07, 1], { easing: Easing.out(Easing.quad) });

  // screen shake on winner-stamp impact
  const shakeStart = 340;
  const shake = frame >= shakeStart && frame <= shakeStart + 14
    ? Math.sin((frame - shakeStart) * 2.2) * 11 * (1 - (frame - shakeStart) / 14) : 0;

  const bars = [
    { label: 'FD', sub: 'PRE-TAX', val: 6.5, delay: 60, fill: PAPER },
    { label: 'DEBT FUND', sub: 'POST-TAX', val: 5.5, delay: 90, fill: PAPER },
    { label: 'PREPAYMENT', sub: 'RISK-FREE · GUARANTEED', val: 8.5, delay: 130, fill: YEL },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: BG2, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `linear-gradient(${135 + Math.sin(frame * 0.01) * 12}deg, ${BG1} 0%, #FF9B51 48%, ${BG2} 100%)` }} />

      {/* Animated shape layers — drifting transparent geometry */}
      <div style={{ position: 'absolute', width: 920, height: 920, borderRadius: '50%', border: `4px solid ${INK}`, opacity: 0.06, left: -280, top: -320, transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})` }} />
      <div style={{ position: 'absolute', width: 640, height: 640, background: INK, opacity: 0.05, right: -200, bottom: -240, transform: `rotate(${18 + Math.sin(frame * 0.015) * 6}deg)` }} />

      {/* Motion texture — drifting halftone dots */}
      <AbsoluteFill style={{ backgroundImage: `radial-gradient(${INK} 2.2px, transparent 2.6px)`, backgroundSize: '42px 42px', backgroundPosition: `${frame * 0.3}px ${frame * 0.15}px`, opacity: 0.07 }} />

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const spd = 0.4 + seededRandom(i + 7) * 0.9;
        const y = ((seededRandom(i + 3) * 1080) - frame * spd + 2160) % 1180 - 50;
        const s = 6 + seededRandom(i + 5) * 10;
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 1) * 1920, top: y, width: s, height: s, background: i % 3 === 0 ? YEL : INK, opacity: 0.15, transform: `rotate(${frame * (i % 2 ? 1 : -1)}deg)` }} />;
      })}

      {/* Main content — camera wrapper */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Headline — punk tape strip */}
        <div style={{ position: 'absolute', top: 70, transform: `rotate(-2deg) scale(${sp(8)})`, background: INK, color: YEL, padding: '18px 54px', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 96, letterSpacing: 2, boxShadow: `12px 12px 0 rgba(37,52,63,0.35)` }}>
          GUARANTEED RETURN का मुकाबला
        </div>

        {/* Bar arena — brutalist comparison chart */}
        <div style={{ display: 'flex', gap: 90, alignItems: 'flex-end', marginTop: 140, height: 520 }}>
          {bars.map((b, i) => {
            const grow = sp(b.delay, { damping: 14 });
            const h = (b.val / 9) * 460 * grow;
            const winner = i === 2;
            const wob = winner ? Math.sin(frame * 0.06) * 2 : 0;
            const shown = (b.val * Math.min(1, grow)).toFixed(1);
            return (
              <div key={b.label} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', transform: `rotate(${wob * 0.3}deg)` }}>
                {/* value counter */}
                <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: winner ? 92 : 66, color: winner ? YEL : INK, textShadow: winner ? `5px 5px 0 ${INK}` : 'none', marginBottom: 14, transform: `translateY(${wob}px)` }}>
                  {shown}%
                </div>
                {/* bar block with hard offset shadow */}
                <div style={{ width: winner ? 300 : 240, height: Math.max(8, h), background: b.fill, border: `7px solid ${INK}`, boxShadow: `14px 14px 0 ${INK}`, transformOrigin: 'bottom' }} />
                <div style={{ marginTop: 22, background: winner ? YEL : 'transparent', border: `5px solid ${INK}`, color: INK, padding: '8px 20px', fontFamily: 'Inter, sans-serif', fontWeight: 900, fontSize: 40, transform: `rotate(${i === 1 ? 1.5 : -1.5}deg)` }}>
                  {b.label}
                </div>
                <div style={{ marginTop: 8, fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 28, color: INK, opacity: 0.85 }}>{b.sub}</div>
              </div>
            );
          })}
        </div>

        {/* Winner stamp — slams in with shake */}
        <div style={{ position: 'absolute', right: 140, top: 300, transform: `rotate(10deg) scale(${sp(shakeStart, { damping: 9, stiffness: 200 })})`, border: `8px solid ${INK}`, color: INK, background: YEL, padding: '16px 30px', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 56, boxShadow: `10px 10px 0 ${INK}` }}>
          WINNER
        </div>

        {/* Footer strip — payoff line */}
        <div style={{ position: 'absolute', bottom: 90, transform: `translateY(${(1 - sp(470)) * 120}px)`, background: PAPER, border: `6px solid ${INK}`, boxShadow: `12px 12px 0 ${INK}`, padding: '20px 46px', fontFamily: 'Noto Sans Devanagari, sans-serif', fontWeight: 800, fontSize: 46, color: INK }}>
          पहले <span style={{ background: YEL, padding: '0 14px' }}>7–8 साल</span> = PREPAYMENT को PRIORITY
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(25,15,5,0.42) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene31_ReturnBattle;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32_VolatileVsGuaranteed',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const BG1 = '#DE7627';
const BG2 = '#BD5C1A';
const PAPER = '#FFF1DC';

const Scene32_VolatileVsGuaranteed: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // spring helper
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // exit fade — last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // camera pan left→right
  const camX = interpolate(frame, [0, durationInFrames], [30, -30], { easing: Easing.inOut(Easing.quad) });

  // line-draw progress (stroke reveal)
  const draw = interpolate(frame, [50, 260], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const DASH = 2400;

  // stamp shake
  const stampAt = 360;
  const shake = frame >= stampAt && frame <= stampAt + 14
    ? Math.sin((frame - stampAt) * 2.4) * 10 * (1 - (frame - stampAt) / 14) : 0;

  // jagged equity path (volatile, dips negative) + steady prepayment path
  const equityPath = 'M 60 380 L 180 300 L 300 420 L 420 250 L 540 330 L 660 180 L 780 360 L 900 140 L 1020 460 L 1140 220 L 1260 260 L 1380 120';
  const steadyPath = 'M 60 470 L 1380 210';

  return (
    <AbsoluteFill style={{ backgroundColor: BG2, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `linear-gradient(${120 + Math.sin(frame * 0.012) * 14}deg, ${BG1}, #FF9B51 52%, ${BG2})` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 800, height: 800, border: `4px solid ${INK}`, opacity: 0.05, left: -220, bottom: -300, transform: `rotate(${frame * -0.08}deg)` }} />
      <div style={{ position: 'absolute', width: 500, height: 500, borderRadius: '50%', background: YEL, opacity: 0.05, right: -140, top: -160, transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})` }} />

      {/* Motion texture — diagonal punk stripes */}
      <AbsoluteFill style={{ backgroundImage: `repeating-linear-gradient(45deg, ${INK} 0 3px, transparent 3px 46px)`, backgroundPosition: `${frame * 0.5}px 0`, opacity: 0.05 }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const spd = 0.5 + seededRandom(i + 9) * 0.8;
        const y = ((seededRandom(i + 2) * 1080) - frame * spd + 2160) % 1160 - 40;
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 4) * 1920, top: y, width: 9, height: 9, background: i % 4 === 0 ? YEL : INK, opacity: 0.14, transform: `rotate(45deg)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camX + shake}px)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Headline */}
        <div style={{ position: 'absolute', top: 64, display: 'flex', gap: 26, alignItems: 'center', transform: `scale(${sp(6)})` }}>
          <div style={{ background: PAPER, border: `7px solid ${INK}`, boxShadow: `12px 12px 0 ${INK}`, padding: '12px 40px', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 88, color: INK, transform: 'rotate(-1.5deg)' }}>VOLATILE</div>
          <div style={{ fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 72, color: INK }}>vs</div>
          <div style={{ background: INK, border: `7px solid ${INK}`, boxShadow: `12px 12px 0 rgba(37,52,63,0.4)`, padding: '12px 40px', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 88, color: YEL, transform: 'rotate(1.5deg)' }}>GUARANTEED</div>
        </div>

        {/* Chart panel — brutalist frame */}
        <div style={{ marginTop: 120, width: 1460, height: 560, background: PAPER, border: `8px solid ${INK}`, boxShadow: `18px 18px 0 ${INK}`, position: 'relative', transform: `scale(${sp(30, { damping: 13 })})` }}>
          <svg width="1460" height="560" viewBox="0 0 1460 560">
            {/* zero baseline */}
            <line x1="40" y1="440" x2="1420" y2="440" stroke={INK} strokeWidth="4" strokeDasharray="14 12" strokeDashoffset={-frame * 0.6} opacity="0.5" />
            {/* equity — jagged, drops below line */}
            <path d={equityPath} fill="none" stroke={INK} strokeWidth="10" strokeLinejoin="round"
              strokeDasharray={DASH} strokeDashoffset={DASH * (1 - draw)} />
            {/* negative dip highlight */}
            <circle cx="1020" cy="460" r={14 * sp(280)} fill={BG2} stroke={INK} strokeWidth="6" />
            {/* prepayment — dead straight, rising */}
            <path d={steadyPath} fill="none" stroke={YEL} strokeWidth="16"
              strokeDasharray={DASH} strokeDashoffset={DASH * (1 - draw)} style={{ filter: `drop-shadow(6px 6px 0 ${INK})` }} />
            {/* moving marker on steady line */}
            <circle cx={60 + 1320 * draw} cy={470 - 260 * draw} r={16 + Math.sin(frame * 0.2) * 3} fill={YEL} stroke={INK} strokeWidth="7" />
          </svg>
          {/* labels */}
          <div style={{ position: 'absolute', top: 28, left: 40, opacity: sp(260), fontFamily: 'Inter, sans-serif', fontWeight: 900, fontSize: 38, color: INK, background: 'rgba(255,244,73,0.0)', borderBottom: `6px solid ${INK}`, paddingBottom: 4 }}>
            EQUITY 12–15% · NOT GUARANTEED · NEGATIVE भी
          </div>
          <div style={{ position: 'absolute', bottom: 26, right: 40, opacity: sp(300), fontFamily: 'Inter, sans-serif', fontWeight: 900, fontSize: 38, color: INK, background: YEL, border: `5px solid ${INK}`, padding: '6px 18px', transform: 'rotate(-1deg)' }}>
            PREPAYMENT · DAY 1 से GUARANTEED
          </div>
        </div>

        {/* Rubber stamp — 8.5% TAX-FREE FD */}
        <div style={{ position: 'absolute', bottom: 120, left: 180, transform: `rotate(-8deg) scale(${sp(stampAt, { damping: 8, stiffness: 220 })})`, border: `9px double ${INK}`, color: INK, padding: '14px 30px', fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 44, background: 'rgba(255,244,73,0.92)' }}>
          8.5% TAX-FREE · ZERO RISK FD
        </div>

        {/* Step 1 kicker — next beat */}
        <div style={{ position: 'absolute', bottom: 60, right: 150, transform: `translateX(${(1 - sp(560)) * 300}px)`, background: INK, color: YEL, padding: '14px 34px', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 54 }}>
          PRACTICAL FRAMEWORK → STEP 1
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(25,15,5,0.42) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene32_VolatileVsGuaranteed;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33_FourNumbers',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const BG1 = '#DA7224';
const BG2 = '#B85818';
const PAPER = '#FFF1DC';

const Scene33_FourNumbers: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // spring helper
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // exit fade — last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // camera slow tilt-zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.0], { easing: Easing.out(Easing.quad) });
  const camRot = Math.sin(frame * 0.008) * 0.5;

  // trap-sticker slam shake
  const trapAt = 420;
  const shake = frame >= trapAt && frame <= trapAt + 14
    ? Math.sin((frame - trapAt) * 2.3) * 12 * (1 - (frame - trapAt) / 14) : 0;

  const items = [
    { t: 'OUTSTANDING BALANCE', d: 80 },
    { t: 'CURRENT INTEREST RATE', d: 130 },
    { t: 'REMAINING TENURE', d: 180 },
    { t: 'ORIGINAL TENURE', d: 230 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: BG2, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `linear-gradient(${160 + Math.sin(frame * 0.011) * 10}deg, ${BG1}, #FF9B51 50%, ${BG2})` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 760, height: 760, borderRadius: '50%', border: `4px solid ${INK}`, opacity: 0.06, right: -240, top: -260, transform: `rotate(${frame * 0.09}deg)` }} />
      <div style={{ position: 'absolute', width: 420, height: 420, background: YEL, opacity: 0.05, left: -120, bottom: -140, transform: `rotate(${-12 + Math.sin(frame * 0.02) * 5}deg)` }} />

      {/* Motion texture — notebook grid */}
      <AbsoluteFill style={{ backgroundImage: `linear-gradient(${INK} 1.6px, transparent 1.6px), linear-gradient(90deg, ${INK} 1.6px, transparent 1.6px)`, backgroundSize: '72px 72px', backgroundPosition: `${frame * 0.2}px ${frame * 0.1}px`, opacity: 0.05 }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const spd = 0.4 + seededRandom(i + 6) * 0.8;
        const y = ((seededRandom(i + 2) * 1080) - frame * spd + 2160) % 1160 - 40;
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 3) * 1920, top: y, width: 8, height: 8, borderRadius: i % 2 ? '50%' : 0, background: i % 3 === 0 ? YEL : INK, opacity: 0.15 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg) translateX(${shake}px)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Phone icon + headline tape */}
        <div style={{ position: 'absolute', top: 66, display: 'flex', alignItems: 'center', gap: 30, transform: `rotate(-1.5deg) scale(${sp(6)})` }}>
          {/* SVG phone icon — ringing wobble */}
          <div style={{ background: YEL, border: `7px solid ${INK}`, boxShadow: `10px 10px 0 ${INK}`, padding: 18, transform: `rotate(${Math.sin(frame * 0.4) * (frame < 120 ? 10 : 2)}deg)` }}>
            <svg width="70" height="70" viewBox="0 0 24 24" fill="none" stroke={INK} strokeWidth="2.4" strokeLinecap="round" strokeLinejoin="round">
              <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z"/>
            </svg>
          </div>
          <div style={{ background: INK, color: YEL, padding: '16px 46px', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 92, boxShadow: `12px 12px 0 rgba(37,52,63,0.35)` }}>
            BANK को CALL करो — 4 NUMBERS
          </div>
        </div>

        {/* Checklist card — torn-paper collage feel */}
        <div style={{ marginTop: 130, width: 1180, background: PAPER, border: `8px solid ${INK}`, boxShadow: `20px 20px 0 ${INK}`, padding: '46px 60px', transform: `rotate(-0.6deg) scale(${sp(40, { damping: 13 })})` }}>
          {items.map((it, i) => {
            const p = sp(it.d);
            const tick = interpolate(frame, [it.d + 24, it.d + 46], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            return (
              <div key={it.t} style={{ display: 'flex', alignItems: 'center', gap: 34, marginBottom: i === 3 ? 0 : 30, transform: `translateX(${(1 - p) * -400}px)`, opacity: Math.min(1, p * 1.4) }}>
                {/* numbered brutalist chip */}
                <div style={{ width: 76, height: 76, background: YEL, border: `6px solid ${INK}`, display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 44, color: INK, transform: `rotate(${(i % 2 ? 1 : -1) * 3}deg)` }}>
                  {i + 1}
                </div>
                <div style={{ flex: 1, fontFamily: 'Inter, sans-serif', fontWeight: 900, fontSize: 52, color: INK, letterSpacing: 1 }}>{it.t}</div>
                {/* animated tick draw */}
                <svg width="62" height="62" viewBox="0 0 24 24" fill="none" stroke={INK} strokeWidth="3.4" strokeLinecap="round" strokeLinejoin="round">
                  <path d="M4 13 L10 19 L20 5" strokeDasharray="40" strokeDashoffset={40 * (1 - tick)} />
                </svg>
              </div>
            );
          })}
        </div>

        {/* TRAP warning sticker — slams with shake */}
        <div style={{ position: 'absolute', right: 110, bottom: 200, transform: `rotate(7deg) scale(${sp(trapAt, { damping: 8, stiffness: 230 })})`, background: INK, color: YEL, border: `6px solid ${YEL}`, outline: `6px solid ${INK}`, padding: '22px 36px', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 50, textAlign: 'center', lineHeight: 1.2 }}>
          REMAINING − ORIGINAL<br />≥ 2 साल = TRAP
        </div>

        {/* Step-3 ticker tape — endless scroll */}
        <div style={{ position: 'absolute', bottom: 70, left: 0, width: 1920, overflow: 'hidden', background: YEL, borderTop: `6px solid ${INK}`, borderBottom: `6px solid ${INK}`, opacity: sp(560) }}>
          <div style={{ whiteSpace: 'nowrap', transform: `translateX(${-((frame * 4) % 1400)}px)`, fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 38, color: INK, padding: '10px 0' }}>
            {'STEP 3: हर 3 साल में CALENDAR REMINDER ··· STEP 3: हर 3 साल में CALENDAR REMINDER ··· STEP 3: हर 3 साल में CALENDAR REMINDER ··· '}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(25,15,5,0.42) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene33_FourNumbers;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34_ReactiveVsProactive',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const BG1 = '#D76F22';
const BG2 = '#B25415';
const PAPER = '#FFF1DC';

const Scene34_ReactiveVsProactive: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // spring helper
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // exit fade — last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // camera slow orbit (subtle rotate + scale)
  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1], { easing: Easing.out(Easing.quad) });

  // stamp slam shake
  const stampAt = 520;
  const shake = frame >= stampAt && frame <= stampAt + 16
    ? Math.sin((frame - stampAt) * 2.1) * 13 * (1 - (frame - stampAt) / 16) : 0;

  const leftItems = ['RBI का WAIT', 'बस EMI भरते रहो', 'HOPE = STRATEGY'];
  const rightItems = ['MARKET RATES CHECK', 'CIBIL REVIEW', 'SPREAD REDUCTION मांगो', '0.35%+ सस्ता → TRANSFER MATH'];

  return (
    <AbsoluteFill style={{ backgroundColor: BG2, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `linear-gradient(${100 + Math.sin(frame * 0.013) * 16}deg, ${BG1}, #FF9B51 55%, ${BG2})` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 1000, height: 1000, borderRadius: '50%', border: `3px dashed ${INK}`, opacity: 0.07, left: 460, top: -420, transform: `rotate(${frame * 0.15}deg)` }} />
      <div style={{ position: 'absolute', width: 380, height: 380, background: INK, opacity: 0.05, left: -110, top: 300, transform: `rotate(${frame * 0.05}deg)` }} />

      {/* Motion texture — halftone */}
      <AbsoluteFill style={{ backgroundImage: `radial-gradient(${INK} 2px, transparent 2.4px)`, backgroundSize: '36px 36px', backgroundPosition: `${-frame * 0.25}px ${frame * 0.2}px`, opacity: 0.06 }} />

      {/* Floating particles */}
      {Array.from({ length: 13 }).map((_, i) => {
        const spd = 0.45 + seededRandom(i + 8) * 0.9;
        const y = ((seededRandom(i + 1) * 1080) - frame * spd + 2160) % 1160 - 40;
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 5) * 1920, top: y, width: 10, height: 3, background: i % 3 === 0 ? YEL : INK, opacity: 0.16, transform: `rotate(${frame * 1.5 + i * 30}deg)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Split arena */}
        <div style={{ display: 'flex', gap: 60, alignItems: 'stretch', marginTop: 30 }}>
          {/* REACTIVE panel — dull, sinking */}
          <div style={{ width: 700, background: INK, border: `8px solid ${INK}`, boxShadow: `16px 16px 0 rgba(37,52,63,0.4)`, padding: '40px 44px', transform: `rotate(-1.2deg) translateY(${(1 - sp(30)) * 500}px) translateY(${Math.sin(frame * 0.03) * 4}px)` }}>
            <div style={{ fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 84, color: '#8FA0AC', textDecoration: frame > stampAt ? 'line-through' : 'none', textDecorationThickness: 8 }}>REACTIVE</div>
            {leftItems.map((t, i) => (
              <div key={t} style={{ marginTop: 30, opacity: sp(120 + i * 45), transform: `translateX(${(1 - sp(120 + i * 45)) * -160}px)`, display: 'flex', alignItems: 'center', gap: 20 }}>
                {/* X icon */}
                <svg width="44" height="44" viewBox="0 0 24 24" stroke="#8FA0AC" strokeWidth="3.2" fill="none" strokeLinecap="round"><path d="M5 5 L19 19 M19 5 L5 19" /></svg>
                <span style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontWeight: 700, fontSize: 42, color: '#B9C6CF' }}>{t}</span>
              </div>
            ))}
          </div>

          {/* PROACTIVE panel — loud, floating up */}
          <div style={{ width: 760, background: YEL, border: `8px solid ${INK}`, boxShadow: `16px 16px 0 ${INK}`, padding: '40px 44px', transform: `rotate(1.2deg) translateY(${(1 - sp(70)) * 500}px) translateY(${Math.sin(frame * 0.05 + 2) * 5}px)` }}>
            <div style={{ fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 84, color: INK }}>PROACTIVE</div>
            {rightItems.map((t, i) => (
              <div key={t} style={{ marginTop: 26, opacity: sp(160 + i * 45), transform: `translateX(${(1 - sp(160 + i * 45)) * 160}px)`, display: 'flex', alignItems: 'center', gap: 20 }}>
                {/* bolt icon */}
                <svg width="44" height="44" viewBox="0 0 24 24" fill={INK}><path d="M13 2 L4 14 h6 l-1 8 9-12 h-6 z" /></svg>
                <span style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontWeight: 800, fontSize: 40, color: INK }}>{t}</span>
              </div>
            ))}
          </div>
        </div>

        {/* VS badge — spinning collage sticker */}
        <div style={{ position: 'absolute', top: 60, transform: `rotate(${Math.sin(frame * 0.04) * 8}deg) scale(${sp(100, { damping: 9 })})`, width: 170, height: 170, borderRadius: '50%', background: PAPER, border: `8px solid ${INK}`, boxShadow: `10px 10px 0 ${INK}`, display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 78, color: INK }}>
          VS
        </div>

        {/* Verdict stamp — slams in */}
        <div style={{ position: 'absolute', bottom: 84, transform: `rotate(-5deg) scale(${sp(stampAt, { damping: 8, stiffness: 240 })})`, border: `10px double ${INK}`, background: 'rgba(255,241,220,0.95)', color: INK, padding: '18px 44px', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 64 }}>
          PROACTIVE ही जीतता है
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(25,15,5,0.44) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene34_ReactiveVsProactive;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35_Interest92',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const BG1 = '#D06A1E';
const BG2 = '#A94E12';
const PAPER = '#FFF1DC';

const Scene35_Interest92: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // spring helper
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // exit fade — last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // camera zoom-out reveal
  const camScale = interpolate(frame, [0, 120, durationInFrames], [1.18, 1.04, 1], { easing: Easing.out(Easing.cubic) });

  // donut arc progress → 92%
  const arc = interpolate(frame, [80, 240], [0, 0.92], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });
  const R = 260;
  const CIRC = 2 * Math.PI * R;
  const pct = Math.round(arc * 100);

  // hero shake when counter hits 92
  const hitAt = 240;
  const shake = frame >= hitAt && frame <= hitAt + 14
    ? Math.sin((frame - hitAt) * 2.4) * 10 * (1 - (frame - hitAt) / 14) : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: BG2, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `radial-gradient(circle at ${50 + Math.sin(frame * 0.01) * 8}% 40%, #FF9B51 0%, ${BG1} 55%, ${BG2} 100%)` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 1200, height: 1200, borderRadius: '50%', border: `3px solid ${INK}`, opacity: 0.06, left: 360, top: -260, transform: `rotate(${frame * -0.06}deg) scale(${1 + Math.sin(frame * 0.018) * 0.03})` }} />
      <div style={{ position: 'absolute', width: 0, height: 0, borderLeft: '260px solid transparent', borderRight: '260px solid transparent', borderBottom: `440px solid ${INK}`, opacity: 0.04, left: -160, bottom: -180, transform: `rotate(${14 + Math.sin(frame * 0.02) * 4}deg)` }} />

      {/* Motion texture — concentric ring scanlines */}
      <AbsoluteFill style={{ backgroundImage: `repeating-radial-gradient(circle at 50% 50%, ${INK} 0 2px, transparent 2px 60px)`, opacity: 0.045, transform: `scale(${1 + (frame % 90) / 900})` }} />

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const spd = 0.4 + seededRandom(i + 4) * 0.9;
        const y = ((seededRandom(i + 2) * 1080) - frame * spd + 2160) % 1160 - 40;
        const s = 5 + seededRandom(i + 9) * 9;
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 7) * 1920, top: y, width: s, height: s, borderRadius: '50%', background: i % 4 === 0 ? YEL : INK, opacity: 0.14 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)`, flexDirection: 'row', justifyContent: 'center', alignItems: 'center', gap: 110 }}>
        {/* Donut — YEAR 1 EMI split */}
        <div style={{ position: 'relative', width: 640, height: 640, transform: `scale(${sp(20, { damping: 13 })})` }}>
          <svg width="640" height="640" viewBox="0 0 640 640">
            {/* base ring — principal (paper) */}
            <circle cx="320" cy="320" r={R} fill="none" stroke={PAPER} strokeWidth="86" />
            {/* interest arc (ink) sweeps to 92% */}
            <circle cx="320" cy="320" r={R} fill="none" stroke={INK} strokeWidth="86"
              strokeDasharray={`${CIRC * arc} ${CIRC}`} transform="rotate(-90 320 320)" strokeLinecap="butt" />
            {/* outer brutal border rings */}
            <circle cx="320" cy="320" r={R + 47} fill="none" stroke={INK} strokeWidth="7" />
            <circle cx="320" cy="320" r={R - 47} fill="none" stroke={INK} strokeWidth="7" />
          </svg>
          {/* hero counter in donut hole */}
          <div style={{ position: 'absolute', inset: 0, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center' }}>
            <div style={{ fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 190, color: YEL, textShadow: `8px 8px 0 ${INK}`, lineHeight: 1 }}>{pct}%</div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontWeight: 900, fontSize: 40, color: INK, background: PAPER, border: `5px solid ${INK}`, padding: '4px 18px', marginTop: 12, transform: 'rotate(-2deg)' }}>INTEREST</div>
          </div>
          {/* tiny principal tag */}
          <div style={{ position: 'absolute', bottom: 16, right: -30, transform: `rotate(6deg) scale(${sp(300)})`, background: YEL, border: `5px solid ${INK}`, padding: '6px 16px', fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 30, color: INK }}>
            सिर्फ 8% PRINCIPAL
          </div>
        </div>

        {/* Right column — loan spec + message */}
        <div style={{ width: 660 }}>
          <div style={{ transform: `translateX(${(1 - sp(60)) * 400}px)`, background: INK, color: YEL, padding: '14px 30px', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 66, display: 'inline-block', rotate: '-1.5deg' }}>
            YEAR 1 की EMI
          </div>
          {/* spec chips — mono receipt style */}
          {['₹60,00,000 LOAN', '20 साल TENURE', '8.75% RATE'].map((t, i) => (
            <div key={t} style={{ marginTop: 24, transform: `translateX(${(1 - sp(140 + i * 50)) * 400}px)`, background: PAPER, border: `6px solid ${INK}`, boxShadow: `10px 10px 0 ${INK}`, padding: '14px 26px', fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 46, color: INK, display: 'inline-block' }}>
              {t}
            </div>
          ))}
          {/* payoff line */}
          <div style={{ marginTop: 40, opacity: sp(420), transform: `translateY(${(1 - sp(420)) * 60}px)`, fontFamily: 'Noto Sans Devanagari, sans-serif', fontWeight: 800, fontSize: 44, color: INK, lineHeight: 1.35 }}>
            TIMING उतनी ही ज़रूरी<br />
            <span style={{ background: YEL, border: `4px solid ${INK}`, padding: '2px 14px' }}>जितनी AMOUNT</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(25,15,5,0.46) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene35_Interest92;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36_GoldenWindow',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const BG1 = '#DB7325';
const BG2 = '#B65716';
const PAPER = '#FFF1DC';

const Scene36_GoldenWindow: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // spring helper
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // exit fade — last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // camera lateral pan across the timeline
  const camX = interpolate(frame, [0, durationInFrames], [-40, 40], { easing: Easing.inOut(Easing.quad) });

  // multiplier stamp slam
  const stampAt = 330;
  const shake = frame >= stampAt && frame <= stampAt + 15
    ? Math.sin((frame - stampAt) * 2.2) * 12 * (1 - (frame - stampAt) / 15) : 0;

  // scanning marker sweeps years 1→20
  const scan = interpolate(frame, [90, 560], [0, 19], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: BG2, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `linear-gradient(${180 + Math.sin(frame * 0.012) * 14}deg, ${BG1}, #FF9B51 46%, ${BG2})` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 860, height: 860, border: `4px solid ${INK}`, borderRadius: '50%', opacity: 0.05, left: -300, top: -380, transform: `rotate(${frame * 0.07}deg)` }} />
      <div style={{ position: 'absolute', width: 520, height: 520, background: YEL, opacity: 0.045, right: -170, bottom: -200, transform: `rotate(${30 + Math.sin(frame * 0.016) * 8}deg)` }} />

      {/* Motion texture — vertical bars drift */}
      <AbsoluteFill style={{ backgroundImage: `repeating-linear-gradient(90deg, ${INK} 0 2px, transparent 2px 90px)`, backgroundPosition: `${frame * 0.4}px 0`, opacity: 0.05 }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const spd = 0.5 + seededRandom(i + 3) * 0.8;
        const y = ((seededRandom(i + 6) * 1080) - frame * spd + 2160) % 1160 - 40;
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 1) * 1920, top: y, width: 7, height: 7, background: i % 3 === 0 ? YEL : INK, opacity: 0.15, transform: `rotate(${frame + i * 24}deg)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camX + shake}px)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Headline */}
        <div style={{ position: 'absolute', top: 72, transform: `rotate(-1.8deg) scale(${sp(8)})`, background: YEL, border: `8px solid ${INK}`, boxShadow: `14px 14px 0 ${INK}`, padding: '16px 52px', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 96, color: INK }}>
          GOLDEN WINDOW: साल 1–7
        </div>

        {/* 20-year timeline — brutalist blocks */}
        <div style={{ display: 'flex', gap: 10, marginTop: 60, position: 'relative' }}>
          {Array.from({ length: 20 }).map((_, i) => {
            const inWindow = i < 7;
            const pop = sp(80 + i * 12, { damping: 13 });
            const glow = inWindow ? (Math.sin(frame * 0.12 + i) + 1) / 2 : 0;
            const isScanned = Math.floor(scan) === i;
            return (
              <div key={i} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', transform: `translateY(${(1 - pop) * 200}px) ${isScanned ? 'translateY(-16px)' : ''}` }}>
                <div style={{ width: 74, height: inWindow ? 260 : 170, background: inWindow ? YEL : PAPER, border: `6px solid ${INK}`, boxShadow: inWindow ? `8px 8px 0 ${INK}` : `5px 5px 0 ${INK}`, opacity: 0.92 + glow * 0.08 }} />
                <div style={{ marginTop: 10, fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 26, color: INK }}>{i + 1}</div>
              </div>
            );
          })}
          {/* scanning marker arrow */}
          <div style={{ position: 'absolute', top: -84, left: Math.min(19, scan) * 84 + 14, transition: 'none' }}>
            <svg width="52" height="60" viewBox="0 0 24 28" fill={INK}><path d="M12 28 L0 10 h7 V0 h10 v10 h7 z" transform="rotate(180 12 14)" /></svg>
          </div>
        </div>

        {/* Multiplier hero stamp */}
        <div style={{ position: 'absolute', left: 150, bottom: 150, transform: `rotate(-7deg) scale(${sp(stampAt, { damping: 8, stiffness: 230 })})`, background: INK, color: YEL, padding: '20px 44px', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 120, boxShadow: `14px 14px 0 rgba(37,52,63,0.4)`, lineHeight: 1 }}>
          8–10x
          <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontSize: 34, fontWeight: 700, marginTop: 6 }}>हर PREPAY रुपया = FUTURE INTEREST कट</div>
        </div>

        {/* Right info chips */}
        <div style={{ position: 'absolute', right: 130, bottom: 150 }}>
          <div style={{ transform: `translateX(${(1 - sp(430)) * 340}px)`, background: PAPER, border: `6px solid ${INK}`, boxShadow: `10px 10px 0 ${INK}`, padding: '14px 26px', fontFamily: 'Inter, sans-serif', fontWeight: 900, fontSize: 40, color: INK, marginBottom: 20, rotate: '1.5deg' }}>
            YEAR 8 पर भी INTEREST ≈ 75%
          </div>
          <div style={{ transform: `translateX(${(1 - sp(490)) * 340}px)`, background: YEL, border: `6px solid ${INK}`, boxShadow: `10px 10px 0 ${INK}`, padding: '14px 26px', fontFamily: 'Noto Sans Devanagari, sans-serif', fontWeight: 800, fontSize: 40, color: INK, rotate: '-1.5deg' }}>
            LOAN 1–5 साल पुराना? अभी मौका है
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(25,15,5,0.44) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene36_GoldenWindow;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37_RaviCaseFile',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const BG1 = '#D66E20';
const BG2 = '#AE5212';
const PAPER = '#FFF1DC';

const Scene37_RaviCaseFile: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // spring helper
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // exit fade — last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // camera dolly-in
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.1], { easing: Easing.inOut(Easing.quad) });

  // typewriter reveal helper for dossier lines
  const typed = (text: string, start: number, speed = 1.4) => {
    const chars = Math.max(0, Math.floor((frame - start) * speed));
    return text.slice(0, chars);
  };

  // total slam shake
  const slamAt = 480;
  const shake = frame >= slamAt && frame <= slamAt + 16
    ? Math.sin((frame - slamAt) * 2.2) * 14 * (1 - (frame - slamAt) / 16) : 0;

  const lines = [
    { t: 'NAME ........ RAVI', s: 150 },
    { t: 'AGE ......... 30 · PUNE · IT', s: 200 },
    { t: 'LOAN ........ ₹60,00,000 (2024)', s: 255 },
    { t: 'RATE ........ 8.5% | 20 YEARS', s: 315 },
    { t: 'EMI ......... ₹52,000 / MONTH', s: 375 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: BG2, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `linear-gradient(${145 + Math.sin(frame * 0.01) * 10}deg, ${BG1}, #FF9B51 50%, ${BG2})` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 700, height: 700, background: INK, opacity: 0.045, left: -220, top: -240, transform: `rotate(${frame * 0.04}deg)` }} />
      <div style={{ position: 'absolute', width: 900, height: 900, borderRadius: '50%', border: `4px solid ${YEL}`, opacity: 0.07, right: -320, bottom: -380, transform: `scale(${1 + Math.sin(frame * 0.02) * 0.04})` }} />

      {/* Motion texture — file-paper scanlines */}
      <AbsoluteFill style={{ backgroundImage: `repeating-linear-gradient(0deg, ${INK} 0 2px, transparent 2px 54px)`, backgroundPosition: `0 ${frame * 0.35}px`, opacity: 0.05 }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const spd = 0.4 + seededRandom(i + 5) * 0.9;
        const y = ((seededRandom(i + 8) * 1080) - frame * spd + 2160) % 1160 - 40;
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 2) * 1920, top: y, width: 8, height: 8, background: i % 4 === 0 ? YEL : INK, opacity: 0.14, transform: `rotate(${45 + frame}deg)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)`, flexDirection: 'row', justifyContent: 'center', alignItems: 'center', gap: 90 }}>
        {/* Case-file dossier card */}
        <div style={{ width: 880, background: PAPER, border: `8px solid ${INK}`, boxShadow: `20px 20px 0 ${INK}`, transform: `rotate(-1deg) translateY(${(1 - sp(20, { damping: 13 })) * 600}px)` }}>
          {/* file header tab */}
          <div style={{ background: INK, color: YEL, padding: '16px 34px', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 60, display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
            <span>CASE FILE #001</span>
            {/* folder icon */}
            <svg width="54" height="54" viewBox="0 0 24 24" fill="none" stroke={YEL} strokeWidth="2.2"><path d="M3 7 V19 a2 2 0 0 0 2 2 h14 a2 2 0 0 0 2-2 V9 a2 2 0 0 0-2-2 h-8 L9 5 H5 a2 2 0 0 0-2 2 z" /></svg>
          </div>
          {/* typed dossier lines — terminal receipt style */}
          <div style={{ padding: '36px 44px' }}>
            {lines.map((l) => (
              <div key={l.t} style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 44, color: INK, marginBottom: 22, minHeight: 52, borderBottom: `3px dashed rgba(37,52,63,0.25)`, paddingBottom: 8 }}>
                {typed(l.t, l.s)}
                {frame >= l.s && frame < l.s + l.t.length / 1.4 + 8 && <span style={{ opacity: Math.round(frame / 8) % 2 ? 1 : 0 }}>▌</span>}
              </div>
            ))}
            <div style={{ marginTop: 6, opacity: sp(440), fontFamily: 'Noto Sans Devanagari, sans-serif', fontWeight: 800, fontSize: 36, color: INK, background: YEL, display: 'inline-block', border: `4px solid ${INK}`, padding: '4px 16px', transform: 'rotate(-1deg)' }}>
              STRATEGY: कोई PREPAYMENT नहीं
            </div>
          </div>
        </div>

        {/* Right — verdict tower */}
        <div style={{ width: 620 }}>
          <div style={{ transform: `translateX(${(1 - sp(410)) * 420}px)`, fontFamily: 'Inter, sans-serif', fontWeight: 900, fontSize: 46, color: INK, background: PAPER, border: `6px solid ${INK}`, boxShadow: `10px 10px 0 ${INK}`, padding: '12px 26px', display: 'inline-block', rotate: '1.2deg' }}>
            20 साल सिर्फ EMI भरी तो...
          </div>
          {/* mega total — slams in */}
          <div style={{ marginTop: 34, transform: `rotate(-3deg) scale(${sp(slamAt, { damping: 8, stiffness: 220 })})`, background: INK, color: YEL, padding: '26px 40px', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 150, lineHeight: 1, boxShadow: `16px 16px 0 rgba(37,52,63,0.4)`, display: 'inline-block' }}>
            ₹1.25 Cr
          </div>
          <div style={{ marginTop: 28, opacity: sp(slamAt + 50), fontFamily: 'Noto Sans Devanagari, sans-serif', fontWeight: 800, fontSize: 42, color: INK }}>
            TOTAL REPAYMENT — <span style={{ background: YEL, border: `4px solid ${INK}`, padding: '2px 12px' }}>₹60L के LOAN पर</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 54%, rgba(25,15,5,0.45) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene37_RaviCaseFile;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38_TenureCut',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const BG1 = '#D97124';
const BG2 = '#B05414';
const PAPER = '#FFF1DC';

const Scene38_TenureCut: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // spring helper
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // exit fade — last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // camera slow rise
  const camY = interpolate(frame, [0, durationInFrames], [20, -20], { easing: Easing.inOut(Easing.quad) });

  // the CUT moment — chunk breaks off the tenure bar
  const cutAt = 330;
  const cut = sp(cutAt, { damping: 10 });
  const shake = frame >= cutAt && frame <= cutAt + 16
    ? Math.sin((frame - cutAt) * 2.3) * 13 * (1 - (frame - cutAt) / 16) : 0;
  // broken chunk physics — falls + rotates away after the cut
  const fallT = Math.max(0, frame - cutAt);
  const chunkY = fallT * fallT * 0.35;
  const chunkRot = fallT * 1.8;

  return (
    <AbsoluteFill style={{ backgroundColor: BG2, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `linear-gradient(${115 + Math.sin(frame * 0.012) * 12}deg, ${BG1}, #FF9B51 52%, ${BG2})` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 840, height: 840, borderRadius: '50%', border: `4px solid ${INK}`, opacity: 0.055, right: -260, top: -300, transform: `rotate(${frame * -0.08}deg)` }} />
      <div style={{ position: 'absolute', width: 460, height: 460, background: YEL, opacity: 0.05, left: -140, bottom: -170, transform: `rotate(${8 + Math.sin(frame * 0.02) * 6}deg)` }} />

      {/* Motion texture — caution diagonals */}
      <AbsoluteFill style={{ backgroundImage: `repeating-linear-gradient(-45deg, ${INK} 0 3px, transparent 3px 58px)`, backgroundPosition: `${-frame * 0.45}px 0`, opacity: 0.05 }} />

      {/* Floating particles */}
      {Array.from({ length: 13 }).map((_, i) => {
        const spd = 0.45 + seededRandom(i + 7) * 0.85;
        const y = ((seededRandom(i + 3) * 1080) - frame * spd + 2160) % 1160 - 40;
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 9) * 1920, top: y, width: 9, height: 9, background: i % 3 === 0 ? YEL : INK, opacity: 0.15, transform: `rotate(${frame * 2 + i * 40}deg)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translate(${shake}px, ${camY}px)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Top fact strip — interest > principal */}
        <div style={{ position: 'absolute', top: 70, transform: `scale(${sp(8)}) rotate(-1.2deg)`, background: INK, color: PAPER, padding: '16px 44px', fontFamily: 'Noto Sans Devanagari, sans-serif', fontWeight: 800, fontSize: 56, boxShadow: `12px 12px 0 rgba(37,52,63,0.35)` }}>
          ₹60L PRINCIPAL पर <span style={{ color: YEL }}>₹65L INTEREST</span>
        </div>

        {/* BEFORE bar — 20 years, full */}
        <div style={{ position: 'absolute', top: 300, left: 200, opacity: sp(70) }}>
          <div style={{ fontFamily: 'Inter, sans-serif', fontWeight: 900, fontSize: 38, color: INK, marginBottom: 12 }}>BEFORE — कोई PREPAY नहीं</div>
          <div style={{ width: 1400 * Math.min(1, sp(70)), maxWidth: 1400, height: 110, background: PAPER, border: `7px solid ${INK}`, boxShadow: `12px 12px 0 ${INK}`, display: 'flex', alignItems: 'center', justifyContent: 'flex-end', paddingRight: 26, fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 48, color: INK }}>
            20 साल
          </div>
        </div>

        {/* AFTER bar — chunk gets sawed off */}
        <div style={{ position: 'absolute', top: 560, left: 200 }}>
          <div style={{ opacity: sp(200), fontFamily: 'Inter, sans-serif', fontWeight: 900, fontSize: 38, color: INK, marginBottom: 12 }}>AFTER — हर MARCH ₹1L BONUS PREPAY</div>
          <div style={{ display: 'flex' }}>
            {/* surviving tenure */}
            <div style={{ width: 1155 * Math.min(1, sp(200)), height: 110, background: YEL, border: `7px solid ${INK}`, boxShadow: `12px 12px 0 ${INK}`, display: 'flex', alignItems: 'center', justifyContent: 'flex-end', paddingRight: 26, fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 48, color: INK }}>
              16.5 साल
            </div>
            {/* severed chunk — falls away with rotation */}
            <div style={{ width: 245, height: 110, background: INK, border: `7px solid ${INK}`, marginLeft: 6, display: 'flex', alignItems: 'center', justifyContent: 'center', fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 40, color: PAPER, opacity: frame < cutAt ? sp(200) : 1, transform: frame >= cutAt ? `translateY(${chunkY}px) rotate(${chunkRot}deg)` : 'none' }}>
              −3.5 साल
            </div>
          </div>
        </div>

        {/* Saw-cut flash line at the cut moment */}
        {frame >= cutAt - 6 && frame <= cutAt + 10 && (
          <div style={{ position: 'absolute', top: 540, left: 1385, width: 10, height: 180, background: YEL, boxShadow: `0 0 30px ${YEL}`, transform: `rotate(${8 + Math.sin(frame) * 6}deg)` }} />
        )}

        {/* Savings sticker */}
        <div style={{ position: 'absolute', bottom: 110, right: 170, transform: `rotate(-6deg) scale(${sp(cutAt + 40, { damping: 8, stiffness: 220 })})`, background: YEL, border: `9px double ${INK}`, boxShadow: `14px 14px 0 ${INK}`, padding: '22px 40px', textAlign: 'center' }}>
          <div style={{ fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 110, color: INK, lineHeight: 1 }}>₹8–10 लाख</div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontWeight: 900, fontSize: 40, color: INK, marginTop: 6 }}>INTEREST SAVING</div>
        </div>

        {/* Tiny math chip */}
        <div style={{ position: 'absolute', bottom: 130, left: 200, transform: `translateY(${(1 - sp(cutAt + 110)) * 120}px)`, background: PAPER, border: `6px solid ${INK}`, boxShadow: `10px 10px 0 ${INK}`, padding: '12px 26px', fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 38, color: INK }}>
          ₹1L = LOAN का सिर्फ 1.6%
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(25,15,5,0.44) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene38_TenureCut;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39_EffectiveReturn',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const BG1 = '#D26A1C';
const BG2 = '#A84E10';
const PAPER = '#FFF1DC';

const Scene39_EffectiveReturn: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // spring helper
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // exit fade — last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // hero counter 0 → 100
  const heroVal = Math.round(interpolate(frame, [60, 200], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));

  // camera pulse on counter finish
  const hitAt = 200;
  const punch = frame >= hitAt && frame <= hitAt + 12 ? Math.sin((frame - hitAt) * 2.6) * 0.02 * (1 - (frame - hitAt) / 12) : 0;
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1], { easing: Easing.out(Easing.quad) }) + punch;

  return (
    <AbsoluteFill style={{ backgroundColor: BG2, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `radial-gradient(circle at ${30 + Math.sin(frame * 0.01) * 10}% 30%, #FF9B51, ${BG1} 52%, ${BG2})` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 1100, height: 1100, borderRadius: '50%', border: `4px solid ${INK}`, opacity: 0.05, left: -400, bottom: -480, transform: `rotate(${frame * 0.06}deg)` }} />
      <div style={{ position: 'absolute', width: 540, height: 540, background: INK, opacity: 0.04, right: -170, top: -190, transform: `rotate(${frame * 0.05}deg)` }} />

      {/* Motion texture — cross-hatch drift */}
      <AbsoluteFill style={{ backgroundImage: `repeating-linear-gradient(45deg, ${INK} 0 2px, transparent 2px 64px), repeating-linear-gradient(-45deg, ${INK} 0 2px, transparent 2px 64px)`, backgroundPosition: `${frame * 0.3}px ${frame * 0.2}px`, opacity: 0.04 }} />

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const spd = 0.4 + seededRandom(i + 2) * 0.9;
        const y = ((seededRandom(i + 5) * 1080) - frame * spd + 2160) % 1160 - 40;
        const s = 6 + seededRandom(i + 11) * 8;
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 8) * 1920, top: y, width: s, height: s, borderRadius: i % 2 ? '50%' : 0, background: i % 4 === 0 ? YEL : INK, opacity: 0.14 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Hero — EFFECTIVE BENEFIT counter */}
        <div style={{ position: 'absolute', top: 90, textAlign: 'center', transform: `scale(${sp(10, { damping: 13 })})` }}>
          <div style={{ fontFamily: 'Inter, sans-serif', fontWeight: 900, fontSize: 42, color: INK, letterSpacing: 6, marginBottom: 8 }}>EFFECTIVE BENEFIT</div>
          <div style={{ fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 220, lineHeight: 1, color: YEL, textShadow: `10px 10px 0 ${INK}`, transform: `rotate(${Math.sin(frame * 0.05) * 1}deg)` }}>
            80–{heroVal}%
          </div>
          <div style={{ display: 'inline-block', marginTop: 16, background: INK, color: PAPER, padding: '8px 26px', fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 36, transform: 'rotate(-1deg)' }}>
            ₹10L PREPAY → ₹8–10L INTEREST SAVED
          </div>
        </div>

        {/* Two plan cards — collage tilt */}
        <div style={{ position: 'absolute', bottom: 190, display: 'flex', gap: 70 }}>
          {/* Plan A */}
          <div style={{ width: 640, background: PAPER, border: `8px solid ${INK}`, boxShadow: `16px 16px 0 ${INK}`, padding: '30px 38px', transform: `rotate(-2deg) translateY(${(1 - sp(300)) * 400}px) translateY(${Math.sin(frame * 0.04) * 4}px)` }}>
            <div style={{ fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 62, color: INK }}>PLAN A — ₹1L / साल</div>
            <div style={{ marginTop: 14, fontFamily: 'Noto Sans Devanagari, sans-serif', fontWeight: 800, fontSize: 42, color: INK }}>
              TENURE <span style={{ background: YEL, border: `4px solid ${INK}`, padding: '0 12px' }}>−3.5 साल</span>
            </div>
            <div style={{ marginTop: 12, fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 40, color: INK }}>SAVING: ₹8–10 LAKH</div>
          </div>
          {/* Plan B — aggressive */}
          <div style={{ width: 640, background: INK, border: `8px solid ${INK}`, boxShadow: `16px 16px 0 rgba(37,52,63,0.4)`, padding: '30px 38px', transform: `rotate(2deg) translateY(${(1 - sp(360)) * 400}px) translateY(${Math.sin(frame * 0.04 + 2) * 4}px)` }}>
            <div style={{ fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 62, color: YEL }}>PLAN B — ₹2L / साल</div>
            <div style={{ marginTop: 14, fontFamily: 'Noto Sans Devanagari, sans-serif', fontWeight: 800, fontSize: 42, color: PAPER }}>
              TENURE <span style={{ background: YEL, color: INK, border: `4px solid ${YEL}`, padding: '0 12px' }}>−5–6 साल</span>
            </div>
            <div style={{ marginTop: 12, fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 40, color: YEL }}>SAVING: ₹15 LAKH+</div>
          </div>
        </div>

        {/* Bottom challenge tape */}
        <div style={{ position: 'absolute', bottom: 70, left: 0, width: 1920, overflow: 'hidden', background: YEL, borderTop: `6px solid ${INK}`, borderBottom: `6px solid ${INK}`, opacity: sp(520) }}>
          <div style={{ whiteSpace: 'nowrap', transform: `translateX(${-((frame * 4.5) % 1500)}px)`, fontFamily: 'Inter, sans-serif', fontWeight: 900, fontSize: 36, color: INK, padding: '10px 0' }}>
            {'कौन सा MUTUAL FUND ये देगा? GUARANTEED. ··· कौन सा MUTUAL FUND ये देगा? GUARANTEED. ··· कौन सा MUTUAL FUND ये देगा? GUARANTEED. ··· '}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 54%, rgba(25,15,5,0.45) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene39_EffectiveReturn;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40_EPFvsLoan',
  durationInSeconds: 26.11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const INK = '#25343F';
const YEL = '#FFF449';
const BG1 = '#D46C1E';
const BG2 = '#A64D0F';
const PAPER = '#FFF1DC';

const Scene40_EPFvsLoan: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  // spring helper
  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // exit fade — last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // camera drift
  const camX = interpolate(frame, [0, durationInFrames], [24, -24], { easing: Easing.inOut(Easing.quad) });

  // seesaw tilt — loan side heavier, settles after springing
  const tiltP = sp(160, { damping: 9, mass: 0.8 });
  const tilt = tiltP * 7 + Math.sin(frame * 0.03) * 0.8;

  // ticket slam
  const ticketAt = 430;
  const shake = frame >= ticketAt && frame <= ticketAt + 14
    ? Math.sin((frame - ticketAt) * 2.3) * 11 * (1 - (frame - ticketAt) / 14) : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: BG2, overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: `linear-gradient(${150 + Math.sin(frame * 0.011) * 12}deg, ${BG1}, #FF9B51 48%, ${BG2})` }} />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', width: 940, height: 940, borderRadius: '50%', border: `3px dashed ${INK}`, opacity: 0.06, left: 490, top: -430, transform: `rotate(${frame * 0.12}deg)` }} />
      <div style={{ position: 'absolute', width: 500, height: 500, background: YEL, opacity: 0.045, left: -160, top: 260, transform: `rotate(${-18 + Math.sin(frame * 0.018) * 6}deg)` }} />

      {/* Motion texture — halftone */}
      <AbsoluteFill style={{ backgroundImage: `radial-gradient(${INK} 2px, transparent 2.4px)`, backgroundSize: '40px 40px', backgroundPosition: `${frame * 0.28}px ${-frame * 0.18}px`, opacity: 0.065 }} />

      {/* Floating particles */}
      {Array.from({ length: 13 }).map((_, i) => {
        const spd = 0.45 + seededRandom(i + 6) * 0.85;
        const y = ((seededRandom(i + 4) * 1080) - frame * spd + 2160) % 1160 - 40;
        return <div key={i} style={{ position: 'absolute', left: seededRandom(i + 10) * 1920, top: y, width: 8, height: 8, background: i % 3 === 0 ? YEL : INK, opacity: 0.15, transform: `rotate(${frame * 1.4 + i * 20}deg)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camX + shake}px)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Headline */}
        <div style={{ position: 'absolute', top: 66, transform: `rotate(-1.5deg) scale(${sp(6)})`, background: INK, color: YEL, padding: '16px 50px', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 92, boxShadow: `12px 12px 0 rgba(37,52,63,0.35)` }}>
          EPF vs HOME LOAN — तराजू देखो
        </div>

        {/* Seesaw rig */}
        <div style={{ position: 'absolute', top: 300, width: 1400, height: 460 }}>
          {/* beam — tilts toward loan (heavier cost) */}
          <div style={{ position: 'absolute', top: 190, left: 50, width: 1300, height: 22, background: INK, transformOrigin: '50% 50%', transform: `rotate(${tilt}deg)` }}>
            {/* EPF pan — rises */}
            <div style={{ position: 'absolute', left: -30, top: -180, transform: `rotate(${-tilt}deg) scale(${sp(60, { damping: 13 })})`, width: 420, background: PAPER, border: `7px solid ${INK}`, boxShadow: `12px 12px 0 ${INK}`, padding: '22px 28px', textAlign: 'center' }}>
              <div style={{ fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 56, color: INK }}>EPF देता है</div>
              <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 74, color: INK }}>8.15–8.25%</div>
            </div>
            {/* LOAN pan — sinks */}
            <div style={{ position: 'absolute', right: -30, top: -180, transform: `rotate(${-tilt}deg) scale(${sp(100, { damping: 13 })})`, width: 420, background: YEL, border: `7px solid ${INK}`, boxShadow: `12px 12px 0 ${INK}`, padding: '22px 28px', textAlign: 'center' }}>
              <div style={{ fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 56, color: INK }}>LOAN लेता है</div>
              <div style={{ fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 74, color: INK }}>8–9%</div>
            </div>
          </div>
          {/* fulcrum triangle */}
          <div style={{ position: 'absolute', top: 200, left: 640, width: 0, height: 0, borderLeft: '60px solid transparent', borderRight: '60px solid transparent', borderBottom: `130px solid ${INK}`, transform: `scale(${sp(30)})`, transformOrigin: 'bottom' }} />
        </div>

        {/* Net-loss punch tag */}
        <div style={{ position: 'absolute', bottom: 250, left: 210, transform: `rotate(-5deg) scale(${sp(300, { damping: 9 })})`, background: INK, color: YEL, border: `6px solid ${YEL}`, outline: `6px solid ${INK}`, padding: '16px 32px', fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 72 }}>
          NET LOSS: −0.25% / साल
        </div>

        {/* 90% withdrawal ticket — slams in */}
        <div style={{ position: 'absolute', bottom: 220, right: 150, transform: `rotate(4deg) scale(${sp(ticketAt, { damping: 8, stiffness: 230 })})`, background: PAPER, border: `8px dashed ${INK}`, boxShadow: `14px 14px 0 ${INK}`, padding: '20px 36px', textAlign: 'center' }}>
          <div style={{ fontFamily: 'Yantra One, Impact, sans-serif', fontSize: 96, color: INK, lineHeight: 1 }}>90%</div>
          <div style={{ fontFamily: 'Noto Sans Devanagari, sans-serif', fontWeight: 800, fontSize: 36, color: INK, marginTop: 6 }}>EPF BALANCE तक WITHDRAWAL ALLOWED</div>
        </div>

        {/* Verify tape — bottom scroll */}
        <div style={{ position: 'absolute', bottom: 70, left: 0, width: 1920, overflow: 'hidden', background: YEL, borderTop: `6px solid ${INK}`, borderBottom: `6px solid ${INK}`, opacity: sp(560) }}>
          <div style={{ whiteSpace: 'nowrap', transform: `translateX(${-((frame * 4) % 1450)}px)`, fontFamily: 'JetBrains Mono, monospace', fontWeight: 800, fontSize: 34, color: INK, padding: '10px 0' }}>
            {'STANDARD AMORTIZATION MATH — किसी भी EMI CALCULATOR पर VERIFY करो ··· STANDARD AMORTIZATION MATH — किसी भी EMI CALCULATOR पर VERIFY करो ··· '}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 54%, rgba(25,15,5,0.45) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene40_EPFvsLoan;
```

