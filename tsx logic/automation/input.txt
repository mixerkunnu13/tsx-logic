```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1_BailOutEra',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_BailOutEra: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  // Title reveals
  const titleY = sp(10) * 100 - 100;
  const titleOp = interpolate(frame, [10, 30], [0, 1], { extrapolateRight: 'clamp' });

  // Money flow animation - from Government to Bank
  const flowProgress = interpolate(frame, [60, 180], [0, 1], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });

  // Bank shake when receiving money
  const bankShake = frame > 150 && frame < 200 ? Math.sin(frame * 0.8) * 3 : 0;

  // Old era stamp
  const stampScale = sp(200, { damping: 8, stiffness: 180 });
  const stampRot = interpolate(sp(200), [0, 1], [-15, -8]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(8,6,22,0.85) 0%, rgba(255,0,0,1) 70%)',
      }} />

      {/* Shape Layers - large drifting geometry */}
      {[0, 1, 2, 3].map(i => {
        const seed = i + 1;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 10) * 1080;
        const size = 300 + seededRandom(seed + 20) * 400;
        const drift = Math.sin(frame * 0.008 + i) * 30;
        const rot = frame * 0.1 + i * 45;
        return (
          <div key={`shape-${i}`} style={{
            position: 'absolute',
            left: x + drift,
            top: y + drift,
            width: size,
            height: size,
            border: '3px solid #F5F5F5',
            opacity: 0.05,
            borderRadius: i % 2 === 0 ? '50%' : '0%',
            transform: `rotate(${rot}deg)`,
          }} />
        );
      })}

      {/* Motion texture - vertical lines grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 24 }).map((_, i) => (
          <line key={i} x1={i * 80} y1="0" x2={i * 80} y2="1080" stroke="#F5F5F5" strokeWidth="1" />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const px = (seededRandom(i + 30) * 1920 + frame * (0.5 + seededRandom(i) * 1)) % 1920;
        const py = seededRandom(i + 40) * 1080 + Math.sin(frame * 0.02 + i) * 20;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: px, top: py,
            width: 4, height: 4, borderRadius: '50%',
            background: '#FFC107', opacity: 0.6,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>
        {/* Title */}
        <div style={{
          position: 'absolute', top: 70, left: 0, right: 0,
          textAlign: 'center', opacity: titleOp,
          transform: `translateY(${titleY}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            background: '#080616',
            padding: '14px 38px',
            borderLeft: '6px solid #FFC107',
            fontFamily: 'Yantra One',
            fontSize: 64,
            color: '#F5F5F5',
            letterSpacing: '2px',
          }}>
            पुराना युग — BAIL OUT
          </div>
        </div>

        {/* Main flow visual */}
        <div style={{
          position: 'absolute', top: 280, left: 0, right: 0,
          display: 'flex', justifyContent: 'space-around', alignItems: 'center',
        }}>
          {/* Government building */}
          <div style={{ textAlign: 'center', transform: `scale(${sp(40)})` }}>
            <svg width="280" height="280" viewBox="0 0 280 280">
              <rect x="40" y="80" width="200" height="160" fill="#F5F5F5" />
              <polygon points="140,30 30,90 250,90" fill="#FFC107" />
              <rect x="60" y="120" width="20" height="100" fill="#080616" />
              <rect x="100" y="120" width="20" height="100" fill="#080616" />
              <rect x="140" y="120" width="20" height="100" fill="#080616" />
              <rect x="180" y="120" width="20" height="100" fill="#080616" />
              <rect x="220" y="120" width="20" height="100" fill="#080616" />
              <text x="140" y="270" textAnchor="middle" fill="#F5F5F5" fontSize="22" fontFamily="Inter" fontWeight="700">SARKAR</text>
            </svg>
            <div style={{
              marginTop: 12, fontSize: 30, color: '#F5F5F5',
              fontFamily: 'Noto Sans Devanagari', fontWeight: 700,
            }}>
              जनता का टैक्स
            </div>
          </div>

          {/* Arrow + Money flowing */}
          <div style={{ position: 'relative', width: 400, height: 200 }}>
            <svg width="400" height="200" style={{ position: 'absolute' }}>
              <defs>
                <marker id="ar1" markerWidth="15" markerHeight="15" refX="10" refY="5" orient="auto">
                  <polygon points="0,0 15,5 0,10" fill="#FFC107" />
                </marker>
              </defs>
              <line x1="20" y1="100" x2="370" y2="100" stroke="#FFC107" strokeWidth="5"
                strokeDasharray="15,10" markerEnd="url(#ar1)" />
            </svg>
            {/* Moving money symbols */}
            {[0, 1, 2, 3].map(i => {
              const delay = i * 25;
              const localFrame = Math.max(0, frame - 60 - delay);
              const tx = interpolate(localFrame, [0, 80], [20, 360], { extrapolateRight: 'clamp' });
              const op = interpolate(localFrame, [0, 10, 70, 80], [0, 1, 1, 0], { extrapolateRight: 'clamp' });
              return (
                <div key={`m-${i}`} style={{
                  position: 'absolute', left: tx, top: 70,
                  width: 60, height: 60, borderRadius: 12,
                  background: '#FFC107', border: '3px solid #080616',
                  display: 'flex', alignItems: 'center', justifyContent: 'center',
                  fontSize: 32, fontWeight: 900, color: '#080616',
                  fontFamily: 'Inter', opacity: op,
                }}>
                  ₹
                </div>
              );
            })}
            <div style={{
              position: 'absolute', top: 0, left: 100,
              fontSize: 28, color: '#F5F5F5', fontFamily: 'Inter', fontWeight: 600,
              opacity: interpolate(frame, [80, 110], [0, 1], { extrapolateRight: 'clamp' }),
            }}>
              बचाव → 
            </div>
          </div>

          {/* Sinking Bank */}
          <div style={{ textAlign: 'center', transform: `scale(${sp(60)}) translate(${bankShake}px, ${bankShake * 0.5}px)` }}>
            <svg width="280" height="280" viewBox="0 0 280 280">
              <rect x="30" y="100" width="220" height="140" fill="#080616" stroke="#F5F5F5" strokeWidth="3" />
              <polygon points="140,40 20,110 260,110" fill="#080616" stroke="#F5F5F5" strokeWidth="3" />
              <rect x="60" y="140" width="15" height="90" fill="#F5F5F5" />
              <rect x="100" y="140" width="15" height="90" fill="#F5F5F5" />
              <rect x="140" y="140" width="15" height="90" fill="#F5F5F5" />
              <rect x="180" y="140" width="15" height="90" fill="#F5F5F5" />
              <rect x="220" y="140" width="15" height="90" fill="#F5F5F5" />
              {/* Sinking lines */}
              <line x1="30" y1="245" x2="250" y2="270" stroke="#FFC107" strokeWidth="4" strokeDasharray="10,5" />
              {/* SOS */}
              <text x="140" y="80" textAnchor="middle" fill="#FFC107" fontSize="32" fontFamily="Inter" fontWeight="900">SOS</text>
            </svg>
            <div style={{
              marginTop: 12, fontSize: 30, color: '#F5F5F5',
              fontFamily: 'Noto Sans Devanagari', fontWeight: 700,
            }}>
              डूबता बैंक
            </div>
          </div>
        </div>

        {/* Bottom stamp */}
        <div style={{
          position: 'absolute', bottom: 130, left: 0, right: 0,
          textAlign: 'center',
          transform: `scale(${stampScale}) rotate(${stampRot}deg)`,
        }}>
          <div style={{
            display: 'inline-block',
            padding: '20px 50px',
            border: '6px solid #F5F5F5',
            background: 'rgba(8,6,22,0.7)',
            fontFamily: 'Inter',
            fontSize: 54,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: '4px',
          }}>
            "BAIL-OUT" युग
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 40%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1_BailOutEra;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2_CyprusBailIn',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_CyprusBailIn: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pan
  const panX = interpolate(frame, [0, durationInFrames], [-20, 20]);

  // Year counter
  const yearProgress = interpolate(frame, [20, 80], [0, 2013], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const yearDisplay = Math.floor(yearProgress);

  // Cyprus map reveal
  const mapScale = sp(60);

  // Bail-in flip
  const flipRot = interpolate(sp(180, { damping: 10 }), [0, 1], [0, 360]);
  const showBailIn = frame > 220;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(8,6,22,0.9) 0%, rgba(255,0,0,1) 60%, rgba(8,6,22,0.7) 100%)',
      }} />

      {/* Shape layers - triangles */}
      {[0, 1, 2].map(i => {
        const seed = i + 5;
        const x = 200 + i * 600 + Math.sin(frame * 0.01 + i) * 40;
        const y = 100 + seededRandom(seed) * 700;
        const size = 350;
        return (
          <svg key={`tri-${i}`} width={size} height={size} style={{
            position: 'absolute', left: x, top: y,
            opacity: 0.06,
            transform: `rotate(${frame * 0.08 + i * 60}deg)`,
          }}>
            <polygon points={`${size/2},10 ${size-10},${size-10} 10,${size-10}`}
              fill="none" stroke="#F5F5F5" strokeWidth="3" />
          </svg>
        );
      })}

      {/* Motion texture - dotted grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        {Array.from({ length: 12 }).map((_, r) =>
          Array.from({ length: 22 }).map((_, c) => (
            <circle key={`${r}-${c}`} cx={c * 90 + 45} cy={r * 90 + 45} r="2" fill="#F5F5F5" />
          ))
        )}
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const px = (seededRandom(i + 50) * 1920 - frame * 0.8) % 1920;
        const adjustedPx = px < 0 ? px + 1920 : px;
        const py = seededRandom(i + 60) * 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: adjustedPx, top: py,
            width: 3, height: 3, borderRadius: '50%',
            background: '#FFC107', opacity: 0.5,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${panX}px)` }}>
        {/* Year display - HERO */}
        <div style={{
          position: 'absolute', top: 100, left: 0, right: 0,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 220,
            fontWeight: 900, color: '#FFC107',
            letterSpacing: '8px',
            textShadow: '0 0 30px rgba(255,193,7,0.4)',
            opacity: interpolate(frame, [10, 30], [0, 1], { extrapolateRight: 'clamp' }),
          }}>
            {yearDisplay}
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 60,
            color: '#F5F5F5', marginTop: -10,
            opacity: interpolate(frame, [70, 100], [0, 1], { extrapolateRight: 'clamp' }),
          }}>
            CYPRUS संकट
          </div>
        </div>

        {/* Cyprus + Bail-in card */}
        <div style={{
          position: 'absolute', top: 510, left: 0, right: 0,
          display: 'flex', justifyContent: 'center', gap: 60,
        }}>
          {/* Cyprus island visual */}
          <div style={{
            transform: `scale(${mapScale})`,
            background: '#080616',
            border: '4px solid #F5F5F5',
            padding: '30px 40px',
            borderRadius: 16,
            textAlign: 'center',
            width: 480,
          }}>
            <svg width="200" height="80" viewBox="0 0 200 80" style={{ display: 'block', margin: '0 auto 12px' }}>
              <path d="M 20 40 Q 40 15, 80 25 Q 120 10, 160 30 Q 180 50, 150 65 Q 100 75, 50 60 Q 15 55, 20 40 Z"
                fill="#FFC107" stroke="#F5F5F5" strokeWidth="2" />
            </svg>
            <div style={{
              fontFamily: 'Inter', fontSize: 32, color: '#F5F5F5',
              fontWeight: 700,
            }}>
              पहली बार लागू
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 38, color: '#FFC107',
              fontWeight: 900, marginTop: 8,
            }}>
              जमाकर्ताओं पर
            </div>
          </div>

          {/* Bail-in flip card */}
          {showBailIn && (
            <div style={{
              transform: `rotateY(${flipRot}deg) scale(${sp(220)})`,
              background: '#080616',
              border: '4px solid #FFC107',
              padding: '30px 50px',
              borderRadius: 16,
              textAlign: 'center',
              width: 480,
              boxShadow: '0 0 40px rgba(255,193,7,0.4)',
            }}>
              <div style={{
                fontFamily: 'Inter', fontSize: 28, color: '#9E9E9E',
                letterSpacing: '4px', fontWeight: 600,
              }}>
                NEW MODEL
              </div>
              <div style={{
                fontFamily: 'Inter', fontSize: 120, color: '#F5F5F5',
                fontWeight: 900, lineHeight: 1, marginTop: 10,
                letterSpacing: '2px',
              }}>
                BAIL-IN
              </div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#FFC107',
                marginTop: 12, fontWeight: 700,
              }}>
                अब आपकी जेब से
              </div>
            </div>
          )}
        </div>

        {/* Arrow transition indicator */}
        {frame > 160 && frame < 230 && (
          <div style={{
            position: 'absolute', top: 600, left: 870,
            opacity: interpolate(frame, [160, 180, 220, 230], [0, 1, 1, 0]),
          }}>
            <svg width="180" height="80">
              <defs>
                <marker id="ar2" markerWidth="14" markerHeight="14" refX="10" refY="5" orient="auto">
                  <polygon points="0,0 14,5 0,10" fill="#F5F5F5" />
                </marker>
              </defs>
              <line x1="0" y1="40" x2="160" y2="40" stroke="#F5F5F5" strokeWidth="6" markerEnd="url(#ar2)" />
            </svg>
          </div>
        )}
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_CyprusBailIn;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3_FRDIBill',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_FRDIBill: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Tilt camera
  const tilt = interpolate(frame, [0, durationInFrames], [-1, 1]);

  // Document scroll in
  const docY = interpolate(sp(30, { damping: 14, stiffness: 100 }), [0, 1], [1100, 280]);

  // Stamp INDIA
  const stampScale = sp(150, { damping: 8 });

  // Underline
  const underlineW = interpolate(sp(100), [0, 1], [0, 700]);

  // Acronym reveal letters
  const letters = ['F', 'R', 'D', 'I'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at top, rgba(255,0,0,1) 0%, rgba(8,6,22,0.9) 100%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map(i => {
        const seed = i + 11;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 5) * 1080;
        const w = 200 + seededRandom(seed + 10) * 300;
        return (
          <div key={`s-${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: w, height: w * 0.6,
            border: '2px solid #F5F5F5',
            opacity: 0.05,
            transform: `rotate(${frame * 0.06 + i * 30}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.05})`,
          }} />
        );
      })}

      {/* Motion texture - horizontal stripes */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <line key={i} x1="0" y1={i * 40} x2="1920" y2={i * 40} stroke="#F5F5F5" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = seededRandom(i + 70) * 1920;
        const py = (seededRandom(i + 80) * 1080 + frame * 0.6) % 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: px, top: py,
            width: 3, height: 3, borderRadius: '50%',
            background: '#FFC107', opacity: 0.5,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${tilt * 0.3}deg)` }}>
        {/* Top header */}
        <div style={{
          position: 'absolute', top: 60, left: 0, right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [5, 25], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            display: 'inline-block',
            fontFamily: 'JetBrains Mono', fontSize: 38,
            color: '#FFC107', fontWeight: 700,
            letterSpacing: '6px',
            background: '#080616',
            padding: '10px 30px',
            borderRadius: 6,
          }}>
            INDIA · 2017
          </div>
        </div>

        {/* The Bill Document */}
        <div style={{
          position: 'absolute', top: docY, left: 460,
          width: 1000, minHeight: 540,
          background: '#F5F5F5',
          padding: '40px 60px',
          boxShadow: '0 30px 80px rgba(0,0,0,0.5)',
          border: '3px solid #080616',
        }}>
          {/* Bill header */}
          <div style={{
            fontFamily: 'Inter', fontSize: 28,
            color: '#9E9E9E', fontWeight: 600,
            letterSpacing: '4px',
          }}>
            BILL · PROPOSED
          </div>

          {/* Acronym FRDI - big letters */}
          <div style={{
            display: 'flex', gap: 20, marginTop: 20,
          }}>
            {letters.map((L, i) => {
              const lOp = interpolate(frame, [60 + i * 12, 80 + i * 12], [0, 1], { extrapolateRight: 'clamp' });
              const lY = interpolate(sp(60 + i * 12), [0, 1], [40, 0]);
              return (
                <div key={i} style={{
                  fontFamily: 'Yantra One',
                  fontSize: 180, color: '#FF0000',
                  fontWeight: 900, lineHeight: 1,
                  opacity: lOp,
                  transform: `translateY(${lY}px)`,
                }}>
                  {L}
                </div>
              );
            })}
          </div>

          <div style={{
            height: 4, background: '#FF0000',
            width: underlineW, marginTop: 10,
          }} />

          {/* Full form */}
          <div style={{
            marginTop: 30,
            fontFamily: 'Inter', fontSize: 36,
            color: '#080616', fontWeight: 700,
            opacity: interpolate(frame, [130, 160], [0, 1], { extrapolateRight: 'clamp' }),
          }}>
            <span style={{ color: '#FF0000' }}>F</span>inancial{' '}
            <span style={{ color: '#FF0000' }}>R</span>esolution &
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 36,
            color: '#080616', fontWeight: 700, marginTop: 4,
            opacity: interpolate(frame, [150, 180], [0, 1], { extrapolateRight: 'clamp' }),
          }}>
            <span style={{ color: '#FF0000' }}>D</span>eposit{' '}
            <span style={{ color: '#FF0000' }}>I</span>nsurance Bill
          </div>

          {/* Hindi tagline */}
          <div style={{
            marginTop: 30,
            background: '#080616',
            color: '#FFC107',
            padding: '14px 22px',
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 30, fontWeight: 700,
            opacity: interpolate(frame, [200, 230], [0, 1], { extrapolateRight: 'clamp' }),
          }}>
            लक्ष्य: Bail-In को भारत में लाना
          </div>
        </div>

        {/* PROPOSED stamp diagonally */}
        <div style={{
          position: 'absolute', top: 700, right: 380,
          transform: `scale(${stampScale}) rotate(-18deg)`,
        }}>
          <div style={{
            padding: '16px 40px',
            border: '6px solid #FFC107',
            color: '#FFC107',
            fontFamily: 'Inter', fontSize: 60,
            fontWeight: 900,
            letterSpacing: '4px',
            background: 'rgba(8,6,22,0.85)',
          }}>
            PROPOSED
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 45%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3_FRDIBill;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4_PublicOutcry',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4_PublicOutcry: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Screen shake on impact
  const shake = frame > 90 && frame < 130
    ? Math.sin(frame * 1.5) * 4
    : 0;

  // Stamp "WITHDRAWN"
  const wScale = sp(180, { damping: 6, stiffness: 200 });
  const wRot = interpolate(sp(180), [0, 1], [-30, -12]);

  // Rip / tear effect on document
  const ripProgress = interpolate(frame, [200, 260], [0, 100], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });

  // Noise/protest indicator bars
  const protestY = sp(40);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(255,0,0,1) 0%, rgba(8,6,22,0.95) 100%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2].map(i => {
        const seed = i + 17;
        const x = 100 + i * 700;
        const y = 200 + seededRandom(seed) * 600;
        const s = 400;
        return (
          <div key={`sh-${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: s, height: s,
            border: '3px solid #F5F5F5',
            opacity: 0.05,
            transform: `rotate(${frame * 0.05 + i * 45}deg)`,
            clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
          }} />
        );
      })}

      {/* Motion texture - protest waves */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        {Array.from({ length: 8 }).map((_, i) => {
          const y = i * 140;
          return (
            <path key={i}
              d={`M 0 ${y} Q 480 ${y - 20} 960 ${y} T 1920 ${y}`}
              fill="none" stroke="#F5F5F5" strokeWidth="2" />
          );
        })}
      </svg>

      {/* Particles - protest sparks */}
      {Array.from({ length: 30 }).map((_, i) => {
        const px = seededRandom(i + 90) * 1920;
        const py = (seededRandom(i + 100) * 1080 - frame * 1.2) % 1080;
        const adj = py < 0 ? py + 1080 : py;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: px, top: adj,
            width: 4, height: 4, borderRadius: '50%',
            background: '#FFC107', opacity: 0.6,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translate(${shake}px, ${shake * 0.5}px)` }}>
        {/* Top label */}
        <div style={{
          position: 'absolute', top: 60, left: 0, right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [5, 25], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            display: 'inline-block',
            fontFamily: 'JetBrains Mono', fontSize: 32,
            color: '#F5F5F5', fontWeight: 700,
            letterSpacing: '6px',
            background: '#080616',
            padding: '10px 30px',
            border: '2px solid #FFC107',
          }}>
            जनता का गुस्सा · 2017-18
          </div>
        </div>

        {/* Protest bars - sound levels */}
        <div style={{
          position: 'absolute', top: 200, left: 100,
          display: 'flex', alignItems: 'flex-end', gap: 12,
          transform: `scaleY(${protestY})`,
          transformOrigin: 'bottom',
        }}>
          {Array.from({ length: 18 }).map((_, i) => {
            const h = 60 + Math.abs(Math.sin(frame * 0.15 + i * 0.5)) * 220;
            return (
              <div key={i} style={{
                width: 28, height: h,
                background: i % 3 === 0 ? '#FFC107' : '#F5F5F5',
                opacity: 0.85,
              }} />
            );
          })}
        </div>

        {/* Right side mirror bars */}
        <div style={{
          position: 'absolute', top: 200, right: 100,
          display: 'flex', alignItems: 'flex-end', gap: 12,
          transform: `scaleY(${protestY})`,
          transformOrigin: 'bottom',
        }}>
          {Array.from({ length: 18 }).map((_, i) => {
            const h = 60 + Math.abs(Math.cos(frame * 0.15 + i * 0.5)) * 220;
            return (
              <div key={i} style={{
                width: 28, height: h,
                background: i % 3 === 0 ? '#FFC107' : '#F5F5F5',
                opacity: 0.85,
              }} />
            );
          })}
        </div>

        {/* Central HEADLINE */}
        <div style={{
          position: 'absolute', top: 350, left: 0, right: 0,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 140,
            color: '#F5F5F5',
            lineHeight: 1, letterSpacing: '4px',
            opacity: interpolate(frame, [40, 70], [0, 1], { extrapolateRight: 'clamp' }),
            transform: `scale(${sp(40)})`,
          }}>
            हंगामा!
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 56,
            color: '#FFC107', fontWeight: 800,
            marginTop: 18, letterSpacing: '3px',
            opacity: interpolate(frame, [90, 120], [0, 1], { extrapolateRight: 'clamp' }),
          }}>
            BAIL-IN CLAUSE = OUTRAGE
          </div>
        </div>

        {/* Year transition */}
        <div style={{
          position: 'absolute', bottom: 280, left: 0, right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [130, 160], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            display: 'inline-flex', alignItems: 'center', gap: 30,
            background: '#080616',
            padding: '18px 40px',
            border: '3px solid #F5F5F5',
            borderRadius: 12,
          }}>
            <span style={{
              fontFamily: 'JetBrains Mono', fontSize: 56,
              color: '#9E9E9E', fontWeight: 900,
            }}>2017</span>
            <svg width="80" height="40">
              <defs>
                <marker id="ar4" markerWidth="14" markerHeight="14" refX="10" refY="5" orient="auto">
                  <polygon points="0,0 14,5 0,10" fill="#FFC107" />
                </marker>
              </defs>
              <line x1="0" y1="20" x2="60" y2="20" stroke="#FFC107" strokeWidth="5" markerEnd="url(#ar4)" />
            </svg>
            <span style={{
              fontFamily: 'JetBrains Mono', fontSize: 56,
              color: '#FFC107', fontWeight: 900,
            }}>2018</span>
          </div>
        </div>

        {/* WITHDRAWN stamp */}
        <div style={{
          position: 'absolute', bottom: 110, left: 0, right: 0,
          textAlign: 'center',
          transform: `scale(${wScale}) rotate(${wRot}deg)`,
        }}>
          <div style={{
            display: 'inline-block',
            padding: '20px 60px',
            border: '8px solid #FFC107',
            color: '#FFC107',
            fontFamily: 'Inter', fontSize: 90,
            fontWeight: 900, letterSpacing: '8px',
            background: 'rgba(8,6,22,0.85)',
          }}>
            WITHDRAWN
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 40%, rgba(0,0,0,0.65) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4_PublicOutcry;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5_BailInMechanism',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5_BailInMechanism: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Orbit camera
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.0]);

  // Sinking bank in center
  const bankSink = interpolate(frame, [30, 200], [0, 20], { extrapolateRight: 'clamp' });
  const bankRot = Math.sin(frame * 0.04) * 2;

  // Money particles flowing FROM customers TO bank
  const cuStomerEntry = sp(60);

  // X mark over government
  const xMark = sp(150, { damping: 8 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(8,6,22,0.7) 0%, rgba(255,0,0,1) 80%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map(i => {
        const seed = i + 23;
        const x = seededRandom(seed) * 1700;
        const y = seededRandom(seed + 5) * 900;
        const s = 280;
        return (
          <div key={`sh-${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: s, height: s, borderRadius: '50%',
            border: '3px solid #F5F5F5',
            opacity: 0.04,
            transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.1})`,
          }} />
        );
      })}

      {/* Motion texture - radial lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 24 }).map((_, i) => {
          const angle = (i / 24) * Math.PI * 2;
          const cx = 960, cy = 540;
          const x2 = cx + Math.cos(angle) * 900;
          const y2 = cy + Math.sin(angle) * 900;
          return (
            <line key={i} x1={cx} y1={cy} x2={x2} y2={y2}
              stroke="#F5F5F5" strokeWidth="1" />
          );
        })}
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const px = seededRandom(i + 110) * 1920;
        const py = (seededRandom(i + 120) * 1080 + frame * 0.5) % 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: px, top: py,
            width: 3, height: 3, borderRadius: '50%',
            background: '#FFC107', opacity: 0.5,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Header */}
        <div style={{
          position: 'absolute', top: 50, left: 0, right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [5, 25], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            display: 'inline-block',
            fontFamily: 'Yantra One', fontSize: 64,
            color: '#F5F5F5',
            background: '#080616',
            padding: '14px 40px',
            borderTop: '5px solid #FFC107',
            borderBottom: '5px solid #FFC107',
          }}>
            BAIL-IN कैसे काम करता है?
          </div>
        </div>

        {/* Central Drowning Bank */}
        <div style={{
          position: 'absolute', top: 280 + bankSink, left: 760,
          transform: `rotate(${bankRot}deg) scale(${sp(20)})`,
        }}>
          <svg width="400" height="350" viewBox="0 0 400 350">
            <polygon points="200,30 30,110 370,110" fill="#080616" stroke="#F5F5F5" strokeWidth="4" />
            <rect x="50" y="110" width="300" height="200" fill="#080616" stroke="#F5F5F5" strokeWidth="4" />
            <rect x="80" y="150" width="20" height="140" fill="#F5F5F5" />
            <rect x="130" y="150" width="20" height="140" fill="#F5F5F5" />
            <rect x="180" y="150" width="20" height="140" fill="#F5F5F5" />
            <rect x="230" y="150" width="20" height="140" fill="#F5F5F5" />
            <rect x="280" y="150" width="20" height="140" fill="#F5F5F5" />
            <rect x="320" y="150" width="20" height="140" fill="#F5F5F5" />
            <text x="200" y="80" textAnchor="middle" fill="#FFC107" fontSize="36" fontFamily="Inter" fontWeight="900">BANK</text>
            {/* Water/sinking waves */}
            <path d={`M 30 ${310 + Math.sin(frame * 0.2) * 6} Q 100 ${300 + Math.sin(frame * 0.2 + 1) * 6} 200 ${315 + Math.sin(frame * 0.2 + 2) * 6} T 370 ${310 + Math.sin(frame * 0.2 + 3) * 6}`}
              fill="none" stroke="#FFC107" strokeWidth="4" />
          </svg>
          <div style={{
            textAlign: 'center', marginTop: -10,
            fontFamily: 'Noto Sans Devanagari', fontSize: 30,
            color: '#F5F5F5', fontWeight: 700,
          }}>
            डूबता बैंक
          </div>
        </div>

        {/* LEFT: Government with X */}
        <div style={{
          position: 'absolute', left: 80, top: 380,
          textAlign: 'center',
          transform: `scale(${sp(80)})`,
        }}>
          <div style={{ position: 'relative', width: 260, height: 200 }}>
            <svg width="260" height="200" viewBox="0 0 260 200">
              <rect x="30" y="60" width="200" height="130" fill="#080616" stroke="#F5F5F5" strokeWidth="3" />
              <polygon points="130,20 20,70 240,70" fill="#9E9E9E" />
              <rect x="60" y="90" width="18" height="90" fill="#F5F5F5" opacity="0.5" />
              <rect x="100" y="90" width="18" height="90" fill="#F5F5F5" opacity="0.5" />
              <rect x="140" y="90" width="18" height="90" fill="#F5F5F5" opacity="0.5" />
              <rect x="180" y="90" width="18" height="90" fill="#F5F5F5" opacity="0.5" />
            </svg>
            {/* Big X */}
            <svg width="280" height="220" style={{ position: 'absolute', top: -10, left: -10, transform: `scale(${xMark})` }}>
              <line x1="40" y1="40" x2="240" y2="180" stroke="#FFC107" strokeWidth="14" strokeLinecap="round" />
              <line x1="240" y1="40" x2="40" y2="180" stroke="#FFC107" strokeWidth="14" strokeLinecap="round" />
            </svg>
          </div>
          <div style={{
            marginTop: 20,
            fontFamily: 'Noto Sans Devanagari', fontSize: 28,
            color: '#F5F5F5', fontWeight: 700,
          }}>
            सरकार पैसा नहीं देगी
          </div>
        </div>

        {/* RIGHT: Depositors (people grid) */}
        <div style={{
          position: 'absolute', right: 80, top: 380,
          textAlign: 'center',
          transform: `scale(${cuStomerEntry})`,
        }}>
          <div style={{
            display: 'grid',
            gridTemplateColumns: 'repeat(4, 1fr)',
            gap: 12,
            width: 280,
          }}>
            {Array.from({ length: 12 }).map((_, i) => (
              <div key={i} style={{
                width: 60, height: 60,
                background: '#FFC107',
                border: '3px solid #080616',
                borderRadius: 10,
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                fontFamily: 'Inter', fontSize: 30, fontWeight: 900,
                color: '#080616',
                transform: `scale(${0.8 + Math.sin(frame * 0.1 + i * 0.5) * 0.05})`,
              }}>
                ₹
              </div>
            ))}
          </div>
          <div style={{
            marginTop: 20,
            fontFamily: 'Noto Sans Devanagari', fontSize: 28,
            color: '#F5F5F5', fontWeight: 700,
          }}>
            आम जमाकर्ता
          </div>
        </div>

        {/* Money flowing from depositors to bank */}
        {Array.from({ length: 6 }).map((_, i) => {
          const delay = 150 + i * 18;
          const localFrame = Math.max(0, frame - delay);
          const progress = interpolate(localFrame, [0, 70], [0, 1], { extrapolateRight: 'clamp' });
          const startX = 1620, startY = 460;
          const endX = 1000, endY = 400;
          const cx = startX + (endX - startX) * progress;
          const cy = startY + (endY - startY) * progress - Math.sin(progress * Math.PI) * 80;
          const op = interpolate(localFrame, [0, 8, 65, 70], [0, 1, 1, 0]);
          return (
            <div key={`fly-${i}`} style={{
              position: 'absolute', left: cx, top: cy,
              width: 50, height: 50, borderRadius: 8,
              background: '#FFC107', border: '3px solid #080616',
              display: 'flex', alignItems: 'center', justifyContent: 'center',
              fontFamily: 'Inter', fontSize: 26, fontWeight: 900,
              color: '#080616',
              opacity: op,
            }}>
              ₹
            </div>
          );
        })}

        {/* Bottom caption */}
        <div style={{
          position: 'absolute', bottom: 80, left: 0, right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [240, 270], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            display: 'inline-block',
            background: '#080616',
            padding: '16px 36px',
            border: '3px solid #FFC107',
            fontFamily: 'Inter', fontSize: 44,
            color: '#FFC107', fontWeight: 900,
            letterSpacing: '3px',
          }}>
            आपका पैसा = बैंक की बेल-इन रकम
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 40%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene5_BailInMechanism;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6_DepositToShares',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6_DepositToShares: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Zoom in
  const zoom = interpolate(frame, [0, durationInFrames], [1.0, 1.08]);

  // Conversion animation - deposit to share
  const conversionProgress = interpolate(frame, [80, 180], [0, 1], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });

  // Chart crashing
  const chartProgress = interpolate(frame, [200, 290], [0, 1], { extrapolateRight: 'clamp', extrapolateLeft: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(255,0,0,1) 0%, rgba(8,6,22,0.95) 100%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2].map(i => {
        const seed = i + 31;
        const x = 150 + i * 650;
        const y = 100 + seededRandom(seed) * 700;
        return (
          <svg key={`sh-${i}`} width="400" height="400" style={{
            position: 'absolute', left: x, top: y,
            opacity: 0.06,
            transform: `rotate(${frame * 0.07 + i * 60}deg)`,
          }}>
            <polygon points="200,20 380,200 200,380 20,200" fill="none" stroke="#F5F5F5" strokeWidth="3" />
          </svg>
        );
      })}

      {/* Motion texture - candle stick lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 30 }).map((_, i) => {
          const x = i * 65 + 30;
          const h = 50 + Math.abs(Math.sin(i + frame * 0.05)) * 150;
          return (
            <rect key={i} x={x} y={1080 - h} width={20} height={h} fill="#F5F5F5" />
          );
        })}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = seededRandom(i + 130) * 1920;
        const py = (seededRandom(i + 140) * 1080 + frame * 0.7) % 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: px, top: py,
            width: 3, height: 3, borderRadius: '50%',
            background: '#FFC107', opacity: 0.5,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${zoom})` }}>
        {/* Header */}
        <div style={{
          position: 'absolute', top: 50, left: 0, right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [5, 25], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 62,
            color: '#F5F5F5',
            display: 'inline-block',
            background: '#080616',
            padding: '14px 36px',
            borderLeft: '6px solid #FFC107',
          }}>
            जबरदस्ती परिवर्तन
          </div>
        </div>

        {/* Conversion visual: Deposit → Share */}
        <div style={{
          position: 'absolute', top: 240, left: 0, right: 0,
          display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 0,
        }}>
          {/* DEPOSIT side */}
          <div style={{
            transform: `scale(${sp(30)})`,
            background: '#080616',
            border: '4px solid #FFC107',
            padding: '30px 36px',
            borderRadius: 16,
            width: 420,
            textAlign: 'center',
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 26,
              color: '#9E9E9E', letterSpacing: '3px', fontWeight: 600,
            }}>
              YOUR DEPOSIT
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 90,
              color: '#FFC107', fontWeight: 900, lineHeight: 1, marginTop: 14,
            }}>
              ₹10L
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 26,
              color: '#F5F5F5', marginTop: 14, fontWeight: 700,
            }}>
              आपकी मेहनत की कमाई
            </div>
            {/* Solid value bar */}
            <div style={{
              height: 14, background: '#FFC107',
              marginTop: 16, borderRadius: 7,
            }} />
          </div>

          {/* Conversion arrow with rotating gears */}
          <div style={{
            width: 240, height: 200,
            display: 'flex', flexDirection: 'column',
            alignItems: 'center', justifyContent: 'center',
            margin: '0 30px',
          }}>
            <svg width="100" height="100" style={{ transform: `rotate(${frame * 4}deg)` }}>
              <circle cx="50" cy="50" r="35" fill="none" stroke="#F5F5F5" strokeWidth="6" />
              {Array.from({ length: 8 }).map((_, i) => {
                const a = (i / 8) * Math.PI * 2;
                const x1 = 50 + Math.cos(a) * 35;
                const y1 = 50 + Math.sin(a) * 35;
                const x2 = 50 + Math.cos(a) * 48;
                const y2 = 50 + Math.sin(a) * 48;
                return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#F5F5F5" strokeWidth="6" />;
              })}
            </svg>
            <svg width="180" height="40" style={{ marginTop: 10 }}>
              <defs>
                <marker id="ar6" markerWidth="14" markerHeight="14" refX="10" refY="5" orient="auto">
                  <polygon points="0,0 14,5 0,10" fill="#F5F5F5" />
                </marker>
              </defs>
              <line x1="0" y1="20" x2="160" y2="20" stroke="#F5F5F5" strokeWidth="6" strokeDasharray="12,6" markerEnd="url(#ar6)" />
            </svg>
            <div style={{
              fontFamily: 'Inter', fontSize: 22,
              color: '#F5F5F5', marginTop: 8, fontWeight: 700,
              letterSpacing: '2px',
            }}>
              FORCED
            </div>
          </div>

          {/* WORTHLESS SHARES side */}
          <div style={{
            transform: `scale(${sp(120)})`,
            background: '#080616',
            border: '4px solid #F5F5F5',
            padding: '30px 36px',
            borderRadius: 16,
            width: 420,
            textAlign: 'center',
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 26,
              color: '#9E9E9E', letterSpacing: '3px', fontWeight: 600,
            }}>
              BANK SHARES
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 90,
              color: '#F5F5F5', fontWeight: 900, lineHeight: 1, marginTop: 14,
              textDecoration: 'line-through',
              textDecorationColor: '#FFC107',
              textDecorationThickness: 6,
            }}>
              ₹0
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 26,
              color: '#FFC107', marginTop: 14, fontWeight: 700,
            }}>
              बेकार कागज़ के टुकड़े
            </div>
            {/* Crashing bar - falling */}
            <div style={{
              height: 14, background: '#F5F5F5',
              marginTop: 16, borderRadius: 7,
              width: `${(1 - chartProgress) * 100}%`,
              opacity: 0.5,
            }} />
          </div>
        </div>

        {/* Bottom warning bar */}
        <div style={{
          position: 'absolute', bottom: 100, left: 0, right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [220, 250], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            display: 'inline-block',
            background: '#FFC107',
            color: '#080616',
            padding: '14px 40px',
            fontFamily: 'Inter', fontSize: 42,
            fontWeight: 900,
            letterSpacing: '3px',
          }}>
            निश्चित सीमा से ऊपर का पैसा = खतरे में
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 45%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene6_DepositToShares;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7_InternationalStandard',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7_InternationalStandard: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Globe rotation
  const rot = frame * 1.2;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, rgba(8,6,22,0.85) 0%, rgba(255,0,0,1) 70%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map(i => {
        const seed = i + 37;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 5) * 1080;
        const s = 250 + i * 60;
        return (
          <div key={`sh-${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: s, height: s, borderRadius: '50%',
            border: '2px solid #F5F5F5', opacity: 0.04,
          }} />
        );
      })}

      {/* Motion texture - latitude lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 14 }).map((_, i) => (
          <ellipse key={i} cx="960" cy="540" rx={400 + i * 30} ry={150 + i * 20}
            fill="none" stroke="#F5F5F5" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const px = seededRandom(i + 150) * 1920;
        const py = (seededRandom(i + 160) * 1080 + frame * 0.6) % 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: px, top: py,
            width: 3, height: 3, borderRadius: '50%',
            background: '#FFC107', opacity: 0.5,
          }} />
        );
      })}

      {/* Header */}
      <div style={{
        position: 'absolute', top: 60, left: 0, right: 0,
        textAlign: 'center',
        opacity: interpolate(frame, [5, 25], [0, 1], { extrapolateRight: 'clamp' }),
        transform: `translateY(${interpolate(sp(10), [0, 1], [-30, 0])}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 30,
          color: '#FFC107', letterSpacing: '6px', fontWeight: 700,
        }}>
          GLOBAL ADOPTION · NEW NORM
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 84,
          color: '#F5F5F5', marginTop: 14,
        }}>
          अंतर्राष्ट्रीय मानक
        </div>
      </div>

      {/* Center Globe with countries */}
      <div style={{
        position: 'absolute', top: 290, left: 0, right: 0,
        display: 'flex', justifyContent: 'center',
        transform: `scale(${sp(30)})`,
      }}>
        <div style={{ position: 'relative', width: 500, height: 500 }}>
          {/* Globe circle */}
          <svg width="500" height="500" style={{ position: 'absolute' }}>
            <circle cx="250" cy="250" r="220" fill="#080616" stroke="#FFC107" strokeWidth="4" />
            {/* Lat/long */}
            <ellipse cx="250" cy="250" rx="220" ry="80" fill="none" stroke="#9E9E9E" strokeWidth="2" />
            <ellipse cx="250" cy="250" rx="220" ry="40" fill="none" stroke="#9E9E9E" strokeWidth="1" />
            <ellipse cx="250" cy="250" rx="60" ry="220" fill="none" stroke="#9E9E9E" strokeWidth="2" />
            <ellipse cx="250" cy="250" rx="150" ry="220" fill="none" stroke="#9E9E9E" strokeWidth="1" />
            <line x1="30" y1="250" x2="470" y2="250" stroke="#9E9E9E" strokeWidth="2" />
            {/* Continents approx */}
            <g transform={`rotate(${rot}, 250, 250)`}>
              <path d="M 180 180 Q 220 160 260 180 Q 290 200 270 230 Q 230 240 200 220 Z" fill="#FFC107" opacity="0.7" />
              <path d="M 290 200 Q 340 180 380 220 Q 370 270 320 280 Q 290 250 290 200 Z" fill="#FFC107" opacity="0.7" />
              <path d="M 150 280 Q 200 290 220 320 Q 200 350 160 340 Q 130 320 150 280 Z" fill="#FFC107" opacity="0.7" />
              <path d="M 310 300 Q 360 310 370 340 Q 340 370 310 350 Z" fill="#FFC107" opacity="0.7" />
            </g>
          </svg>

          {/* Orbiting country tags */}
          {['USA', 'UK', 'EU', 'JAPAN', 'INDIA?'].map((country, i) => {
            const angle = (i / 5) * Math.PI * 2 + frame * 0.01;
            const radius = 290;
            const cx = 250 + Math.cos(angle) * radius;
            const cy = 250 + Math.sin(angle) * radius;
            const tagOp = interpolate(frame, [60 + i * 15, 90 + i * 15], [0, 1], { extrapolateRight: 'clamp' });
            return (
              <div key={i} style={{
                position: 'absolute', left: cx - 50, top: cy - 22,
                width: 100, padding: '6px 10px',
                background: country === 'INDIA?' ? '#FFC107' : '#F5F5F5',
                color: '#080616',
                fontFamily: 'Inter', fontSize: 22, fontWeight: 900,
                textAlign: 'center', borderRadius: 6,
                border: '2px solid #080616',
                opacity: tagOp,
              }}>
                {country}
              </div>
            );
          })}
        </div>
      </div>

      {/* Bottom statement */}
      <div style={{
        position: 'absolute', bottom: 90, left: 0, right: 0,
        textAlign: 'center',
        opacity: interpolate(frame, [200, 230], [0, 1], { extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          display: 'inline-block',
          background: '#080616',
          border: '3px solid #FFC107',
          padding: '14px 36px',
          fontFamily: 'Noto Sans Devanagari', fontSize: 38,
          color: '#F5F5F5', fontWeight: 700,
        }}>
          आज नहीं तो कल — भारत भी इसी राह पर
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 45%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene7_InternationalStandard;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8_DICGCIntro',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8_DICGCIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter for 5 LAKH
  const counter = interpolate(sp(80, { damping: 18, stiffness: 80 }), [0, 1], [0, 500000]);

  // Shield reveal
  const shieldScale = sp(40, { damping: 8 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 30%, rgba(8,6,22,0.7) 0%, rgba(255,0,0,1) 80%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2].map(i => {
        const seed = i + 43;
        const x = seededRandom(seed) * 1700;
        const y = seededRandom(seed + 4) * 900;
        return (
          <div key={`sh-${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: 380, height: 380,
            border: '3px solid #F5F5F5', opacity: 0.05,
            transform: `rotate(45deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.05})`,
          }} />
        );
      })}

      {/* Motion texture - vertical bars */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 40 }).map((_, i) => (
          <rect key={i} x={i * 48 + 20} y="0" width="2" height="1080" fill="#F5F5F5" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const px = (seededRandom(i + 170) * 1920 + frame * 0.5) % 1920;
        const py = seededRandom(i + 180) * 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: px, top: py,
            width: 3, height: 3, borderRadius: '50%',
            background: '#FFC107', opacity: 0.6,
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0,
        textAlign: 'center',
        opacity: interpolate(frame, [5, 25], [0, 1], { extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          display: 'inline-block',
          fontFamily: 'JetBrains Mono', fontSize: 32,
          color: '#FFC107', letterSpacing: '8px', fontWeight: 700,
          background: '#080616', padding: '10px 26px',
        }}>
          भारतीय ढाल · INDIA SAFETY NET
        </div>
      </div>

      {/* Shield with DICGC */}
      <div style={{
        position: 'absolute', top: 200, left: 0, right: 0,
        display: 'flex', justifyContent: 'center',
        transform: `scale(${shieldScale})`,
      }}>
        <div style={{ position: 'relative', width: 400, height: 460 }}>
          <svg width="400" height="460" viewBox="0 0 400 460">
            <path d="M 200 20 L 380 90 L 360 280 Q 300 400 200 440 Q 100 400 40 280 L 20 90 Z"
              fill="#080616" stroke="#FFC107" strokeWidth="6" />
            <text x="200" y="180" textAnchor="middle" fill="#FFC107" fontSize="60" fontFamily="Inter" fontWeight="900">DICGC</text>
            <text x="200" y="230" textAnchor="middle" fill="#F5F5F5" fontSize="22" fontFamily="Inter" fontWeight="600">Deposit Insurance</text>
            <text x="200" y="260" textAnchor="middle" fill="#F5F5F5" fontSize="22" fontFamily="Inter" fontWeight="600">& Credit Guarantee</text>
            <text x="200" y="290" textAnchor="middle" fill="#F5F5F5" fontSize="22" fontFamily="Inter" fontWeight="600">Corporation</text>
            <line x1="80" y1="320" x2="320" y2="320" stroke="#FFC107" strokeWidth="2" />
            <text x="200" y="365" textAnchor="middle" fill="#F5F5F5" fontSize="28" fontFamily="Noto Sans Devanagari" fontWeight="700">RBI की</text>
            <text x="200" y="400" textAnchor="middle" fill="#F5F5F5" fontSize="28" fontFamily="Noto Sans Devanagari" fontWeight="700">सहायक</text>
          </svg>
        </div>
      </div>

      {/* RIGHT: Big number */}
      <div style={{
        position: 'absolute', top: 280, right: 120,
        textAlign: 'right',
        transform: `scale(${sp(80)})`,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 36,
          color: '#F5F5F5', fontWeight: 700,
          letterSpacing: '3px',
        }}>
          प्रति खाता बीमा
        </div>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 200,
          color: '#FFC107', fontWeight: 900, lineHeight: 1,
          marginTop: 10,
          textShadow: '0 0 40px rgba(255,193,7,0.4)',
        }}>
          ₹{Math.floor(counter).toLocaleString('en-IN')}
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 50,
          color: '#F5F5F5', fontWeight: 700, marginTop: 10,
        }}>
          5 लाख रुपये तक
        </div>
        <div style={{
          marginTop: 16,
          height: 6, width: 480,
          background: '#FFC107',
          marginLeft: 'auto',
        }} />
      </div>

      {/* Bottom caption */}
      <div style={{
        position: 'absolute', bottom: 70, left: 0, right: 0,
        textAlign: 'center',
        opacity: interpolate(frame, [180, 210], [0, 1], { extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          display: 'inline-block',
          background: '#080616',
          color: '#F5F5F5',
          padding: '12px 32px',
          fontFamily: 'Inter', fontSize: 36,
          fontWeight: 700, letterSpacing: '2px',
          border: '2px solid #FFC107',
        }}>
          सुनने में अच्छा · पर असलियत?
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 45%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene8_DICGCIntro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9_DICGCRealityCheck',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9_DICGCRealityCheck: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pan
  const pan = interpolate(frame, [0, durationInFrames], [10, -10]);

  // Left arc: 97.6%
  const leftPercent = interpolate(sp(60, { damping: 18, stiffness: 80 }), [0, 1], [0, 97.6]);
  const leftDash = (leftPercent / 100) * 565; // 2*pi*r where r=90

  // Right arc: 41.5%
  const rightPercent = interpolate(sp(180, { damping: 18, stiffness: 80 }), [0, 1], [0, 41.5]);
  const rightDash = (rightPercent / 100) * 565;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(8,6,22,0.9) 0%, rgba(255,0,0,1) 100%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map(i => {
        const seed = i + 51;
        const x = seededRandom(seed) * 1700;
        const y = seededRandom(seed + 4) * 900;
        const s = 200 + seededRandom(seed + 8) * 300;
        return (
          <div key={`sh-${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: s, height: s,
            border: '2px solid #F5F5F5', opacity: 0.04,
            borderRadius: i % 2 === 0 ? '50%' : '20px',
            transform: `rotate(${frame * 0.05 + i * 30}deg)`,
          }} />
        );
      })}

      {/* Motion texture - grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({ length: 12 }).map((_, i) => (
          <line key={`h${i}`} x1="0" y1={i * 90} x2="1920" y2={i * 90} stroke="#F5F5F5" strokeWidth="1" />
        ))}
        {Array.from({ length: 22 }).map((_, i) => (
          <line key={`v${i}`} x1={i * 90} y1="0" x2={i * 90} y2="1080" stroke="#F5F5F5" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const px = seededRandom(i + 190) * 1920;
        const py = (seededRandom(i + 200) * 1080 + frame * 0.6) % 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: px, top: py,
            width: 3, height: 3, borderRadius: '50%',
            background: '#FFC107', opacity: 0.5,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${pan}px)` }}>
        {/* Header */}
        <div style={{
          position: 'absolute', top: 50, left: 0, right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [5, 25], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 26,
            color: '#9E9E9E', letterSpacing: '6px', fontWeight: 700,
          }}>
            DATA · MARCH 2025
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 64,
            color: '#F5F5F5', marginTop: 10,
          }}>
            DICGC का असली सच
          </div>
        </div>

        {/* Two Circular comparison */}
        <div style={{
          position: 'absolute', top: 230, left: 0, right: 0,
          display: 'flex', justifyContent: 'space-around', alignItems: 'center', padding: '0 80px',
        }}>
          {/* LEFT: 97.6% accounts - GREEN ILLUSION */}
          <div style={{
            textAlign: 'center',
            transform: `scale(${sp(20)})`,
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 28,
              color: '#FFC107', fontWeight: 700,
              letterSpacing: '3px', marginBottom: 16,
            }}>
              खातों की संख्या
            </div>
            <svg width="320" height="320" viewBox="0 0 200 200">
              <circle cx="100" cy="100" r="90" fill="none" stroke="#080616" strokeWidth="20" />
              <circle cx="100" cy="100" r="90" fill="none" stroke="#FFC107" strokeWidth="20"
                strokeDasharray={`${leftDash} 565`}
                transform="rotate(-90, 100, 100)" strokeLinecap="round" />
              <text x="100" y="95" textAnchor="middle" fill="#F5F5F5" fontSize="44" fontFamily="JetBrains Mono" fontWeight="900">
                {leftPercent.toFixed(1)}%
              </text>
              <text x="100" y="125" textAnchor="middle" fill="#9E9E9E" fontSize="14" fontFamily="Inter" fontWeight="600">COVERED</text>
            </svg>
            <div style={{
              marginTop: 12,
              fontFamily: 'Noto Sans Devanagari', fontSize: 28,
              color: '#F5F5F5', fontWeight: 700,
            }}>
              "अच्छा सुनने में"
            </div>
          </div>

          {/* VS Divider */}
          <div style={{
            textAlign: 'center',
            transform: `scale(${sp(120)})`,
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 90,
              color: '#FFC107', fontWeight: 900,
              letterSpacing: '4px',
              textShadow: '0 0 20px rgba(255,193,7,0.5)',
            }}>
              VS
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 26,
              color: '#F5F5F5', marginTop: 6, fontWeight: 700,
            }}>
              असलियत
            </div>
          </div>

          {/* RIGHT: 41.5% deposit value - RED REALITY */}
          <div style={{
            textAlign: 'center',
            transform: `scale(${sp(140)})`,
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 28,
              color: '#FFC107', fontWeight: 700,
              letterSpacing: '3px', marginBottom: 16,
            }}>
              जमा रकम का मूल्य
            </div>
            <svg width="320" height="320" viewBox="0 0 200 200">
              <circle cx="100" cy="100" r="90" fill="none" stroke="#080616" strokeWidth="20" />
              <circle cx="100" cy="100" r="90" fill="none" stroke="#F5F5F5" strokeWidth="20"
                strokeDasharray={`${rightDash} 565`}
                transform="rotate(-90, 100, 100)" strokeLinecap="round" />
              <text x="100" y="95" textAnchor="middle" fill="#FFC107" fontSize="44" fontFamily="JetBrains Mono" fontWeight="900">
                {rightPercent.toFixed(1)}%
              </text>
              <text x="100" y="125" textAnchor="middle" fill="#9E9E9E" fontSize="14" fontFamily="Inter" fontWeight="600">VALUE INSURED</text>
            </svg>
            <div style={{
              marginTop: 12,
              fontFamily: 'Noto Sans Devanagari', fontSize: 28,
              color: '#FFC107', fontWeight: 700,
            }}>
              "ज़मीनी सच्चाई"
            </div>
          </div>
        </div>

        {/* Bottom comparison bar */}
        <div style={{
          position: 'absolute', bottom: 90, left: 100, right: 100,
          opacity: interpolate(frame, [280, 310], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            background: '#080616',
            padding: '20px 36px',
            border: '3px solid #FFC107',
            display: 'flex', justifyContent: 'space-between', alignItems: 'center',
          }}>
            <div>
              <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E', fontWeight: 600 }}>
                खाते covered
              </div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 44, color: '#F5F5F5', fontWeight: 900 }}>
                97.6%
              </div>
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 32, color: '#FFC107',
              fontWeight: 700,
            }}>
              यानि बड़ी रकम वाले · असुरक्षित
            </div>
            <div>
              <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E', fontWeight: 600, textAlign: 'right' }}>
                रकम covered
              </div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 44, color: '#FFC107', fontWeight: 900 }}>
                41.5%
              </div>
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 45%, rgba(0,0,0,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene9_DICGCRealityCheck;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_TwoRupeesShock',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_TwoRupeesShock: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter animations
  const totalDep = interpolate(sp(30, { damping: 20, stiffness: 60 }), [0, 1], [0, 241]);
  const uninsuredDep = interpolate(sp(90, { damping: 20, stiffness: 60 }), [0, 1], [0, 140]);
  const twoRupees = interpolate(sp(220, { damping: 14, stiffness: 80 }), [0, 1], [0, 2.30]);

  // Shake on shock
  const shockShake = frame > 240 && frame < 290 ? Math.sin(frame * 1.5) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FF0000', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 60%, rgba(8,6,22,0.85) 0%, rgba(255,0,0,1) 75%)',
      }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map(i => {
        const seed = i + 59;
        const x = seededRandom(seed) * 1700;
        const y = seededRandom(seed + 4) * 900;
        return (
          <div key={`sh-${i}`} style={{
            position: 'absolute', left: x, top: y,
            width: 340, height: 340,
            border: '3px solid #F5F5F5', opacity: 0.04,
            transform: `rotate(${frame * 0.06 + i * 45}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.06})`,
          }} />
        );
      })}

      {/* Motion texture - currency lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 16 }).map((_, i) => (
          <line key={i} x1="0" y1={i * 70 + 30} x2="1920" y2={i * 70 + 30}
            stroke="#F5F5F5" strokeWidth="1" strokeDasharray="8,8" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const px = seededRandom(i + 210) * 1920;
        const py = (seededRandom(i + 220) * 1080 + frame * 0.5) % 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute', left: px, top: py,
            width: 3, height: 3, borderRadius: '50%',
            background: '#FFC107', opacity: 0.6,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translate(${shockShake}px, ${shockShake * 0.3}px)` }}>
        {/* Header */}
        <div style={{
          position: 'absolute', top: 40, left: 0, right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [5, 25], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 28,
            color: '#FFC107', letterSpacing: '6px', fontWeight: 700,
          }}>
            INDIA · DEPOSIT MATHEMATICS
          </div>
        </div>

        {/* Top split: Total vs Uninsured */}
        <div style={{
          position: 'absolute', top: 110, left: 80, right: 80,
          display: 'flex', justifyContent: 'space-between', gap: 30,
        }}>
          <div style={{
            flex: 1,
            background: '#080616',
            border: '3px solid #FFC107',
            padding: '20px 28px',
            transform: `scale(${sp(20)})`,
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E', letterSpacing: '3px', fontWeight: 600 }}>
              कुल जमा
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 100,
              color: '#FFC107', fontWeight: 900, lineHeight: 1, marginTop: 4,
            }}>
              ₹{totalDep.toFixed(0)}L Cr
            </div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 24, color: '#F5F5F5', marginTop: 6, fontWeight: 700 }}>
              241 लाख करोड़
            </div>
          </div>

          <div style={{
            flex: 1,
            background: '#080616',
            border: '3px solid #F5F5F5',
            padding: '20px 28px',
            transform: `scale(${sp(80)})`,
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E', letterSpacing: '3px', fontWeight: 600 }}>
              बिना बीमा
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 100,
              color: '#F5F5F5', fontWeight: 900, lineHeight: 1, marginTop: 4,
            }}>
              ₹{uninsuredDep.toFixed(0)}L Cr
            </div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 24, color: '#FFC107', marginTop: 6, fontWeight: 700 }}>
              UNINSURED — खतरे में
            </div>
          </div>
        </div>

        {/* THE BIG SHOCK STATEMENT */}
        <div style={{
          position: 'absolute', top: 410, left: 0, right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [180, 210], [0, 1], { extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 42,
            color: '#F5F5F5', fontWeight: 700,
          }}>
            हर <span style={{ color: '#FFC107' }}>₹100</span> बीमित डिपॉजिट के पीछे
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 38,
            color: '#F5F5F5', marginTop: 4, fontWeight: 700,
          }}>
            DICGC के पास असली पैसा सिर्फ —
          </div>
        </div>

        {/* The 2.30 Rupees - HERO */}
        <div style={{
          position: 'absolute', top: 540, left: 0, right: 0,
          textAlign: 'center',
        }}>
          <div style={{
            display: 'inline-block',
            background: '#080616',
            border: '8px solid #FFC107',
            padding: '30px 80px',
            transform: `scale(${sp(220, { damping: 8, stiffness: 200 })})`,
            boxShadow: '0 0 60px rgba(255,193,7,0.5)',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 240,
              color: '#FFC107', fontWeight: 900, lineHeight: 1,
              letterSpacing: '4px',
            }}>
              ₹{twoRupees.toFixed(2)}
            </div>
            <div style={{
              fontFamily: 'Inter', fontSize: 32,
              color: '#F5F5F5', marginTop: 10, fontWeight: 700,
              letterSpacing: '4px',
            }}>
              ONLY · सिर्फ इतना ही
            </div>
          </div>
        </div>

        {/* Final stamp */}
        <div style={{
          position: 'absolute', bottom: 60, left: 0, right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [290, 320], [0, 1], { extrapolateRight: 'clamp' }),
          transform: `scale(${sp(290, { damping: 8 })})`,
        }}>
          <div style={{
            display: 'inline-block',
            background: '#FFC107',
            color: '#080616',
            padding: '14px 40px',
            fontFamily: 'Inter', fontSize: 44,
            fontWeight: 900,
            letterSpacing: '4px',
          }}>
            ये एक बड़ा भ्रम है
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 40%, rgba(0,0,0,0.65) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_TwoRupeesShock;
```

