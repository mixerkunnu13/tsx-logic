```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01SplitParadox',
  durationInSeconds: 26.05,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01SplitParadox: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade in the last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Slow camera zoom for the entire scene
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.07]);

  // Screen shake on the "SAME PRESS RELEASE" impact beat
  const shake = frame > 330 && frame < 344 ? Math.sin(frame * 2.4) * 7 : 0;

  const barHeights = [90, 150, 210, 300, 400];

  return (
    <AbsoluteFill style={{ backgroundColor: '#08CB00', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)` }}>

        {/* Layer 1: Dynamic gradient background */}
        <AbsoluteFill style={{ background: 'linear-gradient(135deg, #08CB00 0%, #07B500 45%, #056B00 100%)' }} />

        {/* Layer 2: Animated transparent shape layers */}
        <div style={{
          position: 'absolute', width: 900, height: 900, borderRadius: '50%',
          border: '3px solid rgba(0,0,0,0.06)', left: -250, top: -300,
          transform: `rotate(${frame * 0.08}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
        }} />
        <div style={{
          position: 'absolute', width: 520, height: 520,
          background: 'rgba(238,238,238,0.05)', right: -140, bottom: -180,
          transform: `rotate(${45 + frame * 0.05}deg)`,
        }} />

        {/* Layer 3: Motion texture — vertical ticker lines */}
        {Array.from({ length: 26 }).map((_, i) => (
          <div key={`ln-${i}`} style={{
            position: 'absolute', left: i * 76, top: 0, width: 1, height: 1080,
            background: 'rgba(0,0,0,0.045)',
          }} />
        ))}

        {/* Layer 4: Floating particles */}
        {Array.from({ length: 14 }).map((_, i) => {
          const s = seededRandom(i + 7);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: s * 1920,
              top: ((seededRandom(i + 40) * 1080) + frame * (0.3 + s * 0.5)) % 1080,
              width: 5 + s * 6, height: 5 + s * 6, borderRadius: '50%',
              background: i % 2 === 0 ? 'rgba(0,0,0,0.18)' : 'rgba(238,238,238,0.35)',
            }} />
          );
        })}

        {/* Layer 5: Main content — split screen paradox */}
        {/* LEFT: RECORD PROFITS — black panel, rising bars */}
        <div style={{
          position: 'absolute', left: 90, top: 150, width: 800, height: 640,
          background: '#000000', borderRadius: 28, padding: 50,
          transform: `translateY(${(1 - sp(15)) * 400}px)`, opacity: Math.min(1, sp(15) * 1.4),
        }}>
          <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 84, color: '#08CB00', lineHeight: 1 }}>
            RECORD<br />PROFITS
          </div>
          <div style={{ display: 'flex', alignItems: 'flex-end', gap: 26, height: 330, marginTop: 60 }}>
            {barHeights.map((h, i) => (
              <div key={`bar-${i}`} style={{
                width: 110, borderRadius: '10px 10px 0 0', background: i === 4 ? '#EEEEEE' : '#08CB00',
                height: h * interpolate(frame, [60 + i * 15, 110 + i * 15], [0, 1], {
                  extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
                }),
              }} />
            ))}
          </div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 30, color: '#EEEEEE', marginTop: 24, opacity: sp(150) }}>
            BEST QUARTER IN HISTORY
          </div>
        </div>

        {/* RIGHT: PINK SLIPS — light panel, falling slips */}
        <div style={{
          position: 'absolute', right: 90, top: 150, width: 800, height: 640,
          background: '#EEEEEE', borderRadius: 28, padding: 50, overflow: 'hidden',
          transform: `translateY(${(1 - sp(45)) * 400}px)`, opacity: Math.min(1, sp(45) * 1.4),
        }}>
          <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 84, color: '#000000', lineHeight: 1 }}>
            PINK<br />SLIPS
          </div>
          {Array.from({ length: 9 }).map((_, i) => {
            const s = seededRandom(i + 90);
            const fall = ((frame * (2.2 + s * 2) + s * 600) % 760) - 120;
            return (
              <div key={`slip-${i}`} style={{
                position: 'absolute', left: 60 + s * 620, top: fall,
                width: 120, height: 74, background: '#000000', borderRadius: 6,
                transform: `rotate(${Math.sin(frame * 0.06 + i) * 22}deg)`, opacity: 0.85,
              }}>
                <div style={{ width: 70, height: 6, background: '#08CB00', margin: '16px 0 0 16px' }} />
                <div style={{ width: 50, height: 6, background: '#EEEEEE', margin: '8px 0 0 16px' }} />
              </div>
            );
          })}
        </div>

        {/* Impact headline — SAME PRESS RELEASE */}
        <div style={{
          position: 'absolute', left: 0, right: 0, top: 850, textAlign: 'center',
          transform: `scale(${0.6 + sp(330, { stiffness: 200 }) * 0.4})`, opacity: Math.min(1, sp(330) * 1.5),
        }}>
          <span style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 92, color: '#000000',
            background: '#EEEEEE', padding: '10px 48px', borderRadius: 16, letterSpacing: 2,
          }}>
            SAME PRESS RELEASE.
          </span>
        </div>

        {/* Beat 3: badge-in denial line */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 60, textAlign: 'center', opacity: sp(560) }}>
          <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 38, color: '#000000', letterSpacing: 4 }}>
            ACCESS REVOKED — DO NOT BADGE IN TOMORROW
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene01SplitParadox;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02NotARecession',
  durationInSeconds: 26.05,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02NotARecession: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade in the last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Slow horizontal camera pan
  const camX = interpolate(frame, [0, durationInFrames], [0, -40]);

  // Strike-through wipe over "RECESSION"
  const strikeW = interpolate(frame, [95, 130], [0, 100], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#08CB00', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ transform: `translateX(${camX}px) scale(1.05)` }}>

        {/* Layer 1: Dynamic gradient background */}
        <AbsoluteFill style={{ background: 'linear-gradient(180deg, #0AE600 0%, #08CB00 40%, #067A00 100%)' }} />

        {/* Layer 2: Animated transparent shape layers */}
        <div style={{
          position: 'absolute', width: 1100, height: 1100, borderRadius: '50%',
          background: 'rgba(0,0,0,0.045)', left: 1250, top: -420,
          transform: `scale(${1 + Math.sin(frame * 0.018) * 0.05})`,
        }} />
        <div style={{
          position: 'absolute', width: 0, height: 0, left: -80, bottom: -60,
          borderLeft: '380px solid transparent', borderRight: '380px solid transparent',
          borderBottom: '600px solid rgba(238,238,238,0.05)',
          transform: `rotate(${frame * 0.04}deg)`,
        }} />

        {/* Layer 3: Motion texture — horizontal flow lines */}
        {Array.from({ length: 12 }).map((_, i) => (
          <div key={`hl-${i}`} style={{
            position: 'absolute', top: 60 + i * 90, height: 1, width: 1920,
            background: 'rgba(0,0,0,0.05)', left: 0,
          }} />
        ))}

        {/* Layer 4: Floating particles */}
        {Array.from({ length: 12 }).map((_, i) => {
          const s = seededRandom(i + 21);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute',
              left: ((s * 1920) + frame * (0.8 + s)) % 1920,
              top: seededRandom(i + 55) * 1080,
              width: 4 + s * 7, height: 4 + s * 7, borderRadius: 2,
              background: 'rgba(0,0,0,0.15)', transform: `rotate(${frame + i * 30}deg)`,
            }} />
          );
        })}

        {/* Layer 5: Main content */}
        {/* Beat 1: THIS IS NOT A RECESSION with strike-through */}
        <div style={{ position: 'absolute', left: 120, top: 130, opacity: Math.min(1, sp(10) * 1.4), transform: `translateY(${(1 - sp(10)) * -80}px)` }}>
          <div style={{ fontFamily: 'Inter, sans-serif', fontWeight: 800, fontSize: 50, color: '#000000', letterSpacing: 6 }}>
            THIS IS NOT A
          </div>
          <div style={{ position: 'relative', display: 'inline-block' }}>
            <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 190, color: '#EEEEEE', lineHeight: 1 }}>
              RECESSION
            </div>
            <div style={{
              position: 'absolute', top: '52%', left: 0, height: 16, borderRadius: 8,
              width: `${strikeW}%`, background: '#000000',
            }} />
          </div>
        </div>

        {/* Beat 2: MONEY IN / JOBS OUT opposing flow diagram */}
        <div style={{ position: 'absolute', left: 120, top: 480, width: 1680, opacity: Math.min(1, sp(180) * 1.4) }}>
          {/* Money flowing IN (left to right) */}
          <div style={{ display: 'flex', alignItems: 'center', gap: 30 }}>
            <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 76, color: '#000000' }}>MONEY IN</div>
            <div style={{ position: 'relative', flex: 1, height: 60, overflow: 'hidden' }}>
              {Array.from({ length: 7 }).map((_, i) => (
                <div key={`min-${i}`} style={{
                  position: 'absolute', top: 18,
                  left: ((i * 190 + frame * 6) % 1200),
                  width: 90, height: 22, background: '#000000', borderRadius: 11,
                }} />
              ))}
            </div>
            <div style={{ width: 0, height: 0, borderTop: '34px solid transparent', borderBottom: '34px solid transparent', borderLeft: '52px solid #000000' }} />
          </div>
          {/* Jobs flowing OUT (right to left) */}
          <div style={{ display: 'flex', alignItems: 'center', gap: 30, marginTop: 55, opacity: Math.min(1, sp(240) * 1.4) }}>
            <div style={{ width: 0, height: 0, borderTop: '34px solid transparent', borderBottom: '34px solid transparent', borderRight: '52px solid #EEEEEE' }} />
            <div style={{ position: 'relative', flex: 1, height: 60, overflow: 'hidden' }}>
              {Array.from({ length: 7 }).map((_, i) => (
                <div key={`jout-${i}`} style={{
                  position: 'absolute', top: 18,
                  left: 1200 - ((i * 190 + frame * 6) % 1200),
                  width: 90, height: 22, background: '#EEEEEE', borderRadius: 11,
                }} />
              ))}
            </div>
            <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 76, color: '#EEEEEE' }}>JOBS OUT</div>
          </div>
        </div>

        {/* Beat 3: Research credibility card */}
        <div style={{
          position: 'absolute', left: 120, bottom: 110, width: 1680, background: '#000000',
          borderRadius: 22, padding: '36px 50px', display: 'flex', justifyContent: 'space-between', alignItems: 'center',
          transform: `translateY(${(1 - sp(430)) * 250}px)`, opacity: Math.min(1, sp(430) * 1.4),
        }}>
          <div style={{ fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 46, color: '#08CB00' }}>
            MONTHS OF DIGGING
          </div>
          {['LAYOFF FILINGS', 'EARNINGS CALLS', 'GOVERNMENT DATA'].map((t, i) => (
            <div key={t} style={{
              fontFamily: 'JetBrains Mono, monospace', fontSize: 28, color: '#EEEEEE',
              border: '2px solid #08CB00', borderRadius: 12, padding: '12px 26px',
              opacity: sp(470 + i * 20), transform: `scale(${0.7 + sp(470 + i * 20) * 0.3})`,
            }}>
              {t}
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene02NotARecession;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03TruthSpectrum',
  durationInSeconds: 26.05,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03TruthSpectrum: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade in the last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: gentle zoom out then in
  const camScale = interpolate(frame, [0, 300, durationInFrames], [1.1, 1.02, 1.08]);

  // Needle swings hard left, hard right, settles middle
  const needleX = interpolate(frame, [40, 110, 190, 280], [8, 92, 20, 50], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });

  // Big counter: 0 -> 101,743
  const cuts = Math.floor(interpolate(frame, [430, 640], [0, 101743], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  }));

  // Screen shake when counter lands
  const shake = frame > 640 && frame < 654 ? Math.sin(frame * 2.6) * 8 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#08CB00', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${shake}px)` }}>

        {/* Layer 1: Dynamic gradient background */}
        <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 30%, #0AE600 0%, #08CB00 45%, #056B00 100%)' }} />

        {/* Layer 2: Animated transparent shape layers */}
        {[0, 1, 2].map((i) => (
          <div key={`ring-${i}`} style={{
            position: 'absolute', left: 960, top: 420,
            width: 500 + i * 320, height: 500 + i * 320, borderRadius: '50%',
            border: '2px solid rgba(0,0,0,0.07)',
            transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.02 + i) * 0.03})`,
          }} />
        ))}

        {/* Layer 3: Motion texture — diagonal hatch */}
        <AbsoluteFill style={{
          backgroundImage: 'repeating-linear-gradient(45deg, rgba(0,0,0,0.035) 0px, rgba(0,0,0,0.035) 2px, transparent 2px, transparent 46px)',
        }} />

        {/* Layer 4: Floating particles */}
        {Array.from({ length: 13 }).map((_, i) => {
          const s = seededRandom(i + 33);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute', left: s * 1920,
              top: (seededRandom(i + 70) * 1080 - frame * (0.4 + s * 0.6) + 1080) % 1080,
              width: 6, height: 6, borderRadius: '50%', background: 'rgba(238,238,238,0.4)',
            }} />
          );
        })}

        {/* Layer 5: Main content — spectrum meter */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 90, textAlign: 'center', opacity: Math.min(1, sp(5) * 1.4) }}>
          <span style={{ fontFamily: 'Inter, sans-serif', fontWeight: 800, fontSize: 44, color: '#000000', letterSpacing: 8 }}>
            THE HEADLINES ARE LYING IN BOTH DIRECTIONS
          </span>
        </div>

        {/* Spectrum bar with two poles */}
        <div style={{ position: 'absolute', left: 160, top: 260, width: 1600, opacity: Math.min(1, sp(30) * 1.4) }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', marginBottom: 26 }}>
            <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 82, color: '#000000' }}>APOCALYPSE</div>
            <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 82, color: '#EEEEEE' }}>ALL HYPE</div>
          </div>
          <div style={{ position: 'relative', height: 44, borderRadius: 22, background: 'linear-gradient(90deg, #000000 0%, #067A00 50%, #EEEEEE 100%)' }}>
            {/* Needle */}
            <div style={{
              position: 'absolute', top: -34, left: `${needleX}%`,
              transform: 'translateX(-50%)',
            }}>
              <div style={{ width: 0, height: 0, borderLeft: '26px solid transparent', borderRight: '26px solid transparent', borderTop: '44px solid #EEEEEE', filter: 'drop-shadow(0 4px 6px rgba(0,0,0,0.4))' }} />
            </div>
          </div>
          {/* Truth marker at middle */}
          <div style={{ textAlign: 'center', marginTop: 40, opacity: Math.min(1, sp(300) * 1.5), transform: `scale(${0.7 + sp(300) * 0.3})` }}>
            <span style={{
              fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 58, color: '#08CB00',
              background: '#000000', padding: '14px 44px', borderRadius: 16,
            }}>
              THE TRUTH — WHERE YOUR PAYCHECK LIVES
            </span>
          </div>
        </div>

        {/* Beat 3: the giant number */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 640, textAlign: 'center', opacity: Math.min(1, sp(420) * 1.4) }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 36, color: '#000000', letterSpacing: 6 }}>
            AI — THE #1 STATED REASON FOR JOB CUTS, H1
          </div>
          <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 218, color: '#EEEEEE', lineHeight: 1.05, textShadow: '0 10px 0 rgba(0,0,0,0.35)' }}>
            {cuts.toLocaleString('en-US')}
          </div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontWeight: 800, fontSize: 40, color: '#000000', letterSpacing: 4, opacity: sp(660) }}>
            JOBS CUT IN THE FIRST HALF
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene03TruthSpectrum;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04RisingShare',
  durationInSeconds: 26.05,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04RisingShare: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade in the last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt-pan
  const camY = interpolate(frame, [0, durationInFrames], [0, -35]);

  // Donut 1: 23% | Donut 2: 31%
  const pct1 = interpolate(frame, [50, 160], [0, 23], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const pct2 = interpolate(frame, [200, 320], [0, 31], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  const C = 2 * Math.PI * 150;

  const months = ['MONTH 1', 'MONTH 2', 'MONTH 3', 'MONTH 4'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#08CB00', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ transform: `translateY(${camY}px) scale(1.06)` }}>

        {/* Layer 1: Dynamic gradient background */}
        <AbsoluteFill style={{ background: 'linear-gradient(120deg, #067A00 0%, #08CB00 55%, #0AE600 100%)' }} />

        {/* Layer 2: Animated transparent shape layers */}
        <div style={{
          position: 'absolute', width: 700, height: 700, borderRadius: 60,
          background: 'rgba(0,0,0,0.05)', left: -200, top: 520,
          transform: `rotate(${frame * 0.06}deg)`,
        }} />
        <div style={{
          position: 'absolute', width: 460, height: 460, borderRadius: '50%',
          border: '4px solid rgba(238,238,238,0.08)', right: -120, top: -140,
          transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
        }} />

        {/* Layer 3: Motion texture — dot grid */}
        {Array.from({ length: 8 }).map((_, r) =>
          Array.from({ length: 14 }).map((_, c) => (
            <div key={`d-${r}-${c}`} style={{
              position: 'absolute', left: 60 + c * 140, top: 60 + r * 140,
              width: 5, height: 5, borderRadius: '50%', background: 'rgba(0,0,0,0.08)',
            }} />
          ))
        )}

        {/* Layer 4: Floating particles */}
        {Array.from({ length: 12 }).map((_, i) => {
          const s = seededRandom(i + 44);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute', left: ((s * 1920) - frame * (0.5 + s * 0.7) + 1920) % 1920,
              top: seededRandom(i + 88) * 1080, width: 5 + s * 5, height: 5 + s * 5,
              background: 'rgba(238,238,238,0.3)', borderRadius: '50%',
            }} />
          );
        })}

        {/* Layer 5: Main content — dual donuts + streak */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 70, textAlign: 'center', opacity: Math.min(1, sp(5) * 1.4) }}>
          <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 88, color: '#000000' }}>
            AI'S SHARE OF ALL JOB CUTS
          </span>
        </div>

        {/* Donut 23% — First Half */}
        <div style={{ position: 'absolute', left: 300, top: 250, textAlign: 'center', transform: `scale(${sp(40)})` }}>
          <svg width={380} height={380}>
            <circle cx={190} cy={190} r={150} stroke="rgba(0,0,0,0.18)" strokeWidth={42} fill="none" />
            <circle cx={190} cy={190} r={150} stroke="#000000" strokeWidth={42} fill="none"
              strokeDasharray={`${(pct1 / 100) * C} ${C}`} strokeLinecap="round"
              transform="rotate(-90 190 190)" />
            <text x={190} y={210} textAnchor="middle" fontFamily="Mont Heavy, Inter, sans-serif" fontSize={92} fill="#EEEEEE">
              {Math.round(pct1)}%
            </text>
          </svg>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 30, color: '#000000', letterSpacing: 3 }}>FIRST HALF</div>
        </div>

        {/* Arrow between donuts */}
        <div style={{ position: 'absolute', left: 850, top: 400, opacity: sp(190), transform: `translateX(${(1 - sp(190)) * -60}px)` }}>
          <div style={{ width: 160, height: 20, background: '#000000', display: 'inline-block', verticalAlign: 'middle' }} />
          <div style={{ width: 0, height: 0, display: 'inline-block', verticalAlign: 'middle', borderTop: '30px solid transparent', borderBottom: '30px solid transparent', borderLeft: '44px solid #000000' }} />
        </div>

        {/* Donut 31% — June */}
        <div style={{ position: 'absolute', right: 300, top: 250, textAlign: 'center', transform: `scale(${sp(200)})` }}>
          <svg width={380} height={380}>
            <circle cx={190} cy={190} r={150} stroke="rgba(238,238,238,0.25)" strokeWidth={42} fill="none" />
            <circle cx={190} cy={190} r={150} stroke="#EEEEEE" strokeWidth={42} fill="none"
              strokeDasharray={`${(pct2 / 100) * C} ${C}`} strokeLinecap="round"
              transform="rotate(-90 190 190)" />
            <text x={190} y={210} textAnchor="middle" fontFamily="Mont Heavy, Inter, sans-serif" fontSize={92} fill="#000000">
              {Math.round(pct2)}%
            </text>
          </svg>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 30, color: '#EEEEEE', letterSpacing: 3 }}>IN JUNE</div>
        </div>

        {/* Beat 3: four-month streak blocks */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 760, display: 'flex', justifyContent: 'center', gap: 34 }}>
          {months.map((m, i) => (
            <div key={m} style={{
              width: 300, background: '#000000', borderRadius: 20, padding: '26px 0',
              textAlign: 'center', transform: `translateY(${(1 - sp(430 + i * 22)) * 200}px)`,
              opacity: Math.min(1, sp(430 + i * 22) * 1.4),
            }}>
              <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 68, color: '#08CB00', lineHeight: 1 }}>#1</div>
              <div style={{ fontFamily: 'Inter, sans-serif', fontWeight: 700, fontSize: 26, color: '#EEEEEE', marginTop: 8, letterSpacing: 3 }}>{m}</div>
            </div>
          ))}
        </div>
        <div style={{ position: 'absolute', left: 0, right: 0, top: 960, textAlign: 'center', opacity: sp(560) }}>
          <span style={{ fontFamily: 'Inter, sans-serif', fontWeight: 800, fontSize: 40, color: '#000000', letterSpacing: 4 }}>
            FOUR CONSECUTIVE MONTHS AS THE #1 REASON
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene04RisingShare;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05NoPrecedent',
  durationInSeconds: 26.05,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05NoPrecedent: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade in the last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow orbit feel via combined pan + zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.1]);
  const camX = interpolate(frame, [0, durationInFrames], [20, -20]);

  // Timeline draw
  const lineW = interpolate(frame, [60, 200], [0, 1400], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  // STAMP slam
  const stampS = sp(480, { stiffness: 260, damping: 14 });
  const shake = frame > 486 && frame < 500 ? Math.sin(frame * 2.8) * 9 : 0;

  const years = ['2023', '2024', '2025', '2026'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#08CB00', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${camX + shake}px, 0px)` }}>

        {/* Layer 1: Dynamic gradient background */}
        <AbsoluteFill style={{ background: 'linear-gradient(200deg, #08CB00 0%, #07AE00 50%, #045800 100%)' }} />

        {/* Layer 2: Animated transparent shape layers */}
        <div style={{
          position: 'absolute', width: 800, height: 800, borderRadius: '50%',
          border: '2px solid rgba(238,238,238,0.08)', left: 1350, top: 500,
          transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
        }} />
        <div style={{
          position: 'absolute', width: 540, height: 540, background: 'rgba(0,0,0,0.05)',
          left: -180, top: -180, borderRadius: 48, transform: `rotate(${frame * -0.04}deg)`,
        }} />

        {/* Layer 3: Motion texture — graph paper grid */}
        <AbsoluteFill style={{
          backgroundImage:
            'linear-gradient(rgba(0,0,0,0.05) 1px, transparent 1px), linear-gradient(90deg, rgba(0,0,0,0.05) 1px, transparent 1px)',
          backgroundSize: '96px 96px',
        }} />

        {/* Layer 4: Floating particles */}
        {Array.from({ length: 12 }).map((_, i) => {
          const s = seededRandom(i + 61);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute', left: s * 1920,
              top: ((seededRandom(i + 15) * 1080) + frame * (0.35 + s * 0.4)) % 1080,
              width: 4 + s * 6, height: 4 + s * 6, background: 'rgba(0,0,0,0.16)',
              borderRadius: i % 2 === 0 ? '50%' : 2, transform: `rotate(${frame * 1.5}deg)`,
            }} />
          );
        })}

        {/* Layer 5: Main content — tracking timeline */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 100, textAlign: 'center', opacity: Math.min(1, sp(8) * 1.4) }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 34, color: '#000000', letterSpacing: 6 }}>
            CHALLENGER OUTPLACEMENT DATA
          </div>
          <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 96, color: '#EEEEEE', marginTop: 8 }}>
            TRACKING AI CUTS SINCE 2023
          </div>
        </div>

        {/* Timeline */}
        <div style={{ position: 'absolute', left: 260, top: 480, width: 1400 }}>
          <div style={{ height: 14, width: lineW, background: '#000000', borderRadius: 7 }} />
          {years.map((y, i) => (
            <div key={y} style={{
              position: 'absolute', left: i * 440 - 10, top: -30, textAlign: 'center',
              opacity: sp(120 + i * 40), transform: `translateY(${(1 - sp(120 + i * 40)) * 60}px)`,
            }}>
              <div style={{
                width: 34, height: 34, borderRadius: '50%', margin: '0 auto',
                background: i === 3 ? '#EEEEEE' : '#000000', border: '6px solid #08CB00',
                boxShadow: '0 0 0 3px rgba(0,0,0,0.3)',
              }} />
              <div style={{ fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 46, color: i === 3 ? '#EEEEEE' : '#000000', marginTop: 16 }}>
                {y}
              </div>
              {i === 3 && (
                <div style={{
                  fontFamily: 'Inter, sans-serif', fontWeight: 800, fontSize: 26, color: '#000000',
                  background: '#EEEEEE', borderRadius: 10, padding: '6px 16px', marginTop: 10,
                  opacity: sp(300),
                }}>
                  4 STRAIGHT MONTHS AT #1
                </div>
              )}
            </div>
          ))}
        </div>

        {/* Flat history bars vs spike — structural shift mini chart */}
        <div style={{ position: 'absolute', left: 260, top: 660, display: 'flex', alignItems: 'flex-end', gap: 20, height: 220 }}>
          {[24, 30, 26, 34, 30, 38, 36, 44, 200].map((h, i) => (
            <div key={`sb-${i}`} style={{
              width: 64, borderRadius: '8px 8px 0 0',
              background: i === 8 ? '#EEEEEE' : 'rgba(0,0,0,0.55)',
              height: h * interpolate(frame, [320 + i * 12, 360 + i * 12], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }),
            }} />
          ))}
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 28, color: '#000000', alignSelf: 'flex-end', marginLeft: 16, opacity: sp(440) }}>
            STRUCTURAL SHIFT — NEVER RECORDED BEFORE
          </div>
        </div>

        {/* Stamp: NO PRECEDENT */}
        <div style={{
          position: 'absolute', right: 200, top: 700,
          transform: `rotate(-9deg) scale(${0.5 + stampS * 0.5})`, opacity: Math.min(1, stampS * 1.4),
        }}>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 96, color: '#EEEEEE',
            border: '10px solid #EEEEEE', borderRadius: 20, padding: '10px 40px',
            background: 'rgba(0,0,0,0.85)', letterSpacing: 4,
          }}>
            NO PRECEDENT
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene05NoPrecedent;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06NewNormal',
  durationInSeconds: 26.05,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06NewNormal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade in the last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: push-in
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.09]);

  // Install progress bar 0 -> 100
  const install = interpolate(frame, [260, 500], [0, 100], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#08CB00', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Layer 1: Dynamic gradient background */}
        <AbsoluteFill style={{ background: 'linear-gradient(160deg, #056B00 0%, #08CB00 60%, #0AE600 100%)' }} />

        {/* Layer 2: Animated transparent shape layers */}
        <div style={{
          position: 'absolute', width: 1000, height: 1000, borderRadius: '50%',
          background: 'rgba(238,238,238,0.045)', left: -300, bottom: -500,
          transform: `scale(${1 + Math.sin(frame * 0.015) * 0.06})`,
        }} />
        <div style={{
          position: 'absolute', width: 380, height: 380, border: '3px solid rgba(0,0,0,0.08)',
          right: 120, top: -100, borderRadius: 40, transform: `rotate(${frame * 0.12}deg)`,
        }} />

        {/* Layer 3: Motion texture — concentric circles */}
        {[0, 1, 2, 3].map((i) => (
          <div key={`cc-${i}`} style={{
            position: 'absolute', left: '50%', top: '50%',
            width: 300 + i * 340, height: 300 + i * 340, borderRadius: '50%',
            border: '1px solid rgba(0,0,0,0.06)', transform: 'translate(-50%, -50%)',
          }} />
        ))}

        {/* Layer 4: Floating particles */}
        {Array.from({ length: 12 }).map((_, i) => {
          const s = seededRandom(i + 77);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute', left: ((s * 1920) + frame * (0.4 + s * 0.5)) % 1920,
              top: seededRandom(i + 29) * 1080, width: 6, height: 6,
              background: 'rgba(0,0,0,0.15)', borderRadius: '50%',
            }} />
          );
        })}

        {/* Layer 5: Main content — kinetic typography stack */}
        <div style={{ position: 'absolute', left: 140, top: 130 }}>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 120, color: '#000000', lineHeight: 1.02,
            opacity: Math.min(1, sp(15) * 1.4), transform: `translateX(${(1 - sp(15)) * -200}px)`,
            textDecoration: frame > 90 ? 'line-through' : 'none', textDecorationThickness: 12,
          }}>
            NOT A BLIP.
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 120, color: '#000000', lineHeight: 1.02,
            opacity: Math.min(1, sp(90) * 1.4), transform: `translateX(${(1 - sp(90)) * -200}px)`,
            textDecoration: frame > 170 ? 'line-through' : 'none', textDecorationThickness: 12,
          }}>
            NOT A BAD QUARTER.
          </div>
          <div style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 172, color: '#EEEEEE', lineHeight: 1.05, marginTop: 20,
            opacity: Math.min(1, sp(180) * 1.4), transform: `scale(${0.8 + sp(180) * 0.2})`, transformOrigin: 'left center',
            textShadow: '0 8px 0 rgba(0,0,0,0.35)',
          }}>
            A NEW NORMAL.
          </div>
        </div>

        {/* Installer window metaphor */}
        <div style={{
          position: 'absolute', left: 140, top: 700, width: 1180, background: '#000000',
          borderRadius: 20, padding: '30px 44px',
          opacity: Math.min(1, sp(250) * 1.4), transform: `translateY(${(1 - sp(250)) * 160}px)`,
        }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 30, color: '#EEEEEE', marginBottom: 18 }}>
            installing_new_normal.exe — {Math.round(install)}%
          </div>
          <div style={{ height: 30, borderRadius: 15, background: 'rgba(238,238,238,0.15)' }}>
            <div style={{ height: 30, borderRadius: 15, width: `${install}%`, background: '#08CB00' }} />
          </div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 26, color: '#9CF096', marginTop: 16, opacity: sp(380) }}>
            Running quietly while the world debates poems.
          </div>
        </div>

        {/* Disclaimer card */}
        <div style={{
          position: 'absolute', right: 120, top: 700, width: 480, background: '#EEEEEE',
          borderRadius: 20, padding: '30px 36px', border: '6px solid #000000',
          opacity: Math.min(1, sp(520) * 1.4), transform: `rotate(${(1 - sp(520)) * 6 - 2}deg)`,
        }}>
          <div style={{ fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 40, color: '#000000' }}>
            STRAIGHT TALK
          </div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 27, color: '#000000', marginTop: 12, lineHeight: 1.4 }}>
            Not personalized financial advice. One guy's research — a starting point for your own thinking.
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene06NewNormal;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07SeesawParadox',
  durationInSeconds: 26.05,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07SeesawParadox: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade in the last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow pan up
  const camY = interpolate(frame, [0, durationInFrames], [30, -30]);

  // Seesaw tilts: profits side rises, people side sinks
  const tilt = interpolate(frame, [150, 300], [0, -11], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  }) + Math.sin(frame * 0.03) * 0.6;

  // Price tag punch
  const tagS = sp(520, { stiffness: 240 });
  const shake = frame > 526 && frame < 540 ? Math.sin(frame * 2.5) * 8 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#08CB00', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ transform: `translateY(${camY + shake}px) scale(1.05)` }}>

        {/* Layer 1: Dynamic gradient background */}
        <AbsoluteFill style={{ background: 'radial-gradient(circle at 30% 70%, #0AE600 0%, #08CB00 40%, #056B00 100%)' }} />

        {/* Layer 2: Animated transparent shape layers */}
        <div style={{
          position: 'absolute', width: 760, height: 760, borderRadius: '50%',
          border: '3px solid rgba(0,0,0,0.07)', right: -200, bottom: -260,
          transform: `rotate(${frame * 0.07}deg)`,
        }} />
        <div style={{
          position: 'absolute', width: 420, height: 420, background: 'rgba(238,238,238,0.05)',
          left: -120, top: -140, transform: `rotate(${frame * -0.05}deg)`, borderRadius: 36,
        }} />

        {/* Layer 3: Motion texture — angled speed lines */}
        {Array.from({ length: 14 }).map((_, i) => (
          <div key={`sl-${i}`} style={{
            position: 'absolute', left: -200 + i * 170, top: 0, width: 2, height: 1400,
            background: 'rgba(0,0,0,0.05)', transform: 'rotate(18deg)', transformOrigin: 'top left',
          }} />
        ))}

        {/* Layer 4: Floating particles */}
        {Array.from({ length: 12 }).map((_, i) => {
          const s = seededRandom(i + 99);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute', left: s * 1920,
              top: (seededRandom(i + 12) * 1080 - frame * (0.5 + s * 0.5) + 1080) % 1080,
              width: 5 + s * 6, height: 5 + s * 6, background: 'rgba(238,238,238,0.35)', borderRadius: '50%',
            }} />
          );
        })}

        {/* Layer 5: Main content */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 90, textAlign: 'center', opacity: Math.min(1, sp(10) * 1.4) }}>
          <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 100, color: '#000000' }}>
            THE PARADOX
          </span>
        </div>

        {/* Seesaw graphic */}
        <div style={{ position: 'absolute', left: 260, top: 480, width: 1400, opacity: Math.min(1, sp(70) * 1.4) }}>
          {/* Beam */}
          <div style={{
            width: 1400, height: 26, background: '#000000', borderRadius: 13,
            transform: `rotate(${tilt}deg)`, transformOrigin: 'center center', position: 'relative',
          }}>
            {/* PROFITS block (right, up) */}
            <div style={{
              position: 'absolute', right: 20, top: -170, width: 340, textAlign: 'center',
              background: '#EEEEEE', borderRadius: 18, padding: '22px 0', border: '5px solid #000000',
            }}>
              <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 54, color: '#000000' }}>PROFITS</div>
              <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 30, color: '#067A00' }}>RECORD HIGH</div>
            </div>
            {/* HEADCOUNT block (left, down) */}
            <div style={{
              position: 'absolute', left: 20, top: -170, width: 340, textAlign: 'center',
              background: '#000000', borderRadius: 18, padding: '22px 0',
            }}>
              <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 54, color: '#EEEEEE' }}>HEADCOUNT</div>
              <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 30, color: '#08CB00' }}>THOUSANDS CUT</div>
            </div>
          </div>
          {/* Fulcrum */}
          <div style={{
            width: 0, height: 0, margin: '0 auto',
            borderLeft: '90px solid transparent', borderRight: '90px solid transparent',
            borderBottom: '150px solid #000000',
          }} />
        </div>

        {/* Beat 2: not broke */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 820, textAlign: 'center', opacity: Math.min(1, sp(330) * 1.4) }}>
          <span style={{ fontFamily: 'Inter, sans-serif', fontWeight: 800, fontSize: 52, color: '#000000', letterSpacing: 2 }}>
            THEY ARE NOT CUTTING BECAUSE THEY ARE BROKE.
          </span>
        </div>

        {/* Beat 3: the price tag */}
        <div style={{
          position: 'absolute', left: 0, right: 0, top: 910, textAlign: 'center',
          transform: `scale(${0.6 + tagS * 0.4}) rotate(${(1 - tagS) * 8 - 2}deg)`, opacity: Math.min(1, tagS * 1.5),
        }}>
          <span style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 74, color: '#08CB00',
            background: '#000000', padding: '14px 52px', borderRadius: 18,
          }}>
            THEY FOUND SOMETHING CHEAPER THAN YOU.
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene07SeesawParadox;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08SevenHundredBillion',
  durationInSeconds: 26.05,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08SevenHundredBillion: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade in the last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom-in toward the number
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.1]);

  // $700B counter
  const capex = Math.floor(interpolate(frame, [220, 430], [0, 700], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  }));

  // 2025 vs 2026 bars (nearly double)
  const bar25 = interpolate(frame, [480, 560], [0, 190], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const bar26 = interpolate(frame, [530, 640], [0, 360], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  const companies = ['AMAZON', 'MICROSOFT', 'ALPHABET', 'META'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#08CB00', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Layer 1: Dynamic gradient background */}
        <AbsoluteFill style={{ background: 'linear-gradient(140deg, #08CB00 0%, #067A00 70%, #045800 100%)' }} />

        {/* Layer 2: Animated transparent shape layers */}
        <div style={{
          position: 'absolute', width: 900, height: 900, borderRadius: '50%',
          background: 'rgba(238,238,238,0.04)', left: '50%', top: '46%',
          transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }} />
        <div style={{
          position: 'absolute', width: 480, height: 480, border: '3px solid rgba(0,0,0,0.08)',
          left: -160, bottom: -160, borderRadius: '50%', transform: `rotate(${frame * 0.09}deg)`,
        }} />

        {/* Layer 3: Motion texture — currency rain columns */}
        {Array.from({ length: 10 }).map((_, i) => {
          const s = seededRandom(i + 5);
          return (
            <div key={`col-${i}`} style={{
              position: 'absolute', left: 100 + i * 190,
              top: ((frame * (2 + s * 3)) % 1300) - 200,
              fontFamily: 'JetBrains Mono, monospace', fontSize: 28,
              color: 'rgba(0,0,0,0.1)', whiteSpace: 'pre-line', lineHeight: 1.6,
            }}>
              {'$\n$\n$\n$'}
            </div>
          );
        })}

        {/* Layer 4: Floating particles */}
        {Array.from({ length: 10 }).map((_, i) => {
          const s = seededRandom(i + 120);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute', left: s * 1920,
              top: ((seededRandom(i + 8) * 1080) + frame * 0.5) % 1080,
              width: 6, height: 6, background: 'rgba(238,238,238,0.35)', borderRadius: '50%',
            }} />
          );
        })}

        {/* Layer 5: Main content */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 80, textAlign: 'center', opacity: Math.min(1, sp(10) * 1.4) }}>
          <span style={{ fontFamily: 'Inter, sans-serif', fontWeight: 800, fontSize: 42, color: '#EEEEEE', letterSpacing: 6 }}>
            THE LARGEST CAPITAL INVESTMENT CYCLE IN INDUSTRY HISTORY
          </span>
        </div>

        {/* Company chips */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 180, display: 'flex', justifyContent: 'center', gap: 30 }}>
          {companies.map((c, i) => (
            <div key={c} style={{
              fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 40, color: '#08CB00',
              background: '#000000', borderRadius: 14, padding: '14px 38px',
              transform: `translateY(${(1 - sp(60 + i * 20)) * -120}px)`, opacity: Math.min(1, sp(60 + i * 20) * 1.4),
            }}>
              {c}
            </div>
          ))}
        </div>

        {/* Hero number */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 320, textAlign: 'center', opacity: Math.min(1, sp(210) * 1.4) }}>
          <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 250, color: '#EEEEEE', lineHeight: 1, textShadow: '0 12px 0 rgba(0,0,0,0.4)' }}>
            ${capex}B
          </div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 34, color: '#000000', letterSpacing: 5, marginTop: 14 }}>
            GUIDED 2026 CAPITAL EXPENDITURE
          </div>
        </div>

        {/* Doubling bar chart 2025 vs 2026 */}
        <div style={{ position: 'absolute', left: 560, top: 660, display: 'flex', alignItems: 'flex-end', gap: 90, height: 380 }}>
          <div style={{ textAlign: 'center' }}>
            <div style={{ width: 240, height: bar25, background: '#000000', borderRadius: '14px 14px 0 0' }} />
            <div style={{ fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 40, color: '#000000', marginTop: 12 }}>2025</div>
          </div>
          <div style={{ textAlign: 'center' }}>
            <div style={{ width: 240, height: bar26, background: '#EEEEEE', borderRadius: '14px 14px 0 0', border: '5px solid #000000' }} />
            <div style={{ fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 40, color: '#EEEEEE', marginTop: 12 }}>2026</div>
          </div>
          <div style={{
            alignSelf: 'center', fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 62, color: '#000000',
            opacity: sp(650), transform: `scale(${0.6 + sp(650) * 0.4})`,
          }}>
            NEARLY 2X
          </div>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene08SevenHundredBillion;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09PayrollToProcessors',
  durationInSeconds: 26.05,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09PayrollToProcessors: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade in the last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: lateral drift
  const camX = interpolate(frame, [0, durationInFrames], [-25, 25]);

  const targets = ['AI DATA CENTERS', 'CUSTOM SILICON', 'INFERENCE INFRASTRUCTURE'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#08CB00', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ transform: `translateX(${camX}px) scale(1.06)` }}>

        {/* Layer 1: Dynamic gradient background */}
        <AbsoluteFill style={{ background: 'linear-gradient(100deg, #0AE600 0%, #08CB00 45%, #056B00 100%)' }} />

        {/* Layer 2: Animated transparent shape layers */}
        <div style={{
          position: 'absolute', width: 620, height: 620, borderRadius: 50,
          border: '3px solid rgba(0,0,0,0.07)', left: '50%', top: '50%',
          transform: `translate(-50%, -50%) rotate(${frame * 0.08}deg)`,
        }} />
        <div style={{
          position: 'absolute', width: 900, height: 900, borderRadius: '50%',
          background: 'rgba(238,238,238,0.04)', right: -400, top: -350,
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }} />

        {/* Layer 3: Motion texture — circuit trace lines */}
        {Array.from({ length: 9 }).map((_, i) => (
          <div key={`tr-${i}`} style={{
            position: 'absolute', top: 110 + i * 110, left: 0, width: 1920, height: 1,
            background: 'rgba(0,0,0,0.06)',
          }} />
        ))}
        {Array.from({ length: 9 }).map((_, i) => (
          <div key={`tn-${i}`} style={{
            position: 'absolute', top: 106 + i * 110, left: (i * 260 + frame * 2) % 1920,
            width: 9, height: 9, borderRadius: '50%', background: 'rgba(0,0,0,0.14)',
          }} />
        ))}

        {/* Layer 4: Floating particles */}
        {Array.from({ length: 10 }).map((_, i) => {
          const s = seededRandom(i + 140);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute', left: s * 1920,
              top: (seededRandom(i + 45) * 1080 + frame * (0.3 + s * 0.5)) % 1080,
              width: 5, height: 5, background: 'rgba(238,238,238,0.4)', borderRadius: 1,
              transform: `rotate(${frame * 2}deg)`,
            }} />
          );
        })}

        {/* Layer 5: Main content — money migration diagram */}
        {/* PAYROLL box */}
        <div style={{
          position: 'absolute', left: 130, top: 330, width: 480, background: '#EEEEEE',
          borderRadius: 26, padding: '44px 40px', textAlign: 'center', border: '6px solid #000000',
          transform: `translateX(${(1 - sp(20)) * -400}px)`, opacity: Math.min(1, sp(20) * 1.4),
        }}>
          <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 74, color: '#000000' }}>PAYROLL</div>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 28, color: '#067A00', marginTop: 10 }}>
            SALARIES • PEOPLE • DESKS
          </div>
        </div>

        {/* Animated money packets crossing */}
        <div style={{ position: 'absolute', left: 640, top: 400, width: 620, height: 120, opacity: Math.min(1, sp(120) * 1.4) }}>
          <div style={{ position: 'absolute', top: 52, width: 620, height: 8, background: 'rgba(0,0,0,0.3)', borderRadius: 4 }} />
          {Array.from({ length: 5 }).map((_, i) => (
            <div key={`pk-${i}`} style={{
              position: 'absolute', top: 34,
              left: ((frame * 5 + i * 130) % 620),
              width: 76, height: 44, background: '#000000', borderRadius: 10,
              display: 'flex', alignItems: 'center', justifyContent: 'center',
              fontFamily: 'JetBrains Mono, monospace', fontSize: 26, color: '#08CB00',
            }}>
              $
            </div>
          ))}
        </div>

        {/* PROCESSORS box with chip SVG */}
        <div style={{
          position: 'absolute', right: 130, top: 280, width: 520, background: '#000000',
          borderRadius: 26, padding: '40px 40px', textAlign: 'center',
          transform: `translateX(${(1 - sp(60)) * 400}px) scale(${1 + Math.sin(frame * 0.05) * 0.012})`,
          opacity: Math.min(1, sp(60) * 1.4),
        }}>
          <svg width={150} height={150} viewBox="0 0 100 100" style={{ margin: '0 auto' }}>
            <rect x={25} y={25} width={50} height={50} rx={8} fill="#08CB00" />
            <rect x={38} y={38} width={24} height={24} rx={4} fill="#000000" />
            {[32, 44, 56, 68].map((p) => (
              <React.Fragment key={p}>
                <rect x={p - 2} y={10} width={4} height={12} fill="#EEEEEE" />
                <rect x={p - 2} y={78} width={4} height={12} fill="#EEEEEE" />
                <rect x={10} y={p - 2} width={12} height={4} fill="#EEEEEE" />
                <rect x={78} y={p - 2} width={12} height={4} fill="#EEEEEE" />
              </React.Fragment>
            ))}
          </svg>
          <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 74, color: '#EEEEEE', marginTop: 8 }}>PROCESSORS</div>
        </div>

        {/* Spend target chips */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 720, display: 'flex', justifyContent: 'center', gap: 30 }}>
          {targets.map((t, i) => (
            <div key={t} style={{
              fontFamily: 'Nexa Bold, Inter, sans-serif', fontSize: 34, color: '#000000',
              background: '#EEEEEE', border: '4px solid #000000', borderRadius: 14, padding: '16px 34px',
              transform: `translateY(${(1 - sp(280 + i * 25)) * 180}px)`, opacity: Math.min(1, sp(280 + i * 25) * 1.4),
            }}>
              {t}
            </div>
          ))}
        </div>

        {/* Payoff line */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 880, textAlign: 'center', opacity: Math.min(1, sp(480) * 1.4), transform: `scale(${0.75 + sp(480) * 0.25})` }}>
          <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 66, color: '#EEEEEE', textShadow: '0 6px 0 rgba(0,0,0,0.4)' }}>
            THE BUDGET DIDN'T DISAPPEAR. IT MOVED.
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene09PayrollToProcessors;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10MetaWhiplash',
  durationInSeconds: 26.05,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10MetaWhiplash: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Exit fade in the last 15 frames
  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: push in with subtle tilt
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.08]);
  const camRot = interpolate(frame, [0, durationInFrames], [-0.4, 0.4]);

  // Counters
  const rev = interpolate(frame, [80, 220], [0, 56.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const cuts = Math.floor(interpolate(frame, [260, 400], [0, 8000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));
  const capex = Math.floor(interpolate(frame, [440, 580], [0, 145], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));

  // Screen shake when the 8,000 lands
  const shake = frame > 400 && frame < 415 ? Math.sin(frame * 2.7) * 9 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#08CB00', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg) translateX(${shake}px)` }}>

        {/* Layer 1: Dynamic gradient background */}
        <AbsoluteFill style={{ background: 'linear-gradient(170deg, #067A00 0%, #08CB00 50%, #0AE600 100%)' }} />

        {/* Layer 2: Animated transparent shape layers */}
        <div style={{
          position: 'absolute', width: 1000, height: 1000, borderRadius: '50%',
          border: '2px solid rgba(0,0,0,0.06)', left: '50%', top: '40%',
          transform: `translate(-50%, -50%) rotate(${frame * 0.05}deg)`,
        }} />
        <div style={{
          position: 'absolute', width: 500, height: 500, background: 'rgba(238,238,238,0.045)',
          left: -160, bottom: -180, borderRadius: 44, transform: `rotate(${frame * 0.06}deg)`,
        }} />

        {/* Layer 3: Motion texture — column bars */}
        {Array.from({ length: 18 }).map((_, i) => (
          <div key={`cb-${i}`} style={{
            position: 'absolute', left: 30 + i * 108, bottom: 0,
            width: 42, height: 130 + Math.sin(frame * 0.03 + i) * 40,
            background: 'rgba(0,0,0,0.05)', borderRadius: '8px 8px 0 0',
          }} />
        ))}

        {/* Layer 4: Floating particles */}
        {Array.from({ length: 12 }).map((_, i) => {
          const s = seededRandom(i + 160);
          return (
            <div key={`p-${i}`} style={{
              position: 'absolute', left: ((s * 1920) + frame * (0.4 + s * 0.6)) % 1920,
              top: seededRandom(i + 33) * 1080, width: 5 + s * 5, height: 5 + s * 5,
              background: 'rgba(238,238,238,0.35)', borderRadius: '50%',
            }} />
          );
        })}

        {/* Layer 5: Main content — whiplash stat triptych */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 80, textAlign: 'center', opacity: Math.min(1, sp(8) * 1.4) }}>
          <span style={{
            fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 96, color: '#EEEEEE',
            background: '#000000', padding: '8px 56px', borderRadius: 18,
          }}>
            META: THE WHOLE MOVIE IN ONE SENTENCE
          </span>
        </div>

        {/* Card 1: Revenue */}
        <div style={{
          position: 'absolute', left: 110, top: 300, width: 520, background: '#EEEEEE',
          borderRadius: 26, padding: '44px 30px', textAlign: 'center', border: '6px solid #000000',
          transform: `translateY(${(1 - sp(60)) * 300}px)`, opacity: Math.min(1, sp(60) * 1.4),
        }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 28, color: '#067A00', letterSpacing: 3 }}>Q1 REVENUE</div>
          <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 108, color: '#000000', lineHeight: 1.1 }}>
            ${rev.toFixed(1)}B
          </div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontWeight: 800, fontSize: 42, color: '#067A00' }}>UP 33%</div>
          {/* Up arrow */}
          <div style={{ width: 0, height: 0, margin: '16px auto 0', borderLeft: '30px solid transparent', borderRight: '30px solid transparent', borderBottom: '46px solid #067A00' }} />
        </div>

        {/* Card 2: Layoffs */}
        <div style={{
          position: 'absolute', left: 700, top: 300, width: 520, background: '#000000',
          borderRadius: 26, padding: '44px 30px', textAlign: 'center',
          transform: `translateY(${(1 - sp(240)) * 300}px)`, opacity: Math.min(1, sp(240) * 1.4),
        }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 28, color: '#08CB00', letterSpacing: 3 }}>SAME PERIOD</div>
          <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 108, color: '#EEEEEE', lineHeight: 1.1 }}>
            {cuts.toLocaleString('en-US')}
          </div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontWeight: 800, fontSize: 42, color: '#EEEEEE' }}>LAYOFFS</div>
          {/* Down arrow */}
          <div style={{ width: 0, height: 0, margin: '16px auto 0', borderLeft: '30px solid transparent', borderRight: '30px solid transparent', borderTop: '46px solid #08CB00' }} />
        </div>

        {/* Card 3: Capex */}
        <div style={{
          position: 'absolute', right: 110, top: 300, width: 520, background: '#EEEEEE',
          borderRadius: 26, padding: '44px 30px', textAlign: 'center', border: '6px solid #000000',
          transform: `translateY(${(1 - sp(420)) * 300}px)`, opacity: Math.min(1, sp(420) * 1.4),
        }}>
          <div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 28, color: '#067A00', letterSpacing: 3 }}>AI CAPEX GUIDANCE</div>
          <div style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 108, color: '#000000', lineHeight: 1.1 }}>
            ${capex}B
          </div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontWeight: 800, fontSize: 42, color: '#067A00' }}>RAISED HIGHER</div>
          <div style={{ width: 0, height: 0, margin: '16px auto 0', borderLeft: '30px solid transparent', borderRight: '30px solid transparent', borderBottom: '46px solid #067A00' }} />
        </div>

        {/* Payoff strip */}
        <div style={{
          position: 'absolute', left: 0, right: 0, top: 880, textAlign: 'center',
          opacity: Math.min(1, sp(600) * 1.4), transform: `scale(${0.8 + sp(600) * 0.2})`,
        }}>
          <span style={{ fontFamily: 'Mont Heavy, Inter, sans-serif', fontSize: 64, color: '#000000', letterSpacing: 1 }}>
            REVENUE UP A THIRD — 8,000 PEOPLE OUT THE DOOR
          </span>
        </div>
      </AbsoluteFill>

      {/* Layer 6: Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.28) 100%)', pointerEvents: 'none' }} />

      {/* Layer 7: Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene10MetaWhiplash;
```

