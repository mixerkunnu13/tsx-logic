```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene31: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow push-in
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.08], { easing: Easing.inOut(Easing.ease) });

  // Counter roll-up: 0 -> 157
  const counter = Math.round(interpolate(frame, [70, 170], [0, 157], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  }));

  // Bar chart values
  const bar2024 = interpolate(frame, [180, 230], [0, 116], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const bar2025 = interpolate(frame, [210, 270], [0, 157], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Micro shake on counter finish
  const shake = frame > 168 && frame < 180 ? Math.sin(frame * 3) * (180 - frame) * 0.4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1E4640', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse at ${50 + Math.sin(frame * 0.01) * 10}% 30%, #408175 0%, #2A5B52 45%, #16352F 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        border: '2px solid rgba(235,237,227,0.05)', left: -250, top: -300,
        transform: `rotate(${frame * 0.08}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 600, height: 600,
        background: 'rgba(255,244,73,0.03)', right: -180, bottom: -220,
        transform: `rotate(${45 + frame * 0.05}deg)`,
      }} />

      {/* Motion texture: vertical scan lines */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(90deg, rgba(235,237,227,0.02) 0px, rgba(235,237,227,0.02) 1px, transparent 1px, transparent 90px)',
        transform: `translateX(${(frame * 0.3) % 90}px)`,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const sx = seededRandom(i + 1) * 1920;
        const sy = seededRandom(i + 50) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 20;
        return (
          <div key={i} style={{
            position: 'absolute', left: sx + drift, top: (sy - frame * (0.2 + seededRandom(i) * 0.4) + 1080) % 1080,
            width: 3 + seededRandom(i + 9) * 3, height: 3 + seededRandom(i + 9) * 3, borderRadius: '50%',
            backgroundColor: i % 4 === 0 ? 'rgba(255,244,73,0.35)' : 'rgba(235,237,227,0.2)',
          }} />
        );
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)`, justifyContent: 'center', alignItems: 'center' }}>

        {/* Question line */}
        <div style={{
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 44, fontWeight: 600, color: '#EBEDE3',
          opacity: sp(10), transform: `translateY(${(1 - sp(10)) * 40}px)`, letterSpacing: 2, textTransform: 'uppercase',
        }}>
          How many unused subscriptions are draining you?
        </div>

        {/* Hero counter */}
        <div style={{
          fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 200, fontWeight: 900, color: '#FFF449',
          lineHeight: 1, marginTop: 30, opacity: sp(60),
          transform: `scale(${0.8 + sp(60) * 0.2})`,
          textShadow: '0 20px 60px rgba(0,0,0,0.35)',
        }}>
          ${counter}B
        </div>

        <div style={{
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 40, fontWeight: 700, color: '#EBEDE3',
          opacity: sp(90), letterSpacing: 4, marginTop: 10, textTransform: 'uppercase',
        }}>
          BNPL Consumer Credit Originated — 2025
        </div>

        {/* Comparison bars 2024 vs 2025 */}
        <div style={{ display: 'flex', gap: 60, alignItems: 'flex-end', height: 220, marginTop: 55, opacity: sp(175) }}>
          {[
            { label: '2024', val: bar2024, max: 116, color: 'rgba(235,237,227,0.4)' },
            { label: '2025', val: bar2025, max: 157, color: '#FFF449' },
          ].map((b, i) => (
            <div key={i} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
              <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 34, color: b.color === '#FFF449' ? '#FFF449' : '#EBEDE3', marginBottom: 10 }}>
                ${Math.round(b.val)}B
              </div>
              <div style={{
                width: 150, height: (b.val / 157) * 160, backgroundColor: b.color, borderRadius: 6,
                boxShadow: b.color === '#FFF449' ? '0 0 40px rgba(255,244,73,0.3)' : 'none',
              }} />
              <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 30, color: '#EBEDE3', marginTop: 12, fontWeight: 600 }}>{b.label}</div>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(10,25,22,0.55) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene31;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow lateral pan
  const camX = interpolate(frame, [0, durationInFrames], [-25, 25], { easing: Easing.inOut(Easing.ease) });

  // Radar sweep angle
  const sweep = (frame * 2.2) % 360;

  // Warning pulse
  const pulse = 1 + Math.sin(frame * 0.18) * 0.05;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1E4640', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${140 + Math.sin(frame * 0.008) * 12}deg, #16352F 0%, #2E6156 50%, #408175 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 700, height: 700,
        border: '3px dashed rgba(255,244,73,0.06)', borderRadius: '50%',
        left: 1250, top: -200, transform: `rotate(${-frame * 0.12}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 0, height: 0,
        borderLeft: '260px solid transparent', borderRight: '260px solid transparent',
        borderBottom: '440px solid rgba(235,237,227,0.03)',
        left: -120, bottom: -160, transform: `rotate(${12 + Math.sin(frame * 0.015) * 4}deg)`,
      }} />

      {/* Motion texture: radar grid rings */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center' }}>
        {[220, 380, 540, 700].map((r, i) => (
          <div key={i} style={{
            position: 'absolute', width: r, height: r, borderRadius: '50%',
            border: '1px solid rgba(235,237,227,0.06)',
            transform: `scale(${1 + ((frame * 0.004 + i * 0.25) % 1) * 0.08})`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 3) * 1920 + Math.sin(frame * 0.03 + i * 2) * 25,
          top: (seededRandom(i + 40) * 1080 + frame * 0.25) % 1080,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: 'rgba(235,237,227,0.22)',
        }} />
      ))}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translateX(${camX}px)`, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 110 }}>

        {/* Radar blind-spot iconographic */}
        <div style={{ position: 'relative', width: 480, height: 480, opacity: sp(20), transform: `scale(${sp(20)})` }}>
          <svg width="480" height="480" viewBox="0 0 480 480">
            <circle cx="240" cy="240" r="220" fill="rgba(30,70,64,0.5)" stroke="#EBEDE3" strokeWidth="2" strokeOpacity="0.35" />
            <circle cx="240" cy="240" r="150" fill="none" stroke="#EBEDE3" strokeWidth="1" strokeOpacity="0.25" />
            <circle cx="240" cy="240" r="80" fill="none" stroke="#EBEDE3" strokeWidth="1" strokeOpacity="0.25" />
            {/* Radar sweep */}
            <g transform={`rotate(${sweep} 240 240)`}>
              <path d="M240,240 L240,20 A220,220 0 0,1 380,70 Z" fill="rgba(255,244,73,0.16)" />
              <line x1="240" y1="240" x2="240" y2="20" stroke="#FFF449" strokeWidth="3" strokeOpacity="0.8" />
            </g>
            {/* The blind spot: dark wedge that sweep never lights */}
            <path d="M240,240 L440,160 A220,220 0 0,1 440,320 Z" fill="rgba(10,25,22,0.75)" />
            <text x="392" y="248" fill="#FFF449" fontFamily="JetBrains Mono, monospace" fontSize="22" textAnchor="middle" opacity={sp(90)}>?</text>
          </svg>
          {/* Pulsing warning triangle */}
          <svg width="110" height="100" viewBox="0 0 110 100" style={{
            position: 'absolute', right: -20, top: -10, transform: `scale(${pulse})`, opacity: sp(110),
          }}>
            <path d="M55 6 L104 92 L6 92 Z" fill="none" stroke="#FFF449" strokeWidth="6" strokeLinejoin="round" />
            <rect x="50" y="34" width="10" height="30" rx="4" fill="#FFF449" />
            <circle cx="55" cy="76" r="6" fill="#FFF449" />
          </svg>
        </div>

        {/* Typography block */}
        <div style={{ width: 760 }}>
          <div style={{
            fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 108, lineHeight: 1.02, color: '#EBEDE3',
            opacity: sp(45), transform: `translateY(${(1 - sp(45)) * 60}px)`,
          }}>
            A REGULATORY
          </div>
          <div style={{
            fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 128, lineHeight: 1.02, color: '#FFF449',
            opacity: sp(65), transform: `translateY(${(1 - sp(65)) * 60}px)`,
          }}>
            BLIND SPOT
          </div>
          <div style={{
            width: interpolate(frame, [95, 130], [0, 620], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            height: 5, backgroundColor: '#FFF449', margin: '26px 0', borderRadius: 3,
          }} />
          <div style={{
            fontFamily: 'Inter, Poppins, sans-serif', fontSize: 42, fontWeight: 600, color: '#EBEDE3',
            opacity: sp(130), lineHeight: 1.4,
          }}>
            A structural emergency, monetized in real time — with none of the oversight of traditional lending.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(10,25,22,0.55) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene32;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const STEPS = [
  { label: 'CARDS MAXED', num: '01' },
  { label: 'TURN TO BNPL', num: '02' },
  { label: 'MISSED PAYMENT', num: '03' },
  { label: 'LATE FEES', num: '04' },
  { label: 'OVERDRAFT', num: '05' },
  { label: 'BORROW AGAIN', num: '06' },
];

const Scene33: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow tilt-zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1], { easing: Easing.inOut(Easing.ease) });
  const camRot = interpolate(frame, [0, durationInFrames], [-0.6, 0.6]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#1E4640', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(180deg, #245049 0%, #408175 ${55 + Math.sin(frame * 0.012) * 8}%, #16352F 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 800, height: 800, borderRadius: 60,
        background: 'rgba(235,237,227,0.03)', left: -300, top: 500,
        transform: `rotate(${25 + frame * 0.04}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500, borderRadius: '50%',
        border: '2px solid rgba(255,244,73,0.06)', right: -140, top: -160,
        transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
      }} />

      {/* Motion texture: diagonal hazard lines */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(-45deg, rgba(255,244,73,0.02) 0px, rgba(255,244,73,0.02) 2px, transparent 2px, transparent 70px)',
        transform: `translateX(${-(frame * 0.4) % 70}px)`,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 13 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: (seededRandom(i + 7) * 1920 + frame * 0.3) % 1920,
          top: seededRandom(i + 70) * 1080 + Math.sin(frame * 0.025 + i) * 18,
          width: 3, height: 3, borderRadius: '50%',
          backgroundColor: i % 3 === 0 ? 'rgba(255,244,73,0.3)' : 'rgba(235,237,227,0.18)',
        }} />
      ))}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)`, justifyContent: 'center', alignItems: 'center' }}>

        <div style={{
          fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 96, color: '#EBEDE3',
          opacity: sp(8), transform: `translateY(${(1 - sp(8)) * -50}px)`, marginBottom: 60,
        }}>
          THE DEBT <span style={{ color: '#FFF449' }}>CASCADE</span>
        </div>

        {/* Domino step cards — sequential fall-in */}
        <div style={{ display: 'flex', gap: 26, alignItems: 'center' }}>
          {STEPS.map((s, i) => {
            const d = 45 + i * 22; // stagger 22 frames
            const p = sp(d, { damping: 10 });
            const tilt = interpolate(p, [0, 1], [18, 0]);
            return (
              <React.Fragment key={i}>
                <div style={{
                  width: 220, height: 300, borderRadius: 18, padding: '26px 20px',
                  background: i === STEPS.length - 1 ? 'rgba(255,244,73,0.12)' : 'rgba(235,237,227,0.06)',
                  border: `2px solid ${i === STEPS.length - 1 ? '#FFF449' : 'rgba(235,237,227,0.2)'}`,
                  display: 'flex', flexDirection: 'column', justifyContent: 'space-between',
                  opacity: p, transform: `translateY(${(1 - p) * -90}px) rotate(${tilt}deg)`,
                  transformOrigin: 'bottom left',
                  boxShadow: '0 24px 50px rgba(10,25,22,0.4)',
                }}>
                  <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 44, color: '#FFF449', fontWeight: 700 }}>{s.num}</div>
                  {/* Descending bars micro-animation inside each card */}
                  <div style={{ display: 'flex', gap: 6, alignItems: 'flex-end', height: 70 }}>
                    {[0, 1, 2, 3].map((b) => (
                      <div key={b} style={{
                        width: 14, borderRadius: 3, backgroundColor: 'rgba(235,237,227,0.35)',
                        height: 20 + Math.abs(Math.sin(frame * 0.08 + i + b)) * 45,
                      }} />
                    ))}
                  </div>
                  <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 30, fontWeight: 800, color: '#EBEDE3', lineHeight: 1.15 }}>
                    {s.label}
                  </div>
                </div>
                {i < STEPS.length - 1 && (
                  <svg width="34" height="30" viewBox="0 0 34 30" style={{ opacity: sp(d + 14), flexShrink: 0 }}>
                    <path d="M2 15 H24 M18 5 L28 15 L18 25" stroke="#FFF449" strokeWidth="4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
                  </svg>
                )}
              </React.Fragment>
            );
          })}
        </div>

        <div style={{
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 40, fontWeight: 600, color: '#EBEDE3',
          opacity: sp(200), marginTop: 60, letterSpacing: 1,
        }}>
          Each step borrows to cover the damage of the last one.
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(10,25,22,0.55) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene33;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene34: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow orbit feel via skew + scale
  const camScale = 1 + Math.sin(frame * 0.006) * 0.03;

  // Chain links snap in with impact shake
  const linkDelays = [50, 80, 110, 140, 170];
  const impactShake = linkDelays.reduce((acc, d) => {
    if (frame > d && frame < d + 8) return Math.sin(frame * 4) * (d + 8 - frame) * 1.2;
    return acc;
  }, 0);

  const ringRot = frame * 0.5;

  return (
    <AbsoluteFill style={{ backgroundColor: '#1E4640', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 30% ${60 + Math.sin(frame * 0.01) * 10}%, #35695F 0%, #244F48 50%, #142F2A 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 1100, height: 1100, borderRadius: '50%',
        border: '1px solid rgba(235,237,227,0.05)', left: 410, top: -10,
        transform: `translate(-50%,-0%) rotate(${-frame * 0.06}deg) scale(${1 + Math.sin(frame * 0.018) * 0.03})`,
      }} />
      <div style={{
        position: 'absolute', width: 420, height: 420,
        background: 'rgba(255,244,73,0.035)', borderRadius: 40,
        left: -140, top: -140, transform: `rotate(${frame * 0.07}deg)`,
      }} />

      {/* Motion texture: concentric dotted circles */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center' }}>
        {[300, 480, 660].map((r, i) => (
          <div key={i} style={{
            position: 'absolute', width: r, height: r, borderRadius: '50%',
            border: '2px dotted rgba(235,237,227,0.07)',
            transform: `rotate(${frame * (0.2 + i * 0.1) * (i % 2 ? -1 : 1)}deg)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const ang = seededRandom(i + 11) * Math.PI * 2 + frame * 0.008 * (i % 2 ? 1 : -1);
        const rad = 350 + seededRandom(i + 60) * 320;
        return (
          <div key={i} style={{
            position: 'absolute', left: 960 + Math.cos(ang) * rad, top: 540 + Math.sin(ang) * rad * 0.6,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: i % 4 === 0 ? 'rgba(255,244,73,0.35)' : 'rgba(235,237,227,0.2)',
          }} />
        );
      })}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{
        transform: `scale(${camScale}) translateX(${impactShake}px)`,
        flexDirection: 'row', justifyContent: 'center', alignItems: 'center', gap: 120,
      }}>

        {/* Rotating cycle ring with chain links */}
        <div style={{ position: 'relative', width: 520, height: 520, opacity: sp(15), transform: `scale(${sp(15)})` }}>
          <svg width="520" height="520" viewBox="0 0 520 520">
            <g transform={`rotate(${ringRot} 260 260)`}>
              <circle cx="260" cy="260" r="200" fill="none" stroke="rgba(235,237,227,0.18)" strokeWidth="3" strokeDasharray="14 10" />
              {/* Chain links around the ring */}
              {linkDelays.map((d, i) => {
                const a = (i / 5) * Math.PI * 2;
                const lx = 260 + Math.cos(a) * 200;
                const ly = 260 + Math.sin(a) * 200;
                const p = spring({ frame: Math.max(0, frame - d), fps, config: { damping: 9, stiffness: 160 } });
                return (
                  <g key={i} transform={`translate(${lx} ${ly}) rotate(${(a * 180) / Math.PI + 90}) scale(${p})`}>
                    <rect x="-26" y="-16" width="52" height="32" rx="16" fill="none" stroke="#FFF449" strokeWidth="7" />
                  </g>
                );
              })}
            </g>
            {/* Center arrow loop icon */}
            <g opacity={sp(35)}>
              <path d="M260 150 A110 110 0 1 1 155 300" fill="none" stroke="#EBEDE3" strokeWidth="10" strokeLinecap="round" />
              <path d="M140 268 L155 305 L190 288" fill="none" stroke="#EBEDE3" strokeWidth="10" strokeLinecap="round" strokeLinejoin="round" />
            </g>
          </svg>
        </div>

        {/* Typography */}
        <div style={{ width: 720 }}>
          <div style={{
            fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 118, lineHeight: 1, color: '#FFF449',
            opacity: sp(40), transform: `translateX(${(1 - sp(40)) * 80}px)`,
          }}>
            THE CYCLE
          </div>
          <div style={{
            fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 118, lineHeight: 1.05, color: '#EBEDE3',
            opacity: sp(58), transform: `translateX(${(1 - sp(58)) * 80}px)`,
          }}>
            COMPOUNDS
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins, sans-serif', fontSize: 44, fontWeight: 600, color: '#EBEDE3',
            marginTop: 36, lineHeight: 1.45, opacity: sp(120),
          }}>
            Marketed as a <span style={{ color: '#FFF449', fontWeight: 800 }}>solution</span>.
            <br />
            Forged as another <span style={{ color: '#FFF449', fontWeight: 800 }}>link in the chain</span>.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(10,25,22,0.55) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene34;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // APR counter: 22 -> 400
  const apr = Math.round(interpolate(frame, [70, 190], [22, 400], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  }));

  // Spike line draw progress
  const lineDraw = interpolate(frame, [60, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  // Screen shake at 400% impact
  const shake = frame > 188 && frame < 202 ? Math.sin(frame * 3.5) * (202 - frame) * 0.9 : 0;

  // Camera pull-back
  const camScale = interpolate(frame, [0, durationInFrames], [1.1, 1.0], { easing: Easing.out(Easing.ease) });

  // Trap jaws closing (top/bottom triangle rows)
  const jaws = interpolate(frame, [220, 280], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1E4640', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${200 + Math.sin(frame * 0.01) * 10}deg, #12302B 0%, #2C5B52 55%, #408175 110%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 760, height: 760, borderRadius: '50%',
        background: 'rgba(255,244,73,0.03)', left: -260, top: -280,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 520, height: 520,
        border: '2px solid rgba(235,237,227,0.05)', right: -160, bottom: -190,
        transform: `rotate(${frame * 0.06}deg)`,
      }} />

      {/* Motion texture: chart grid */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(rgba(235,237,227,0.035) 1px, transparent 1px), linear-gradient(90deg, rgba(235,237,227,0.035) 1px, transparent 1px)',
        backgroundSize: '110px 110px',
        transform: `translateY(${(frame * 0.15) % 110}px)`,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 21) * 1920,
          top: (seededRandom(i + 80) * 1080 - frame * (0.3 + seededRandom(i) * 0.5) + 2160) % 1080,
          width: 3 + seededRandom(i + 5) * 3, height: 3 + seededRandom(i + 5) * 3, borderRadius: '50%',
          backgroundColor: i % 3 === 0 ? 'rgba(255,244,73,0.3)' : 'rgba(235,237,227,0.18)',
        }} />
      ))}

      {/* Trap jaws — closing from top and bottom */}
      <div style={{ position: 'absolute', top: -14 + jaws * 40, left: 0, right: 0, display: 'flex', justifyContent: 'center' }}>
        {Array.from({ length: 16 }).map((_, i) => (
          <div key={i} style={{
            width: 0, height: 0, borderLeft: '60px solid transparent', borderRight: '60px solid transparent',
            borderTop: '52px solid rgba(255,244,73,0.14)',
          }} />
        ))}
      </div>
      <div style={{ position: 'absolute', bottom: -14 + jaws * 40, left: 0, right: 0, display: 'flex', justifyContent: 'center' }}>
        {Array.from({ length: 16 }).map((_, i) => (
          <div key={i} style={{
            width: 0, height: 0, borderLeft: '60px solid transparent', borderRight: '60px solid transparent',
            borderBottom: '52px solid rgba(255,244,73,0.14)',
          }} />
        ))}
      </div>

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${shake}px, ${shake * 0.5}px)`, flexDirection: 'row', justifyContent: 'center', alignItems: 'center', gap: 100 }}>

        {/* Spike chart */}
        <div style={{ opacity: sp(30) }}>
          <svg width="640" height="500" viewBox="0 0 640 500">
            <line x1="60" y1="440" x2="620" y2="440" stroke="rgba(235,237,227,0.35)" strokeWidth="3" />
            <line x1="60" y1="440" x2="60" y2="20" stroke="rgba(235,237,227,0.35)" strokeWidth="3" />
            {/* Baseline: credit card APR */}
            <line x1="60" y1="400" x2="620" y2="400" stroke="rgba(235,237,227,0.3)" strokeWidth="2" strokeDasharray="10 8" />
            <text x="620" y="388" fill="#EBEDE3" opacity="0.7" fontFamily="JetBrains Mono, monospace" fontSize="26" textAnchor="end">CREDIT CARD ~22%</text>
            {/* Spike path drawn progressively */}
            <path
              d="M60 400 L200 380 L300 330 L400 220 L500 90 L590 40"
              fill="none" stroke="#FFF449" strokeWidth="9" strokeLinecap="round" strokeLinejoin="round"
              strokeDasharray={800} strokeDashoffset={800 - lineDraw * 800}
            />
            {/* Spike endpoint glow dot */}
            {lineDraw > 0.98 && (
              <circle cx="590" cy="40" r={10 + Math.sin(frame * 0.3) * 4} fill="#FFF449" />
            )}
          </svg>
        </div>

        {/* Typography + counter */}
        <div style={{ width: 680 }}>
          <div style={{
            fontFamily: 'Inter, Poppins, sans-serif', fontSize: 42, fontWeight: 700, color: '#EBEDE3',
            letterSpacing: 6, textTransform: 'uppercase', opacity: sp(15),
          }}>
            Payday Lending APR
          </div>
          <div style={{
            fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 216, lineHeight: 1, color: '#FFF449',
            opacity: sp(55), textShadow: '0 18px 70px rgba(0,0,0,0.4)',
          }}>
            {apr}%
          </div>
          <div style={{
            fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 84, color: '#EBEDE3',
            opacity: sp(210), transform: `translateY(${(1 - sp(210)) * 40}px)`, marginTop: 20,
          }}>
            PREDATORY <span style={{ color: '#FFF449' }}>BY DESIGN</span>
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins, sans-serif', fontSize: 38, fontWeight: 600, color: '#EBEDE3',
            opacity: sp(245), marginTop: 20, lineHeight: 1.4,
          }}>
            Rollover mechanics engineered to keep borrowers trapped.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 52%, rgba(10,25,22,0.6) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene35;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene36: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Split divider sweeps in at midpoint of the audio
  const divider = interpolate(frame, [230, 275], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  // EKG line draw
  const ekg = interpolate(frame, [280, 420], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Fee stack tickers
  const feeCount = Math.min(6, Math.floor(interpolate(frame, [50, 200], [0, 6], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })));

  const camY = interpolate(frame, [0, durationInFrames], [15, -15], { easing: Easing.inOut(Easing.ease) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1E4640', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(105deg, #2A5850 0%, #408175 ${48 + Math.sin(frame * 0.01) * 6}%, #142F2A 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 640, height: 640, borderRadius: '50%',
        border: '2px solid rgba(235,237,227,0.05)', left: 200, top: -280,
        transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 480, height: 480, borderRadius: 40,
        background: 'rgba(255,244,73,0.03)', right: -120, bottom: -160,
        transform: `rotate(${-frame * 0.05}deg)`,
      }} />

      {/* Motion texture: horizontal ledger lines */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(0deg, rgba(235,237,227,0.03) 0px, rgba(235,237,227,0.03) 1px, transparent 1px, transparent 64px)',
        transform: `translateY(${(frame * 0.2) % 64}px)`,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 31) * 1920 + Math.sin(frame * 0.02 + i * 3) * 22,
          top: (seededRandom(i + 90) * 1080 + frame * 0.2) % 1080,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: 'rgba(235,237,227,0.2)',
        }} />
      ))}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `translateY(${camY}px)`, flexDirection: 'row' }}>

        {/* LEFT: fee stack = the business model */}
        <div style={{ width: '50%', display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center' }}>
          <div style={{
            fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 78, color: '#EBEDE3',
            opacity: sp(10), marginBottom: 40, textAlign: 'center', lineHeight: 1.05,
          }}>
            FEES ARE THE<br /><span style={{ color: '#FFF449' }}>BUSINESS MODEL</span>
          </div>
          {/* Receipt-style fee stack */}
          <div style={{ width: 520 }}>
            {['LATE FEE .............. $34', 'ROLLOVER FEE .......... $45', 'OVERDRAFT ............. $35', 'LATE FEE .............. $34', 'ROLLOVER FEE .......... $45', 'OVERDRAFT ............. $35'].map((t, i) => (
              <div key={i} style={{
                fontFamily: "'JetBrains Mono', monospace", fontSize: 32, color: i % 2 ? '#EBEDE3' : '#FFF449',
                padding: '10px 24px', borderBottom: '1px dashed rgba(235,237,227,0.25)',
                background: 'rgba(20,47,42,0.35)',
                opacity: i < feeCount ? 1 : 0,
                transform: `translateX(${i < feeCount ? 0 : -60}px)`,
                transition: 'none',
              }}>
                {t}
              </div>
            ))}
          </div>
        </div>

        {/* Divider sweep */}
        <div style={{
          position: 'absolute', left: '50%', top: 0, width: 6, height: 1080 * divider,
          background: 'linear-gradient(#FFF449, rgba(255,244,73,0.2))', transform: 'translateX(-3px)',
        }} />

        {/* RIGHT: healthcare enters — EKG + cross */}
        <div style={{ width: '50%', display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center', opacity: interpolate(frame, [250, 290], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          {/* Medical cross icon */}
          <svg width="140" height="140" viewBox="0 0 140 140" style={{ transform: `scale(${sp(265)})` }}>
            <rect x="52" y="14" width="36" height="112" rx="8" fill="#EBEDE3" />
            <rect x="14" y="52" width="112" height="36" rx="8" fill="#EBEDE3" />
          </svg>
          <div style={{
            fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 78, color: '#EBEDE3',
            marginTop: 30, textAlign: 'center', lineHeight: 1.05, opacity: sp(285),
          }}>
            THEN <span style={{ color: '#FFF449' }}>HEALTHCARE</span><br />ENTERS THE CRISIS
          </div>
          {/* EKG heartbeat line drawing across */}
          <svg width="700" height="140" viewBox="0 0 700 140" style={{ marginTop: 30 }}>
            <path
              d="M0 70 L160 70 L200 70 L225 20 L255 120 L285 45 L310 70 L480 70 L700 70"
              fill="none" stroke="#FFF449" strokeWidth="6" strokeLinecap="round" strokeLinejoin="round"
              strokeDasharray={900} strokeDashoffset={900 - ekg * 900}
            />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(10,25,22,0.55) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene36;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene37: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.07], { easing: Easing.inOut(Easing.ease) });

  // Safety net fade + crack
  const netFade = interpolate(frame, [300, 400], [0.55, 0.08], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const crack = interpolate(frame, [330, 380], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1E4640', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(circle at ${50 + Math.cos(frame * 0.008) * 12}% 75%, #386E63 0%, #26534B 45%, #12302B 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 450, borderRadius: '50% 50% 0 0',
        border: '2px solid rgba(235,237,227,0.05)', borderBottom: 'none',
        left: 510, bottom: -60, transform: `scale(${1 + Math.sin(frame * 0.02) * 0.03})`,
      }} />
      <div style={{
        position: 'absolute', width: 380, height: 380, borderRadius: '50%',
        background: 'rgba(255,244,73,0.035)', left: -110, top: -110,
        transform: `translateY(${Math.sin(frame * 0.02) * 20}px)`,
      }} />

      {/* Motion texture: net mesh (fading safety net) */}
      <div style={{
        position: 'absolute', left: 0, right: 0, bottom: 0, height: 340, opacity: netFade,
        backgroundImage: 'repeating-linear-gradient(45deg, rgba(235,237,227,0.16) 0px, rgba(235,237,227,0.16) 2px, transparent 2px, transparent 46px), repeating-linear-gradient(-45deg, rgba(235,237,227,0.16) 0px, rgba(235,237,227,0.16) 2px, transparent 2px, transparent 46px)',
      }} />
      {/* Net tear */}
      <svg width="1920" height="340" viewBox="0 0 1920 340" style={{ position: 'absolute', bottom: 0 }}>
        <path
          d={`M960 0 L${960 - crack * 180} ${crack * 150} L${960 + crack * 60} ${crack * 220} L${960 - crack * 90} ${crack * 340}`}
          fill="none" stroke="#FFF449" strokeWidth={crack > 0 ? 5 : 0} strokeLinecap="round" opacity={crack * 0.9}
        />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 41) * 1920,
          top: (seededRandom(i + 100) * 1080 + frame * (0.25 + seededRandom(i) * 0.3)) % 1080,
          width: 3, height: 3, borderRadius: '50%',
          backgroundColor: i % 4 === 0 ? 'rgba(255,244,73,0.3)' : 'rgba(235,237,227,0.18)',
        }} />
      ))}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, justifyContent: 'center', alignItems: 'center' }}>

        <div style={{
          fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 92, color: '#EBEDE3',
          opacity: sp(10), transform: `translateY(${(1 - sp(10)) * -40}px)`, textAlign: 'center', lineHeight: 1.05,
        }}>
          NO BUDGET CAN <span style={{ color: '#FFF449' }}>PREDICT THIS</span>
        </div>

        {/* Three event cards */}
        <div style={{ display: 'flex', gap: 50, marginTop: 70 }}>
          {[
            {
              label: 'CANCER DIAGNOSIS', delay: 60,
              icon: (
                <svg width="90" height="90" viewBox="0 0 90 90">
                  <circle cx="45" cy="45" r="30" fill="none" stroke="#EBEDE3" strokeWidth="6" />
                  <line x1="45" y1="5" x2="45" y2="20" stroke="#EBEDE3" strokeWidth="6" strokeLinecap="round" />
                  <line x1="45" y1="70" x2="45" y2="85" stroke="#EBEDE3" strokeWidth="6" strokeLinecap="round" />
                  <line x1="5" y1="45" x2="20" y2="45" stroke="#EBEDE3" strokeWidth="6" strokeLinecap="round" />
                  <line x1="70" y1="45" x2="85" y2="45" stroke="#EBEDE3" strokeWidth="6" strokeLinecap="round" />
                  <circle cx="45" cy="45" r="10" fill="#FFF449" />
                </svg>
              ),
            },
            {
              label: 'CAR ACCIDENT', delay: 85,
              icon: (
                <svg width="110" height="90" viewBox="0 0 110 90">
                  <path d="M15 62 L25 38 Q28 30 36 30 L74 30 Q82 30 85 38 L95 62" fill="none" stroke="#EBEDE3" strokeWidth="6" strokeLinecap="round" />
                  <rect x="10" y="60" width="90" height="16" rx="8" fill="none" stroke="#EBEDE3" strokeWidth="6" />
                  <path d="M88 12 L78 26 L92 24 L82 40" fill="none" stroke="#FFF449" strokeWidth="6" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
              ),
            },
            {
              label: 'PREMATURE BIRTH', delay: 110,
              icon: (
                <svg width="90" height="90" viewBox="0 0 90 90">
                  <path d="M45 78 C20 60 8 42 16 28 C22 16 38 16 45 28 C52 16 68 16 74 28 C82 42 70 60 45 78 Z" fill="none" stroke="#EBEDE3" strokeWidth="6" strokeLinejoin="round" />
                  <path d="M28 46 L38 46 L43 34 L49 56 L54 46 L62 46" fill="none" stroke="#FFF449" strokeWidth="5" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
              ),
            },
          ].map((c, i) => {
            const p = sp(c.delay, { damping: 10 });
            return (
              <div key={i} style={{
                width: 380, padding: '46px 30px', borderRadius: 22,
                background: 'rgba(18,48,43,0.55)', border: '2px solid rgba(235,237,227,0.18)',
                display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 28,
                opacity: p, transform: `translateY(${(1 - p) * 100}px) scale(${0.9 + p * 0.1})`,
                boxShadow: '0 26px 60px rgba(10,25,22,0.45)',
              }}>
                <div style={{ transform: `translateY(${Math.sin(frame * 0.06 + i * 2) * 6}px)` }}>{c.icon}</div>
                <div style={{ fontFamily: 'Inter, Poppins, sans-serif', fontSize: 34, fontWeight: 800, color: '#EBEDE3', letterSpacing: 2 }}>
                  {c.label}
                </div>
              </div>
            );
          })}
        </div>

        <div style={{
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 42, fontWeight: 700, color: '#FFF449',
          marginTop: 65, opacity: interpolate(frame, [310, 350], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          letterSpacing: 2,
        }}>
          AND THE SAFETY NET IS SHRINKING
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(10,25,22,0.55) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene37;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const NODES = ['INFLATION', 'HOUSING', 'MEDICAL DEBT', 'BNPL', 'STUDENT LOANS'];

const Scene38: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const cx = 700;
  const cy = 560;
  const orbit = frame * 0.15;
  const gearRot = frame * 0.6;

  // Spoke connection draw
  const spokeDraw = interpolate(frame, [140, 220], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1], { easing: Easing.inOut(Easing.ease) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1E4640', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `linear-gradient(${320 + Math.sin(frame * 0.009) * 15}deg, #142F2A 0%, #2C5B52 50%, #408175 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 1000, height: 1000, borderRadius: '50%',
        border: '1px solid rgba(235,237,227,0.05)', left: cx - 500, top: cy - 500,
        transform: `rotate(${orbit}deg) scale(${1 + Math.sin(frame * 0.015) * 0.03})`,
      }} />
      <div style={{
        position: 'absolute', width: 340, height: 340,
        background: 'rgba(255,244,73,0.04)', right: -90, top: -90,
        transform: `rotate(${45 + frame * 0.08}deg)`,
      }} />

      {/* Motion texture: hex-ish dot field */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(rgba(235,237,227,0.06) 2px, transparent 2px)',
        backgroundSize: '80px 80px',
        transform: `translate(${(frame * 0.1) % 80}px, ${(frame * 0.05) % 80}px)`,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 51) * 1920 + Math.cos(frame * 0.02 + i) * 20,
          top: (seededRandom(i + 110) * 1080 - frame * 0.3 + 2160) % 1080,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: i % 3 === 0 ? 'rgba(255,244,73,0.3)' : 'rgba(235,237,227,0.2)',
        }} />
      ))}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>

        {/* Headline */}
        <div style={{
          position: 'absolute', top: 90, left: 0, right: 0, textAlign: 'center',
          fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 88, color: '#EBEDE3',
          opacity: sp(10), transform: `translateY(${(1 - sp(10)) * -50}px)`,
        }}>
          NOT FIVE PROBLEMS. <span style={{ color: '#FFF449' }}>ONE MACHINE.</span>
        </div>

        {/* Hub-and-spoke system */}
        <svg width="1920" height="1080" viewBox="0 0 1920 1080" style={{ position: 'absolute' }}>
          {/* Spokes */}
          {NODES.map((_, i) => {
            const a = ((i / 5) * Math.PI * 2 - Math.PI / 2) + (orbit * Math.PI) / 180;
            const nx = cx + Math.cos(a) * 360;
            const ny = cy + Math.sin(a) * 300;
            return (
              <line key={i}
                x1={cx} y1={cy}
                x2={cx + (nx - cx) * spokeDraw} y2={cy + (ny - cy) * spokeDraw}
                stroke="rgba(255,244,73,0.5)" strokeWidth="3" strokeDasharray="10 8"
                strokeDashoffset={-frame * 0.8}
              />
            );
          })}
          {/* Center gear */}
          <g transform={`translate(${cx} ${cy}) rotate(${gearRot}) scale(${sp(30)})`}>
            {Array.from({ length: 8 }).map((_, i) => (
              <rect key={i} x="-14" y="-118" width="28" height="34" rx="6" fill="#FFF449"
                transform={`rotate(${i * 45})`} />
            ))}
            <circle r="96" fill="#12302B" stroke="#FFF449" strokeWidth="8" />
            <circle r="34" fill="none" stroke="#EBEDE3" strokeWidth="6" />
          </g>
          <text x={cx} y={cy + 160} fill="#EBEDE3" fontFamily="Inter, sans-serif" fontSize="30" fontWeight="800"
            textAnchor="middle" letterSpacing="4" opacity={sp(50)}>
            THE EXTRACTION MACHINE
          </text>
        </svg>

        {/* Orbiting entry-point nodes (HTML for crisp text) */}
        {NODES.map((label, i) => {
          const a = ((i / 5) * Math.PI * 2 - Math.PI / 2) + (orbit * Math.PI) / 180;
          const nx = cx + Math.cos(a) * 360;
          const ny = cy + Math.sin(a) * 300;
          const p = sp(70 + i * 18, { damping: 10 });
          return (
            <div key={i} style={{
              position: 'absolute', left: nx, top: ny,
              transform: `translate(-50%, -50%) scale(${p})`, opacity: p,
              background: 'rgba(18,48,43,0.85)', border: '2px solid #EBEDE3',
              borderRadius: 14, padding: '16px 28px',
              fontFamily: 'Inter, Poppins, sans-serif', fontSize: 30, fontWeight: 800,
              color: '#EBEDE3', letterSpacing: 2, whiteSpace: 'nowrap',
              boxShadow: '0 14px 40px rgba(10,25,22,0.5)',
            }}>
              {label}
            </div>
          );
        })}

        {/* Right-side copy */}
        <div style={{ position: 'absolute', right: 120, top: 420, width: 520 }}>
          <div style={{
            fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 66, lineHeight: 1.1, color: '#FFF449',
            opacity: sp(200), transform: `translateX(${(1 - sp(200)) * 70}px)`,
          }}>
            FIVE ENTRY POINTS.
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins, sans-serif', fontSize: 40, fontWeight: 600, color: '#EBEDE3',
            marginTop: 24, lineHeight: 1.4, opacity: sp(225),
          }}>
            Every door leads into the same interconnected system.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(10,25,22,0.55) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene38;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene39: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Diverging K path draw
  const draw = interpolate(frame, [60, 210], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06], { easing: Easing.inOut(Easing.ease) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1E4640', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background: split mood — brighter top, darker bottom */}
      <AbsoluteFill style={{
        background: `linear-gradient(180deg, #408175 0%, #29564E ${50 + Math.sin(frame * 0.01) * 5}%, #0F2823 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 560, height: 560, borderRadius: '50%',
        border: '2px solid rgba(255,244,73,0.06)', right: -170, top: -190,
        transform: `rotate(${frame * 0.09}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 700, height: 700, borderRadius: 50,
        background: 'rgba(235,237,227,0.03)', left: -260, bottom: -300,
        transform: `rotate(${-8 + Math.sin(frame * 0.012) * 3}deg)`,
      }} />

      {/* Motion texture: fine chart grid */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(rgba(235,237,227,0.04) 1px, transparent 1px), linear-gradient(90deg, rgba(235,237,227,0.04) 1px, transparent 1px)',
        backgroundSize: '96px 96px',
        transform: `translateX(${-(frame * 0.18) % 96}px)`,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 13 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: (seededRandom(i + 61) * 1920 + frame * 0.2) % 1920,
          top: seededRandom(i + 120) * 1080 + Math.sin(frame * 0.03 + i * 1.5) * 16,
          width: 3, height: 3, borderRadius: '50%',
          backgroundColor: i % 4 === 0 ? 'rgba(255,244,73,0.35)' : 'rgba(235,237,227,0.18)',
        }} />
      ))}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: 90 }}>

        {/* Diverging K chart */}
        <div style={{ opacity: sp(25) }}>
          <svg width="760" height="640" viewBox="0 0 760 640">
            <line x1="70" y1="320" x2="720" y2="320" stroke="rgba(235,237,227,0.25)" strokeWidth="2" strokeDasharray="8 8" />
            <text x="78" y="306" fill="#EBEDE3" opacity="0.6" fontFamily="JetBrains Mono, monospace" fontSize="24">SAME STARTING POINT</text>

            {/* Upper arm: asset holders */}
            <path d="M70 320 L230 300 L370 240 L520 150 L690 60"
              fill="none" stroke="#FFF449" strokeWidth="10" strokeLinecap="round" strokeLinejoin="round"
              strokeDasharray={760} strokeDashoffset={760 - draw * 760} />
            {/* Lower arm: wage earners */}
            <path d="M70 320 L230 345 L370 400 L520 490 L690 580"
              fill="none" stroke="#EBEDE3" strokeWidth="10" strokeLinecap="round" strokeLinejoin="round"
              strokeDasharray={760} strokeDashoffset={760 - draw * 760} strokeOpacity="0.85" />

            {/* Endpoint markers + micro pulse */}
            {draw > 0.97 && (
              <>
                <circle cx="690" cy="60" r={9 + Math.sin(frame * 0.25) * 3} fill="#FFF449" />
                <circle cx="690" cy="580" r={9 + Math.sin(frame * 0.25 + 1.5) * 3} fill="#EBEDE3" />
              </>
            )}
          </svg>
          {/* Arm labels */}
          <div style={{
            position: 'absolute', marginTop: -620, marginLeft: 430, opacity: sp(200),
            fontFamily: 'Inter, sans-serif', fontSize: 32, fontWeight: 800, color: '#FFF449', letterSpacing: 1,
          }}>
            ASSET HOLDERS
          </div>
          <div style={{
            position: 'absolute', marginTop: -95, marginLeft: 430, opacity: sp(215),
            fontFamily: 'Inter, sans-serif', fontSize: 32, fontWeight: 800, color: '#EBEDE3', letterSpacing: 1,
          }}>
            WAGE EARNERS
          </div>
        </div>

        {/* Typography */}
        <div style={{ width: 640 }}>
          <div style={{
            fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 104, lineHeight: 1.02, color: '#EBEDE3',
            opacity: sp(45), transform: `translateY(${(1 - sp(45)) * 50}px)`,
          }}>
            SAME
            <br />
            INFLATION.
          </div>
          <div style={{
            fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 104, lineHeight: 1.02, color: '#FFF449',
            opacity: sp(80), transform: `translateY(${(1 - sp(80)) * 50}px)`, marginTop: 8,
          }}>
            DIFFERENT
            <br />
            OUTCOME.
          </div>
          <div style={{
            width: interpolate(frame, [120, 155], [0, 500], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            height: 5, backgroundColor: '#EBEDE3', opacity: 0.5, margin: '30px 0', borderRadius: 3,
          }} />
          <div style={{
            fontFamily: 'Inter, Poppins, sans-serif', fontSize: 40, fontWeight: 600, color: '#EBEDE3',
            lineHeight: 1.45, opacity: sp(160),
          }}>
            Higher incomes absorb the shock through wage and asset growth. Everyone else absorbs the damage.
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(10,25,22,0.55) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene39;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40',
  durationInSeconds: 24.64,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene40: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Salary counter: 58,000 -> 42,000 (taxes slicing down)
  const salary = Math.round(interpolate(frame, [180, 300], [58000, 42000], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  }));

  // Deduction bars slice in
  const deductions = [
    { label: 'FEDERAL TAX', delay: 190 },
    { label: 'STATE TAX', delay: 220 },
    { label: 'FICA', delay: 250 },
  ];

  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1], { easing: Easing.out(Easing.ease) });
  const camPan = interpolate(frame, [0, durationInFrames], [12, -12], { easing: Easing.inOut(Easing.ease) });

  // Salary bar shrink
  const barW = interpolate(frame, [180, 300], [740, 740 * (42 / 58)], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#1E4640', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient + Shape Layers + Texture + Particles + Content + Vignette + Watermark */}

      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: `radial-gradient(ellipse at ${70 + Math.sin(frame * 0.01) * 8}% 25%, #3A7166 0%, #26534B 50%, #102B26 100%)`,
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 820, height: 820, borderRadius: '50%',
        border: '2px solid rgba(235,237,227,0.05)', right: -300, top: -320,
        transform: `rotate(${frame * 0.07}deg) scale(${1 + Math.sin(frame * 0.018) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 460, height: 460,
        background: 'rgba(255,244,73,0.03)', left: -160, bottom: -170,
        transform: `rotate(${30 - frame * 0.05}deg)`,
      }} />

      {/* Motion texture: city skyline silhouette bars */}
      <div style={{ position: 'absolute', bottom: 0, left: 0, right: 0, display: 'flex', alignItems: 'flex-end', opacity: 0.1 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <div key={i} style={{
            flex: 1, marginRight: 4,
            height: 60 + seededRandom(i + 200) * 220 + Math.sin(frame * 0.015 + i) * 8,
            backgroundColor: '#EBEDE3',
          }} />
        ))}
      </div>

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 71) * 1920 + Math.sin(frame * 0.025 + i * 2) * 20,
          top: (seededRandom(i + 130) * 1080 - frame * 0.25 + 2160) % 1080,
          width: 3, height: 3, borderRadius: '50%',
          backgroundColor: i % 3 === 0 ? 'rgba(255,244,73,0.3)' : 'rgba(235,237,227,0.2)',
        }} />
      ))}

      {/* Main content with spring animations */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camPan}px)`, justifyContent: 'center', alignItems: 'center' }}>

        {/* Case-file header */}
        <div style={{
          fontFamily: "'JetBrains Mono', monospace", fontSize: 34, color: '#FFF449', letterSpacing: 8,
          opacity: sp(10), marginBottom: 20,
        }}>
          CASE STUDY // CHICAGO
        </div>

        {/* Profile spec line */}
        <div style={{ display: 'flex', gap: 40, opacity: sp(35), transform: `translateY(${(1 - sp(35)) * 40}px)` }}>
          {['AGE 28', "MASTER'S DEGREE", 'HEALTHCARE ADMIN'].map((t, i) => (
            <div key={i} style={{
              fontFamily: 'Inter, Poppins, sans-serif', fontSize: 34, fontWeight: 700, color: '#EBEDE3',
              border: '2px solid rgba(235,237,227,0.3)', borderRadius: 100, padding: '12px 34px',
              background: 'rgba(18,48,43,0.5)',
            }}>
              {t}
            </div>
          ))}
        </div>

        {/* Hero salary counter */}
        <div style={{
          fontFamily: "'Mont Heavy', 'Nexa Bold', sans-serif", fontSize: 190, lineHeight: 1,
          color: frame > 180 ? '#FFF449' : '#EBEDE3', marginTop: 50,
          opacity: sp(80), transform: `scale(${0.85 + sp(80) * 0.15})`,
          textShadow: '0 20px 70px rgba(0,0,0,0.4)',
        }}>
          ${salary.toLocaleString('en-US')}
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 36, fontWeight: 700, color: '#EBEDE3',
          letterSpacing: 4, marginTop: 8, opacity: sp(100), textTransform: 'uppercase',
        }}>
          {frame > 240 ? 'Take-Home Pay' : 'Gross Salary'}
        </div>

        {/* Shrinking salary bar + deduction slicers */}
        <div style={{ position: 'relative', width: 740, height: 46, marginTop: 46, opacity: sp(140) }}>
          <div style={{ position: 'absolute', width: 740, height: 46, borderRadius: 10, border: '2px dashed rgba(235,237,227,0.25)' }} />
          <div style={{
            position: 'absolute', width: barW, height: 46, borderRadius: 10,
            background: 'linear-gradient(90deg, #FFF449, rgba(255,244,73,0.65))',
            boxShadow: '0 0 44px rgba(255,244,73,0.25)',
          }} />
        </div>
        <div style={{ display: 'flex', gap: 26, marginTop: 30 }}>
          {deductions.map((d, i) => {
            const p = sp(d.delay, { damping: 9 });
            return (
              <div key={i} style={{
                fontFamily: "'JetBrains Mono', monospace", fontSize: 28, color: '#12302B', fontWeight: 700,
                backgroundColor: '#EBEDE3', borderRadius: 8, padding: '10px 24px',
                opacity: p, transform: `translateY(${(1 - p) * -50}px) rotate(${(1 - p) * -6}deg)`,
              }}>
                − {d.label}
              </div>
            );
          })}
        </div>

        <div style={{
          fontFamily: 'Inter, Poppins, sans-serif', fontSize: 38, fontWeight: 600, color: '#EBEDE3',
          marginTop: 44, opacity: sp(320), letterSpacing: 1,
        }}>
          Before rent. Before groceries. Before anything goes wrong.
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, transparent 55%, rgba(10,25,22,0.58) 100%)' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif',
        fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3,
      }}>
        KuxAI.7x
      </div>
    </AbsoluteFill>
  );
};

export default Scene40;
```

