```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31_Section504Intro',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene31_Section504Intro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(10);
  const badgeIn = sp(25);
  const houseIn = sp(40);
  const crackIn = interpolate(frame, [80, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const priceIn = sp(150);
  const strikeIn = interpolate(frame, [200, 240], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const stampIn = sp(260, { damping: 8, stiffness: 180 });
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 20%, rgba(250,185,91,0.18) 0%, rgba(65,45,21,0) 55%), radial-gradient(circle at 80% 90%, rgba(232,237,242,0.06) 0%, rgba(65,45,21,0) 60%)',
      }} />

      {/* Shape layers — drifting geometric forms */}
      <div style={{
        position: 'absolute', width: 800, height: 800, borderRadius: '50%',
        border: '2px solid #FAB95B', opacity: 0.05,
        top: -200, left: -150,
        transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500,
        border: '2px solid #E8EDF2', opacity: 0.04,
        top: 600, right: -100,
        transform: `rotate(${-frame * 0.2 + 45}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 0, height: 0, opacity: 0.06,
        borderLeft: '180px solid transparent',
        borderRight: '180px solid transparent',
        borderBottom: '320px solid #FAB95B',
        top: 200, right: 200,
        transform: `rotate(${Math.sin(frame * 0.015) * 8}deg)`,
      }} />

      {/* Motion texture — grid lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <line key={`v${i}`} x1={i * 96} y1={0} x2={i * 96} y2={1080} stroke="#FAB95B" strokeWidth="0.5" />
        ))}
        {Array.from({ length: 12 }).map((_, i) => (
          <line key={`h${i}`} x1={0} y1={i * 90} x2={1920} y2={i * 90} stroke="#FAB95B" strokeWidth="0.5" />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const sx = seededRandom(i + 1) * 1920;
        const sy = seededRandom(i + 50) * 1080;
        const size = 2 + seededRandom(i + 100) * 4;
        const drift = Math.sin(frame * 0.02 + i) * 30;
        return (
          <div key={i} style={{
            position: 'absolute', left: sx + drift, top: sy + Math.cos(frame * 0.03 + i) * 20,
            width: size, height: size, borderRadius: '50%',
            backgroundColor: '#FAB95B', opacity: 0.4,
          }} />
        );
      })}

      {/* Main content wrapper with camera zoom */}
      <div style={{
        position: 'absolute', width: '100%', height: '100%',
        transform: `scale(${camZoom})`,
      }}>

        {/* Top badge — PROGRAM #5 */}
        <div style={{
          position: 'absolute', top: 70, left: '50%',
          transform: `translateX(-50%) scale(${badgeIn})`,
          display: 'flex', alignItems: 'center', gap: 16,
          padding: '14px 32px',
          backgroundColor: 'rgba(232,237,242,0.08)',
          border: '2px solid #FAB95B', borderRadius: 50,
        }}>
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
            <path d="M3 12L12 3L21 12V21H14V14H10V21H3V12Z" stroke="#FAB95B" strokeWidth="2.2" strokeLinejoin="round"/>
          </svg>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 30, fontWeight: 700,
            color: '#FAB95B', letterSpacing: 4,
          }}>PROGRAM #5 — UNLOCKED</div>
        </div>

        {/* Main headline */}
        <div style={{
          position: 'absolute', top: 160, left: 100,
          opacity: titleIn,
          transform: `translateY(${(1 - titleIn) * 40}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 60, fontWeight: 500,
            color: '#E8EDF2', letterSpacing: 2, marginBottom: 12,
          }}>USDA SECTION 504</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 132, fontWeight: 900,
            color: '#E8EDF2', lineHeight: 1.0, letterSpacing: -2,
          }}>HOME REPAIR</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 132, fontWeight: 900,
            color: '#FAB95B', lineHeight: 1.0, letterSpacing: -2,
          }}>PROGRAM</div>
        </div>

        {/* House SVG — middle right, with cracks */}
        <div style={{
          position: 'absolute', right: 140, top: 320,
          transform: `scale(${houseIn})`, transformOrigin: 'center',
        }}>
          <svg width="480" height="420" viewBox="0 0 480 420">
            {/* roof — leaking */}
            <path d="M60 200 L240 60 L420 200 L420 220 L60 220 Z" fill="#2D2D2D" stroke="#FAB95B" strokeWidth="3"/>
            {/* roof crack */}
            <path d="M200 90 L220 150 L210 200" stroke="#ff0000" strokeWidth={3 * crackIn} fill="none" strokeDasharray="6 4"/>
            <path d="M260 100 L275 160" stroke="#ff0000" strokeWidth={3 * crackIn} fill="none" strokeDasharray="6 4"/>

            {/* body */}
            <rect x="90" y="220" width="300" height="180" fill="#1C1C1C" stroke="#E8EDF2" strokeWidth="3"/>

            {/* wall crack */}
            <path d="M150 230 L160 290 L145 360" stroke="#ff0000" strokeWidth={2.5 * crackIn} fill="none"/>
            <path d="M340 240 L350 320" stroke="#ff0000" strokeWidth={2.5 * crackIn} fill="none"/>

            {/* door */}
            <rect x="210" y="290" width="60" height="110" fill="#412D15" stroke="#FAB95B" strokeWidth="2.5"/>
            <circle cx="258" cy="345" r="3" fill="#FAB95B"/>

            {/* windows */}
            <rect x="120" y="250" width="50" height="50" fill="#FAB95B" opacity="0.4" stroke="#E8EDF2" strokeWidth="2"/>
            <rect x="310" y="250" width="50" height="50" fill="#FAB95B" opacity="0.4" stroke="#E8EDF2" strokeWidth="2"/>

            {/* leaking drops */}
            {[0, 1, 2].map((i) => {
              const dropY = ((frame * 1.5 + i * 30) % 80) + 220;
              return (
                <ellipse key={i} cx={200 + i * 30} cy={dropY} rx="3" ry="6" fill="#4FC3F7" opacity={crackIn * 0.8}/>
              );
            })}

            {/* warning bubble */}
            <g transform={`translate(380, 180) scale(${crackIn})`}>
              <circle cx="0" cy="0" r="28" fill="#ff0000"/>
              <text x="0" y="9" textAnchor="middle" fontFamily="Inter" fontSize="34" fontWeight="900" fill="#E8EDF2">!</text>
            </g>
          </svg>

          {/* House label */}
          <div style={{
            textAlign: 'center', marginTop: 10, opacity: crackIn,
            fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 600,
            color: '#E8EDF2', letterSpacing: 3,
          }}>HOUSE POOR</div>
        </div>

        {/* Price tag — left side */}
        <div style={{
          position: 'absolute', left: 100, top: 640,
          opacity: priceIn,
          transform: `translateY(${(1 - priceIn) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 500,
            color: '#E8EDF2', letterSpacing: 2, marginBottom: 6, opacity: 0.7,
          }}>REPAIR COST</div>
          <div style={{ display: 'flex', alignItems: 'baseline', gap: 4, position: 'relative' }}>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 180, fontWeight: 900,
              color: '#ff0000', lineHeight: 1, letterSpacing: -4,
            }}>$10,000</div>
            {/* strikethrough */}
            <div style={{
              position: 'absolute', left: -10, top: '50%',
              width: `${strikeIn * 540}px`, height: 8,
              backgroundColor: '#FAB95B', transform: 'rotate(-6deg)',
            }} />
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 600,
            color: '#FAB95B', letterSpacing: 1, marginTop: 12,
          }}>can't afford → live with the damage</div>
        </div>

        {/* Issue chips — bottom right */}
        <div style={{
          position: 'absolute', right: 100, top: 800,
          display: 'flex', flexDirection: 'column', gap: 14,
          opacity: priceIn,
        }}>
          {[
            { label: 'LEAKING ROOF', delay: 0 },
            { label: 'FAILING PLUMBING', delay: 15 },
            { label: 'STEEP STAIRS', delay: 30 },
          ].map((item, i) => {
            const chipIn = sp(170 + item.delay);
            return (
              <div key={i} style={{
                padding: '12px 26px',
                backgroundColor: 'rgba(255,0,0,0.12)',
                border: '2px solid #ff0000',
                borderRadius: 12,
                fontFamily: 'Inter, Poppins', fontSize: 30, fontWeight: 700,
                color: '#E8EDF2', letterSpacing: 2,
                transform: `translateX(${(1 - chipIn) * 80}px)`,
                opacity: chipIn,
              }}>{item.label}</div>
            );
          })}
        </div>

        {/* LIFESAVER stamp — diagonal */}
        <div style={{
          position: 'absolute', left: 760, top: 380,
          transform: `rotate(-12deg) scale(${stampIn})`,
          opacity: stampIn,
        }}>
          <div style={{
            padding: '20px 40px',
            border: '5px solid #FAB95B', borderRadius: 14,
            backgroundColor: 'rgba(65,45,21,0.85)',
            fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 900,
            color: '#FAB95B', letterSpacing: 6,
          }}>ULTIMATE LIFESAVER</div>
        </div>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene31_Section504Intro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32_GrantNotLoan',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32_GrantNotLoan: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headIn = sp(8);
  const vsIn = sp(35);
  const leftCardIn = sp(60);
  const rightCardIn = sp(90);
  const stampIn = sp(160, { damping: 8 });
  const eligIn = sp(220);

  const camPan = interpolate(frame, [0, durationInFrames], [-20, 20]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(250,185,91,0.10) 0%, rgba(65,45,21,0) 50%, rgba(232,237,242,0.05) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 700, height: 700,
        border: '2px solid #FAB95B', opacity: 0.05,
        top: 200, left: -200,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 600, height: 600, borderRadius: '50%',
        border: '2px solid #E8EDF2', opacity: 0.04,
        bottom: -200, right: -150,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 40 }).map((_, i) => (
          <line key={i} x1={-200 + i * 80} y1={0} x2={i * 80} y2={1080} stroke="#FAB95B" strokeWidth="0.6" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const sx = seededRandom(i + 7) * 1920;
        const sy = seededRandom(i + 77) * 1080;
        const drift = Math.sin(frame * 0.025 + i) * 25;
        return (
          <div key={i} style={{
            position: 'absolute', left: sx + drift, top: sy,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#FAB95B' : '#E8EDF2', opacity: 0.45,
          }} />
        );
      })}

      <div style={{ position: 'absolute', width: '100%', height: '100%', transform: `translateX(${camPan}px)` }}>

        {/* Headline */}
        <div style={{
          position: 'absolute', top: 90, left: 0, right: 0, textAlign: 'center',
          opacity: headIn, transform: `translateY(${(1 - headIn) * 30}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 500,
            color: '#FAB95B', letterSpacing: 6, marginBottom: 8,
          }}>THE BIG DIFFERENCE</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 100, fontWeight: 900,
            color: '#E8EDF2', lineHeight: 1.0, letterSpacing: -2,
          }}>Money You DON'T Pay Back</div>
        </div>

        {/* LOAN card — left */}
        <div style={{
          position: 'absolute', left: 130, top: 340,
          width: 680, height: 480,
          backgroundColor: 'rgba(255,0,0,0.08)',
          border: '3px solid #ff0000', borderRadius: 24,
          padding: 50,
          opacity: leftCardIn,
          transform: `translateX(${(1 - leftCardIn) * -80}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 600,
            color: '#ff0000', letterSpacing: 4, marginBottom: 18,
          }}>TYPICAL LOAN</div>

          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 130, fontWeight: 900,
            color: '#ff0000', lineHeight: 1, letterSpacing: -3, marginBottom: 30,
          }}>PAY BACK</div>

          <div style={{ display: 'flex', flexDirection: 'column', gap: 18 }}>
            {['+ interest', '+ monthly payments', '+ debt burden'].map((t, i) => (
              <div key={i} style={{
                display: 'flex', alignItems: 'center', gap: 16,
                fontFamily: 'Inter, Poppins', fontSize: 42, fontWeight: 600,
                color: '#E8EDF2',
              }}>
                <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
                  <path d="M6 6L18 18M18 6L6 18" stroke="#ff0000" strokeWidth="3" strokeLinecap="round"/>
                </svg>
                {t}
              </div>
            ))}
          </div>
        </div>

        {/* VS divider */}
        <div style={{
          position: 'absolute', left: '50%', top: 510,
          transform: `translateX(-50%) scale(${vsIn}) rotate(${Math.sin(frame * 0.06) * 8}deg)`,
          width: 140, height: 140, borderRadius: '50%',
          backgroundColor: '#FAB95B',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          fontFamily: 'Inter, Poppins', fontSize: 56, fontWeight: 900,
          color: '#412D15', letterSpacing: 2,
          boxShadow: '0 0 40px rgba(250,185,91,0.5)',
          zIndex: 5,
        }}>VS</div>

        {/* GRANT card — right */}
        <div style={{
          position: 'absolute', right: 130, top: 340,
          width: 680, height: 480,
          backgroundColor: 'rgba(250,185,91,0.14)',
          border: '3px solid #FAB95B', borderRadius: 24,
          padding: 50,
          opacity: rightCardIn,
          transform: `translateX(${(1 - rightCardIn) * 80}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 600,
            color: '#FAB95B', letterSpacing: 4, marginBottom: 18,
          }}>SECTION 504 GRANT</div>

          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 130, fontWeight: 900,
            color: '#FAB95B', lineHeight: 1, letterSpacing: -3, marginBottom: 30,
          }}>FREE MONEY</div>

          <div style={{ display: 'flex', flexDirection: 'column', gap: 18 }}>
            {['no payback', 'no interest', 'yours to keep'].map((t, i) => (
              <div key={i} style={{
                display: 'flex', alignItems: 'center', gap: 16,
                fontFamily: 'Inter, Poppins', fontSize: 42, fontWeight: 600,
                color: '#E8EDF2',
              }}>
                <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
                  <path d="M5 12L10 17L20 7" stroke="#FAB95B" strokeWidth="3.5" strokeLinecap="round" strokeLinejoin="round"/>
                </svg>
                {t}
              </div>
            ))}
          </div>
        </div>

        {/* Bottom — eligibility strip */}
        <div style={{
          position: 'absolute', bottom: 70, left: '50%',
          transform: `translateX(-50%) translateY(${(1 - eligIn) * 30}px)`,
          opacity: eligIn,
          display: 'flex', alignItems: 'center', gap: 24,
          padding: '20px 40px',
          backgroundColor: 'rgba(232,237,242,0.06)',
          border: '2px solid rgba(232,237,242,0.2)', borderRadius: 60,
        }}>
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none">
            <path d="M12 2L4 6V12C4 17 7.5 21 12 22C16.5 21 20 17 20 12V6L12 2Z" stroke="#FAB95B" strokeWidth="2.2" strokeLinejoin="round"/>
          </svg>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 700,
            color: '#E8EDF2', letterSpacing: 2,
          }}>FOR VERY LOW-INCOME SENIORS</div>
          <div style={{
            padding: '6px 16px', borderRadius: 20,
            backgroundColor: '#FAB95B',
            fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 800,
            color: '#412D15', letterSpacing: 2,
          }}>RURAL / TOWN EDGE</div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene32_GrantNotLoan;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33_10KGrant',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene33_10KGrant: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const labelIn = sp(8);
  const moneyIn = sp(20, { damping: 9, stiffness: 100 });
  const counterEnd = 10000;
  const counterProgress = interpolate(frame, [40, 110], [0, counterEnd], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });
  const counterValue = Math.floor(counterProgress);

  const card1In = sp(130);
  const card2In = sp(160);
  const card3In = sp(190);
  const plusIn = sp(260, { damping: 8 });
  const weatherIn = sp(290);

  const pulse = 1 + Math.sin(frame * 0.1) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient with golden glow */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 35%, rgba(250,185,91,0.22) 0%, rgba(65,45,21,0) 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        border: '2px solid #FAB95B', opacity: 0.05,
        top: '50%', left: '50%',
        transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.03) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 600, height: 600, borderRadius: '50%',
        border: '2px solid #FAB95B', opacity: 0.07,
        top: '50%', left: '50%',
        transform: `translate(-50%, -50%) rotate(${frame * 0.2}deg)`,
      }} />

      {/* Radial burst lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        {Array.from({ length: 24 }).map((_, i) => {
          const angle = (i / 24) * Math.PI * 2 + frame * 0.005;
          const x1 = 960 + Math.cos(angle) * 300;
          const y1 = 400 + Math.sin(angle) * 300;
          const x2 = 960 + Math.cos(angle) * 800;
          const y2 = 400 + Math.sin(angle) * 800;
          return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#FAB95B" strokeWidth="1"/>;
        })}
      </svg>

      {/* Particles — celebratory */}
      {Array.from({ length: 50 }).map((_, i) => {
        const sx = seededRandom(i + 3) * 1920;
        const sy = seededRandom(i + 88) * 1080;
        const drift = Math.sin(frame * 0.04 + i) * 40;
        const fall = (frame * 1.2 + i * 30) % 1100;
        return (
          <div key={i} style={{
            position: 'absolute', left: sx + drift, top: (sy + fall) % 1080,
            width: 4, height: 4,
            backgroundColor: i % 2 === 0 ? '#FAB95B' : '#E8EDF2', opacity: 0.5,
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 90, left: 0, right: 0, textAlign: 'center',
        opacity: labelIn, transform: `translateY(${(1 - labelIn) * 20}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          padding: '12px 30px',
          backgroundColor: 'rgba(250,185,91,0.15)',
          border: '2px solid #FAB95B', borderRadius: 50,
          fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 4,
        }}>YOU CAN GET UP TO</div>
      </div>

      {/* Hero money counter */}
      <div style={{
        position: 'absolute', top: 200, left: 0, right: 0, textAlign: 'center',
        transform: `scale(${moneyIn * pulse})`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 320, fontWeight: 900,
          color: '#FAB95B', lineHeight: 1, letterSpacing: -8,
          textShadow: '0 0 60px rgba(250,185,91,0.4)',
        }}>${counterValue.toLocaleString()}</div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 50, fontWeight: 600,
          color: '#E8EDF2', letterSpacing: 4, marginTop: -10,
        }}>IN GRANT MONEY · NO REPAYMENT</div>
      </div>

      {/* Three use-case cards */}
      <div style={{
        position: 'absolute', bottom: 220, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', gap: 36,
      }}>
        {[
          {
            icon: (
              <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
                <path d="M3 10L12 3L21 10V12H3V10Z" fill="#FAB95B"/>
                <rect x="5" y="12" width="14" height="9" fill="none" stroke="#E8EDF2" strokeWidth="2"/>
              </svg>
            ),
            label: 'NEW ROOF',
            anim: card1In,
          },
          {
            icon: (
              <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
                <rect x="4" y="6" width="16" height="12" rx="2" stroke="#FAB95B" strokeWidth="2.5"/>
                <path d="M9 11C9 13 12 13 12 11C12 9 15 9 15 11C15 13 12 13 12 15" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round"/>
              </svg>
            ),
            label: 'FIXED FURNACE',
            anim: card2In,
          },
          {
            icon: (
              <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
                <circle cx="9" cy="5" r="2" fill="#FAB95B"/>
                <path d="M9 8V14L13 18L9 21" stroke="#E8EDF2" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/>
                <circle cx="9" cy="18" r="4" stroke="#FAB95B" strokeWidth="2"/>
              </svg>
            ),
            label: 'WHEELCHAIR RAMPS',
            anim: card3In,
          },
        ].map((card, i) => (
          <div key={i} style={{
            width: 340, padding: '30px 28px',
            backgroundColor: 'rgba(232,237,242,0.06)',
            border: '2px solid #FAB95B', borderRadius: 18,
            opacity: card.anim,
            transform: `translateY(${(1 - card.anim) * 50}px)`,
            textAlign: 'center',
          }}>
            <div style={{ marginBottom: 14 }}>{card.icon}</div>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 800,
              color: '#E8EDF2', letterSpacing: 2,
            }}>{card.label}</div>
          </div>
        ))}
      </div>

      {/* COMBINE WITH strip — bottom */}
      <div style={{
        position: 'absolute', bottom: 70, left: '50%',
        transform: `translateX(-50%) scale(${weatherIn})`,
        display: 'flex', alignItems: 'center', gap: 24,
      }}>
        <div style={{
          width: 60, height: 60, borderRadius: '50%',
          backgroundColor: '#FAB95B',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 900,
          color: '#412D15',
          transform: `scale(${plusIn})`,
        }}>+</div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 40, fontWeight: 700,
          color: '#E8EDF2', letterSpacing: 3,
        }}>COMBINE WITH</div>
        <div style={{
          padding: '12px 28px',
          backgroundColor: 'rgba(250,185,91,0.18)',
          border: '2px solid #FAB95B', borderRadius: 14,
          fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 900,
          color: '#FAB95B', letterSpacing: 2,
        }}>WEATHERIZATION ASSISTANCE</div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene33_10KGrant;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34_WeatherizationServices',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene34_WeatherizationServices: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(8);
  const houseIn = sp(35);
  const s1 = sp(70);
  const s2 = sp(100);
  const s3 = sp(130);
  const billIn = sp(220);
  const ageIn = sp(280);

  const camTilt = interpolate(frame, [0, durationInFrames], [-1, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(250,185,91,0.08) 0%, rgba(65,45,21,0) 50%, rgba(250,185,91,0.10) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 0, height: 0, opacity: 0.05,
        borderLeft: '250px solid transparent',
        borderRight: '250px solid transparent',
        borderBottom: '430px solid #FAB95B',
        top: 100, left: 80,
        transform: `rotate(${Math.sin(frame * 0.015) * 6}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500, borderRadius: '50%',
        border: '2px solid #E8EDF2', opacity: 0.05,
        bottom: -100, right: -80,
      }} />

      {/* Crosshatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <line key={`a${i}`} x1={i * 64} y1={0} x2={i * 64 + 200} y2={1080} stroke="#FAB95B" strokeWidth="0.5" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const sx = seededRandom(i + 4) * 1920;
        const sy = (seededRandom(i + 99) * 1080 + frame * 0.8) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: sx, top: sy,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: '#FAB95B', opacity: 0.4,
          }} />
        );
      })}

      <div style={{ position: 'absolute', width: '100%', height: '100%', transform: `rotate(${camTilt * 0.3}deg)` }}>

        {/* Top title */}
        <div style={{
          position: 'absolute', top: 80, left: 0, right: 0, textAlign: 'center',
          opacity: titleIn, transform: `translateY(${(1 - titleIn) * 25}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 500,
            color: '#FAB95B', letterSpacing: 5, marginBottom: 6,
          }}>GOVERNMENT COMES TO YOUR HOUSE</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 110, fontWeight: 900,
            color: '#E8EDF2', lineHeight: 1, letterSpacing: -2,
          }}>FOR <span style={{ color: '#FAB95B' }}>FREE</span></div>
        </div>

        {/* Big house illustration — center */}
        <div style={{
          position: 'absolute', left: '50%', top: 360,
          transform: `translateX(-50%) scale(${houseIn})`,
        }}>
          <svg width="520" height="440" viewBox="0 0 520 440">
            {/* roof */}
            <path d="M50 220 L260 60 L470 220 L470 240 L50 240 Z" fill="#2D2D2D" stroke="#FAB95B" strokeWidth="3"/>
            {/* attic insulation lines */}
            {s2 > 0.3 && Array.from({ length: 6 }).map((_, i) => (
              <line key={i} x1={140 + i * 30} y1={140 + i * 12} x2={380 - i * 30} y2={140 + i * 12} stroke="#FAB95B" strokeWidth={2 * s2} opacity={0.6}/>
            ))}
            {/* body */}
            <rect x="90" y="240" width="340" height="180" fill="#1C1C1C" stroke="#E8EDF2" strokeWidth="3"/>
            {/* windows — sealed glow */}
            <rect x="130" y="280" width="80" height="80" fill="#FAB95B" opacity={0.3 + s1 * 0.4} stroke="#E8EDF2" strokeWidth="2.5"/>
            <rect x="310" y="280" width="80" height="80" fill="#FAB95B" opacity={0.3 + s1 * 0.4} stroke="#E8EDF2" strokeWidth="2.5"/>
            {/* seal glow */}
            {s1 > 0.5 && (
              <>
                <rect x="125" y="275" width="90" height="90" fill="none" stroke="#FAB95B" strokeWidth={3 * s1} opacity={s1 * 0.8} strokeDasharray="4 4"/>
                <rect x="305" y="275" width="90" height="90" fill="none" stroke="#FAB95B" strokeWidth={3 * s1} opacity={s1 * 0.8} strokeDasharray="4 4"/>
              </>
            )}
            {/* door */}
            <rect x="240" y="320" width="60" height="100" fill="#412D15" stroke="#FAB95B" strokeWidth="2.5"/>
            <circle cx="288" cy="375" r="3" fill="#FAB95B"/>
            {/* heater chimney with warm smoke */}
            <rect x="350" y="80" width="30" height="60" fill="#2D2D2D" stroke="#FAB95B" strokeWidth="2"/>
            {s3 > 0.3 && Array.from({ length: 3 }).map((_, i) => {
              const sy = 60 - ((frame * 0.6 + i * 15) % 40);
              return <circle key={i} cx={365} cy={sy} r={6 + i * 2} fill="#FAB95B" opacity={(0.5 - i * 0.15) * s3}/>;
            })}
          </svg>
        </div>

        {/* Three service callouts */}
        {/* Service 1 — Windows */}
        <div style={{
          position: 'absolute', left: 100, top: 420,
          opacity: s1, transform: `translateX(${(1 - s1) * -50}px)`,
        }}>
          <div style={{
            display: 'flex', alignItems: 'center', gap: 16,
            padding: '14px 24px',
            backgroundColor: 'rgba(250,185,91,0.12)',
            border: '2px solid #FAB95B', borderRadius: 14,
          }}>
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
              <rect x="3" y="3" width="18" height="18" stroke="#FAB95B" strokeWidth="2.5"/>
              <line x1="12" y1="3" x2="12" y2="21" stroke="#FAB95B" strokeWidth="2"/>
              <line x1="3" y1="12" x2="21" y2="12" stroke="#FAB95B" strokeWidth="2"/>
            </svg>
            <div>
              <div style={{ fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 500, color: '#FAB95B', letterSpacing: 2 }}>SERVICE 01</div>
              <div style={{ fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 800, color: '#E8EDF2' }}>SEAL WINDOWS</div>
            </div>
          </div>
        </div>

        {/* Service 2 — Attic insulation */}
        <div style={{
          position: 'absolute', right: 100, top: 320,
          opacity: s2, transform: `translateX(${(1 - s2) * 50}px)`,
        }}>
          <div style={{
            display: 'flex', alignItems: 'center', gap: 16,
            padding: '14px 24px',
            backgroundColor: 'rgba(250,185,91,0.12)',
            border: '2px solid #FAB95B', borderRadius: 14,
          }}>
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
              <path d="M3 10L12 3L21 10" stroke="#FAB95B" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/>
              <line x1="6" y1="13" x2="18" y2="13" stroke="#FAB95B" strokeWidth="2"/>
              <line x1="6" y1="16" x2="18" y2="16" stroke="#FAB95B" strokeWidth="2"/>
              <line x1="6" y1="19" x2="18" y2="19" stroke="#FAB95B" strokeWidth="2"/>
            </svg>
            <div>
              <div style={{ fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 500, color: '#FAB95B', letterSpacing: 2 }}>SERVICE 02</div>
              <div style={{ fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 800, color: '#E8EDF2' }}>INSULATE ATTIC</div>
            </div>
          </div>
        </div>

        {/* Service 3 — Heater */}
        <div style={{
          position: 'absolute', right: 130, top: 560,
          opacity: s3, transform: `translateX(${(1 - s3) * 50}px)`,
        }}>
          <div style={{
            display: 'flex', alignItems: 'center', gap: 16,
            padding: '14px 24px',
            backgroundColor: 'rgba(250,185,91,0.12)',
            border: '2px solid #FAB95B', borderRadius: 14,
          }}>
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
              <path d="M12 2C12 6 8 8 8 12C8 16 12 18 12 22" stroke="#FAB95B" strokeWidth="2.5" strokeLinecap="round"/>
              <path d="M16 6C16 9 14 10 14 13C14 16 16 17 16 20" stroke="#FAB95B" strokeWidth="2.5" strokeLinecap="round" opacity="0.7"/>
            </svg>
            <div>
              <div style={{ fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 500, color: '#FAB95B', letterSpacing: 2 }}>SERVICE 03</div>
              <div style={{ fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 800, color: '#E8EDF2' }}>SERVICE HEATER</div>
            </div>
          </div>
        </div>

        {/* Bottom — Bill chart going down */}
        <div style={{
          position: 'absolute', left: 130, bottom: 90,
          opacity: billIn, transform: `translateY(${(1 - billIn) * 30}px)`,
          display: 'flex', alignItems: 'flex-end', gap: 18,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 30, fontWeight: 600,
            color: '#E8EDF2', letterSpacing: 2,
          }}>UTILITY BILLS</div>
          <svg width="220" height="80" viewBox="0 0 220 80">
            <path d="M10 20 L60 25 L110 45 L160 60 L210 70" stroke="#FAB95B" strokeWidth="4" fill="none" strokeLinecap="round" strokeDasharray="240" strokeDashoffset={240 - billIn * 240}/>
            <polygon points="210,70 200,62 200,78" fill="#FAB95B"/>
          </svg>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 900,
            color: '#FAB95B', letterSpacing: 2,
          }}>PERMANENTLY LOWER</div>
        </div>

        {/* Bottom right — Age in place stamp */}
        <div style={{
          position: 'absolute', right: 130, bottom: 90,
          transform: `scale(${ageIn})`,
          padding: '16px 28px',
          backgroundColor: 'rgba(232,237,242,0.08)',
          border: '2px solid #E8EDF2', borderRadius: 12,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 26, fontWeight: 500,
            color: '#E8EDF2', letterSpacing: 3, opacity: 0.7,
          }}>RESULT</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 42, fontWeight: 900,
            color: '#FAB95B', letterSpacing: 2,
          }}>AGE IN PLACE · NO DEBT</div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene34_WeatherizationServices;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35_TheCatch',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35_TheCatch: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const catchIn = sp(8);
  const applyIn = sp(40);
  const paperIn = sp(90);
  const wipeOut = interpolate(frame, [180, 220], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const secretIn = sp(220);
  const urlIn = sp(260);

  const shake = frame > 30 && frame < 60 ? Math.sin(frame * 1.5) * 6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(255,0,0,0.08) 0%, rgba(65,45,21,0) 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 800, height: 800,
        border: '2px solid #FAB95B', opacity: 0.05,
        top: -200, right: -200,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 600, height: 600, borderRadius: '50%',
        border: '2px solid #E8EDF2', opacity: 0.04,
        bottom: -150, left: -150,
      }} />

      {/* Dot grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        {Array.from({ length: 24 }).map((_, i) =>
          Array.from({ length: 14 }).map((__, j) => (
            <circle key={`${i}-${j}`} cx={i * 80 + 40} cy={j * 80 + 40} r="1.5" fill="#FAB95B"/>
          ))
        )}
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const sx = seededRandom(i + 5) * 1920;
        const sy = seededRandom(i + 55) * 1080;
        const drift = Math.sin(frame * 0.03 + i) * 25;
        return (
          <div key={i} style={{
            position: 'absolute', left: sx + drift, top: sy,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: '#FAB95B', opacity: 0.4,
          }} />
        );
      })}

      <div style={{ position: 'absolute', width: '100%', height: '100%', transform: `translateX(${shake}px)` }}>

        {/* "HERE'S THE CATCH" — top */}
        <div style={{
          position: 'absolute', top: 80, left: 0, right: 0, textAlign: 'center',
          opacity: catchIn, transform: `scale(${catchIn})`,
        }}>
          <div style={{
            display: 'inline-flex', alignItems: 'center', gap: 18,
            padding: '14px 32px',
            backgroundColor: 'rgba(255,0,0,0.18)',
            border: '3px solid #ff0000', borderRadius: 60,
          }}>
            <svg width="36" height="36" viewBox="0 0 24 24" fill="none">
              <path d="M12 2L2 22H22L12 2Z" stroke="#ff0000" strokeWidth="2.5" strokeLinejoin="round"/>
              <line x1="12" y1="10" x2="12" y2="15" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round"/>
              <circle cx="12" cy="18" r="1.2" fill="#ff0000"/>
            </svg>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 800,
              color: '#ff0000', letterSpacing: 5,
            }}>HERE'S THE CATCH</div>
          </div>
        </div>

        {/* APPLY — hero text middle (gets replaced) */}
        <div style={{
          position: 'absolute', top: 220, left: 0, right: 0, textAlign: 'center',
          opacity: applyIn * (1 - wipeOut),
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 70, fontWeight: 500,
            color: '#E8EDF2', letterSpacing: 3, marginBottom: 4,
          }}>YOU HAVE TO</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 240, fontWeight: 900,
            color: '#FAB95B', lineHeight: 1, letterSpacing: -4,
          }}>APPLY.</div>
        </div>

        {/* Paperwork chaos illustration */}
        <div style={{
          position: 'absolute', bottom: 130, left: 0, right: 0,
          display: 'flex', justifyContent: 'center', gap: 30,
          opacity: paperIn * (1 - wipeOut),
        }}>
          {[0, 1, 2, 3, 4].map((i) => {
            const rot = (i - 2) * 8 + Math.sin(frame * 0.05 + i) * 3;
            const ty = Math.sin(frame * 0.06 + i) * 8;
            return (
              <div key={i} style={{
                width: 180, height: 230,
                backgroundColor: '#E8EDF2',
                border: '2px solid #FAB95B', borderRadius: 6,
                transform: `rotate(${rot}deg) translateY(${ty}px) scale(${paperIn})`,
                padding: 16,
                boxShadow: '0 8px 24px rgba(0,0,0,0.4)',
              }}>
                <div style={{ width: '60%', height: 10, backgroundColor: '#412D15', marginBottom: 12 }}/>
                {Array.from({ length: 8 }).map((_, j) => (
                  <div key={j} style={{ width: `${60 + (j * 7) % 35}%`, height: 5, backgroundColor: '#412D15', opacity: 0.6, marginBottom: 8 }}/>
                ))}
                <div style={{
                  position: 'absolute', bottom: 10, right: 10,
                  fontFamily: 'Inter, Poppins', fontSize: 16, fontWeight: 800,
                  color: '#ff0000', letterSpacing: 1,
                }}>FORM {i + 1}</div>
              </div>
            );
          })}
        </div>

        {/* Wipe-over: BUT WAIT — secret reveal */}
        <div style={{
          position: 'absolute', top: 240, left: 0, right: 0, textAlign: 'center',
          opacity: secretIn,
          transform: `translateY(${(1 - secretIn) * 40}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 50, fontWeight: 500,
            color: '#E8EDF2', letterSpacing: 4, marginBottom: 16,
          }}>BUT THERE'S A</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 170, fontWeight: 900,
            color: '#FAB95B', lineHeight: 1, letterSpacing: -3,
          }}>ONE-STOP SECRET</div>
        </div>

        {/* URL pill */}
        <div style={{
          position: 'absolute', bottom: 180, left: '50%',
          transform: `translateX(-50%) scale(${urlIn})`,
          padding: '26px 60px',
          backgroundColor: '#E8EDF2',
          border: '4px solid #FAB95B', borderRadius: 70,
          display: 'flex', alignItems: 'center', gap: 22,
          boxShadow: '0 10px 40px rgba(250,185,91,0.4)',
        }}>
          <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
            <circle cx="12" cy="12" r="10" stroke="#412D15" strokeWidth="2.5"/>
            <path d="M2 12H22M12 2C14.5 4.5 16 8 16 12C16 16 14.5 19.5 12 22C9.5 19.5 8 16 8 12C8 8 9.5 4.5 12 2Z" stroke="#412D15" strokeWidth="2.5" strokeLinejoin="round"/>
          </svg>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 64, fontWeight: 900,
            color: '#412D15', letterSpacing: 1,
          }}>benefitscheckup.org</div>
        </div>

        {/* Bottom small label */}
        <div style={{
          position: 'absolute', bottom: 100, left: 0, right: 0, textAlign: 'center',
          opacity: urlIn,
          fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 600,
          color: '#FAB95B', letterSpacing: 4,
        }}>★ FREE · ALL PROGRAMS IN ONE PLACE ★</div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene35_TheCatch;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36_HowItWorks',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene36_HowItWorks: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(8);
  const step1In = sp(40);
  const step2In = sp(90);
  const step3In = sp(140);
  const scanIn = sp(200);

  const scanCount = Math.floor(interpolate(frame, [220, 350], [0, 12847], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(120deg, rgba(250,185,91,0.10) 0%, rgba(65,45,21,0) 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 500, height: 500, borderRadius: '50%',
        border: '2px solid #FAB95B', opacity: 0.05,
        top: 100, right: 100,
        transform: `scale(${1 + Math.sin(frame * 0.025) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 400, height: 400,
        border: '2px solid #E8EDF2', opacity: 0.04,
        bottom: 50, left: 50,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />

      {/* Circuit-style texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <line key={i} x1={0} y1={i * 135 + 60} x2={1920} y2={i * 135 + 60} stroke="#FAB95B" strokeWidth="0.6" strokeDasharray="20 14"/>
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const sx = seededRandom(i + 6) * 1920;
        const sy = (seededRandom(i + 66) * 1080 - frame * 0.5 + 2000) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: sx, top: sy,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: '#FAB95B', opacity: 0.5,
          }} />
        );
      })}

      {/* Title — top */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0, textAlign: 'center',
        opacity: titleIn, transform: `translateY(${(1 - titleIn) * 20}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 500,
          color: '#FAB95B', letterSpacing: 6, marginBottom: 6,
        }}>NATIONAL COUNCIL ON AGING · FREE</div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 96, fontWeight: 900,
          color: '#E8EDF2', lineHeight: 1, letterSpacing: -2,
        }}>HOW IT WORKS</div>
      </div>

      {/* Three steps — left side stacked */}
      <div style={{
        position: 'absolute', left: 110, top: 280,
        display: 'flex', flexDirection: 'column', gap: 28,
      }}>
        {[
          {
            num: '01',
            label: 'TYPE YOUR ZIP CODE',
            anim: step1In,
            icon: (
              <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
                <path d="M12 2C8 2 5 5 5 9C5 14 12 22 12 22C12 22 19 14 19 9C19 5 16 2 12 2Z" stroke="#FAB95B" strokeWidth="2.5" strokeLinejoin="round"/>
                <circle cx="12" cy="9" r="2.5" fill="#FAB95B"/>
              </svg>
            ),
          },
          {
            num: '02',
            label: 'ANSWER A FEW QUESTIONS',
            anim: step2In,
            icon: (
              <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
                <circle cx="12" cy="12" r="10" stroke="#FAB95B" strokeWidth="2.5"/>
                <path d="M9 9C9 7 10.5 6 12 6C13.5 6 15 7 15 9C15 11 12 11 12 13" stroke="#FAB95B" strokeWidth="2.5" strokeLinecap="round"/>
                <circle cx="12" cy="17" r="1.2" fill="#FAB95B"/>
              </svg>
            ),
          },
          {
            num: '03',
            label: 'GET YOUR PERSONAL LIST',
            anim: step3In,
            icon: (
              <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
                <rect x="4" y="3" width="16" height="18" stroke="#FAB95B" strokeWidth="2.5"/>
                <line x1="8" y1="9" x2="16" y2="9" stroke="#FAB95B" strokeWidth="2.5" strokeLinecap="round"/>
                <line x1="8" y1="13" x2="16" y2="13" stroke="#FAB95B" strokeWidth="2.5" strokeLinecap="round"/>
                <line x1="8" y1="17" x2="13" y2="17" stroke="#FAB95B" strokeWidth="2.5" strokeLinecap="round"/>
              </svg>
            ),
          },
        ].map((step, i) => (
          <div key={i} style={{
            display: 'flex', alignItems: 'center', gap: 24,
            padding: '22px 34px',
            backgroundColor: 'rgba(232,237,242,0.06)',
            border: '2px solid #FAB95B', borderRadius: 18,
            width: 740,
            opacity: step.anim,
            transform: `translateX(${(1 - step.anim) * -60}px)`,
          }}>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 64, fontWeight: 900,
              color: '#FAB95B', letterSpacing: -1, minWidth: 90,
            }}>{step.num}</div>
            <div style={{ width: 2, height: 60, backgroundColor: '#FAB95B', opacity: 0.4 }}/>
            <div>{step.icon}</div>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 40, fontWeight: 800,
              color: '#E8EDF2', letterSpacing: 1,
            }}>{step.label}</div>
          </div>
        ))}
      </div>

      {/* Right side — Live scanner panel */}
      <div style={{
        position: 'absolute', right: 110, top: 290,
        width: 460, height: 540,
        backgroundColor: 'rgba(232,237,242,0.06)',
        border: '2px solid #E8EDF2', borderRadius: 22,
        padding: 36,
        opacity: scanIn,
        transform: `scale(${scanIn})`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 24, fontWeight: 500,
          color: '#E8EDF2', letterSpacing: 4, opacity: 0.8, marginBottom: 14,
        }}>LIVE SCAN</div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 600,
          color: '#FAB95B', letterSpacing: 2, marginBottom: 8,
        }}>PROGRAMS SCANNED</div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 130, fontWeight: 900,
          color: '#E8EDF2', lineHeight: 1, letterSpacing: -3, marginBottom: 24,
        }}>{scanCount.toLocaleString()}</div>

        {/* Animated scan bar */}
        <div style={{ marginBottom: 24 }}>
          <div style={{ width: '100%', height: 12, backgroundColor: 'rgba(232,237,242,0.15)', borderRadius: 6, overflow: 'hidden' }}>
            <div style={{
              width: `${Math.min(100, (scanCount / 12847) * 100)}%`,
              height: '100%', backgroundColor: '#FAB95B',
            }}/>
          </div>
        </div>

        {/* Scrolling check lines */}
        {Array.from({ length: 5 }).map((_, i) => {
          const lineDelay = 250 + i * 12;
          const op = interpolate(frame, [lineDelay, lineDelay + 10], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
          const labels = ['Federal benefit found', 'State program match', 'Local subsidy detected', 'Senior-only program', 'Eligibility confirmed'];
          return (
            <div key={i} style={{
              display: 'flex', alignItems: 'center', gap: 12, marginBottom: 12,
              opacity: op,
            }}>
              <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
                <path d="M5 12L10 17L20 7" stroke="#FAB95B" strokeWidth="3.5" strokeLinecap="round" strokeLinejoin="round"/>
              </svg>
              <div style={{
                fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 500,
                color: '#E8EDF2', letterSpacing: 0.5,
              }}>{labels[i]}</div>
            </div>
          );
        })}
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene36_HowItWorks;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37_PersonalList',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene37_PersonalList: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headIn = sp(8);
  const paperIn = sp(35);
  const items = [60, 85, 110, 135, 160];
  const timeIn = sp(220);
  const ctaIn = sp(280);

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 50%, rgba(250,185,91,0.12) 0%, rgba(65,45,21,0) 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 0, height: 0, opacity: 0.05,
        borderLeft: '200px solid transparent',
        borderRight: '200px solid transparent',
        borderBottom: '350px solid #FAB95B',
        bottom: 100, right: 50,
        transform: `rotate(${Math.sin(frame * 0.02) * 6 - 15}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 700, height: 700, borderRadius: '50%',
        border: '2px solid #E8EDF2', opacity: 0.04,
        top: -200, left: -150,
      }} />

      {/* Dotted texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 20 }).map((_, i) =>
          Array.from({ length: 12 }).map((__, j) => (
            <circle key={`${i}-${j}`} cx={i * 100 + 50} cy={j * 90 + 50} r="1.5" fill="#FAB95B"/>
          ))
        )}
      </svg>

      {/* Particles */}
      {Array.from({ length: 26 }).map((_, i) => {
        const sx = seededRandom(i + 9) * 1920;
        const sy = seededRandom(i + 33) * 1080;
        const drift = Math.sin(frame * 0.03 + i) * 22;
        return (
          <div key={i} style={{
            position: 'absolute', left: sx + drift, top: sy,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: '#FAB95B', opacity: 0.4,
          }} />
        );
      })}

      {/* Headline */}
      <div style={{
        position: 'absolute', top: 70, left: 100,
        opacity: headIn, transform: `translateY(${(1 - headIn) * 25}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 500,
          color: '#FAB95B', letterSpacing: 5, marginBottom: 6,
        }}>YOUR PERSONALIZED RESULT</div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 105, fontWeight: 900,
          color: '#E8EDF2', lineHeight: 1, letterSpacing: -2,
        }}>Every Benefit You Qualify For</div>
      </div>

      {/* Receipt-style paper — left center */}
      <div style={{
        position: 'absolute', left: 100, top: 290,
        width: 720, padding: 40,
        backgroundColor: '#E8EDF2',
        borderRadius: 14,
        transform: `scale(${paperIn}) rotate(-2deg)`,
        transformOrigin: 'top left',
        boxShadow: '0 20px 50px rgba(0,0,0,0.5)',
      }}>
        {/* Receipt header */}
        <div style={{
          textAlign: 'center', borderBottom: '2px dashed #412D15',
          paddingBottom: 16, marginBottom: 20,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 900,
            color: '#412D15', letterSpacing: 4,
          }}>YOUR BENEFIT LIST</div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 18, fontWeight: 500,
            color: '#412D15', opacity: 0.7, marginTop: 4,
          }}>benefitscheckup.org · 2026</div>
        </div>

        {/* Items */}
        {[
          { name: 'SNAP FOR SENIORS', value: '$250/mo' },
          { name: 'MEDICARE EXTRA HELP', value: '$0 co-pays' },
          { name: 'MEDICARE SAVINGS · QMB', value: '$174/mo' },
          { name: 'LIHEAP ENERGY HELP', value: 'Bills paid' },
          { name: 'SECTION 504 REPAIR', value: '$10,000' },
        ].map((item, i) => {
          const itemAnim = sp(items[i]);
          return (
            <div key={i} style={{
              display: 'flex', justifyContent: 'space-between', alignItems: 'center',
              padding: '14px 4px',
              borderBottom: '1px dashed rgba(65,45,21,0.3)',
              opacity: itemAnim,
              transform: `translateX(${(1 - itemAnim) * -30}px)`,
            }}>
              <div style={{ display: 'flex', alignItems: 'center', gap: 14 }}>
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none">
                  <path d="M5 12L10 17L20 7" stroke="#412D15" strokeWidth="3.5" strokeLinecap="round" strokeLinejoin="round"/>
                </svg>
                <div style={{
                  fontFamily: 'Inter, Poppins', fontSize: 30, fontWeight: 800,
                  color: '#412D15', letterSpacing: 1,
                }}>{item.name}</div>
              </div>
              <div style={{
                fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700,
                color: '#412D15',
              }}>{item.value}</div>
            </div>
          );
        })}

        <div style={{
          textAlign: 'center', marginTop: 20,
          fontFamily: 'JetBrains Mono', fontSize: 16, fontWeight: 500,
          color: '#412D15', opacity: 0.6, letterSpacing: 2,
        }}>· · · DIRECT APPLY LINKS INCLUDED · · ·</div>
      </div>

      {/* Right side — 5 MINUTES badge */}
      <div style={{
        position: 'absolute', right: 130, top: 320,
        opacity: timeIn, transform: `scale(${timeIn})`,
      }}>
        <div style={{
          width: 460, padding: 36,
          backgroundColor: 'rgba(250,185,91,0.15)',
          border: '3px solid #FAB95B', borderRadius: 22,
          textAlign: 'center',
        }}>
          <div style={{ marginBottom: 14, display: 'flex', justifyContent: 'center' }}>
            <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
              <circle cx="12" cy="12" r="10" stroke="#FAB95B" strokeWidth="2.5"/>
              <path d="M12 7V12L15 14" stroke="#FAB95B" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/>
            </svg>
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 500,
            color: '#E8EDF2', letterSpacing: 4, marginBottom: 6,
          }}>TOTAL TIME</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 180, fontWeight: 900,
            color: '#FAB95B', lineHeight: 1, letterSpacing: -4,
          }}>5</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 800,
            color: '#E8EDF2', letterSpacing: 4, marginTop: -8,
          }}>MINUTES</div>
        </div>
      </div>

      {/* Bottom CTA */}
      <div style={{
        position: 'absolute', bottom: 80, left: '50%',
        transform: `translateX(-50%) translateY(${(1 - ctaIn) * 30}px)`,
        opacity: ctaIn,
        display: 'flex', alignItems: 'center', gap: 30,
        padding: '18px 40px',
        backgroundColor: 'rgba(232,237,242,0.06)',
        border: '2px solid #E8EDF2', borderRadius: 60,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 2,
        }}>SWALLOW YOUR PRIDE</div>
        <svg width="36" height="20" viewBox="0 0 36 20" fill="none">
          <path d="M2 10H32M32 10L24 3M32 10L24 17" stroke="#E8EDF2" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/>
        </svg>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 700,
          color: '#E8EDF2', letterSpacing: 2,
        }}>GO · PRINT · APPLY</div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene37_PersonalList;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38_RecapPart1',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene38_RecapPart1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(8);
  const card1In = sp(50);
  const card2In = sp(110);
  const card3In = sp(170);

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(250,185,91,0.10) 0%, rgba(65,45,21,0) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 1000, height: 1000, borderRadius: '50%',
        border: '2px solid #FAB95B', opacity: 0.04,
        top: -400, left: '50%', transform: `translateX(-50%) rotate(${frame * 0.08}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 600, height: 600,
        border: '2px solid #E8EDF2', opacity: 0.04,
        bottom: -200, right: -100,
        transform: `rotate(${-frame * 0.1 + 25}deg)`,
      }} />

      {/* Grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <line key={i} x1={i * 96} y1={0} x2={i * 96} y2={1080} stroke="#FAB95B" strokeWidth="0.5"/>
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const sx = seededRandom(i + 12) * 1920;
        const sy = seededRandom(i + 44) * 1080;
        const drift = Math.sin(frame * 0.025 + i) * 28;
        return (
          <div key={i} style={{
            position: 'absolute', left: sx + drift, top: sy,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#FAB95B' : '#E8EDF2', opacity: 0.45,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 80, left: 0, right: 0, textAlign: 'center',
        opacity: titleIn, transform: `translateY(${(1 - titleIn) * 25}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          padding: '10px 28px',
          backgroundColor: 'rgba(250,185,91,0.15)',
          border: '2px solid #FAB95B', borderRadius: 50,
          fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 5, marginBottom: 18,
        }}>RECAP · PART 1 OF 2</div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 92, fontWeight: 900,
          color: '#E8EDF2', lineHeight: 1, letterSpacing: -2,
        }}>5 Free Programs · <span style={{ color: '#FAB95B' }}>2026</span></div>
      </div>

      {/* Three cards row */}
      <div style={{
        position: 'absolute', top: 320, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', gap: 32, padding: '0 90px',
      }}>
        {[
          {
            num: '01',
            title: 'SNAP FOR SENIORS',
            value: '$250+/mo',
            sub: 'On your grocery card',
            icon: (
              <svg width="70" height="70" viewBox="0 0 24 24" fill="none">
                <path d="M5 8H19L17 20H7L5 8Z" stroke="#FAB95B" strokeWidth="2.5" strokeLinejoin="round"/>
                <path d="M9 8V5C9 3.5 10.5 3 12 3C13.5 3 15 3.5 15 5V8" stroke="#FAB95B" strokeWidth="2.5"/>
              </svg>
            ),
            anim: card1In,
          },
          {
            num: '02',
            title: 'EXTRA HELP',
            value: '$0 co-pays',
            sub: 'Prescription wipeout',
            icon: (
              <svg width="70" height="70" viewBox="0 0 24 24" fill="none">
                <rect x="3" y="8" width="18" height="8" rx="4" stroke="#FAB95B" strokeWidth="2.5"/>
                <line x1="12" y1="8" x2="12" y2="16" stroke="#FAB95B" strokeWidth="2.5"/>
              </svg>
            ),
            anim: card2In,
          },
          {
            num: '03',
            title: 'MEDICARE SAVINGS',
            value: '$174/mo',
            sub: 'Part B premium paid',
            icon: (
              <svg width="70" height="70" viewBox="0 0 24 24" fill="none">
                <path d="M12 2L4 6V12C4 17 7.5 21 12 22C16.5 21 20 17 20 12V6L12 2Z" stroke="#FAB95B" strokeWidth="2.5" strokeLinejoin="round"/>
                <path d="M9 12L11 14L15 10" stroke="#FAB95B" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/>
              </svg>
            ),
            anim: card3In,
          },
        ].map((card, i) => (
          <div key={i} style={{
            width: 560, padding: 44,
            backgroundColor: 'rgba(232,237,242,0.07)',
            border: '2px solid #FAB95B', borderRadius: 22,
            opacity: card.anim,
            transform: `translateY(${(1 - card.anim) * 60}px) scale(${0.92 + card.anim * 0.08})`,
          }}>
            <div style={{
              display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 24,
            }}>
              <div style={{
                fontFamily: 'Inter, Poppins', fontSize: 60, fontWeight: 900,
                color: 'rgba(250,185,91,0.4)', letterSpacing: -1,
              }}>{card.num}</div>
              {card.icon}
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 40, fontWeight: 800,
              color: '#E8EDF2', letterSpacing: 1, marginBottom: 14, lineHeight: 1.1,
            }}>{card.title}</div>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 70, fontWeight: 900,
              color: '#FAB95B', letterSpacing: -1, lineHeight: 1, marginBottom: 10,
            }}>{card.value}</div>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 26, fontWeight: 500,
              color: '#E8EDF2', opacity: 0.75, letterSpacing: 1,
            }}>{card.sub}</div>
          </div>
        ))}
      </div>

      {/* Bottom — 2 more coming */}
      <div style={{
        position: 'absolute', bottom: 90, left: 0, right: 0, textAlign: 'center',
      }}>
        <div style={{
          display: 'inline-flex', alignItems: 'center', gap: 16,
          padding: '12px 30px',
          backgroundColor: 'rgba(232,237,242,0.05)',
          border: '1.5px solid rgba(232,237,242,0.3)', borderRadius: 50,
        }}>
          <div style={{ width: 12, height: 12, borderRadius: '50%', backgroundColor: '#FAB95B', opacity: 0.5 + Math.sin(frame * 0.15) * 0.5 }}/>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 26, fontWeight: 600,
            color: '#E8EDF2', letterSpacing: 3, opacity: 0.85,
          }}>TWO MORE PROGRAMS COMING UP</div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene38_RecapPart1;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39_RecapPart2',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene39_RecapPart2: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const card4In = sp(8);
  const card5In = sp(50);
  const verdict1 = sp(140);
  const verdict2 = sp(180);
  const verdict3 = sp(220);
  const stampIn = sp(270, { damping: 8 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 80% 30%, rgba(250,185,91,0.15) 0%, rgba(65,45,21,0) 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 0, height: 0, opacity: 0.06,
        borderLeft: '200px solid transparent',
        borderRight: '200px solid transparent',
        borderBottom: '350px solid #FAB95B',
        top: 150, left: 60,
        transform: `rotate(${15 + Math.sin(frame * 0.02) * 5}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500, borderRadius: '50%',
        border: '2px solid #E8EDF2', opacity: 0.05,
        bottom: -100, right: -100,
      }} />

      {/* Texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({ length: 24 }).map((_, i) => (
          <line key={i} x1={0} y1={i * 45 + 20} x2={1920} y2={i * 45 + 20} stroke="#FAB95B" strokeWidth="0.5" strokeDasharray="6 10"/>
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const sx = seededRandom(i + 14) * 1920;
        const sy = (seededRandom(i + 78) * 1080 + frame * 0.6) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: sx, top: sy,
            width: 3, height: 3, borderRadius: '50%',
            backgroundColor: '#FAB95B', opacity: 0.4,
          }} />
        );
      })}

      {/* Two cards — top */}
      <div style={{
        position: 'absolute', top: 80, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', gap: 36, padding: '0 90px',
      }}>
        {/* Card 04 — LIHEAP */}
        <div style={{
          width: 800, padding: 44,
          backgroundColor: 'rgba(232,237,242,0.07)',
          border: '2px solid #FAB95B', borderRadius: 22,
          opacity: card4In,
          transform: `translateY(${(1 - card4In) * 60}px)`,
        }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 22 }}>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 60, fontWeight: 900,
              color: 'rgba(250,185,91,0.4)',
            }}>04</div>
            <svg width="70" height="70" viewBox="0 0 24 24" fill="none">
              <path d="M12 2C12 6 8 8 8 12C8 16 12 18 12 22C12 18 16 16 16 12C16 8 12 6 12 2Z" stroke="#FAB95B" strokeWidth="2.5" strokeLinejoin="round"/>
            </svg>
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 800,
            color: '#E8EDF2', letterSpacing: 1, marginBottom: 14,
          }}>LIHEAP · ENERGY</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 70, fontWeight: 900,
            color: '#FAB95B', lineHeight: 1, letterSpacing: -1, marginBottom: 10,
          }}>BILLS PAID</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 500,
            color: '#E8EDF2', opacity: 0.8, letterSpacing: 1,
          }}>Heating & cooling so you don't freeze</div>
        </div>

        {/* Card 05 — Section 504 */}
        <div style={{
          width: 800, padding: 44,
          backgroundColor: 'rgba(250,185,91,0.14)',
          border: '3px solid #FAB95B', borderRadius: 22,
          opacity: card5In,
          transform: `translateY(${(1 - card5In) * 60}px)`,
        }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 22 }}>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 60, fontWeight: 900,
              color: 'rgba(250,185,91,0.5)',
            }}>05</div>
            <svg width="70" height="70" viewBox="0 0 24 24" fill="none">
              <path d="M3 10L12 3L21 10V21H14V14H10V21H3V10Z" stroke="#FAB95B" strokeWidth="2.5" strokeLinejoin="round"/>
            </svg>
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 800,
            color: '#E8EDF2', letterSpacing: 1, marginBottom: 14,
          }}>SECTION 504 REPAIR</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 70, fontWeight: 900,
            color: '#FAB95B', lineHeight: 1, letterSpacing: -1, marginBottom: 10,
          }}>$10,000 FREE</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 500,
            color: '#E8EDF2', opacity: 0.8, letterSpacing: 1,
          }}>Roof & plumbing fixed · no payback</div>
        </div>
      </div>

      {/* Verdict — center bottom */}
      <div style={{
        position: 'absolute', bottom: 180, left: 0, right: 0, textAlign: 'center',
      }}>
        <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'baseline', gap: 40 }}>
          <div style={{
            opacity: verdict1, transform: `translateY(${(1 - verdict1) * 25}px)`,
            fontFamily: 'Inter, Poppins', fontSize: 78, fontWeight: 900,
            color: '#E8EDF2', letterSpacing: -1,
          }}>You paid your taxes.</div>
        </div>
        <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'baseline', gap: 40, marginTop: 4 }}>
          <div style={{
            opacity: verdict2, transform: `translateY(${(1 - verdict2) * 25}px)`,
            fontFamily: 'Inter, Poppins', fontSize: 78, fontWeight: 900,
            color: '#E8EDF2', letterSpacing: -1,
          }}>You did your time.</div>
        </div>
        <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'baseline', gap: 40, marginTop: 4 }}>
          <div style={{
            opacity: verdict3, transform: `translateY(${(1 - verdict3) * 25}px)`,
            fontFamily: 'Inter, Poppins', fontSize: 100, fontWeight: 900,
            color: '#FAB95B', letterSpacing: -2,
          }}>You EARNED these.</div>
        </div>
      </div>

      {/* EARNED stamp — right corner */}
      <div style={{
        position: 'absolute', top: 720, right: 130,
        transform: `rotate(-10deg) scale(${stampIn})`,
        opacity: stampIn,
        padding: '14px 30px',
        border: '4px solid #FAB95B', borderRadius: 10,
        backgroundColor: 'rgba(65,45,21,0.85)',
        fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 900,
        color: '#FAB95B', letterSpacing: 4,
      }}>RIGHTFULLY YOURS</div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene39_RecapPart2;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40_FinalCTA',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene40_FinalCTA: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const lineIn = sp(8);
  const subIn = sp(60, { damping: 8 });
  const personA = sp(130);
  const personB = sp(160);
  const personC = sp(190);
  const closingIn = sp(260);

  const pulse = 1 + Math.sin(frame * 0.12) * 0.025;

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 40%, rgba(250,185,91,0.18) 0%, rgba(65,45,21,0) 65%)',
      }} />

      {/* Shape layers — radiating */}
      <div style={{
        position: 'absolute', width: 1200, height: 1200, borderRadius: '50%',
        border: '2px solid #FAB95B', opacity: 0.05,
        top: '50%', left: '50%',
        transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.025) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 900, height: 900, borderRadius: '50%',
        border: '2px solid #FAB95B', opacity: 0.06,
        top: '50%', left: '50%',
        transform: `translate(-50%, -50%) rotate(${frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 600, height: 600, borderRadius: '50%',
        border: '2px solid #E8EDF2', opacity: 0.05,
        top: '50%', left: '50%',
        transform: `translate(-50%, -50%) rotate(${-frame * 0.2}deg)`,
      }} />

      {/* Radial burst */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        {Array.from({ length: 28 }).map((_, i) => {
          const angle = (i / 28) * Math.PI * 2;
          const x1 = 960 + Math.cos(angle) * 320;
          const y1 = 540 + Math.sin(angle) * 320;
          const x2 = 960 + Math.cos(angle) * 900;
          const y2 = 540 + Math.sin(angle) * 900;
          return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#FAB95B" strokeWidth="1"/>;
        })}
      </svg>

      {/* Particles — rising */}
      {Array.from({ length: 50 }).map((_, i) => {
        const sx = seededRandom(i + 15) * 1920;
        const sy = (seededRandom(i + 99) * 1200 - frame * 1.0 + 1200) % 1200;
        const size = 2 + seededRandom(i + 200) * 4;
        return (
          <div key={i} style={{
            position: 'absolute', left: sx, top: sy,
            width: size, height: size, borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#FAB95B' : '#E8EDF2', opacity: 0.55,
          }} />
        );
      })}

      {/* Hero line — DON'T LET PRIDE */}
      <div style={{
        position: 'absolute', top: 110, left: 0, right: 0, textAlign: 'center',
        opacity: lineIn, transform: `translateY(${(1 - lineIn) * 30}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 500,
          color: '#FAB95B', letterSpacing: 6, marginBottom: 10,
        }}>DON'T LET PRIDE OR CONFUSION</div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 130, fontWeight: 900,
          color: '#E8EDF2', lineHeight: 1, letterSpacing: -3,
        }}>CLAIM <span style={{ color: '#FAB95B' }}>WHAT'S YOURS</span></div>
      </div>

      {/* Subscribe & share pill */}
      <div style={{
        position: 'absolute', top: 360, left: '50%',
        transform: `translateX(-50%) scale(${subIn * pulse})`,
        display: 'flex', alignItems: 'center', gap: 26,
        padding: '24px 50px',
        backgroundColor: '#FAB95B',
        borderRadius: 70,
        boxShadow: '0 15px 50px rgba(250,185,91,0.5)',
      }}>
        <svg width="48" height="48" viewBox="0 0 24 24" fill="none">
          <path d="M19 11V18C19 19.5 17.5 21 16 21H6C4.5 21 3 19.5 3 18V8C3 6.5 4.5 5 6 5H13" stroke="#412D15" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/>
          <path d="M14 4L21 4M21 4L21 11M21 4L11 14" stroke="#412D15" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/>
        </svg>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 50, fontWeight: 900,
          color: '#412D15', letterSpacing: 3,
        }}>SUBSCRIBE · SHARE</div>
      </div>

      {/* Three people row */}
      <div style={{
        position: 'absolute', top: 530, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', gap: 50,
      }}>
        {[
          { label: 'A PARENT', anim: personA, icon: (
            <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
              <circle cx="12" cy="7" r="4" stroke="#FAB95B" strokeWidth="2.5"/>
              <path d="M4 22C4 17 7.5 14 12 14C16.5 14 20 17 20 22" stroke="#FAB95B" strokeWidth="2.5" strokeLinecap="round"/>
            </svg>
          )},
          { label: 'A NEIGHBOR', anim: personB, icon: (
            <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
              <path d="M3 12L12 3L21 12V21H3V12Z" stroke="#FAB95B" strokeWidth="2.5" strokeLinejoin="round"/>
              <rect x="10" y="14" width="4" height="7" fill="#FAB95B"/>
            </svg>
          )},
          { label: 'A FRIEND', anim: personC, icon: (
            <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
              <path d="M12 21C12 21 4 16 4 9C4 6 6 4 9 4C10.5 4 12 5 12 5C12 5 13.5 4 15 4C18 4 20 6 20 9C20 16 12 21 12 21Z" stroke="#FAB95B" strokeWidth="2.5" strokeLinejoin="round"/>
            </svg>
          )},
        ].map((p, i) => (
          <div key={i} style={{
            display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 12,
            padding: '24px 36px',
            backgroundColor: 'rgba(232,237,242,0.06)',
            border: '2px solid #FAB95B', borderRadius: 18,
            opacity: p.anim, transform: `translateY(${(1 - p.anim) * 40}px)`,
          }}>
            {p.icon}
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 800,
              color: '#E8EDF2', letterSpacing: 3,
            }}>{p.label}</div>
          </div>
        ))}
      </div>

      {/* Bottom closing line */}
      <div style={{
        position: 'absolute', bottom: 130, left: 0, right: 0, textAlign: 'center',
        opacity: closingIn, transform: `translateY(${(1 - closingIn) * 25}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 600,
          color: '#FAB95B', letterSpacing: 3, marginBottom: 8,
        }}>One conversation can save someone from ruin.</div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 56, fontWeight: 900,
          color: '#E8EDF2', letterSpacing: 2,
        }}>See you in the next one.</div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3, letterSpacing: 1,
      }}>Senior Fund USA</div>
    </AbsoluteFill>
  );
};

export default Scene40_FinalCTA;
```

