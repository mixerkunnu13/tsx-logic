```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31_PaperworkTest',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene31_PaperworkTest: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.05], { extrapolateRight: 'clamp' });
  const titleIn = sp(8);
  const cardIn = sp(40, { stiffness: 90 });
  const stampIn = sp(80, { stiffness: 200, damping: 8 });
  const stampRot = interpolate(stampIn, [0, 1], [-25, -12]);
  const shake = frame > 80 && frame < 92 ? Math.sin(frame * 3) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 30% 20%, rgba(147,181,198,0.35), transparent 60%)' }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: -150, right: -100, width: 600, height: 600, borderRadius: '50%', border: '3px solid #000000', opacity: 0.05, transform: `rotate(${frame * 0.2}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})` }} />
        <div style={{ position: 'absolute', bottom: -200, left: -150, width: 500, height: 500, background: '#93B5C6', opacity: 0.08, transform: `rotate(${-frame * 0.15}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — grid */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="100%" height="100%">
          <defs>
            <pattern id="grid31" width="80" height="80" patternUnits="userSpaceOnUse">
              <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#000000" strokeWidth="1.5" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#grid31)" transform={`translate(${(frame * 0.3) % 80}, 0)`} />
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 1);
        const x = r * 1920;
        const y = (seededRandom(i + 50) * 1080 + frame * (0.4 + r)) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 6 + r * 6, height: 6 + r * 6, borderRadius: '50%', background: '#000000', opacity: 0.12 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camZoom}) translateX(${shake}px)`, alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ textAlign: 'center', width: '85%' }}>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 64, fontWeight: 800, color: '#000000', opacity: titleIn, transform: `translateY(${(1 - titleIn) * 40}px)`, letterSpacing: -1 }}>
            THE PAPERWORK TEST
          </div>
          <div style={{ width: interpolate(titleIn, [0, 1], [0, 380]), height: 8, background: '#93B5C6', margin: '18px auto', borderRadius: 4 }} />

          {/* Document card with stamp */}
          <div style={{ position: 'relative', display: 'inline-block', marginTop: 40, opacity: cardIn, transform: `translateY(${(1 - cardIn) * 60}px) scale(${0.8 + cardIn * 0.2})` }}>
            <div style={{ width: 520, height: 360, background: '#F5F5F5', borderRadius: 16, border: '4px solid #000000', boxShadow: '14px 14px 0 #93B5C6', padding: 36, textAlign: 'left' }}>
              <svg width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="#000000" strokeWidth="2">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
                <polyline points="14 2 14 8 20 8" />
                <line x1="8" y1="13" x2="16" y2="13" />
                <line x1="8" y1="17" x2="14" y2="17" />
              </svg>
              <div style={{ fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 800, color: '#000000', marginTop: 22 }}>DEATH</div>
              <div style={{ fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 800, color: '#000000' }}>CERTIFICATE</div>
              <div style={{ fontFamily: 'Inter, Poppins', fontSize: 26, fontWeight: 600, color: '#93B5C6', marginTop: 14 }}>Required Document #1</div>
            </div>
            <div style={{ position: 'absolute', top: 30, right: -30, transform: `rotate(${stampRot}deg) scale(${stampIn})`, border: '5px solid #ff0000', color: '#ff0000', fontFamily: 'Inter, Poppins', fontWeight: 900, fontSize: 34, padding: '8px 20px', borderRadius: 10, opacity: 0.9 }}>
              MANDATORY
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.4)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene31_PaperworkTest;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32_MarriageRule',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32_MarriageRule: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [-20, 20], { extrapolateRight: 'clamp' });
  const titleIn = sp(8);
  const bigNumIn = sp(45, { stiffness: 200, damping: 9 });
  const labelIn = sp(70);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, rgba(147,181,198,0.3) 0%, transparent 50%, rgba(0,0,0,0.08) 100%)' }} />

      {/* Shape layers — triangles */}
      <AbsoluteFill>
        <svg width="100%" height="100%" style={{ opacity: 0.06 }}>
          <polygon points="300,100 500,400 100,400" fill="none" stroke="#000000" strokeWidth="3" transform={`rotate(${frame * 0.3} 300 300)`} />
          <polygon points="1600,700 1800,1000 1400,1000" fill="#93B5C6" transform={`rotate(${-frame * 0.25} 1600 850) scale(${1 + Math.sin(frame * 0.03) * 0.08})`} />
        </svg>
      </AbsoluteFill>

      {/* Motion texture — diagonal lines */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="100%" height="100%">
          <defs>
            <pattern id="lines32" width="60" height="60" patternUnits="userSpaceOnUse" patternTransform={`rotate(45) translate(${(frame * 0.4) % 60},0)`}>
              <line x1="0" y1="0" x2="0" y2="60" stroke="#000000" strokeWidth="2" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#lines32)" />
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const r = seededRandom(i + 10);
        const x = (r * 1920 + frame * 0.3) % 1920;
        const y = seededRandom(i + 60) * 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 8, height: 8, background: '#93B5C6', borderRadius: 2, opacity: 0.25, transform: `rotate(${frame + i * 30}deg)` }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camPan}px)`, alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ textAlign: 'center', width: '85%' }}>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 58, fontWeight: 700, color: '#000000', opacity: titleIn, transform: `translateY(${(1 - titleIn) * 30}px)` }}>
            MARRIAGE CERTIFICATE RULE
          </div>

          <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 40, marginTop: 30 }}>
            {/* Ring icon */}
            <svg width="120" height="120" viewBox="0 0 24 24" fill="none" stroke="#000000" strokeWidth="1.5" style={{ opacity: bigNumIn, transform: `rotate(${(1 - bigNumIn) * 180}deg)` }}>
              <circle cx="12" cy="14" r="6" />
              <path d="M9 8l3-5 3 5" />
            </svg>

            <div style={{ opacity: bigNumIn, transform: `scale(${0.5 + bigNumIn * 0.5})` }}>
              <div style={{ fontFamily: 'Inter, Poppins', fontSize: 240, fontWeight: 900, color: '#000000', lineHeight: 0.9, textShadow: '8px 8px 0 #93B5C6' }}>9</div>
            </div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 90, fontWeight: 800, color: '#000000', opacity: bigNumIn, textAlign: 'left' }}>
              MONTHS<br /><span style={{ fontSize: 40, fontWeight: 600, color: '#93B5C6' }}>minimum married</span>
            </div>
          </div>

          <div style={{ marginTop: 40, display: 'inline-block', background: '#000000', color: '#F4CE14', fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 700, padding: '14px 40px', borderRadius: 50, opacity: labelIn, transform: `translateY(${(1 - labelIn) * 40}px)` }}>
            Exceptions exist for accidents
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.38)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene32_MarriageRule;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33_DocsChecklist',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene33_DocsChecklist: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.04, 1], { extrapolateRight: 'clamp' });
  const titleIn = sp(8);

  const items = [
    { label: 'Both Social Security Numbers', delay: 40 },
    { label: "Late spouse's recent tax return", delay: 65 },
    { label: 'Proof of earnings record', delay: 90 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 70% 80%, rgba(147,181,198,0.35), transparent 55%)' }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: 100, left: -120, width: 450, height: 450, borderRadius: 40, border: '3px solid #000000', opacity: 0.05, transform: `rotate(${frame * 0.25}deg)` }} />
        <div style={{ position: 'absolute', bottom: -100, right: 80, width: 380, height: 380, borderRadius: '50%', background: '#93B5C6', opacity: 0.1, transform: `scale(${1 + Math.sin(frame * 0.025) * 0.06})` }} />
      </AbsoluteFill>

      {/* Motion texture — dots */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="100%" height="100%">
          <defs>
            <pattern id="dots33" width="50" height="50" patternUnits="userSpaceOnUse" patternTransform={`translate(0,${(frame * 0.3) % 50})`}>
              <circle cx="10" cy="10" r="3" fill="#000000" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#dots33)" />
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const r = seededRandom(i + 20);
        const x = r * 1920;
        const y = (seededRandom(i + 70) * 1080 - frame * 0.5 + 1080) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 7, height: 7, borderRadius: '50%', background: '#000000', opacity: 0.14 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ width: '85%' }}>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 62, fontWeight: 800, color: '#000000', textAlign: 'center', opacity: titleIn, transform: `translateY(${(1 - titleIn) * 35}px)`, marginBottom: 50 }}>
            BRING THESE DOCUMENTS
          </div>

          {items.map((it, idx) => {
            const inAnim = sp(it.delay, { stiffness: 100 });
            return (
              <div key={idx} style={{ display: 'flex', alignItems: 'center', gap: 28, background: '#F5F5F5', border: '4px solid #000000', borderRadius: 18, padding: '22px 34px', marginBottom: 24, opacity: inAnim, transform: `translateX(${(1 - inAnim) * -80}px)`, boxShadow: '8px 8px 0 #93B5C6' }}>
                <svg width="56" height="56" viewBox="0 0 24 24" fill="none" stroke="#000000" strokeWidth="3">
                  <path d="M20 6L9 17l-5-5" style={{ strokeDasharray: 40, strokeDashoffset: interpolate(inAnim, [0, 1], [40, 0]) }} />
                </svg>
                <div style={{ fontFamily: 'Inter, Poppins', fontSize: 48, fontWeight: 700, color: '#000000' }}>{it.label}</div>
              </div>
            );
          })}
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.38)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene33_DocsChecklist;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34_ReturnTheCheck',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene34_ReturnTheCheck: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(8);
  const checkIn = sp(45, { stiffness: 90 });
  // check slides back (return motion)
  const returnSlide = interpolate(sp(150, { stiffness: 60 }), [0, 1], [0, -320]);
  const arrowIn = sp(120);
  const calIn = sp(80, { stiffness: 110 });
  const shake = frame > 150 && frame < 162 ? Math.sin(frame * 3) * 5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, transparent 40%, rgba(147,181,198,0.4) 100%)' }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: -120, right: 200, width: 520, height: 520, borderRadius: '50%', border: '4px solid #000000', opacity: 0.05, transform: `rotate(${-frame * 0.2}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})` }} />
        <div style={{ position: 'absolute', bottom: 60, left: -100, width: 360, height: 360, background: '#93B5C6', opacity: 0.09, borderRadius: 30, transform: `rotate(${frame * 0.18}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — grid */}
      <AbsoluteFill style={{ opacity: 0.045 }}>
        <svg width="100%" height="100%">
          <defs>
            <pattern id="grid34" width="70" height="70" patternUnits="userSpaceOnUse">
              <path d="M 70 0 L 0 0 0 70" fill="none" stroke="#000000" strokeWidth="1.5" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#grid34)" transform={`translate(0,${(frame * 0.3) % 70})`} />
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const r = seededRandom(i + 30);
        const x = r * 1920;
        const y = (seededRandom(i + 80) * 1080 + frame * (0.3 + r * 0.5)) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 6, height: 6, borderRadius: '50%', background: '#000000', opacity: 0.12 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ alignItems: 'center', justifyContent: 'center', transform: `translateX(${shake}px)` }}>
        <div style={{ textAlign: 'center', width: '85%' }}>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 60, fontWeight: 800, color: '#000000', opacity: titleIn, transform: `translateY(${(1 - titleIn) * 35}px)` }}>
            RETURN THE FINAL CHECK
          </div>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 34, fontWeight: 600, color: '#93B5C6', marginTop: 10, opacity: titleIn }}>
            Benefits are paid for the previous month
          </div>

          <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 50, marginTop: 60 }}>
            {/* Check graphic */}
            <div style={{ position: 'relative', opacity: checkIn, transform: `translateX(${returnSlide}px) scale(${0.85 + checkIn * 0.15}) rotate(${interpolate(checkIn, [0, 1], [-6, 0])}deg)` }}>
              <div style={{ width: 460, height: 220, background: '#F5F5F5', border: '4px solid #000000', borderRadius: 14, padding: 24, textAlign: 'left', boxShadow: '10px 10px 0 #93B5C6' }}>
                <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 700, color: '#000000' }}>SOCIAL SECURITY ADMIN</div>
                <div style={{ fontFamily: 'Inter, Poppins', fontSize: 30, fontWeight: 600, color: '#000000', marginTop: 8 }}>Pay to the order of:</div>
                <div style={{ fontFamily: 'JetBrains Mono', fontSize: 48, fontWeight: 800, color: '#ff0000', marginTop: 6 }}>$ 2,500.00</div>
                <div style={{ fontFamily: 'Inter, Poppins', fontSize: 26, fontWeight: 700, color: '#000000', marginTop: 14, borderTop: '2px solid #9E9E9E', paddingTop: 8 }}>MARCH CHECK</div>
              </div>
            </div>

            {/* Return arrow */}
            <svg width="120" height="120" viewBox="0 0 24 24" fill="none" stroke="#ff0000" strokeWidth="2.5" style={{ opacity: arrowIn, transform: `scale(${arrowIn})` }}>
              <polyline points="9 14 4 9 9 4" />
              <path d="M20 20v-7a4 4 0 0 0-4-4H4" />
            </svg>

            {/* Calendar */}
            <div style={{ opacity: calIn, transform: `translateY(${(1 - calIn) * 50}px) scale(${0.9 + calIn * 0.1})` }}>
              <div style={{ width: 200, height: 220, background: '#000000', borderRadius: 16, overflow: 'hidden', boxShadow: '8px 8px 0 #93B5C6' }}>
                <div style={{ background: '#ff0000', color: '#F5F5F5', fontFamily: 'Inter, Poppins', fontSize: 40, fontWeight: 800, padding: '12px 0' }}>MARCH</div>
                <div style={{ fontFamily: 'Inter, Poppins', fontSize: 100, fontWeight: 900, color: '#F4CE14', marginTop: 18 }}>✕</div>
                <div style={{ fontFamily: 'Inter, Poppins', fontSize: 24, fontWeight: 600, color: '#93B5C6' }}>Do not cash</div>
              </div>
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(0,0,0,0.4)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene34_ReturnTheCheck;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35_DoNotTouch',
  durationInSeconds: 8,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35_DoNotTouch: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const warnIn = sp(8, { stiffness: 180, damping: 9 });
  const pulse = 1 + Math.sin(frame * 0.18) * 0.04;
  const textIn = sp(40);
  const flagIn = sp(70, { stiffness: 150 });
  const shake = frame < 20 ? Math.sin(frame * 4) * 6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 40%, rgba(255,0,0,0.12), transparent 60%)' }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: -150, left: -150, width: 550, height: 550, borderRadius: '50%', border: '5px solid #000000', opacity: 0.05, transform: `rotate(${frame * 0.3}deg)` }} />
        <div style={{ position: 'absolute', bottom: -180, right: -120, width: 480, height: 480, background: '#93B5C6', opacity: 0.1, borderRadius: '50%', transform: `scale(${pulse})` }} />
      </AbsoluteFill>

      {/* Motion texture — warning stripes */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="100%" height="100%">
          <defs>
            <pattern id="stripe35" width="80" height="80" patternUnits="userSpaceOnUse" patternTransform={`rotate(45) translate(${(frame * 0.5) % 80},0)`}>
              <rect width="40" height="80" fill="#000000" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#stripe35)" />
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const r = seededRandom(i + 40);
        const x = r * 1920;
        const y = (seededRandom(i + 90) * 1080 + frame * 0.6) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 8, height: 8, borderRadius: '50%', background: '#ff0000', opacity: 0.18 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ alignItems: 'center', justifyContent: 'center', transform: `translateX(${shake}px)` }}>
        <div style={{ textAlign: 'center', width: '85%' }}>
          {/* Warning triangle icon */}
          <svg width="180" height="180" viewBox="0 0 24 24" fill="none" stroke="#ff0000" strokeWidth="2" style={{ opacity: warnIn, transform: `scale(${warnIn * pulse})` }}>
            <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
            <line x1="12" y1="9" x2="12" y2="13" />
            <line x1="12" y1="17" x2="12.01" y2="17" />
          </svg>

          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 130, fontWeight: 900, color: '#000000', lineHeight: 1, marginTop: 20, opacity: textIn, transform: `translateY(${(1 - textIn) * 40}px)`, letterSpacing: -2 }}>
            DO NOT TOUCH
          </div>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 130, fontWeight: 900, color: '#ff0000', lineHeight: 1, opacity: textIn, transform: `translateY(${(1 - textIn) * 40}px)`, letterSpacing: -2 }}>
            THAT MONEY
          </div>

          <div style={{ marginTop: 40, display: 'inline-flex', alignItems: 'center', gap: 16, background: '#000000', color: '#F4CE14', fontFamily: 'Inter, Poppins', fontSize: 40, fontWeight: 800, padding: '16px 44px', borderRadius: 50, opacity: flagIn, transform: `scale(${0.85 + flagIn * 0.15})` }}>
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="#F4CE14" strokeWidth="2.5">
              <path d="M4 15s1-1 4-1 5 2 8 2 4-1 4-1V3s-1 1-4 1-5-2-8-2-4 1-4 1z" />
              <line x1="4" y1="22" x2="4" y2="15" />
            </svg>
            Cash it = Account Flagged
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 340px rgba(0,0,0,0.45)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene35_DoNotTouch;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36_BringItTogether',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene36_BringItTogether: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [0.95, 1.05], { extrapolateRight: 'clamp' });
  const lineIn = sp(10);
  const bigIn = sp(40, { stiffness: 200, damping: 10 });
  const subIn = sp(75);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(147,181,198,0.4), transparent 65%)' }} />

      {/* Shape layers — orbiting */}
      <AbsoluteFill>
        {Array.from({ length: 4 }).map((_, i) => {
          const ang = (frame * 0.4 + i * 90) * (Math.PI / 180);
          const x = 960 + Math.cos(ang) * 600;
          const y = 540 + Math.sin(ang) * 400;
          return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 160, height: 160, borderRadius: i % 2 ? '50%' : 20, border: '3px solid #000000', opacity: 0.06, transform: `rotate(${frame}deg)` }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — concentric circles */}
      <AbsoluteFill style={{ opacity: 0.05, alignItems: 'center', justifyContent: 'center' }}>
        <svg width="1200" height="1200" viewBox="0 0 1200 1200">
          {[200, 400, 600].map((r, i) => (
            <circle key={i} cx="600" cy="600" r={r + Math.sin(frame * 0.04 + i) * 15} fill="none" stroke="#000000" strokeWidth="2" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 50);
        const x = r * 1920;
        const y = (seededRandom(i + 100) * 1080 - frame * 0.4 + 1080) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 6, height: 6, borderRadius: '50%', background: '#93B5C6', opacity: 0.3 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ textAlign: 'center' }}>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 50, fontWeight: 600, color: '#000000', opacity: lineIn, transform: `translateY(${(1 - lineIn) * 30}px)` }}>
            LET'S BRING IT ALL TOGETHER
          </div>

          <div style={{ display: 'flex', alignItems: 'baseline', justifyContent: 'center', gap: 20, marginTop: 10 }}>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 320, fontWeight: 900, color: '#000000', lineHeight: 1, opacity: bigIn, transform: `scale(${0.6 + bigIn * 0.4})`, textShadow: '12px 12px 0 #93B5C6' }}>4</div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 110, fontWeight: 800, color: '#000000', opacity: bigIn }}>STEPS</div>
          </div>

          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 700, color: '#000000', opacity: subIn, transform: `translateY(${(1 - subIn) * 30}px)`, background: '#93B5C6', display: 'inline-block', padding: '14px 40px', borderRadius: 50, marginTop: 10 }}>
            To Maximize Your Survivor Benefit
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.38)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene36_BringItTogether;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37_RecapSteps12',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene37_RecapSteps12: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [15, -15], { extrapolateRight: 'clamp' });
  const card1 = sp(20, { stiffness: 90 });
  const card2 = sp(70, { stiffness: 90 });
  // growth bar for 8%
  const growth = interpolate(sp(110, { stiffness: 60 }), [0, 1], [0, 180]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'linear-gradient(120deg, rgba(147,181,198,0.3), transparent 60%)' }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: 60, right: -100, width: 420, height: 420, borderRadius: 50, border: '3px solid #000000', opacity: 0.05, transform: `rotate(${frame * 0.2}deg)` }} />
        <div style={{ position: 'absolute', bottom: -120, left: 100, width: 380, height: 380, borderRadius: '50%', background: '#93B5C6', opacity: 0.08, transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})` }} />
      </AbsoluteFill>

      {/* Motion texture */}
      <AbsoluteFill style={{ opacity: 0.045 }}>
        <svg width="100%" height="100%">
          <defs>
            <pattern id="grid37" width="75" height="75" patternUnits="userSpaceOnUse">
              <path d="M 75 0 L 0 0 0 75" fill="none" stroke="#000000" strokeWidth="1.5" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#grid37)" transform={`translate(${(frame * 0.25) % 75},0)`} />
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const r = seededRandom(i + 60);
        const x = r * 1920;
        const y = (seededRandom(i + 110) * 1080 + frame * 0.4) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 6, height: 6, borderRadius: '50%', background: '#000000', opacity: 0.12 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camPan}px)`, alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ display: 'flex', gap: 50 }}>
          {/* Card 1 */}
          <div style={{ width: 580, background: '#F5F5F5', border: '4px solid #000000', borderRadius: 24, padding: 40, opacity: card1, transform: `translateY(${(1 - card1) * 70}px)`, boxShadow: '10px 10px 0 #93B5C6' }}>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 120, fontWeight: 900, color: '#93B5C6', lineHeight: 0.9 }}>01</div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 52, fontWeight: 800, color: '#000000', marginTop: 16 }}>Widow's Age 60 Loophole</div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 40, fontWeight: 600, color: '#000000', marginTop: 20 }}>Early access if you are in crisis</div>
          </div>

          {/* Card 2 with growth graph */}
          <div style={{ width: 580, background: '#000000', borderRadius: 24, padding: 40, opacity: card2, transform: `translateY(${(1 - card2) * 70}px)`, boxShadow: '10px 10px 0 #93B5C6' }}>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 120, fontWeight: 900, color: '#93B5C6', lineHeight: 0.9 }}>02</div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 52, fontWeight: 800, color: '#F4CE14', marginTop: 16 }}>Staggered Claiming</div>
            <div style={{ display: 'flex', alignItems: 'flex-end', gap: 14, marginTop: 24, height: 200 }}>
              {[0.3, 0.5, 0.7, 1].map((h, i) => (
                <div key={i} style={{ width: 60, height: growth * h, background: '#F4CE14', borderRadius: '6px 6px 0 0' }} />
              ))}
              <div style={{ fontFamily: 'Inter, Poppins', fontSize: 70, fontWeight: 900, color: '#F4CE14', marginLeft: 10 }}>+8%<span style={{ fontSize: 30, color: '#93B5C6' }}>/yr</span></div>
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.38)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene37_RecapSteps12;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38_RecapSteps34',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene38_RecapSteps34: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [-15, 15], { extrapolateRight: 'clamp' });
  const card3 = sp(20, { stiffness: 90 });
  const card4 = sp(70, { stiffness: 90 });
  const switchAnim = interpolate(sp(110, { stiffness: 70 }), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'linear-gradient(240deg, rgba(147,181,198,0.32), transparent 60%)' }} />

      {/* Shape layers */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: -100, left: -80, width: 440, height: 440, borderRadius: '50%', border: '3px solid #000000', opacity: 0.05, transform: `rotate(${-frame * 0.2}deg)` }} />
        <div style={{ position: 'absolute', bottom: -130, right: 120, width: 360, height: 360, borderRadius: 40, background: '#93B5C6', opacity: 0.08, transform: `rotate(${frame * 0.18}deg)` }} />
      </AbsoluteFill>

      {/* Motion texture — diagonal */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="100%" height="100%">
          <defs>
            <pattern id="diag38" width="55" height="55" patternUnits="userSpaceOnUse" patternTransform={`rotate(-45) translate(0,${(frame * 0.4) % 55})`}>
              <line x1="0" y1="0" x2="55" y2="0" stroke="#000000" strokeWidth="2" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#diag38)" />
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const r = seededRandom(i + 70);
        const x = r * 1920;
        const y = (seededRandom(i + 120) * 1080 - frame * 0.4 + 1080) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 7, height: 7, borderRadius: '50%', background: '#000000', opacity: 0.12 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `translateX(${camPan}px)`, alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ display: 'flex', gap: 50 }}>
          {/* Card 3 — Working Widow Penalty */}
          <div style={{ width: 580, background: '#F5F5F5', border: '4px solid #ff0000', borderRadius: 24, padding: 40, opacity: card3, transform: `translateX(${(1 - card3) * -80}px)`, boxShadow: '10px 10px 0 #93B5C6' }}>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 120, fontWeight: 900, color: '#ff0000', lineHeight: 0.9 }}>03</div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 50, fontWeight: 800, color: '#000000', marginTop: 16 }}>Working Widow Penalty</div>
            <div style={{ display: 'flex', alignItems: 'center', gap: 16, marginTop: 24 }}>
              <svg width="60" height="60" viewBox="0 0 24 24" fill="none" stroke="#ff0000" strokeWidth="2.5">
                <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
                <line x1="12" y1="9" x2="12" y2="13" />
                <line x1="12" y1="17" x2="12.01" y2="17" />
              </svg>
              <div style={{ fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 700, color: '#000000' }}>$22k earnings limit</div>
            </div>
          </div>

          {/* Card 4 — Maximize & Switch */}
          <div style={{ width: 580, background: '#000000', borderRadius: 24, padding: 40, opacity: card4, transform: `translateX(${(1 - card4) * 80}px)`, boxShadow: '10px 10px 0 #93B5C6' }}>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 120, fontWeight: 900, color: '#93B5C6', lineHeight: 0.9 }}>04</div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 50, fontWeight: 800, color: '#F4CE14', marginTop: 16 }}>Maximize &amp; Switch</div>
            <div style={{ display: 'flex', alignItems: 'center', gap: 20, marginTop: 30 }}>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 44, fontWeight: 800, color: '#93B5C6' }}>$1,500</div>
              <svg width="60" height="44" viewBox="0 0 24 24" fill="none" stroke="#F4CE14" strokeWidth="2.5" style={{ transform: `translateX(${switchAnim * 8}px)` }}>
                <line x1="5" y1="12" x2="19" y2="12" />
                <polyline points="12 5 19 12 12 19" />
              </svg>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 56, fontWeight: 900, color: '#F4CE14', transform: `scale(${0.8 + switchAnim * 0.2})` }}>$2,500</div>
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.38)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene38_RecapSteps34;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39_YourRight',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene39_YourRight: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.06, 1], { extrapolateRight: 'clamp' });
  const line1 = sp(15);
  const line2 = sp(55);
  const heartBeat = 1 + Math.sin(frame * 0.12) * 0.05;
  const claimIn = sp(95, { stiffness: 150, damping: 9 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient — soft warm */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 30%, rgba(147,181,198,0.45), transparent 70%)' }} />

      {/* Shape layers — slow drift */}
      <AbsoluteFill>
        <div style={{ position: 'absolute', top: 80, left: 120, width: 320, height: 320, borderRadius: '50%', border: '3px solid #000000', opacity: 0.05, transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})` }} />
        <div style={{ position: 'absolute', bottom: 100, right: 150, width: 380, height: 380, borderRadius: '50%', background: '#93B5C6', opacity: 0.1, transform: `scale(${1 + Math.cos(frame * 0.025) * 0.05})` }} />
      </AbsoluteFill>

      {/* Motion texture — gentle dots */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="100%" height="100%">
          <defs>
            <pattern id="dots39" width="60" height="60" patternUnits="userSpaceOnUse" patternTransform={`translate(0,${(frame * 0.2) % 60})`}>
              <circle cx="12" cy="12" r="2.5" fill="#000000" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#dots39)" />
        </svg>
      </AbsoluteFill>

      {/* Particles — rising softly */}
      {Array.from({ length: 12 }).map((_, i) => {
        const r = seededRandom(i + 80);
        const x = r * 1920;
        const y = (seededRandom(i + 130) * 1080 - frame * 0.3 + 1080) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 5 + r * 5, height: 5 + r * 5, borderRadius: '50%', background: '#93B5C6', opacity: 0.25 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ textAlign: 'center', width: '85%' }}>
          {/* Heart icon */}
          <svg width="120" height="120" viewBox="0 0 24 24" fill="none" stroke="#000000" strokeWidth="1.8" style={{ opacity: line1, transform: `scale(${line1 * heartBeat})` }}>
            <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z" />
          </svg>

          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 56, fontWeight: 600, color: '#000000', marginTop: 30, opacity: line1, transform: `translateY(${(1 - line1) * 30}px)` }}>
            Losing a spouse is the hardest thing
          </div>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 500, color: '#000000', marginTop: 12, opacity: line2, transform: `translateY(${(1 - line2) * 30}px)` }}>
            Don't let the system take advantage of your grief
          </div>

          <div style={{ marginTop: 44, display: 'inline-block', background: '#000000', color: '#F4CE14', fontFamily: 'Inter, Poppins', fontSize: 64, fontWeight: 900, padding: '20px 56px', borderRadius: 16, opacity: claimIn, transform: `scale(${0.85 + claimIn * 0.15})`, boxShadow: '10px 10px 0 #93B5C6' }}>
            CLAIM EVERY DOLLAR YOU'RE OWED
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.36)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene39_YourRight;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40_SubscribeOutro',
  durationInSeconds: 7,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene40_SubscribeOutro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [0.96, 1.04], { extrapolateRight: 'clamp' });
  const titleIn = sp(8, { stiffness: 160, damping: 10 });
  const btnIn = sp(40, { stiffness: 180, damping: 8 });
  const btnPulse = 1 + Math.sin(frame * 0.2) * 0.04;
  const chipsIn = sp(75);

  const chips = ['A Friend', 'A Sibling', 'A Neighbor'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(147,181,198,0.4), transparent 65%)' }} />

      {/* Shape layers — orbit */}
      <AbsoluteFill>
        {Array.from({ length: 3 }).map((_, i) => {
          const ang = (frame * 0.5 + i * 120) * (Math.PI / 180);
          const x = 960 + Math.cos(ang) * 650;
          const y = 540 + Math.sin(ang) * 380;
          return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 200, height: 200, borderRadius: '50%', border: '3px solid #000000', opacity: 0.06 }} />;
        })}
      </AbsoluteFill>

      {/* Motion texture — concentric */}
      <AbsoluteFill style={{ opacity: 0.05, alignItems: 'center', justifyContent: 'center' }}>
        <svg width="1400" height="1400" viewBox="0 0 1400 1400">
          {[250, 450, 650].map((r, i) => (
            <circle key={i} cx="700" cy="700" r={r + Math.sin(frame * 0.05 + i) * 12} fill="none" stroke="#000000" strokeWidth="2" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 90);
        const x = r * 1920;
        const y = (seededRandom(i + 140) * 1080 - frame * 0.5 + 1080) % 1080;
        return <div key={i} style={{ position: 'absolute', left: x, top: y, width: 6, height: 6, borderRadius: '50%', background: '#93B5C6', opacity: 0.3 }} />;
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})`, alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ textAlign: 'center' }}>
          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 70, fontWeight: 800, color: '#000000', opacity: titleIn, transform: `translateY(${(1 - titleIn) * 35}px)` }}>
            SHARE THIS. SAVE SOMEONE.
          </div>

          {/* Subscribe button */}
          <div style={{ display: 'inline-flex', alignItems: 'center', gap: 22, background: '#ff0000', borderRadius: 60, padding: '24px 56px', marginTop: 36, opacity: btnIn, transform: `scale(${btnIn * btnPulse})`, boxShadow: '0 0 50px rgba(255,0,0,0.4)' }}>
            <svg width="64" height="64" viewBox="0 0 24 24" fill="#F5F5F5">
              <path d="M8 5v14l11-7z" />
            </svg>
            <span style={{ fontFamily: 'Inter, Poppins', fontSize: 58, fontWeight: 900, color: '#F5F5F5' }}>SUBSCRIBE</span>
          </div>

          {/* Chips */}
          <div style={{ display: 'flex', gap: 24, justifyContent: 'center', marginTop: 44, opacity: chipsIn, transform: `translateY(${(1 - chipsIn) * 40}px)` }}>
            {chips.map((c, i) => (
              <div key={i} style={{ background: '#000000', color: '#F4CE14', fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 700, padding: '14px 36px', borderRadius: 50 }}>{c}</div>
            ))}
          </div>

          <div style={{ fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 600, color: '#000000', marginTop: 40, opacity: chipsIn }}>
            I'll see you in the next one
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.38)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#000000', opacity: 0.3 }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene40_SubscribeOutro;
```

