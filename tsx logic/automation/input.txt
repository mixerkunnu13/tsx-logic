```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01NotificationFlood',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const OFFERS = [
  { tag: 'AMAZON', label: 'GREAT INDIAN FESTIVAL', accent: '#FFD166', x: -640, y: -250, rot: -7 },
  { tag: 'FLIPKART', label: 'BIG BILLION DAYS', accent: '#FF5C5C', x: 620, y: -280, rot: 6 },
  { tag: 'MEGA SALE', label: '70% तक छूट', accent: '#F4F2F2', x: -700, y: 120, rot: 5 },
  { tag: 'BANK OFFER', label: 'NO COST EMI', accent: '#9BF3D2', x: 660, y: 130, rot: -5 },
  { tag: 'FREE', label: 'BUY 1 GET 1', accent: '#FFD166', x: -60, y: 330, rot: -3 },
];

const BellIcon: React.FC<{ size: number; color: string }> = ({ size, color }) => (
  <svg width={size} height={size} viewBox="0 0 48 48" fill="none">
    <path
      d="M24 6c-7 0-12 5-12 12v9l-4 6h32l-4-6v-9c0-7-5-12-12-12z"
      stroke={color}
      strokeWidth={3.5}
      strokeLinejoin="round"
    />
    <path d="M19 37a5 5 0 0 0 10 0" stroke={color} strokeWidth={3.5} strokeLinecap="round" />
  </svg>
);

const Scene01NotificationFlood: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Beat helper — setup / build / payoff windows
  const beat = (s: number, e: number, f = 15) =>
    interpolate(frame, [s, s + f, e - f, e], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const b1 = beat(0, 260);
  const b2 = beat(255, 570);
  const b3 = beat(565, 780);
  const b4 = interpolate(frame, [775, 800], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera — slow push-in with micro drift
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.15], { extrapolateRight: 'clamp' });
  const camX = Math.sin(frame * 0.006) * 24;
  const camY = Math.cos(frame * 0.008) * 14;

  const shake = frame > 775 && frame < 800 ? Math.sin(frame * 2.2) * 9 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#525CEB', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 22% 18%, #6A74FF 0%, #525CEB 42%, #2E3596 100%)',
        }}
      />
      <AbsoluteFill
        style={{
          background: 'linear-gradient(120deg, rgba(0,0,0,0.28) 0%, rgba(0,0,0,0) 45%, rgba(0,0,0,0.34) 100%)',
        }}
      />

      {/* Punk collage layer — torn paper blocks + tape strips */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${camX * 0.4}px, ${camY * 0.4}px)` }}>
        {Array.from({ length: 9 }).map((_, i) => {
          const r = seededRandom(i + 3);
          const drift = Math.sin(frame * 0.004 + i) * 22;
          return (
            <div
              key={`collage-${i}`}
              style={{
                position: 'absolute',
                left: `${r * 92 - 4}%`,
                top: `${seededRandom(i + 21) * 88 - 4}%`,
                width: 180 + r * 340,
                height: 90 + seededRandom(i + 44) * 220,
                backgroundColor: i % 3 === 0 ? '#000000' : '#F4F2F2',
                opacity: 0.05,
                transform: `rotate(${(r - 0.5) * 34}deg) translateY(${drift}px)`,
                clipPath: 'polygon(2% 6%, 98% 0%, 96% 94%, 4% 100%)',
              }}
            />
          );
        })}
        {[0, 1, 2].map((i) => (
          <div
            key={`tape-${i}`}
            style={{
              position: 'absolute',
              left: `${12 + i * 33}%`,
              top: `${8 + i * 30}%`,
              width: 260,
              height: 46,
              backgroundColor: '#FFD166',
              opacity: 0.12,
              transform: `rotate(${i % 2 === 0 ? -18 : 14}deg)`,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Motion texture — halftone dots + scanlines */}
      <AbsoluteFill
        style={{
          backgroundImage: 'radial-gradient(#F4F2F2 1.6px, transparent 1.7px)',
          backgroundSize: '26px 26px',
          opacity: 0.09,
          transform: `translate(${(frame * 0.35) % 26}px, ${(frame * 0.22) % 26}px)`,
        }}
      />
      <AbsoluteFill
        style={{
          backgroundImage: 'repeating-linear-gradient(0deg, rgba(0,0,0,0.16) 0px, rgba(0,0,0,0.16) 2px, transparent 2px, transparent 9px)',
          opacity: 0.35,
        }}
      />

      {/* Animated shape layers — large drifting geometry */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 140,
            width: 640,
            height: 640,
            borderRadius: '50%',
            border: '3px solid #F4F2F2',
            opacity: 0.06,
            transform: `scale(${1 + Math.sin(frame * 0.012) * 0.06}) rotate(${frame * 0.08}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: 90,
            bottom: 60,
            width: 560,
            height: 560,
            backgroundColor: '#000000',
            opacity: 0.05,
            transform: `rotate(${-frame * 0.05}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 46 }).map((_, i) => {
          const r = seededRandom(i + 7);
          const speed = 0.25 + r * 0.7;
          const y = (100 + seededRandom(i + 90) * 900 - frame * speed) % 1160;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: r * 1900,
                top: y < 0 ? y + 1160 : y,
                width: 4 + r * 7,
                height: 4 + r * 7,
                backgroundColor: i % 4 === 0 ? '#FFD166' : '#F4F2F2',
                opacity: 0.16 + r * 0.24,
                transform: `rotate(${frame * 0.6 + i * 20}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill
        style={{
          transform: `scale(${camScale}) translate(${camX + shake}px, ${camY}px)`,
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        {/* BEAT 1 — phone fills with notifications */}
        <div style={{ position: 'absolute', opacity: b1, display: 'flex', alignItems: 'center', gap: 90 }}>
          <div
            style={{
              width: 330,
              height: 660,
              borderRadius: 48,
              backgroundColor: '#1B1F6B',
              border: '8px solid #F4F2F2',
              boxShadow: '0 40px 90px rgba(0,0,0,0.45)',
              transform: `translateY(${(1 - sp(10)) * 220}px) rotate(${-4 + Math.sin(frame * 0.03) * 1.4}deg)`,
              overflow: 'hidden',
              padding: 22,
            }}
          >
            {Array.from({ length: 5 }).map((_, i) => {
              const en = sp(40 + i * 14);
              return (
                <div
                  key={`np-${i}`}
                  style={{
                    height: 92,
                    marginBottom: 16,
                    borderRadius: 16,
                    backgroundColor: i % 2 === 0 ? '#F4F2F2' : '#FFD166',
                    opacity: en,
                    transform: `translateX(${(1 - en) * 260}px)`,
                    display: 'flex',
                    alignItems: 'center',
                    gap: 14,
                    padding: '0 16px',
                  }}
                >
                  <BellIcon size={34} color="#000000" />
                  <div style={{ flex: 1 }}>
                    <div style={{ height: 12, backgroundColor: '#000000', opacity: 0.75, marginBottom: 10, width: '80%' }} />
                    <div style={{ height: 10, backgroundColor: '#000000', opacity: 0.4, width: '55%' }} />
                  </div>
                </div>
              );
            })}
          </div>

          <div style={{ maxWidth: 900 }}>
            <div
              style={{
                fontFamily: "'Inter', 'Poppins', sans-serif",
                fontSize: 30,
                letterSpacing: 8,
                fontWeight: 800,
                color: '#000000',
                backgroundColor: '#FFD166',
                display: 'inline-block',
                padding: '10px 26px',
                transform: `translateY(${(1 - sp(20)) * -60}px) rotate(-1.5deg)`,
              }}
            >
              अगले कुछ हफ्ते
            </div>
            <div
              style={{
                fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                fontSize: 128,
                lineHeight: 1.08,
                color: '#F4F2F2',
                marginTop: 28,
                textShadow: '10px 10px 0 rgba(0,0,0,0.35)',
                transform: `translateX(${(1 - sp(34)) * -160}px)`,
              }}
            >
              तुम्हारा फोन
              <br />
              <span style={{ color: '#FFD166' }}>नोटिफिकेशन</span> से
              <br />
              भर जाएगा
            </div>
          </div>
        </div>

        {/* BEAT 2 — offer cards collide into frame */}
        <div style={{ position: 'absolute', opacity: b2, width: 1920, height: 1080 }}>
          {OFFERS.map((o, i) => {
            const en = sp(270 + i * 18, { damping: 14, stiffness: 90 });
            const float = Math.sin(frame * 0.03 + i) * 8;
            return (
              <div
                key={o.tag}
                style={{
                  position: 'absolute',
                  left: 960 + o.x - 250,
                  top: 540 + o.y - 90,
                  width: 500,
                  backgroundColor: '#F4F2F2',
                  border: '6px solid #000000',
                  boxShadow: '14px 14px 0 rgba(0,0,0,0.35)',
                  padding: '22px 28px',
                  transform: `scale(${0.5 + en * 0.5}) rotate(${o.rot}deg) translateY(${float}px)`,
                  opacity: en,
                }}
              >
                <div
                  style={{
                    display: 'inline-block',
                    backgroundColor: '#000000',
                    color: o.accent,
                    fontFamily: "'JetBrains Mono', monospace",
                    fontSize: 26,
                    letterSpacing: 4,
                    padding: '6px 14px',
                    marginBottom: 14,
                  }}
                >
                  {o.tag}
                </div>
                <div
                  style={{
                    fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                    fontSize: 62,
                    color: '#000000',
                    lineHeight: 1.05,
                  }}
                >
                  {o.label}
                </div>
              </div>
            );
          })}
        </div>

        {/* BEAT 3 — the discount payoff */}
        <div style={{ position: 'absolute', opacity: b3, textAlign: 'center' }}>
          <div
            style={{
              fontFamily: "'Inter', 'Poppins', sans-serif",
              fontWeight: 900,
              fontSize: 221,
              color: '#FFD166',
              letterSpacing: -6,
              transform: `scale(${0.7 + sp(575, { damping: 9, stiffness: 140 }) * 0.3})`,
              textShadow: '16px 16px 0 #000000',
            }}
          >
            70% OFF
          </div>
          <div style={{ display: 'flex', gap: 40, justifyContent: 'center', marginTop: 40 }}>
            {['NO COST EMI', 'BUY 1 GET 1', 'FLAT DISCOUNT'].map((t, i) => {
              const en = sp(610 + i * 20);
              return (
                <div
                  key={t}
                  style={{
                    fontFamily: "'JetBrains Mono', monospace",
                    fontSize: 34,
                    color: '#000000',
                    backgroundColor: '#F4F2F2',
                    padding: '14px 26px',
                    border: '4px solid #000000',
                    opacity: en,
                    transform: `translateY(${(1 - en) * 60}px) rotate(${i - 1}deg)`,
                  }}
                >
                  {t}
                </div>
              );
            })}
          </div>
        </div>

        {/* BEAT 4 — the question that flips the frame */}
        <div style={{ position: 'absolute', opacity: b4, textAlign: 'center' }}>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 170,
              color: '#F4F2F2',
              lineHeight: 1.1,
              transform: `scale(${0.85 + sp(780, { damping: 10, stiffness: 130 }) * 0.15})`,
              textShadow: '12px 12px 0 #000000',
            }}
          >
            क्या ये सच में
          </div>
          <div
            style={{
              display: 'inline-block',
              marginTop: 26,
              backgroundColor: '#000000',
              color: '#FFD166',
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 136,
              padding: '14px 54px',
              transform: `rotate(${-2 + Math.sin(frame * 0.08) * 1.2}deg)`,
            }}
          >
            कोइंसिडेंस है?
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 45%, rgba(0,0,0,0.55) 100%)',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: "'Inter', sans-serif",
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene01NotificationFlood;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02StrategyGraph',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const MONTHS = [
  { m: 'JAN', v: 0.30 },
  { m: 'FEB', v: 0.28 },
  { m: 'MAR', v: 0.33 },
  { m: 'APR', v: 0.31 },
  { m: 'MAY', v: 0.29 },
  { m: 'JUN', v: 0.34 },
  { m: 'JUL', v: 0.40 },
  { m: 'AUG', v: 0.48 },
  { m: 'SEP', v: 0.58 },
  { m: 'OCT', v: 0.82 },
  { m: 'NOV', v: 0.95 },
  { m: 'DEC', v: 0.88 },
];

const TargetIcon: React.FC<{ size: number }> = ({ size }) => (
  <svg width={size} height={size} viewBox="0 0 64 64" fill="none">
    <circle cx="32" cy="32" r="26" stroke="#F4F2F2" strokeWidth={4} />
    <circle cx="32" cy="32" r="16" stroke="#FFD166" strokeWidth={4} />
    <circle cx="32" cy="32" r="6" fill="#FF5C5C" />
  </svg>
);

const Scene02StrategyGraph: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const beat = (s: number, e: number, f = 15) =>
    interpolate(frame, [s, s + f, e - f, e], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const b1 = beat(0, 200);
  const b2 = beat(195, 640);
  const b3 = interpolate(frame, [635, 660], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera — lateral pan across the data board
  const camX = interpolate(frame, [0, durationInFrames], [40, -40], { extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, 300, durationInFrames], [1.12, 1.02, 1.08], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#525CEB', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(ellipse at 78% 82%, #7280FF 0%, #525CEB 45%, #262C82 100%)' }}
      />

      {/* Punk collage layer — xerox blocks */}
      <AbsoluteFill>
        {Array.from({ length: 7 }).map((_, i) => {
          const r = seededRandom(i + 11);
          return (
            <div
              key={`cl-${i}`}
              style={{
                position: 'absolute',
                left: `${r * 90}%`,
                top: `${seededRandom(i + 55) * 85}%`,
                width: 240 + r * 300,
                height: 140 + r * 160,
                backgroundColor: i % 2 === 0 ? '#000000' : '#FFD166',
                opacity: 0.055,
                transform: `rotate(${(r - 0.5) * 40}deg) translateX(${Math.sin(frame * 0.005 + i) * 26}px)`,
                clipPath: 'polygon(0% 4%, 97% 0%, 100% 96%, 3% 100%)',
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Motion texture — isometric grid */}
      <AbsoluteFill
        style={{
          backgroundImage:
            'repeating-linear-gradient(60deg, rgba(244,242,242,0.10) 0px, rgba(244,242,242,0.10) 1.5px, transparent 1.5px, transparent 46px), repeating-linear-gradient(-60deg, rgba(244,242,242,0.10) 0px, rgba(244,242,242,0.10) 1.5px, transparent 1.5px, transparent 46px)',
          transform: `translateY(${(frame * 0.4) % 46}px)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: 200,
            top: 90,
            width: 0,
            height: 0,
            borderLeft: '300px solid transparent',
            borderRight: '300px solid transparent',
            borderBottom: '520px solid #F4F2F2',
            opacity: 0.05,
            transform: `rotate(${frame * 0.06}deg) scale(${1 + Math.sin(frame * 0.014) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: 160,
            top: 200,
            width: 500,
            height: 500,
            borderRadius: '50%',
            backgroundColor: '#000000',
            opacity: 0.06,
            transform: `translateY(${Math.sin(frame * 0.01) * 30}px)`,
          }}
        />
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 38 }).map((_, i) => {
          const r = seededRandom(i + 13);
          const y = (r * 1080 + frame * (0.3 + r * 0.5)) % 1140;
          return (
            <div
              key={`pt-${i}`}
              style={{
                position: 'absolute',
                left: r * 1880,
                top: y,
                width: 3,
                height: 22,
                backgroundColor: '#F4F2F2',
                opacity: 0.14 + r * 0.2,
                transform: `rotate(${20 + i * 6}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill
        style={{
          transform: `scale(${camScale}) translateX(${camX}px)`,
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        {/* BEAT 1 — the refusal stamp */}
        <div style={{ position: 'absolute', opacity: b1, textAlign: 'center' }}>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 204,
              color: '#FF5C5C',
              textShadow: '14px 14px 0 #000000',
              transform: `scale(${0.6 + sp(6, { damping: 8, stiffness: 150 }) * 0.4}) rotate(-3deg)`,
            }}
          >
            नहीं भाई
          </div>
          <div
            style={{
              marginTop: 30,
              display: 'inline-block',
              backgroundColor: '#F4F2F2',
              color: '#000000',
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 96,
              padding: '16px 48px',
              border: '6px solid #000000',
              opacity: sp(40),
              transform: `translateY(${(1 - sp(40)) * 70}px) rotate(1.5deg)`,
            }}
          >
            ये सोची-समझी रणनीति है
          </div>
          <div style={{ marginTop: 44, display: 'flex', justifyContent: 'center', opacity: sp(70) }}>
            <TargetIcon size={110} />
          </div>
        </div>

        {/* BEAT 2 — 12 month growth bar chart */}
        <div style={{ position: 'absolute', opacity: b2, width: 1560 }}>
          <div
            style={{
              fontFamily: "'Inter', 'Poppins', sans-serif",
              fontSize: 30,
              letterSpacing: 7,
              fontWeight: 800,
              color: '#000000',
              backgroundColor: '#FFD166',
              display: 'inline-block',
              padding: '10px 24px',
              marginBottom: 24,
              opacity: sp(210),
            }}
          >
            INDIA GROWTH — MONTH BY MONTH
          </div>

          <div
            style={{
              display: 'flex',
              alignItems: 'flex-end',
              gap: 22,
              height: 560,
              borderBottom: '6px solid #F4F2F2',
              borderLeft: '6px solid #F4F2F2',
              padding: '0 24px 0 30px',
            }}
          >
            {MONTHS.map((mo, i) => {
              const en = sp(225 + i * 12, { damping: 13, stiffness: 100 });
              const isFestive = i >= 9;
              const pulse = isFestive ? 1 + Math.sin(frame * 0.09 + i) * 0.02 : 1;
              return (
                <div key={mo.m} style={{ flex: 1, textAlign: 'center' }}>
                  <div
                    style={{
                      fontFamily: "'JetBrains Mono', monospace",
                      fontSize: 26,
                      color: isFestive ? '#FFD166' : '#F4F2F2',
                      opacity: en,
                      marginBottom: 10,
                    }}
                  >
                    {Math.round(mo.v * en * 100)}
                  </div>
                  <div
                    style={{
                      height: 470 * mo.v * en,
                      backgroundColor: isFestive ? '#FFD166' : 'rgba(244,242,242,0.55)',
                      border: '4px solid #000000',
                      transform: `scaleY(${pulse})`,
                      transformOrigin: 'bottom',
                    }}
                  />
                  <div
                    style={{
                      fontFamily: "'JetBrains Mono', monospace",
                      fontSize: 24,
                      color: '#F4F2F2',
                      marginTop: 12,
                      opacity: 0.75,
                    }}
                  >
                    {mo.m}
                  </div>
                </div>
              );
            })}
          </div>

          <div
            style={{
              display: 'flex',
              gap: 30,
              marginTop: 34,
              opacity: sp(400),
            }}
          >
            <div
              style={{
                flex: 1,
                backgroundColor: 'rgba(0,0,0,0.35)',
                border: '4px solid #F4F2F2',
                padding: '18px 26px',
                fontFamily: "'Noto Sans Devanagari', sans-serif",
                fontSize: 43,
                color: '#F4F2F2',
              }}
            >
              JAN – JUN : वृद्धि धीमी
            </div>
            <div
              style={{
                flex: 1,
                backgroundColor: '#FFD166',
                border: '4px solid #000000',
                padding: '18px 26px',
                fontFamily: "'Noto Sans Devanagari', sans-serif",
                fontSize: 43,
                color: '#000000',
                transform: `translateY(${Math.sin(frame * 0.06) * 6}px)`,
              }}
            >
              OCT – DEC : मांग में उछाल
            </div>
          </div>
        </div>

        {/* BEAT 3 — payoff line */}
        <div style={{ position: 'absolute', opacity: b3, textAlign: 'center', maxWidth: 1500 }}>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 119,
              color: '#F4F2F2',
              lineHeight: 1.2,
              textShadow: '10px 10px 0 rgba(0,0,0,0.4)',
              transform: `translateY(${(1 - sp(645)) * 80}px)`,
            }}
          >
            पूरी अर्थव्यवस्था इन
          </div>
          <div
            style={{
              display: 'inline-block',
              marginTop: 24,
              backgroundColor: '#F4F2F2',
              color: '#000000',
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 153,
              padding: '10px 46px',
              border: '8px solid #000000',
              transform: `rotate(-1.5deg) scale(${0.9 + sp(680, { damping: 9 }) * 0.1})`,
            }}
          >
            3 महीनों पर टिकी है
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.58) 100%)' }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: "'Inter', sans-serif",
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene02StrategyGraph;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03MoneyLoop',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BuildingIcon: React.FC = () => (
  <svg width="86" height="86" viewBox="0 0 64 64" fill="none">
    <rect x="10" y="16" width="44" height="40" stroke="#000000" strokeWidth={4} />
    <path d="M32 4l24 12H8L32 4z" stroke="#000000" strokeWidth={4} strokeLinejoin="round" />
    <rect x="20" y="28" width="8" height="8" fill="#000000" />
    <rect x="36" y="28" width="8" height="8" fill="#000000" />
    <rect x="27" y="42" width="10" height="14" fill="#000000" />
  </svg>
);

const FactoryIcon: React.FC = () => (
  <svg width="86" height="86" viewBox="0 0 64 64" fill="none">
    <path d="M8 56V26l16 10V26l16 10V14h16v42H8z" stroke="#000000" strokeWidth={4} strokeLinejoin="round" />
    <rect x="46" y="4" width="8" height="10" fill="#000000" />
  </svg>
);

const WalletIcon: React.FC = () => (
  <svg width="86" height="86" viewBox="0 0 64 64" fill="none">
    <rect x="6" y="16" width="52" height="36" rx="6" stroke="#000000" strokeWidth={4} />
    <path d="M6 26h52" stroke="#000000" strokeWidth={4} />
    <circle cx="46" cy="38" r="5" fill="#000000" />
  </svg>
);

const NODES = [
  { label: 'सरकार', Icon: BuildingIcon, angle: -90, color: '#FFD166' },
  { label: 'कंपनियां', Icon: FactoryIcon, angle: 30, color: '#9BF3D2' },
  { label: 'तुम्हारी जेब', Icon: WalletIcon, angle: 150, color: '#FF5C5C' },
];

const Scene03MoneyLoop: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const beat = (s: number, e: number, f = 15) =>
    interpolate(frame, [s, s + f, e - f, e], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const b1 = beat(0, 520);
  const b2 = interpolate(frame, [515, 545], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera — slow orbit tilt
  const camRot = Math.sin(frame * 0.005) * 1.6;
  const camScale = interpolate(frame, [0, durationInFrames], [1.14, 1.02], { extrapolateRight: 'clamp' });

  const R = 290;

  return (
    <AbsoluteFill style={{ backgroundColor: '#525CEB', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(circle at 50% 45%, #6E79FF 0%, #525CEB 40%, #232A7C 100%)' }}
      />

      {/* Punk collage layer */}
      <AbsoluteFill>
        {Array.from({ length: 8 }).map((_, i) => {
          const r = seededRandom(i + 31);
          return (
            <div
              key={`col-${i}`}
              style={{
                position: 'absolute',
                left: `${r * 88}%`,
                top: `${seededRandom(i + 66) * 84}%`,
                width: 200 + r * 260,
                height: 60 + r * 140,
                backgroundColor: i % 3 === 0 ? '#FFD166' : '#F4F2F2',
                opacity: 0.05,
                transform: `rotate(${(r - 0.5) * 50}deg) translateY(${Math.cos(frame * 0.006 + i) * 20}px)`,
                clipPath: 'polygon(3% 0%, 100% 5%, 95% 100%, 0% 92%)',
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Motion texture — concentric rings */}
      <AbsoluteFill style={{ alignItems: 'center', justifyContent: 'center' }}>
        {Array.from({ length: 7 }).map((_, i) => {
          const s = 300 + i * 190 + Math.sin(frame * 0.02 + i) * 16;
          return (
            <div
              key={`ring-${i}`}
              style={{
                position: 'absolute',
                width: s,
                height: s,
                borderRadius: '50%',
                border: '2px solid #F4F2F2',
                opacity: 0.07,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: -120,
            bottom: -140,
            width: 620,
            height: 620,
            backgroundColor: '#000000',
            opacity: 0.06,
            transform: `rotate(${frame * 0.04}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: -100,
            top: -120,
            width: 540,
            height: 540,
            borderRadius: '50%',
            border: '4px solid #FFD166',
            opacity: 0.08,
            transform: `scale(${1 + Math.sin(frame * 0.011) * 0.07})`,
          }}
        />
      </AbsoluteFill>

      {/* Floating particles — currency dots orbiting the loop */}
      <AbsoluteFill style={{ alignItems: 'center', justifyContent: 'center' }}>
        {Array.from({ length: 26 }).map((_, i) => {
          const a = ((frame * 0.9 + i * (360 / 26)) * Math.PI) / 180;
          const rad = 380 + seededRandom(i + 5) * 90;
          return (
            <div
              key={`orb-${i}`}
              style={{
                position: 'absolute',
                width: 9,
                height: 9,
                borderRadius: '50%',
                backgroundColor: i % 3 === 0 ? '#FFD166' : '#F4F2F2',
                opacity: 0.35,
                transform: `translate(${Math.cos(a) * rad}px, ${Math.sin(a) * rad * 0.55}px)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill
        style={{
          transform: `scale(${camScale}) rotate(${camRot}deg)`,
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        {/* BEAT 1 — the spending loop diagram */}
        <div style={{ position: 'absolute', opacity: b1, width: 1920, height: 1080 }}>
          <div
            style={{
              position: 'absolute',
              top: 70,
              left: 0,
              width: '100%',
              textAlign: 'center',
              opacity: sp(8),
            }}
          >
            <div
              style={{
                display: 'inline-block',
                backgroundColor: '#000000',
                color: '#FFD166',
                fontFamily: "'Inter', 'Poppins', sans-serif",
                fontSize: 30,
                fontWeight: 800,
                letterSpacing: 8,
                padding: '10px 26px',
              }}
            >
              THE SPENDING LOOP
            </div>
          </div>

          {/* rotating connector ring */}
          <div
            style={{
              position: 'absolute',
              left: 960 - R,
              top: 560 - R,
              width: R * 2,
              height: R * 2,
              borderRadius: '50%',
              border: '6px dashed rgba(244,242,242,0.45)',
              transform: `rotate(${frame * 0.5}deg)`,
              opacity: sp(20),
            }}
          />

          {NODES.map((n, i) => {
            const en = sp(40 + i * 24, { damping: 13, stiffness: 95 });
            const a = (n.angle * Math.PI) / 180;
            const x = 960 + Math.cos(a) * R;
            const y = 560 + Math.sin(a) * R;
            const float = Math.sin(frame * 0.045 + i * 1.4) * 9;
            return (
              <div
                key={n.label}
                style={{
                  position: 'absolute',
                  left: x - 175,
                  top: y - 110 + float,
                  width: 350,
                  backgroundColor: n.color,
                  border: '6px solid #000000',
                  boxShadow: '12px 12px 0 rgba(0,0,0,0.35)',
                  padding: '24px 20px',
                  textAlign: 'center',
                  opacity: en,
                  transform: `scale(${0.6 + en * 0.4}) rotate(${i % 2 === 0 ? -2 : 2}deg)`,
                }}
              >
                <n.Icon />
                <div
                  style={{
                    fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                    fontSize: 66,
                    color: '#000000',
                    marginTop: 10,
                    lineHeight: 1,
                  }}
                >
                  {n.label}
                </div>
                <div
                  style={{
                    fontFamily: "'JetBrains Mono', monospace",
                    fontSize: 25,
                    color: '#000000',
                    opacity: 0.7,
                    marginTop: 8,
                  }}
                >
                  WANTS YOU TO SPEND
                </div>
              </div>
            );
          })}

          <div
            style={{
              position: 'absolute',
              left: 960 - 200,
              top: 560 - 70,
              width: 400,
              textAlign: 'center',
              opacity: sp(160),
            }}
          >
            <div
              style={{
                fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                fontSize: 96,
                color: '#F4F2F2',
                textShadow: '8px 8px 0 #000000',
                transform: `scale(${1 + Math.sin(frame * 0.07) * 0.03})`,
              }}
            >
              खर्च करो
            </div>
          </div>
        </div>

        {/* BEAT 2 — the hidden truth teaser cards */}
        <div style={{ position: 'absolute', opacity: b2, textAlign: 'center' }}>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 111,
              color: '#F4F2F2',
              marginBottom: 44,
              textShadow: '10px 10px 0 rgba(0,0,0,0.4)',
              transform: `translateY(${(1 - sp(530)) * 60}px)`,
            }}
          >
            इस वीडियो में तुम समझोगे
          </div>
          <div style={{ display: 'flex', gap: 36, justifyContent: 'center' }}>
            {[
              { n: '01', t: 'फेस्टिव सीजन का असली खेल' },
              { n: '02', t: 'स्मार्ट बजटिंग कैसे करें' },
              { n: '03', t: 'नो कॉस्ट EMI का सच' },
            ].map((c, i) => {
              const en = sp(560 + i * 22, { damping: 13, stiffness: 100 });
              return (
                <div
                  key={c.n}
                  style={{
                    width: 420,
                    backgroundColor: i === 2 ? '#FF5C5C' : '#F4F2F2',
                    border: '6px solid #000000',
                    boxShadow: '14px 14px 0 rgba(0,0,0,0.35)',
                    padding: '30px 26px',
                    opacity: en,
                    transform: `translateY(${(1 - en) * 120}px) rotate(${i - 1}deg)`,
                  }}
                >
                  <div
                    style={{
                      fontFamily: "'JetBrains Mono', monospace",
                      fontSize: 76,
                      color: '#000000',
                      opacity: 0.35,
                      lineHeight: 1,
                    }}
                  >
                    {c.n}
                  </div>
                  <div
                    style={{
                      fontFamily: "'Noto Sans Devanagari', sans-serif",
                      fontWeight: 700,
                      fontSize: 51,
                      color: '#000000',
                      marginTop: 14,
                      lineHeight: 1.2,
                    }}
                  >
                    {c.t}
                  </div>
                </div>
              );
            })}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 44%, rgba(0,0,0,0.56) 100%)' }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: "'Inter', sans-serif",
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene03MoneyLoop;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04ChannelIdent',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BellRing: React.FC<{ tilt: number }> = ({ tilt }) => (
  <svg width="72" height="72" viewBox="0 0 48 48" fill="none" style={{ transform: `rotate(${tilt}deg)` }}>
    <path d="M24 6c-7 0-12 5-12 12v9l-4 6h32l-4-6v-9c0-7-5-12-12-12z" fill="#000000" />
    <path d="M19 37a5 5 0 0 0 10 0" stroke="#000000" strokeWidth={4} strokeLinecap="round" />
  </svg>
);

const PlayIcon: React.FC = () => (
  <svg width="64" height="64" viewBox="0 0 64 64" fill="none">
    <rect x="4" y="12" width="56" height="40" rx="10" fill="#FF5C5C" stroke="#000000" strokeWidth={4} />
    <path d="M27 24l14 8-14 8V24z" fill="#F4F2F2" />
  </svg>
);

const TrapIcon: React.FC = () => (
  <svg width="80" height="80" viewBox="0 0 64 64" fill="none">
    <path d="M8 44h48" stroke="#000000" strokeWidth={5} strokeLinecap="round" />
    <path d="M14 44V26l18 12 18-12v18" stroke="#000000" strokeWidth={5} strokeLinejoin="round" />
    <circle cx="32" cy="16" r="6" fill="#000000" />
  </svg>
);

const Scene04ChannelIdent: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const beat = (s: number, e: number, f = 15) =>
    interpolate(frame, [s, s + f, e - f, e], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const b1 = beat(0, 300);
  const b2 = beat(295, 620);
  const b3 = interpolate(frame, [615, 645], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera — tilt drift with slight pull-back
  const camScale = interpolate(frame, [0, durationInFrames], [1.12, 1.03], { extrapolateRight: 'clamp' });
  const camY = Math.sin(frame * 0.007) * 18;

  const bellTilt = frame > 615 ? Math.sin(frame * 0.5) * 14 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#525CEB', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{ background: 'linear-gradient(160deg, #7C86FF 0%, #525CEB 45%, #1F2578 100%)' }}
      />

      {/* Punk collage layer — cut-out strips */}
      <AbsoluteFill>
        {Array.from({ length: 10 }).map((_, i) => {
          const r = seededRandom(i + 71);
          return (
            <div
              key={`strip-${i}`}
              style={{
                position: 'absolute',
                left: `${r * 90 - 5}%`,
                top: `${seededRandom(i + 17) * 92}%`,
                width: 320 + r * 420,
                height: 34 + r * 40,
                backgroundColor: i % 2 === 0 ? '#F4F2F2' : '#FFD166',
                opacity: 0.06,
                transform: `rotate(${(r - 0.5) * 26}deg) translateX(${Math.sin(frame * 0.007 + i) * 30}px)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Motion texture — diagonal stripes */}
      <AbsoluteFill
        style={{
          backgroundImage:
            'repeating-linear-gradient(45deg, rgba(0,0,0,0.18) 0px, rgba(0,0,0,0.18) 12px, transparent 12px, transparent 34px)',
          opacity: 0.4,
          transform: `translateX(${(frame * 0.6) % 46}px)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: 640,
            top: 120,
            width: 700,
            height: 700,
            borderRadius: '50%',
            backgroundColor: '#FFD166',
            opacity: 0.07,
            transform: `scale(${1 + Math.sin(frame * 0.013) * 0.08})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: 100,
            bottom: 80,
            width: 380,
            height: 380,
            border: '5px solid #F4F2F2',
            opacity: 0.07,
            transform: `rotate(${frame * 0.07}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 40 }).map((_, i) => {
          const r = seededRandom(i + 23);
          const y = (r * 1080 - frame * (0.35 + r * 0.6) + 1200) % 1200;
          return (
            <div
              key={`sp-${i}`}
              style={{
                position: 'absolute',
                left: r * 1900,
                top: y,
                width: 12,
                height: 12,
                opacity: 0.2 + r * 0.2,
              }}
            >
              <div style={{ position: 'absolute', left: 4, top: 0, width: 4, height: 12, backgroundColor: '#F4F2F2' }} />
              <div style={{ position: 'absolute', left: 0, top: 4, width: 12, height: 4, backgroundColor: '#F4F2F2' }} />
            </div>
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill
        style={{
          transform: `scale(${camScale}) translateY(${camY}px)`,
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        {/* BEAT 1 — host identity card */}
        <div style={{ position: 'absolute', opacity: b1, display: 'flex', alignItems: 'center', gap: 70 }}>
          <div
            style={{
              width: 360,
              height: 360,
              backgroundColor: '#F4F2F2',
              border: '10px solid #000000',
              boxShadow: '20px 20px 0 rgba(0,0,0,0.35)',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              transform: `rotate(${-4 + Math.sin(frame * 0.03) * 1.5}deg) scale(${0.7 + sp(8, { damping: 10 }) * 0.3})`,
            }}
          >
            <div style={{ textAlign: 'center' }}>
              <div
                style={{
                  fontFamily: "'Inter', 'Poppins', sans-serif",
                  fontWeight: 900,
                  fontSize: 128,
                  color: '#000000',
                  lineHeight: 1,
                }}
              >
                PB
              </div>
              <div
                style={{
                  fontFamily: "'JetBrains Mono', monospace",
                  fontSize: 25,
                  letterSpacing: 5,
                  color: '#000000',
                  marginTop: 12,
                }}
              >
                HQ
              </div>
            </div>
          </div>

          <div>
            <div
              style={{
                fontFamily: "'Noto Sans Devanagari', sans-serif",
                fontSize: 46,
                color: '#F4F2F2',
                opacity: sp(30),
              }}
            >
              मेरा नाम
            </div>
            <div
              style={{
                fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                fontSize: 187,
                color: '#FFD166',
                lineHeight: 1,
                textShadow: '12px 12px 0 #000000',
                transform: `translateX(${(1 - sp(40)) * -140}px)`,
              }}
            >
              कुनाल
            </div>
            <div
              style={{
                display: 'inline-block',
                marginTop: 22,
                backgroundColor: '#000000',
                color: '#F4F2F2',
                fontFamily: "'Inter', 'Poppins', sans-serif",
                fontWeight: 800,
                fontSize: 42,
                letterSpacing: 4,
                padding: '12px 28px',
                opacity: sp(70),
              }}
            >
              ProBuffet HQ
            </div>
          </div>
        </div>

        {/* BEAT 2 — promise cards */}
        <div style={{ position: 'absolute', opacity: b2, textAlign: 'center' }}>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 102,
              color: '#F4F2F2',
              marginBottom: 46,
              textShadow: '10px 10px 0 rgba(0,0,0,0.4)',
            }}
          >
            होशियारी से खर्च · ट्रैप से बचाव
          </div>
          <div style={{ display: 'flex', gap: 42, justifyContent: 'center' }}>
            {[
              { t: 'FESTIVE TRAP', s: 'पहचानो', Icon: TrapIcon, bg: '#FF5C5C' },
              { t: 'SMART BUDGET', s: 'बनाओ', Icon: PlayIcon, bg: '#9BF3D2' },
            ].map((c, i) => {
              const en = sp(320 + i * 26, { damping: 13, stiffness: 100 });
              return (
                <div
                  key={c.t}
                  style={{
                    width: 520,
                    backgroundColor: c.bg,
                    border: '7px solid #000000',
                    boxShadow: '16px 16px 0 rgba(0,0,0,0.35)',
                    padding: '34px 30px',
                    opacity: en,
                    transform: `translateY(${(1 - en) * 140}px) rotate(${i === 0 ? -2.5 : 2.5}deg)`,
                  }}
                >
                  <c.Icon />
                  <div
                    style={{
                      fontFamily: "'Inter', 'Poppins', sans-serif",
                      fontWeight: 900,
                      fontSize: 55,
                      color: '#000000',
                      marginTop: 16,
                    }}
                  >
                    {c.t}
                  </div>
                  <div
                    style={{
                      fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                      fontSize: 68,
                      color: '#000000',
                      marginTop: 6,
                    }}
                  >
                    {c.s}
                  </div>
                </div>
              );
            })}
          </div>
        </div>

        {/* BEAT 3 — subscribe payoff */}
        <div style={{ position: 'absolute', opacity: b3, textAlign: 'center' }}>
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 26,
              backgroundColor: '#FF5C5C',
              border: '8px solid #000000',
              boxShadow: '18px 18px 0 rgba(0,0,0,0.4)',
              padding: '26px 54px',
              transform: `scale(${0.8 + sp(625, { damping: 9, stiffness: 140 }) * 0.2}) rotate(-1.5deg)`,
            }}
          >
            <BellRing tilt={bellTilt} />
            <div
              style={{
                fontFamily: "'Inter', 'Poppins', sans-serif",
                fontWeight: 900,
                fontSize: 111,
                color: '#F4F2F2',
                letterSpacing: 2,
              }}
            >
              SUBSCRIBE
            </div>
          </div>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 128,
              color: '#F4F2F2',
              marginTop: 48,
              textShadow: '10px 10px 0 #000000',
              opacity: sp(700),
              transform: `translateY(${(1 - sp(700)) * 60}px)`,
            }}
          >
            चलिए शुरू करते हैं
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.58) 100%)' }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: "'Inter', sans-serif",
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene04ChannelIdent;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05SurveyData',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const PersonGlyph: React.FC<{ fill: string; opacity: number }> = ({ fill, opacity }) => (
  <svg width="46" height="60" viewBox="0 0 24 32" fill="none" style={{ opacity }}>
    <circle cx="12" cy="7" r="6" fill={fill} />
    <path d="M2 31c0-6 4.5-10 10-10s10 4 10 10H2z" fill={fill} />
  </svg>
);

const CartIcon: React.FC = () => (
  <svg width="90" height="90" viewBox="0 0 64 64" fill="none">
    <path d="M6 10h8l7 28h28" stroke="#000000" strokeWidth={5} strokeLinecap="round" strokeLinejoin="round" />
    <path d="M16 18h40l-5 16H21" stroke="#000000" strokeWidth={5} strokeLinejoin="round" />
    <circle cx="24" cy="50" r="4.5" fill="#000000" />
    <circle cx="44" cy="50" r="4.5" fill="#000000" />
  </svg>
);

const Scene05SurveyData: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const beat = (s: number, e: number, f = 15) =>
    interpolate(frame, [s, s + f, e - f, e], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const b1 = beat(0, 250);
  const b2 = beat(245, 600);
  const b3 = interpolate(frame, [595, 625], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Counters
  const surveyCount = Math.round(interpolate(frame, [40, 200], [0, 5700], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const pct = Math.round(interpolate(frame, [270, 430], [0, 45], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Camera — dolly in on the data grid
  const camScale = interpolate(frame, [0, 600, durationInFrames], [1.02, 1.10, 1.16], { extrapolateRight: 'clamp' });
  const camX = Math.sin(frame * 0.005) * 20;

  return (
    <AbsoluteFill style={{ backgroundColor: '#525CEB', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(circle at 30% 80%, #6B76FF 0%, #525CEB 44%, #232977 100%)' }}
      />

      {/* Punk collage layer */}
      <AbsoluteFill>
        {Array.from({ length: 8 }).map((_, i) => {
          const r = seededRandom(i + 91);
          return (
            <div
              key={`pc-${i}`}
              style={{
                position: 'absolute',
                left: `${r * 86}%`,
                top: `${seededRandom(i + 39) * 82}%`,
                width: 260 + r * 320,
                height: 120 + r * 180,
                backgroundColor: i % 3 === 0 ? '#9BF3D2' : '#000000',
                opacity: 0.05,
                transform: `rotate(${(r - 0.5) * 44}deg) translateY(${Math.sin(frame * 0.005 + i) * 24}px)`,
                clipPath: 'polygon(4% 0%, 100% 6%, 94% 100%, 0% 90%)',
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Motion texture — dot matrix */}
      <AbsoluteFill
        style={{
          backgroundImage: 'radial-gradient(rgba(244,242,242,0.5) 1.4px, transparent 1.5px)',
          backgroundSize: '34px 34px',
          opacity: 0.12,
          transform: `translate(${(-frame * 0.3) % 34}px, ${(frame * 0.3) % 34}px)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            right: 120,
            top: 100,
            width: 620,
            height: 620,
            borderRadius: '50%',
            border: '6px solid #FFD166',
            opacity: 0.08,
            transform: `scale(${1 + Math.sin(frame * 0.01) * 0.06}) rotate(${frame * 0.03}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: 60,
            bottom: 40,
            width: 460,
            height: 460,
            backgroundColor: '#F4F2F2',
            opacity: 0.05,
            transform: `rotate(${-frame * 0.05}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 34 }).map((_, i) => {
          const r = seededRandom(i + 3);
          const y = (r * 1080 + frame * (0.4 + r * 0.5)) % 1160;
          return (
            <div
              key={`fp-${i}`}
              style={{
                position: 'absolute',
                left: r * 1890,
                top: y,
                width: 0,
                height: 0,
                borderLeft: '7px solid transparent',
                borderRight: '7px solid transparent',
                borderBottom: `12px solid ${i % 3 === 0 ? '#FFD166' : '#F4F2F2'}`,
                opacity: 0.22,
                transform: `rotate(${frame + i * 30}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill
        style={{
          transform: `scale(${camScale}) translateX(${camX}px)`,
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        {/* BEAT 1 — survey sample counter */}
        <div style={{ position: 'absolute', opacity: b1, textAlign: 'center' }}>
          <div style={{ display: 'flex', justifyContent: 'center', marginBottom: 22, opacity: sp(6) }}>
            <CartIcon />
          </div>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontSize: 51,
              color: '#F4F2F2',
              opacity: 0.9,
            }}
          >
            हाल के सर्वे में शामिल भारतीय खरीदार
          </div>
          <div
            style={{
              fontFamily: "'JetBrains Mono', monospace",
              fontWeight: 700,
              fontSize: 221,
              color: '#FFD166',
              lineHeight: 1,
              marginTop: 16,
              textShadow: '14px 14px 0 #000000',
              transform: `scale(${0.85 + sp(20, { damping: 10 }) * 0.15})`,
            }}
          >
            {surveyCount.toLocaleString('en-IN')}+
          </div>
          <div
            style={{
              display: 'inline-block',
              marginTop: 30,
              backgroundColor: '#F4F2F2',
              color: '#000000',
              fontFamily: "'Inter', 'Poppins', sans-serif",
              fontWeight: 800,
              fontSize: 34,
              letterSpacing: 6,
              padding: '12px 30px',
              border: '5px solid #000000',
              opacity: sp(120),
            }}
          >
            NATIONWIDE SURVEY
          </div>
        </div>

        {/* BEAT 2 — 45% pictograph grid */}
        <div style={{ position: 'absolute', opacity: b2, width: 1600 }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 60 }}>
            <div style={{ width: 560, flexShrink: 0 }}>
              <div
                style={{
                  fontFamily: "'Inter', 'Poppins', sans-serif",
                  fontWeight: 900,
                  fontSize: 238,
                  color: '#FFD166',
                  lineHeight: 0.95,
                  textShadow: '16px 16px 0 #000000',
                  transform: `scale(${0.8 + sp(255, { damping: 10 }) * 0.2})`,
                }}
              >
                {pct}%
              </div>
              <div
                style={{
                  fontFamily: "'Noto Sans Devanagari', sans-serif",
                  fontWeight: 700,
                  fontSize: 51,
                  color: '#F4F2F2',
                  marginTop: 18,
                  lineHeight: 1.25,
                  opacity: sp(300),
                }}
              >
                लोग इस साल पिछले साल से
                <br />
                ज्यादा festive खर्च करेंगे
              </div>
            </div>

            <div
              style={{
                flex: 1,
                display: 'flex',
                flexWrap: 'wrap',
                gap: 14,
                backgroundColor: 'rgba(0,0,0,0.28)',
                border: '5px solid #F4F2F2',
                padding: 28,
              }}
            >
              {Array.from({ length: 100 }).map((_, i) => {
                const on = i < 45;
                const en = interpolate(frame, [270 + i * 3, 288 + i * 3], [0, 1], {
                  extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
                });
                return (
                  <PersonGlyph
                    key={`pg-${i}`}
                    fill={on ? '#FFD166' : '#F4F2F2'}
                    opacity={on ? en : 0.18}
                  />
                );
              })}
            </div>
          </div>
        </div>

        {/* BEAT 3 — the takeaway */}
        <div style={{ position: 'absolute', opacity: b3, textAlign: 'center', maxWidth: 1550 }}>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 111,
              color: '#F4F2F2',
              lineHeight: 1.22,
              textShadow: '10px 10px 0 rgba(0,0,0,0.4)',
              transform: `translateY(${(1 - sp(605)) * 70}px)`,
            }}
          >
            यही लोग GDP के आंकड़े
          </div>
          <div
            style={{
              display: 'inline-block',
              marginTop: 26,
              backgroundColor: '#FFD166',
              color: '#000000',
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 145,
              padding: '12px 50px',
              border: '8px solid #000000',
              transform: `rotate(-1.5deg) scale(${0.9 + sp(640, { damping: 9 }) * 0.1})`,
            }}
          >
            ऊपर ले जाते हैं
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 45%, rgba(0,0,0,0.55) 100%)' }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: "'Inter', sans-serif",
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene05SurveyData;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06FmcgSystem',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BoxIcon: React.FC<{ color: string }> = ({ color }) => (
  <svg width="70" height="70" viewBox="0 0 64 64" fill="none">
    <path d="M32 6l24 12v28L32 58 8 46V18L32 6z" stroke={color} strokeWidth={4} strokeLinejoin="round" />
    <path d="M8 18l24 12 24-12M32 30v28" stroke={color} strokeWidth={4} />
  </svg>
);

const ChartUpIcon: React.FC<{ color: string }> = ({ color }) => (
  <svg width="70" height="70" viewBox="0 0 64 64" fill="none">
    <path d="M8 52h48" stroke={color} strokeWidth={4} strokeLinecap="round" />
    <path d="M14 42l12-14 10 8 14-20" stroke={color} strokeWidth={5} strokeLinecap="round" strokeLinejoin="round" />
    <path d="M40 16h10v10" stroke={color} strokeWidth={5} strokeLinecap="round" strokeLinejoin="round" />
  </svg>
);

const BankIcon: React.FC<{ color: string }> = ({ color }) => (
  <svg width="70" height="70" viewBox="0 0 64 64" fill="none">
    <path d="M6 24L32 8l26 16" stroke={color} strokeWidth={4} strokeLinejoin="round" />
    <path d="M12 26v22M24 26v22M40 26v22M52 26v22" stroke={color} strokeWidth={4} />
    <path d="M6 54h52" stroke={color} strokeWidth={4} strokeLinecap="round" />
  </svg>
);

const CoinIcon: React.FC<{ color: string }> = ({ color }) => (
  <svg width="70" height="70" viewBox="0 0 64 64" fill="none">
    <circle cx="32" cy="32" r="24" stroke={color} strokeWidth={4} />
    <path d="M22 20h20M22 28h20M28 20c8 0 8 16 0 16h-6l16 14" stroke={color} strokeWidth={4} strokeLinecap="round" strokeLinejoin="round" />
  </svg>
);

const NODES = [
  { t: 'सरकार', s: 'TAX REVENUE', Icon: BankIcon },
  { t: 'कंपनियां', s: 'SALES + PROFIT', Icon: BoxIcon },
  { t: 'STOCK MARKET', s: 'VALUATIONS', Icon: ChartUpIcon },
  { t: 'तुम', s: 'YOUR WALLET', Icon: CoinIcon },
];

const Scene06FmcgSystem: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const beat = (s: number, e: number, f = 15) =>
    interpolate(frame, [s, s + f, e - f, e], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const b1 = beat(0, 330);
  const b2 = beat(325, 660);
  const b3 = interpolate(frame, [655, 685], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const lowPct = interpolate(frame, [50, 190], [0, 9], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const highPct = interpolate(frame, [60, 210], [0, 11], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera — gentle tilt + pan
  const camScale = interpolate(frame, [0, durationInFrames], [1.10, 1.02], { extrapolateRight: 'clamp' });
  const camRot = Math.sin(frame * 0.006) * 1.2;

  return (
    <AbsoluteFill style={{ backgroundColor: '#525CEB', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{ background: 'linear-gradient(210deg, #6C77FF 0%, #525CEB 50%, #1E2472 100%)' }}
      />

      {/* Punk collage layer */}
      <AbsoluteFill>
        {Array.from({ length: 9 }).map((_, i) => {
          const r = seededRandom(i + 61);
          return (
            <div
              key={`cg-${i}`}
              style={{
                position: 'absolute',
                left: `${r * 92 - 6}%`,
                top: `${seededRandom(i + 12) * 88}%`,
                width: 220 + r * 380,
                height: 100 + r * 200,
                backgroundColor: i % 4 === 0 ? '#FF5C5C' : '#F4F2F2',
                opacity: 0.05,
                transform: `rotate(${(r - 0.5) * 38}deg) translateX(${Math.cos(frame * 0.006 + i) * 22}px)`,
                clipPath: 'polygon(0% 8%, 96% 0%, 100% 92%, 5% 100%)',
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Motion texture — moving grid */}
      <AbsoluteFill
        style={{
          backgroundImage:
            'linear-gradient(rgba(244,242,242,0.10) 1.5px, transparent 1.5px), linear-gradient(90deg, rgba(244,242,242,0.10) 1.5px, transparent 1.5px)',
          backgroundSize: '80px 80px',
          transform: `translate(${(frame * 0.35) % 80}px, ${(-frame * 0.25) % 80}px)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: 380,
            top: 60,
            width: 800,
            height: 800,
            borderRadius: '50%',
            backgroundColor: '#9BF3D2',
            opacity: 0.05,
            transform: `scale(${1 + Math.sin(frame * 0.009) * 0.07})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: 60,
            bottom: 90,
            width: 420,
            height: 420,
            border: '6px solid #FFD166',
            opacity: 0.09,
            transform: `rotate(${frame * 0.06}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 36 }).map((_, i) => {
          const r = seededRandom(i + 47);
          const y = (r * 1080 - frame * (0.3 + r * 0.55) + 1200) % 1200;
          return (
            <div
              key={`fl-${i}`}
              style={{
                position: 'absolute',
                left: r * 1900,
                top: y,
                width: 8,
                height: 8,
                borderRadius: '50%',
                border: '2px solid #F4F2F2',
                opacity: 0.25,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill
        style={{
          transform: `scale(${camScale}) rotate(${camRot}deg)`,
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        {/* BEAT 1 — FMCG demand gauge */}
        <div style={{ position: 'absolute', opacity: b1, textAlign: 'center', width: 1500 }}>
          <div
            style={{
              display: 'inline-block',
              backgroundColor: '#000000',
              color: '#9BF3D2',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 30,
              letterSpacing: 6,
              padding: '10px 26px',
              opacity: sp(6),
            }}
          >
            FMCG DEMAND · AUG → NOV
          </div>

          <div
            style={{
              display: 'flex',
              alignItems: 'flex-end',
              justifyContent: 'center',
              gap: 40,
              marginTop: 44,
            }}
          >
            <div
              style={{
                fontFamily: "'Inter', 'Poppins', sans-serif",
                fontWeight: 900,
                fontSize: 204,
                color: '#F4F2F2',
                textShadow: '12px 12px 0 #000000',
                lineHeight: 1,
              }}
            >
              {lowPct.toFixed(0)}%
            </div>
            <div
              style={{
                fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                fontSize: 85,
                color: '#FFD166',
                paddingBottom: 24,
              }}
            >
              से
            </div>
            <div
              style={{
                fontFamily: "'Inter', 'Poppins', sans-serif",
                fontWeight: 900,
                fontSize: 204,
                color: '#FFD166',
                textShadow: '12px 12px 0 #000000',
                lineHeight: 1,
              }}
            >
              {highPct.toFixed(0)}%
            </div>
          </div>

          {/* growth track */}
          <div
            style={{
              marginTop: 46,
              height: 46,
              backgroundColor: 'rgba(0,0,0,0.35)',
              border: '5px solid #F4F2F2',
              position: 'relative',
              overflow: 'hidden',
            }}
          >
            <div
              style={{
                position: 'absolute',
                left: 0,
                top: 0,
                bottom: 0,
                width: `${interpolate(frame, [70, 250], [0, 82], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}%`,
                backgroundColor: '#FFD166',
              }}
            />
            <div
              style={{
                position: 'absolute',
                inset: 0,
                backgroundImage:
                  'repeating-linear-gradient(45deg, rgba(0,0,0,0.25) 0px, rgba(0,0,0,0.25) 10px, transparent 10px, transparent 22px)',
                transform: `translateX(${(frame * 0.9) % 32}px)`,
              }}
            />
          </div>

          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontWeight: 700,
              fontSize: 47,
              color: '#F4F2F2',
              marginTop: 30,
              opacity: sp(200),
            }}
          >
            बेहतर क्रय शक्ति · फेस्टिव सीजन को सपोर्ट
          </div>
        </div>

        {/* BEAT 2 — the whole system benefits */}
        <div style={{ position: 'absolute', opacity: b2, width: 1720 }}>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 94,
              color: '#F4F2F2',
              textAlign: 'center',
              marginBottom: 44,
              textShadow: '8px 8px 0 rgba(0,0,0,0.4)',
              opacity: sp(335),
            }}
          >
            ये सिर्फ तुम्हारे लिए नहीं — पूरे सिस्टम के लिए है
          </div>
          <div style={{ display: 'flex', gap: 28, justifyContent: 'center' }}>
            {NODES.map((n, i) => {
              const en = sp(355 + i * 20, { damping: 13, stiffness: 100 });
              const isYou = i === 3;
              return (
                <div
                  key={n.t}
                  style={{
                    width: 390,
                    backgroundColor: isYou ? '#FF5C5C' : '#F4F2F2',
                    border: '6px solid #000000',
                    boxShadow: '14px 14px 0 rgba(0,0,0,0.35)',
                    padding: '30px 24px',
                    opacity: en,
                    transform: `translateY(${(1 - en) * 130}px) rotate(${(i % 2 === 0 ? -1.5 : 1.5)}deg)`,
                  }}
                >
                  <n.Icon color="#000000" />
                  <div
                    style={{
                      fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                      fontSize: 62,
                      color: '#000000',
                      marginTop: 14,
                      lineHeight: 1.05,
                    }}
                  >
                    {n.t}
                  </div>
                  <div
                    style={{
                      fontFamily: "'JetBrains Mono', monospace",
                      fontSize: 25,
                      color: '#000000',
                      opacity: 0.68,
                      marginTop: 10,
                      letterSpacing: 2,
                    }}
                  >
                    {n.s}
                  </div>
                </div>
              );
            })}
          </div>
        </div>

        {/* BEAT 3 — the pivot question */}
        <div style={{ position: 'absolute', opacity: b3, textAlign: 'center', maxWidth: 1600 }}>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 119,
              color: '#F4F2F2',
              lineHeight: 1.2,
              textShadow: '10px 10px 0 rgba(0,0,0,0.42)',
              transform: `translateY(${(1 - sp(665)) * 70}px)`,
            }}
          >
            सवाल ये है —
          </div>
          <div
            style={{
              display: 'inline-block',
              marginTop: 28,
              backgroundColor: '#F4F2F2',
              color: '#000000',
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontWeight: 700,
              fontSize: 76,
              padding: '22px 44px',
              border: '7px solid #000000',
              lineHeight: 1.3,
              transform: `rotate(-1deg) scale(${0.92 + sp(700, { damping: 9 }) * 0.08})`,
            }}
          >
            तुम इस ग्रोथ का हिस्सा बनकर अपनी
            <br />
            financial health खराब कर रहे हो, या
            <br />
            smart तरीके से फायदा ले रहे हो?
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 43%, rgba(0,0,0,0.57) 100%)' }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: "'Inter', sans-serif",
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene06FmcgSystem;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07RajStory',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const CardIcon: React.FC = () => (
  <svg width="96" height="96" viewBox="0 0 64 64" fill="none">
    <rect x="4" y="14" width="56" height="36" rx="6" fill="#F4F2F2" stroke="#000000" strokeWidth={4} />
    <rect x="4" y="22" width="56" height="8" fill="#000000" />
    <rect x="12" y="38" width="18" height="6" fill="#000000" opacity="0.5" />
  </svg>
);

const PhoneIcon: React.FC = () => (
  <svg width="76" height="76" viewBox="0 0 64 64" fill="none">
    <rect x="18" y="4" width="28" height="56" rx="6" stroke="#000000" strokeWidth={4} />
    <path d="M28 10h8" stroke="#000000" strokeWidth={4} strokeLinecap="round" />
    <circle cx="32" cy="52" r="3" fill="#000000" />
  </svg>
);

const TvIcon: React.FC = () => (
  <svg width="76" height="76" viewBox="0 0 64 64" fill="none">
    <rect x="6" y="12" width="52" height="34" rx="4" stroke="#000000" strokeWidth={4} />
    <path d="M24 54h16M32 46v8" stroke="#000000" strokeWidth={4} strokeLinecap="round" />
  </svg>
);

const Scene07RajStory: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const beat = (s: number, e: number, f = 15) =>
    interpolate(frame, [s, s + f, e - f, e], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const b1 = beat(0, 240);
  const b2 = beat(235, 520);
  const b3 = interpolate(frame, [515, 545], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera — handheld drift + impact shake on bill reveal
  const camScale = interpolate(frame, [0, durationInFrames], [1.03, 1.12], { extrapolateRight: 'clamp' });
  const shake = frame > 20 && frame < 44 ? Math.sin(frame * 2.6) * 10 : 0;
  const camY = Math.sin(frame * 0.008) * 16;

  return (
    <AbsoluteFill style={{ backgroundColor: '#525CEB', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(ellipse at 68% 22%, #7681FF 0%, #525CEB 42%, #1D2370 100%)' }}
      />

      {/* Punk collage layer — receipt scraps */}
      <AbsoluteFill>
        {Array.from({ length: 10 }).map((_, i) => {
          const r = seededRandom(i + 101);
          return (
            <div
              key={`rc-${i}`}
              style={{
                position: 'absolute',
                left: `${r * 92 - 4}%`,
                top: `${seededRandom(i + 8) * 90}%`,
                width: 130 + r * 150,
                height: 200 + r * 160,
                backgroundColor: '#F4F2F2',
                opacity: 0.055,
                transform: `rotate(${(r - 0.5) * 46}deg) translateY(${Math.sin(frame * 0.006 + i) * 22}px)`,
                clipPath: 'polygon(0% 0%, 100% 0%, 100% 94%, 88% 100%, 74% 94%, 60% 100%, 46% 94%, 32% 100%, 16% 94%, 0% 100%)',
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Motion texture — graph paper */}
      <AbsoluteFill
        style={{
          backgroundImage:
            'linear-gradient(rgba(0,0,0,0.14) 1px, transparent 1px), linear-gradient(90deg, rgba(0,0,0,0.14) 1px, transparent 1px)',
          backgroundSize: '48px 48px',
          transform: `translateY(${(frame * 0.3) % 48}px)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: -160,
            top: 180,
            width: 700,
            height: 700,
            borderRadius: '50%',
            backgroundColor: '#FF5C5C',
            opacity: 0.06,
            transform: `scale(${1 + Math.sin(frame * 0.012) * 0.06})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: 120,
            top: 80,
            width: 460,
            height: 460,
            border: '5px solid #F4F2F2',
            opacity: 0.07,
            transform: `rotate(${frame * 0.05}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 32 }).map((_, i) => {
          const r = seededRandom(i + 55);
          const y = (r * 1080 + frame * (0.3 + r * 0.6)) % 1150;
          return (
            <div
              key={`pp-${i}`}
              style={{
                position: 'absolute',
                left: r * 1890,
                top: y,
                width: 16,
                height: 3,
                backgroundColor: '#FFD166',
                opacity: 0.2 + r * 0.2,
                transform: `rotate(${i * 24 + frame * 0.3}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill
        style={{
          transform: `scale(${camScale}) translate(${shake}px, ${camY}px)`,
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        {/* BEAT 1 — the January bill shock */}
        <div style={{ position: 'absolute', opacity: b1, textAlign: 'center' }}>
          <div style={{ display: 'flex', justifyContent: 'center', opacity: sp(4) }}>
            <CardIcon />
          </div>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 128,
              color: '#F4F2F2',
              marginTop: 24,
              lineHeight: 1.12,
              textShadow: '12px 12px 0 #000000',
              transform: `scale(${0.8 + sp(10, { damping: 9, stiffness: 140 }) * 0.2})`,
            }}
          >
            जनवरी में क्रेडिट कार्ड
            <br />
            का बिल देखकर
          </div>
          <div
            style={{
              display: 'inline-block',
              marginTop: 30,
              backgroundColor: '#FF5C5C',
              color: '#F4F2F2',
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 111,
              padding: '12px 44px',
              border: '7px solid #000000',
              opacity: sp(70),
              transform: `rotate(-2deg) translateY(${(1 - sp(70)) * 60}px)`,
            }}
          >
            हैरान रह जाओगे
          </div>
        </div>

        {/* BEAT 2 — Raj's profile + purchases */}
        <div style={{ position: 'absolute', opacity: b2, width: 1680 }}>
          <div style={{ display: 'flex', gap: 40, alignItems: 'stretch' }}>
            <div
              style={{
                width: 520,
                backgroundColor: '#F4F2F2',
                border: '7px solid #000000',
                boxShadow: '16px 16px 0 rgba(0,0,0,0.35)',
                padding: '34px 30px',
                opacity: sp(250),
                transform: `translateX(${(1 - sp(250)) * -180}px) rotate(-1.5deg)`,
              }}
            >
              <div
                style={{
                  fontFamily: "'JetBrains Mono', monospace",
                  fontSize: 26,
                  letterSpacing: 5,
                  color: '#000000',
                  opacity: 0.6,
                }}
              >
                CASE FILE / 01
              </div>
              <div
                style={{
                  fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                  fontSize: 145,
                  color: '#000000',
                  lineHeight: 1,
                  marginTop: 10,
                }}
              >
                राज
              </div>
              <div
                style={{
                  fontFamily: "'Noto Sans Devanagari', sans-serif",
                  fontWeight: 700,
                  fontSize: 43,
                  color: '#000000',
                  marginTop: 14,
                }}
              >
                दिल्ली · जॉब
              </div>
              <div style={{ height: 5, backgroundColor: '#000000', margin: '24px 0', opacity: 0.25 }} />
              <div
                style={{
                  fontFamily: "'JetBrains Mono', monospace",
                  fontSize: 28,
                  color: '#000000',
                  opacity: 0.65,
                  letterSpacing: 3,
                }}
              >
                MONTHLY SALARY
              </div>
              <div
                style={{
                  fontFamily: "'Inter', 'Poppins', sans-serif",
                  fontWeight: 900,
                  fontSize: 111,
                  color: '#000000',
                  lineHeight: 1.05,
                }}
              >
                ₹60,000
              </div>
            </div>

            <div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: 26 }}>
              {[
                { name: 'नया आईफोन', price: '₹70,000', emi: '₹7,000 / महीना', Icon: PhoneIcon, bg: '#FFD166' },
                { name: '55" स्मार्ट TV', price: '₹50,000', emi: '₹5,000 / महीना', Icon: TvIcon, bg: '#9BF3D2' },
              ].map((it, i) => {
                const en = sp(280 + i * 34, { damping: 13, stiffness: 100 });
                return (
                  <div
                    key={it.name}
                    style={{
                      flex: 1,
                      backgroundColor: it.bg,
                      border: '7px solid #000000',
                      boxShadow: '16px 16px 0 rgba(0,0,0,0.35)',
                      padding: '26px 32px',
                      display: 'flex',
                      alignItems: 'center',
                      gap: 28,
                      opacity: en,
                      transform: `translateX(${(1 - en) * 220}px) rotate(${i === 0 ? 1 : -1}deg)`,
                    }}
                  >
                    <it.Icon />
                    <div style={{ flex: 1 }}>
                      <div
                        style={{
                          fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                          fontSize: 68,
                          color: '#000000',
                          lineHeight: 1,
                        }}
                      >
                        {it.name}
                      </div>
                      <div
                        style={{
                          fontFamily: "'JetBrains Mono', monospace",
                          fontSize: 32,
                          color: '#000000',
                          opacity: 0.7,
                          marginTop: 10,
                        }}
                      >
                        {it.price}
                      </div>
                    </div>
                    <div
                      style={{
                        backgroundColor: '#000000',
                        color: '#F4F2F2',
                        fontFamily: "'Inter', 'Poppins', sans-serif",
                        fontWeight: 800,
                        fontSize: 40,
                        padding: '16px 24px',
                        transform: `scale(${1 + Math.sin(frame * 0.08 + i) * 0.02})`,
                      }}
                    >
                      {it.emi}
                    </div>
                  </div>
                );
              })}
            </div>
          </div>
        </div>

        {/* BEAT 3 — the trap closes */}
        <div style={{ position: 'absolute', opacity: b3, textAlign: 'center' }}>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontWeight: 700,
              fontSize: 62,
              color: '#F4F2F2',
              opacity: 0.9,
            }}
          >
            हर EMI छोटी लगती है...
          </div>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 187,
              color: '#FFD166',
              lineHeight: 1.05,
              marginTop: 20,
              textShadow: '14px 14px 0 #000000',
              transform: `scale(${0.82 + sp(525, { damping: 9, stiffness: 150 }) * 0.18})`,
            }}
          >
            जब तक सब जुड़ न जाएं
          </div>
          <div style={{ display: 'flex', gap: 22, justifyContent: 'center', marginTop: 40 }}>
            {['₹7,000', '+', '₹5,000', '+', '...'].map((t, i) => {
              const en = sp(570 + i * 14);
              return (
                <div
                  key={`sum-${i}`}
                  style={{
                    fontFamily: "'JetBrains Mono', monospace",
                    fontSize: 55,
                    color: '#000000',
                    backgroundColor: '#F4F2F2',
                    border: '5px solid #000000',
                    padding: '10px 22px',
                    opacity: en,
                    transform: `translateY(${(1 - en) * 50}px)`,
                  }}
                >
                  {t}
                </div>
              );
            })}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.58) 100%)' }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: "'Inter', sans-serif",
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene07RajStory;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08SalaryBreakdown',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const ROWS = [
  { label: 'EMI', hi: 'EMI', amt: 16000, color: '#FF5C5C' },
  { label: 'RENT', hi: 'किराया', amt: 15000, color: '#FFD166' },
  { label: 'HOME', hi: 'घर का खर्च', amt: 10000, color: '#9BF3D2' },
  { label: 'TRANSPORT', hi: 'ट्रांसपोर्ट', amt: 5000, color: '#C9CCFF' },
  { label: 'LEFT', hi: 'बचा', amt: 14000, color: '#F4F2F2' },
];

const CalendarIcon: React.FC = () => (
  <svg width="80" height="80" viewBox="0 0 64 64" fill="none">
    <rect x="8" y="12" width="48" height="44" rx="5" stroke="#F4F2F2" strokeWidth={4} />
    <path d="M8 24h48M20 6v12M44 6v12" stroke="#F4F2F2" strokeWidth={4} strokeLinecap="round" />
    <rect x="18" y="32" width="10" height="10" fill="#FF5C5C" />
  </svg>
);

const Scene08SalaryBreakdown: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const beat = (s: number, e: number, f = 15) =>
    interpolate(frame, [s, s + f, e - f, e], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const b1 = beat(0, 180);
  const b2 = beat(175, 640);
  const b3 = interpolate(frame, [635, 665], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera — vertical tilt down the salary sheet
  const camY = interpolate(frame, [175, 640], [30, -30], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1.13], { extrapolateRight: 'clamp' });
  const shake = frame > 635 && frame < 658 ? Math.sin(frame * 2.4) * 8 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#525CEB', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{ background: 'linear-gradient(20deg, #1E2472 0%, #525CEB 52%, #7A85FF 100%)' }}
      />

      {/* Punk collage layer */}
      <AbsoluteFill>
        {Array.from({ length: 8 }).map((_, i) => {
          const r = seededRandom(i + 121);
          return (
            <div
              key={`ck-${i}`}
              style={{
                position: 'absolute',
                left: `${r * 90}%`,
                top: `${seededRandom(i + 28) * 86}%`,
                width: 300 + r * 300,
                height: 70 + r * 120,
                backgroundColor: i % 2 === 0 ? '#000000' : '#FF5C5C',
                opacity: 0.055,
                transform: `rotate(${(r - 0.5) * 30}deg) translateX(${Math.sin(frame * 0.005 + i) * 26}px)`,
                clipPath: 'polygon(2% 0%, 100% 8%, 97% 100%, 0% 90%)',
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Motion texture — horizontal rules */}
      <AbsoluteFill
        style={{
          backgroundImage:
            'repeating-linear-gradient(0deg, rgba(244,242,242,0.10) 0px, rgba(244,242,242,0.10) 2px, transparent 2px, transparent 40px)',
          transform: `translateY(${(frame * 0.5) % 40}px)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            right: -120,
            top: 60,
            width: 640,
            height: 640,
            backgroundColor: '#FF5C5C',
            opacity: 0.06,
            transform: `rotate(${frame * 0.04}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: 40,
            bottom: -140,
            width: 560,
            height: 560,
            borderRadius: '50%',
            border: '6px solid #F4F2F2',
            opacity: 0.07,
            transform: `scale(${1 + Math.sin(frame * 0.011) * 0.06})`,
          }}
        />
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 30 }).map((_, i) => {
          const r = seededRandom(i + 77);
          const y = (r * 1080 + frame * (0.25 + r * 0.5)) % 1140;
          return (
            <div
              key={`fz-${i}`}
              style={{
                position: 'absolute',
                left: r * 1900,
                top: y,
                width: 6,
                height: 6,
                backgroundColor: '#F4F2F2',
                opacity: 0.18 + r * 0.2,
                transform: `rotate(45deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill
        style={{
          transform: `scale(${camScale}) translate(${shake}px, ${camY}px)`,
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        {/* BEAT 1 — January reality check */}
        <div style={{ position: 'absolute', opacity: b1, textAlign: 'center' }}>
          <div style={{ display: 'flex', justifyContent: 'center', opacity: sp(4) }}>
            <CalendarIcon />
          </div>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 170,
              color: '#F4F2F2',
              marginTop: 20,
              lineHeight: 1.05,
              textShadow: '14px 14px 0 #000000',
              transform: `scale(${0.82 + sp(8, { damping: 9, stiffness: 145 }) * 0.18})`,
            }}
          >
            जनवरी आई
          </div>
          <div
            style={{
              display: 'inline-block',
              marginTop: 26,
              backgroundColor: '#000000',
              color: '#FFD166',
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontWeight: 700,
              fontSize: 60,
              padding: '16px 38px',
              opacity: sp(55),
              transform: `translateY(${(1 - sp(55)) * 60}px)`,
            }}
          >
            और असली हिसाब सामने आया
          </div>
        </div>

        {/* BEAT 2 — salary split sheet */}
        <div style={{ position: 'absolute', opacity: b2, width: 1560 }}>
          <div
            style={{
              display: 'flex',
              justifyContent: 'space-between',
              alignItems: 'flex-end',
              marginBottom: 28,
              opacity: sp(185),
            }}
          >
            <div
              style={{
                fontFamily: "'JetBrains Mono', monospace",
                fontSize: 30,
                letterSpacing: 6,
                color: '#F4F2F2',
                opacity: 0.8,
              }}
            >
              MONTHLY SALARY SPLIT
            </div>
            <div
              style={{
                fontFamily: "'Inter', 'Poppins', sans-serif",
                fontWeight: 900,
                fontSize: 94,
                color: '#F4F2F2',
                textShadow: '8px 8px 0 #000000',
              }}
            >
              ₹60,000
            </div>
          </div>

          {ROWS.map((r, i) => {
            const en = sp(200 + i * 40, { damping: 14, stiffness: 90 });
            const w = (r.amt / 16000) * 100;
            const isLeft = i === 4;
            return (
              <div key={r.label} style={{ marginBottom: 20, opacity: en }}>
                <div style={{ display: 'flex', alignItems: 'center', gap: 22 }}>
                  <div
                    style={{
                      width: 300,
                      textAlign: 'right',
                      fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                      fontSize: 55,
                      color: isLeft ? '#FFD166' : '#F4F2F2',
                    }}
                  >
                    {r.hi}
                  </div>
                  <div style={{ flex: 1, position: 'relative', height: 74 }}>
                    <div
                      style={{
                        position: 'absolute',
                        inset: 0,
                        backgroundColor: 'rgba(0,0,0,0.28)',
                        border: '4px solid rgba(244,242,242,0.35)',
                      }}
                    />
                    <div
                      style={{
                        position: 'absolute',
                        left: 0,
                        top: 0,
                        bottom: 0,
                        width: `${w * en}%`,
                        backgroundColor: r.color,
                        border: '4px solid #000000',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'flex-end',
                        paddingRight: 18,
                      }}
                    >
                      <span
                        style={{
                          fontFamily: "'Inter', 'Poppins', sans-serif",
                          fontWeight: 900,
                          fontSize: 42,
                          color: '#000000',
                        }}
                      >
                        ₹{Math.round(r.amt * en).toLocaleString('en-IN')}
                      </span>
                    </div>
                  </div>
                  <div
                    style={{
                      width: 210,
                      fontFamily: "'JetBrains Mono', monospace",
                      fontSize: 25,
                      letterSpacing: 3,
                      color: '#F4F2F2',
                      opacity: 0.6,
                    }}
                  >
                    {r.label}
                  </div>
                </div>
              </div>
            );
          })}
        </div>

        {/* BEAT 3 — what's actually left */}
        <div style={{ position: 'absolute', opacity: b3, textAlign: 'center' }}>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontWeight: 700,
              fontSize: 60,
              color: '#F4F2F2',
              opacity: 0.9,
            }}
          >
            सब निकालने के बाद बचे सिर्फ
          </div>
          <div
            style={{
              fontFamily: "'Inter', 'Poppins', sans-serif",
              fontWeight: 900,
              fontSize: 255,
              color: '#FFD166',
              lineHeight: 1,
              marginTop: 10,
              textShadow: '18px 18px 0 #000000',
              transform: `scale(${0.8 + sp(645, { damping: 9, stiffness: 150 }) * 0.2})`,
            }}
          >
            ₹14,000
          </div>
          <div
            style={{
              display: 'inline-block',
              marginTop: 32,
              backgroundColor: '#FF5C5C',
              color: '#F4F2F2',
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 76,
              padding: '14px 40px',
              border: '7px solid #000000',
              opacity: sp(700),
              transform: `rotate(-1.5deg) translateY(${(1 - sp(700)) * 50}px)`,
            }}
          >
            बाकी पूरा महीना इसी में
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 43%, rgba(0,0,0,0.57) 100%)' }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: "'Inter', sans-serif",
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene08SalaryBreakdown;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09EmptySavings',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const JarIcon: React.FC<{ fillLevel: number }> = ({ fillLevel }) => (
  <svg width="180" height="220" viewBox="0 0 90 110" fill="none">
    <rect x="26" y="4" width="38" height="12" rx="3" stroke="#F4F2F2" strokeWidth={4} />
    <path d="M20 20h50v76a8 8 0 0 1-8 8H28a8 8 0 0 1-8-8V20z" stroke="#F4F2F2" strokeWidth={4} />
    <rect x="24" y={100 - 78 * fillLevel} width="42" height={78 * fillLevel} fill="#FFD166" opacity={0.85} />
  </svg>
);

const ShieldIcon: React.FC = () => (
  <svg width="120" height="140" viewBox="0 0 64 76" fill="none">
    <path d="M32 4l24 10v26c0 16-12 26-24 32C20 66 8 56 8 40V14L32 4z" stroke="#F4F2F2" strokeWidth={4} strokeLinejoin="round" />
    <path d="M22 38l8 8 14-16" stroke="#FF5C5C" strokeWidth={5} strokeLinecap="round" strokeLinejoin="round" />
  </svg>
);

const Scene09EmptySavings: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const beat = (s: number, e: number, f = 15) =>
    interpolate(frame, [s, s + f, e - f, e], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const b1 = beat(0, 300);
  const b2 = beat(295, 600);
  const b3 = interpolate(frame, [595, 630], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const drain = interpolate(frame, [30, 220], [0.85, 0.02], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera — slow pull back then push into the reveal
  const camScale = interpolate(frame, [0, 400, durationInFrames], [1.14, 1.02, 1.12], { extrapolateRight: 'clamp' });
  const camX = Math.cos(frame * 0.006) * 22;

  return (
    <AbsoluteFill style={{ backgroundColor: '#525CEB', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(circle at 20% 30%, #6873FF 0%, #525CEB 40%, #1B216D 100%)' }}
      />

      {/* Punk collage layer */}
      <AbsoluteFill>
        {Array.from({ length: 9 }).map((_, i) => {
          const r = seededRandom(i + 141);
          return (
            <div
              key={`cp-${i}`}
              style={{
                position: 'absolute',
                left: `${r * 90}%`,
                top: `${seededRandom(i + 33) * 88}%`,
                width: 200 + r * 340,
                height: 130 + r * 170,
                backgroundColor: i % 3 === 0 ? '#FFD166' : '#000000',
                opacity: 0.05,
                transform: `rotate(${(r - 0.5) * 42}deg) translateY(${Math.cos(frame * 0.005 + i) * 24}px)`,
                clipPath: 'polygon(6% 0%, 100% 4%, 92% 100%, 0% 94%)',
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Motion texture — cross hatch */}
      <AbsoluteFill
        style={{
          backgroundImage:
            'repeating-linear-gradient(45deg, rgba(0,0,0,0.12) 0px, rgba(0,0,0,0.12) 1.5px, transparent 1.5px, transparent 26px), repeating-linear-gradient(-45deg, rgba(0,0,0,0.12) 0px, rgba(0,0,0,0.12) 1.5px, transparent 1.5px, transparent 26px)',
          transform: `translate(${(frame * 0.25) % 26}px, ${(frame * 0.25) % 26}px)`,
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: 700,
            top: 30,
            width: 720,
            height: 720,
            borderRadius: '50%',
            border: '7px solid #F4F2F2',
            opacity: 0.06,
            transform: `scale(${1 + Math.sin(frame * 0.01) * 0.07}) rotate(${-frame * 0.03}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: 80,
            bottom: 60,
            width: 0,
            height: 0,
            borderLeft: '240px solid transparent',
            borderRight: '240px solid transparent',
            borderBottom: '420px solid #FFD166',
            opacity: 0.05,
            transform: `rotate(${frame * 0.05}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 34 }).map((_, i) => {
          const r = seededRandom(i + 87);
          const y = (r * 1080 + frame * (0.5 + r * 0.7)) % 1160;
          return (
            <div
              key={`dp-${i}`}
              style={{
                position: 'absolute',
                left: r * 1900,
                top: y,
                width: 5,
                height: 14,
                borderRadius: 3,
                backgroundColor: '#FFD166',
                opacity: 0.18 + r * 0.2,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill
        style={{
          transform: `scale(${camScale}) translateX(${camX}px)`,
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        {/* BEAT 1 — savings drain + zero emergency fund */}
        <div style={{ position: 'absolute', opacity: b1, display: 'flex', alignItems: 'center', gap: 110 }}>
          <div style={{ textAlign: 'center', transform: `translateY(${(1 - sp(10)) * 120}px)` }}>
            <JarIcon fillLevel={drain} />
            <div
              style={{
                fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                fontSize: 68,
                color: '#F4F2F2',
                marginTop: 16,
              }}
            >
              सेविंग
            </div>
            <div
              style={{
                fontFamily: "'JetBrains Mono', monospace",
                fontSize: 42,
                color: '#FF5C5C',
                marginTop: 6,
              }}
            >
              ₹0
            </div>
          </div>

          <div style={{ textAlign: 'center', transform: `translateY(${(1 - sp(45)) * 120}px)`, opacity: sp(45) }}>
            <div style={{ position: 'relative', display: 'inline-block' }}>
              <ShieldIcon />
              <div
                style={{
                  position: 'absolute',
                  left: 0,
                  top: 60,
                  width: 120,
                  height: 8,
                  backgroundColor: '#FF5C5C',
                  transform: `rotate(-38deg) scaleX(${sp(90)})`,
                  transformOrigin: 'left center',
                }}
              />
            </div>
            <div
              style={{
                fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                fontSize: 68,
                color: '#F4F2F2',
                marginTop: 16,
              }}
            >
              इमरजेंसी फंड
            </div>
            <div
              style={{
                fontFamily: "'JetBrains Mono', monospace",
                fontSize: 42,
                color: '#FF5C5C',
                marginTop: 6,
              }}
            >
              NOT BUILT
            </div>
          </div>

          <div style={{ maxWidth: 620, opacity: sp(90), transform: `translateX(${(1 - sp(90)) * 140}px)` }}>
            <div
              style={{
                fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                fontSize: 111,
                color: '#FFD166',
                lineHeight: 1.1,
                textShadow: '10px 10px 0 #000000',
              }}
            >
              अगले साल का
              <br />
              कोई प्लान नहीं
            </div>
          </div>
        </div>

        {/* BEAT 2 — 10 months locked in EMI */}
        <div style={{ position: 'absolute', opacity: b2, width: 1500, textAlign: 'center' }}>
          <div
            style={{
              display: 'inline-block',
              backgroundColor: '#000000',
              color: '#FFD166',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 30,
              letterSpacing: 6,
              padding: '10px 26px',
              opacity: sp(305),
            }}
          >
            NEXT 10 MONTHS · LOCKED
          </div>
          <div style={{ display: 'flex', gap: 18, justifyContent: 'center', marginTop: 44, flexWrap: 'wrap' }}>
            {Array.from({ length: 10 }).map((_, i) => {
              const en = sp(320 + i * 16, { damping: 13, stiffness: 110 });
              return (
                <div
                  key={`mo-${i}`}
                  style={{
                    width: 132,
                    height: 156,
                    backgroundColor: '#FF5C5C',
                    border: '6px solid #000000',
                    boxShadow: '10px 10px 0 rgba(0,0,0,0.35)',
                    display: 'flex',
                    flexDirection: 'column',
                    alignItems: 'center',
                    justifyContent: 'center',
                    opacity: en,
                    transform: `scale(${0.5 + en * 0.5}) rotate(${(i % 2 === 0 ? -2 : 2)}deg)`,
                  }}
                >
                  <div
                    style={{
                      fontFamily: "'Inter', 'Poppins', sans-serif",
                      fontWeight: 900,
                      fontSize: 60,
                      color: '#F4F2F2',
                      lineHeight: 1,
                    }}
                  >
                    {i + 1}
                  </div>
                  <div
                    style={{
                      fontFamily: "'JetBrains Mono', monospace",
                      fontSize: 22,
                      color: '#000000',
                      marginTop: 8,
                      letterSpacing: 2,
                    }}
                  >
                    EMI
                  </div>
                </div>
              );
            })}
          </div>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontWeight: 700,
              fontSize: 55,
              color: '#F4F2F2',
              marginTop: 44,
              opacity: sp(500),
            }}
          >
            ये सिर्फ राज नहीं — लाखों भारतीयों की हकीकत है
          </div>
        </div>

        {/* BEAT 3 — chapter turn to No Cost EMI */}
        <div style={{ position: 'absolute', opacity: b3, textAlign: 'center' }}>
          <div
            style={{
              fontFamily: "'Noto Sans Devanagari', sans-serif",
              fontWeight: 700,
              fontSize: 60,
              color: '#F4F2F2',
              opacity: 0.9,
            }}
          >
            अब बात करते हैं
          </div>
          <div
            style={{
              display: 'inline-block',
              marginTop: 24,
              backgroundColor: '#FFD166',
              color: '#000000',
              fontFamily: "'Inter', 'Poppins', sans-serif",
              fontWeight: 900,
              fontSize: 187,
              padding: '14px 52px',
              border: '9px solid #000000',
              letterSpacing: -2,
              transform: `rotate(-1.5deg) scale(${0.82 + sp(605, { damping: 9, stiffness: 145 }) * 0.18})`,
            }}
          >
            NO COST EMI
          </div>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 111,
              color: '#F4F2F2',
              marginTop: 34,
              textShadow: '10px 10px 0 #000000',
              opacity: sp(660),
              transform: `translateY(${(1 - sp(660)) * 60}px)`,
            }}
          >
            के सच की
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 44%, rgba(0,0,0,0.56) 100%)' }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: "'Inter', sans-serif",
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene09EmptySavings;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10NoCostMyth',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BankIcon: React.FC = () => (
  <svg width="96" height="96" viewBox="0 0 64 64" fill="none">
    <path d="M6 24L32 8l26 16" stroke="#000000" strokeWidth={4} strokeLinejoin="round" />
    <path d="M12 26v22M24 26v22M40 26v22M52 26v22" stroke="#000000" strokeWidth={4} />
    <path d="M6 54h52" stroke="#000000" strokeWidth={4} strokeLinecap="round" />
  </svg>
);

const LaptopIcon: React.FC = () => (
  <svg width="110" height="110" viewBox="0 0 64 64" fill="none">
    <rect x="12" y="14" width="40" height="28" rx="3" stroke="#000000" strokeWidth={4} />
    <path d="M4 48h56l-6-6H10l-6 6z" stroke="#000000" strokeWidth={4} strokeLinejoin="round" />
  </svg>
);

const HiddenIcon: React.FC = () => (
  <svg width="72" height="72" viewBox="0 0 64 64" fill="none">
    <path d="M4 32s10-16 28-16 28 16 28 16-10 16-28 16S4 32 4 32z" stroke="#000000" strokeWidth={4} strokeLinejoin="round" />
    <circle cx="32" cy="32" r="7" fill="#000000" />
    <path d="M10 54L54 10" stroke="#FF5C5C" strokeWidth={5} strokeLinecap="round" />
  </svg>
);

const Scene10NoCostMyth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const beat = (s: number, e: number, f = 15) =>
    interpolate(frame, [s, s + f, e - f, e], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const b1 = beat(0, 230);
  const b2 = beat(225, 520);
  const b3 = interpolate(frame, [515, 550], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera — punch-in on the myth-buster stamp, then settle
  const camScale = interpolate(frame, [0, 120, durationInFrames], [1.18, 1.04, 1.11], { extrapolateRight: 'clamp' });
  const shake = frame > 8 && frame < 30 ? Math.sin(frame * 2.8) * 11 : 0;
  const camY = Math.sin(frame * 0.007) * 14;

  return (
    <AbsoluteFill style={{ backgroundColor: '#525CEB', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(ellipse at 50% 100%, #7B86FF 0%, #525CEB 46%, #1A2069 100%)' }}
      />

      {/* Punk collage layer — price-tag scraps */}
      <AbsoluteFill>
        {Array.from({ length: 10 }).map((_, i) => {
          const r = seededRandom(i + 161);
          return (
            <div
              key={`tg-${i}`}
              style={{
                position: 'absolute',
                left: `${r * 92 - 4}%`,
                top: `${seededRandom(i + 44) * 88}%`,
                width: 180 + r * 260,
                height: 90 + r * 130,
                backgroundColor: i % 3 === 0 ? '#FF5C5C' : '#F4F2F2',
                opacity: 0.055,
                transform: `rotate(${(r - 0.5) * 48}deg) translateX(${Math.sin(frame * 0.005 + i) * 28}px)`,
                clipPath: 'polygon(10% 0%, 100% 6%, 94% 100%, 0% 88%)',
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Motion texture — radial burst lines */}
      <AbsoluteFill style={{ alignItems: 'center', justifyContent: 'center' }}>
        {Array.from({ length: 26 }).map((_, i) => (
          <div
            key={`ray-${i}`}
            style={{
              position: 'absolute',
              width: 3,
              height: 1500,
              backgroundColor: '#F4F2F2',
              opacity: 0.05,
              transform: `rotate(${i * (180 / 26) + frame * 0.12}deg)`,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 100,
            width: 520,
            height: 520,
            backgroundColor: '#000000',
            opacity: 0.06,
            transform: `rotate(${-frame * 0.05}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: 100,
            bottom: 60,
            width: 620,
            height: 620,
            borderRadius: '50%',
            border: '8px solid #FFD166',
            opacity: 0.07,
            transform: `scale(${1 + Math.sin(frame * 0.012) * 0.06})`,
          }}
        />
      </AbsoluteFill>

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 40 }).map((_, i) => {
          const r = seededRandom(i + 199);
          const y = (r * 1080 - frame * (0.4 + r * 0.6) + 1200) % 1200;
          return (
            <div
              key={`pf-${i}`}
              style={{
                position: 'absolute',
                left: r * 1900,
                top: y,
                width: 6 + r * 6,
                height: 6 + r * 6,
                backgroundColor: i % 4 === 0 ? '#FF5C5C' : '#F4F2F2',
                opacity: 0.16 + r * 0.22,
                transform: `rotate(${frame * 0.8 + i * 18}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill
        style={{
          transform: `scale(${camScale}) translate(${shake}px, ${camY}px)`,
          alignItems: 'center',
          justifyContent: 'center',
        }}
      >
        {/* BEAT 1 — myth busted stamp */}
        <div style={{ position: 'absolute', opacity: b1, textAlign: 'center' }}>
          <div
            style={{
              display: 'inline-block',
              backgroundColor: '#F4F2F2',
              color: '#000000',
              fontFamily: "'Inter', 'Poppins', sans-serif",
              fontWeight: 900,
              fontSize: 128,
              padding: '16px 46px',
              border: '9px solid #000000',
              textDecoration: 'line-through',
              textDecorationColor: '#FF5C5C',
              textDecorationThickness: 12,
              transform: `rotate(-2deg) scale(${0.75 + sp(6, { damping: 9, stiffness: 150 }) * 0.25})`,
            }}
          >
            NO COST
          </div>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 170,
              color: '#FFD166',
              marginTop: 34,
              lineHeight: 1.05,
              textShadow: '14px 14px 0 #000000',
              opacity: sp(45),
              transform: `translateY(${(1 - sp(45)) * 80}px)`,
            }}
          >
            असल में मुफ्त नहीं है
          </div>
          <div
            style={{
              display: 'flex',
              gap: 26,
              justifyContent: 'center',
              alignItems: 'center',
              marginTop: 40,
              opacity: sp(95),
            }}
          >
            <div
              style={{
                backgroundColor: '#F4F2F2',
                border: '6px solid #000000',
                padding: '16px 24px',
                display: 'flex',
                alignItems: 'center',
                gap: 16,
              }}
            >
              <HiddenIcon />
              <span
                style={{
                  fontFamily: "'Noto Sans Devanagari', sans-serif",
                  fontWeight: 700,
                  fontSize: 47,
                  color: '#000000',
                }}
              >
                छिपे हुए शुल्क
              </span>
            </div>
          </div>
        </div>

        {/* BEAT 2 — bank is not a charity */}
        <div style={{ position: 'absolute', opacity: b2, width: 1620 }}>
          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 111,
              color: '#F4F2F2',
              textAlign: 'center',
              marginBottom: 44,
              textShadow: '10px 10px 0 rgba(0,0,0,0.42)',
              opacity: sp(235),
            }}
          >
            बैंक कोई चैरिटी नहीं कर रहा
          </div>
          <div style={{ display: 'flex', gap: 34, justifyContent: 'center' }}>
            {[
              { t: 'बैंक', s: 'ब्याज वसूलता है', Icon: BankIcon, bg: '#FFD166' },
              { t: 'सेलर', s: 'कीमत एडजस्ट करता है', Icon: LaptopIcon, bg: '#9BF3D2' },
            ].map((c, i) => {
              const en = sp(255 + i * 30, { damping: 13, stiffness: 100 });
              return (
                <div
                  key={c.t}
                  style={{
                    width: 620,
                    backgroundColor: c.bg,
                    border: '7px solid #000000',
                    boxShadow: '16px 16px 0 rgba(0,0,0,0.35)',
                    padding: '32px 34px',
                    opacity: en,
                    transform: `translateY(${(1 - en) * 150}px) rotate(${i === 0 ? -2 : 2}deg)`,
                  }}
                >
                  <c.Icon />
                  <div
                    style={{
                      fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
                      fontSize: 85,
                      color: '#000000',
                      marginTop: 12,
                      lineHeight: 1,
                    }}
                  >
                    {c.t}
                  </div>
                  <div
                    style={{
                      fontFamily: "'Noto Sans Devanagari', sans-serif",
                      fontWeight: 700,
                      fontSize: 47,
                      color: '#000000',
                      marginTop: 12,
                      opacity: 0.8,
                    }}
                  >
                    {c.s}
                  </div>
                </div>
              );
            })}
          </div>
          <div
            style={{
              textAlign: 'center',
              marginTop: 40,
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 34,
              letterSpacing: 4,
              color: '#F4F2F2',
              opacity: sp(400) * 0.85,
            }}
          >
            COST NEVER DISAPPEARS · IT ONLY MOVES
          </div>
        </div>

        {/* BEAT 3 — the laptop math setup */}
        <div style={{ position: 'absolute', opacity: b3, textAlign: 'center' }}>
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 30,
              backgroundColor: '#F4F2F2',
              border: '8px solid #000000',
              boxShadow: '18px 18px 0 rgba(0,0,0,0.4)',
              padding: '26px 42px',
              transform: `scale(${0.85 + sp(525, { damping: 10 }) * 0.15}) rotate(-1deg)`,
            }}
          >
            <LaptopIcon />
            <div style={{ textAlign: 'left' }}>
              <div
                style={{
                  fontFamily: "'JetBrains Mono', monospace",
                  fontSize: 28,
                  letterSpacing: 4,
                  color: '#000000',
                  opacity: 0.65,
                }}
              >
                LAPTOP · NO COST EMI
              </div>
              <div
                style={{
                  fontFamily: "'Inter', 'Poppins', sans-serif",
                  fontWeight: 900,
                  fontSize: 128,
                  color: '#000000',
                  lineHeight: 1.05,
                }}
              >
                ₹50,000
              </div>
            </div>
          </div>

          <div style={{ display: 'flex', gap: 20, justifyContent: 'center', marginTop: 40 }}>
            {Array.from({ length: 10 }).map((_, i) => {
              const en = sp(570 + i * 12, { damping: 13, stiffness: 120 });
              return (
                <div
                  key={`emi-${i}`}
                  style={{
                    width: 118,
                    height: 92,
                    backgroundColor: '#FFD166',
                    border: '5px solid #000000',
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    fontFamily: "'JetBrains Mono', monospace",
                    fontSize: 27,
                    color: '#000000',
                    opacity: en,
                    transform: `translateY(${(1 - en) * 60}px) rotate(${i % 2 === 0 ? -1.5 : 1.5}deg)`,
                  }}
                >
                  ₹5,000
                </div>
              );
            })}
          </div>

          <div
            style={{
              fontFamily: "'Yantra One', 'Noto Sans Devanagari', sans-serif",
              fontSize: 102,
              color: '#FFD166',
              marginTop: 40,
              textShadow: '10px 10px 0 #000000',
              opacity: sp(720),
              transform: `translateY(${(1 - sp(720)) * 60}px)`,
            }}
          >
            कोई एक्स्ट्रा चार्ज नहीं... सच में?
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.58) 100%)' }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: "'Inter', sans-serif",
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10NoCostMyth;
```

