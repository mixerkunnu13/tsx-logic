```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene31: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  // Title reveal
  const titleY = interpolate(sp(5), [0, 1], [60, 0]);
  const titleOp = interpolate(sp(5), [0, 1], [0, 1]);

  // YEAR boxes stagger
  const yearBoxOp = (i: number) => interpolate(sp(20 + i * 12), [0, 1], [0, 1]);
  const yearBoxY = (i: number) => interpolate(sp(20 + i * 12), [0, 1], [40, 0]);

  // Arrow grow
  const arrowProgress = interpolate(sp(90), [0, 1], [0, 1]);

  // 8% mega reveal
  const bigOp = interpolate(sp(140), [0, 1], [0, 1]);
  const bigScale = sp(140, { damping: 8, stiffness: 140 });
  const bigPulse = 1 + Math.sin(frame * 0.18) * 0.025;

  // "GUARANTEED" stamp
  const stampScale = sp(170, { damping: 6, stiffness: 180 });
  const stampRot = interpolate(sp(170), [0, 1], [-25, -8]);

  // Bottom caption
  const capOp = interpolate(sp(200), [0, 1], [0, 1]);

  return (
    // Outer container — solid background prevents any black flash
    <AbsoluteFill style={{ backgroundColor: '#2C3947' }}>
      <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp, transform: `scale(${camScale})` }}>

        {/* Dynamic gradient background */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at 30% 20%, rgba(250,185,91,0.12) 0%, transparent 55%), radial-gradient(circle at 75% 80%, rgba(232,237,242,0.06) 0%, transparent 50%), linear-gradient(135deg, #2C3947 0%, #243240 100%)',
          }}
        />

        {/* Shape layer — drifting transparent geometry */}
        <div
          style={{
            position: 'absolute',
            left: -180,
            top: -160,
            width: 720,
            height: 720,
            border: '2px solid #FAB95B',
            borderRadius: '50%',
            opacity: 0.06,
            transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: -120,
            bottom: -200,
            width: 640,
            height: 640,
            background: '#FAB95B',
            opacity: 0.05,
            borderRadius: 40,
            transform: `rotate(${-frame * 0.1 + 25}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: '60%',
            top: '5%',
            width: 380,
            height: 380,
            border: '2px solid #E8EDF2',
            opacity: 0.05,
            transform: `rotate(${frame * 0.2}deg)`,
            clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
          }}
        />

        {/* Motion texture — vertical lines grid */}
        {Array.from({ length: 18 }).map((_, i) => (
          <div
            key={`line-${i}`}
            style={{
              position: 'absolute',
              left: `${(i / 18) * 100}%`,
              top: 0,
              width: 1,
              height: '100%',
              background: '#E8EDF2',
              opacity: 0.04,
              transform: `translateY(${Math.sin(frame * 0.03 + i) * 8}px)`,
            }}
          />
        ))}

        {/* Floating particles */}
        {Array.from({ length: 24 }).map((_, i) => {
          const px = seededRandom(i + 1) * 1920;
          const py = seededRandom(i + 50) * 1080;
          const ps = 3 + seededRandom(i + 90) * 5;
          const drift = Math.sin(frame * 0.025 + i) * 18;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px + drift,
                top: py + Math.cos(frame * 0.02 + i) * 14,
                width: ps,
                height: ps,
                borderRadius: '50%',
                background: i % 3 === 0 ? '#FAB95B' : '#E8EDF2',
                opacity: 0.35,
                boxShadow: '0 0 8px rgba(250,185,91,0.4)',
              }}
            />
          );
        })}

        {/* MAIN CONTENT */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            transform: `translateY(${titleY}px)`,
            opacity: titleOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 800,
              fontSize: 64,
              color: '#E8EDF2',
              letterSpacing: 6,
              textTransform: 'uppercase',
              borderTop: '3px solid #FAB95B',
              borderBottom: '3px solid #FAB95B',
              padding: '14px 50px',
            }}
          >
            Delay = <span style={{ color: '#FAB95B' }}>Free Raise</span>
          </div>
        </div>

        {/* Year progression boxes */}
        <div
          style={{
            position: 'absolute',
            top: 280,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 24,
          }}
        >
          {[67, 68, 69, 70].map((year, i) => {
            const isPeak = year === 70;
            return (
              <div
                key={year}
                style={{
                  opacity: yearBoxOp(i),
                  transform: `translateY(${yearBoxY(i)}px)`,
                  width: 200,
                  height: 220,
                  background: isPeak ? '#FAB95B' : 'rgba(232,237,242,0.06)',
                  border: isPeak ? '3px solid #FAB95B' : '2px solid rgba(232,237,242,0.25)',
                  borderRadius: 18,
                  display: 'flex',
                  flexDirection: 'column',
                  alignItems: 'center',
                  justifyContent: 'center',
                  boxShadow: isPeak ? '0 0 60px rgba(250,185,91,0.55)' : 'none',
                }}
              >
                <div
                  style={{
                    fontFamily: 'Inter, Poppins',
                    fontSize: 28,
                    color: isPeak ? '#2C3947' : '#9FB0BF',
                    fontWeight: 600,
                    letterSpacing: 3,
                  }}
                >
                  AGE
                </div>
                <div
                  style={{
                    fontFamily: 'Inter, Poppins',
                    fontWeight: 900,
                    fontSize: 110,
                    color: isPeak ? '#2C3947' : '#E8EDF2',
                    lineHeight: 1,
                    marginTop: 8,
                  }}
                >
                  {year}
                </div>
                {i < 3 && (
                  <div
                    style={{
                      position: 'absolute',
                      right: -28,
                      top: '50%',
                      transform: 'translateY(-50%)',
                      fontSize: 40,
                      color: '#FAB95B',
                      fontWeight: 900,
                      opacity: yearBoxOp(i + 1),
                    }}
                  >
                    ›
                  </div>
                )}
              </div>
            );
          })}
        </div>

        {/* Arrow + +8% labels between boxes */}
        <div
          style={{
            position: 'absolute',
            top: 535,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 24,
            opacity: arrowProgress,
          }}
        >
          {[0, 1, 2].map((i) => (
            <div
              key={i}
              style={{
                width: 200,
                marginRight: i < 2 ? 24 : 0,
                display: 'flex',
                justifyContent: 'flex-end',
                paddingRight: 24,
              }}
            >
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontWeight: 800,
                  fontSize: 36,
                  color: '#FAB95B',
                  background: 'rgba(250,185,91,0.12)',
                  padding: '6px 18px',
                  borderRadius: 10,
                  border: '2px solid #FAB95B',
                  transform: `translateX(${interpolate(sp(90 + i * 10), [0, 1], [-30, 0])}px)`,
                  opacity: interpolate(sp(90 + i * 10), [0, 1], [0, 1]),
                }}
              >
                +8%
              </div>
            </div>
          ))}
        </div>

        {/* MEGA 8% PAYOFF */}
        <div
          style={{
            position: 'absolute',
            top: 620,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 30,
            opacity: bigOp,
            transform: `scale(${bigScale * bigPulse})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 900,
              fontSize: 230,
              color: '#FAB95B',
              lineHeight: 1,
              letterSpacing: -6,
              textShadow: '0 0 80px rgba(250,185,91,0.5)',
            }}
          >
            8%
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 700,
              fontSize: 50,
              color: '#E8EDF2',
              textAlign: 'left',
              lineHeight: 1.1,
              letterSpacing: 1,
            }}
          >
            PER YEAR<br />
            <span style={{ color: '#9FB0BF', fontSize: 32, fontWeight: 500 }}>
              added to your check
            </span>
          </div>
        </div>

        {/* GUARANTEED stamp */}
        <div
          style={{
            position: 'absolute',
            top: 660,
            left: '64%',
            transform: `rotate(${stampRot}deg) scale(${stampScale})`,
            border: '5px solid #E8EDF2',
            padding: '10px 26px',
            color: '#E8EDF2',
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 34,
            letterSpacing: 5,
            opacity: 0.85,
          }}
        >
          GUARANTEED
        </div>

        {/* Bottom caption */}
        <div
          style={{
            position: 'absolute',
            bottom: 100,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: capOp,
            fontFamily: 'Inter, Poppins',
            fontWeight: 600,
            fontSize: 38,
            color: '#9FB0BF',
            letterSpacing: 2,
          }}
        >
          Federal law. Locked. <span style={{ color: '#FAB95B' }}>Let that sink in.</span>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.45) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
            letterSpacing: 1,
          }}
        >
          Senior Fund USA
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene31;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [0, -25]);

  // Comparison rows
  const rowOp = (i: number) => interpolate(sp(40 + i * 25), [0, 1], [0, 1]);
  const rowX = (i: number) => interpolate(sp(40 + i * 25), [0, 1], [-80, 0]);

  // X mark on each
  const xMark = (i: number) => sp(60 + i * 25, { damping: 8, stiffness: 200 });

  // Center 8% pulse
  const heroOp = interpolate(sp(10), [0, 1], [0, 1]);
  const heroScale = sp(10, { damping: 9, stiffness: 110 });
  const heroPulse = 1 + Math.sin(frame * 0.14) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947' }}>
      <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp, transform: `translateX(${camX}px)` }}>

        {/* Gradient */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(ellipse at 50% 50%, rgba(250,185,91,0.14) 0%, transparent 60%), linear-gradient(180deg, #2C3947 0%, #1F2A36 100%)',
          }}
        />

        {/* Diagonal shape layer */}
        <div
          style={{
            position: 'absolute',
            top: '-30%',
            left: '-10%',
            width: '70%',
            height: '160%',
            background: 'linear-gradient(45deg, rgba(250,185,91,0.05), transparent)',
            transform: `rotate(15deg) translateY(${Math.sin(frame * 0.03) * 10}px)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: -200,
            top: '20%',
            width: 500,
            height: 500,
            border: '3px solid #E8EDF2',
            borderRadius: '50%',
            opacity: 0.04,
            transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
          }}
        />

        {/* Grid texture */}
        {Array.from({ length: 12 }).map((_, i) => (
          <div
            key={`hl-${i}`}
            style={{
              position: 'absolute',
              left: 0,
              right: 0,
              top: `${(i / 12) * 100}%`,
              height: 1,
              background: '#E8EDF2',
              opacity: 0.04,
            }}
          />
        ))}

        {/* Particles */}
        {Array.from({ length: 20 }).map((_, i) => {
          const px = seededRandom(i + 7) * 1920;
          const py = seededRandom(i + 33) * 1080;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px + Math.sin(frame * 0.02 + i) * 20,
                top: py + Math.cos(frame * 0.025 + i) * 15,
                width: 4,
                height: 4,
                borderRadius: '50%',
                background: '#FAB95B',
                opacity: 0.35,
              }}
            />
          );
        })}

        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(0), [0, 1], [-40, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 800,
              fontSize: 60,
              color: '#E8EDF2',
              letterSpacing: 3,
            }}
          >
            FIND <span style={{ color: '#FAB95B' }}>8%</span> ANYWHERE ELSE?
          </div>
        </div>

        {/* Center HERO 8% */}
        <div
          style={{
            position: 'absolute',
            top: 230,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            opacity: heroOp,
            transform: `scale(${heroScale * heroPulse})`,
          }}
        >
          <div
            style={{
              width: 360,
              height: 360,
              borderRadius: '50%',
              background: 'radial-gradient(circle, #FAB95B 0%, #d99935 100%)',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              boxShadow: '0 0 120px rgba(250,185,91,0.7), inset 0 0 60px rgba(255,255,255,0.15)',
              border: '4px solid #FAB95B',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 900,
                fontSize: 220,
                color: '#2C3947',
                lineHeight: 1,
                letterSpacing: -8,
              }}
            >
              8%
            </div>
          </div>
        </div>

        {/* LEFT side options (rejected) */}
        <div style={{ position: 'absolute', top: 280, left: 100, width: 380 }}>
          {['BANK SAVINGS', 'BONDS', 'CD ACCOUNTS'].map((label, i) => {
            const rates = ['~0.5%', '~4.2%', '~4.5%'];
            return (
              <div
                key={label}
                style={{
                  opacity: rowOp(i),
                  transform: `translateX(${rowX(i)}px)`,
                  marginBottom: 28,
                  padding: '20px 24px',
                  background: 'rgba(232,237,242,0.06)',
                  border: '2px solid rgba(232,237,242,0.15)',
                  borderRadius: 14,
                  position: 'relative',
                }}
              >
                <div
                  style={{
                    fontFamily: 'Inter, Poppins',
                    fontWeight: 700,
                    fontSize: 32,
                    color: '#E8EDF2',
                    letterSpacing: 2,
                  }}
                >
                  {label}
                </div>
                <div
                  style={{
                    fontFamily: 'JetBrains Mono',
                    fontWeight: 700,
                    fontSize: 38,
                    color: '#9FB0BF',
                    marginTop: 4,
                  }}
                >
                  {rates[i]}
                </div>
                {/* X overlay */}
                <div
                  style={{
                    position: 'absolute',
                    right: 16,
                    top: '50%',
                    transform: `translateY(-50%) scale(${xMark(i)}) rotate(${interpolate(xMark(i), [0, 1], [-90, 0])}deg)`,
                    width: 56,
                    height: 56,
                    borderRadius: '50%',
                    background: '#E8EDF2',
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    color: '#2C3947',
                    fontWeight: 900,
                    fontSize: 36,
                    fontFamily: 'Inter, Poppins',
                  }}
                >
                  ✕
                </div>
              </div>
            );
          })}
        </div>

        {/* RIGHT side — SSA winner */}
        <div
          style={{
            position: 'absolute',
            top: 320,
            right: 100,
            width: 380,
            opacity: interpolate(sp(140), [0, 1], [0, 1]),
            transform: `translateX(${interpolate(sp(140), [0, 1], [80, 0])}px)`,
          }}
        >
          <div
            style={{
              padding: '28px 24px',
              background: 'rgba(250,185,91,0.12)',
              border: '3px solid #FAB95B',
              borderRadius: 14,
              boxShadow: '0 0 50px rgba(250,185,91,0.3)',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 700,
                fontSize: 28,
                color: '#FAB95B',
                letterSpacing: 2,
              }}
            >
              SOCIAL SECURITY
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 900,
                fontSize: 76,
                color: '#E8EDF2',
                lineHeight: 1.1,
              }}
            >
              8.0%
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 600,
                fontSize: 22,
                color: '#E8EDF2',
                opacity: 0.85,
                marginTop: 4,
                letterSpacing: 1,
              }}
            >
              RISK-FREE • FEDERALLY BACKED
            </div>
          </div>
        </div>

        {/* Bottom verdict */}
        <div
          style={{
            position: 'absolute',
            bottom: 100,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(180), [0, 1], [0, 1]),
            fontFamily: 'Inter, Poppins',
            fontWeight: 800,
            fontSize: 44,
            color: '#E8EDF2',
            letterSpacing: 3,
          }}
        >
          NOWHERE. <span style={{ color: '#FAB95B' }}>NOT EVEN CLOSE.</span>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.45) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
            letterSpacing: 1,
          }}
        >
          Senior Fund USA
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene32;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene33: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Money counter animation
  const counterStart = 60;
  const counterEnd = 180;
  const counterProgress = interpolate(frame, [counterStart, counterEnd], [2000, 2400], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const displayValue = Math.round(counterProgress);

  // Bars
  const leftBarH = interpolate(sp(20), [0, 1], [0, 320]);
  const rightBarH = interpolate(sp(80), [0, 1], [0, 384]); // 20% taller

  // Jump arrow
  const arrowOp = interpolate(sp(140), [0, 1], [0, 1]);
  const arrowY = Math.sin(frame * 0.15) * 6;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947' }}>
      <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>

        {/* Gradient */}
        <AbsoluteFill
          style={{
            background: 'linear-gradient(160deg, #2C3947 0%, #243240 60%, #1F2A36 100%)',
          }}
        />

        {/* Shape layer */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '50%',
            width: 800,
            height: 800,
            border: '2px solid #FAB95B',
            borderRadius: 30,
            opacity: 0.04,
            transform: `translate(-50%,-50%) rotate(${frame * 0.1}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: -100,
            bottom: -100,
            width: 400,
            height: 400,
            background: '#FAB95B',
            opacity: 0.05,
            borderRadius: '50%',
            transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
          }}
        />

        {/* Dotted texture */}
        {Array.from({ length: 50 }).map((_, i) => {
          const x = (i % 10) * 192;
          const y = Math.floor(i / 10) * 216;
          return (
            <div
              key={`d-${i}`}
              style={{
                position: 'absolute',
                left: x + 90,
                top: y + 100,
                width: 4,
                height: 4,
                borderRadius: '50%',
                background: '#E8EDF2',
                opacity: 0.08,
              }}
            />
          );
        })}

        {/* Floating particles */}
        {Array.from({ length: 18 }).map((_, i) => {
          const px = seededRandom(i + 11) * 1920;
          const py = seededRandom(i + 71) * 1080;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: py + Math.sin(frame * 0.02 + i) * 20,
                width: 5,
                height: 5,
                borderRadius: '50%',
                background: '#FAB95B',
                opacity: 0.4,
                boxShadow: '0 0 8px #FAB95B',
              }}
            />
          );
        })}

        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 800,
              fontSize: 56,
              color: '#E8EDF2',
              letterSpacing: 3,
            }}
          >
            REAL NUMBERS, <span style={{ color: '#FAB95B' }}>REAL CASH</span>
          </div>
        </div>

        {/* BARS comparison */}
        <div
          style={{
            position: 'absolute',
            bottom: 240,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'flex-end',
            gap: 140,
          }}
        >
          {/* Left bar — Age 67 */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 900,
                fontSize: 64,
                color: '#E8EDF2',
                marginBottom: 12,
                opacity: interpolate(sp(30), [0, 1], [0, 1]),
              }}
            >
              $2,000
            </div>
            <div
              style={{
                width: 220,
                height: leftBarH,
                background: 'linear-gradient(180deg, #9FB0BF 0%, #6E7E8F 100%)',
                borderRadius: '12px 12px 0 0',
                border: '2px solid #E8EDF2',
                position: 'relative',
              }}
            >
              <div
                style={{
                  position: 'absolute',
                  bottom: -50,
                  left: 0,
                  right: 0,
                  textAlign: 'center',
                  fontFamily: 'Inter, Poppins',
                  fontWeight: 700,
                  fontSize: 32,
                  color: '#9FB0BF',
                  letterSpacing: 2,
                }}
              >
                AGE 67
              </div>
            </div>
          </div>

          {/* Arrow + label */}
          <div
            style={{
              position: 'absolute',
              top: '52%',
              left: '50%',
              transform: `translate(-50%, ${arrowY}px)`,
              opacity: arrowOp,
              textAlign: 'center',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 900,
                fontSize: 70,
                color: '#FAB95B',
                lineHeight: 1,
              }}
            >
              →
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 800,
                fontSize: 28,
                color: '#FAB95B',
                letterSpacing: 2,
                marginTop: 4,
                background: 'rgba(250,185,91,0.15)',
                padding: '4px 14px',
                borderRadius: 8,
              }}
            >
              WAIT 3 YEARS
            </div>
          </div>

          {/* Right bar — Age 70 (winner) */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 900,
                fontSize: 80,
                color: '#FAB95B',
                marginBottom: 12,
                opacity: interpolate(sp(80), [0, 1], [0, 1]),
                textShadow: '0 0 40px rgba(250,185,91,0.6)',
              }}
            >
              ${displayValue.toLocaleString()}
            </div>
            <div
              style={{
                width: 220,
                height: rightBarH,
                background: 'linear-gradient(180deg, #FAB95B 0%, #d99935 100%)',
                borderRadius: '12px 12px 0 0',
                border: '3px solid #FAB95B',
                position: 'relative',
                boxShadow: '0 0 50px rgba(250,185,91,0.5)',
              }}
            >
              <div
                style={{
                  position: 'absolute',
                  bottom: -50,
                  left: 0,
                  right: 0,
                  textAlign: 'center',
                  fontFamily: 'Inter, Poppins',
                  fontWeight: 700,
                  fontSize: 32,
                  color: '#FAB95B',
                  letterSpacing: 2,
                }}
              >
                AGE 70
              </div>
            </div>
          </div>
        </div>

        {/* Bottom callout */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(170), [0, 1], [0, 1]),
            fontFamily: 'Inter, Poppins',
            fontWeight: 700,
            fontSize: 38,
            color: '#E8EDF2',
            letterSpacing: 2,
          }}
        >
          +$400 every month. <span style={{ color: '#FAB95B' }}>For life.</span>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.45) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
            letterSpacing: 1,
          }}
        >
          Senior Fund USA
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene33;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene34: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slight orbit
  const camRot = Math.sin(frame * 0.01) * 0.5;
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.07]);

  // $4,800 counter
  const valStart = 30;
  const valEnd = 130;
  const v = interpolate(frame, [valStart, valEnd], [0, 4800], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const displayV = Math.round(v);

  const heroScale = sp(20, { damping: 9, stiffness: 100 });
  const heroPulse = 1 + Math.sin(frame * 0.12) * 0.015;

  // Three benefit pills
  const pillOp = (i: number) => interpolate(sp(140 + i * 20), [0, 1], [0, 1]);
  const pillY = (i: number) => interpolate(sp(140 + i * 20), [0, 1], [30, 0]);

  // Inflation arrow growing
  const inflProgress = interpolate(sp(180), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947' }}>
      <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp, transform: `scale(${camScale}) rotate(${camRot}deg)` }}>

        {/* Gradient */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at 50% 40%, rgba(250,185,91,0.18) 0%, transparent 55%), linear-gradient(180deg, #2C3947 0%, #1F2A36 100%)',
          }}
        />

        {/* Shape layer — concentric circles */}
        {[1, 2, 3].map((i) => (
          <div
            key={`c-${i}`}
            style={{
              position: 'absolute',
              left: '50%',
              top: '45%',
              width: 400 + i * 200,
              height: 400 + i * 200,
              borderRadius: '50%',
              border: '2px solid #FAB95B',
              opacity: 0.07 - i * 0.015,
              transform: `translate(-50%,-50%) scale(${1 + Math.sin(frame * 0.025 + i) * 0.03})`,
            }}
          />
        ))}

        {/* Diagonal texture lines */}
        {Array.from({ length: 14 }).map((_, i) => (
          <div
            key={`dl-${i}`}
            style={{
              position: 'absolute',
              left: -200,
              top: i * 100 - 100,
              width: 2400,
              height: 1,
              background: '#E8EDF2',
              opacity: 0.04,
              transform: 'rotate(-15deg)',
            }}
          />
        ))}

        {/* Particles — dollar-rain feel */}
        {Array.from({ length: 26 }).map((_, i) => {
          const px = seededRandom(i + 5) * 1920;
          const fallSpeed = 0.5 + seededRandom(i + 50) * 0.8;
          const py = ((seededRandom(i + 20) * 1080) + frame * fallSpeed) % 1180 - 50;
          return (
            <div
              key={`pp-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: py,
                fontFamily: 'Inter, Poppins',
                fontWeight: 800,
                fontSize: 18,
                color: '#FAB95B',
                opacity: 0.3,
              }}
            >
              $
            </div>
          );
        })}

        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 700,
              fontSize: 38,
              color: '#9FB0BF',
              letterSpacing: 4,
              textTransform: 'uppercase',
            }}
          >
            Annual Increase
          </div>
        </div>

        {/* HERO $4,800 */}
        <div
          style={{
            position: 'absolute',
            top: 180,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(20), [0, 1], [0, 1]),
            transform: `scale(${heroScale * heroPulse})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 900,
              fontSize: 260,
              color: '#FAB95B',
              letterSpacing: -8,
              lineHeight: 1,
              textShadow: '0 0 80px rgba(250,185,91,0.5)',
            }}
          >
            +${displayV.toLocaleString()}
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 700,
              fontSize: 44,
              color: '#E8EDF2',
              letterSpacing: 6,
              marginTop: -10,
            }}
          >
            EVERY YEAR
          </div>
        </div>

        {/* Three pills — guarantees */}
        <div
          style={{
            position: 'absolute',
            bottom: 200,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 30,
          }}
        >
          {[
            { label: 'FEDERAL LAW', sub: 'Locked in' },
            { label: 'INFLATION-PROOF', sub: 'COLA adjusted' },
            { label: 'FOR LIFE', sub: 'Never expires' },
          ].map((p, i) => (
            <div
              key={p.label}
              style={{
                opacity: pillOp(i),
                transform: `translateY(${pillY(i)}px)`,
                padding: '20px 32px',
                background: 'rgba(250,185,91,0.1)',
                border: '2px solid #FAB95B',
                borderRadius: 50,
                textAlign: 'center',
              }}
            >
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontWeight: 800,
                  fontSize: 30,
                  color: '#FAB95B',
                  letterSpacing: 2,
                }}
              >
                {p.label}
              </div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontWeight: 500,
                  fontSize: 22,
                  color: '#E8EDF2',
                  opacity: 0.8,
                  marginTop: 4,
                }}
              >
                {p.sub}
              </div>
            </div>
          ))}
        </div>

        {/* Inflation arrow growing upward */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: inflProgress,
            fontFamily: 'Inter, Poppins',
            fontWeight: 700,
            fontSize: 30,
            color: '#9FB0BF',
            letterSpacing: 3,
          }}
        >
          ▲ Grows with inflation every year ▲
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.45) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
            letterSpacing: 1,
          }}
        >
          Senior Fund USA
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene34;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan up
  const camY = interpolate(frame, [0, durationInFrames], [20, -20]);

  // Three checklist items
  const itemOp = (i: number) => interpolate(sp(20 + i * 25), [0, 1], [0, 1]);
  const itemX = (i: number) => interpolate(sp(20 + i * 25), [0, 1], [-60, 0]);
  const checkScale = (i: number) => sp(40 + i * 25, { damping: 7, stiffness: 200 });

  // Bottom verdict
  const verdictScale = sp(150, { damping: 8, stiffness: 110 });
  const verdictOp = interpolate(sp(150), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947' }}>
      <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp, transform: `translateY(${camY}px)` }}>

        {/* Gradient */}
        <AbsoluteFill
          style={{
            background: 'linear-gradient(135deg, #2C3947 0%, #243240 50%, #2C3947 100%)',
          }}
        />

        {/* Shape layer */}
        <div
          style={{
            position: 'absolute',
            right: -200,
            top: -150,
            width: 600,
            height: 600,
            background: '#FAB95B',
            opacity: 0.05,
            borderRadius: 30,
            transform: `rotate(${15 + frame * 0.08}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: -100,
            bottom: -150,
            width: 500,
            height: 500,
            border: '3px solid #E8EDF2',
            opacity: 0.05,
            borderRadius: '50%',
            transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
          }}
        />

        {/* Vertical lines texture */}
        {Array.from({ length: 24 }).map((_, i) => (
          <div
            key={`vl-${i}`}
            style={{
              position: 'absolute',
              left: `${(i / 24) * 100}%`,
              top: 0,
              width: 1,
              height: '100%',
              background: '#E8EDF2',
              opacity: 0.03,
            }}
          />
        ))}

        {/* Particles */}
        {Array.from({ length: 18 }).map((_, i) => {
          const px = seededRandom(i + 3) * 1920;
          const py = seededRandom(i + 23) * 1080;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px + Math.sin(frame * 0.02 + i) * 15,
                top: py,
                width: 4,
                height: 4,
                borderRadius: '50%',
                background: '#FAB95B',
                opacity: 0.35,
              }}
            />
          );
        })}

        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 800,
              fontSize: 58,
              color: '#E8EDF2',
              letterSpacing: 3,
            }}
          >
            CAN YOU AFFORD <span style={{ color: '#FAB95B' }}>TO WAIT?</span>
          </div>
        </div>

        {/* Checklist */}
        <div
          style={{
            position: 'absolute',
            top: 240,
            left: '50%',
            transform: 'translateX(-50%)',
            width: 900,
          }}
        >
          {[
            { label: 'Other Savings or Investments', icon: '◆' },
            { label: 'A Pension or 401(k)', icon: '■' },
            { label: 'Still Working / Side Income', icon: '●' },
          ].map((item, i) => (
            <div
              key={item.label}
              style={{
                opacity: itemOp(i),
                transform: `translateX(${itemX(i)}px)`,
                marginBottom: 28,
                padding: '24px 32px',
                background: 'rgba(232,237,242,0.05)',
                border: '2px solid rgba(250,185,91,0.3)',
                borderRadius: 18,
                display: 'flex',
                alignItems: 'center',
                gap: 28,
              }}
            >
              {/* Icon */}
              <div
                style={{
                  width: 80,
                  height: 80,
                  borderRadius: 14,
                  background: '#FAB95B',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontSize: 50,
                  color: '#2C3947',
                  fontWeight: 900,
                  flexShrink: 0,
                }}
              >
                {item.icon}
              </div>

              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontWeight: 700,
                  fontSize: 42,
                  color: '#E8EDF2',
                  flex: 1,
                  letterSpacing: 1,
                }}
              >
                {item.label}
              </div>

              {/* Check */}
              <div
                style={{
                  width: 70,
                  height: 70,
                  borderRadius: '50%',
                  background: '#FAB95B',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  transform: `scale(${checkScale(i)})`,
                }}
              >
                <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
                  <path d="M5 12l5 5L20 7" stroke="#2C3947" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
              </div>
            </div>
          ))}
        </div>

        {/* Verdict box */}
        <div
          style={{
            position: 'absolute',
            bottom: 120,
            left: '50%',
            transform: `translateX(-50%) scale(${verdictScale})`,
            opacity: verdictOp,
            padding: '24px 56px',
            background: '#FAB95B',
            borderRadius: 14,
            boxShadow: '0 0 60px rgba(250,185,91,0.5)',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 900,
              fontSize: 52,
              color: '#2C3947',
              letterSpacing: 3,
              textAlign: 'center',
            }}
          >
            DELAY = MAXIMUM WEALTH
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.45) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
            letterSpacing: 1,
          }}
        >
          Senior Fund USA
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene35;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene36: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Note paper tilt
  const noteRot = interpolate(sp(10), [0, 1], [-8, -3]);
  const noteScale = sp(10, { damping: 9, stiffness: 100 });

  // Underline draw
  const underlineW = interpolate(sp(70), [0, 1], [0, 100]);

  // Handwriting reveal — line by line
  const line1 = interpolate(sp(40), [0, 1], [0, 1]);
  const line2 = interpolate(sp(80), [0, 1], [0, 1]);
  const line3 = interpolate(sp(120), [0, 1], [0, 1]);

  // Bonus 4 incoming
  const bonus4Op = interpolate(sp(180), [0, 1], [0, 1]);
  const bonus4Scale = sp(180, { damping: 8, stiffness: 130 });
  const bonus4Slide = interpolate(sp(180), [0, 1], [200, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947' }}>
      <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>

        {/* Gradient */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at 30% 60%, rgba(250,185,91,0.14) 0%, transparent 55%), linear-gradient(160deg, #2C3947 0%, #1F2A36 100%)',
          }}
        />

        {/* Shape layer */}
        <div
          style={{
            position: 'absolute',
            right: 100,
            top: 80,
            width: 350,
            height: 350,
            border: '2px solid #FAB95B',
            opacity: 0.08,
            borderRadius: '50%',
            transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            left: -150,
            top: 350,
            width: 450,
            height: 450,
            background: '#E8EDF2',
            opacity: 0.03,
            borderRadius: 30,
            transform: `rotate(${frame * 0.08}deg)`,
          }}
        />

        {/* Texture — graph paper feel */}
        {Array.from({ length: 30 }).map((_, i) => (
          <div
            key={`gh-${i}`}
            style={{
              position: 'absolute',
              left: 0,
              right: 0,
              top: i * 40,
              height: 1,
              background: '#E8EDF2',
              opacity: 0.03,
            }}
          />
        ))}

        {/* Particles */}
        {Array.from({ length: 16 }).map((_, i) => {
          const px = seededRandom(i + 17) * 1920;
          const py = seededRandom(i + 41) * 1080;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: py + Math.sin(frame * 0.02 + i) * 18,
                width: 4,
                height: 4,
                borderRadius: '50%',
                background: '#FAB95B',
                opacity: 0.35,
              }}
            />
          );
        })}

        {/* "WRITE THIS DOWN" tag */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 800,
              fontSize: 38,
              color: '#FAB95B',
              letterSpacing: 6,
              display: 'inline-block',
              border: '3px dashed #FAB95B',
              padding: '10px 28px',
              borderRadius: 8,
            }}
          >
            ✎ WRITE THIS DOWN
          </div>
        </div>

        {/* Note card */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            left: 140,
            width: 880,
            background: '#E8EDF2',
            borderRadius: 16,
            padding: '40px 50px',
            transform: `rotate(${noteRot}deg) scale(${noteScale})`,
            boxShadow: '0 25px 80px rgba(0,0,0,0.4)',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 800,
              fontSize: 54,
              color: '#2C3947',
              lineHeight: 1.2,
              opacity: line1,
            }}
          >
            Every year you wait
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 800,
              fontSize: 54,
              color: '#2C3947',
              lineHeight: 1.2,
              opacity: line2,
              marginTop: 8,
            }}
          >
            between <span style={{ color: '#FAB95B', position: 'relative' }}>
              age 67 & 70
              <div
                style={{
                  position: 'absolute',
                  bottom: -6,
                  left: 0,
                  width: `${underlineW}%`,
                  height: 5,
                  background: '#FAB95B',
                }}
              />
            </span>
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 900,
              fontSize: 82,
              color: '#FAB95B',
              lineHeight: 1.1,
              opacity: line3,
              marginTop: 16,
              letterSpacing: -1,
            }}
          >
            = an 8% raise.
          </div>
        </div>

        {/* Right side — Bonus 4 teaser */}
        <div
          style={{
            position: 'absolute',
            right: 80,
            bottom: 140,
            opacity: bonus4Op,
            transform: `translateX(${bonus4Slide}px) scale(${bonus4Scale})`,
            padding: '24px 36px',
            background: '#FAB95B',
            borderRadius: 18,
            boxShadow: '0 0 60px rgba(250,185,91,0.5)',
            maxWidth: 600,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 700,
              fontSize: 26,
              color: '#2C3947',
              letterSpacing: 3,
              opacity: 0.8,
            }}
          >
            NEXT UP →
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 900,
              fontSize: 60,
              color: '#2C3947',
              letterSpacing: -1,
              lineHeight: 1,
              marginTop: 4,
            }}
          >
            BONUS #4
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 600,
              fontSize: 30,
              color: '#2C3947',
              opacity: 0.85,
              marginTop: 4,
            }}
          >
            The Medicare Surprise
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.45) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
            letterSpacing: 1,
          }}
        >
          Senior Fund USA
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene36;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene37: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Card flip in
  const cardScale = sp(15, { damping: 10, stiffness: 100 });
  const cardRot = interpolate(sp(15), [0, 1], [-15, 0]);

  // Title slam
  const titleScale = sp(60, { damping: 7, stiffness: 150 });
  const titleOp = interpolate(sp(60), [0, 1], [0, 1]);

  // Part B badge
  const badgeOp = interpolate(sp(120), [0, 1], [0, 1]);
  const badgeY = interpolate(sp(120), [0, 1], [40, 0]);

  // Give-back text
  const giveOp = interpolate(sp(160), [0, 1], [0, 1]);
  const givePulse = 1 + Math.sin(frame * 0.15) * 0.025;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947' }}>
      <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>

        {/* Gradient */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at 70% 30%, rgba(250,185,91,0.15) 0%, transparent 55%), linear-gradient(135deg, #2C3947 0%, #243240 100%)',
          }}
        />

        {/* Shape layer */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '50%',
            width: 1000,
            height: 1000,
            border: '2px solid #E8EDF2',
            opacity: 0.04,
            borderRadius: '50%',
            transform: `translate(-50%,-50%) scale(${1 + Math.sin(frame * 0.02) * 0.03})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: -180,
            top: 100,
            width: 500,
            height: 500,
            background: '#FAB95B',
            opacity: 0.06,
            borderRadius: 40,
            transform: `rotate(${20 + frame * 0.08}deg)`,
          }}
        />

        {/* Grid texture */}
        {Array.from({ length: 14 }).map((_, i) => (
          <div
            key={`tx-${i}`}
            style={{
              position: 'absolute',
              top: 0,
              bottom: 0,
              left: `${(i / 14) * 100}%`,
              width: 1,
              background: '#E8EDF2',
              opacity: 0.04,
            }}
          />
        ))}

        {/* Particles */}
        {Array.from({ length: 22 }).map((_, i) => {
          const px = seededRandom(i + 4) * 1920;
          const py = seededRandom(i + 44) * 1080;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px + Math.sin(frame * 0.025 + i) * 16,
                top: py,
                width: 4,
                height: 4,
                borderRadius: '50%',
                background: '#FAB95B',
                opacity: 0.4,
              }}
            />
          );
        })}

        {/* BONUS 4 label */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 700,
              fontSize: 30,
              color: '#9FB0BF',
              letterSpacing: 8,
            }}
          >
            BONUS NUMBER FOUR
          </div>
        </div>

        {/* Big title */}
        <div
          style={{
            position: 'absolute',
            top: 150,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
            transform: `scale(${titleScale})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 900,
              fontSize: 130,
              color: '#E8EDF2',
              lineHeight: 1,
              letterSpacing: -3,
            }}
          >
            THE <span style={{ color: '#FAB95B' }}>MEDICARE</span>
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 900,
              fontSize: 130,
              color: '#FAB95B',
              lineHeight: 1,
              letterSpacing: -3,
              opacity: giveOp,
              transform: `scale(${givePulse})`,
            }}
          >
            GIVE-BACK
          </div>
        </div>

        {/* Medicare card visual */}
        <div
          style={{
            position: 'absolute',
            bottom: 200,
            left: '50%',
            transform: `translateX(-50%) rotate(${cardRot}deg) scale(${cardScale})`,
            width: 720,
            height: 220,
            background: 'linear-gradient(135deg, #E8EDF2 0%, #c9d4dd 100%)',
            borderRadius: 18,
            border: '3px solid #FAB95B',
            padding: '24px 36px',
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'space-between',
            boxShadow: '0 25px 60px rgba(0,0,0,0.5)',
          }}
        >
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 800,
                fontSize: 34,
                color: '#2C3947',
                letterSpacing: 2,
              }}
            >
              MEDICARE
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 700,
                fontSize: 22,
                color: '#2C3947',
                opacity: 0.6,
                letterSpacing: 3,
              }}
            >
              HEALTH INSURANCE
            </div>
          </div>
          <div style={{ display: 'flex', alignItems: 'flex-end', justifyContent: 'space-between' }}>
            <div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontWeight: 600,
                  fontSize: 20,
                  color: '#2C3947',
                  opacity: 0.6,
                  letterSpacing: 2,
                }}
              >
                COVERAGE
              </div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontWeight: 900,
                  fontSize: 76,
                  color: '#2C3947',
                  lineHeight: 1,
                }}
              >
                PART <span style={{ color: '#FAB95B' }}>B</span>
              </div>
            </div>
            <div
              style={{
                opacity: badgeOp,
                transform: `translateY(${badgeY}px)`,
                padding: '12px 24px',
                background: '#FAB95B',
                borderRadius: 10,
                fontFamily: 'Inter, Poppins',
                fontWeight: 800,
                fontSize: 30,
                color: '#2C3947',
                letterSpacing: 1,
              }}
            >
              ELIGIBLE @ 65
            </div>
          </div>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.45) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
            letterSpacing: 1,
          }}
        >
          Senior Fund USA
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene37;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene38: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera subtle zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  // SS check slides in left
  const checkX = interpolate(sp(15), [0, 1], [-400, 0]);
  const checkOp = interpolate(sp(15), [0, 1], [0, 1]);

  // Deduction arrow
  const arrowOp = interpolate(sp(90), [0, 1], [0, 1]);
  const arrowX = interpolate(frame, [90, durationInFrames], [0, 30]);

  // Counter for $202.90
  const valStart = 60;
  const valEnd = 140;
  const v = interpolate(frame, [valStart, valEnd], [0, 20290], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const displayV = (v / 100).toFixed(2);

  // Bill paper appears
  const billScale = sp(70, { damping: 9, stiffness: 110 });
  const billRot = interpolate(sp(70), [0, 1], [8, 3]);

  // Bottom mystery line
  const mysteryOp = interpolate(sp(190), [0, 1], [0, 1]);
  const mysteryGlow = 0.5 + Math.sin(frame * 0.1) * 0.3;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947' }}>
      <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp, transform: `scale(${camScale})` }}>

        {/* Gradient */}
        <AbsoluteFill
          style={{
            background: 'linear-gradient(135deg, #2C3947 0%, #1F2A36 100%)',
          }}
        />

        {/* Shape layer */}
        <div
          style={{
            position: 'absolute',
            left: -150,
            bottom: -150,
            width: 500,
            height: 500,
            background: '#FAB95B',
            opacity: 0.05,
            borderRadius: '50%',
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: 100,
            top: 50,
            width: 400,
            height: 400,
            border: '2px solid #FAB95B',
            opacity: 0.06,
            borderRadius: 30,
            transform: `rotate(${frame * 0.1}deg)`,
          }}
        />

        {/* Texture */}
        {Array.from({ length: 20 }).map((_, i) => (
          <div
            key={`tx-${i}`}
            style={{
              position: 'absolute',
              top: i * 60,
              left: 0,
              right: 0,
              height: 1,
              background: '#E8EDF2',
              opacity: 0.025,
            }}
          />
        ))}

        {/* Particles */}
        {Array.from({ length: 18 }).map((_, i) => {
          const px = seededRandom(i + 9) * 1920;
          const py = seededRandom(i + 29) * 1080;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: py + Math.sin(frame * 0.02 + i) * 16,
                width: 4,
                height: 4,
                borderRadius: '50%',
                background: '#FAB95B',
                opacity: 0.35,
              }}
            />
          );
        })}

        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 800,
              fontSize: 50,
              color: '#E8EDF2',
              letterSpacing: 3,
            }}
          >
            PART B PREMIUM <span style={{ color: '#FAB95B' }}>2026</span>
          </div>
        </div>

        {/* Left: SS check */}
        <div
          style={{
            position: 'absolute',
            top: 220,
            left: 100,
            width: 620,
            height: 280,
            background: 'linear-gradient(135deg, #E8EDF2 0%, #c9d4dd 100%)',
            borderRadius: 14,
            padding: '24px 30px',
            opacity: checkOp,
            transform: `translateX(${checkX}px)`,
            boxShadow: '0 20px 50px rgba(0,0,0,0.4)',
            border: '2px solid rgba(0,0,0,0.1)',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 700,
              fontSize: 22,
              color: '#2C3947',
              opacity: 0.6,
              letterSpacing: 3,
            }}
          >
            SOCIAL SECURITY CHECK
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 600,
              fontSize: 26,
              color: '#2C3947',
              marginTop: 10,
              opacity: 0.7,
            }}
          >
            Pay to: You
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 900,
              fontSize: 100,
              color: '#2C3947',
              lineHeight: 1,
              marginTop: 12,
            }}
          >
            $2,000<span style={{ color: '#9FB0BF', fontSize: 50 }}>.00</span>
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 600,
              fontSize: 20,
              color: '#2C3947',
              opacity: 0.5,
              marginTop: 6,
              letterSpacing: 2,
            }}
          >
            MONTHLY BENEFIT
          </div>
        </div>

        {/* Arrow */}
        <div
          style={{
            position: 'absolute',
            top: 330,
            left: 740,
            opacity: arrowOp,
            transform: `translateX(${arrowX}px)`,
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 80,
            color: '#FAB95B',
          }}
        >
          →
        </div>

        {/* Right: Premium bill */}
        <div
          style={{
            position: 'absolute',
            top: 220,
            right: 100,
            width: 540,
            background: '#FAB95B',
            borderRadius: 14,
            padding: '28px 32px',
            transform: `scale(${billScale}) rotate(${billRot}deg)`,
            boxShadow: '0 20px 50px rgba(250,185,91,0.4)',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 700,
              fontSize: 22,
              color: '#2C3947',
              opacity: 0.7,
              letterSpacing: 3,
            }}
          >
            MEDICARE PART B
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 900,
              fontSize: 96,
              color: '#2C3947',
              lineHeight: 1,
              marginTop: 14,
              letterSpacing: -2,
            }}
          >
            ${displayV}
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 700,
              fontSize: 28,
              color: '#2C3947',
              opacity: 0.85,
              marginTop: 6,
              letterSpacing: 2,
            }}
          >
            DEDUCTED EVERY MONTH
          </div>
        </div>

        {/* Mystery teaser bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: mysteryOp,
            fontFamily: 'Inter, Poppins',
            fontWeight: 800,
            fontSize: 46,
            color: '#FAB95B',
            letterSpacing: 3,
            textShadow: `0 0 ${20 + mysteryGlow * 30}px rgba(250,185,91,${mysteryGlow})`,
          }}
        >
          BUT THERE'S A WAY OUT...
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.45) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
            letterSpacing: 1,
          }}
        >
          Senior Fund USA
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene38;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene39: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Diagram nodes
  const node1Op = interpolate(sp(10), [0, 1], [0, 1]);
  const node2Op = interpolate(sp(70), [0, 1], [0, 1]);
  const node3Op = interpolate(sp(130), [0, 1], [0, 1]);

  // Money flow particles (animated dots traveling along path)
  const flowProgress = (delay: number) => ((frame - delay) % 60) / 60;

  // Highlight on give-back
  const highlightOp = interpolate(sp(180), [0, 1], [0, 1]);
  const highlightPulse = 1 + Math.sin(frame * 0.15) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947' }}>
      <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>

        {/* Gradient */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at 50% 60%, rgba(250,185,91,0.15) 0%, transparent 60%), linear-gradient(180deg, #2C3947 0%, #243240 100%)',
          }}
        />

        {/* Shape layer */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '50%',
            width: 900,
            height: 900,
            border: '2px solid #FAB95B',
            opacity: 0.05,
            borderRadius: '50%',
            transform: `translate(-50%,-50%) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            right: -100,
            top: -100,
            width: 380,
            height: 380,
            background: '#E8EDF2',
            opacity: 0.03,
            borderRadius: 30,
            transform: `rotate(${frame * 0.1}deg)`,
          }}
        />

        {/* Dot grid */}
        {Array.from({ length: 80 }).map((_, i) => {
          const cols = 16;
          const x = (i % cols) * 120 + 60;
          const y = Math.floor(i / cols) * 180 + 100;
          return (
            <div
              key={`g-${i}`}
              style={{
                position: 'absolute',
                left: x,
                top: y,
                width: 3,
                height: 3,
                borderRadius: '50%',
                background: '#E8EDF2',
                opacity: 0.1,
              }}
            />
          );
        })}

        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 800,
              fontSize: 52,
              color: '#E8EDF2',
              letterSpacing: 3,
            }}
          >
            THE <span style={{ color: '#FAB95B' }}>GIVE-BACK</span> FLOW
          </div>
        </div>

        {/* Three node diagram */}
        {/* Node 1: Medicare Advantage Plan */}
        <div
          style={{
            position: 'absolute',
            top: 280,
            left: 100,
            width: 380,
            opacity: node1Op,
            transform: `scale(${sp(10)})`,
          }}
        >
          <div
            style={{
              background: 'rgba(232,237,242,0.08)',
              border: '3px solid #E8EDF2',
              borderRadius: 18,
              padding: '28px 24px',
              textAlign: 'center',
              height: 220,
              display: 'flex',
              flexDirection: 'column',
              justifyContent: 'center',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 700,
                fontSize: 24,
                color: '#9FB0BF',
                letterSpacing: 3,
                marginBottom: 6,
              }}
            >
              STEP 1
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 900,
                fontSize: 44,
                color: '#E8EDF2',
                lineHeight: 1.1,
              }}
            >
              MEDICARE
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 900,
                fontSize: 44,
                color: '#E8EDF2',
                lineHeight: 1.1,
              }}
            >
              ADVANTAGE
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 600,
                fontSize: 22,
                color: '#FAB95B',
                marginTop: 8,
                letterSpacing: 2,
              }}
            >
              PLAN ENROLLED
            </div>
          </div>
        </div>

        {/* Flow line 1→2 */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            left: 480,
            width: 240,
            height: 4,
            background: '#FAB95B',
            opacity: 0.4,
          }}
        />
        {Array.from({ length: 3 }).map((_, i) => (
          <div
            key={`f1-${i}`}
            style={{
              position: 'absolute',
              top: 374,
              left: 480 + flowProgress(i * 20) * 240,
              width: 16,
              height: 16,
              borderRadius: '50%',
              background: '#FAB95B',
              opacity: node2Op,
              boxShadow: '0 0 12px #FAB95B',
            }}
          />
        ))}

        {/* Node 2: Plan pays */}
        <div
          style={{
            position: 'absolute',
            top: 280,
            left: 720,
            width: 380,
            opacity: node2Op,
            transform: `scale(${sp(70)})`,
          }}
        >
          <div
            style={{
              background: 'rgba(250,185,91,0.12)',
              border: '3px solid #FAB95B',
              borderRadius: 18,
              padding: '28px 24px',
              textAlign: 'center',
              height: 220,
              display: 'flex',
              flexDirection: 'column',
              justifyContent: 'center',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 700,
                fontSize: 24,
                color: '#FAB95B',
                letterSpacing: 3,
                marginBottom: 6,
              }}
            >
              STEP 2
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 900,
                fontSize: 44,
                color: '#E8EDF2',
                lineHeight: 1.1,
              }}
            >
              PLAN PAYS
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 900,
                fontSize: 44,
                color: '#FAB95B',
                lineHeight: 1.1,
              }}
            >
              YOUR PREMIUM
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 600,
                fontSize: 22,
                color: '#E8EDF2',
                marginTop: 8,
                letterSpacing: 2,
                opacity: 0.8,
              }}
            >
              PART OR ALL
            </div>
          </div>
        </div>

        {/* Flow line 2→3 */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            left: 1100,
            width: 240,
            height: 4,
            background: '#FAB95B',
            opacity: 0.4,
          }}
        />
        {Array.from({ length: 3 }).map((_, i) => (
          <div
            key={`f2-${i}`}
            style={{
              position: 'absolute',
              top: 374,
              left: 1100 + flowProgress(i * 20 + 30) * 240,
              width: 16,
              height: 16,
              borderRadius: '50%',
              background: '#FAB95B',
              opacity: node3Op,
              boxShadow: '0 0 12px #FAB95B',
            }}
          />
        ))}

        {/* Node 3: You keep */}
        <div
          style={{
            position: 'absolute',
            top: 280,
            left: 1340,
            width: 380,
            opacity: node3Op,
            transform: `scale(${sp(130) * (1 + Math.sin(frame * 0.1) * 0.015)})`,
          }}
        >
          <div
            style={{
              background: '#FAB95B',
              borderRadius: 18,
              padding: '28px 24px',
              textAlign: 'center',
              height: 220,
              display: 'flex',
              flexDirection: 'column',
              justifyContent: 'center',
              boxShadow: '0 0 60px rgba(250,185,91,0.5)',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 700,
                fontSize: 24,
                color: '#2C3947',
                letterSpacing: 3,
                opacity: 0.7,
                marginBottom: 6,
              }}
            >
              STEP 3
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 900,
                fontSize: 44,
                color: '#2C3947',
                lineHeight: 1.1,
              }}
            >
              YOU KEEP
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 900,
                fontSize: 72,
                color: '#2C3947',
                lineHeight: 1,
                marginTop: 8,
              }}
            >
              MORE
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 600,
                fontSize: 22,
                color: '#2C3947',
                marginTop: 4,
                letterSpacing: 2,
                opacity: 0.8,
              }}
            >
              IN YOUR POCKET
            </div>
          </div>
        </div>

        {/* Bottom takeaway */}
        <div
          style={{
            position: 'absolute',
            bottom: 130,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: highlightOp,
            transform: `scale(${highlightPulse})`,
            fontFamily: 'Inter, Poppins',
            fontWeight: 800,
            fontSize: 42,
            color: '#E8EDF2',
            letterSpacing: 2,
          }}
        >
          Your check stays the <span style={{ color: '#FAB95B' }}>SAME</span> — Medicare cost drops to <span style={{ color: '#FAB95B' }}>$0</span>
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.45) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
            letterSpacing: 1,
          }}
        >
          Senior Fund USA
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene39;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene40: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // BEFORE strikethrough draw
  const strikeW = interpolate(sp(50), [0, 1], [0, 100]);

  // Counter from 202.90 → 0
  const dropStart = 80;
  const dropEnd = 170;
  const v = interpolate(frame, [dropStart, dropEnd], [20290, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const displayV = (v / 100).toFixed(2);

  // Zero hero
  const zeroScale = sp(170, { damping: 7, stiffness: 130 });
  const zeroPulse = 1 + Math.sin(frame * 0.15) * 0.03;

  // Side label "or just $50"
  const sideOp = interpolate(sp(210), [0, 1], [0, 1]);
  const sideX = interpolate(sp(210), [0, 1], [60, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#2C3947' }}>
      <AbsoluteFill style={{ backgroundColor: '#2C3947', overflow: 'hidden', opacity: exitOp }}>

        {/* Gradient */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at 50% 50%, rgba(250,185,91,0.18) 0%, transparent 60%), linear-gradient(135deg, #2C3947 0%, #1F2A36 100%)',
          }}
        />

        {/* Shape layer — burst lines from center */}
        {Array.from({ length: 12 }).map((_, i) => {
          const angle = (i / 12) * Math.PI * 2;
          const len = 600;
          return (
            <div
              key={`ray-${i}`}
              style={{
                position: 'absolute',
                left: '50%',
                top: '50%',
                width: len,
                height: 2,
                background: '#FAB95B',
                opacity: 0.08,
                transformOrigin: '0 50%',
                transform: `rotate(${angle + frame * 0.005}rad)`,
              }}
            />
          );
        })}

        {/* Big rotating ring */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: '50%',
            width: 850,
            height: 850,
            border: '3px dashed #FAB95B',
            borderRadius: '50%',
            opacity: 0.1,
            transform: `translate(-50%,-50%) rotate(${frame * 0.5}deg)`,
          }}
        />

        {/* Particles */}
        {Array.from({ length: 24 }).map((_, i) => {
          const px = seededRandom(i + 13) * 1920;
          const py = seededRandom(i + 27) * 1080;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: px,
                top: py + Math.sin(frame * 0.025 + i) * 20,
                width: 5,
                height: 5,
                borderRadius: '50%',
                background: '#FAB95B',
                opacity: 0.45,
                boxShadow: '0 0 8px #FAB95B',
              }}
            />
          );
        })}

        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(0), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 800,
              fontSize: 56,
              color: '#E8EDF2',
              letterSpacing: 3,
            }}
          >
            YOUR MEDICARE COST: <span style={{ color: '#FAB95B' }}>GONE</span>
          </div>
        </div>

        {/* BEFORE row */}
        <div
          style={{
            position: 'absolute',
            top: 220,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(15), [0, 1], [0, 1]),
            transform: `translateY(${interpolate(sp(15), [0, 1], [20, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 700,
              fontSize: 30,
              color: '#9FB0BF',
              letterSpacing: 6,
              marginBottom: 12,
            }}
          >
            BEFORE
          </div>
          <div style={{ position: 'relative', display: 'inline-block' }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 900,
                fontSize: 110,
                color: '#9FB0BF',
                lineHeight: 1,
                letterSpacing: -2,
              }}
            >
              $202.90/mo
            </div>
            {/* Strikethrough */}
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                height: 6,
                width: `${strikeW}%`,
                background: '#FAB95B',
                transform: 'translateY(-50%) rotate(-3deg)',
              }}
            />
          </div>
        </div>

        {/* Arrow down */}
        <div
          style={{
            position: 'absolute',
            top: 420,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(110), [0, 1], [0, 1]),
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 60,
            color: '#FAB95B',
            transform: `translateY(${Math.sin(frame * 0.18) * 6}px)`,
          }}
        >
          ↓
        </div>

        {/* AFTER row — big ZERO */}
        <div
          style={{
            position: 'absolute',
            top: 510,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(130), [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 700,
              fontSize: 30,
              color: '#FAB95B',
              letterSpacing: 6,
              marginBottom: 12,
            }}
          >
            AFTER GIVE-BACK
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 900,
              fontSize: 220,
              color: '#FAB95B',
              lineHeight: 1,
              letterSpacing: -8,
              transform: `scale(${zeroScale * zeroPulse})`,
              textShadow: '0 0 80px rgba(250,185,91,0.6)',
            }}
          >
            ${displayV.startsWith('0.0') ? '0' : displayV}
            <span style={{ fontSize: 80, color: '#E8EDF2' }}>/mo</span>
          </div>
        </div>

        {/* Side note "or just $50" */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sideOp,
            transform: `translateX(${sideX}px)`,
            fontFamily: 'Inter, Poppins',
            fontWeight: 600,
            fontSize: 34,
            color: '#9FB0BF',
            letterSpacing: 2,
          }}
        >
          ...or maybe just <span style={{ color: '#FAB95B', fontWeight: 800 }}>$50</span>. Either way — you win.
        </div>

        {/* Vignette */}
        <AbsoluteFill
          style={{
            background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.45) 100%)',
            pointerEvents: 'none',
          }}
        />

        {/* Watermark */}
        <div
          style={{
            position: 'absolute',
            bottom: 30,
            right: 45,
            fontFamily: 'Inter, Poppins',
            fontSize: 22,
            fontWeight: 600,
            color: '#9E9E9E',
            opacity: 0.3,
            letterSpacing: 1,
          }}
        >
          Senior Fund USA
        </div>
      </AbsoluteFill>
    </AbsoluteFill>
  );
};

export default Scene40;
```

