```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31_DebtVsBoost',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene31_DebtVsBoost: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const cameraScale = interpolate(frame, [0, durationInFrames], [1.0, 1.06], { easing: Easing.inOut(Easing.ease) });

  // LEFT COLUMN entrance
  const leftIn = sp(20);
  const leftBarHeight = interpolate(sp(35), [0, 1], [0, 180]);
  const plus8Op = interpolate(frame, [55, 75], [0, 1], { extrapolateRight: 'clamp' });

  // VS divider
  const vsScale = sp(80, { damping: 8, stiffness: 180 });

  // RIGHT COLUMN entrance
  const rightIn = sp(95);
  const rightBarHeight = interpolate(sp(115), [0, 1], [0, 380]);
  const minus20Op = interpolate(frame, [140, 165], [0, 1], { extrapolateRight: 'clamp' });

  // Net loss callout
  const lossIn = sp(190, { damping: 10, stiffness: 140 });
  const lossPulse = 1 + Math.sin(frame * 0.18) * 0.025;

  // Shake on impact
  const shake = frame > 190 && frame < 210 ? Math.sin(frame * 1.5) * 4 : 0;

  // Headline
  const headlineIn = sp(5);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 40%, rgba(254,236,65,0.18) 0%, rgba(252,209,209,0) 65%)',
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill style={{ transform: `scale(${cameraScale}) translateX(${shake}px)` }}>
        {/* Large drifting circle */}
        <div
          style={{
            position: 'absolute',
            top: -200,
            left: -150,
            width: 600,
            height: 600,
            borderRadius: '50%',
            border: '3px solid #000000',
            opacity: 0.06,
            transform: `rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
          }}
        />
        {/* Triangle */}
        <svg
          style={{
            position: 'absolute',
            bottom: -100,
            right: -80,
            opacity: 0.05,
            transform: `rotate(${-frame * 0.08}deg)`,
          }}
          width="500"
          height="500"
          viewBox="0 0 100 100"
        >
          <polygon points="50,5 95,95 5,95" fill="none" stroke="#000000" strokeWidth="1" />
        </svg>
        {/* Rectangle */}
        <div
          style={{
            position: 'absolute',
            top: '30%',
            right: '8%',
            width: 280,
            height: 280,
            border: '2px solid #FEEC41',
            opacity: 0.4,
            transform: `rotate(${15 + Math.sin(frame * 0.03) * 5}deg)`,
          }}
        />

        {/* Grid texture */}
        <svg
          style={{ position: 'absolute', inset: 0, opacity: 0.08 }}
          width="100%"
          height="100%"
        >
          <defs>
            <pattern id="grid31" width="60" height="60" patternUnits="userSpaceOnUse">
              <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#000000" strokeWidth="1" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#grid31)" />
        </svg>

        {/* Floating particles */}
        {Array.from({ length: 14 }).map((_, i) => {
          const x = seededRandom(i + 1) * 1920;
          const y = seededRandom(i + 50) * 1080;
          const drift = Math.sin(frame * 0.02 + i) * 20;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x + drift,
                top: y,
                width: 6,
                height: 6,
                borderRadius: '50%',
                backgroundColor: '#000000',
                opacity: 0.15,
              }}
            />
          );
        })}

        {/* HEADLINE */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headlineIn,
            transform: `translateY(${(1 - headlineIn) * -30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 78,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -2,
              lineHeight: 1.05,
            }}
          >
            THE BRUTAL <span style={{ color: '#FEEC41', backgroundColor: '#000000', padding: '0 18px' }}>MATH</span>
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              fontWeight: 600,
              color: '#000000',
              opacity: 0.6,
              marginTop: 12,
              letterSpacing: 4,
            }}
          >
            DELAY BOOST vs CREDIT CARD INTEREST
          </div>
        </div>

        {/* LEFT COLUMN — Delayed Boost */}
        <div
          style={{
            position: 'absolute',
            left: 220,
            top: 320,
            width: 380,
            opacity: leftIn,
            transform: `translateY(${(1 - leftIn) * 40}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.7,
              marginBottom: 16,
              letterSpacing: 1,
            }}
          >
            DELAYED CREDITS
          </div>

          {/* Bar chart visual */}
          <div
            style={{
              width: 380,
              height: 260,
              border: '4px solid #000000',
              backgroundColor: '#FEEC41',
              position: 'relative',
              display: 'flex',
              alignItems: 'flex-end',
              justifyContent: 'center',
              padding: 20,
            }}
          >
            <div
              style={{
                width: 140,
                height: leftBarHeight,
                backgroundColor: '#000000',
                position: 'relative',
              }}
            >
              <div
                style={{
                  position: 'absolute',
                  top: -50,
                  left: '50%',
                  transform: 'translateX(-50%)',
                  fontFamily: 'Inter, Poppins',
                  fontSize: 38,
                  fontWeight: 900,
                  color: '#000000',
                  opacity: plus8Op,
                  whiteSpace: 'nowrap',
                }}
              >
                +8%
              </div>
            </div>
          </div>

          <div
            style={{
              marginTop: 20,
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              fontWeight: 600,
              color: '#000000',
              opacity: 0.75,
              textAlign: 'center',
              letterSpacing: 1,
            }}
          >
            PER YEAR YOU WAIT
          </div>
        </div>

        {/* VS DIVIDER */}
        <div
          style={{
            position: 'absolute',
            left: '50%',
            top: 480,
            transform: `translateX(-50%) scale(${vsScale})`,
            width: 140,
            height: 140,
            borderRadius: '50%',
            backgroundColor: '#000000',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            border: '6px solid #FEEC41',
            boxShadow: '0 12px 0 rgba(0,0,0,0.15)',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 54,
              fontWeight: 900,
              color: '#FEEC41',
              letterSpacing: -2,
            }}
          >
            VS
          </div>
        </div>

        {/* RIGHT COLUMN — Credit Card Interest */}
        <div
          style={{
            position: 'absolute',
            right: 220,
            top: 320,
            width: 380,
            opacity: rightIn,
            transform: `translateY(${(1 - rightIn) * 40}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.7,
              marginBottom: 16,
              letterSpacing: 1,
              textAlign: 'right',
            }}
          >
            CARD INTEREST
          </div>

          {/* Bar chart visual */}
          <div
            style={{
              width: 380,
              height: 460,
              border: '4px solid #000000',
              backgroundColor: '#000000',
              position: 'relative',
              display: 'flex',
              alignItems: 'flex-end',
              justifyContent: 'center',
              padding: 20,
            }}
          >
            <div
              style={{
                width: 140,
                height: rightBarHeight,
                backgroundColor: '#FEEC41',
                position: 'relative',
              }}
            >
              <div
                style={{
                  position: 'absolute',
                  top: -55,
                  left: '50%',
                  transform: 'translateX(-50%)',
                  fontFamily: 'Inter, Poppins',
                  fontSize: 44,
                  fontWeight: 900,
                  color: '#FEEC41',
                  opacity: minus20Op,
                  whiteSpace: 'nowrap',
                }}
              >
                -20%
              </div>
            </div>
          </div>

          <div
            style={{
              marginTop: 20,
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              fontWeight: 600,
              color: '#000000',
              opacity: 0.75,
              textAlign: 'center',
              letterSpacing: 1,
            }}
          >
            EVERY SINGLE YEAR
          </div>
        </div>

        {/* NET LOSS CALLOUT */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: '50%',
            transform: `translateX(-50%) scale(${lossIn * lossPulse})`,
            backgroundColor: '#000000',
            padding: '22px 60px',
            border: '5px solid #FEEC41',
            display: 'flex',
            alignItems: 'center',
            gap: 24,
          }}
        >
          {/* Down arrow SVG */}
          <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
            <path d="M12 2 L12 22 M5 15 L12 22 L19 15" stroke="#FEEC41" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 52,
              fontWeight: 900,
              color: '#FEEC41',
              letterSpacing: -1,
            }}
          >
            NET LOSS: <span style={{ color: '#FCD1D1' }}>-12% / YEAR</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene31_DebtVsBoost;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32_RescueMission',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32_RescueMission: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Stamp animation — strike-through "PENALTY"
  const penaltyIn = sp(10);
  const strikeWidth = interpolate(sp(40), [0, 1], [0, 520]);

  // Pivot to RESCUE MISSION
  const rescueIn = sp(70, { damping: 9, stiffness: 150 });
  const rescuePulse = 1 + Math.sin(frame * 0.15) * 0.02;

  // Three step cards
  const card1 = sp(110);
  const card2 = sp(140);
  const card3 = sp(170);

  // Camera
  const cameraScale = interpolate(frame, [0, durationInFrames], [1.02, 1.08], { easing: Easing.inOut(Easing.ease) });

  // Bleeding stop visual
  const stopOp = interpolate(frame, [220, 245], [0, 1], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(254,236,65,0.12) 0%, rgba(252,209,209,0) 70%)',
        }}
      />

      <AbsoluteFill style={{ transform: `scale(${cameraScale})` }}>
        {/* Shape layers */}
        <div
          style={{
            position: 'absolute',
            top: '15%',
            left: '5%',
            width: 400,
            height: 400,
            border: '2px solid #000000',
            opacity: 0.06,
            transform: `rotate(${frame * 0.05}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '10%',
            right: '8%',
            width: 320,
            height: 320,
            borderRadius: '50%',
            backgroundColor: '#FEEC41',
            opacity: 0.15,
            transform: `scale(${1 + Math.sin(frame * 0.04) * 0.08})`,
          }}
        />

        {/* Diagonal line texture */}
        <svg style={{ position: 'absolute', inset: 0, opacity: 0.05 }} width="100%" height="100%">
          <defs>
            <pattern id="diag32" width="40" height="40" patternUnits="userSpaceOnUse">
              <path d="M 0 40 L 40 0" stroke="#000000" strokeWidth="1" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#diag32)" />
        </svg>

        {/* Particles */}
        {Array.from({ length: 12 }).map((_, i) => {
          const x = seededRandom(i + 10) * 1920;
          const y = seededRandom(i + 80) * 1080;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x + Math.sin(frame * 0.03 + i) * 15,
                top: y + Math.cos(frame * 0.02 + i) * 10,
                width: 5,
                height: 5,
                backgroundColor: '#000000',
                opacity: 0.18,
              }}
            />
          );
        })}

        {/* TOP: Old framing "PENALTY" with strike-through */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: penaltyIn,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              position: 'relative',
              fontFamily: 'Inter, Poppins',
              fontSize: 92,
              fontWeight: 900,
              color: '#000000',
              opacity: 0.45,
              letterSpacing: -3,
            }}
          >
            NOT A PENALTY
            <div
              style={{
                position: 'absolute',
                top: '52%',
                left: '50%',
                transform: 'translate(-50%, -50%)',
                width: strikeWidth,
                height: 10,
                backgroundColor: '#000000',
              }}
            />
          </div>
        </div>

        {/* MAIN: RESCUE MISSION */}
        <div
          style={{
            position: 'absolute',
            top: 240,
            left: 0,
            right: 0,
            textAlign: 'center',
            transform: `scale(${rescueIn * rescuePulse})`,
            opacity: rescueIn,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.65,
              letterSpacing: 6,
              marginBottom: 14,
            }}
          >
            IT IS A
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 178,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -6,
              lineHeight: 0.95,
            }}
          >
            FINANCIAL
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 178,
              fontWeight: 900,
              color: '#FEEC41',
              backgroundColor: '#000000',
              display: 'inline-block',
              padding: '0 40px',
              letterSpacing: -6,
              lineHeight: 1,
              marginTop: 8,
            }}
          >
            RESCUE MISSION
          </div>
        </div>

        {/* Three step cards at bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 32,
          }}
        >
          {[
            { n: '01', label: 'TAKE THE MONEY', op: card1 },
            { n: '02', label: 'KILL THE DEBT', op: card2 },
            { n: '03', label: 'STOP THE BLEEDING', op: card3 },
          ].map((s, i) => (
            <div
              key={i}
              style={{
                width: 420,
                padding: '22px 30px',
                backgroundColor: i === 2 ? '#000000' : '#FEEC41',
                border: '4px solid #000000',
                opacity: s.op,
                transform: `translateY(${(1 - s.op) * 40}px)`,
                display: 'flex',
                alignItems: 'center',
                gap: 22,
              }}
            >
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 52,
                  fontWeight: 900,
                  color: i === 2 ? '#FEEC41' : '#000000',
                  opacity: 0.85,
                }}
              >
                {s.n}
              </div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 30,
                  fontWeight: 800,
                  color: i === 2 ? '#FEEC41' : '#000000',
                  letterSpacing: -0.5,
                  lineHeight: 1.05,
                }}
              >
                {s.label}
                {i === 2 && (
                  <span
                    style={{
                      display: 'inline-block',
                      marginLeft: 10,
                      opacity: stopOp,
                      color: '#FCD1D1',
                    }}
                  >
                    ✕
                  </span>
                )}
              </div>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene32_RescueMission;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33_SurvivalFirst',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene33_SurvivalFirst: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Phase 1: bankruptcy warning
  const bankruptcyIn = sp(10);
  const bankruptcyOut = interpolate(frame, [120, 145], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Phase 2: SURVIVAL FIRST
  const survivalIn = sp(140, { damping: 10, stiffness: 140 });
  const survivalOut = interpolate(frame, [225, 245], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Phase 3: Loophole #3 reveal
  const loopholeIn = sp(245, { damping: 9, stiffness: 130 });

  // Camera pan
  const cameraX = interpolate(frame, [0, durationInFrames], [0, -20], { easing: Easing.inOut(Easing.ease) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 50%, rgba(254,236,65,0.16) 0%, rgba(252,209,209,0) 60%)',
        }}
      />

      <AbsoluteFill style={{ transform: `translateX(${cameraX}px)` }}>
        {/* Shape layers */}
        <div
          style={{
            position: 'absolute',
            top: -100,
            right: -100,
            width: 500,
            height: 500,
            borderRadius: '50%',
            border: '4px solid #000000',
            opacity: 0.05,
            transform: `rotate(${frame * 0.08}deg) scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: -120,
            left: -80,
            width: 380,
            height: 380,
            backgroundColor: '#FEEC41',
            opacity: 0.12,
            transform: `rotate(${15 + frame * 0.04}deg)`,
          }}
        />

        {/* Dot grid texture */}
        <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="100%" height="100%">
          <defs>
            <pattern id="dot33" width="50" height="50" patternUnits="userSpaceOnUse">
              <circle cx="25" cy="25" r="1.5" fill="#000000" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#dot33)" />
        </svg>

        {/* Particles */}
        {Array.from({ length: 16 }).map((_, i) => {
          const x = seededRandom(i + 20) * 1920;
          const y = seededRandom(i + 90) * 1080;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x + Math.sin(frame * 0.025 + i) * 18,
                top: y + Math.cos(frame * 0.02 + i) * 12,
                width: 4,
                height: 4,
                borderRadius: '50%',
                backgroundColor: '#000000',
                opacity: 0.2,
              }}
            />
          );
        })}

        {/* PHASE 1: Bankruptcy warning */}
        <div
          style={{
            position: 'absolute',
            inset: 0,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            opacity: bankruptcyIn * bankruptcyOut,
            transform: `translateY(${(1 - bankruptcyIn) * 30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.65,
              letterSpacing: 5,
              marginBottom: 18,
            }}
          >
            DON'T TRADE
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 110,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -4,
              lineHeight: 1.0,
              textAlign: 'center',
            }}
          >
            A LARGER CHECK
            <br />
            IN <span style={{ color: '#FEEC41', backgroundColor: '#000000', padding: '0 24px' }}>5 YEARS</span>
          </div>
          <div
            style={{
              marginTop: 32,
              fontFamily: 'Inter, Poppins',
              fontSize: 52,
              fontWeight: 800,
              color: '#000000',
              opacity: 0.85,
            }}
          >
            for BANKRUPTCY today
          </div>
        </div>

        {/* PHASE 2: SURVIVAL FIRST */}
        <div
          style={{
            position: 'absolute',
            inset: 0,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            opacity: survivalIn * survivalOut,
            transform: `scale(${0.85 + survivalIn * 0.15})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 220,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -8,
              lineHeight: 0.9,
            }}
          >
            SURVIVAL
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 96,
              fontWeight: 900,
              color: '#FEEC41',
              backgroundColor: '#000000',
              padding: '14px 56px',
              letterSpacing: -3,
              marginTop: 18,
            }}
          >
            COMES FIRST
          </div>

          {/* Underline marks */}
          <div style={{ display: 'flex', gap: 14, marginTop: 26 }}>
            {[0, 1, 2].map((i) => (
              <div
                key={i}
                style={{
                  width: 60,
                  height: 8,
                  backgroundColor: '#000000',
                  opacity: 0.3 + i * 0.25,
                }}
              />
            ))}
          </div>
        </div>

        {/* PHASE 3: Loophole #3 teaser */}
        <div
          style={{
            position: 'absolute',
            inset: 0,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            opacity: loopholeIn,
            transform: `translateY(${(1 - loopholeIn) * 40}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 30,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.65,
              letterSpacing: 6,
            }}
          >
            AND NOW
          </div>
          <div
            style={{
              marginTop: 20,
              display: 'flex',
              alignItems: 'center',
              gap: 30,
              border: '6px solid #000000',
              padding: '28px 60px',
              backgroundColor: '#FEEC41',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 200,
                fontWeight: 900,
                color: '#000000',
                letterSpacing: -10,
                lineHeight: 0.85,
              }}
            >
              #3
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 72,
                fontWeight: 900,
                color: '#000000',
                letterSpacing: -2,
                lineHeight: 1,
              }}
            >
              LOOPHOLE
              <div
                style={{
                  fontSize: 32,
                  fontWeight: 700,
                  opacity: 0.7,
                  marginTop: 8,
                  letterSpacing: 2,
                }}
              >
                NOBODY TALKS ABOUT
              </div>
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene33_SurvivalFirst;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34_SpousalBridge',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene34_SpousalBridge: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleIn = sp(10);

  // Left pillar (age 62)
  const pillar1In = sp(50);
  // Right pillar (age 60)
  const pillar2In = sp(75);
  // Bridge span connecting them
  const bridgeWidth = interpolate(sp(110), [0, 1], [0, 540]);

  // Zero income stamp
  const zeroIn = sp(180, { damping: 8, stiffness: 160 });
  const zeroPulse = 1 + Math.sin(frame * 0.2) * 0.03;

  // Job loss markers
  const jobX1 = interpolate(frame, [200, 220], [0, 1], { extrapolateRight: 'clamp' });
  const jobX2 = interpolate(frame, [215, 235], [0, 1], { extrapolateRight: 'clamp' });

  // Camera
  const cameraScale = interpolate(frame, [0, durationInFrames], [1.0, 1.05], { easing: Easing.inOut(Easing.ease) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(254,236,65,0.10) 0%, rgba(252,209,209,0) 50%)',
        }}
      />

      <AbsoluteFill style={{ transform: `scale(${cameraScale})` }}>
        {/* Shape layers */}
        <div
          style={{
            position: 'absolute',
            top: '20%',
            left: '40%',
            width: 600,
            height: 600,
            borderRadius: '50%',
            border: '3px solid #000000',
            opacity: 0.05,
            transform: `rotate(${frame * 0.06}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '5%',
            right: '5%',
            width: 280,
            height: 280,
            border: '2px solid #FEEC41',
            opacity: 0.35,
            transform: `rotate(${-20 + Math.sin(frame * 0.03) * 6}deg)`,
          }}
        />

        {/* Cross-hatch texture */}
        <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
          <defs>
            <pattern id="cross34" width="55" height="55" patternUnits="userSpaceOnUse">
              <path d="M 0 0 L 55 55 M 55 0 L 0 55" stroke="#000000" strokeWidth="1" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#cross34)" />
        </svg>

        {/* Particles */}
        {Array.from({ length: 14 }).map((_, i) => {
          const x = seededRandom(i + 30) * 1920;
          const y = seededRandom(i + 100) * 1080;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x + Math.sin(frame * 0.022 + i) * 16,
                top: y,
                width: 5,
                height: 5,
                borderRadius: '50%',
                backgroundColor: '#000000',
                opacity: 0.17,
              }}
            />
          );
        })}

        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleIn,
            transform: `translateY(${(1 - titleIn) * -25}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.65,
              letterSpacing: 6,
            }}
          >
            STRATEGY #3
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 96,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -3,
              lineHeight: 1.0,
              marginTop: 8,
            }}
          >
            THE <span style={{ backgroundColor: '#000000', color: '#FEEC41', padding: '0 22px' }}>SPOUSAL BRIDGE</span>
          </div>
        </div>

        {/* BRIDGE DIAGRAM */}
        <div
          style={{
            position: 'absolute',
            top: 360,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'flex-end',
            height: 380,
          }}
        >
          {/* Pillar 1 - YOU (62) */}
          <div
            style={{
              width: 220,
              height: 320,
              backgroundColor: '#000000',
              border: '5px solid #000000',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'flex-end',
              padding: '20px 0',
              opacity: pillar1In,
              transform: `translateY(${(1 - pillar1In) * 60}px)`,
              position: 'relative',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 28,
                fontWeight: 700,
                color: '#FEEC41',
                opacity: 0.75,
                letterSpacing: 2,
              }}
            >
              YOU
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 138,
                fontWeight: 900,
                color: '#FEEC41',
                letterSpacing: -5,
                lineHeight: 1,
              }}
            >
              62
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 18,
                fontWeight: 600,
                color: '#FEEC41',
                opacity: 0.55,
                letterSpacing: 2,
                marginTop: 8,
              }}
            >
              AGE
            </div>
            {/* Job-loss X */}
            <div
              style={{
                position: 'absolute',
                top: -50,
                right: -30,
                width: 80,
                height: 80,
                borderRadius: '50%',
                backgroundColor: '#FEEC41',
                border: '5px solid #000000',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                opacity: jobX1,
                transform: `scale(${jobX1})`,
              }}
            >
              <div style={{ fontSize: 48, fontWeight: 900, color: '#000000', lineHeight: 1 }}>✕</div>
            </div>
          </div>

          {/* BRIDGE SPAN */}
          <div
            style={{
              width: bridgeWidth,
              height: 24,
              backgroundColor: '#FEEC41',
              border: '4px solid #000000',
              alignSelf: 'flex-start',
              marginTop: 80,
              position: 'relative',
              overflow: 'hidden',
            }}
          >
            {/* Bridge label */}
            <div
              style={{
                position: 'absolute',
                top: -52,
                left: '50%',
                transform: 'translateX(-50%)',
                fontFamily: 'JetBrains Mono',
                fontSize: 24,
                fontWeight: 700,
                color: '#000000',
                opacity: bridgeWidth > 200 ? 0.85 : 0,
                letterSpacing: 3,
                whiteSpace: 'nowrap',
              }}
            >
              BRIDGE
            </div>
            {/* Bridge support struts */}
            {Array.from({ length: 6 }).map((_, i) => (
              <div
                key={i}
                style={{
                  position: 'absolute',
                  top: 0,
                  left: `${(i + 1) * 14}%`,
                  width: 3,
                  height: '100%',
                  backgroundColor: '#000000',
                  opacity: bridgeWidth > 300 ? 0.5 : 0,
                }}
              />
            ))}
          </div>

          {/* Pillar 2 - SPOUSE (60) */}
          <div
            style={{
              width: 220,
              height: 320,
              backgroundColor: '#FEEC41',
              border: '5px solid #000000',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'flex-end',
              padding: '20px 0',
              opacity: pillar2In,
              transform: `translateY(${(1 - pillar2In) * 60}px)`,
              position: 'relative',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 28,
                fontWeight: 700,
                color: '#000000',
                opacity: 0.75,
                letterSpacing: 2,
              }}
            >
              SPOUSE
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 138,
                fontWeight: 900,
                color: '#000000',
                letterSpacing: -5,
                lineHeight: 1,
              }}
            >
              60
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 18,
                fontWeight: 600,
                color: '#000000',
                opacity: 0.55,
                letterSpacing: 2,
                marginTop: 8,
              }}
            >
              AGE
            </div>
            {/* Job-loss X */}
            <div
              style={{
                position: 'absolute',
                top: -50,
                left: -30,
                width: 80,
                height: 80,
                borderRadius: '50%',
                backgroundColor: '#000000',
                border: '5px solid #000000',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                opacity: jobX2,
                transform: `scale(${jobX2})`,
              }}
            >
              <div style={{ fontSize: 48, fontWeight: 900, color: '#FEEC41', lineHeight: 1 }}>✕</div>
            </div>
          </div>
        </div>

        {/* ZERO INCOME stamp */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: '50%',
            transform: `translateX(-50%) rotate(-3deg) scale(${zeroIn * zeroPulse})`,
            border: '6px solid #000000',
            backgroundColor: '#000000',
            padding: '18px 50px',
            display: 'flex',
            alignItems: 'baseline',
            gap: 18,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              fontWeight: 700,
              color: '#FEEC41',
              opacity: 0.75,
              letterSpacing: 3,
            }}
          >
            RESULT:
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 68,
              fontWeight: 900,
              color: '#FEEC41',
              letterSpacing: -2,
            }}
          >
            ZERO INCOME
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene34_SpousalBridge;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35_CashFlowDilemma',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35_CashFlowDilemma: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Headline NEED NOW
  const needIn = sp(10);

  // Two need cards
  const groceriesIn = sp(45);
  const mortgageIn = sp(70);

  // Pivot to "BUT"
  const butIn = sp(110, { damping: 9, stiffness: 150 });

  // Spouse benefit comparison
  const comparisonIn = sp(140);
  const yourBarH = interpolate(sp(160), [0, 1], [0, 90]);
  const spouseBarH = interpolate(sp(180), [0, 1], [0, 200]);
  const seventyIn = sp(220, { damping: 8, stiffness: 170 });

  // Camera
  const cameraY = interpolate(frame, [0, durationInFrames], [0, -15], { easing: Easing.inOut(Easing.ease) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 70% 30%, rgba(254,236,65,0.14) 0%, rgba(252,209,209,0) 60%)',
        }}
      />

      <AbsoluteFill style={{ transform: `translateY(${cameraY}px)` }}>
        {/* Shape layers */}
        <div
          style={{
            position: 'absolute',
            top: '10%',
            left: '8%',
            width: 280,
            height: 280,
            border: '3px solid #000000',
            opacity: 0.06,
            transform: `rotate(${frame * 0.07}deg)`,
          }}
        />
        <svg
          style={{
            position: 'absolute',
            bottom: '8%',
            right: '6%',
            opacity: 0.05,
            transform: `rotate(${frame * 0.04}deg)`,
          }}
          width="400"
          height="400"
          viewBox="0 0 100 100"
        >
          <polygon points="50,5 95,95 5,95" fill="none" stroke="#000000" strokeWidth="1.5" />
        </svg>

        {/* Line texture */}
        <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
          <defs>
            <pattern id="line35" width="6" height="6" patternUnits="userSpaceOnUse">
              <path d="M 0 6 L 6 0" stroke="#000000" strokeWidth="0.5" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#line35)" />
        </svg>

        {/* Particles */}
        {Array.from({ length: 13 }).map((_, i) => {
          const x = seededRandom(i + 40) * 1920;
          const y = seededRandom(i + 110) * 1080;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: y + Math.sin(frame * 0.025 + i) * 14,
                width: 5,
                height: 5,
                backgroundColor: '#000000',
                opacity: 0.16,
                transform: 'rotate(45deg)',
              }}
            />
          );
        })}

        {/* TOP LEFT: NEED CASH NOW */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            left: 80,
            opacity: needIn,
            transform: `translateX(${(1 - needIn) * -30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 24,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.6,
              letterSpacing: 5,
            }}
          >
            THE PROBLEM
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 82,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -3,
              lineHeight: 1,
              marginTop: 6,
            }}
          >
            CASH FLOW
            <br />
            <span style={{ color: '#FEEC41', backgroundColor: '#000000', padding: '0 16px' }}>RIGHT NOW</span>
          </div>
        </div>

        {/* Need cards stacked left */}
        <div
          style={{
            position: 'absolute',
            top: 360,
            left: 80,
            display: 'flex',
            flexDirection: 'column',
            gap: 20,
          }}
        >
          {[
            { label: 'GROCERIES', sub: 'Weekly', op: groceriesIn },
            { label: 'MORTGAGE', sub: 'Monthly', op: mortgageIn },
          ].map((item, i) => (
            <div
              key={i}
              style={{
                width: 460,
                padding: '20px 28px',
                backgroundColor: '#FEEC41',
                border: '4px solid #000000',
                opacity: item.op,
                transform: `translateX(${(1 - item.op) * -40}px)`,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'space-between',
              }}
            >
              <div>
                <div
                  style={{
                    fontFamily: 'JetBrains Mono',
                    fontSize: 18,
                    fontWeight: 600,
                    color: '#000000',
                    opacity: 0.55,
                    letterSpacing: 2,
                  }}
                >
                  {item.sub.toUpperCase()}
                </div>
                <div
                  style={{
                    fontFamily: 'Inter, Poppins',
                    fontSize: 48,
                    fontWeight: 900,
                    color: '#000000',
                    letterSpacing: -1,
                    lineHeight: 1.05,
                  }}
                >
                  {item.label}
                </div>
              </div>
              {/* Down arrow indicating outflow */}
              <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
                <path d="M12 2 L12 22 M5 15 L12 22 L19 15" stroke="#000000" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
            </div>
          ))}
        </div>

        {/* CENTER divider — BUT */}
        <div
          style={{
            position: 'absolute',
            left: 620,
            top: 480,
            transform: `scale(${butIn})`,
            opacity: butIn,
          }}
        >
          <div
            style={{
              width: 130,
              height: 130,
              borderRadius: '50%',
              backgroundColor: '#000000',
              border: '6px solid #FEEC41',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Inter, Poppins',
              fontSize: 44,
              fontWeight: 900,
              color: '#FEEC41',
              letterSpacing: -1,
            }}
          >
            BUT
          </div>
        </div>

        {/* RIGHT: Spouse benefit comparison */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            right: 80,
            width: 620,
            opacity: comparisonIn,
            transform: `translateX(${(1 - comparisonIn) * 40}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 24,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.6,
              letterSpacing: 5,
            }}
          >
            BENEFIT SIZE
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 60,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -2,
              lineHeight: 1,
              marginTop: 6,
            }}
          >
            SPOUSE'S CHECK = <span style={{ color: '#000000', textDecoration: 'underline', textDecorationColor: '#FEEC41', textDecorationThickness: 12 }}>BIGGER</span>
          </div>

          {/* Bars side-by-side */}
          <div
            style={{
              marginTop: 36,
              display: 'flex',
              alignItems: 'flex-end',
              gap: 30,
              height: 240,
              padding: '0 30px',
              borderBottom: '4px solid #000000',
            }}
          >
            <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 10 }}>
              <div
                style={{
                  width: 130,
                  height: yourBarH,
                  backgroundColor: '#000000',
                  opacity: 0.7,
                }}
              />
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 20,
                  fontWeight: 700,
                  color: '#000000',
                  letterSpacing: 2,
                }}
              >
                YOU
              </div>
            </div>
            <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 10 }}>
              <div
                style={{
                  width: 130,
                  height: spouseBarH,
                  backgroundColor: '#FEEC41',
                  border: '4px solid #000000',
                  position: 'relative',
                }}
              >
                <div
                  style={{
                    position: 'absolute',
                    top: -50,
                    left: '50%',
                    transform: `translateX(-50%) scale(${seventyIn})`,
                    backgroundColor: '#000000',
                    color: '#FEEC41',
                    padding: '6px 18px',
                    fontFamily: 'Inter, Poppins',
                    fontSize: 36,
                    fontWeight: 900,
                    letterSpacing: -1,
                    whiteSpace: 'nowrap',
                  }}
                >
                  WAIT TO 70
                </div>
              </div>
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 20,
                  fontWeight: 700,
                  color: '#000000',
                  letterSpacing: 2,
                }}
              >
                SPOUSE
              </div>
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene35_CashFlowDilemma;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36_ThePlay',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene36_ThePlay: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big "THE PLAY" header
  const playIn = sp(8);

  // Playbook style numbered steps
  const step1In = sp(50);
  const step2In = sp(85);
  const step3In = sp(120);

  // Penalty stamp 30%
  const penaltyIn = sp(155, { damping: 8, stiffness: 170 });
  const penaltyRotate = interpolate(sp(155), [0, 1], [-15, -8]);

  // Cash flow arrow
  const arrowProgress = interpolate(frame, [200, 250], [0, 1], { extrapolateRight: 'clamp' });

  // Camera
  const cameraScale = interpolate(frame, [0, durationInFrames], [1.0, 1.04], { easing: Easing.inOut(Easing.ease) });
  const shake = frame > 155 && frame < 175 ? Math.sin(frame * 1.4) * 3 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(254,236,65,0.15) 0%, rgba(252,209,209,0) 65%)',
        }}
      />

      <AbsoluteFill style={{ transform: `scale(${cameraScale}) translateX(${shake}px)` }}>
        {/* Shape layers */}
        <div
          style={{
            position: 'absolute',
            top: -150,
            right: -150,
            width: 600,
            height: 600,
            borderRadius: '50%',
            backgroundColor: '#FEEC41',
            opacity: 0.13,
            transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: -100,
            left: -100,
            width: 380,
            height: 380,
            border: '3px solid #000000',
            opacity: 0.06,
            transform: `rotate(${frame * 0.06}deg)`,
          }}
        />

        {/* Playbook grid texture */}
        <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="100%" height="100%">
          <defs>
            <pattern id="grid36" width="80" height="80" patternUnits="userSpaceOnUse">
              <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#000000" strokeWidth="1" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#grid36)" />
        </svg>

        {/* Particles */}
        {Array.from({ length: 12 }).map((_, i) => {
          const x = seededRandom(i + 50) * 1920;
          const y = seededRandom(i + 120) * 1080;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x + Math.sin(frame * 0.022 + i) * 14,
                top: y,
                width: 5,
                height: 5,
                borderRadius: '50%',
                backgroundColor: '#000000',
                opacity: 0.16,
              }}
            />
          );
        })}

        {/* HEADER */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 80,
            opacity: playIn,
            transform: `translateY(${(1 - playIn) * -25}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.6,
              letterSpacing: 5,
            }}
          >
            EXECUTE
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 138,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -5,
              lineHeight: 0.95,
              marginTop: 4,
            }}
          >
            THE PLAY
          </div>
        </div>

        {/* PLAYBOOK STEPS — left column */}
        <div
          style={{
            position: 'absolute',
            top: 320,
            left: 80,
            width: 920,
            display: 'flex',
            flexDirection: 'column',
            gap: 22,
          }}
        >
          {[
            { n: '01', label: 'YOU = LOWER EARNER', op: step1In, dark: false },
            { n: '02', label: 'CLAIM AT AGE 62', op: step2In, dark: true },
            { n: '03', label: 'FUND THE HOUSEHOLD', op: step3In, dark: false },
          ].map((s, i) => (
            <div
              key={i}
              style={{
                display: 'flex',
                alignItems: 'center',
                opacity: s.op,
                transform: `translateX(${(1 - s.op) * -50}px)`,
              }}
            >
              <div
                style={{
                  width: 110,
                  height: 110,
                  backgroundColor: s.dark ? '#000000' : '#FEEC41',
                  border: '5px solid #000000',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontFamily: 'JetBrains Mono',
                  fontSize: 50,
                  fontWeight: 900,
                  color: s.dark ? '#FEEC41' : '#000000',
                  marginRight: 26,
                }}
              >
                {s.n}
              </div>
              <div
                style={{
                  flex: 1,
                  height: 110,
                  backgroundColor: s.dark ? '#FEEC41' : '#FCD1D1',
                  border: '5px solid #000000',
                  display: 'flex',
                  alignItems: 'center',
                  padding: '0 30px',
                  fontFamily: 'Inter, Poppins',
                  fontSize: 50,
                  fontWeight: 900,
                  color: '#000000',
                  letterSpacing: -1,
                }}
              >
                {s.label}
              </div>
            </div>
          ))}
        </div>

        {/* 30% PENALTY STAMP — right side */}
        <div
          style={{
            position: 'absolute',
            top: 350,
            right: 90,
            transform: `rotate(${penaltyRotate}deg) scale(${penaltyIn})`,
            opacity: penaltyIn,
          }}
        >
          <div
            style={{
              width: 320,
              height: 320,
              borderRadius: '50%',
              backgroundColor: '#000000',
              border: '8px solid #FEEC41',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              boxShadow: '0 20px 0 rgba(0,0,0,0.15)',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 22,
                fontWeight: 700,
                color: '#FEEC41',
                opacity: 0.7,
                letterSpacing: 5,
              }}
            >
              PRICE PAID
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 138,
                fontWeight: 900,
                color: '#FEEC41',
                letterSpacing: -5,
                lineHeight: 1,
                marginTop: 4,
              }}
            >
              30%
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 30,
                fontWeight: 800,
                color: '#FEEC41',
                opacity: 0.85,
                letterSpacing: 1,
              }}
            >
              PENALTY
            </div>
          </div>
        </div>

        {/* Bottom arrow flow — CHECK → FAMILY */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 18,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 42,
              fontWeight: 900,
              color: '#000000',
              padding: '12px 28px',
              backgroundColor: '#FEEC41',
              border: '4px solid #000000',
            }}
          >
            YOUR CHECK
          </div>
          <div style={{ width: 200, height: 6, backgroundColor: '#000000', position: 'relative' }}>
            <div
              style={{
                position: 'absolute',
                left: 0,
                top: -8,
                height: 22,
                width: arrowProgress * 200,
                backgroundColor: '#000000',
              }}
            />
            <svg
              style={{ position: 'absolute', right: -22, top: -14, opacity: arrowProgress }}
              width="34"
              height="34"
              viewBox="0 0 24 24"
              fill="none"
            >
              <path d="M5 12 L19 12 M12 5 L19 12 L12 19" stroke="#000000" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 42,
              fontWeight: 900,
              color: '#FEEC41',
              padding: '12px 28px',
              backgroundColor: '#000000',
              border: '4px solid #000000',
            }}
          >
            FAMILY SURVIVES
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene36_ThePlay;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37_LetItGrow',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene37_LetItGrow: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleIn = sp(8);

  // Growth chart line drawing
  const lineProgress = interpolate(sp(40), [0, 1], [0, 1]);

  // Age markers along the line
  const ages = [
    { age: 62, x: 100, y: 380, delay: 60 },
    { age: 65, x: 360, y: 320, delay: 85 },
    { age: 67, x: 600, y: 250, delay: 110 },
    { age: 70, x: 880, y: 90, delay: 140 },
  ];

  // MAX BENEFIT callout
  const maxIn = sp(180, { damping: 8, stiffness: 160 });
  const maxPulse = 1 + Math.sin(frame * 0.18) * 0.025;

  // Side panel: retirement account
  const accountIn = sp(70);

  // Camera
  const cameraScale = interpolate(frame, [0, durationInFrames], [1.0, 1.05], { easing: Easing.inOut(Easing.ease) });

  // Generate growth curve path
  const buildPath = (progress: number) => {
    const points = [
      { x: 100, y: 380 },
      { x: 250, y: 360 },
      { x: 360, y: 320 },
      { x: 480, y: 290 },
      { x: 600, y: 250 },
      { x: 740, y: 180 },
      { x: 880, y: 90 },
    ];
    const totalLen = points.length - 1;
    const cutoff = progress * totalLen;
    let d = `M ${points[0].x} ${points[0].y}`;
    for (let i = 1; i < points.length; i++) {
      if (i <= cutoff) {
        d += ` L ${points[i].x} ${points[i].y}`;
      } else {
        const frac = cutoff - (i - 1);
        if (frac > 0) {
          const px = points[i - 1].x + (points[i].x - points[i - 1].x) * frac;
          const py = points[i - 1].y + (points[i].y - points[i - 1].y) * frac;
          d += ` L ${px} ${py}`;
        }
        break;
      }
    }
    return d;
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 20% 80%, rgba(254,236,65,0.15) 0%, rgba(252,209,209,0) 65%)',
        }}
      />

      <AbsoluteFill style={{ transform: `scale(${cameraScale})` }}>
        {/* Shape layers */}
        <div
          style={{
            position: 'absolute',
            top: '5%',
            right: '5%',
            width: 360,
            height: 360,
            border: '3px solid #000000',
            opacity: 0.06,
            transform: `rotate(${frame * 0.06}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '-10%',
            left: '40%',
            width: 500,
            height: 500,
            borderRadius: '50%',
            backgroundColor: '#FEEC41',
            opacity: 0.1,
            transform: `scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
          }}
        />

        {/* Subtle dot texture */}
        <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="100%" height="100%">
          <defs>
            <pattern id="dot37" width="44" height="44" patternUnits="userSpaceOnUse">
              <circle cx="22" cy="22" r="1.5" fill="#000000" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#dot37)" />
        </svg>

        {/* Particles */}
        {Array.from({ length: 14 }).map((_, i) => {
          const x = seededRandom(i + 60) * 1920;
          const y = seededRandom(i + 130) * 1080;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: y + Math.sin(frame * 0.025 + i) * 15,
                width: 6,
                height: 6,
                borderRadius: '50%',
                backgroundColor: '#000000',
                opacity: 0.15,
              }}
            />
          );
        })}

        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 80,
            opacity: titleIn,
            transform: `translateY(${(1 - titleIn) * -20}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 24,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.6,
              letterSpacing: 5,
            }}
          >
            HIGHER EARNER MOVE
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 112,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -4,
              lineHeight: 1,
              marginTop: 4,
            }}
          >
            LET IT <span style={{ color: '#FEEC41', backgroundColor: '#000000', padding: '0 22px' }}>GROW</span>
          </div>
        </div>

        {/* LEFT: Retirement account panel */}
        <div
          style={{
            position: 'absolute',
            top: 270,
            left: 80,
            width: 280,
            opacity: accountIn,
            transform: `translateX(${(1 - accountIn) * -30}px)`,
          }}
        >
          <div
            style={{
              padding: '24px 22px',
              backgroundColor: '#FEEC41',
              border: '5px solid #000000',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 18,
                fontWeight: 700,
                color: '#000000',
                opacity: 0.6,
                letterSpacing: 3,
              }}
            >
              ACCOUNT
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 56,
                fontWeight: 900,
                color: '#000000',
                letterSpacing: -2,
                lineHeight: 1.05,
                marginTop: 4,
              }}
            >
              RETIRE
              <br />
              -MENT
            </div>
            {/* Stack-of-bills icon */}
            <div style={{ display: 'flex', gap: 4, marginTop: 18 }}>
              {[0, 1, 2, 3, 4].map((i) => (
                <div
                  key={i}
                  style={{
                    flex: 1,
                    height: 36,
                    backgroundColor: '#000000',
                    opacity: 0.6 + i * 0.1,
                    transform: `translateY(${Math.sin(frame * 0.05 + i) * 2}px)`,
                  }}
                />
              ))}
            </div>
          </div>
        </div>

        {/* CENTER/RIGHT: Growth chart */}
        <div
          style={{
            position: 'absolute',
            top: 260,
            left: 410,
            width: 1000,
            height: 480,
          }}
        >
          {/* Axis */}
          <svg width="1000" height="480" viewBox="0 0 1000 480">
            {/* X axis */}
            <line x1="60" y1="430" x2="960" y2="430" stroke="#000000" strokeWidth="3" />
            {/* Y axis */}
            <line x1="60" y1="40" x2="60" y2="430" stroke="#000000" strokeWidth="3" />

            {/* Reference dashed grid lines */}
            {[100, 200, 300].map((y) => (
              <line key={y} x1="60" y1={y} x2="960" y2={y} stroke="#000000" strokeDasharray="6 6" strokeWidth="1" opacity="0.25" />
            ))}

            {/* Growth curve */}
            <path
              d={buildPath(lineProgress)}
              fill="none"
              stroke="#000000"
              strokeWidth="8"
              strokeLinecap="round"
              strokeLinejoin="round"
            />

            {/* Age dots */}
            {ages.map((a, i) => {
              const op = sp(a.delay);
              return (
                <g key={i}>
                  <circle cx={a.x} cy={a.y} r={op * 16} fill="#FEEC41" stroke="#000000" strokeWidth="4" opacity={op} />
                  <text
                    x={a.x}
                    y={a.y - 28}
                    textAnchor="middle"
                    style={{
                      fontFamily: 'JetBrains Mono',
                      fontSize: 26,
                      fontWeight: 900,
                      fill: '#000000',
                      opacity: op,
                    }}
                  >
                    {a.age}
                  </text>
                </g>
              );
            })}

            {/* Axis labels */}
            <text
              x="500"
              y="465"
              textAnchor="middle"
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 20,
                fontWeight: 700,
                fill: '#000000',
                opacity: 0.6,
                letterSpacing: 3,
              }}
            >
              AGE — DELAY TO 70
            </text>
            <text
              x="40"
              y="60"
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 18,
                fontWeight: 700,
                fill: '#000000',
                opacity: 0.6,
                letterSpacing: 2,
              }}
              transform="rotate(-90, 40, 60)"
            >
              BENEFIT $
            </text>
          </svg>

          {/* MAX BENEFIT callout at peak */}
          <div
            style={{
              position: 'absolute',
              top: 30,
              right: 0,
              transform: `scale(${maxIn * maxPulse})`,
              opacity: maxIn,
              backgroundColor: '#000000',
              border: '5px solid #FEEC41',
              padding: '14px 28px',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 18,
                fontWeight: 700,
                color: '#FEEC41',
                opacity: 0.7,
                letterSpacing: 3,
              }}
            >
              RESULT
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 54,
                fontWeight: 900,
                color: '#FEEC41',
                letterSpacing: -2,
                lineHeight: 1,
                marginTop: 4,
              }}
            >
              MAX
              <br />
              BENEFIT
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene37_LetItGrow;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38_SurvivorSwitch',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene38_SurvivorSwitch: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleIn = sp(8);

  // Small check (your reduced benefit)
  const smallIn = sp(50);
  // Large check (spouse's maximized)
  const largeIn = sp(85);

  // Switch arrow appears
  const switchIn = sp(140, { damping: 8, stiffness: 160 });
  const arrowSwap = interpolate(frame, [160, 200], [0, 1], { extrapolateRight: 'clamp' });

  // After switch — small check fades, big check moves to survivor
  const fadeSmall = interpolate(frame, [200, 230], [1, 0.25], { extrapolateRight: 'clamp' });
  const survivorLabelIn = sp(210, { damping: 10, stiffness: 140 });

  // Camera
  const cameraScale = interpolate(frame, [0, durationInFrames], [1.0, 1.05], { easing: Easing.inOut(Easing.ease) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(254,236,65,0.12) 0%, rgba(252,209,209,0) 60%)',
        }}
      />

      <AbsoluteFill style={{ transform: `scale(${cameraScale})` }}>
        {/* Shape layers */}
        <div
          style={{
            position: 'absolute',
            top: -120,
            left: -120,
            width: 480,
            height: 480,
            borderRadius: '50%',
            border: '3px solid #000000',
            opacity: 0.06,
            transform: `rotate(${frame * 0.05}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: -100,
            right: -100,
            width: 420,
            height: 420,
            backgroundColor: '#FEEC41',
            opacity: 0.14,
            transform: `rotate(${20 + frame * 0.03}deg)`,
          }}
        />

        {/* Diagonal texture */}
        <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
          <defs>
            <pattern id="diag38" width="36" height="36" patternUnits="userSpaceOnUse">
              <path d="M 0 0 L 36 36" stroke="#000000" strokeWidth="0.6" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#diag38)" />
        </svg>

        {/* Particles */}
        {Array.from({ length: 13 }).map((_, i) => {
          const x = seededRandom(i + 70) * 1920;
          const y = seededRandom(i + 140) * 1080;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x + Math.sin(frame * 0.022 + i) * 14,
                top: y,
                width: 5,
                height: 5,
                backgroundColor: '#000000',
                opacity: 0.15,
              }}
            />
          );
        })}

        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleIn,
            transform: `translateY(${(1 - titleIn) * -20}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.6,
              letterSpacing: 5,
            }}
          >
            THE FINAL HANDOFF
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 102,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -3,
              lineHeight: 1,
              marginTop: 6,
            }}
          >
            SWITCH TO <span style={{ color: '#FEEC41', backgroundColor: '#000000', padding: '0 22px' }}>SURVIVOR</span>
          </div>
        </div>

        {/* TWO CHECKS — side by side */}
        <div
          style={{
            position: 'absolute',
            top: 320,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 60,
          }}
        >
          {/* Small check */}
          <div
            style={{
              opacity: smallIn * fadeSmall,
              transform: `translateY(${(1 - smallIn) * 40}px)`,
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 22,
                fontWeight: 700,
                color: '#000000',
                opacity: 0.65,
                letterSpacing: 3,
                textAlign: 'center',
                marginBottom: 14,
              }}
            >
              YOUR CHECK
            </div>
            <div
              style={{
                width: 340,
                height: 200,
                backgroundColor: '#FEEC41',
                border: '5px solid #000000',
                padding: '20px 24px',
                display: 'flex',
                flexDirection: 'column',
                justifyContent: 'space-between',
                position: 'relative',
              }}
            >
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 16,
                  fontWeight: 700,
                  color: '#000000',
                  opacity: 0.6,
                  letterSpacing: 2,
                }}
              >
                MONTHLY BENEFIT
              </div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 64,
                  fontWeight: 900,
                  color: '#000000',
                  letterSpacing: -2,
                  lineHeight: 1,
                }}
              >
                $1,400
              </div>
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 16,
                  fontWeight: 700,
                  color: '#000000',
                  opacity: 0.55,
                  letterSpacing: 2,
                }}
              >
                CLAIMED AT 62
              </div>
            </div>
          </div>

          {/* SWITCH arrow */}
          <div
            style={{
              transform: `scale(${switchIn})`,
              opacity: switchIn,
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              gap: 8,
            }}
          >
            <svg width="120" height="80" viewBox="0 0 120 80" fill="none">
              {/* Right arrow */}
              <path
                d={`M 10 25 L ${10 + arrowSwap * 95} 25`}
                stroke="#000000"
                strokeWidth="6"
                strokeLinecap="round"
              />
              <path
                d="M 90 12 L 110 25 L 90 38"
                stroke="#000000"
                strokeWidth="6"
                strokeLinecap="round"
                strokeLinejoin="round"
                fill="none"
                opacity={arrowSwap}
              />
              {/* Left arrow (smaller, dimmer) */}
              <path
                d={`M 110 55 L ${110 - arrowSwap * 95} 55`}
                stroke="#000000"
                strokeWidth="4"
                strokeLinecap="round"
                opacity="0.5"
              />
              <path
                d="M 30 45 L 15 55 L 30 65"
                stroke="#000000"
                strokeWidth="4"
                strokeLinecap="round"
                strokeLinejoin="round"
                fill="none"
                opacity={arrowSwap * 0.5}
              />
            </svg>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 24,
                fontWeight: 900,
                color: '#000000',
                letterSpacing: 4,
              }}
            >
              SWITCH
            </div>
          </div>

          {/* Large check */}
          <div
            style={{
              opacity: largeIn,
              transform: `translateY(${(1 - largeIn) * 40}px) scale(${1 + (1 - fadeSmall) * 0.05})`,
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 22,
                fontWeight: 700,
                color: '#000000',
                opacity: 0.65,
                letterSpacing: 3,
                textAlign: 'center',
                marginBottom: 14,
              }}
            >
              SPOUSE'S CHECK
            </div>
            <div
              style={{
                width: 420,
                height: 280,
                backgroundColor: '#000000',
                border: '5px solid #000000',
                padding: '24px 28px',
                display: 'flex',
                flexDirection: 'column',
                justifyContent: 'space-between',
                position: 'relative',
                boxShadow: '0 12px 0 rgba(0,0,0,0.2)',
              }}
            >
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 16,
                  fontWeight: 700,
                  color: '#FEEC41',
                  opacity: 0.7,
                  letterSpacing: 2,
                }}
              >
                MAX MONTHLY BENEFIT
              </div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 92,
                  fontWeight: 900,
                  color: '#FEEC41',
                  letterSpacing: -3,
                  lineHeight: 1,
                }}
              >
                $3,480
              </div>
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 16,
                  fontWeight: 700,
                  color: '#FEEC41',
                  opacity: 0.7,
                  letterSpacing: 2,
                }}
              >
                DELAYED TO AGE 70
              </div>

              {/* SURVIVOR badge appears */}
              <div
                style={{
                  position: 'absolute',
                  top: -28,
                  right: -28,
                  backgroundColor: '#FEEC41',
                  border: '4px solid #000000',
                  padding: '8px 18px',
                  transform: `rotate(8deg) scale(${survivorLabelIn})`,
                  opacity: survivorLabelIn,
                  fontFamily: 'Inter, Poppins',
                  fontSize: 28,
                  fontWeight: 900,
                  color: '#000000',
                  letterSpacing: -1,
                }}
              >
                SURVIVOR
              </div>
            </div>
          </div>
        </div>

        {/* Bottom caption */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: survivorLabelIn,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              backgroundColor: '#FEEC41',
              border: '4px solid #000000',
              padding: '14px 36px',
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -1,
            }}
          >
            THE BRIDGE PROTECTS YOU FOREVER
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene38_SurvivorSwitch;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39_BrilliantStrategy',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene39_BrilliantStrategy: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Phase 1: BRILLIANT badge
  const brilliantIn = sp(10, { damping: 9, stiffness: 150 });
  const brilliantOut = interpolate(frame, [150, 175], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Phase 2: warning pivot - "BUT IF YOU DON'T..."
  const warningIn = sp(170, { damping: 10, stiffness: 140 });

  // Three category badges revealing in warning phase
  const cat1 = sp(210);
  const cat2 = sp(235);
  const cat3 = sp(260);

  // Camera tilt
  const cameraRot = interpolate(frame, [0, durationInFrames], [0, 0.5], { easing: Easing.inOut(Easing.ease) });

  const sparkle = 1 + Math.sin(frame * 0.15) * 0.025;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(254,236,65,0.18) 0%, rgba(252,209,209,0) 65%)',
        }}
      />

      <AbsoluteFill style={{ transform: `rotate(${cameraRot}deg)` }}>
        {/* Shape layers */}
        <div
          style={{
            position: 'absolute',
            top: '15%',
            right: '10%',
            width: 340,
            height: 340,
            borderRadius: '50%',
            border: '3px solid #000000',
            opacity: 0.06,
            transform: `rotate(${frame * 0.07}deg) scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '12%',
            left: '8%',
            width: 280,
            height: 280,
            backgroundColor: '#FEEC41',
            opacity: 0.18,
            transform: `rotate(${-12 + Math.sin(frame * 0.04) * 5}deg)`,
          }}
        />

        {/* Cross-hatch */}
        <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
          <defs>
            <pattern id="cross39" width="50" height="50" patternUnits="userSpaceOnUse">
              <path d="M 0 0 L 50 50 M 50 0 L 0 50" stroke="#000000" strokeWidth="0.7" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#cross39)" />
        </svg>

        {/* Particles */}
        {Array.from({ length: 15 }).map((_, i) => {
          const x = seededRandom(i + 80) * 1920;
          const y = seededRandom(i + 150) * 1080;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: y + Math.sin(frame * 0.025 + i) * 16,
                width: 6,
                height: 6,
                borderRadius: '50%',
                backgroundColor: '#000000',
                opacity: 0.15,
              }}
            />
          );
        })}

        {/* PHASE 1: BRILLIANT */}
        <div
          style={{
            position: 'absolute',
            inset: 0,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            opacity: brilliantIn * brilliantOut,
            transform: `scale(${0.85 + brilliantIn * 0.15})`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 30,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.65,
              letterSpacing: 8,
            }}
          >
            VERDICT
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 240,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -10,
              lineHeight: 0.9,
              marginTop: 12,
              transform: `scale(${sparkle})`,
            }}
          >
            BRILLIANT
          </div>
          <div
            style={{
              marginTop: 22,
              fontFamily: 'Inter, Poppins',
              fontSize: 56,
              fontWeight: 800,
              color: '#000000',
              backgroundColor: '#FEEC41',
              padding: '12px 38px',
              border: '5px solid #000000',
              letterSpacing: -1,
            }}
          >
            STRATEGIC USE OF THE PENALTY
          </div>

          {/* Decorative star burst lines */}
          <svg
            style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              transform: 'translate(-50%, -50%)',
              opacity: 0.5,
            }}
            width="900"
            height="900"
            viewBox="0 0 900 900"
          >
            {Array.from({ length: 12 }).map((_, i) => {
              const angle = (i / 12) * Math.PI * 2;
              const x1 = 450 + Math.cos(angle) * 360;
              const y1 = 450 + Math.sin(angle) * 360;
              const x2 = 450 + Math.cos(angle) * 420;
              const y2 = 450 + Math.sin(angle) * 420;
              return (
                <line
                  key={i}
                  x1={x1}
                  y1={y1}
                  x2={x2}
                  y2={y2}
                  stroke="#000000"
                  strokeWidth="4"
                  opacity={brilliantIn * 0.6}
                />
              );
            })}
          </svg>
        </div>

        {/* PHASE 2: WARNING */}
        <div
          style={{
            position: 'absolute',
            inset: 0,
            display: 'flex',
            flexDirection: 'column',
            alignItems: 'center',
            justifyContent: 'center',
            opacity: warningIn,
            transform: `translateY(${(1 - warningIn) * 30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.65,
              letterSpacing: 5,
            }}
          >
            BUT WARNING
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 92,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -3,
              lineHeight: 1,
              marginTop: 8,
              textAlign: 'center',
            }}
          >
            IF YOU AREN'T IN ONE OF
            <br />
            <span style={{ color: '#FEEC41', backgroundColor: '#000000', padding: '0 22px' }}>THESE 3 GROUPS</span>
          </div>

          {/* Three category chips */}
          <div style={{ display: 'flex', gap: 26, marginTop: 50 }}>
            {[
              { label: 'HEALTH CRISIS', op: cat1 },
              { label: 'HIGH-INTEREST DEBT', op: cat2 },
              { label: 'SPOUSAL BRIDGE', op: cat3 },
            ].map((c, i) => (
              <div
                key={i}
                style={{
                  padding: '20px 28px',
                  backgroundColor: '#FEEC41',
                  border: '4px solid #000000',
                  opacity: c.op,
                  transform: `translateY(${(1 - c.op) * 30}px) scale(${c.op})`,
                  fontFamily: 'Inter, Poppins',
                  fontSize: 32,
                  fontWeight: 900,
                  color: '#000000',
                  letterSpacing: -0.5,
                }}
              >
                {c.label}
              </div>
            ))}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.22) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene39_BrilliantStrategy;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40_LongevityGamble',
  durationInSeconds: 11.314,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene40_LongevityGamble: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title — LONGEVITY GAMBLE
  const titleIn = sp(8);

  // Three checkmark conditions
  const c1 = sp(45);
  const c2 = sp(70);
  const c3 = sp(95);

  // Dice (gamble visual)
  const diceIn = sp(140, { damping: 8, stiffness: 160 });
  const diceRoll = Math.sin(frame * 0.12) * 4;
  const diceLift = Math.sin(frame * 0.08) * 6;

  // MASSIVE GAMBLE callout
  const gambleIn = sp(195, { damping: 9, stiffness: 150 });
  const gamblePulse = 1 + Math.sin(frame * 0.18) * 0.025;

  // Camera
  const cameraScale = interpolate(frame, [0, durationInFrames], [1.0, 1.06], { easing: Easing.inOut(Easing.ease) });
  const shake = frame > 195 && frame < 220 ? Math.sin(frame * 1.6) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FCD1D1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 60% 70%, rgba(254,236,65,0.18) 0%, rgba(252,209,209,0) 65%)',
        }}
      />

      <AbsoluteFill style={{ transform: `scale(${cameraScale}) translateX(${shake}px)` }}>
        {/* Shape layers */}
        <div
          style={{
            position: 'absolute',
            top: -100,
            left: -100,
            width: 460,
            height: 460,
            borderRadius: '50%',
            border: '4px solid #000000',
            opacity: 0.06,
            transform: `rotate(${frame * 0.06}deg)`,
          }}
        />
        <svg
          style={{
            position: 'absolute',
            bottom: '5%',
            right: '8%',
            opacity: 0.06,
            transform: `rotate(${-frame * 0.05}deg)`,
          }}
          width="380"
          height="380"
          viewBox="0 0 100 100"
        >
          <polygon points="50,5 95,95 5,95" fill="none" stroke="#000000" strokeWidth="1.5" />
        </svg>

        {/* Hex / grid texture */}
        <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
          <defs>
            <pattern id="grid40" width="70" height="70" patternUnits="userSpaceOnUse">
              <path d="M 70 0 L 0 0 0 70" fill="none" stroke="#000000" strokeWidth="1" />
            </pattern>
          </defs>
          <rect width="100%" height="100%" fill="url(#grid40)" />
        </svg>

        {/* Particles */}
        {Array.from({ length: 14 }).map((_, i) => {
          const x = seededRandom(i + 90) * 1920;
          const y = seededRandom(i + 160) * 1080;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x + Math.sin(frame * 0.025 + i) * 14,
                top: y,
                width: 5,
                height: 5,
                backgroundColor: '#000000',
                opacity: 0.15,
                transform: 'rotate(45deg)',
              }}
            />
          );
        })}

        {/* TITLE */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 80,
            opacity: titleIn,
            transform: `translateY(${(1 - titleIn) * -20}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.6,
              letterSpacing: 5,
            }}
          >
            THE TRUE COST
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 128,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -5,
              lineHeight: 0.95,
              marginTop: 4,
            }}
          >
            LONGEVITY <span style={{ color: '#FEEC41', backgroundColor: '#000000', padding: '0 22px' }}>GAMBLE</span>
          </div>
        </div>

        {/* LEFT: 3 condition checkmarks */}
        <div
          style={{
            position: 'absolute',
            top: 360,
            left: 80,
            width: 760,
            display: 'flex',
            flexDirection: 'column',
            gap: 18,
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 22,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.65,
              letterSpacing: 4,
              marginBottom: 8,
            }}
          >
            IF ALL THREE ARE TRUE:
          </div>
          {[
            { text: 'YOU ARE HEALTHY', op: c1 },
            { text: 'NO CRUSHING DEBT', op: c2 },
            { text: 'NO SPOUSAL BRIDGE NEED', op: c3 },
          ].map((row, i) => (
            <div
              key={i}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 22,
                opacity: row.op,
                transform: `translateX(${(1 - row.op) * -50}px)`,
              }}
            >
              {/* Checkbox */}
              <div
                style={{
                  width: 76,
                  height: 76,
                  border: '5px solid #000000',
                  backgroundColor: '#FEEC41',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  flexShrink: 0,
                }}
              >
                <svg width="44" height="44" viewBox="0 0 24 24" fill="none">
                  <path
                    d="M4 12 L10 18 L20 6"
                    stroke="#000000"
                    strokeWidth="4"
                    strokeLinecap="round"
                    strokeLinejoin="round"
                  />
                </svg>
              </div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 46,
                  fontWeight: 900,
                  color: '#000000',
                  letterSpacing: -1,
                  lineHeight: 1,
                }}
              >
                {row.text}
              </div>
            </div>
          ))}
        </div>

        {/* RIGHT: Dice / gamble visual */}
        <div
          style={{
            position: 'absolute',
            top: 340,
            right: 100,
            width: 380,
            height: 380,
            opacity: diceIn,
            transform: `translateY(${(1 - diceIn) * 40 + diceLift}px) rotate(${diceRoll}deg) scale(${diceIn})`,
          }}
        >
          {/* Dice 1 */}
          <div
            style={{
              position: 'absolute',
              top: 30,
              left: 20,
              width: 200,
              height: 200,
              backgroundColor: '#FEEC41',
              border: '6px solid #000000',
              borderRadius: 24,
              boxShadow: '0 14px 0 rgba(0,0,0,0.18)',
              transform: 'rotate(-8deg)',
              display: 'grid',
              gridTemplateColumns: 'repeat(3, 1fr)',
              gridTemplateRows: 'repeat(3, 1fr)',
              padding: 28,
            }}
          >
            {[0, 0, 1, 0, 1, 0, 1, 0, 0].map((dot, i) => (
              <div
                key={i}
                style={{
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                }}
              >
                {dot === 1 && (
                  <div style={{ width: 28, height: 28, borderRadius: '50%', backgroundColor: '#000000' }} />
                )}
              </div>
            ))}
          </div>
          {/* Dice 2 */}
          <div
            style={{
              position: 'absolute',
              bottom: 30,
              right: 0,
              width: 200,
              height: 200,
              backgroundColor: '#000000',
              border: '6px solid #000000',
              borderRadius: 24,
              boxShadow: '0 14px 0 rgba(0,0,0,0.18)',
              transform: `rotate(${10 + diceRoll * 0.5}deg)`,
              display: 'grid',
              gridTemplateColumns: 'repeat(3, 1fr)',
              gridTemplateRows: 'repeat(3, 1fr)',
              padding: 28,
            }}
          >
            {[1, 0, 1, 0, 0, 0, 1, 0, 1].map((dot, i) => (
              <div
                key={i}
                style={{
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                }}
              >
                {dot === 1 && (
                  <div style={{ width: 28, height: 28, borderRadius: '50%', backgroundColor: '#FEEC41' }} />
                )}
              </div>
            ))}
          </div>
        </div>

        {/* BOTTOM: MASSIVE GAMBLE callout */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            left: '50%',
            transform: `translateX(-50%) scale(${gambleIn * gamblePulse})`,
            opacity: gambleIn,
            backgroundColor: '#000000',
            border: '6px solid #FEEC41',
            padding: '20px 50px',
            display: 'flex',
            alignItems: 'center',
            gap: 22,
            boxShadow: '0 14px 0 rgba(0,0,0,0.2)',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 56,
              fontWeight: 900,
              color: '#FEEC41',
              letterSpacing: -2,
              lineHeight: 1,
            }}
          >
            CLAIM AT 62 = <span style={{ color: '#FCD1D1' }}>MASSIVE GAMBLE</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.25) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#000000',
          opacity: 0.4,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene40_LongevityGamble;
```

