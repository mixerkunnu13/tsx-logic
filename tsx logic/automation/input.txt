```tsx
import React from 'react';
Audio: "The HDG plan has the exact same core federal benefits — same hospital stays, same skilled nursing, same foreign travel emergencies."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31_HDGCoreBenefits',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene31_HDGCoreBenefits: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headerY = interpolate(sp(5), [0, 1], [-80, 0]);
  const headerOp = interpolate(sp(5), [0, 1], [0, 1]);

  const cardEntry = (delay: number) => ({
    scale: interpolate(sp(delay), [0, 1], [0.6, 1]),
    op: interpolate(sp(delay), [0, 1], [0, 1]),
    y: interpolate(sp(delay), [0, 1], [60, 0]),
  });

  const card1 = cardEntry(35);
  const card2 = cardEntry(55);
  const card3 = cardEntry(75);

  const stampScale = interpolate(sp(110, { damping: 6, stiffness: 200 }), [0, 1], [3, 1]);
  const stampOp = interpolate(sp(110), [0, 1], [0, 1]);
  const stampRot = interpolate(sp(110), [0, 1], [-25, -12]);

  const breathe = Math.sin(frame * 0.04) * 0.015 + 1;

  return (
    <AbsoluteFill style={{ backgroundColor: '#4647AE', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 20%, rgba(250,185,91,0.18) 0%, transparent 55%), radial-gradient(circle at 75% 85%, rgba(232,237,242,0.10) 0%, transparent 60%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', width: 700, height: 700, borderRadius: '50%',
        border: '2px solid rgba(232,237,242,0.06)',
        top: -200, left: -200,
        transform: `rotate(${frame * 0.15}deg) scale(${breathe})`,
      }} />
      <div style={{
        position: 'absolute', width: 500, height: 500,
        border: '2px solid rgba(250,185,91,0.07)',
        bottom: -150, right: -150,
        transform: `rotate(${-frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 300, height: 300, borderRadius: '30%',
        background: 'rgba(250,185,91,0.04)',
        top: '40%', left: '5%',
        transform: `rotate(${frame * 0.3}deg) scale(${breathe})`,
      }} />

      {/* Motion texture - vertical grid lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 24 }).map((_, i) => (
          <line key={i} x1={i * 80} y1={0} x2={i * 80} y2={1080} stroke="#E8EDF2" strokeWidth={1} />
        ))}
        {Array.from({ length: 14 }).map((_, i) => (
          <line key={`h${i}`} x1={0} y1={i * 80} x2={1920} y2={i * 80} stroke="#E8EDF2" strokeWidth={1} />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const rx = seededRandom(i + 1) * 1920;
        const ry = seededRandom(i + 5) * 1080;
        const size = seededRandom(i + 9) * 4 + 2;
        const drift = Math.sin(frame * 0.02 + i) * 20;
        return (
          <div key={i} style={{
            position: 'absolute', left: rx, top: ry + drift,
            width: size, height: size, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.4,
          }} />
        );
      })}

      {/* Header */}
      <div style={{
        position: 'absolute', top: 90, left: 0, right: 0,
        textAlign: 'center', opacity: headerOp,
        transform: `translateY(${headerY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 8, marginBottom: 12,
        }}>
          HIGH DEDUCTIBLE PLAN G
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 102, fontWeight: 900,
          color: '#E8EDF2', lineHeight: 1.05, letterSpacing: -2,
        }}>
          SAME CORE BENEFITS
        </div>
        <div style={{
          width: 180, height: 6, background: '#FAB95B',
          margin: '20px auto 0', borderRadius: 3,
        }} />
      </div>

      {/* Three benefit cards */}
      <div style={{
        position: 'absolute', top: 430, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', gap: 40,
      }}>
        {/* CARD 1: Hospital */}
        <div style={{
          width: 420, height: 380,
          background: 'rgba(232,237,242,0.08)',
          border: '2px solid rgba(250,185,91,0.4)',
          borderRadius: 24, padding: 32,
          opacity: card1.op,
          transform: `translateY(${card1.y}px) scale(${card1.scale})`,
          backdropFilter: 'blur(10px)',
        }}>
          <svg width="80" height="80" viewBox="0 0 24 24" style={{ marginBottom: 18 }}>
            <rect x="3" y="6" width="18" height="15" fill="none" stroke="#FAB95B" strokeWidth="2" rx="1" />
            <path d="M12 9 L12 15 M9 12 L15 12" stroke="#FAB95B" strokeWidth="2.5" strokeLinecap="round" />
            <path d="M7 6 L7 3 L17 3 L17 6" fill="none" stroke="#FAB95B" strokeWidth="2" />
          </svg>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 48, fontWeight: 800,
            color: '#E8EDF2', marginBottom: 10,
          }}>HOSPITAL STAYS</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 500,
            color: '#E8EDF2', opacity: 0.75, lineHeight: 1.3,
          }}>Full inpatient coverage — identical to standard Plan G</div>
          <div style={{
            marginTop: 20, display: 'inline-block',
            padding: '6px 14px', background: '#FAB95B',
            color: '#4647AE', fontFamily: 'JetBrains Mono',
            fontSize: 22, fontWeight: 800, borderRadius: 6,
          }}>100% COVERED</div>
        </div>

        {/* CARD 2: Skilled Nursing */}
        <div style={{
          width: 420, height: 380,
          background: 'rgba(232,237,242,0.08)',
          border: '2px solid rgba(250,185,91,0.4)',
          borderRadius: 24, padding: 32,
          opacity: card2.op,
          transform: `translateY(${card2.y}px) scale(${card2.scale})`,
          backdropFilter: 'blur(10px)',
        }}>
          <svg width="80" height="80" viewBox="0 0 24 24" style={{ marginBottom: 18 }}>
            <path d="M4 21 L4 10 L12 4 L20 10 L20 21 Z" fill="none" stroke="#FAB95B" strokeWidth="2" />
            <path d="M10 14 L14 14 M12 12 L12 16" stroke="#FAB95B" strokeWidth="2.5" strokeLinecap="round" />
            <circle cx="12" cy="14" r="3" fill="none" stroke="#FAB95B" strokeWidth="1.5" />
          </svg>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 48, fontWeight: 800,
            color: '#E8EDF2', marginBottom: 10,
          }}>SKILLED NURSING</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 500,
            color: '#E8EDF2', opacity: 0.75, lineHeight: 1.3,
          }}>Same facility benefits — no compromise on care quality</div>
          <div style={{
            marginTop: 20, display: 'inline-block',
            padding: '6px 14px', background: '#FAB95B',
            color: '#4647AE', fontFamily: 'JetBrains Mono',
            fontSize: 22, fontWeight: 800, borderRadius: 6,
          }}>100% COVERED</div>
        </div>

        {/* CARD 3: Foreign Travel */}
        <div style={{
          width: 420, height: 380,
          background: 'rgba(232,237,242,0.08)',
          border: '2px solid rgba(250,185,91,0.4)',
          borderRadius: 24, padding: 32,
          opacity: card3.op,
          transform: `translateY(${card3.y}px) scale(${card3.scale})`,
          backdropFilter: 'blur(10px)',
        }}>
          <svg width="80" height="80" viewBox="0 0 24 24" style={{ marginBottom: 18 }}>
            <circle cx="12" cy="12" r="9" fill="none" stroke="#FAB95B" strokeWidth="2" />
            <path d="M3 12 L21 12 M12 3 C15 7 15 17 12 21 C9 17 9 7 12 3" fill="none" stroke="#FAB95B" strokeWidth="1.8" />
          </svg>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 48, fontWeight: 800,
            color: '#E8EDF2', marginBottom: 10,
          }}>FOREIGN TRAVEL</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 500,
            color: '#E8EDF2', opacity: 0.75, lineHeight: 1.3,
          }}>Emergency medical abroad — exact same protection</div>
          <div style={{
            marginTop: 20, display: 'inline-block',
            padding: '6px 14px', background: '#FAB95B',
            color: '#4647AE', fontFamily: 'JetBrains Mono',
            fontSize: 22, fontWeight: 800, borderRadius: 6,
          }}>100% COVERED</div>
        </div>
      </div>

      {/* IDENTICAL stamp */}
      <div style={{
        position: 'absolute', bottom: 90, right: 80,
        opacity: stampOp,
        transform: `rotate(${stampRot}deg) scale(${stampScale})`,
      }}>
        <div style={{
          padding: '18px 36px',
          border: '5px solid #FAB95B',
          fontFamily: 'Inter, Poppins', fontSize: 54, fontWeight: 900,
          color: '#FAB95B', letterSpacing: 4,
          background: 'rgba(70,71,174,0.6)',
        }}>
          IDENTICAL
        </div>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene31_HDGCoreBenefits;
```

```tsx
import React from 'react';
Audio: "The only difference is a small annual deductible before insurance kicks in. In 2026, that deductible is $257."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32_DeductibleReveal',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32_DeductibleReveal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Top label
  const labelOp = interpolate(sp(5), [0, 1], [0, 1]);
  const labelY = interpolate(sp(5), [0, 1], [-40, 0]);

  // Year 2026 swoop
  const yearX = interpolate(sp(25), [0, 1], [-300, 0]);
  const yearOp = interpolate(sp(25), [0, 1], [0, 1]);

  // Counting number animation
  const countProgress = interpolate(frame, [60, 130], [0, 257], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });
  const displayNumber = Math.floor(countProgress);

  // Big number scale punch at finish
  const numScale = interpolate(sp(130, { damping: 8, stiffness: 200 }), [0, 1], [1, 1.08]);
  const numGlow = interpolate(frame, [130, 145, 160], [0, 30, 15], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Subtitle
  const subOp = interpolate(sp(155), [0, 1], [0, 1]);
  const subY = interpolate(sp(155), [0, 1], [30, 0]);

  // Shake on number lock
  const shakeX = frame > 130 && frame < 145 ? Math.sin(frame * 2) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#4647AE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(250,185,91,0.15) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 900, height: 900,
        border: '3px solid rgba(250,185,91,0.08)',
        borderRadius: '50%',
        top: '50%', left: '50%',
        transform: `translate(-50%,-50%) scale(${1 + Math.sin(frame * 0.03) * 0.05}) rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 600, height: 600,
        border: '2px dashed rgba(232,237,242,0.07)',
        borderRadius: '50%',
        top: '50%', left: '50%',
        transform: `translate(-50%,-50%) rotate(${-frame * 0.3}deg)`,
      }} />

      {/* Diagonal stripe texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <line key={i} x1={i * 100 - 500} y1={0} x2={i * 100 + 500} y2={1080} stroke="#FAB95B" strokeWidth={2} />
        ))}
      </svg>

      {/* Floating dots */}
      {Array.from({ length: 25 }).map((_, i) => {
        const rx = seededRandom(i + 11) * 1920;
        const ry = seededRandom(i + 17) * 1080;
        const drift = Math.sin(frame * 0.025 + i * 0.5) * 25;
        return (
          <div key={i} style={{
            position: 'absolute', left: rx, top: ry + drift,
            width: 3, height: 3, borderRadius: '50%',
            background: '#E8EDF2', opacity: 0.3,
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 110, left: 0, right: 0, textAlign: 'center',
        opacity: labelOp, transform: `translateY(${labelY}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          padding: '14px 36px',
          background: 'rgba(250,185,91,0.15)',
          border: '2px solid #FAB95B',
          borderRadius: 50,
          fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 5,
        }}>
          THE ONLY DIFFERENCE
        </div>
      </div>

      {/* Year tag */}
      <div style={{
        position: 'absolute', top: 240, left: 0, right: 0, textAlign: 'center',
        opacity: yearOp, transform: `translateX(${yearX}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 64, fontWeight: 700,
          color: '#E8EDF2', letterSpacing: 12,
        }}>
          2026 ANNUAL DEDUCTIBLE
        </div>
      </div>

      {/* Big number */}
      <div style={{
        position: 'absolute', top: 380, left: 0, right: 0, textAlign: 'center',
        transform: `scale(${numScale}) translateX(${shakeX}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 360, fontWeight: 900,
          color: '#FAB95B', lineHeight: 1, letterSpacing: -10,
          textShadow: `0 0 ${numGlow}px rgba(250,185,91,0.8)`,
        }}>
          ${displayNumber}
        </div>
      </div>

      {/* Underline accent */}
      <div style={{
        position: 'absolute', top: 760, left: '50%',
        transform: `translateX(-50%) scaleX(${interpolate(sp(135), [0, 1], [0, 1])})`,
        width: 600, height: 8, background: '#FAB95B',
        borderRadius: 4,
      }} />

      {/* Subtitle */}
      <div style={{
        position: 'absolute', top: 820, left: 0, right: 0, textAlign: 'center',
        opacity: subOp, transform: `translateY(${subY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 56, fontWeight: 600,
          color: '#E8EDF2', maxWidth: 1400, margin: '0 auto', lineHeight: 1.25,
        }}>
          Pay this <span style={{ color: '#FAB95B', fontWeight: 800 }}>FIRST</span>, then insurance kicks in
        </div>
      </div>

      {/* Side accents */}
      <div style={{
        position: 'absolute', left: 80, top: '50%',
        transform: `translateY(-50%) scale(${interpolate(sp(50), [0, 1], [0, 1])})`,
      }}>
        <svg width="60" height="200" viewBox="0 0 60 200">
          <path d="M30 10 L50 100 L30 190 L10 100 Z" fill="none" stroke="#FAB95B" strokeWidth="3" />
        </svg>
      </div>
      <div style={{
        position: 'absolute', right: 80, top: '50%',
        transform: `translateY(-50%) scale(${interpolate(sp(50), [0, 1], [0, 1])})`,
      }}>
        <svg width="60" height="200" viewBox="0 0 60 200">
          <path d="M30 10 L50 100 L30 190 L10 100 Z" fill="none" stroke="#FAB95B" strokeWidth="3" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 45%, rgba(0,0,0,0.5) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene32_DeductibleReveal;
```

```tsx
import React from 'react';
Audio: "Let that sink in. If you are healthy, you only pay the first $257 of your Medicare covered costs for the entire year."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33_HealthyMath',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene33_HealthyMath: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // "Let that sink in" punch
  const sinkScale = interpolate(sp(5, { damping: 8 }), [0, 1], [0.5, 1]);
  const sinkOp = interpolate(sp(5), [0, 1], [0, 1]);
  const sinkExit = interpolate(frame, [60, 75], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Timeline bar entrance
  const barOp = interpolate(sp(75), [0, 1], [0, 1]);
  const barScale = interpolate(sp(75), [0, 1], [0.85, 1]);

  // Progress dot fills the year
  const fillWidth = interpolate(frame, [100, 220], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Deductible callout
  const deductOp = interpolate(sp(110), [0, 1], [0, 1]);
  const deductY = interpolate(sp(110), [0, 1], [-30, 0]);

  // Bottom label
  const bottomOp = interpolate(sp(180), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4647AE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(70,71,174,1) 0%, rgba(58,59,150,1) 100%), radial-gradient(circle at 50% 30%, rgba(250,185,91,0.12) 0%, transparent 50%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 400, height: 400,
        background: 'rgba(232,237,242,0.04)',
        borderRadius: 80,
        top: 100, right: -100,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 350, height: 350,
        border: '2px solid rgba(250,185,91,0.08)',
        borderRadius: '50%',
        bottom: 50, left: -120,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
      }} />

      {/* Dot grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 19 }).map((_, x) =>
          Array.from({ length: 11 }).map((_, y) => (
            <circle key={`${x}-${y}`} cx={x * 100 + 50} cy={y * 100 + 50} r={2} fill="#E8EDF2" />
          ))
        )}
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const rx = seededRandom(i + 21) * 1920;
        const ry = seededRandom(i + 27) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 18;
        return (
          <div key={i} style={{
            position: 'absolute', left: rx, top: ry + drift,
            width: 4, height: 4, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.35,
          }} />
        );
      })}

      {/* "LET THAT SINK IN" big intro */}
      <div style={{
        position: 'absolute', top: 200, left: 0, right: 0, textAlign: 'center',
        opacity: sinkOp * sinkExit, transform: `scale(${sinkScale})`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 170, fontWeight: 900,
          color: '#FAB95B', letterSpacing: -3, lineHeight: 1,
          textShadow: '0 0 40px rgba(250,185,91,0.5)',
        }}>
          LET THAT
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 170, fontWeight: 900,
          color: '#E8EDF2', letterSpacing: -3, lineHeight: 1,
        }}>
          SINK IN.
        </div>
      </div>

      {/* Year timeline */}
      <div style={{
        position: 'absolute', top: 280, left: 0, right: 0,
        opacity: barOp, transform: `scale(${barScale})`,
      }}>
        <div style={{ textAlign: 'center', marginBottom: 40 }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 42, fontWeight: 800,
            color: '#E8EDF2', letterSpacing: 4,
          }}>
            YOUR ENTIRE YEAR OF MEDICARE COSTS
          </div>
        </div>

        {/* Timeline track */}
        <div style={{
          position: 'relative',
          width: 1500, height: 120,
          margin: '0 auto',
          background: 'rgba(232,237,242,0.08)',
          border: '3px solid rgba(232,237,242,0.3)',
          borderRadius: 60, overflow: 'hidden',
        }}>
          {/* Filled portion (deductible zone) */}
          <div style={{
            position: 'absolute', left: 0, top: 0, bottom: 0,
            width: `${Math.min(fillWidth * 100, 8)}%`,
            background: '#FAB95B',
          }} />
          {/* Insurance covered zone */}
          <div style={{
            position: 'absolute', left: '8%', top: 0, bottom: 0,
            width: `${Math.max(0, (fillWidth - 0.08) * 100)}%`,
            background: 'linear-gradient(90deg, rgba(232,237,242,0.7), rgba(232,237,242,0.9))',
          }} />

          {/* Month markers */}
          {['JAN', 'APR', 'JUL', 'OCT', 'DEC'].map((m, i) => (
            <div key={m} style={{
              position: 'absolute', left: `${i * 25}%`, top: 130,
              fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 700,
              color: '#E8EDF2', opacity: 0.7,
            }}>{m}</div>
          ))}
        </div>

        {/* YOU PAY callout - deductible portion */}
        <div style={{
          position: 'absolute', top: 230, left: '11%',
          opacity: deductOp, transform: `translateY(${deductY}px)`,
        }}>
          <div style={{
            background: '#FAB95B', padding: '14px 24px', borderRadius: 12,
            fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 900,
            color: '#4647AE', letterSpacing: 2,
            boxShadow: '0 8px 24px rgba(0,0,0,0.3)',
          }}>
            YOU PAY: $257
          </div>
          <svg width="40" height="60" style={{ position: 'absolute', top: -55, left: 40 }}>
            <path d="M20 0 L20 50 M10 40 L20 50 L30 40" stroke="#FAB95B" strokeWidth="4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>

        {/* INSURANCE PAYS callout */}
        <div style={{
          position: 'absolute', top: 230, left: '55%',
          opacity: interpolate(sp(160), [0, 1], [0, 1]),
          transform: `translateY(${interpolate(sp(160), [0, 1], [-30, 0])}px)`,
        }}>
          <div style={{
            background: '#E8EDF2', padding: '14px 24px', borderRadius: 12,
            fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 900,
            color: '#4647AE', letterSpacing: 2,
            boxShadow: '0 8px 24px rgba(0,0,0,0.3)',
          }}>
            INSURANCE PAYS 100%
          </div>
          <svg width="40" height="60" style={{ position: 'absolute', top: -55, left: 80 }}>
            <path d="M20 0 L20 50 M10 40 L20 50 L30 40" stroke="#E8EDF2" strokeWidth="4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>
      </div>

      {/* Bottom statement */}
      <div style={{
        position: 'absolute', bottom: 140, left: 0, right: 0, textAlign: 'center',
        opacity: bottomOp,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 52, fontWeight: 700,
          color: '#E8EDF2', lineHeight: 1.3,
        }}>
          If you're healthy — that's <span style={{ color: '#FAB95B', fontWeight: 900 }}>ALL you pay</span> all year
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene33_HealthyMath;
```

```tsx
import React from 'react';
Audio: "After you hit that deductible, the HDG plan pays exactly 100% of the rest — just like standard Plan G."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34_HundredPercent',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene34_HundredPercent: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Header
  const headOp = interpolate(sp(5), [0, 1], [0, 1]);
  const headY = interpolate(sp(5), [0, 1], [-40, 0]);

  // Threshold gate animation
  const gateOp = interpolate(sp(30), [0, 1], [0, 1]);
  const gateX = interpolate(sp(30), [0, 1], [-100, 0]);

  // Arrow shoot through
  const arrowProgress = interpolate(frame, [70, 130], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const arrowX = interpolate(arrowProgress, [0, 1], [350, 1300]);
  const arrowOp = interpolate(frame, [70, 80, 125, 135], [0, 1, 1, 0]);

  // 100% reveal
  const hundredScale = interpolate(sp(135, { damping: 7, stiffness: 180 }), [0, 1], [0, 1]);
  const hundredOp = interpolate(sp(135), [0, 1], [0, 1]);

  // Pulse on 100%
  const pulse = 1 + Math.sin(frame * 0.15) * 0.03;

  // Comparison row
  const compOp = interpolate(sp(170), [0, 1], [0, 1]);
  const compY = interpolate(sp(170), [0, 1], [40, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4647AE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 50%, rgba(250,185,91,0.18) 0%, transparent 55%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 700, height: 700,
        border: '3px solid rgba(250,185,91,0.07)',
        borderRadius: '50%',
        right: -200, top: '20%',
        transform: `rotate(${frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 250, height: 250,
        background: 'rgba(232,237,242,0.05)',
        borderRadius: 30,
        left: 100, bottom: 100,
        transform: `rotate(${frame * 0.3}deg)`,
      }} />

      {/* Concentric arcs texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 8 }).map((_, i) => (
          <circle key={i} cx={1400} cy={540} r={150 + i * 90} fill="none" stroke="#E8EDF2" strokeWidth={1} />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const rx = seededRandom(i + 31) * 1920;
        const ry = seededRandom(i + 37) * 1080;
        const drift = Math.cos(frame * 0.025 + i * 0.3) * 22;
        return (
          <div key={i} style={{
            position: 'absolute', left: rx + drift, top: ry,
            width: 5, height: 5, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.35,
          }} />
        );
      })}

      {/* Header */}
      <div style={{
        position: 'absolute', top: 100, left: 0, right: 0, textAlign: 'center',
        opacity: headOp, transform: `translateY(${headY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 8, marginBottom: 14,
        }}>
          AFTER YOU HIT THE DEDUCTIBLE
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 110, fontWeight: 900,
          color: '#E8EDF2', lineHeight: 1, letterSpacing: -2,
        }}>
          HDG PAYS THE REST
        </div>
      </div>

      {/* Gate / Threshold visual */}
      <div style={{
        position: 'absolute', top: 460, left: 200,
        opacity: gateOp, transform: `translateX(${gateX}px)`,
      }}>
        <div style={{
          width: 180, height: 280,
          border: '4px solid #FAB95B',
          borderRadius: 12,
          background: 'rgba(250,185,91,0.1)',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          flexDirection: 'column',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700,
            color: '#FAB95B', letterSpacing: 3,
          }}>
            $257
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 20, fontWeight: 600,
            color: '#FAB95B', marginTop: 8, letterSpacing: 2,
          }}>
            GATE
          </div>
        </div>
      </div>

      {/* Arrow shooting through */}
      <div style={{
        position: 'absolute', top: 580, left: arrowX, opacity: arrowOp,
      }}>
        <svg width="120" height="40" viewBox="0 0 120 40">
          <path d="M0 20 L100 20 M85 8 L100 20 L85 32"
            stroke="#E8EDF2" strokeWidth="6" fill="none" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      </div>

      {/* 100% Big Reveal */}
      <div style={{
        position: 'absolute', top: 440, right: 220,
        opacity: hundredOp, transform: `scale(${hundredScale * pulse})`,
      }}>
        <div style={{
          width: 360, height: 360, borderRadius: '50%',
          background: 'radial-gradient(circle, #FAB95B 0%, rgba(250,185,91,0.6) 100%)',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          flexDirection: 'column',
          boxShadow: '0 0 60px rgba(250,185,91,0.6)',
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 140, fontWeight: 900,
            color: '#4647AE', lineHeight: 1, letterSpacing: -4,
          }}>
            100%
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 26, fontWeight: 800,
            color: '#4647AE', letterSpacing: 4, marginTop: 4,
          }}>
            COVERED
          </div>
        </div>
      </div>

      {/* Bottom comparison */}
      <div style={{
        position: 'absolute', bottom: 100, left: 0, right: 0,
        textAlign: 'center',
        opacity: compOp, transform: `translateY(${compY}px)`,
      }}>
        <div style={{
          display: 'inline-flex', alignItems: 'center', gap: 30,
          background: 'rgba(232,237,242,0.08)',
          border: '2px solid rgba(232,237,242,0.25)',
          padding: '24px 50px', borderRadius: 60,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 700,
            color: '#E8EDF2',
          }}>
            HDG Plan
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 50, fontWeight: 900,
            color: '#FAB95B',
          }}>
            =
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 700,
            color: '#E8EDF2',
          }}>
            Standard Plan G
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 24, fontWeight: 700,
            color: '#FAB95B', letterSpacing: 3,
            padding: '6px 16px', border: '2px solid #FAB95B', borderRadius: 8,
          }}>
            AFTER $257
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene34_HundredPercent;
```

```tsx
import React from 'react';
Audio: "Instead of paying $250 a month, you might only pay $50 a month. That is a savings of $2,400 a year."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35_PremiumComparison',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35_PremiumComparison: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Headline
  const headOp = interpolate(sp(5), [0, 1], [0, 1]);
  const headScale = interpolate(sp(5), [0, 1], [0.9, 1]);

  // Bar chart entries
  const bar1Height = interpolate(sp(35), [0, 1], [0, 480]);
  const bar2Height = interpolate(sp(75), [0, 1], [0, 96]);

  const label1Op = interpolate(sp(50), [0, 1], [0, 1]);
  const label2Op = interpolate(sp(90), [0, 1], [0, 1]);

  // Savings burst
  const burstScale = interpolate(sp(130, { damping: 6, stiffness: 200 }), [0, 1], [0, 1]);
  const burstOp = interpolate(sp(130), [0, 1], [0, 1]);
  const burstRot = Math.sin(frame * 0.08) * 4;

  // Bottom annual line
  const annualOp = interpolate(sp(170), [0, 1], [0, 1]);
  const annualY = interpolate(sp(170), [0, 1], [40, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4647AE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(70,71,174,1) 0%, rgba(55,56,140,1) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 500, height: 500,
        border: '3px solid rgba(250,185,91,0.08)',
        top: -100, left: -100,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 600, height: 600,
        background: 'rgba(232,237,242,0.04)',
        borderRadius: '50%',
        bottom: -200, right: -200,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
      }} />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        {Array.from({ length: 11 }).map((_, i) => (
          <line key={i} x1={0} y1={i * 100} x2={1920} y2={i * 100} stroke="#E8EDF2" strokeWidth={1} />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const rx = seededRandom(i + 41) * 1920;
        const ry = seededRandom(i + 47) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 18;
        return (
          <div key={i} style={{
            position: 'absolute', left: rx, top: ry + drift,
            width: 4, height: 4, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.3,
          }} />
        );
      })}

      {/* Headline */}
      <div style={{
        position: 'absolute', top: 80, left: 0, right: 0, textAlign: 'center',
        opacity: headOp, transform: `scale(${headScale})`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 8, marginBottom: 10,
        }}>
          MONTHLY PREMIUM COMPARISON
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 90, fontWeight: 900,
          color: '#E8EDF2', letterSpacing: -2,
        }}>
          $250 vs $50
        </div>
      </div>

      {/* Bar chart container */}
      <div style={{
        position: 'absolute', bottom: 220, left: 200,
        display: 'flex', alignItems: 'flex-end', gap: 80,
      }}>
        {/* BAR 1 - Standard Plan G */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
          <div style={{
            opacity: label1Op,
            fontFamily: 'JetBrains Mono', fontSize: 56, fontWeight: 900,
            color: '#E8EDF2', marginBottom: 12,
          }}>
            $250
          </div>
          <div style={{
            width: 220, height: bar1Height,
            background: 'linear-gradient(180deg, rgba(232,237,242,0.95) 0%, rgba(232,237,242,0.7) 100%)',
            borderRadius: '12px 12px 0 0',
            boxShadow: '0 8px 24px rgba(0,0,0,0.3)',
          }} />
          <div style={{
            marginTop: 20,
            fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 800,
            color: '#E8EDF2', textAlign: 'center',
          }}>
            STANDARD<br/>PLAN G
          </div>
        </div>

        {/* Equal sign */}
        <div style={{
          alignSelf: 'center',
          fontFamily: 'Inter, Poppins', fontSize: 80, fontWeight: 900,
          color: '#FAB95B', opacity: interpolate(sp(80), [0, 1], [0, 0.6]),
          marginBottom: 100,
        }}>
          vs
        </div>

        {/* BAR 2 - HDG */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
          <div style={{
            opacity: label2Op,
            fontFamily: 'JetBrains Mono', fontSize: 56, fontWeight: 900,
            color: '#FAB95B', marginBottom: 12,
          }}>
            $50
          </div>
          <div style={{
            width: 220, height: bar2Height,
            background: 'linear-gradient(180deg, #FAB95B 0%, rgba(250,185,91,0.7) 100%)',
            borderRadius: '12px 12px 0 0',
            boxShadow: '0 8px 24px rgba(250,185,91,0.4)',
          }} />
          <div style={{
            marginTop: 20,
            fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 800,
            color: '#FAB95B', textAlign: 'center',
          }}>
            HDG<br/>PLAN
          </div>
        </div>
      </div>

      {/* SAVINGS BURST */}
      <div style={{
        position: 'absolute', top: 380, right: 160,
        opacity: burstOp, transform: `scale(${burstScale}) rotate(${burstRot}deg)`,
      }}>
        <div style={{ position: 'relative' }}>
          {/* Burst star */}
          <svg width="500" height="500" viewBox="0 0 500 500" style={{ position: 'absolute', top: -50, left: -50 }}>
            <polygon points="250,30 280,180 430,180 310,260 360,420 250,330 140,420 190,260 70,180 220,180"
              fill="#FAB95B" opacity="0.95" />
          </svg>
          <div style={{
            position: 'relative', width: 400, height: 400,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            flexDirection: 'column',
          }}>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 800,
              color: '#4647AE', letterSpacing: 4,
            }}>
              SAVE
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 95, fontWeight: 900,
              color: '#4647AE', lineHeight: 1, letterSpacing: -3,
            }}>
              $2,400
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 800,
              color: '#4647AE', letterSpacing: 3,
            }}>
              EVERY YEAR
            </div>
          </div>
        </div>
      </div>

      {/* Bottom annual line */}
      <div style={{
        position: 'absolute', bottom: 100, left: 0, right: 0, textAlign: 'center',
        opacity: annualOp, transform: `translateY(${annualY}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          padding: '14px 36px',
          background: 'rgba(232,237,242,0.1)',
          border: '2px dashed rgba(232,237,242,0.4)',
          borderRadius: 50,
          fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 700,
          color: '#E8EDF2',
        }}>
          $200/month back in your pocket
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene35_PremiumComparison;
```

```tsx
import React from 'react';
Audio: "You keep the difference in your pocket, and you still have absolute protection against a catastrophic hospital bill."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36_PocketProtection',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene36_PocketProtection: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Header
  const headOp = interpolate(sp(5), [0, 1], [0, 1]);
  const headY = interpolate(sp(5), [0, 1], [-30, 0]);

  // Split panels
  const leftScale = interpolate(sp(40), [0, 1], [0.85, 1]);
  const leftOp = interpolate(sp(40), [0, 1], [0, 1]);
  const leftX = interpolate(sp(40), [0, 1], [-80, 0]);

  const rightScale = interpolate(sp(70), [0, 1], [0.85, 1]);
  const rightOp = interpolate(sp(70), [0, 1], [0, 1]);
  const rightX = interpolate(sp(70), [0, 1], [80, 0]);

  // AND connector
  const andOp = interpolate(sp(100), [0, 1], [0, 1]);
  const andScale = interpolate(sp(100, { damping: 7, stiffness: 200 }), [0, 1], [0, 1]);

  // Bottom seal
  const sealOp = interpolate(sp(160), [0, 1], [0, 1]);
  const sealY = interpolate(sp(160), [0, 1], [40, 0]);

  // Floating coin animations for left
  const coin = (i: number) => {
    const startFrame = 50 + i * 8;
    const progress = interpolate(frame, [startFrame, startFrame + 60], [0, 1], {
      extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    });
    return {
      y: interpolate(progress, [0, 1], [-200, 50]),
      op: interpolate(progress, [0, 0.2, 0.85, 1], [0, 1, 1, 0]),
      x: Math.sin(progress * Math.PI * 2 + i) * 15,
    };
  };

  return (
    <AbsoluteFill style={{ backgroundColor: '#4647AE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 25% 50%, rgba(250,185,91,0.15) 0%, transparent 50%), radial-gradient(circle at 75% 50%, rgba(232,237,242,0.12) 0%, transparent 50%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 500, height: 500,
        border: '2px solid rgba(232,237,242,0.06)',
        borderRadius: '50%',
        top: '50%', left: '50%',
        transform: `translate(-50%,-50%) scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', width: 280, height: 280,
        background: 'rgba(250,185,91,0.04)',
        borderRadius: 40,
        top: 100, right: 80,
        transform: `rotate(${frame * 0.25}deg)`,
      }} />

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <line key={i} x1={i * 120} y1={0} x2={i * 120 - 400} y2={1080} stroke="#E8EDF2" strokeWidth={1} />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const rx = seededRandom(i + 51) * 1920;
        const ry = seededRandom(i + 57) * 1080;
        const drift = Math.cos(frame * 0.02 + i) * 20;
        return (
          <div key={i} style={{
            position: 'absolute', left: rx + drift, top: ry,
            width: 4, height: 4, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.3,
          }} />
        );
      })}

      {/* Header */}
      <div style={{
        position: 'absolute', top: 90, left: 0, right: 0, textAlign: 'center',
        opacity: headOp, transform: `translateY(${headY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 100, fontWeight: 900,
          color: '#E8EDF2', letterSpacing: -2, lineHeight: 1,
        }}>
          BEST OF <span style={{ color: '#FAB95B' }}>BOTH WORLDS</span>
        </div>
      </div>

      {/* LEFT PANEL - Keep the cash */}
      <div style={{
        position: 'absolute', top: 320, left: 140,
        width: 700, height: 540,
        opacity: leftOp, transform: `translateX(${leftX}px) scale(${leftScale})`,
        background: 'rgba(250,185,91,0.12)',
        border: '3px solid #FAB95B', borderRadius: 30,
        padding: 40, overflow: 'hidden',
      }}>
        {/* Falling coins */}
        {Array.from({ length: 8 }).map((_, i) => {
          const c = coin(i);
          const xPos = 80 + (i % 4) * 130;
          return (
            <div key={i} style={{
              position: 'absolute',
              left: xPos + c.x, top: c.y,
              opacity: c.op,
            }}>
              <svg width="50" height="50" viewBox="0 0 50 50">
                <circle cx="25" cy="25" r="22" fill="#FAB95B" stroke="#E8EDF2" strokeWidth="2" />
                <text x="25" y="33" textAnchor="middle" fontFamily="Inter" fontSize="24" fontWeight="900" fill="#4647AE">$</text>
              </svg>
            </div>
          );
        })}

        <div style={{ position: 'relative', zIndex: 2, marginTop: 60 }}>
          <svg width="90" height="90" viewBox="0 0 24 24" style={{ marginBottom: 20 }}>
            <path d="M3 8 L21 8 L20 20 L4 20 Z" fill="none" stroke="#FAB95B" strokeWidth="2" />
            <path d="M8 8 L8 5 L16 5 L16 8" fill="none" stroke="#FAB95B" strokeWidth="2" />
            <circle cx="12" cy="14" r="3" fill="#FAB95B" />
          </svg>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 64, fontWeight: 900,
            color: '#FAB95B', lineHeight: 1, marginBottom: 16,
          }}>
            KEEP THE<br/>DIFFERENCE
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 600,
            color: '#E8EDF2', lineHeight: 1.3, opacity: 0.9,
          }}>
            $2,400 stays in YOUR pocket every single year
          </div>
        </div>
      </div>

      {/* AND connector circle */}
      <div style={{
        position: 'absolute', top: 530, left: '50%',
        transform: `translateX(-50%) scale(${andScale})`,
        opacity: andOp, zIndex: 5,
      }}>
        <div style={{
          width: 120, height: 120, borderRadius: '50%',
          background: '#E8EDF2',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 900,
          color: '#4647AE', letterSpacing: 2,
          boxShadow: '0 8px 30px rgba(0,0,0,0.4)',
        }}>
          AND
        </div>
      </div>

      {/* RIGHT PANEL - Catastrophic protection */}
      <div style={{
        position: 'absolute', top: 320, right: 140,
        width: 700, height: 540,
        opacity: rightOp, transform: `translateX(${rightX}px) scale(${rightScale})`,
        background: 'rgba(232,237,242,0.1)',
        border: '3px solid #E8EDF2', borderRadius: 30,
        padding: 40,
      }}>
        {/* Shield icon */}
        <svg width="120" height="120" viewBox="0 0 24 24" style={{ marginBottom: 20 }}>
          <path d="M12 2 L20 5 L20 12 C20 17 16 21 12 22 C8 21 4 17 4 12 L4 5 Z"
            fill="none" stroke="#E8EDF2" strokeWidth="2" />
          <path d="M9 12 L11 14 L15 10" stroke="#E8EDF2" strokeWidth="2.5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 64, fontWeight: 900,
          color: '#E8EDF2', lineHeight: 1, marginBottom: 16,
        }}>
          ABSOLUTE<br/>PROTECTION
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 600,
          color: '#E8EDF2', lineHeight: 1.3, opacity: 0.9, marginBottom: 24,
        }}>
          Full coverage against any catastrophic hospital bill
        </div>
        <div style={{
          display: 'inline-block',
          padding: '10px 22px',
          background: '#FAB95B',
          color: '#4647AE',
          fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 800,
          borderRadius: 8, letterSpacing: 2,
        }}>
          ZERO RISK EXPOSURE
        </div>
      </div>

      {/* Bottom seal */}
      <div style={{
        position: 'absolute', bottom: 50, left: 0, right: 0, textAlign: 'center',
        opacity: sealOp, transform: `translateY(${sealY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 700,
          color: '#E8EDF2', opacity: 0.85,
        }}>
          Smarter coverage. Bigger savings. Same security.
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene36_PocketProtection;
```

```tsx
import React from 'react';
Audio: "If you have some savings in the bank to cover that deductible, standard Plan G is a massive waste of your money."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37_WasteWarning',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene37_WasteWarning: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Condition tag
  const condOp = interpolate(sp(5), [0, 1], [0, 1]);
  const condY = interpolate(sp(5), [0, 1], [-50, 0]);

  // Bank vault visual
  const vaultScale = interpolate(sp(30), [0, 1], [0.7, 1]);
  const vaultOp = interpolate(sp(30), [0, 1], [0, 1]);

  // Arrow flow
  const arrowOp = interpolate(sp(75), [0, 1], [0, 1]);
  const arrowDash = -frame * 2;

  // Warning verdict explosion
  const verdictScale = interpolate(sp(110, { damping: 6, stiffness: 180 }), [0, 1], [0, 1]);
  const verdictOp = interpolate(sp(110), [0, 1], [0, 1]);
  const verdictShake = frame > 110 && frame < 140 ? Math.sin(frame * 3) * 5 : 0;

  // Strike-through line on Plan G
  const strikeWidth = interpolate(sp(150), [0, 1], [0, 480]);

  // Bottom callout
  const bottomOp = interpolate(sp(190), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4647AE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 60%, rgba(250,185,91,0.18) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 600, height: 600,
        border: '2px solid rgba(250,185,91,0.07)',
        borderRadius: '50%',
        top: '50%', right: -200,
        transform: `translateY(-50%) rotate(${frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 350, height: 350,
        background: 'rgba(232,237,242,0.04)',
        borderRadius: 60,
        bottom: -100, left: -100,
        transform: `rotate(${-frame * 0.2}deg)`,
      }} />

      {/* Crosshatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <line key={`d1${i}`} x1={i * 80} y1={0} x2={i * 80 + 300} y2={1080} stroke="#E8EDF2" strokeWidth={1} />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const rx = seededRandom(i + 61) * 1920;
        const ry = seededRandom(i + 67) * 1080;
        const drift = Math.sin(frame * 0.022 + i) * 18;
        return (
          <div key={i} style={{
            position: 'absolute', left: rx, top: ry + drift,
            width: 4, height: 4, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.3,
          }} />
        );
      })}

      {/* IF condition tag */}
      <div style={{
        position: 'absolute', top: 100, left: 0, right: 0, textAlign: 'center',
        opacity: condOp, transform: `translateY(${condY}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          padding: '14px 36px',
          background: '#FAB95B',
          borderRadius: 14,
          fontFamily: 'JetBrains Mono', fontSize: 38, fontWeight: 900,
          color: '#4647AE', letterSpacing: 4,
        }}>
          IF YOU HAVE SAVINGS
        </div>
      </div>

      {/* Bank vault */}
      <div style={{
        position: 'absolute', top: 250, left: 200,
        opacity: vaultOp, transform: `scale(${vaultScale})`,
      }}>
        <div style={{
          width: 380, height: 380,
          background: 'rgba(232,237,242,0.1)',
          border: '4px solid #E8EDF2',
          borderRadius: 28,
          padding: 30,
          display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
        }}>
          <svg width="120" height="120" viewBox="0 0 24 24">
            <rect x="3" y="6" width="18" height="14" fill="none" stroke="#E8EDF2" strokeWidth="2" rx="2" />
            <circle cx="12" cy="13" r="3.5" fill="none" stroke="#E8EDF2" strokeWidth="2" />
            <path d="M12 11 L12 15" stroke="#E8EDF2" strokeWidth="2.5" strokeLinecap="round" />
            <path d="M7 6 L7 4 L17 4 L17 6" fill="none" stroke="#E8EDF2" strokeWidth="2" />
          </svg>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 60, fontWeight: 900,
            color: '#FAB95B', marginTop: 18, letterSpacing: 2,
          }}>
            $257+
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 26, fontWeight: 700,
            color: '#E8EDF2', letterSpacing: 3, marginTop: 8,
          }}>
            IN THE BANK
          </div>
        </div>
      </div>

      {/* Flow arrow */}
      <div style={{
        position: 'absolute', top: 420, left: 600,
        opacity: arrowOp,
      }}>
        <svg width="200" height="60" viewBox="0 0 200 60">
          <line x1="0" y1="30" x2="170" y2="30"
            stroke="#FAB95B" strokeWidth="6" strokeDasharray="14 8"
            strokeDashoffset={arrowDash} strokeLinecap="round" />
          <path d="M155 15 L180 30 L155 45"
            stroke="#FAB95B" strokeWidth="6" fill="none" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
        <div style={{
          marginTop: 8,
          fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 3, textAlign: 'center',
        }}>
          THEN...
        </div>
      </div>

      {/* WASTE verdict */}
      <div style={{
        position: 'absolute', top: 280, right: 140,
        width: 640, opacity: verdictOp,
        transform: `scale(${verdictScale}) translateX(${verdictShake}px)`,
      }}>
        {/* Plan G card with strikethrough */}
        <div style={{
          position: 'relative',
          padding: '24px 36px',
          background: 'rgba(232,237,242,0.08)',
          border: '2px solid rgba(232,237,242,0.3)',
          borderRadius: 16, marginBottom: 30,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 56, fontWeight: 900,
            color: '#E8EDF2',
          }}>
            STANDARD PLAN G
          </div>
          <div style={{
            position: 'absolute', top: '50%', left: 30,
            width: strikeWidth, height: 8,
            background: '#FAB95B', borderRadius: 4,
            transform: 'translateY(-50%) rotate(-3deg)',
            boxShadow: '0 0 14px rgba(250,185,91,0.7)',
          }} />
        </div>

        {/* Verdict big */}
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 110, fontWeight: 900,
          color: '#FAB95B', lineHeight: 0.95, letterSpacing: -3,
          textShadow: '0 0 30px rgba(250,185,91,0.5)',
        }}>
          MASSIVE
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 110, fontWeight: 900,
          color: '#E8EDF2', lineHeight: 0.95, letterSpacing: -3,
        }}>
          WASTE.
        </div>
      </div>

      {/* Bottom callout */}
      <div style={{
        position: 'absolute', bottom: 80, left: 0, right: 0, textAlign: 'center',
        opacity: bottomOp,
      }}>
        <div style={{
          display: 'inline-block',
          padding: '18px 40px',
          border: '2px dashed #FAB95B', borderRadius: 50,
          fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 700,
          color: '#E8EDF2',
        }}>
          Stop paying premium for protection you already self-fund
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene37_WasteWarning;
```

```tsx
import React from 'react';
Audio: "Overcharge number three is the Plan N Copay Illusion — the trap that catches people who think they're saving money."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38_OverchargeThreeIntro',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene38_OverchargeThreeIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Number 03 rolling in
  const numX = interpolate(sp(5, { damping: 9, stiffness: 100 }), [0, 1], [-400, 0]);
  const numOp = interpolate(sp(5), [0, 1], [0, 1]);
  const numRot = interpolate(sp(5), [0, 1], [-30, 0]);

  // OVERCHARGE label
  const labelOp = interpolate(sp(35), [0, 1], [0, 1]);
  const labelY = interpolate(sp(35), [0, 1], [-30, 0]);

  // Main title
  const titleOp = interpolate(sp(60), [0, 1], [0, 1]);
  const titleY = interpolate(sp(60), [0, 1], [40, 0]);

  // Illusion visual - rotating mask/mirror
  const illuOp = interpolate(sp(110), [0, 1], [0, 1]);
  const illuRot = frame * 0.5;
  const illuScale = interpolate(sp(110), [0, 1], [0.7, 1]);

  // Bottom subtitle
  const subOp = interpolate(sp(160), [0, 1], [0, 1]);
  const subY = interpolate(sp(160), [0, 1], [30, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4647AE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 20% 50%, rgba(250,185,91,0.18) 0%, transparent 50%), linear-gradient(135deg, rgba(70,71,174,1) 0%, rgba(50,51,130,1) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 800, height: 800,
        border: '3px solid rgba(250,185,91,0.06)',
        borderRadius: '50%',
        top: '50%', left: '50%',
        transform: `translate(-50%,-50%) rotate(${frame * 0.1}deg) scale(${1 + Math.sin(frame * 0.03) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', width: 400, height: 400,
        background: 'rgba(232,237,242,0.04)',
        borderRadius: 70,
        top: 80, right: 80,
        transform: `rotate(${frame * 0.25}deg)`,
      }} />

      {/* Triangle pattern texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 12 }).map((_, i) =>
          Array.from({ length: 7 }).map((_, j) => (
            <polygon key={`${i}-${j}`}
              points={`${i * 170 + 30},${j * 170 + 60} ${i * 170 + 60},${j * 170 + 10} ${i * 170 + 90},${j * 170 + 60}`}
              fill="none" stroke="#E8EDF2" strokeWidth={1} />
          ))
        )}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const rx = seededRandom(i + 71) * 1920;
        const ry = seededRandom(i + 77) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 22;
        return (
          <div key={i} style={{
            position: 'absolute', left: rx, top: ry + drift,
            width: 4, height: 4, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.35,
          }} />
        );
      })}

      {/* Big 03 */}
      <div style={{
        position: 'absolute', top: 220, left: 100,
        opacity: numOp,
        transform: `translateX(${numX}px) rotate(${numRot}deg)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 520, fontWeight: 900,
          color: 'transparent',
          WebkitTextStroke: '6px #FAB95B',
          lineHeight: 0.9, letterSpacing: -20,
        }}>
          03
        </div>
      </div>

      {/* Right side content */}
      <div style={{
        position: 'absolute', top: 260, left: 720, right: 100,
      }}>
        <div style={{
          opacity: labelOp, transform: `translateY(${labelY}px)`,
          fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 10, marginBottom: 18,
        }}>
          OVERCHARGE NUMBER THREE
        </div>

        <div style={{
          opacity: titleOp, transform: `translateY(${titleY}px)`,
          fontFamily: 'Inter, Poppins', fontSize: 130, fontWeight: 900,
          color: '#E8EDF2', lineHeight: 0.95, letterSpacing: -3,
        }}>
          PLAN N
        </div>
        <div style={{
          opacity: titleOp, transform: `translateY(${titleY}px)`,
          fontFamily: 'Inter, Poppins', fontSize: 130, fontWeight: 900,
          color: '#FAB95B', lineHeight: 0.95, letterSpacing: -3,
          fontStyle: 'italic',
        }}>
          COPAY ILLUSION
        </div>

        {/* Bottom subtitle */}
        <div style={{
          marginTop: 50,
          opacity: subOp, transform: `translateY(${subY}px)`,
          fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 600,
          color: '#E8EDF2', lineHeight: 1.3, maxWidth: 950,
        }}>
          A trap that catches people who <span style={{ color: '#FAB95B', fontWeight: 800 }}>think</span> they're saving money
        </div>
      </div>

      {/* Illusion mirror visual - bottom left */}
      <div style={{
        position: 'absolute', bottom: 100, left: 280,
        opacity: illuOp, transform: `scale(${illuScale})`,
      }}>
        <svg width="220" height="220" viewBox="0 0 220 220"
          style={{ transform: `rotate(${illuRot}deg)` }}>
          <defs>
            <linearGradient id="illuGrad" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" stopColor="#FAB95B" stopOpacity="0.9" />
              <stop offset="100%" stopColor="#E8EDF2" stopOpacity="0.3" />
            </linearGradient>
          </defs>
          <circle cx="110" cy="110" r="100" fill="none" stroke="url(#illuGrad)" strokeWidth="4" strokeDasharray="20 10" />
          <circle cx="110" cy="110" r="70" fill="none" stroke="#FAB95B" strokeWidth="3" opacity="0.6" />
          <circle cx="110" cy="110" r="40" fill="rgba(250,185,91,0.2)" stroke="#FAB95B" strokeWidth="2" />
        </svg>
        <div style={{
          position: 'absolute', top: '50%', left: '50%',
          transform: 'translate(-50%, -50%)',
          fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 800,
          color: '#FAB95B', letterSpacing: 3,
        }}>
          ILLUSION
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.5) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene38_OverchargeThreeIntro;
```

```tsx
import React from 'react';
Audio: "Plan N requires a $20 copay for office visits and a $50 copay for ER visits that don't result in admission."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39_PlanNCopayBreakdown',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene39_PlanNCopayBreakdown: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Header
  const headOp = interpolate(sp(5), [0, 1], [0, 1]);
  const headY = interpolate(sp(5), [0, 1], [-30, 0]);

  // Receipt card 1
  const r1Op = interpolate(sp(35), [0, 1], [0, 1]);
  const r1Y = interpolate(sp(35), [0, 1], [80, 0]);
  const r1Rot = interpolate(sp(35), [0, 1], [-8, -3]);

  // Receipt card 2
  const r2Op = interpolate(sp(85), [0, 1], [0, 1]);
  const r2Y = interpolate(sp(85), [0, 1], [80, 0]);
  const r2Rot = interpolate(sp(85), [0, 1], [8, 3]);

  // Asterisk note
  const noteOp = interpolate(sp(150), [0, 1], [0, 1]);
  const noteY = interpolate(sp(150), [0, 1], [30, 0]);

  // Stamp on ER
  const stampScale = interpolate(sp(130, { damping: 7, stiffness: 180 }), [0, 1], [2, 1]);
  const stampOp = interpolate(sp(130), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4647AE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 40%, rgba(250,185,91,0.14) 0%, transparent 55%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 500, height: 500,
        border: '2px solid rgba(232,237,242,0.06)',
        borderRadius: '50%',
        top: -150, left: -100,
        transform: `rotate(${frame * 0.18}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 380, height: 380,
        background: 'rgba(250,185,91,0.05)',
        borderRadius: 60,
        bottom: -130, right: -100,
        transform: `rotate(${-frame * 0.22}deg)`,
      }} />

      {/* Subtle wave lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 10 }).map((_, i) => (
          <path key={i}
            d={`M0 ${100 + i * 100} Q480 ${80 + i * 100} 960 ${100 + i * 100} T1920 ${100 + i * 100}`}
            fill="none" stroke="#E8EDF2" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const rx = seededRandom(i + 81) * 1920;
        const ry = seededRandom(i + 87) * 1080;
        const drift = Math.cos(frame * 0.022 + i) * 18;
        return (
          <div key={i} style={{
            position: 'absolute', left: rx + drift, top: ry,
            width: 4, height: 4, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.3,
          }} />
        );
      })}

      {/* Header */}
      <div style={{
        position: 'absolute', top: 90, left: 0, right: 0, textAlign: 'center',
        opacity: headOp, transform: `translateY(${headY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 8, marginBottom: 12,
        }}>
          PLAN N COPAY STRUCTURE
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 88, fontWeight: 900,
          color: '#E8EDF2', letterSpacing: -2,
        }}>
          THE FINE PRINT
        </div>
      </div>

      {/* Receipt card 1 — Office visit */}
      <div style={{
        position: 'absolute', top: 360, left: 220,
        width: 600, height: 480,
        background: '#E8EDF2',
        borderRadius: 18,
        padding: 36,
        opacity: r1Op,
        transform: `translateY(${r1Y}px) rotate(${r1Rot}deg)`,
        boxShadow: '0 20px 40px rgba(0,0,0,0.35)',
      }}>
        {/* Icon */}
        <svg width="80" height="80" viewBox="0 0 24 24" style={{ marginBottom: 14 }}>
          <rect x="4" y="6" width="16" height="14" fill="none" stroke="#4647AE" strokeWidth="2" rx="2" />
          <circle cx="12" cy="11" r="2.5" fill="none" stroke="#4647AE" strokeWidth="2" />
          <path d="M8 17 C8 14 16 14 16 17" fill="none" stroke="#4647AE" strokeWidth="2" />
          <path d="M9 6 L9 4 L15 4 L15 6" fill="none" stroke="#4647AE" strokeWidth="2" />
        </svg>

        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 700,
          color: '#4647AE', letterSpacing: 4, marginBottom: 6,
        }}>
          SERVICE
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 50, fontWeight: 900,
          color: '#4647AE', lineHeight: 1, marginBottom: 30,
        }}>
          OFFICE VISIT
        </div>

        {/* Dashed line */}
        <div style={{
          borderTop: '3px dashed #4647AE', margin: '0 0 30px 0',
        }} />

        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 600,
          color: '#4647AE', opacity: 0.7, letterSpacing: 3, marginBottom: 8,
        }}>
          YOUR COPAY
        </div>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 140, fontWeight: 900,
          color: '#FAB95B', lineHeight: 1, letterSpacing: -4,
          textShadow: '0 4px 0 #4647AE',
        }}>
          $20
        </div>
      </div>

      {/* Receipt card 2 — ER visit */}
      <div style={{
        position: 'absolute', top: 360, right: 220,
        width: 600, height: 480,
        background: '#E8EDF2',
        borderRadius: 18,
        padding: 36,
        opacity: r2Op,
        transform: `translateY(${r2Y}px) rotate(${r2Rot}deg)`,
        boxShadow: '0 20px 40px rgba(0,0,0,0.35)',
      }}>
        {/* Icon */}
        <svg width="80" height="80" viewBox="0 0 24 24" style={{ marginBottom: 14 }}>
          <path d="M3 12 L6 12 L8 8 L11 16 L13 10 L15 13 L18 13 L21 13"
            fill="none" stroke="#4647AE" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
          <rect x="2" y="4" width="20" height="16" fill="none" stroke="#4647AE" strokeWidth="2" rx="2" />
        </svg>

        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 700,
          color: '#4647AE', letterSpacing: 4, marginBottom: 6,
        }}>
          SERVICE
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 50, fontWeight: 900,
          color: '#4647AE', lineHeight: 1, marginBottom: 30,
        }}>
          ER VISIT
        </div>

        {/* Dashed line */}
        <div style={{
          borderTop: '3px dashed #4647AE', margin: '0 0 30px 0',
        }} />

        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 600,
          color: '#4647AE', opacity: 0.7, letterSpacing: 3, marginBottom: 8,
        }}>
          YOUR COPAY
        </div>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 140, fontWeight: 900,
          color: '#FAB95B', lineHeight: 1, letterSpacing: -4,
          textShadow: '0 4px 0 #4647AE',
        }}>
          $50
        </div>

        {/* Stamp */}
        <div style={{
          position: 'absolute', bottom: 30, right: 30,
          opacity: stampOp, transform: `scale(${stampScale}) rotate(-15deg)`,
        }}>
          <div style={{
            padding: '6px 14px',
            border: '3px solid #4647AE',
            fontFamily: 'Inter, Poppins', fontSize: 16, fontWeight: 900,
            color: '#4647AE', letterSpacing: 2,
          }}>
            NO ADMISSION
          </div>
        </div>
      </div>

      {/* Bottom asterisk note */}
      <div style={{
        position: 'absolute', bottom: 90, left: 0, right: 0, textAlign: 'center',
        opacity: noteOp, transform: `translateY(${noteY}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          padding: '14px 30px',
          background: 'rgba(250,185,91,0.18)',
          border: '2px solid #FAB95B',
          borderRadius: 12,
          fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 700,
          color: '#E8EDF2',
        }}>
          <span style={{ color: '#FAB95B' }}>* ER copay waived</span> if you get admitted to the hospital
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene39_PlanNCopayBreakdown;
```

```tsx
import React from 'react';
Audio: "A lot of seniors sign up for Plan N then refuse to go to the doctor because they don't want to pay that $20 copay."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40_AvoidanceBehavior',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene40_AvoidanceBehavior: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Top headline
  const headOp = interpolate(sp(5), [0, 1], [0, 1]);
  const headY = interpolate(sp(5), [0, 1], [-30, 0]);

  // Step 1 - signs up
  const s1Op = interpolate(sp(30), [0, 1], [0, 1]);
  const s1Scale = interpolate(sp(30), [0, 1], [0.85, 1]);

  // Arrow 1
  const a1Op = interpolate(sp(65), [0, 1], [0, 1]);

  // Step 2 - sees copay
  const s2Op = interpolate(sp(80), [0, 1], [0, 1]);
  const s2Scale = interpolate(sp(80), [0, 1], [0.85, 1]);

  // Arrow 2
  const a2Op = interpolate(sp(115), [0, 1], [0, 1]);

  // Step 3 - avoids doctor
  const s3Op = interpolate(sp(130), [0, 1], [0, 1]);
  const s3Scale = interpolate(sp(130), [0, 1], [0.85, 1]);

  // Strikethrough on hospital
  const strikeW = interpolate(sp(160), [0, 1], [0, 280]);

  // Bottom takeaway
  const takeOp = interpolate(sp(190), [0, 1], [0, 1]);
  const takeY = interpolate(sp(190), [0, 1], [30, 0]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4647AE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(70,71,174,1) 0%, rgba(56,57,140,1) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', width: 600, height: 600,
        border: '2px solid rgba(250,185,91,0.07)',
        borderRadius: '50%',
        top: '50%', left: '50%',
        transform: `translate(-50%,-50%) rotate(${frame * 0.12}deg)`,
      }} />
      <div style={{
        position: 'absolute', width: 280, height: 280,
        background: 'rgba(232,237,242,0.05)',
        borderRadius: 50,
        top: 100, left: 80,
        transform: `rotate(${frame * 0.25}deg)`,
      }} />

      {/* Subtle dot grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 25 }).map((_, x) =>
          Array.from({ length: 14 }).map((_, y) => (
            <circle key={`${x}-${y}`} cx={x * 80 + 40} cy={y * 80 + 40} r={1.5} fill="#E8EDF2" />
          ))
        )}
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const rx = seededRandom(i + 91) * 1920;
        const ry = seededRandom(i + 97) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 18;
        return (
          <div key={i} style={{
            position: 'absolute', left: rx, top: ry + drift,
            width: 4, height: 4, borderRadius: '50%',
            background: '#FAB95B', opacity: 0.3,
          }} />
        );
      })}

      {/* Headline */}
      <div style={{
        position: 'absolute', top: 90, left: 0, right: 0, textAlign: 'center',
        opacity: headOp, transform: `translateY(${headY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 700,
          color: '#FAB95B', letterSpacing: 8, marginBottom: 12,
        }}>
          THE SELF-SABOTAGE LOOP
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 88, fontWeight: 900,
          color: '#E8EDF2', letterSpacing: -2,
        }}>
          PAYING TO AVOID CARE
        </div>
      </div>

      {/* 3-step flow */}
      <div style={{
        position: 'absolute', top: 340, left: 0, right: 0,
        display: 'flex', justifyContent: 'center', alignItems: 'center', gap: 30,
      }}>
        {/* STEP 1 — Signed up */}
        <div style={{
          width: 360, height: 380,
          background: 'rgba(232,237,242,0.1)',
          border: '3px solid #E8EDF2',
          borderRadius: 24, padding: 24,
          opacity: s1Op, transform: `scale(${s1Scale})`,
          display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
        }}>
          <div style={{
            position: 'absolute', top: 18, left: 18,
            width: 44, height: 44, borderRadius: '50%',
            background: '#FAB95B',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: 'Inter', fontSize: 24, fontWeight: 900,
            color: '#4647AE',
          }}>1</div>

          <svg width="110" height="110" viewBox="0 0 24 24">
            <path d="M14 2 L20 8 L20 22 L4 22 L4 2 Z" fill="none" stroke="#E8EDF2" strokeWidth="2" />
            <path d="M14 2 L14 8 L20 8" fill="none" stroke="#E8EDF2" strokeWidth="2" />
            <path d="M8 13 L16 13 M8 17 L13 17" stroke="#E8EDF2" strokeWidth="2" strokeLinecap="round" />
          </svg>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 38, fontWeight: 900,
            color: '#E8EDF2', marginTop: 20, textAlign: 'center', lineHeight: 1.1,
          }}>SIGN UP FOR<br/>PLAN N</div>
        </div>

        {/* Arrow 1 */}
        <div style={{ opacity: a1Op }}>
          <svg width="80" height="40" viewBox="0 0 80 40">
            <path d="M0 20 L60 20 M48 8 L60 20 L48 32"
              stroke="#FAB95B" strokeWidth="5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>

        {/* STEP 2 — Sees copay */}
        <div style={{
          width: 360, height: 380,
          background: 'rgba(250,185,91,0.15)',
          border: '3px solid #FAB95B',
          borderRadius: 24, padding: 24,
          opacity: s2Op, transform: `scale(${s2Scale})`,
          display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
          position: 'relative',
        }}>
          <div style={{
            position: 'absolute', top: 18, left: 18,
            width: 44, height: 44, borderRadius: '50%',
            background: '#FAB95B',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: 'Inter', fontSize: 24, fontWeight: 900,
            color: '#4647AE',
          }}>2</div>

          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 110, fontWeight: 900,
            color: '#FAB95B', lineHeight: 1, letterSpacing: -3,
          }}>$20</div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 800,
            color: '#E8EDF2', marginTop: 20, textAlign: 'center', lineHeight: 1.2,
          }}>"I DON'T WANT<br/>TO PAY THAT"</div>
        </div>

        {/* Arrow 2 */}
        <div style={{ opacity: a2Op }}>
          <svg width="80" height="40" viewBox="0 0 80 40">
            <path d="M0 20 L60 20 M48 8 L60 20 L48 32"
              stroke="#FAB95B" strokeWidth="5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>

        {/* STEP 3 — Avoid doctor */}
        <div style={{
          width: 360, height: 380,
          background: 'rgba(232,237,242,0.08)',
          border: '3px solid #E8EDF2',
          borderRadius: 24, padding: 24,
          opacity: s3Op, transform: `scale(${s3Scale})`,
          display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
          position: 'relative',
        }}>
          <div style={{
            position: 'absolute', top: 18, left: 18,
            width: 44, height: 44, borderRadius: '50%',
            background: '#FAB95B',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: 'Inter', fontSize: 24, fontWeight: 900,
            color: '#4647AE',
          }}>3</div>

          <div style={{ position: 'relative' }}>
            <svg width="110" height="110" viewBox="0 0 24 24">
              <rect x="4" y="4" width="16" height="16" fill="none" stroke="#E8EDF2" strokeWidth="2" rx="2" />
              <path d="M12 8 L12 14 M10 11 L14 11" stroke="#E8EDF2" strokeWidth="2.5" strokeLinecap="round" />
              <path d="M8 18 L16 18" stroke="#E8EDF2" strokeWidth="2" strokeLinecap="round" />
            </svg>
            {/* Strikethrough */}
            <div style={{
              position: 'absolute', top: '50%', left: -8,
              width: strikeW * 0.4, height: 6,
              background: '#FAB95B', borderRadius: 3,
              transform: 'translateY(-50%) rotate(-15deg)',
              transformOrigin: 'left center',
              boxShadow: '0 0 10px rgba(250,185,91,0.6)',
            }} />
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 36, fontWeight: 900,
            color: '#E8EDF2', marginTop: 20, textAlign: 'center', lineHeight: 1.1,
          }}>SKIP THE<br/>DOCTOR</div>
        </div>
      </div>

      {/* Bottom takeaway */}
      <div style={{
        position: 'absolute', bottom: 80, left: 0, right: 0, textAlign: 'center',
        opacity: takeOp, transform: `translateY(${takeY}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins', fontSize: 42, fontWeight: 700,
          color: '#E8EDF2',
        }}>
          Paying monthly premiums to <span style={{ color: '#FAB95B', fontWeight: 900 }}>avoid using the plan</span> = lose-lose
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.45) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene40_AvoidanceBehavior;
```

