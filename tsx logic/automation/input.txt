```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S01_UnknownAlert',
  durationInSeconds: 14.567,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S01_UnknownAlert: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow push-in + micro tilt
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.13], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });
  const camTilt = Math.sin(frame * 0.012) * 0.7;

  // Impact shake when the alert lands
  const shake = frame > 46 && frame < 64 ? Math.sin((frame - 46) * 2.3) * (1 - (frame - 46) / 18) * 12 : 0;

  const phoneIn = sp(8, { damping: 14 });
  const notifIn = sp(46, { damping: 10, stiffness: 140 });
  const ecgProg = interpolate(frame, [70, 190], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const chipIn = (i: number) => sp(96 + i * 20, { damping: 13 });
  const stampIn = sp(180, { damping: 9, stiffness: 90 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#9929EA', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, #A93BF5 0%, #9929EA 42%, #5D0F97 100%)' }} />
      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at ${30 + Math.sin(frame * 0.02) * 8}% 30%, rgba(245,245,245,0.20), rgba(0,0,0,0) 60%)`,
        }}
      />

      {/* Motion texture: halftone dot field */}
      <svg width={1920} height={1080} style={{ position: 'absolute', inset: 0, opacity: 0.13 }}>
        {new Array(18).fill(0).map((_, r) =>
          new Array(32).fill(0).map((_, c) => (
            <circle
              key={`${r}-${c}`}
              cx={c * 60 + 30}
              cy={r * 60 + 30}
              r={Math.max(0.6, 3 + Math.sin((r + c) * 0.55 + frame * 0.06) * 2.4)}
              fill="#F5F5F5"
            />
          ))
        )}
      </svg>

      {/* Animated shape layers (transparent geometric forms drifting) */}
      <div style={{ position: 'absolute', left: -180, top: 120, width: 720, height: 720, borderRadius: '50%', border: '3px solid #F5F5F5', opacity: 0.06, transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.03) * 0.05})` }} />
      <div style={{ position: 'absolute', right: -120, bottom: -160, width: 640, height: 640, background: '#000000', opacity: 0.07, transform: `rotate(${18 + Math.sin(frame * 0.02) * 5}deg)` }} />
      <div style={{ position: 'absolute', left: 1180, top: 60, width: 0, height: 0, borderLeft: '200px solid transparent', borderRight: '200px solid transparent', borderBottom: '340px solid #F5F5F5', opacity: 0.05, transform: `rotate(${-frame * 0.1}deg)` }} />

      {/* Punk collage: torn paper strips + tape */}
      <div style={{ position: 'absolute', left: -60, top: 780, width: 900, height: 120, background: '#000000', opacity: 0.85, transform: 'rotate(-3.5deg)' }} />
      <div style={{ position: 'absolute', left: 1240, top: 92, width: 420, height: 46, background: '#F5D000', transform: `rotate(${5 + Math.sin(frame * 0.05) * 0.6}deg)`, boxShadow: '0 8px 20px rgba(0,0,0,0.28)' }} />
      <div style={{ position: 'absolute', left: 96, top: 96, width: 300, height: 40, background: '#F5F5F5', opacity: 0.9, transform: 'rotate(-2deg)' }} />

      {/* Floating particles */}
      {new Array(26).fill(0).map((_, i) => {
        const a = seededRandom(i + 11);
        const b = seededRandom(i + 77);
        const size = 4 + a * 11;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: a * 1920,
              top: (b * 1080 + frame * (0.5 + a * 1.5)) % 1080,
              width: size,
              height: size,
              background: i % 4 === 0 ? '#F5D000' : '#F5F5F5',
              borderRadius: i % 3 === 0 ? 0 : '50%',
              opacity: 0.16 + a * 0.24,
              transform: `rotate(${frame * (0.6 + a)}deg)`,
            }}
          />
        );
      })}

      {/* Main content group with camera move */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camTilt}deg) translateX(${shake}px)` }}>
        {/* Ticker strip */}
        <div style={{ position: 'absolute', top: 100, left: 106, fontFamily: 'JetBrains Mono', fontSize: 26, letterSpacing: 4, color: '#000000', fontWeight: 700 }}>
          UPI / PRIVACY / ALERT
        </div>

        {/* Ripple rings behind the phone */}
        {new Array(3).fill(0).map((_, i) => {
          const t = ((frame - 46 - i * 22) % 90) / 90;
          const on = frame > 46 + i * 22;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: 560,
                top: 540,
                width: 40 + t * 620,
                height: 40 + t * 620,
                marginLeft: -(20 + t * 310),
                marginTop: -(20 + t * 310),
                borderRadius: '50%',
                border: '4px solid #F5D000',
                opacity: on ? Math.max(0, 0.5 - t * 0.5) : 0,
              }}
            />
          );
        })}

        {/* Phone device card */}
        <div
          style={{
            position: 'absolute',
            left: 360,
            top: 250,
            width: 400,
            height: 640,
            background: '#000000',
            borderRadius: 54,
            border: '10px solid #F5F5F5',
            boxShadow: '26px 26px 0 rgba(0,0,0,0.35)',
            transform: `translateY(${(1 - phoneIn) * 220}px) rotate(${-4 + (1 - phoneIn) * 8}deg)`,
            opacity: phoneIn,
            overflow: 'hidden',
          }}
        >
          <div style={{ position: 'absolute', top: 22, left: 150, width: 100, height: 14, borderRadius: 8, background: '#3A3A3A' }} />
          {/* Screen glow */}
          <div style={{ position: 'absolute', inset: 44, borderRadius: 30, background: 'linear-gradient(180deg, rgba(153,41,234,0.55), rgba(0,0,0,0.9))' }} />

          {/* Notification card sliding in */}
          <div
            style={{
              position: 'absolute',
              left: 34,
              top: 150,
              width: 330,
              background: '#F5F5F5',
              borderRadius: 22,
              padding: '22px 20px',
              border: '5px solid #000000',
              transform: `translateY(${(1 - notifIn) * -260}px) rotate(${(1 - notifIn) * -6}deg)`,
              opacity: notifIn,
              boxShadow: '0 18px 40px rgba(0,0,0,0.45)',
            }}
          >
            <div style={{ display: 'flex', alignItems: 'center', gap: 14 }}>
              <svg width={44} height={44} viewBox="0 0 24 24" style={{ transform: `rotate(${Math.sin(frame * 0.5) * 8}deg)` }}>
                <path d="M12 22a2.5 2.5 0 0 0 2.5-2.5h-5A2.5 2.5 0 0 0 12 22Z" fill="#000000" />
                <path d="M6 17h12l-1.6-2.3V11a4.4 4.4 0 0 0-8.8 0v3.7L6 17Z" fill="#9929EA" stroke="#000000" strokeWidth={1.6} />
              </svg>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 25, fontWeight: 700, color: '#000000', letterSpacing: 1 }}>
                +91 9•••• ••82
              </div>
            </div>
            <div style={{ marginTop: 16, height: 5, background: '#000000', opacity: 0.15 }} />
            <div style={{ marginTop: 16, fontFamily: 'Noto Sans Devanagari', fontSize: 30, fontWeight: 700, color: '#000000', lineHeight: 1.25 }}>
              खाते से पैसे कट गए?
            </div>
          </div>
        </div>

        {/* ECG / heartbeat reaction line */}
        <svg width={1000} height={220} viewBox="0 0 1000 220" style={{ position: 'absolute', left: 820, top: 300, opacity: interpolate(frame, [66, 84], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }) }}>
          <path
            d="M0 140 L200 140 L240 140 L268 40 L296 200 L326 110 L360 140 L560 140 L600 140 L628 60 L654 190 L684 140 L1000 140"
            fill="none"
            stroke="#F5D000"
            strokeWidth={9}
            strokeLinecap="round"
            pathLength={1}
            strokeDasharray={1}
            strokeDashoffset={1 - ecgProg}
          />
        </svg>

        {/* Icon chips: what actually happens in that second */}
        {[
          { label: 'अंजान नंबर', icon: 'q' },
          { label: 'तुरंत डर', icon: 'w' },
          { label: 'पैसा सुरक्षित?', icon: 'l' },
        ].map((c, i) => (
          <div
            key={c.label}
            style={{
              position: 'absolute',
              left: 860 + i * 12,
              top: 500 + i * 116,
              display: 'flex',
              alignItems: 'center',
              gap: 22,
              padding: '18px 30px',
              background: i === 1 ? '#000000' : '#F5F5F5',
              border: '5px solid #000000',
              transform: `translateX(${(1 - chipIn(i)) * 160}px) rotate(${i % 2 === 0 ? -1.4 : 1.6}deg)`,
              opacity: chipIn(i),
              boxShadow: '14px 14px 0 rgba(0,0,0,0.3)',
            }}
          >
            <svg width={48} height={48} viewBox="0 0 24 24">
              {c.icon === 'q' && (
                <>
                  <circle cx={12} cy={12} r={10} fill="none" stroke={i === 1 ? '#F5D000' : '#9929EA'} strokeWidth={2.4} />
                  <path d="M9 9a3 3 0 1 1 4.6 2.5c-.9.6-1.6 1.1-1.6 2.2" fill="none" stroke={i === 1 ? '#F5F5F5' : '#000000'} strokeWidth={2.2} strokeLinecap="round" />
                  <circle cx={12} cy={17} r={1.4} fill={i === 1 ? '#F5F5F5' : '#000000'} />
                </>
              )}
              {c.icon === 'w' && (
                <>
                  <path d="M12 3 22.5 21H1.5L12 3Z" fill="#F5D000" stroke="#F5F5F5" strokeWidth={1.6} />
                  <rect x={10.8} y={9} width={2.4} height={6} fill="#000000" />
                  <circle cx={12} cy={17.6} r={1.4} fill="#000000" />
                </>
              )}
              {c.icon === 'l' && (
                <>
                  <rect x={4} y={10.5} width={16} height={11} rx={2.4} fill="#9929EA" stroke="#000000" strokeWidth={1.8} />
                  <path d="M8 10.5V8a4 4 0 0 1 8 0v2.5" fill="none" stroke="#000000" strokeWidth={2.2} />
                </>
              )}
            </svg>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 44, fontWeight: 800, color: i === 1 ? '#F5F5F5' : '#000000' }}>{c.label}</div>
          </div>
        ))}

        {/* Stamp payoff */}
        <div
          style={{
            position: 'absolute',
            left: 1240,
            top: 862,
            padding: '12px 26px',
            border: '6px solid #F5D000',
            color: '#F5D000',
            fontFamily: 'Inter',
            fontSize: 34,
            fontWeight: 800,
            letterSpacing: 6,
            transform: `rotate(-7deg) scale(${0.7 + stampIn * 0.3})`,
            opacity: stampIn,
          }}
        >
          THE FEELING
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 42%, rgba(0,0,0,0.55) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S01_UnknownAlert;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S02_QrScan',
  durationInSeconds: 14.567,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S02_QrScan: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: pan left to right with easing
  const camX = interpolate(frame, [0, durationInFrames], [60, -60], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease),
  });
  const camScale = interpolate(frame, [0, durationInFrames], [1.12, 1.03], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const qrIn = sp(10, { damping: 14 });
  const qrCells = interpolate(frame, [16, 76], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const scanY = interpolate(frame % 96, [0, 96], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const vendorIn = (i: number) => sp(90 + i * 18, { damping: 13, stiffness: 130 });
  const flowProg = interpolate(frame, [150, 230], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const hitShake = frame > 150 && frame < 164 ? Math.sin((frame - 150) * 2.6) * 8 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9929EA', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(200deg, #6D14AE 0%, #9929EA 55%, #B457F7 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(circle at 70% ${60 + Math.sin(frame * 0.025) * 10}%, rgba(245,208,0,0.18), rgba(0,0,0,0) 55%)` }} />

      {/* Motion texture: diagonal stripes */}
      <div
        style={{
          position: 'absolute',
          inset: -200,
          opacity: 0.1,
          background: 'repeating-linear-gradient(45deg, #F5F5F5 0 10px, rgba(0,0,0,0) 10px 44px)',
          transform: `translateX(${(frame * 0.6) % 44}px)`,
        }}
      />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', left: 120, bottom: -220, width: 700, height: 700, background: '#F5F5F5', opacity: 0.05, borderRadius: 90, transform: `rotate(${25 + Math.sin(frame * 0.02) * 6}deg)` }} />
      <div style={{ position: 'absolute', right: 140, top: -180, width: 520, height: 520, borderRadius: '50%', background: '#000000', opacity: 0.08, transform: `scale(${1 + Math.sin(frame * 0.035) * 0.06})` }} />

      {/* Punk collage blocks */}
      <div style={{ position: 'absolute', left: 0, top: 0, width: 260, height: 1080, background: '#000000', opacity: 0.9, transform: 'skewX(-3deg) translateX(-70px)' }} />
      <div style={{ position: 'absolute', left: 84, top: 640, width: 380, height: 44, background: '#F5D000', transform: 'rotate(-90deg)', transformOrigin: 'left top', opacity: 0.95 }} />

      {/* Floating particles */}
      {new Array(24).fill(0).map((_, i) => {
        const a = seededRandom(i + 5);
        const b = seededRandom(i + 41);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: (a * 1920 + Math.sin(frame * 0.02 + i) * 30) % 1920,
              top: (b * 1080 + frame * (0.4 + a * 1.2)) % 1080,
              width: 6 + a * 8,
              height: 6 + a * 8,
              background: i % 5 === 0 ? '#000000' : '#F5F5F5',
              opacity: 0.18 + b * 0.2,
              transform: `rotate(${45 + frame * a}deg)`,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX + hitShake}px) scale(${camScale})` }}>
        {/* QR card being built cell by cell */}
        <div
          style={{
            position: 'absolute',
            left: 260,
            top: 260,
            width: 520,
            height: 520,
            background: '#F5F5F5',
            border: '10px solid #000000',
            boxShadow: '24px 24px 0 rgba(0,0,0,0.35)',
            transform: `scale(${0.7 + qrIn * 0.3}) rotate(${-3 + (1 - qrIn) * 6}deg)`,
            opacity: qrIn,
            overflow: 'hidden',
          }}
        >
          <svg width={500} height={500} viewBox="0 0 20 20">
            {new Array(20).fill(0).map((_, r) =>
              new Array(20).fill(0).map((_, c) => {
                const v = seededRandom(r * 31 + c * 7 + 3);
                const order = (r * 20 + c) / 400;
                const isFinder =
                  (r < 6 && c < 6) || (r < 6 && c > 13) || (r > 13 && c < 6);
                if (isFinder) return null;
                if (v < 0.45 || order > qrCells) return null;
                return <rect key={`${r}-${c}`} x={c} y={r} width={1} height={1} fill="#000000" />;
              })
            )}
            {/* Finder patterns */}
            {[
              { x: 0, y: 0 },
              { x: 14, y: 0 },
              { x: 0, y: 14 },
            ].map((p) => (
              <g key={`${p.x}-${p.y}`} opacity={qrCells > 0.05 ? 1 : 0}>
                <rect x={p.x} y={p.y} width={6} height={6} fill="#000000" />
                <rect x={p.x + 1} y={p.y + 1} width={4} height={4} fill="#F5F5F5" />
                <rect x={p.x + 2} y={p.y + 2} width={2} height={2} fill="#9929EA" />
              </g>
            ))}
          </svg>

          {/* Scan laser */}
          <div
            style={{
              position: 'absolute',
              left: 0,
              top: scanY * 500,
              width: 500,
              height: 8,
              background: '#9929EA',
              boxShadow: '0 0 30px 10px rgba(153,41,234,0.6)',
            }}
          />
        </div>

        {/* Vendor icon cards */}
        {[
          { name: 'चाय वाला', k: 'cup' },
          { name: 'सब्ज़ी वाला', k: 'basket' },
          { name: 'कैब ड्राइवर', k: 'cab' },
        ].map((v, i) => (
          <div
            key={v.name}
            style={{
              position: 'absolute',
              left: 920,
              top: 210 + i * 200,
              width: 620,
              display: 'flex',
              alignItems: 'center',
              gap: 26,
              padding: '22px 28px',
              background: i === 1 ? '#F5D000' : '#F5F5F5',
              border: '6px solid #000000',
              transform: `translateY(${(1 - vendorIn(i)) * 90}px) rotate(${i % 2 ? 1.5 : -1.5}deg)`,
              opacity: vendorIn(i),
              boxShadow: '16px 16px 0 rgba(0,0,0,0.3)',
            }}
          >
            <svg width={78} height={78} viewBox="0 0 24 24" style={{ transform: `translateY(${Math.sin(frame * 0.08 + i) * 3}px)` }}>
              {v.k === 'cup' && (
                <>
                  <path d="M4 8h12v7a4 4 0 0 1-4 4H8a4 4 0 0 1-4-4V8Z" fill="#9929EA" stroke="#000000" strokeWidth={1.6} />
                  <path d="M16 10h2.5a2.5 2.5 0 0 1 0 5H16" fill="none" stroke="#000000" strokeWidth={1.8} />
                  <path d="M8 5c0-1 1-1.6 1-2.6M11.5 5c0-1 1-1.6 1-2.6" stroke="#000000" strokeWidth={1.6} fill="none" />
                </>
              )}
              {v.k === 'basket' && (
                <>
                  <path d="M3 9h18l-2.4 11H5.4L3 9Z" fill="#9929EA" stroke="#000000" strokeWidth={1.6} />
                  <path d="M8 9 10 3M16 9 14 3" stroke="#000000" strokeWidth={1.8} fill="none" />
                  <path d="M6 13h12" stroke="#000000" strokeWidth={1.4} />
                </>
              )}
              {v.k === 'cab' && (
                <>
                  <path d="M3 15l1.8-5.4A2 2 0 0 1 6.7 8h10.6a2 2 0 0 1 1.9 1.6L21 15v3H3v-3Z" fill="#9929EA" stroke="#000000" strokeWidth={1.6} />
                  <rect x={9} y={4.6} width={6} height={3} fill="#F5D000" stroke="#000000" strokeWidth={1.4} />
                  <circle cx={7} cy={18.4} r={2.1} fill="#000000" />
                  <circle cx={17} cy={18.4} r={2.1} fill="#000000" />
                </>
              )}
            </svg>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 48, fontWeight: 800, color: '#000000' }}>{v.name}</div>
            <div style={{ marginLeft: 'auto', fontFamily: 'JetBrains Mono', fontSize: 30, fontWeight: 700, color: '#000000', opacity: 0.6 }}>QR</div>
          </div>
        ))}

        {/* Flow: money out, data out */}
        <svg width={1920} height={200} viewBox="0 0 1920 200" style={{ position: 'absolute', left: 0, top: 840 }}>
          <path d="M320 120 C620 20, 1120 200, 1600 90" fill="none" stroke="#F5F5F5" strokeWidth={7} strokeDasharray="18 14" opacity={0.55} />
          <path d="M320 120 C620 20, 1120 200, 1600 90" fill="none" stroke="#F5D000" strokeWidth={9} pathLength={1} strokeDasharray={1} strokeDashoffset={1 - flowProg} />
        </svg>
        <div
          style={{
            position: 'absolute',
            left: 300 + flowProg * 1240,
            top: 900 - Math.sin(flowProg * Math.PI) * 70,
            width: 92,
            height: 92,
            borderRadius: '50%',
            background: '#F5D000',
            border: '6px solid #000000',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            fontFamily: 'Inter',
            fontSize: 46,
            fontWeight: 800,
            color: '#000000',
            transform: `rotate(${flowProg * 360}deg)`,
            opacity: flowProg > 0 ? 1 : 0,
          }}
        >
          ₹
        </div>

        <div
          style={{
            position: 'absolute',
            left: 300,
            top: 830,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 40,
            fontWeight: 800,
            color: '#F5F5F5',
            opacity: sp(160),
          }}
        >
          पैसा + पहचान, दोनों
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 45%, rgba(0,0,0,0.5) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S02_QrScan;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S03_NumberExposed',
  durationInSeconds: 14.567,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S03_NumberExposed: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: orbit-style tilt with slow zoom out
  const camScale = interpolate(frame, [0, durationInFrames], [1.16, 1.02], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const camRot = Math.sin(frame * 0.016) * 1.2;

  const digits = ['9', '8', '2', '4', '1', '7', '6', '5', '3', '0'];
  const revealAt = 40;
  const unmask = interpolate(frame, [revealAt, revealAt + 45], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const eyeIn = sp(96, { damping: 11 });
  const leakIn = (i: number) => sp(120 + i * 16, { damping: 13 });
  const stampIn = sp(190, { damping: 8, stiffness: 80 });
  const shake = frame > revealAt && frame < revealAt + 14 ? Math.sin((frame - revealAt) * 3) * 9 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9929EA', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(20deg, #4E0B84 0%, #9929EA 60%, #C071FA 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(circle at 50% ${40 + Math.sin(frame * 0.03) * 8}%, rgba(0,0,0,0.35), rgba(0,0,0,0) 65%)` }} />

      {/* Motion texture: concentric rings */}
      <svg width={1920} height={1080} style={{ position: 'absolute', inset: 0, opacity: 0.12 }}>
        {new Array(12).fill(0).map((_, i) => (
          <circle
            key={i}
            cx={960}
            cy={520}
            r={90 + i * 78 + Math.sin(frame * 0.04 + i) * 10}
            fill="none"
            stroke="#F5F5F5"
            strokeWidth={2}
          />
        ))}
      </svg>

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', left: -240, top: 420, width: 760, height: 760, background: '#000000', opacity: 0.07, transform: `rotate(${-frame * 0.08}deg)` }} />
      <div style={{ position: 'absolute', right: -200, top: -120, width: 640, height: 640, border: '4px solid #F5F5F5', opacity: 0.06, borderRadius: 60, transform: `rotate(${frame * 0.12}deg)` }} />

      {/* Punk collage: torn black band + tape */}
      <div style={{ position: 'absolute', left: -40, top: 300, width: 2000, height: 260, background: '#000000', opacity: 0.9, transform: 'rotate(-2deg)' }} />
      <div style={{ position: 'absolute', left: 1520, top: 250, width: 300, height: 40, background: '#F5D000', transform: 'rotate(9deg)' }} />
      <div style={{ position: 'absolute', left: 70, top: 540, width: 260, height: 36, background: '#F5F5F5', opacity: 0.85, transform: 'rotate(-6deg)' }} />

      {/* Floating particles */}
      {new Array(28).fill(0).map((_, i) => {
        const a = seededRandom(i + 21);
        const b = seededRandom(i + 60);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: a * 1920,
              top: (b * 1080 - frame * (0.4 + a)) < 0 ? ((b * 1080 - frame * (0.4 + a)) % 1080) + 1080 : (b * 1080 - frame * (0.4 + a)) % 1080,
              width: 5 + a * 9,
              height: 5 + a * 9,
              background: i % 3 === 0 ? '#F5D000' : '#F5F5F5',
              borderRadius: '50%',
              opacity: 0.15 + b * 0.2,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg) translateX(${shake}px)` }}>
        {/* Number plate: masked -> exposed */}
        <div style={{ position: 'absolute', left: 300, top: 344, display: 'flex', gap: 16, alignItems: 'center' }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 84, fontWeight: 800, color: '#F5D000' }}>+91</div>
          {digits.map((d, i) => {
            const local = interpolate(unmask, [i / digits.length, (i + 1) / digits.length], [0, 1], {
              extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
            });
            const pop = sp(6 + i * 5, { damping: 14 });
            return (
              <div
                key={i}
                style={{
                  width: 92,
                  height: 118,
                  background: local > 0.5 ? '#F5F5F5' : '#2A2A2A',
                  border: '5px solid #F5F5F5',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontFamily: 'JetBrains Mono',
                  fontSize: 68,
                  fontWeight: 800,
                  color: local > 0.5 ? '#000000' : '#F5F5F5',
                  transform: `translateY(${(1 - pop) * 60}px) rotateX(${(1 - local) * 40}deg) scale(${1 + local * 0.04})`,
                  opacity: pop,
                  boxShadow: local > 0.5 ? '0 0 26px rgba(245,208,0,0.55)' : 'none',
                }}
              >
                {local > 0.5 ? d : '•'}
              </div>
            );
          })}
        </div>

        {/* Eye icon: someone is watching */}
        <div style={{ position: 'absolute', left: 250, top: 640, opacity: eyeIn, transform: `scale(${0.8 + eyeIn * 0.2})` }}>
          <svg width={190} height={130} viewBox="0 0 24 16">
            <path d="M1 8s4.4-6.4 11-6.4S23 8 23 8s-4.4 6.4-11 6.4S1 8 1 8Z" fill="#F5F5F5" stroke="#000000" strokeWidth={1.2} />
            <circle cx={12 + Math.sin(frame * 0.06) * 2.6} cy={8} r={4} fill="#9929EA" />
            <circle cx={12 + Math.sin(frame * 0.06) * 2.6} cy={8} r={1.7} fill="#000000" />
          </svg>
        </div>

        {/* Leak targets */}
        {[
          { t: 'स्पैम कॉल', k: 'call' },
          { t: 'फिशिंग लिंक', k: 'hook' },
          { t: 'डेटा प्रोफाइल', k: 'file' },
        ].map((l, i) => (
          <div
            key={l.t}
            style={{
              position: 'absolute',
              left: 1040,
              top: 636 + i * 132,
              display: 'flex',
              alignItems: 'center',
              gap: 22,
              padding: '16px 26px',
              background: '#000000',
              border: '5px solid #F5D000',
              transform: `translateX(${(1 - leakIn(i)) * 200}px) rotate(${i === 1 ? 1.8 : -1.2}deg)`,
              opacity: leakIn(i),
            }}
          >
            <svg width={54} height={54} viewBox="0 0 24 24">
              {l.k === 'call' && (
                <path d="M6 3h3l2 5-2.4 1.6a11 11 0 0 0 5.8 5.8L16 13l5 2v3a2 2 0 0 1-2.2 2C10.6 19.4 4.6 13.4 4 5.2A2 2 0 0 1 6 3Z" fill="#F5D000" />
              )}
              {l.k === 'hook' && (
                <>
                  <path d="M15 3v9a4.5 4.5 0 1 1-9 0" fill="none" stroke="#F5D000" strokeWidth={2.4} strokeLinecap="round" />
                  <circle cx={15} cy={3} r={2} fill="#F5F5F5" />
                </>
              )}
              {l.k === 'file' && (
                <>
                  <path d="M6 2h8l4 4v16H6V2Z" fill="#F5D000" />
                  <path d="M8.5 11h7M8.5 14.5h7M8.5 18h4.5" stroke="#000000" strokeWidth={1.6} />
                </>
              )}
            </svg>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 42, fontWeight: 800, color: '#F5F5F5' }}>{l.t}</div>
          </div>
        ))}

        {/* Leak arrows from number to targets */}
        <svg width={1920} height={1080} style={{ position: 'absolute', inset: 0 }}>
          {[0, 1, 2].map((i) => {
            const p = leakIn(i);
            return (
              <path
                key={i}
                d={`M700 ${700} C880 ${660 + i * 60}, 940 ${666 + i * 132}, 1030 ${666 + i * 132}`}
                fill="none"
                stroke="#F5F5F5"
                strokeWidth={5}
                opacity={0.5 * p}
                pathLength={1}
                strokeDasharray={1}
                strokeDashoffset={1 - p}
              />
            );
          })}
        </svg>

        {/* Stamp */}
        <div
          style={{
            position: 'absolute',
            left: 620,
            top: 900,
            padding: '14px 30px',
            background: '#F5D000',
            border: '6px solid #000000',
            fontFamily: 'Inter',
            fontSize: 40,
            fontWeight: 800,
            letterSpacing: 5,
            color: '#000000',
            transform: `rotate(-4deg) scale(${0.75 + stampIn * 0.25})`,
            opacity: stampIn,
          }}
        >
          IDENTITY EXPOSED
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 40%, rgba(0,0,0,0.58) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S03_NumberExposed;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S04_ChannelPillars',
  durationInSeconds: 14.567,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S04_ChannelPillars: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: tilt up reveal
  const camY = interpolate(frame, [0, durationInFrames], [50, -30], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1.1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const badgeIn = sp(6, { damping: 13 });
  const pillarIn = (i: number) => sp(48 + i * 22, { damping: 12, stiffness: 110 });
  const lineProg = interpolate(frame, [130, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const kickerIn = sp(210, { damping: 14 });

  const pillars = [
    { t: 'नीतियां', s: 'POLICY', k: 'doc' },
    { t: 'बाज़ार', s: 'MARKET', k: 'chart' },
    { t: 'मनोविज्ञान', s: 'PSYCHOLOGY', k: 'mind' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#9929EA', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, #B45CF9 0%, #9929EA 40%, #55107F 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(circle at ${50 + Math.sin(frame * 0.02) * 6}% 80%, rgba(245,245,245,0.16), rgba(0,0,0,0) 55%)` }} />

      {/* Motion texture: blueprint grid */}
      <svg width={1920} height={1080} style={{ position: 'absolute', inset: 0, opacity: 0.14 }}>
        {new Array(25).fill(0).map((_, i) => (
          <line key={`v${i}`} x1={i * 80 + ((frame * 0.4) % 80)} y1={0} x2={i * 80 + ((frame * 0.4) % 80)} y2={1080} stroke="#F5F5F5" strokeWidth={1.4} />
        ))}
        {new Array(15).fill(0).map((_, i) => (
          <line key={`h${i}`} x1={0} y1={i * 80} x2={1920} y2={i * 80} stroke="#F5F5F5" strokeWidth={1.4} />
        ))}
      </svg>

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', left: 700, top: -260, width: 620, height: 620, borderRadius: '50%', background: '#F5F5F5', opacity: 0.05, transform: `scale(${1 + Math.sin(frame * 0.03) * 0.07})` }} />
      <div style={{ position: 'absolute', left: -160, bottom: 60, width: 480, height: 480, background: '#000000', opacity: 0.07, transform: `rotate(${frame * 0.09}deg)` }} />

      {/* Punk collage bands */}
      <div style={{ position: 'absolute', left: 0, bottom: 0, width: 1920, height: 150, background: '#000000', opacity: 0.92, transform: 'rotate(1.2deg) translateY(40px)' }} />
      <div style={{ position: 'absolute', left: 1330, top: 130, width: 360, height: 42, background: '#F5D000', transform: 'rotate(-7deg)' }} />

      {/* Floating particles */}
      {new Array(22).fill(0).map((_, i) => {
        const a = seededRandom(i + 3);
        const b = seededRandom(i + 88);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: (a * 1920 + Math.cos(frame * 0.02 + i) * 24) % 1920,
              top: (b * 1080 + frame * (0.35 + a)) % 1080,
              width: 8 + a * 7,
              height: 2 + a * 3,
              background: '#F5F5F5',
              opacity: 0.2 + b * 0.2,
              transform: `rotate(${a * 180}deg)`,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px) scale(${camScale})` }}>
        {/* Channel badge card */}
        <div
          style={{
            position: 'absolute',
            left: 130,
            top: 130,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
            padding: '20px 34px',
            background: '#F5F5F5',
            border: '7px solid #000000',
            boxShadow: '18px 18px 0 rgba(0,0,0,0.32)',
            transform: `translateY(${(1 - badgeIn) * -120}px) rotate(-2deg)`,
            opacity: badgeIn,
          }}
        >
          <svg width={66} height={66} viewBox="0 0 24 24" style={{ transform: `rotate(${Math.sin(frame * 0.04) * 5}deg)` }}>
            <circle cx={12} cy={12} r={10.5} fill="#9929EA" stroke="#000000" strokeWidth={1.6} />
            <path d="M6.5 15 10 8l3 5 1.6-2.4L18 15" fill="none" stroke="#F5D000" strokeWidth={2.4} strokeLinecap="round" />
          </svg>
          <div style={{ fontFamily: 'Inter', fontSize: 56, fontWeight: 800, color: '#000000', letterSpacing: -1 }}>ProBuffet HQ</div>
        </div>

        {/* Three icon pillars */}
        {pillars.map((p, i) => {
          const v = pillarIn(i);
          return (
            <div
              key={p.s}
              style={{
                position: 'absolute',
                left: 190 + i * 540,
                top: 380,
                width: 440,
                height: 420,
                background: i === 1 ? '#000000' : '#F5F5F5',
                border: '7px solid #000000',
                boxShadow: '20px 20px 0 rgba(0,0,0,0.3)',
                transform: `translateY(${(1 - v) * 220}px) rotate(${i === 1 ? 0 : i === 0 ? -2.2 : 2.2}deg) scale(${0.92 + v * 0.08})`,
                opacity: v,
                padding: 34,
                boxSizing: 'border-box',
              }}
            >
              <div style={{ height: 190, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
                <svg width={168} height={168} viewBox="0 0 24 24" style={{ transform: `translateY(${Math.sin(frame * 0.06 + i * 2) * 5}px)` }}>
                  {p.k === 'doc' && (
                    <>
                      <path d="M6 2h8l4.5 4.5V22H6V2Z" fill={i === 1 ? '#F5D000' : '#9929EA'} stroke="#000000" strokeWidth={1.3} />
                      <path d="M8.5 11h7M8.5 14.5h7M8.5 18h4.5" stroke={i === 1 ? '#000000' : '#F5F5F5'} strokeWidth={1.6} />
                      <circle cx={17} cy={19} r={3.6} fill="#F5F5F5" stroke="#000000" strokeWidth={1.2} />
                      <path d="M15.4 19l1.2 1.2 2-2.4" stroke="#000000" strokeWidth={1.4} fill="none" />
                    </>
                  )}
                  {p.k === 'chart' && (
                    <>
                      <rect x={3} y={13} width={4} height={8} fill="#F5D000" stroke="#F5F5F5" strokeWidth={1} />
                      <rect x={10} y={8.5} width={4} height={12.5} fill="#F5D000" stroke="#F5F5F5" strokeWidth={1} />
                      <rect x={17} y={4} width={4} height={17} fill="#F5D000" stroke="#F5F5F5" strokeWidth={1} />
                      <path d="M3 11 8 6l4 3 8-5" fill="none" stroke="#F5F5F5" strokeWidth={1.8} />
                    </>
                  )}
                  {p.k === 'mind' && (
                    <>
                      <path d="M15 3a6 6 0 0 1 3.6 10.8V21H9.5v-3H8a5 5 0 0 1-.4-10A6 6 0 0 1 15 3Z" fill="#9929EA" stroke="#000000" strokeWidth={1.3} />
                      <path d="M11.5 7.5c-1.6.6-2 2.6-.6 3.4-1.5.9-1.2 3 .8 3.3" fill="none" stroke="#F5D000" strokeWidth={1.6} strokeLinecap="round" />
                    </>
                  )}
                </svg>
              </div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700, letterSpacing: 5, color: i === 1 ? '#F5D000' : '#9929EA' }}>{p.s}</div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 62, fontWeight: 800, color: i === 1 ? '#F5F5F5' : '#000000', marginTop: 6 }}>{p.t}</div>
            </div>
          );
        })}

        {/* Connector line: pillars -> your pocket */}
        <svg width={1920} height={1080} style={{ position: 'absolute', inset: 0 }}>
          <path
            d="M410 810 L410 890 L1500 890 L1500 810"
            fill="none"
            stroke="#F5D000"
            strokeWidth={7}
            pathLength={1}
            strokeDasharray={1}
            strokeDashoffset={1 - lineProg}
          />
          <path
            d="M950 890 L950 950"
            fill="none"
            stroke="#F5D000"
            strokeWidth={7}
            pathLength={1}
            strokeDasharray={1}
            strokeDashoffset={1 - Math.max(0, lineProg - 0.6) / 0.4}
          />
        </svg>

        <div
          style={{
            position: 'absolute',
            left: 700,
            top: 952,
            display: 'flex',
            alignItems: 'center',
            gap: 18,
            padding: '12px 28px',
            background: '#F5D000',
            border: '6px solid #000000',
            transform: `scale(${0.8 + kickerIn * 0.2})`,
            opacity: kickerIn,
          }}
        >
          <svg width={46} height={46} viewBox="0 0 24 24">
            <path d="M4 8h16v10a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V8Z" fill="#9929EA" stroke="#000000" strokeWidth={1.5} />
            <path d="M4 8l3-4h10l3 4" fill="none" stroke="#000000" strokeWidth={1.6} />
            <circle cx={16.5} cy={13.5} r={1.8} fill="#F5F5F5" />
          </svg>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 44, fontWeight: 800, color: '#000000' }}>सीधे आपकी जेब पर असर</div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 45%, rgba(0,0,0,0.5) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S04_ChannelPillars;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S05_NpciData',
  durationInSeconds: 14.567,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S05_NpciData: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: dolly-in on the chart
  const camScale = interpolate(frame, [0, durationInFrames], [1.0, 1.12], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease) });
  const camX = Math.sin(frame * 0.014) * 14;

  const bars = [0.34, 0.42, 0.5, 0.58, 0.66, 0.79, 0.97];
  const barIn = (i: number) => sp(24 + i * 12, { damping: 15, stiffness: 90 });
  const arrowProg = interpolate(frame, [140, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const cardIn = sp(196, { damping: 11 });
  const shake = frame > 196 && frame < 210 ? Math.sin((frame - 196) * 2.8) * 7 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9929EA', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(120deg, #5B0F97 0%, #9929EA 55%, #A93BF5 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(circle at 20% ${25 + Math.sin(frame * 0.02) * 6}%, rgba(245,208,0,0.20), rgba(0,0,0,0) 50%)` }} />

      {/* Motion texture: dot matrix columns */}
      <svg width={1920} height={1080} style={{ position: 'absolute', inset: 0, opacity: 0.15 }}>
        {new Array(24).fill(0).map((_, c) =>
          new Array(14).fill(0).map((_, r) => (
            <rect
              key={`${c}-${r}`}
              x={c * 82 + 20}
              y={r * 78 + 20}
              width={3.5}
              height={Math.max(2, 16 + Math.sin(frame * 0.05 + c * 0.4) * 12)}
              fill="#F5F5F5"
            />
          ))
        )}
      </svg>

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', right: -260, top: 200, width: 800, height: 800, borderRadius: '50%', border: '5px dashed #F5F5F5', opacity: 0.06, transform: `rotate(${frame * 0.1}deg)` }} />
      <div style={{ position: 'absolute', left: 240, top: -180, width: 520, height: 520, background: '#000000', opacity: 0.07, transform: `rotate(${20 + Math.sin(frame * 0.025) * 8}deg)` }} />

      {/* Punk collage */}
      <div style={{ position: 'absolute', left: 90, top: 110, width: 640, height: 108, background: '#000000', opacity: 0.94, transform: 'rotate(-1.6deg)' }} />
      <div style={{ position: 'absolute', left: 660, top: 96, width: 200, height: 38, background: '#F5D000', transform: 'rotate(11deg)' }} />

      {/* Floating particles */}
      {new Array(20).fill(0).map((_, i) => {
        const a = seededRandom(i + 13);
        const b = seededRandom(i + 51);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: a * 1920,
              top: (b * 1080 + frame * (0.6 + a)) % 1080,
              width: 10 + a * 8,
              height: 10 + a * 8,
              border: '3px solid #F5F5F5',
              opacity: 0.16 + b * 0.18,
              transform: `rotate(${frame * (0.5 + a)}deg)`,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX + shake}px)` }}>
        {/* Source header */}
        <div style={{ position: 'absolute', left: 120, top: 132, display: 'flex', alignItems: 'center', gap: 20, opacity: sp(4) }}>
          <svg width={56} height={56} viewBox="0 0 24 24">
            <rect x={3} y={4} width={18} height={16} rx={2.4} fill="#F5D000" stroke="#F5F5F5" strokeWidth={1.2} />
            <path d="M6 15l3.4-4 2.6 2.4L15 9l3 6" fill="none" stroke="#000000" strokeWidth={2} strokeLinecap="round" />
          </svg>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 34, fontWeight: 700, color: '#F5F5F5', letterSpacing: 3 }}>NPCI / जुलाई 2026</div>
        </div>

        {/* Bar chart card */}
        <div
          style={{
            position: 'absolute',
            left: 120,
            top: 290,
            width: 1080,
            height: 620,
            background: '#F5F5F5',
            border: '8px solid #000000',
            boxShadow: '22px 22px 0 rgba(0,0,0,0.3)',
            transform: `rotate(-1deg)`,
            overflow: 'hidden',
          }}
        >
          {/* Baseline grid */}
          {new Array(5).fill(0).map((_, i) => (
            <div key={i} style={{ position: 'absolute', left: 0, top: 90 + i * 100, width: 1080, height: 2, background: '#000000', opacity: 0.12 }} />
          ))}

          {bars.map((h, i) => {
            const v = barIn(i);
            const barH = h * 440 * v;
            const isPeak = i === bars.length - 1;
            return (
              <div key={i} style={{ position: 'absolute', left: 90 + i * 132, bottom: 88 }}>
                <div
                  style={{
                    width: 86,
                    height: barH,
                    background: isPeak ? '#F5D000' : '#9929EA',
                    border: '4px solid #000000',
                    boxShadow: isPeak ? '0 0 30px rgba(245,208,0,0.7)' : 'none',
                    transform: `translateY(${isPeak ? Math.sin(frame * 0.12) * 3 : 0}px)`,
                  }}
                />
              </div>
            );
          })}

          <div style={{ position: 'absolute', left: 0, bottom: 78, width: 1080, height: 6, background: '#000000' }} />
          <div style={{ position: 'absolute', left: 90, bottom: 22, fontFamily: 'JetBrains Mono', fontSize: 27, fontWeight: 700, color: '#000000', opacity: 0.7 }}>
            MONTHLY UPI VOLUME — RISING TREND
          </div>

          {/* Trend arrow */}
          <svg width={1080} height={620} style={{ position: 'absolute', inset: 0 }}>
            <path
              d="M130 480 C400 430, 700 330, 1010 140"
              fill="none"
              stroke="#000000"
              strokeWidth={8}
              pathLength={1}
              strokeDasharray={1}
              strokeDashoffset={1 - arrowProg}
            />
            {arrowProg > 0.9 && <path d="M1010 140 L960 168 L992 190 Z" fill="#000000" />}
          </svg>
        </div>

        {/* Insight card */}
        <div
          style={{
            position: 'absolute',
            left: 1280,
            top: 380,
            width: 520,
            background: '#000000',
            border: '8px solid #F5D000',
            padding: 34,
            boxSizing: 'border-box',
            transform: `translateX(${(1 - cardIn) * 240}px) rotate(2deg)`,
            opacity: cardIn,
          }}
        >
          <svg width={78} height={78} viewBox="0 0 24 24">
            <path d="M12 3 22.5 21H1.5L12 3Z" fill="#F5D000" />
            <rect x={10.9} y={9} width={2.2} height={6.4} fill="#000000" />
            <circle cx={12} cy={17.8} r={1.4} fill="#000000" />
          </svg>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 60, fontWeight: 800, color: '#F5F5F5', lineHeight: 1.15, marginTop: 16 }}>
            चौंकाने वाली तस्वीर
          </div>
          <div style={{ height: 5, background: '#F5D000', margin: '20px 0' }} />
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 38, fontWeight: 600, color: '#F5F5F5', opacity: 0.85, lineHeight: 1.35 }}>
            स्केल बढ़ा, जोखिम भी बढ़ा
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 44%, rgba(0,0,0,0.52) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S05_NpciData;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S06_Crossroads',
  durationInSeconds: 14.567,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S06_Crossroads: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: slow orbit feel via rotation + scale
  const camRot = interpolate(frame, [0, durationInFrames], [-1.6, 1.6], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, durationInFrames], [1.08, 1.02], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const compassIn = sp(8, { damping: 11 });
  const roadProg = interpolate(frame, [40, 120], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const signIn = (i: number) => sp(110 + i * 26, { damping: 11, stiffness: 100 });
  const qIn = sp(190, { damping: 9, stiffness: 80 });
  const needle = Math.sin(frame * 0.09) * 34;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9929EA', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(0deg, #46096F 0%, #9929EA 62%, #BE6BFB 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(circle at 50% 100%, rgba(245,208,0,0.22), rgba(0,0,0,0) 55%)`, opacity: 0.7 + Math.sin(frame * 0.03) * 0.2 }} />

      {/* Motion texture: radial spokes */}
      <svg width={1920} height={1080} style={{ position: 'absolute', inset: 0, opacity: 0.1 }}>
        {new Array(28).fill(0).map((_, i) => {
          const a = (i / 28) * Math.PI * 2 + frame * 0.004;
          return (
            <line
              key={i}
              x1={960}
              y1={1010}
              x2={960 + Math.cos(a) * 1400}
              y2={1010 + Math.sin(a) * 1400}
              stroke="#F5F5F5"
              strokeWidth={3}
            />
          );
        })}
      </svg>

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', left: -180, top: 120, width: 560, height: 560, background: '#000000', opacity: 0.08, borderRadius: 40, transform: `rotate(${-15 + Math.sin(frame * 0.02) * 6}deg)` }} />
      <div style={{ position: 'absolute', right: -140, bottom: 220, width: 620, height: 620, borderRadius: '50%', border: '4px solid #F5F5F5', opacity: 0.07, transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})` }} />

      {/* Punk collage */}
      <div style={{ position: 'absolute', left: 0, top: 172, width: 1920, height: 92, background: '#000000', opacity: 0.9, transform: 'rotate(-1deg)' }} />
      <div style={{ position: 'absolute', left: 130, top: 152, width: 240, height: 36, background: '#F5D000', transform: 'rotate(-8deg)' }} />

      {/* Floating particles */}
      {new Array(26).fill(0).map((_, i) => {
        const a = seededRandom(i + 31);
        const b = seededRandom(i + 66);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: a * 1920,
              top: 1080 - ((b * 1080 + frame * (0.7 + a * 1.4)) % 1080),
              width: 4 + a * 7,
              height: 4 + a * 7,
              borderRadius: '50%',
              background: i % 4 === 0 ? '#F5D000' : '#F5F5F5',
              opacity: 0.16 + b * 0.24,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg) scale(${camScale})` }}>
        <div style={{ position: 'absolute', left: 140, top: 188, fontFamily: 'JetBrains Mono', fontSize: 32, fontWeight: 700, letterSpacing: 6, color: '#F5F5F5' }}>
          DIRECTION CHECK
        </div>

        {/* Diverging roads */}
        <svg width={1920} height={1080} style={{ position: 'absolute', inset: 0 }}>
          <path
            d="M960 1010 L520 420"
            stroke="#F5F5F5"
            strokeWidth={22}
            strokeLinecap="round"
            pathLength={1}
            strokeDasharray={1}
            strokeDashoffset={1 - roadProg}
            opacity={0.9}
          />
          <path
            d="M960 1010 L1420 420"
            stroke="#F5D000"
            strokeWidth={22}
            strokeLinecap="round"
            pathLength={1}
            strokeDasharray={1}
            strokeDashoffset={1 - roadProg}
          />
          <path
            d="M960 1010 L960 500"
            stroke="#000000"
            strokeWidth={14}
            strokeDasharray="26 22"
            strokeLinecap="round"
            opacity={0.55 * roadProg}
          />
        </svg>

        {/* Compass hub */}
        <div
          style={{
            position: 'absolute',
            left: 960,
            top: 1010,
            marginLeft: -110,
            marginTop: -110,
            width: 220,
            height: 220,
            borderRadius: '50%',
            background: '#F5F5F5',
            border: '9px solid #000000',
            boxShadow: '0 0 44px rgba(0,0,0,0.4)',
            transform: `scale(${0.7 + compassIn * 0.3})`,
            opacity: compassIn,
          }}
        >
          <svg width={202} height={202} viewBox="0 0 24 24">
            <circle cx={12} cy={12} r={10} fill="none" stroke="#9929EA" strokeWidth={1.4} />
            <g transform={`rotate(${needle} 12 12)`}>
              <path d="M12 4 14.4 12 12 20 9.6 12Z" fill="#9929EA" />
              <path d="M12 4 14.4 12H9.6L12 4Z" fill="#F5D000" />
            </g>
            <circle cx={12} cy={12} r={1.6} fill="#000000" />
          </svg>
        </div>

        {/* Two direction signs */}
        {[
          { t: 'सिर्फ स्पीड', s: 'SPEED FIRST', x: 250, y: 300, bg: '#F5F5F5', fg: '#000000', k: 'bolt' },
          { t: 'सुरक्षा पहले', s: 'SAFETY FIRST', x: 1230, y: 300, bg: '#000000', fg: '#F5F5F5', k: 'shield' },
        ].map((s, i) => (
          <div
            key={s.s}
            style={{
              position: 'absolute',
              left: s.x,
              top: s.y,
              width: 470,
              padding: '28px 32px',
              background: s.bg,
              border: `7px solid ${i === 1 ? '#F5D000' : '#000000'}`,
              boxShadow: '18px 18px 0 rgba(0,0,0,0.3)',
              transform: `translateY(${(1 - signIn(i)) * -140}px) rotate(${i === 0 ? -3 : 3}deg)`,
              opacity: signIn(i),
              display: 'flex',
              alignItems: 'center',
              gap: 24,
            }}
          >
            <svg width={82} height={82} viewBox="0 0 24 24" style={{ transform: `scale(${1 + Math.sin(frame * 0.1 + i) * 0.05})` }}>
              {s.k === 'bolt' && <path d="M13.5 2 5 13.5h5l-1.5 8.5L19 10h-5.5L13.5 2Z" fill="#9929EA" stroke="#000000" strokeWidth={1.2} />}
              {s.k === 'shield' && (
                <>
                  <path d="M12 2 20 5v6c0 5-3.4 9-8 11-4.6-2-8-6-8-11V5l8-3Z" fill="#F5D000" />
                  <path d="M8.4 12l2.6 2.6 4.6-5" fill="none" stroke="#000000" strokeWidth={2.2} strokeLinecap="round" />
                </>
              )}
            </svg>
            <div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, fontWeight: 700, letterSpacing: 4, color: i === 1 ? '#F5D000' : '#9929EA' }}>{s.s}</div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 56, fontWeight: 800, color: s.fg }}>{s.t}</div>
            </div>
          </div>
        ))}

        {/* Central question mark built from icon */}
        <div
          style={{
            position: 'absolute',
            left: 960,
            top: 470,
            marginLeft: -90,
            width: 180,
            height: 180,
            transform: `scale(${0.6 + qIn * 0.4}) rotate(${(1 - qIn) * -30}deg)`,
            opacity: qIn,
          }}
        >
          <svg width={180} height={180} viewBox="0 0 24 24">
            <circle cx={12} cy={12} r={11} fill="#F5D000" stroke="#000000" strokeWidth={1.4} />
            <path d="M8.4 9.2a3.7 3.7 0 1 1 5.7 3.1c-1.1.8-2.1 1.4-2.1 2.9" fill="none" stroke="#000000" strokeWidth={2.4} strokeLinecap="round" />
            <circle cx={12} cy={18.4} r={1.6} fill="#000000" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 42%, rgba(0,0,0,0.55) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S06_Crossroads;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S07_ScaleNetwork',
  durationInSeconds: 14.567,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S07_ScaleNetwork: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: zoom out to expose the full network
  const camScale = interpolate(frame, [0, durationInFrames], [1.35, 0.98], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const camRot = Math.sin(frame * 0.01) * 1;

  const nodes = new Array(34).fill(0).map((_, i) => {
    const ring = i < 6 ? 0 : i < 18 ? 1 : 2;
    const idxInRing = ring === 0 ? i : ring === 1 ? i - 6 : i - 18;
    const count = ring === 0 ? 6 : ring === 1 ? 12 : 16;
    const a = (idxInRing / count) * Math.PI * 2 + ring * 0.4 + frame * (0.0025 * (ring + 1));
    const r = 150 + ring * 190;
    return {
      x: 900 + Math.cos(a) * r,
      y: 540 + Math.sin(a) * r * 0.62,
      ring,
      i,
    };
  });

  const nodeIn = (i: number) => sp(14 + i * 4, { damping: 14, stiffness: 130 });
  const meter = interpolate(frame, [110, 250], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const labelIn = sp(180, { damping: 12 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#9929EA', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(300deg, #9929EA 0%, #6A15AC 50%, #380760 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(circle at 47% 50%, rgba(245,245,245,${0.16 + Math.sin(frame * 0.05) * 0.05}), rgba(0,0,0,0) 48%)` }} />

      {/* Motion texture: wavy lines */}
      <svg width={1920} height={1080} style={{ position: 'absolute', inset: 0, opacity: 0.1 }}>
        {new Array(11).fill(0).map((_, i) => {
          let d = `M0 ${60 + i * 100}`;
          for (let x = 0; x <= 1920; x += 60) {
            d += ` L${x} ${60 + i * 100 + Math.sin(x * 0.006 + frame * 0.05 + i) * 18}`;
          }
          return <path key={i} d={d} fill="none" stroke="#F5F5F5" strokeWidth={2.4} />;
        })}
      </svg>

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', left: 620, top: 120, width: 700, height: 700, borderRadius: '50%', border: '6px solid #F5F5F5', opacity: 0.05, transform: `scale(${1 + Math.sin(frame * 0.02) * 0.08})` }} />
      <div style={{ position: 'absolute', left: -200, bottom: -180, width: 620, height: 620, background: '#000000', opacity: 0.07, transform: `rotate(${frame * 0.06}deg)` }} />

      {/* Punk collage */}
      <div style={{ position: 'absolute', right: 0, top: 0, width: 340, height: 1080, background: '#000000', opacity: 0.88, transform: 'skewX(4deg) translateX(90px)' }} />
      <div style={{ position: 'absolute', right: 96, top: 108, width: 250, height: 40, background: '#F5D000', transform: 'rotate(-6deg)' }} />

      {/* Floating particles */}
      {new Array(24).fill(0).map((_, i) => {
        const a = seededRandom(i + 9);
        const b = seededRandom(i + 44);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: (a * 1920 + frame * (0.4 + a)) % 1920,
              top: b * 1080,
              width: 3 + a * 6,
              height: 3 + a * 6,
              borderRadius: '50%',
              background: '#F5F5F5',
              opacity: 0.18 + b * 0.2,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)` }}>
        {/* Network links */}
        <svg width={1920} height={1080} style={{ position: 'absolute', inset: 0 }}>
          {nodes.map((n) => (
            <line
              key={`l${n.i}`}
              x1={900}
              y1={540}
              x2={n.x}
              y2={n.y}
              stroke={n.ring === 2 ? '#F5D000' : '#F5F5F5'}
              strokeWidth={n.ring === 0 ? 4 : 2}
              opacity={0.28 * nodeIn(n.i)}
            />
          ))}
          {nodes.map((n, k) => {
            const nx = nodes[(k + 5) % nodes.length];
            return (
              <line
                key={`c${n.i}`}
                x1={n.x}
                y1={n.y}
                x2={nx.x}
                y2={nx.y}
                stroke="#F5F5F5"
                strokeWidth={1.6}
                opacity={0.14 * nodeIn(n.i)}
              />
            );
          })}
        </svg>

        {/* Nodes as phone icons */}
        {nodes.map((n) => {
          const v = nodeIn(n.i);
          const s = n.ring === 0 ? 1 : n.ring === 1 ? 0.78 : 0.6;
          return (
            <div
              key={`n${n.i}`}
              style={{
                position: 'absolute',
                left: n.x,
                top: n.y,
                marginLeft: -26 * s,
                marginTop: -26 * s,
                transform: `scale(${v * s}) translateY(${Math.sin(frame * 0.08 + n.i) * 3}px)`,
                opacity: v,
              }}
            >
              <svg width={52} height={52} viewBox="0 0 24 24">
                <rect x={6} y={2} width={12} height={20} rx={3} fill={n.ring === 2 ? '#F5D000' : '#F5F5F5'} stroke="#000000" strokeWidth={1.4} />
                <rect x={8} y={5} width={8} height={12} fill="#9929EA" />
                <circle cx={12} cy={19.4} r={1.1} fill="#000000" />
              </svg>
            </div>
          );
        })}

        {/* Central hub */}
        <div
          style={{
            position: 'absolute',
            left: 900,
            top: 540,
            marginLeft: -84,
            marginTop: -84,
            width: 168,
            height: 168,
            borderRadius: 28,
            background: '#000000',
            border: '8px solid #F5D000',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            transform: `rotate(${45 + Math.sin(frame * 0.04) * 3}deg) scale(${0.8 + sp(4) * 0.2})`,
            boxShadow: '0 0 60px rgba(245,208,0,0.45)',
          }}
        >
          <div style={{ transform: 'rotate(-45deg)', fontFamily: 'Inter', fontSize: 44, fontWeight: 800, color: '#F5F5F5' }}>UPI</div>
        </div>

        {/* Scale meter */}
        <div style={{ position: 'absolute', left: 120, bottom: 130, width: 620, opacity: labelIn }}>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 26, letterSpacing: 5, color: '#F5F5F5', fontWeight: 700, marginBottom: 14 }}>
            SYSTEM SCALE
          </div>
          <div style={{ width: 620, height: 46, background: '#000000', border: '5px solid #F5F5F5' }}>
            <div style={{ width: meter * 610, height: '100%', background: '#F5D000' }} />
          </div>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 52, fontWeight: 800, color: '#F5F5F5', marginTop: 18 }}>
            अभूतपूर्व स्तर
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 40%, rgba(0,0,0,0.58) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S07_ScaleNetwork;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S08_Dependency',
  durationInSeconds: 14.567,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S08_Dependency: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: wide pan + slight zoom in
  const camX = interpolate(frame, [0, durationInFrames], [-40, 40], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const cols = 24;
  const rows = 9;
  const total = cols * rows;
  const fill = interpolate(frame, [10, 150], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const counter = Math.round(interpolate(frame, [10, 150], [0, 140], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const riskIn = sp(160, { damping: 11 });
  const barGrow = interpolate(frame, [170, 260], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shake = frame > 160 && frame < 174 ? Math.sin((frame - 160) * 3) * 6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9929EA', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(75deg, #7A1AC4 0%, #9929EA 48%, #C46FFB 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(circle at 80% 20%, rgba(0,0,0,0.35), rgba(0,0,0,0) 55%)` }} />

      {/* Motion texture: checkerboard drift */}
      <div
        style={{
          position: 'absolute',
          inset: -120,
          opacity: 0.07,
          background:
            'repeating-conic-gradient(#F5F5F5 0% 25%, rgba(0,0,0,0) 0% 50%) 0 0 / 90px 90px',
          transform: `translate(${(frame * 0.35) % 90}px, ${(frame * 0.2) % 90}px)`,
        }}
      />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', right: 140, top: 120, width: 520, height: 520, borderRadius: '50%', background: '#F5D000', opacity: 0.06, transform: `scale(${1 + Math.sin(frame * 0.04) * 0.08})` }} />
      <div style={{ position: 'absolute', left: 60, bottom: -200, width: 700, height: 700, background: '#000000', opacity: 0.07, borderRadius: 120, transform: `rotate(${-frame * 0.07}deg)` }} />

      {/* Punk collage */}
      <div style={{ position: 'absolute', left: 60, top: 96, width: 780, height: 120, background: '#000000', opacity: 0.94, transform: 'rotate(-1.4deg)' }} />
      <div style={{ position: 'absolute', left: 800, top: 84, width: 220, height: 38, background: '#F5D000', transform: 'rotate(13deg)' }} />

      {/* Floating particles */}
      {new Array(20).fill(0).map((_, i) => {
        const a = seededRandom(i + 17);
        const b = seededRandom(i + 71);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: a * 1920,
              top: (b * 1080 + frame * (0.45 + a)) % 1080,
              width: 6 + a * 8,
              height: 6 + a * 8,
              background: i % 3 === 0 ? '#F5D000' : '#F5F5F5',
              opacity: 0.14 + b * 0.2,
              transform: `rotate(${frame * a}deg)`,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX + shake}px) scale(${camScale})` }}>
        {/* Counter header */}
        <div style={{ position: 'absolute', left: 96, top: 116, display: 'flex', alignItems: 'baseline', gap: 20 }}>
          <div style={{ fontFamily: 'Inter', fontSize: 96, fontWeight: 800, color: '#F5D000', lineHeight: 1 }}>{counter}</div>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 60, fontWeight: 800, color: '#F5F5F5' }}>करोड़ की आबादी</div>
        </div>

        {/* Phone icon grid filling up */}
        <div style={{ position: 'absolute', left: 96, top: 300, width: 1180, display: 'flex', flexWrap: 'wrap', gap: 12 }}>
          {new Array(total).fill(0).map((_, i) => {
            const on = i / total <= fill;
            const wob = Math.sin(frame * 0.1 + i * 0.4) * 2;
            return (
              <div
                key={i}
                style={{
                  width: 36,
                  height: 52,
                  borderRadius: 6,
                  background: on ? (i % 11 === 0 ? '#F5D000' : '#F5F5F5') : 'rgba(245,245,245,0.14)',
                  border: '2px solid rgba(0,0,0,0.7)',
                  transform: on ? `translateY(${wob}px) scale(1)` : 'scale(0.86)',
                  boxShadow: on && i % 11 === 0 ? '0 0 14px rgba(245,208,0,0.7)' : 'none',
                }}
              />
            );
          })}
        </div>

        {/* Risk card */}
        <div
          style={{
            position: 'absolute',
            right: 90,
            top: 320,
            width: 520,
            background: '#000000',
            border: '8px solid #F5D000',
            padding: 34,
            boxSizing: 'border-box',
            transform: `translateY(${(1 - riskIn) * 160}px) rotate(2deg)`,
            opacity: riskIn,
            boxShadow: '18px 18px 0 rgba(0,0,0,0.3)',
          }}
        >
          <svg width={86} height={86} viewBox="0 0 24 24">
            <circle cx={12} cy={12} r={10.5} fill="none" stroke="#F5D000" strokeWidth={2} />
            <circle cx={12} cy={12} r={6.5} fill="none" stroke="#F5F5F5" strokeWidth={1.8} />
            <circle cx={12} cy={12} r={2.4} fill="#F5D000" />
            <path d="M12 0v4M12 20v4M0 12h4M20 12h4" stroke="#F5F5F5" strokeWidth={1.8} />
          </svg>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 56, fontWeight: 800, color: '#F5F5F5', marginTop: 18, lineHeight: 1.15 }}>
            निर्भरता बढ़ी,
            <br /> टारगेट बड़ा हुआ
          </div>

          {/* Risk bar */}
          <div style={{ marginTop: 26, height: 38, background: '#2A2A2A', border: '4px solid #F5F5F5' }}>
            <div style={{ width: `${barGrow * 100}%`, height: '100%', background: '#F5D000' }} />
          </div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 24, letterSpacing: 3, color: '#F5F5F5', marginTop: 12, opacity: 0.8 }}>
            ATTACK SURFACE
          </div>
        </div>

        {/* Caption strip */}
        <div
          style={{
            position: 'absolute',
            left: 96,
            bottom: 96,
            padding: '14px 30px',
            background: '#F5F5F5',
            border: '6px solid #000000',
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 44,
            fontWeight: 800,
            color: '#000000',
            transform: `rotate(-1.5deg) scale(${0.9 + sp(150) * 0.1})`,
            opacity: sp(150),
          }}
        >
          हर छोटी-बड़ी ज़रूरत, एक स्क्रीन पर
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 45%, rgba(0,0,0,0.52) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S08_Dependency;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S09_TargetLock',
  durationInSeconds: 14.567,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S09_TargetLock: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: hard zoom-in onto the bullseye
  const camScale = interpolate(frame, [0, 120, durationInFrames], [1.0, 1.08, 1.18], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const camRot = Math.sin(frame * 0.02) * 0.8;

  const targetIn = sp(10, { damping: 13 });
  const threatIn = (i: number) => sp(56 + i * 22, { damping: 11, stiffness: 110 });
  const strikeProg = interpolate(frame, [150, 210], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const lockIn = sp(214, { damping: 9, stiffness: 90 });
  const shake = frame > 210 && frame < 226 ? Math.sin((frame - 210) * 2.6) * 11 : 0;

  const threats = [
    { t: 'हैकर्स', s: 'REMOTE ACCESS', k: 'bug', x: 120, y: 200 },
    { t: 'स्कैमर्स', s: 'PHISHING LINK', k: 'hook', x: 120, y: 690 },
    { t: 'साइबर अपराध', s: 'FAKE IDENTITY', k: 'mask', x: 1360, y: 200 },
    { t: 'डेटा चोरी', s: 'NUMBER LEAK', k: 'key', x: 1360, y: 690 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#9929EA', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, #B355F8 0%, #9929EA 38%, #350861 100%)' }} />
      <AbsoluteFill style={{ background: `conic-gradient(from ${frame * 0.6}deg at 50% 50%, rgba(245,208,0,0.14), rgba(0,0,0,0) 30%, rgba(245,208,0,0.14) 60%, rgba(0,0,0,0) 100%)` }} />

      {/* Motion texture: scan lines */}
      <div
        style={{
          position: 'absolute',
          inset: 0,
          opacity: 0.12,
          background: 'repeating-linear-gradient(0deg, #F5F5F5 0 2px, rgba(0,0,0,0) 2px 10px)',
          transform: `translateY(${(frame * 0.8) % 10}px)`,
        }}
      />

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', left: 660, top: 220, width: 640, height: 640, border: '5px solid #F5F5F5', opacity: 0.06, transform: `rotate(${frame * 0.2}deg)` }} />
      <div style={{ position: 'absolute', left: 740, top: 300, width: 480, height: 480, background: '#000000', opacity: 0.06, borderRadius: '50%', transform: `scale(${1 + Math.sin(frame * 0.05) * 0.06})` }} />

      {/* Punk collage */}
      <div style={{ position: 'absolute', left: 0, top: 508, width: 1920, height: 64, background: '#000000', opacity: 0.85, transform: 'rotate(0.8deg)' }} />
      <div style={{ position: 'absolute', left: 780, top: 90, width: 360, height: 42, background: '#F5D000', transform: 'rotate(-3deg)' }} />

      {/* Floating particles */}
      {new Array(30).fill(0).map((_, i) => {
        const a = seededRandom(i + 23);
        const b = seededRandom(i + 92);
        const ang = a * Math.PI * 2;
        const rad = 260 + b * 620 - (frame * (1.2 + a * 2)) % 700;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: 960 + Math.cos(ang) * Math.max(60, rad),
              top: 540 + Math.sin(ang) * Math.max(40, rad) * 0.66,
              width: 4 + a * 6,
              height: 4 + a * 6,
              borderRadius: '50%',
              background: i % 4 === 0 ? '#F5D000' : '#F5F5F5',
              opacity: 0.18 + b * 0.22,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg) translateY(${shake}px)` }}>
        {/* Bullseye target */}
        <div
          style={{
            position: 'absolute',
            left: 960,
            top: 540,
            marginLeft: -230,
            marginTop: -230,
            width: 460,
            height: 460,
            transform: `scale(${0.7 + targetIn * 0.3}) rotate(${(1 - targetIn) * 40}deg)`,
            opacity: targetIn,
          }}
        >
          <svg width={460} height={460} viewBox="0 0 24 24">
            <circle cx={12} cy={12} r={11} fill="#F5F5F5" stroke="#000000" strokeWidth={0.8} />
            <circle cx={12} cy={12} r={8.4} fill="#9929EA" />
            <circle cx={12} cy={12} r={5.8} fill="#F5F5F5" />
            <circle cx={12} cy={12} r={3.2} fill="#F5D000" />
            <circle cx={12} cy={12} r={1.2} fill="#000000" />
            <path d="M12 0.4v3.2M12 20.4v3.2M0.4 12h3.2M20.4 12h3.2" stroke="#F5F5F5" strokeWidth={1} />
          </svg>
          <div style={{ position: 'absolute', left: 150, top: 190, fontFamily: 'Inter', fontSize: 44, fontWeight: 800, color: '#000000' }}>UPI</div>
        </div>

        {/* Crosshair brackets */}
        {[
          { x: 660, y: 240, r: 0 },
          { x: 1180, y: 240, r: 90 },
          { x: 1180, y: 760, r: 180 },
          { x: 660, y: 760, r: 270 },
        ].map((c, i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: c.x + Math.sin(frame * 0.06 + i) * 6,
              top: c.y + Math.cos(frame * 0.06 + i) * 6,
              width: 80,
              height: 80,
              borderTop: '9px solid #F5D000',
              borderLeft: '9px solid #F5D000',
              transform: `rotate(${c.r}deg)`,
              opacity: targetIn,
            }}
          />
        ))}

        {/* Threat cards */}
        {threats.map((t, i) => (
          <div
            key={t.t}
            style={{
              position: 'absolute',
              left: t.x,
              top: t.y,
              width: 440,
              padding: '24px 28px',
              background: i % 2 === 0 ? '#F5F5F5' : '#000000',
              border: `6px solid ${i % 2 === 0 ? '#000000' : '#F5D000'}`,
              boxShadow: '16px 16px 0 rgba(0,0,0,0.3)',
              transform: `translate(${(1 - threatIn(i)) * (t.x < 900 ? -180 : 180)}px, 0) rotate(${i % 2 === 0 ? -2 : 2}deg)`,
              opacity: threatIn(i),
              display: 'flex',
              alignItems: 'center',
              gap: 22,
            }}
          >
            <svg width={70} height={70} viewBox="0 0 24 24" style={{ transform: `translateY(${Math.sin(frame * 0.1 + i) * 3}px)` }}>
              {t.k === 'bug' && (
                <>
                  <ellipse cx={12} cy={13} rx={5.4} ry={6.4} fill="#9929EA" stroke="#000000" strokeWidth={1.3} />
                  <path d="M6.6 9 3 6.4M17.4 9 21 6.4M6.2 13H2.4M17.8 13h3.8M6.6 17 3 19.6M17.4 17 21 19.6" stroke={i % 2 === 0 ? '#000000' : '#F5D000'} strokeWidth={1.8} />
                  <circle cx={12} cy={6} r={2.4} fill="#F5D000" stroke="#000000" strokeWidth={1.1} />
                </>
              )}
              {t.k === 'hook' && (
                <>
                  <path d="M15 3v9.4a4.6 4.6 0 1 1-9.2 0" fill="none" stroke="#F5D000" strokeWidth={2.6} strokeLinecap="round" />
                  <circle cx={15} cy={3} r={2} fill="#9929EA" stroke="#F5F5F5" strokeWidth={1} />
                </>
              )}
              {t.k === 'mask' && (
                <>
                  <path d="M3 8h18v5a6 6 0 0 1-6 6H9a6 6 0 0 1-6-6V8Z" fill="#9929EA" stroke="#000000" strokeWidth={1.3} />
                  <rect x={6} y={11} width={4.4} height={2.6} rx={1.3} fill="#F5D000" />
                  <rect x={13.6} y={11} width={4.4} height={2.6} rx={1.3} fill="#F5D000" />
                  <path d="M3 8 12 4l9 4" fill="none" stroke="#000000" strokeWidth={1.4} />
                </>
              )}
              {t.k === 'key' && (
                <>
                  <circle cx={8} cy={12} r={4.4} fill="none" stroke="#F5D000" strokeWidth={2.4} />
                  <path d="M12.4 12H21M18 12v3.4M15.4 12v2.6" stroke="#F5D000" strokeWidth={2.4} strokeLinecap="round" />
                </>
              )}
            </svg>
            <div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, letterSpacing: 3, fontWeight: 700, color: i % 2 === 0 ? '#9929EA' : '#F5D000' }}>{t.s}</div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 50, fontWeight: 800, color: i % 2 === 0 ? '#000000' : '#F5F5F5' }}>{t.t}</div>
            </div>
          </div>
        ))}

        {/* Converging strike lines */}
        <svg width={1920} height={1080} style={{ position: 'absolute', inset: 0 }}>
          {[
            'M560 260 L860 470',
            'M560 750 L860 620',
            'M1360 260 L1060 470',
            'M1360 750 L1060 620',
          ].map((d, i) => (
            <path
              key={i}
              d={d}
              fill="none"
              stroke="#F5D000"
              strokeWidth={6}
              pathLength={1}
              strokeDasharray={1}
              strokeDashoffset={1 - strikeProg}
              opacity={0.85}
            />
          ))}
        </svg>

        {/* Lock-on stamp */}
        <div
          style={{
            position: 'absolute',
            left: 800,
            top: 950,
            padding: '12px 28px',
            border: '6px solid #F5D000',
            background: 'rgba(0,0,0,0.65)',
            fontFamily: 'Inter',
            fontSize: 36,
            fontWeight: 800,
            letterSpacing: 7,
            color: '#F5D000',
            transform: `scale(${0.7 + lockIn * 0.3})`,
            opacity: lockIn,
          }}
        >
          TARGET LOCKED
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 38%, rgba(0,0,0,0.62) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S09_TargetLock;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'S10_NewRulesLive',
  durationInSeconds: 14.567,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const S10_NewRulesLive: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera: push-in then settle
  const camScale = interpolate(frame, [0, 90, durationInFrames], [1.14, 1.04, 1.09], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.ease) });
  const camY = Math.sin(frame * 0.018) * 10;

  const calIn = sp(8, { damping: 12 });
  const flip = interpolate(frame, [40, 62], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shieldIn = sp(84, { damping: 10, stiffness: 100 });
  const tickIn = interpolate(frame, [120, 150], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const milestone = (i: number) => sp(150 + i * 20, { damping: 12 });
  const liveIn = sp(230, { damping: 9, stiffness: 80 });
  const shake = frame > 84 && frame < 98 ? Math.sin((frame - 84) * 2.7) * 8 : 0;

  const milestones = [
    { t: 'नंबर मास्किंग', k: 'mask' },
    { t: 'Username UPI ID', k: 'id' },
    { t: '₹2000+ पर होल्ड', k: 'pause' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#9929EA', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{ background: 'linear-gradient(210deg, #C773FC 0%, #9929EA 45%, #4C0C7C 100%)' }} />
      <AbsoluteFill style={{ background: `radial-gradient(circle at 55% 45%, rgba(245,208,0,${0.16 + Math.sin(frame * 0.04) * 0.06}), rgba(0,0,0,0) 52%)` }} />

      {/* Motion texture: vertical bars equalizer */}
      <svg width={1920} height={1080} style={{ position: 'absolute', inset: 0, opacity: 0.1 }}>
        {new Array(48).fill(0).map((_, i) => {
          const h = 60 + Math.abs(Math.sin(frame * 0.05 + i * 0.5)) * 260;
          return <rect key={i} x={i * 40 + 6} y={1080 - h} width={16} height={h} fill="#F5F5F5" />;
        })}
      </svg>

      {/* Animated shape layers */}
      <div style={{ position: 'absolute', left: -140, top: 80, width: 600, height: 600, borderRadius: 80, border: '6px solid #F5F5F5', opacity: 0.06, transform: `rotate(${frame * 0.11}deg)` }} />
      <div style={{ position: 'absolute', right: 120, bottom: 40, width: 520, height: 520, background: '#000000', opacity: 0.07, borderRadius: '50%', transform: `scale(${1 + Math.sin(frame * 0.03) * 0.07})` }} />

      {/* Punk collage */}
      <div style={{ position: 'absolute', left: 0, top: 880, width: 1920, height: 200, background: '#000000', opacity: 0.9, transform: 'rotate(-1deg) translateY(30px)' }} />
      <div style={{ position: 'absolute', left: 1420, top: 120, width: 340, height: 44, background: '#F5D000', transform: 'rotate(8deg)' }} />

      {/* Floating particles */}
      {new Array(26).fill(0).map((_, i) => {
        const a = seededRandom(i + 29);
        const b = seededRandom(i + 83);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: a * 1920,
              top: 1080 - ((b * 1080 + frame * (0.6 + a * 1.3)) % 1080),
              width: 5 + a * 9,
              height: 5 + a * 9,
              background: i % 3 === 0 ? '#F5D000' : '#F5F5F5',
              borderRadius: i % 2 === 0 ? '50%' : 0,
              opacity: 0.16 + b * 0.22,
              transform: `rotate(${frame * a}deg)`,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${camY + shake}px)` }}>
        {/* Calendar card flipping to 01 AUG 2026 */}
        <div
          style={{
            position: 'absolute',
            left: 150,
            top: 250,
            width: 420,
            background: '#F5F5F5',
            border: '9px solid #000000',
            boxShadow: '22px 22px 0 rgba(0,0,0,0.32)',
            transform: `rotate(-3deg) scale(${0.8 + calIn * 0.2})`,
            opacity: calIn,
            overflow: 'hidden',
          }}
        >
          <div style={{ background: '#000000', padding: '16px 24px', display: 'flex', alignItems: 'center', gap: 16 }}>
            <svg width={44} height={44} viewBox="0 0 24 24">
              <rect x={3} y={5} width={18} height={16} rx={2.2} fill="#F5D000" stroke="#F5F5F5" strokeWidth={1.2} />
              <path d="M7 3v4M17 3v4M3 10h18" stroke="#F5F5F5" strokeWidth={1.8} />
            </svg>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, fontWeight: 700, letterSpacing: 4, color: '#F5F5F5' }}>EFFECTIVE</div>
          </div>
          <div style={{ padding: '26px 24px 34px', textAlign: 'center' }}>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 168,
                fontWeight: 800,
                lineHeight: 1,
                color: '#9929EA',
                transform: `rotateX(${(1 - flip) * 80}deg)`,
              }}
            >
              01
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 54, fontWeight: 800, color: '#000000', letterSpacing: 4, marginTop: 10 }}>
              AUG 2026
            </div>
          </div>
        </div>

        {/* Shield forming with check */}
        <div
          style={{
            position: 'absolute',
            left: 700,
            top: 260,
            width: 380,
            height: 420,
            transform: `scale(${0.75 + shieldIn * 0.25}) rotate(${(1 - shieldIn) * -12}deg)`,
            opacity: shieldIn,
          }}
        >
          <svg width={380} height={420} viewBox="0 0 24 26">
            <path d="M12 1.5 21 5v7.5c0 6-4 10.8-9 13-5-2.2-9-7-9-13V5l9-3.5Z" fill="#F5D000" stroke="#000000" strokeWidth={0.9} />
            <path d="M12 3.6 19 6.4v6.1c0 5-3.2 9-7 10.8-3.8-1.8-7-5.8-7-10.8V6.4L12 3.6Z" fill="#9929EA" />
            <path
              d="M8 13.4 11.2 16.6 16.6 10.4"
              fill="none"
              stroke="#F5F5F5"
              strokeWidth={2.2}
              strokeLinecap="round"
              pathLength={1}
              strokeDasharray={1}
              strokeDashoffset={1 - tickIn}
            />
          </svg>
        </div>

        {/* Milestone chips on a timeline */}
        <svg width={1920} height={1080} style={{ position: 'absolute', inset: 0 }}>
          <line x1={1180} y1={330} x2={1180} y2={790} stroke="#F5F5F5" strokeWidth={7} opacity={0.5} />
        </svg>
        {milestones.map((m, i) => {
          const v = milestone(i);
          return (
            <div key={m.t} style={{ position: 'absolute', left: 1180, top: 330 + i * 160, opacity: v }}>
              <div
                style={{
                  position: 'absolute',
                  left: -20,
                  top: 22,
                  width: 40,
                  height: 40,
                  borderRadius: '50%',
                  background: '#F5D000',
                  border: '6px solid #000000',
                  transform: `scale(${0.6 + v * 0.4})`,
                }}
              />
              <div
                style={{
                  marginLeft: 54,
                  display: 'flex',
                  alignItems: 'center',
                  gap: 20,
                  padding: '18px 28px',
                  background: i === 2 ? '#000000' : '#F5F5F5',
                  border: `6px solid ${i === 2 ? '#F5D000' : '#000000'}`,
                  transform: `translateX(${(1 - v) * 140}px) rotate(${i % 2 ? 1.4 : -1.4}deg)`,
                  boxShadow: '14px 14px 0 rgba(0,0,0,0.28)',
                }}
              >
                <svg width={52} height={52} viewBox="0 0 24 24">
                  {m.k === 'mask' && (
                    <>
                      <rect x={3} y={9} width={18} height={7} rx={3.4} fill="#9929EA" stroke="#000000" strokeWidth={1.2} />
                      <circle cx={7.5} cy={12.5} r={1.5} fill="#F5D000" />
                      <circle cx={12} cy={12.5} r={1.5} fill="#F5D000" />
                      <circle cx={16.5} cy={12.5} r={1.5} fill="#F5D000" />
                    </>
                  )}
                  {m.k === 'id' && (
                    <>
                      <rect x={2.5} y={5} width={19} height={14} rx={2.4} fill="#9929EA" stroke={i === 2 ? '#F5D000' : '#000000'} strokeWidth={1.3} />
                      <circle cx={8} cy={11} r={2.6} fill="#F5D000" />
                      <path d="M13 9.4h5.4M13 12.4h5.4M13 15.4h3.4" stroke="#F5F5F5" strokeWidth={1.6} />
                    </>
                  )}
                  {m.k === 'pause' && (
                    <>
                      <circle cx={12} cy={12} r={10.4} fill="#F5D000" stroke="#F5F5F5" strokeWidth={1.1} />
                      <rect x={8.6} y={7.4} width={2.6} height={9.2} fill="#000000" />
                      <rect x={12.8} y={7.4} width={2.6} height={9.2} fill="#000000" />
                    </>
                  )}
                </svg>
                <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 46, fontWeight: 800, color: i === 2 ? '#F5F5F5' : '#000000' }}>{m.t}</div>
              </div>
            </div>
          );
        })}

        {/* LIVE stamp */}
        <div
          style={{
            position: 'absolute',
            left: 170,
            top: 840,
            display: 'flex',
            alignItems: 'center',
            gap: 18,
            padding: '14px 32px',
            background: '#F5D000',
            border: '7px solid #000000',
            transform: `rotate(-3deg) scale(${0.75 + liveIn * 0.25})`,
            opacity: liveIn,
          }}
        >
          <div
            style={{
              width: 26,
              height: 26,
              borderRadius: '50%',
              background: '#000000',
              opacity: 0.5 + Math.abs(Math.sin(frame * 0.16)) * 0.5,
            }}
          />
          <div style={{ fontFamily: 'Inter', fontSize: 44, fontWeight: 800, letterSpacing: 5, color: '#000000' }}>NEW RULES LIVE</div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 44%, rgba(0,0,0,0.54) 100%)' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#F5F5F5', opacity: 0.3 }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default S10_NewRulesLive;
```

