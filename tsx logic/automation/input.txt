```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene31: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camScale = interpolate(frame, [0, 270], [1, 1.04], { extrapolateRight: 'clamp' });

  // Stamp impact at frame 60
  const stamp1 = sp(20, { stiffness: 180, damping: 10 });
  const stamp2 = sp(70, { stiffness: 180, damping: 10 });
  const stamp3 = sp(120, { stiffness: 180, damping: 10 });

  // Title reveal
  const titleOp = interpolate(frame, [10, 30], [0, 1], { extrapolateRight: 'clamp' });
  const titleY = interpolate(sp(5), [0, 1], [-40, 0]);

  // Shake on stamp
  const shake = (impact: number) => {
    const d = frame - impact;
    if (d < 0 || d > 10) return 0;
    return Math.sin(d * 2) * (1 - d / 10) * 6;
  };
  const totalShake = shake(20) + shake(70) + shake(120);

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 20%, rgba(254,236,65,0.18) 0%, transparent 55%), radial-gradient(circle at 75% 80%, rgba(0,0,0,0.22) 0%, transparent 60%)',
      }} />

      {/* Shape Layers - drifting transparent geometry */}
      <div style={{
        position: 'absolute', top: '8%', left: '6%',
        width: 380, height: 380, borderRadius: '50%',
        border: '3px solid #000000',
        opacity: 0.06,
        transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '5%', right: '4%',
        width: 460, height: 460,
        border: '4px solid #FEEC41',
        opacity: 0.08,
        transform: `rotate(${-frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '55%', left: '40%',
        width: 0, height: 0,
        borderLeft: '180px solid transparent',
        borderRight: '180px solid transparent',
        borderBottom: '320px solid #000000',
        opacity: 0.04,
        transform: `rotate(${frame * 0.08}deg)`,
      }} />

      {/* Motion texture - grid lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
        <defs>
          <pattern id="grid31" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#000000" strokeWidth="1.2" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid31)" transform={`translate(${frame * 0.3}, ${frame * 0.2})`} />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const r = seededRandom(i + 31);
        const r2 = seededRandom(i + 200);
        const x = r * 1920;
        const y = (r2 * 1080 + frame * (0.4 + r * 0.5)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 6 + r * 6,
            height: 6 + r * 6,
            borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#FEEC41' : '#000000',
            opacity: 0.35,
          }} />
        );
      })}

      {/* Main content wrapper with camera + shake */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${totalShake}px)` }}>

        {/* Top title */}
        <div style={{
          position: 'absolute', top: 70, left: 0, right: 0,
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#000000',
            color: '#FEEC41',
            padding: '14px 42px',
            fontFamily: 'Inter, Poppins',
            fontWeight: 800,
            fontSize: 36,
            letterSpacing: 4,
            borderRadius: 6,
          }}>
            NO CONTACT REQUIRED
          </div>
          <div style={{
            marginTop: 22,
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 102,
            color: '#000000',
            lineHeight: 1,
            letterSpacing: -2,
          }}>
            JUST WALK IN.
          </div>
        </div>

        {/* Three checklist cards */}
        <div style={{
          position: 'absolute',
          top: 380,
          left: 0, right: 0,
          display: 'flex',
          justifyContent: 'center',
          gap: 50,
        }}>
          {/* Card 1 - No Call */}
          <div style={{
            transform: `scale(${stamp1}) rotate(${(1 - stamp1) * -8}deg)`,
            width: 420, height: 420,
            backgroundColor: '#FEEC41',
            border: '6px solid #000000',
            borderRadius: 18,
            padding: 30,
            display: 'flex', flexDirection: 'column',
            alignItems: 'center', justifyContent: 'center',
            boxShadow: '12px 12px 0 #000000',
          }}>
            <svg width="140" height="140" viewBox="0 0 24 24" fill="none">
              <path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72c.127.96.361 1.903.7 2.81a2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0122 16.92z"
                stroke="#000000" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
              <line x1="2" y1="2" x2="22" y2="22" stroke="#FF0000" strokeWidth="4" strokeLinecap="round" />
            </svg>
            <div style={{
              marginTop: 24,
              fontFamily: 'Inter, Poppins',
              fontWeight: 900,
              fontSize: 54,
              color: '#000000',
              textAlign: 'center',
              lineHeight: 1,
            }}>
              NO CALL
            </div>
          </div>

          {/* Card 2 - No Login */}
          <div style={{
            transform: `scale(${stamp2}) rotate(${(1 - stamp2) * 8}deg)`,
            width: 420, height: 420,
            backgroundColor: '#000000',
            border: '6px solid #000000',
            borderRadius: 18,
            padding: 30,
            display: 'flex', flexDirection: 'column',
            alignItems: 'center', justifyContent: 'center',
            boxShadow: '12px 12px 0 #FEEC41',
          }}>
            <svg width="140" height="140" viewBox="0 0 24 24" fill="none">
              <rect x="3" y="11" width="18" height="11" rx="2" stroke="#FEEC41" strokeWidth="2" />
              <path d="M7 11V7a5 5 0 0110 0v4" stroke="#FEEC41" strokeWidth="2" strokeLinecap="round" />
              <line x1="3" y1="3" x2="21" y2="21" stroke="#FF0000" strokeWidth="4" strokeLinecap="round" />
            </svg>
            <div style={{
              marginTop: 24,
              fontFamily: 'Inter, Poppins',
              fontWeight: 900,
              fontSize: 54,
              color: '#FEEC41',
              textAlign: 'center',
              lineHeight: 1,
            }}>
              NO LOGIN
            </div>
          </div>

          {/* Card 3 - 24 Months Proof */}
          <div style={{
            transform: `scale(${stamp3}) rotate(${(1 - stamp3) * -8}deg)`,
            width: 420, height: 420,
            backgroundColor: '#FEEC41',
            border: '6px solid #000000',
            borderRadius: 18,
            padding: 30,
            display: 'flex', flexDirection: 'column',
            alignItems: 'center', justifyContent: 'center',
            boxShadow: '12px 12px 0 #000000',
          }}>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 900,
              fontSize: 150,
              color: '#000000',
              lineHeight: 1,
              letterSpacing: -4,
            }}>
              24
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 800,
              fontSize: 38,
              color: '#000000',
              letterSpacing: 3,
              marginTop: -4,
            }}>
              MONTHS PROOF
            </div>
            <div style={{
              marginTop: 16,
              fontFamily: 'Inter, Poppins',
              fontWeight: 700,
              fontSize: 28,
              color: '#000000',
              textAlign: 'center',
              opacity: 0.7,
            }}>
              walk → file → done
            </div>
          </div>
        </div>

        {/* Bottom strap */}
        <div style={{
          position: 'absolute',
          bottom: 90,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: interpolate(frame, [140, 165], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#000000',
            color: '#FEEC41',
            padding: '18px 50px',
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 44,
            letterSpacing: 2,
            borderRadius: 8,
            border: '4px solid #FEEC41',
          }}>
            THEY CALCULATE ON EX's EARNINGS
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene31;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Bar growth
  const barFill = interpolate(sp(40, { damping: 18 }), [0, 1], [0, 1]);
  // Counter
  const counterRaw = interpolate(frame, [40, 130], [0, 2840], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const counter = Math.round(counterRaw);

  // Camera
  const camY = interpolate(frame, [0, 270], [0, -15], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient bg */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(254,236,65,0.12) 0%, transparent 40%, rgba(0,0,0,0.2) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '-10%', right: '-5%',
        width: 600, height: 600,
        backgroundColor: '#FEEC41',
        borderRadius: '50%',
        opacity: 0.07,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-15%', left: '-8%',
        width: 700, height: 700,
        border: '5px solid #000000',
        opacity: 0.05,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />

      {/* Motion texture - diagonal lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="diag32" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform={`rotate(45) translate(${frame * 0.4}, 0)`}>
            <line x1="0" y1="0" x2="0" y2="40" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diag32)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 50);
        const r2 = seededRandom(i + 700);
        const x = (r * 1920 + Math.sin(frame * 0.02 + i) * 30) % 1920;
        const y = (r2 * 1080 - frame * (0.3 + r * 0.4) + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 4 + r * 5, height: 4 + r * 5,
            backgroundColor: '#000000',
            opacity: 0.25,
            transform: 'rotate(45deg)',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        {/* Title */}
        <div style={{
          position: 'absolute', top: 80, left: 100,
          opacity: interpolate(sp(0), [0, 1], [0, 1]),
          transform: `translateX(${interpolate(sp(0), [0, 1], [-60, 0])}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 800,
            fontSize: 32,
            color: '#000000',
            letterSpacing: 4,
            backgroundColor: '#FEEC41',
            padding: '8px 20px',
            display: 'inline-block',
          }}>
            BENEFIT CALCULATION
          </div>
          <div style={{
            marginTop: 18,
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 95,
            color: '#000000',
            lineHeight: 0.95,
            letterSpacing: -2,
          }}>
            BASED ON <span style={{ color: '#FEEC41', WebkitTextStroke: '3px #000000' }}>EX's RECORD</span>
          </div>
        </div>

        {/* Comparison bars */}
        <div style={{
          position: 'absolute',
          bottom: 180,
          left: 120,
          right: 120,
          display: 'flex',
          alignItems: 'flex-end',
          justifyContent: 'space-around',
          gap: 80,
        }}>
          {/* YOUR record bar */}
          <div style={{ textAlign: 'center', flex: 1 }}>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 800,
              fontSize: 30,
              color: '#000000',
              marginBottom: 12,
              letterSpacing: 2,
            }}>
              YOUR RECORD
            </div>
            <div style={{
              height: 180 * barFill,
              backgroundColor: '#000000',
              borderRadius: '8px 8px 0 0',
              display: 'flex',
              alignItems: 'flex-start',
              justifyContent: 'center',
              paddingTop: 18,
              opacity: barFill,
            }}>
              <span style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 900,
                fontSize: 42,
                color: '#FEEC41',
              }}>
                $1,200
              </span>
            </div>
          </div>

          {/* Arrow */}
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 120,
            color: '#000000',
            opacity: sp(60),
            transform: `translateX(${interpolate(sp(60), [0, 1], [-40, 0])}px) scale(${0.8 + Math.sin(frame * 0.15) * 0.06})`,
            paddingBottom: 60,
          }}>
            →
          </div>

          {/* EX'S record bar */}
          <div style={{ textAlign: 'center', flex: 1 }}>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 800,
              fontSize: 30,
              color: '#000000',
              marginBottom: 12,
              letterSpacing: 2,
            }}>
              EX's RECORD
            </div>
            <div style={{
              height: 460 * sp(70, { damping: 14 }),
              backgroundColor: '#FEEC41',
              border: '5px solid #000000',
              borderRadius: '8px 8px 0 0',
              display: 'flex',
              alignItems: 'flex-start',
              justifyContent: 'center',
              paddingTop: 30,
              boxShadow: '8px 8px 0 #000000',
            }}>
              <span style={{
                fontFamily: 'Inter, Poppins',
                fontWeight: 900,
                fontSize: 76,
                color: '#000000',
              }}>
                ${counter.toLocaleString()}
              </span>
            </div>
          </div>
        </div>

        {/* Bottom tag */}
        <div style={{
          position: 'absolute',
          bottom: 70,
          left: 0, right: 0,
          textAlign: 'center',
          opacity: sp(140),
          transform: `scale(${sp(140)})`,
        }}>
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 56,
            color: '#000000',
            backgroundColor: '#FEEC41',
            padding: '12px 36px',
            border: '4px solid #000000',
            letterSpacing: 2,
          }}>
            STRATEGIC ADVANTAGE
          </span>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene32;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene33: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Clock rotation (ticking)
  const clockAngle = (frame * 6) % 360;
  // Pulse for "TICKED"
  const pulse = 1 + Math.sin(frame * 0.2) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient bg */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 40%, rgba(254,236,65,0.18) 0%, transparent 50%)',
      }} />

      {/* Shape layers - concentric circles */}
      {[1, 2, 3, 4].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          top: '50%', left: '50%',
          width: 400 + i * 200,
          height: 400 + i * 200,
          marginLeft: -(400 + i * 200) / 2,
          marginTop: -(400 + i * 200) / 2,
          border: '3px solid #000000',
          borderRadius: '50%',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.03})`,
        }} />
      ))}

      {/* Motion texture - dot grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="100%" height="100%">
        <defs>
          <pattern id="dots33" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#000000" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots33)" transform={`translate(0, ${frame * 0.3})`} />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 99);
        const angle = (frame * 0.5 + i * 20) * Math.PI / 180;
        const radius = 350 + r * 200;
        const x = 960 + Math.cos(angle) * radius;
        const y = 540 + Math.sin(angle) * radius;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 8, height: 8,
            backgroundColor: '#FEEC41',
            borderRadius: '50%',
            opacity: 0.5,
            boxShadow: '0 0 12px #FEEC41',
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(0),
      }}>
        <div style={{
          display: 'inline-block',
          fontFamily: 'Inter, Poppins',
          fontWeight: 800,
          fontSize: 34,
          color: '#FEEC41',
          backgroundColor: '#000000',
          padding: '10px 36px',
          letterSpacing: 5,
        }}>
          DON'T WAIT FOR AGE 70
        </div>
      </div>

      {/* Center clock */}
      <div style={{
        position: 'absolute',
        top: '50%', left: '50%',
        transform: `translate(-50%, -50%) scale(${sp(20, { damping: 10 })})`,
      }}>
        <svg width="540" height="540" viewBox="0 0 200 200">
          {/* Outer ring */}
          <circle cx="100" cy="100" r="92" fill="#FEEC41" stroke="#000000" strokeWidth="6" />
          {/* Tick marks */}
          {Array.from({ length: 12 }).map((_, i) => {
            const a = (i * 30) * Math.PI / 180;
            const x1 = 100 + Math.cos(a) * 80;
            const y1 = 100 + Math.sin(a) * 80;
            const x2 = 100 + Math.cos(a) * 88;
            const y2 = 100 + Math.sin(a) * 88;
            return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#000000" strokeWidth="3" />;
          })}
          {/* Number 2 highlight */}
          <text x="100" y="115" textAnchor="middle" fontFamily="Inter, Poppins" fontWeight="900" fontSize="70" fill="#000000">
            2
          </text>
          <text x="100" y="148" textAnchor="middle" fontFamily="Inter, Poppins" fontWeight="800" fontSize="14" fill="#000000" letterSpacing="2">
            YEAR CLOCK
          </text>
          {/* Hands */}
          <g transform={`rotate(${clockAngle} 100 100)`}>
            <line x1="100" y1="100" x2="100" y2="40" stroke="#000000" strokeWidth="5" strokeLinecap="round" />
          </g>
          <g transform={`rotate(${clockAngle * 12} 100 100)`}>
            <line x1="100" y1="100" x2="100" y2="55" stroke="#FF0000" strokeWidth="3" strokeLinecap="round" />
          </g>
          <circle cx="100" cy="100" r="6" fill="#000000" />
        </svg>
      </div>

      {/* Left text — TICKED */}
      <div style={{
        position: 'absolute',
        top: 470, left: 120,
        opacity: sp(60),
        transform: `translateX(${interpolate(sp(60), [0, 1], [-80, 0])}px) scale(${pulse})`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 95,
          color: '#000000',
          lineHeight: 0.9,
        }}>
          CLOCK
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 95,
          color: '#FEEC41',
          WebkitTextStroke: '4px #000000',
          lineHeight: 0.9,
        }}>
          TICKED?
        </div>
      </div>

      {/* Right text — CLAIM NOW */}
      <div style={{
        position: 'absolute',
        top: 470, right: 100,
        textAlign: 'right',
        opacity: sp(90),
        transform: `translateX(${interpolate(sp(90), [0, 1], [80, 0])}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 95,
          color: '#000000',
          lineHeight: 0.9,
        }}>
          CLAIM YOUR
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 130,
          color: '#000000',
          lineHeight: 0.9,
          letterSpacing: -3,
        }}>
          50%
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 800,
          fontSize: 36,
          color: '#000000',
          letterSpacing: 3,
        }}>
          SPOUSAL BENEFIT
        </div>
      </div>

      {/* Bottom line */}
      <div style={{
        position: 'absolute',
        bottom: 60, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(130),
      }}>
        <span style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontWeight: 700,
          fontSize: 32,
          color: '#000000',
          letterSpacing: 4,
        }}>
          // 2 YEARS = INDEPENDENCE FROM EX's TIMELINE
        </span>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene33;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene34: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Underline draw
  const underline = interpolate(sp(80, { damping: 14 }), [0, 1], [0, 1]);
  // Number 3 swoop
  const num3Scale = sp(150, { damping: 9, stiffness: 100 });

  // Notebook tilt
  const noteRot = interpolate(sp(10), [0, 1], [-8, -3]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(254,236,65,0.15) 0%, transparent 50%, rgba(0,0,0,0.25) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '15%', right: '10%',
        width: 0, height: 0,
        borderLeft: '200px solid transparent',
        borderRight: '200px solid transparent',
        borderBottom: '360px solid #000000',
        opacity: 0.05,
        transform: `rotate(${15 + frame * 0.08}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', left: '8%',
        width: 320, height: 320,
        backgroundColor: '#FEEC41',
        opacity: 0.08,
        transform: `rotate(${-frame * 0.1}deg)`,
      }} />

      {/* Lined paper texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="100%" height="100%">
        <defs>
          <pattern id="lines34" width="100" height="60" patternUnits="userSpaceOnUse">
            <line x1="0" y1="60" x2="100" y2="60" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#lines34)" />
      </svg>

      {/* Particles - ink dots */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 333);
        const r2 = seededRandom(i + 555);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920,
            top: (r2 * 1080 + frame * 0.4) % 1080,
            width: 5, height: 5,
            backgroundColor: '#000000',
            borderRadius: '50%',
            opacity: 0.3,
          }} />
        );
      })}

      {/* Top instruction */}
      <div style={{
        position: 'absolute', top: 90, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(0),
        transform: `translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 110,
          color: '#000000',
          lineHeight: 1,
          letterSpacing: -2,
        }}>
          WRITE THIS DOWN
        </div>
        <div style={{
          margin: '0 auto',
          marginTop: 6,
          height: 10,
          width: 600 * underline,
          backgroundColor: '#FEEC41',
          border: '2px solid #000000',
        }} />
      </div>

      {/* Notebook card */}
      <div style={{
        position: 'absolute',
        top: 330,
        left: '50%',
        transform: `translateX(-50%) rotate(${noteRot}deg) scale(${sp(30, { damping: 11 })})`,
        width: 1080,
        backgroundColor: '#FEEC41',
        border: '6px solid #000000',
        borderRadius: 14,
        padding: '40px 60px',
        boxShadow: '14px 14px 0 #000000',
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontWeight: 700,
          fontSize: 28,
          color: '#000000',
          letterSpacing: 3,
          opacity: 0.7,
        }}>
          RULE #02 — NOTEPAD
        </div>
        <div style={{
          marginTop: 12,
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 80,
          color: '#000000',
          lineHeight: 1,
          letterSpacing: -1.5,
        }}>
          The 2-Year Rule is your
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 110,
          color: '#000000',
          lineHeight: 1,
          letterSpacing: -3,
          marginTop: 4,
          textTransform: 'uppercase',
          textDecoration: 'underline',
          textDecorationThickness: '8px',
        }}>
          Ultimate Leverage.
        </div>
      </div>

      {/* Number 3 swooping in for next loophole */}
      <div style={{
        position: 'absolute',
        bottom: 60,
        right: 100,
        opacity: num3Scale,
        transform: `scale(${num3Scale}) rotate(${(1 - num3Scale) * 20}deg)`,
      }}>
        <div style={{
          display: 'flex',
          alignItems: 'center',
          gap: 24,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 800,
            fontSize: 30,
            color: '#FEEC41',
            backgroundColor: '#000000',
            padding: '8px 18px',
            letterSpacing: 3,
            writingMode: 'vertical-rl',
            transform: 'rotate(180deg)',
          }}>
            NEXT
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 700,
            fontSize: 42,
            color: '#000000',
            letterSpacing: 4,
            textAlign: 'right',
            lineHeight: 1,
          }}>
            LOOPHOLE
            <div style={{
              fontSize: 220,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 0.85,
              letterSpacing: -8,
            }}>
              #3
            </div>
          </div>
        </div>
      </div>

      {/* Left margin tag */}
      <div style={{
        position: 'absolute',
        left: 60,
        top: 480,
        opacity: sp(60),
        transform: `rotate(-90deg) translateX(-50%)`,
        transformOrigin: 'left top',
      }}>
        <span style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontWeight: 700,
          fontSize: 24,
          color: '#000000',
          letterSpacing: 5,
          opacity: 0.6,
        }}>
          MEMORIZE THIS
        </span>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene34;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Shield drop
  const shieldDrop = sp(50, { damping: 10, stiffness: 90 });
  // Glow
  const glow = 0.5 + Math.sin(frame * 0.12) * 0.25;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Dark gradient for dramatic reveal */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0.45) 0%, transparent 70%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '50%', left: '50%',
        width: 900, height: 900,
        marginLeft: -450, marginTop: -450,
        border: '4px solid #FEEC41',
        borderRadius: '50%',
        opacity: 0.1,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', top: '50%', left: '50%',
        width: 1200, height: 1200,
        marginLeft: -600, marginTop: -600,
        border: '3px solid #000000',
        borderRadius: '50%',
        opacity: 0.08,
        transform: `rotate(${frame * 0.1}deg)`,
        borderStyle: 'dashed',
      }} />

      {/* Radial light rays */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.12 }} width="100%" height="100%">
        {Array.from({ length: 12 }).map((_, i) => {
          const angle = (i * 30 + frame * 0.3);
          return (
            <line
              key={i}
              x1="960" y1="540"
              x2={960 + Math.cos(angle * Math.PI / 180) * 1200}
              y2={540 + Math.sin(angle * Math.PI / 180) * 1200}
              stroke="#FEEC41"
              strokeWidth="2"
            />
          );
        })}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 35);
        const angle = (i * 16 + frame * 0.5) * Math.PI / 180;
        const radius = 300 + Math.sin(frame * 0.03 + i) * 50;
        const x = 960 + Math.cos(angle) * radius;
        const y = 540 + Math.sin(angle) * radius;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 6, height: 6,
            backgroundColor: '#FEEC41',
            borderRadius: '50%',
            opacity: 0.6,
            boxShadow: '0 0 14px #FEEC41',
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(0),
      }}>
        <div style={{
          display: 'inline-block',
          fontFamily: 'Inter, Poppins',
          fontWeight: 800,
          fontSize: 34,
          color: '#000000',
          backgroundColor: '#FEEC41',
          padding: '10px 32px',
          letterSpacing: 5,
          border: '3px solid #000000',
        }}>
          LOOPHOLE #03 — UNLOCKED
        </div>
      </div>

      {/* Central SHIELD */}
      <div style={{
        position: 'absolute',
        top: '50%', left: '50%',
        transform: `translate(-50%, -50%) scale(${shieldDrop})`,
        filter: `drop-shadow(0 0 ${40 * glow}px #FEEC41)`,
      }}>
        <svg width="480" height="540" viewBox="0 0 100 110">
          <path
            d="M50 5 L90 20 L90 55 Q90 90 50 105 Q10 90 10 55 L10 20 Z"
            fill="#FEEC41"
            stroke="#000000"
            strokeWidth="4"
          />
          <path
            d="M50 15 L82 27 L82 55 Q82 82 50 95 Q18 82 18 55 L18 27 Z"
            fill="none"
            stroke="#000000"
            strokeWidth="2"
            strokeDasharray="4 3"
          />
          {/* Dollar sign center */}
          <text x="50" y="68" textAnchor="middle" fontFamily="Inter, Poppins" fontWeight="900" fontSize="48" fill="#000000">
            $
          </text>
        </svg>
      </div>

      {/* Left text */}
      <div style={{
        position: 'absolute',
        top: 380, left: 80,
        opacity: sp(90),
        transform: `translateX(${interpolate(sp(90), [0, 1], [-80, 0])}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 76,
          color: '#FEEC41',
          WebkitTextStroke: '3px #000000',
          lineHeight: 0.95,
        }}>
          THE
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 110,
          color: '#000000',
          lineHeight: 0.95,
          letterSpacing: -2,
        }}>
          SURVIVOR
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 110,
          color: '#000000',
          lineHeight: 0.95,
          letterSpacing: -2,
        }}>
          BENEFIT
        </div>
      </div>

      {/* Right text */}
      <div style={{
        position: 'absolute',
        top: 480, right: 80,
        textAlign: 'right',
        opacity: sp(120),
        transform: `translateX(${interpolate(sp(120), [0, 1], [80, 0])}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 160,
          color: '#FEEC41',
          WebkitTextStroke: '5px #000000',
          lineHeight: 0.9,
          letterSpacing: -4,
        }}>
          SHIELD
        </div>
      </div>

      {/* Bottom strip */}
      <div style={{
        position: 'absolute', bottom: 60, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(160),
      }}>
        <span style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontWeight: 700,
          fontSize: 30,
          color: '#FEEC41',
          backgroundColor: '#000000',
          padding: '10px 28px',
          letterSpacing: 4,
        }}>
          ULTIMATE FINANCIAL SAFETY NET
        </span>
      </div>

      {/* Vignette - heavy for dramatic effect */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 35%, rgba(0,0,0,0.55) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene35;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene36: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Left side fade out, right side build
  const leftScale = sp(10, { damping: 11 });
  const rightScale = sp(120, { damping: 10 });
  const questionPulse = 1 + Math.sin(frame * 0.18) * 0.05;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Split gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(254,236,65,0.12) 0%, rgba(254,236,65,0.12) 48%, rgba(0,0,0,0.25) 52%, rgba(0,0,0,0.25) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '10%', left: '15%',
        width: 280, height: 280, borderRadius: '50%',
        border: '4px solid #000000',
        opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '12%', right: '15%',
        width: 380, height: 380,
        border: '4px solid #FEEC41',
        opacity: 0.1,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />

      {/* Vertical divider */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: 80, bottom: 80,
        width: 4,
        backgroundColor: '#000000',
        opacity: interpolate(sp(60), [0, 1], [0, 0.5]),
        transform: 'translateX(-50%)',
      }} />

      {/* Texture - dots */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="dots36" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#000000" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots36)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 36);
        const r2 = seededRandom(i + 360);
        const x = (r * 1920 + frame * 0.3 * (i % 2 === 0 ? 1 : -1) + 1920) % 1920;
        const y = r2 * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 5, height: 5,
            backgroundColor: i % 2 === 0 ? '#FEEC41' : '#000000',
            borderRadius: '50%',
            opacity: 0.35,
          }} />
        );
      })}

      {/* LEFT SIDE — Alive scenario */}
      <div style={{
        position: 'absolute',
        top: 130,
        left: 80,
        width: 800,
        opacity: leftScale,
        transform: `scale(${leftScale})`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontWeight: 700,
          fontSize: 26,
          color: '#000000',
          letterSpacing: 4,
          opacity: 0.7,
        }}>
          BEFORE — WHILE ALIVE
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 200,
          color: '#000000',
          lineHeight: 1,
          letterSpacing: -6,
          marginTop: 8,
        }}>
          50%
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 800,
          fontSize: 46,
          color: '#000000',
          lineHeight: 1.05,
          marginTop: 10,
        }}>
          Spousal Kicker
        </div>
        <div style={{
          marginTop: 30,
          padding: '14px 22px',
          backgroundColor: '#FEEC41',
          border: '4px solid #000000',
          fontFamily: 'Inter, Poppins',
          fontWeight: 700,
          fontSize: 28,
          color: '#000000',
          display: 'inline-block',
        }}>
          half of their check
        </div>

        {/* Heartbeat line icon */}
        <svg width="700" height="80" viewBox="0 0 700 80" style={{ marginTop: 30 }}>
          <polyline
            points="0,40 100,40 130,40 150,15 180,65 210,40 350,40 380,40 400,15 430,65 460,40 700,40"
            fill="none"
            stroke="#000000"
            strokeWidth="4"
            strokeLinecap="round"
            strokeLinejoin="round"
          />
        </svg>
      </div>

      {/* RIGHT SIDE — Question */}
      <div style={{
        position: 'absolute',
        top: 130,
        right: 60,
        width: 820,
        textAlign: 'right',
        opacity: rightScale,
        transform: `scale(${rightScale})`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontWeight: 700,
          fontSize: 26,
          color: '#FEEC41',
          letterSpacing: 4,
          backgroundColor: '#000000',
          padding: '6px 16px',
          display: 'inline-block',
        }}>
          BUT WHAT IF...
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 90,
          color: '#000000',
          lineHeight: 0.95,
          letterSpacing: -2,
          marginTop: 20,
        }}>
          YOUR
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 130,
          color: '#FEEC41',
          WebkitTextStroke: '4px #000000',
          lineHeight: 0.9,
          letterSpacing: -3,
        }}>
          EX-SPOUSE
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 90,
          color: '#000000',
          lineHeight: 0.95,
          letterSpacing: -2,
        }}>
          PASSES AWAY
          <span style={{
            display: 'inline-block',
            transform: `scale(${questionPulse})`,
            color: '#FEEC41',
            WebkitTextStroke: '3px #000000',
            marginLeft: 10,
          }}>?</span>
        </div>

        {/* Flat line - opposite of heartbeat */}
        <svg width="700" height="80" viewBox="0 0 700 80" style={{ marginTop: 30, display: 'block', marginLeft: 'auto' }}>
          <line x1="0" y1="40" x2="700" y2="40" stroke="#000000" strokeWidth="4" strokeLinecap="round" strokeDasharray="8 6" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene36;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene37: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // 100 counter
  const counterRaw = interpolate(frame, [120, 200], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const counter = Math.round(counterRaw);

  // Stamp at 200
  const stampScale = sp(200, { damping: 8, stiffness: 180 });
  const stampShake = frame > 200 && frame < 210 ? Math.sin(frame * 3) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(254,236,65,0.22) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '5%', left: '5%',
        width: 0, height: 0,
        borderLeft: '160px solid transparent',
        borderRight: '160px solid transparent',
        borderBottom: '280px solid #FEEC41',
        opacity: 0.1,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '8%', right: '8%',
        width: 420, height: 420, borderRadius: '50%',
        border: '5px solid #000000',
        opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
      }} />

      {/* Concentric circles around center */}
      {[1, 2, 3].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          top: '50%', left: '50%',
          width: 500 + i * 180,
          height: 500 + i * 180,
          marginLeft: -(500 + i * 180) / 2,
          marginTop: -(500 + i * 180) / 2,
          border: '2px dashed #000000',
          borderRadius: '50%',
          opacity: 0.06,
          transform: `rotate(${frame * 0.2 * i}deg)`,
        }} />
      ))}

      {/* Particles bursting from center */}
      {Array.from({ length: 24 }).map((_, i) => {
        const r = seededRandom(i + 37);
        const angle = (i * 15) * Math.PI / 180;
        const burst = Math.max(0, Math.min(1, (frame - 200) / 30));
        const dist = 100 + burst * 600 + r * 200;
        const x = 960 + Math.cos(angle) * dist;
        const y = 540 + Math.sin(angle) * dist;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 10, height: 10,
            backgroundColor: '#FEEC41',
            borderRadius: '50%',
            opacity: 0.7 * (1 - burst * 0.6),
            boxShadow: '0 0 10px #FEEC41',
          }} />
        );
      })}

      {/* Top tag */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(0),
      }}>
        <div style={{
          display: 'inline-block',
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 38,
          color: '#000000',
          backgroundColor: '#FEEC41',
          padding: '12px 36px',
          letterSpacing: 5,
          border: '4px solid #000000',
        }}>
          THE RULE THAT CHANGES EVERYTHING
        </div>
      </div>

      {/* Condition row - 10 years marriage */}
      <div style={{
        position: 'absolute',
        top: 200,
        left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(40),
      }}>
        <div style={{
          display: 'inline-flex',
          alignItems: 'center',
          gap: 24,
          backgroundColor: '#000000',
          padding: '16px 40px',
          borderRadius: 8,
        }}>
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 700,
            fontSize: 38,
            color: '#FEEC41',
            letterSpacing: 2,
          }}>
            IF MARRIED
          </span>
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 80,
            color: '#FEEC41',
            lineHeight: 1,
          }}>
            10+
          </span>
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 700,
            fontSize: 38,
            color: '#FEEC41',
            letterSpacing: 2,
          }}>
            YEARS
          </span>
        </div>
      </div>

      {/* Central massive 100% */}
      <div style={{
        position: 'absolute',
        top: '52%',
        left: '50%',
        transform: `translate(-50%, -50%) scale(${stampScale}) translateX(${stampShake}px)`,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 800,
          fontSize: 38,
          color: '#000000',
          letterSpacing: 4,
        }}>
          CLAIM
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 460,
          color: '#FEEC41',
          WebkitTextStroke: '10px #000000',
          lineHeight: 0.85,
          letterSpacing: -20,
          marginTop: -10,
        }}>
          {counter}%
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 800,
          fontSize: 38,
          color: '#000000',
          letterSpacing: 4,
          marginTop: -10,
        }}>
          OF THEIR SOCIAL SECURITY
        </div>
      </div>

      {/* Side ribbons */}
      <div style={{
        position: 'absolute',
        left: 40,
        top: '50%',
        transform: 'translateY(-50%) rotate(-90deg)',
        transformOrigin: 'left center',
        opacity: sp(80),
      }}>
        <span style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontWeight: 700,
          fontSize: 24,
          color: '#000000',
          letterSpacing: 6,
          opacity: 0.5,
        }}>
          AS A SURVIVOR
        </span>
      </div>
      <div style={{
        position: 'absolute',
        right: 40,
        top: '50%',
        transform: 'translateY(-50%) rotate(90deg)',
        transformOrigin: 'right center',
        opacity: sp(80),
      }}>
        <span style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontWeight: 700,
          fontSize: 24,
          color: '#000000',
          letterSpacing: 6,
          opacity: 0.5,
        }}>
          NOT FIFTY — FULL
        </span>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene37;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene38: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Strike through 50%
  const strikeWidth = interpolate(sp(60, { damping: 14 }), [0, 1], [0, 1]);
  // 100% slam in
  const slam = sp(110, { damping: 8, stiffness: 160 });
  const slamShake = frame > 110 && frame < 125 ? Math.sin(frame * 2.5) * 5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(0,0,0,0.18) 0%, transparent 50%, rgba(254,236,65,0.18) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '20%', left: '40%',
        width: 600, height: 600, borderRadius: '50%',
        backgroundColor: '#FEEC41',
        opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', right: '5%',
        width: 380, height: 380,
        border: '4px solid #000000',
        opacity: 0.07,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />

      {/* Grid texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
        <defs>
          <pattern id="g38" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#000000" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#g38)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 38);
        const r2 = seededRandom(i + 380);
        const x = (r * 1920 + frame * 0.25 + 1920) % 1920;
        const y = (r2 * 1080 - frame * 0.3 + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 5, height: 5,
            backgroundColor: i % 2 ? '#FEEC41' : '#000000',
            borderRadius: '50%',
            opacity: 0.3,
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 80, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(0),
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontWeight: 700,
          fontSize: 32,
          color: '#000000',
          letterSpacing: 6,
          opacity: 0.7,
        }}>
          // KEY DISTINCTION
        </div>
      </div>

      {/* 50% with strikethrough */}
      <div style={{
        position: 'absolute',
        top: 200,
        left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(20),
      }}>
        <div style={{ position: 'relative', display: 'inline-block' }}>
          <span style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 240,
            color: '#000000',
            opacity: 0.4,
            lineHeight: 1,
            letterSpacing: -8,
          }}>
            NOT 50%
          </span>
          {/* Strike line */}
          <div style={{
            position: 'absolute',
            top: '52%',
            left: 0,
            height: 14,
            width: `${strikeWidth * 100}%`,
            backgroundColor: '#FF0000',
            transform: 'translateY(-50%) rotate(-3deg)',
          }} />
        </div>
      </div>

      {/* 100% SLAM */}
      <div style={{
        position: 'absolute',
        top: 460,
        left: 0, right: 0,
        textAlign: 'center',
        opacity: slam,
        transform: `scale(${slam * 1.05}) translateX(${slamShake}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 380,
          color: '#FEEC41',
          WebkitTextStroke: '10px #000000',
          lineHeight: 0.85,
          letterSpacing: -12,
          textShadow: '14px 14px 0 #000000',
        }}>
          100%
        </div>
      </div>

      {/* Bottom label */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(150),
      }}>
        <span style={{
          display: 'inline-block',
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 54,
          color: '#FEEC41',
          backgroundColor: '#000000',
          padding: '14px 40px',
          letterSpacing: 3,
          border: '4px solid #FEEC41',
        }}>
          FULL SURVIVOR BENEFIT
        </span>
      </div>

      {/* Side arrows pointing to 100 */}
      {[0, 1].map((side) => (
        <div key={side} style={{
          position: 'absolute',
          top: 580,
          [side === 0 ? 'left' : 'right']: 80,
          opacity: sp(140) * 0.8,
          transform: `scale(${1 + Math.sin(frame * 0.2) * 0.1}) ${side === 0 ? '' : 'scaleX(-1)'}`,
        }}>
          <svg width="180" height="80" viewBox="0 0 180 80">
            <path d="M0 40 L150 40 M150 40 L130 25 M150 40 L130 55" stroke="#000000" strokeWidth="8" strokeLinecap="round" fill="none" />
          </svg>
        </div>
      ))}

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene38;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene39: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter for $1,200
  const c1Raw = interpolate(frame, [30, 90], [0, 1200], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const c1 = Math.round(c1Raw);
  // Counter for $3,000
  const c2Raw = interpolate(frame, [130, 200], [0, 3000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const c2 = Math.round(c2Raw);

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(0,0,0,0.18) 0%, transparent 50%, rgba(254,236,65,0.2) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '8%', left: '5%',
        width: 280, height: 280,
        backgroundColor: '#000000',
        opacity: 0.05,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '8%', right: '5%',
        width: 380, height: 380,
        borderRadius: '50%',
        backgroundColor: '#FEEC41',
        opacity: 0.08,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
      }} />

      {/* Receipt-line texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="rec39" width="100" height="50" patternUnits="userSpaceOnUse">
            <line x1="0" y1="50" x2="100" y2="50" stroke="#000000" strokeWidth="1" strokeDasharray="6 4" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#rec39)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 39);
        const r2 = seededRandom(i + 390);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920,
            top: (r2 * 1080 + frame * (0.3 + r * 0.4)) % 1080,
            width: 4, height: 4,
            backgroundColor: '#000000',
            opacity: 0.3,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* Header */}
      <div style={{
        position: 'absolute', top: 60, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(0),
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontWeight: 700,
          fontSize: 30,
          color: '#000000',
          letterSpacing: 6,
          opacity: 0.7,
        }}>
          // EXAMPLE — REAL NUMBERS
        </div>
      </div>

      {/* Two receipt cards */}
      <div style={{
        position: 'absolute',
        top: 180,
        left: 0, right: 0,
        display: 'flex',
        justifyContent: 'center',
        gap: 100,
        padding: '0 100px',
      }}>
        {/* YOU receipt */}
        <div style={{
          width: 640,
          backgroundColor: '#000000',
          border: '6px solid #000000',
          borderRadius: 14,
          padding: 40,
          transform: `translateY(${interpolate(sp(10, { damping: 10 }), [0, 1], [80, 0])}px) rotate(-2deg)`,
          opacity: sp(10),
          boxShadow: '12px 12px 0 #FEEC41',
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 800,
            fontSize: 32,
            color: '#FEEC41',
            letterSpacing: 4,
            borderBottom: '3px dashed #FEEC41',
            paddingBottom: 12,
          }}>
            YOUR CHECK
          </div>
          <div style={{
            marginTop: 28,
            fontFamily: 'Inter, Poppins',
            fontWeight: 700,
            fontSize: 28,
            color: '#FEEC41',
            opacity: 0.7,
          }}>
            Based on your record
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 170,
            color: '#FEEC41',
            lineHeight: 1,
            letterSpacing: -4,
            marginTop: 8,
          }}>
            ${c1.toLocaleString()}
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 700,
            fontSize: 32,
            color: '#FEEC41',
            opacity: 0.8,
          }}>
            per month
          </div>
        </div>

        {/* EX receipt */}
        <div style={{
          width: 640,
          backgroundColor: '#FEEC41',
          border: '6px solid #000000',
          borderRadius: 14,
          padding: 40,
          transform: `translateY(${interpolate(sp(100, { damping: 10 }), [0, 1], [80, 0])}px) rotate(2deg)`,
          opacity: sp(100),
          boxShadow: '12px 12px 0 #000000',
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 800,
            fontSize: 32,
            color: '#000000',
            letterSpacing: 4,
            borderBottom: '3px dashed #000000',
            paddingBottom: 12,
            display: 'flex',
            justifyContent: 'space-between',
          }}>
            <span>EX's CHECK</span>
            <span style={{ fontSize: 22, padding: '2px 10px', backgroundColor: '#000000', color: '#FEEC41' }}>HIGH EARNER</span>
          </div>
          <div style={{
            marginTop: 28,
            fontFamily: 'Inter, Poppins',
            fontWeight: 700,
            fontSize: 28,
            color: '#000000',
            opacity: 0.7,
          }}>
            Based on their record
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 170,
            color: '#000000',
            lineHeight: 1,
            letterSpacing: -4,
            marginTop: 8,
          }}>
            ${c2.toLocaleString()}
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 700,
            fontSize: 32,
            color: '#000000',
            opacity: 0.8,
          }}>
            per month
          </div>
        </div>
      </div>

      {/* Bottom hint */}
      <div style={{
        position: 'absolute',
        bottom: 70,
        left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(220),
      }}>
        <span style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 800,
          fontSize: 42,
          color: '#000000',
          letterSpacing: 2,
        }}>
          Same person. Same household. <span style={{ color: '#000000', backgroundColor: '#FEEC41', padding: '4px 14px' }}>HUGE gap.</span>
        </span>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene39;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40',
  durationInSeconds: 9,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene40: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Bar climb
  const oldBar = sp(30, { damping: 14 });
  const newBar = sp(110, { damping: 12, stiffness: 100 });

  // Triple text impact
  const tripleScale = sp(180, { damping: 8, stiffness: 160 });
  const tripleShake = frame > 180 && frame < 195 ? Math.sin(frame * 3) * 6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#9FA1FF', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 100%, rgba(254,236,65,0.3) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: '5%', right: '8%',
        width: 360, height: 360, borderRadius: '50%',
        border: '5px solid #FEEC41',
        opacity: 0.1,
        transform: `scale(${1 + Math.sin(frame * 0.05) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-10%', left: '-5%',
        width: 500, height: 500,
        backgroundColor: '#000000',
        opacity: 0.06,
        transform: `rotate(${45 + frame * 0.08}deg)`,
      }} />

      {/* Vertical pulse lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
        <defs>
          <pattern id="v40" width="80" height="80" patternUnits="userSpaceOnUse">
            <line x1="40" y1="0" x2="40" y2="80" stroke="#000000" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#v40)" transform={`translate(${frame * 0.4}, 0)`} />
      </svg>

      {/* Rising particles */}
      {Array.from({ length: 26 }).map((_, i) => {
        const r = seededRandom(i + 40);
        const r2 = seededRandom(i + 400);
        const x = r * 1920;
        const y = (1080 - (frame * (0.8 + r * 1.2) + r2 * 1080) % 1200);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 6 + r * 4, height: 6 + r * 4,
            backgroundColor: '#FEEC41',
            borderRadius: '50%',
            opacity: 0.5,
            boxShadow: '0 0 10px #FEEC41',
          }} />
        );
      })}

      {/* Header */}
      <div style={{
        position: 'absolute', top: 60, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(0),
      }}>
        <div style={{
          display: 'inline-block',
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 48,
          color: '#FEEC41',
          backgroundColor: '#000000',
          padding: '12px 38px',
          letterSpacing: 4,
          border: '4px solid #FEEC41',
        }}>
          INCOME SWITCH
        </div>
      </div>

      {/* Two-bar climb chart */}
      <div style={{
        position: 'absolute',
        bottom: 240,
        left: 200,
        right: 200,
        height: 540,
        display: 'flex',
        alignItems: 'flex-end',
        justifyContent: 'center',
        gap: 100,
      }}>
        {/* Old bar - $1,200 */}
        <div style={{ textAlign: 'center', width: 320 }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 800,
            fontSize: 28,
            color: '#000000',
            marginBottom: 10,
            letterSpacing: 3,
            opacity: 0.7,
          }}>
            BEFORE
          </div>
          <div style={{
            height: 180 * oldBar,
            backgroundColor: '#000000',
            display: 'flex',
            alignItems: 'flex-start',
            justifyContent: 'center',
            paddingTop: 20,
            border: '4px solid #000000',
            opacity: 0.85,
          }}>
            <span style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 900,
              fontSize: 44,
              color: '#FEEC41',
            }}>
              $1,200
            </span>
          </div>
        </div>

        {/* Arrow */}
        <div style={{
          alignSelf: 'center',
          fontSize: 120,
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          color: '#000000',
          opacity: sp(80),
          transform: `translateY(-40px) scale(${0.9 + Math.sin(frame * 0.2) * 0.1})`,
        }}>
          ↗
        </div>

        {/* New bar - $3,000 */}
        <div style={{ textAlign: 'center', width: 320 }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 800,
            fontSize: 28,
            color: '#000000',
            marginBottom: 10,
            letterSpacing: 3,
          }}>
            AFTER (SURVIVOR)
          </div>
          <div style={{
            height: 460 * newBar,
            backgroundColor: '#FEEC41',
            border: '5px solid #000000',
            display: 'flex',
            alignItems: 'flex-start',
            justifyContent: 'center',
            paddingTop: 28,
            boxShadow: '10px 10px 0 #000000',
          }}>
            <span style={{
              fontFamily: 'Inter, Poppins',
              fontWeight: 900,
              fontSize: 80,
              color: '#000000',
            }}>
              $3,000
            </span>
          </div>
        </div>
      </div>

      {/* TRIPLES OVERNIGHT */}
      <div style={{
        position: 'absolute',
        bottom: 60,
        left: 0, right: 0,
        textAlign: 'center',
        transform: `scale(${tripleScale}) translateX(${tripleShake}px)`,
        opacity: tripleScale,
      }}>
        <span style={{
          fontFamily: 'Inter, Poppins',
          fontWeight: 900,
          fontSize: 100,
          color: '#000000',
          letterSpacing: -2,
          lineHeight: 1,
        }}>
          INCOME <span style={{ color: '#FEEC41', WebkitTextStroke: '4px #000000' }}>TRIPLES</span> OVERNIGHT
        </span>
      </div>

      {/* Multiplier badge */}
      <div style={{
        position: 'absolute',
        top: 200,
        right: 80,
        opacity: sp(150),
        transform: `rotate(${(1 - sp(150)) * 25}deg) scale(${sp(150)})`,
      }}>
        <div style={{
          width: 200, height: 200,
          backgroundColor: '#FEEC41',
          border: '6px solid #000000',
          borderRadius: '50%',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          boxShadow: '10px 10px 0 #000000',
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 900,
            fontSize: 100,
            color: '#000000',
            lineHeight: 1,
          }}>
            3×
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontWeight: 800,
            fontSize: 20,
            color: '#000000',
            letterSpacing: 3,
            marginTop: -4,
          }}>
            MULTIPLIER
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins',
        fontSize: 22, fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene40;
```

