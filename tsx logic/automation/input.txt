```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31_Section504Intro',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene31_Section504Intro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title reveal
  const titleY = interpolate(sp(5), [0, 1], [60, 0]);
  const titleOp = sp(5);

  // Stamp animation
  const stampScale = sp(25, { damping: 8, stiffness: 180 });
  const stampRot = interpolate(sp(25), [0, 1], [-25, -12]);

  // House structure
  const houseOp = sp(45);
  const houseScale = sp(45, { damping: 14 });

  // Damage labels stagger
  const dmg1 = sp(75);
  const dmg2 = sp(95);
  const dmg3 = sp(115);

  // Cost reveal
  const costOp = sp(145, { damping: 10 });
  const costScale = sp(145, { damping: 9, stiffness: 140 });

  // Camera subtle zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 30% 20%, rgba(96,153,102,0.18) 0%, transparent 55%), radial-gradient(ellipse at 80% 90%, rgba(147,181,198,0.12) 0%, transparent 60%)',
        }}
      />

      {/* Shape Layer — drifting geometric forms */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            top: '-8%',
            right: '-6%',
            width: 520,
            height: 520,
            borderRadius: '50%',
            border: '2px solid #609966',
            opacity: 0.06,
            transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '-10%',
            left: '-5%',
            width: 600,
            height: 600,
            background: '#93B5C6',
            opacity: 0.04,
            transform: `rotate(${-frame * 0.1}deg)`,
            clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
          }}
        />
        <div
          style={{
            position: 'absolute',
            top: '40%',
            left: '8%',
            width: 220,
            height: 220,
            border: '1.5px solid #609966',
            opacity: 0.08,
            transform: `rotate(${frame * 0.2}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture — diagonal grid */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(45deg, transparent 0, transparent 60px, rgba(147,181,198,0.04) 60px, rgba(147,181,198,0.04) 61px)`,
          opacity: 0.7,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 22 }).map((_, i) => {
          const seed = seededRandom(i + 31);
          const x = seed * 1920;
          const y = (seededRandom(i + 100) * 1080 + frame * (0.4 + seed * 0.6)) % 1080;
          const size = 2 + seed * 4;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: y,
                width: size,
                height: size,
                borderRadius: '50%',
                background: i % 2 === 0 ? '#609966' : '#93B5C6',
                opacity: 0.35,
                boxShadow: '0 0 10px rgba(96,153,102,0.5)',
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>
        {/* Top Title */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
            transform: `translateY(${titleY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 600,
              color: '#93B5C6',
              letterSpacing: 6,
              textTransform: 'uppercase',
              marginBottom: 12,
            }}
          >
            Program Number Five
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 102,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: -2,
              lineHeight: 1,
            }}
          >
            SECTION <span style={{ color: '#609966' }}>504</span>
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 44,
              fontWeight: 500,
              color: '#93B5C6',
              marginTop: 10,
              letterSpacing: 2,
            }}
          >
            Home Repair Program
          </div>
        </div>

        {/* Rotating Stamp */}
        <div
          style={{
            position: 'absolute',
            top: 110,
            right: 140,
            transform: `scale(${stampScale}) rotate(${stampRot}deg)`,
            transformOrigin: 'center',
          }}
        >
          <div
            style={{
              border: '4px solid #609966',
              padding: '14px 28px',
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              fontWeight: 800,
              color: '#609966',
              letterSpacing: 3,
              borderRadius: 8,
              background: 'rgba(57,62,70,0.6)',
            }}
          >
            USDA · GRANT
          </div>
        </div>

        {/* House Diagram - Center Left */}
        <div
          style={{
            position: 'absolute',
            top: 430,
            left: 140,
            width: 540,
            height: 460,
            opacity: houseOp,
            transform: `scale(${houseScale})`,
            transformOrigin: 'center',
          }}
        >
          <svg width="540" height="460" viewBox="0 0 540 460">
            {/* Roof - leaking */}
            <polygon points="270,40 60,200 480,200" fill="none" stroke="#93B5C6" strokeWidth="4" />
            <polygon points="270,40 60,200 480,200" fill="#609966" opacity="0.08" />
            {/* Crack in roof */}
            <polyline
              points="200,120 220,140 210,155 235,170 225,185"
              fill="none"
              stroke="#FFC107"
              strokeWidth="3"
              opacity={interpolate(dmg1, [0, 1], [0, 1])}
            />
            {/* Water drops from roof */}
            {[0, 1, 2].map((d) => {
              const drop = (frame * 3 + d * 30) % 90;
              return (
                <ellipse
                  key={d}
                  cx={210 + d * 12}
                  cy={195 + drop}
                  rx="4"
                  ry="7"
                  fill="#93B5C6"
                  opacity={dmg1 * (1 - drop / 90)}
                />
              );
            })}
            {/* House body */}
            <rect x="100" y="200" width="340" height="220" fill="none" stroke="#93B5C6" strokeWidth="4" />
            <rect x="100" y="200" width="340" height="220" fill="#F5F5F5" opacity="0.04" />
            {/* Door */}
            <rect x="240" y="300" width="70" height="120" fill="none" stroke="#609966" strokeWidth="3" />
            <circle cx="298" cy="360" r="3" fill="#609966" />
            {/* Windows */}
            <rect x="140" y="240" width="70" height="60" fill="none" stroke="#609966" strokeWidth="3" />
            <rect x="340" y="240" width="70" height="60" fill="none" stroke="#609966" strokeWidth="3" />
            <line x1="175" y1="240" x2="175" y2="300" stroke="#609966" strokeWidth="2" />
            <line x1="140" y1="270" x2="210" y2="270" stroke="#609966" strokeWidth="2" />
            {/* Steep stairs */}
            <g opacity={dmg3}>
              <line x1="100" y1="420" x2="50" y2="380" stroke="#FFC107" strokeWidth="3" />
              <line x1="80" y1="400" x2="50" y2="400" stroke="#FFC107" strokeWidth="3" />
              <line x1="65" y1="390" x2="65" y2="420" stroke="#FFC107" strokeWidth="3" />
            </g>
            {/* Plumbing pipe leak */}
            <g opacity={dmg2}>
              <line x1="440" y1="380" x2="490" y2="380" stroke="#FFC107" strokeWidth="4" />
              <line x1="490" y1="380" x2="490" y2="430" stroke="#FFC107" strokeWidth="4" />
              {[0, 1].map((p) => {
                const pdrop = (frame * 4 + p * 25) % 60;
                return (
                  <circle key={p} cx={490} cy={430 + pdrop} r="3" fill="#93B5C6" opacity={1 - pdrop / 60} />
                );
              })}
            </g>
          </svg>
        </div>

        {/* Damage Labels */}
        <div
          style={{
            position: 'absolute',
            top: 470,
            left: 720,
            opacity: dmg1,
            transform: `translateX(${interpolate(dmg1, [0, 1], [-30, 0])}px)`,
          }}
        >
          <div style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
            <div
              style={{
                width: 14,
                height: 14,
                background: '#FFC107',
                borderRadius: '50%',
                boxShadow: '0 0 14px #FFC107',
              }}
            />
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 46,
                fontWeight: 700,
                color: '#F5F5F5',
              }}
            >
              Leaking Roof
            </div>
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            top: 560,
            left: 720,
            opacity: dmg2,
            transform: `translateX(${interpolate(dmg2, [0, 1], [-30, 0])}px)`,
          }}
        >
          <div style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
            <div
              style={{
                width: 14,
                height: 14,
                background: '#FFC107',
                borderRadius: '50%',
                boxShadow: '0 0 14px #FFC107',
              }}
            />
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 46,
                fontWeight: 700,
                color: '#F5F5F5',
              }}
            >
              Failing Plumbing
            </div>
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            top: 650,
            left: 720,
            opacity: dmg3,
            transform: `translateX(${interpolate(dmg3, [0, 1], [-30, 0])}px)`,
          }}
        >
          <div style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
            <div
              style={{
                width: 14,
                height: 14,
                background: '#FFC107',
                borderRadius: '50%',
                boxShadow: '0 0 14px #FFC107',
              }}
            />
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 46,
                fontWeight: 700,
                color: '#F5F5F5',
              }}
            >
              Dangerous Stairs
            </div>
          </div>
        </div>

        {/* Cost Tag - Right Bottom */}
        <div
          style={{
            position: 'absolute',
            top: 770,
            left: 720,
            opacity: costOp,
            transform: `scale(${costScale})`,
            transformOrigin: 'left center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              fontWeight: 500,
              color: '#93B5C6',
              letterSpacing: 3,
              textTransform: 'uppercase',
              marginBottom: 4,
            }}
          >
            Can't Afford the Fix
          </div>
          <div
            style={{
              display: 'flex',
              alignItems: 'baseline',
              gap: 12,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 144,
                fontWeight: 900,
                color: '#609966',
                letterSpacing: -3,
                lineHeight: 1,
                textShadow: '0 0 30px rgba(96,153,102,0.4)',
              }}
            >
              $10,000
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 32,
                fontWeight: 600,
                color: '#93B5C6',
              }}
            >
              repair bill
            </div>
          </div>
          {/* Strike line */}
          <div
            style={{
              width: interpolate(sp(170), [0, 1], [0, 380]),
              height: 6,
              background: '#FFC107',
              marginTop: -90,
              marginLeft: 10,
              transform: 'rotate(-8deg)',
              opacity: sp(170),
            }}
          />
        </div>

        {/* House Poor label */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: 140,
            opacity: sp(60),
            display: 'flex',
            alignItems: 'center',
            gap: 14,
          }}
        >
          <div
            style={{
              width: 6,
              height: 50,
              background: '#FFC107',
            }}
          />
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 36,
              fontWeight: 700,
              color: '#FFC107',
              fontStyle: 'italic',
              letterSpacing: 2,
            }}
          >
            "House Poor" Seniors
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene31_Section504Intro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32_GrantNotLoan',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32_GrantNotLoan: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Badge
  const badgeOp = sp(5);
  const badgeScale = sp(5, { damping: 9 });

  // VS comparison
  const loanOp = sp(35);
  const loanX = interpolate(sp(35), [0, 1], [-80, 0]);

  const grantOp = sp(60);
  const grantX = interpolate(sp(60), [0, 1], [80, 0]);

  // VS divider
  const vsScale = sp(85, { damping: 8, stiffness: 200 });

  // Big "NO PAYBACK" reveal
  const noPaybackOp = sp(115);
  const noPaybackScale = sp(115, { damping: 10 });

  // Eligibility line
  const eligOp = sp(165);

  // Camera pan
  const camPan = interpolate(frame, [0, durationInFrames], [-15, 15]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(96,153,102,0.10) 0%, transparent 50%, rgba(147,181,198,0.10) 100%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            top: '15%',
            left: '-8%',
            width: 480,
            height: 480,
            background: '#609966',
            opacity: 0.05,
            transform: `rotate(${frame * 0.08}deg) scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '5%',
            right: '-6%',
            width: 420,
            height: 420,
            border: '2px solid #93B5C6',
            borderRadius: '50%',
            opacity: 0.07,
            transform: `scale(${1 + Math.cos(frame * 0.03) * 0.04})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            top: '50%',
            right: '15%',
            width: 180,
            height: 180,
            background: '#FFC107',
            opacity: 0.04,
            transform: `rotate(${frame * 0.25}deg)`,
            clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
          }}
        />
      </AbsoluteFill>

      {/* Texture - dots */}
      <AbsoluteFill
        style={{
          backgroundImage: `radial-gradient(circle, rgba(147,181,198,0.08) 1.5px, transparent 1.5px)`,
          backgroundSize: '40px 40px',
          opacity: 0.5,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 18 }).map((_, i) => {
          const seed = seededRandom(i + 60);
          const x = (seed * 1920 + frame * 0.3) % 1920;
          const y = seededRandom(i + 200) * 1080;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: y,
                width: 3,
                height: 3,
                borderRadius: '50%',
                background: '#609966',
                opacity: 0.4,
                boxShadow: '0 0 8px #609966',
              }}
            />
          );
        })}
      </AbsoluteFill>

      <AbsoluteFill style={{ transform: `translateX(${camPan}px)` }}>
        {/* Top USDA Badge */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: badgeOp,
            transform: `scale(${badgeScale})`,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '10px 36px',
              border: '3px solid #93B5C6',
              borderRadius: 50,
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              fontWeight: 700,
              color: '#93B5C6',
              letterSpacing: 5,
              background: 'rgba(57,62,70,0.5)',
            }}
          >
            U.S. DEPARTMENT OF AGRICULTURE
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 88,
              fontWeight: 900,
              color: '#F5F5F5',
              marginTop: 18,
              letterSpacing: -1,
              lineHeight: 1,
            }}
          >
            Section 504 Program
          </div>
        </div>

        {/* LOAN vs GRANT comparison */}
        <div
          style={{
            position: 'absolute',
            top: 360,
            left: 120,
            width: 700,
            opacity: loanOp,
            transform: `translateX(${loanX}px)`,
          }}
        >
          <div
            style={{
              border: '2px solid #9E9E9E',
              borderRadius: 18,
              padding: '40px 36px',
              background: 'rgba(28,28,28,0.4)',
              position: 'relative',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 30,
                fontWeight: 600,
                color: '#9E9E9E',
                letterSpacing: 4,
                textTransform: 'uppercase',
                marginBottom: 16,
              }}
            >
              Typical Help
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 96,
                fontWeight: 900,
                color: '#9E9E9E',
                letterSpacing: -2,
                lineHeight: 1,
                textDecoration: 'line-through',
                textDecorationThickness: '6px',
                textDecorationColor: '#FFC107',
              }}
            >
              LOAN
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 28,
                fontWeight: 500,
                color: '#9E9E9E',
                marginTop: 14,
              }}
            >
              Must pay back + interest
            </div>
          </div>
        </div>

        {/* VS Circle */}
        <div
          style={{
            position: 'absolute',
            top: 470,
            left: 870,
            transform: `scale(${vsScale}) rotate(${Math.sin(frame * 0.05) * 5}deg)`,
            transformOrigin: 'center',
          }}
        >
          <div
            style={{
              width: 130,
              height: 130,
              borderRadius: '50%',
              background: '#FFC107',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Inter, Poppins',
              fontSize: 52,
              fontWeight: 900,
              color: '#393E46',
              letterSpacing: -1,
              boxShadow: '0 0 40px rgba(255,193,7,0.5)',
            }}
          >
            VS
          </div>
        </div>

        {/* GRANT block */}
        <div
          style={{
            position: 'absolute',
            top: 360,
            right: 120,
            width: 700,
            opacity: grantOp,
            transform: `translateX(${grantX}px)`,
          }}
        >
          <div
            style={{
              border: '3px solid #609966',
              borderRadius: 18,
              padding: '40px 36px',
              background: 'rgba(96,153,102,0.12)',
              position: 'relative',
              boxShadow: '0 0 40px rgba(96,153,102,0.25)',
            }}
          >
            {/* Corner check */}
            <div
              style={{
                position: 'absolute',
                top: -22,
                right: -22,
                width: 60,
                height: 60,
                borderRadius: '50%',
                background: '#609966',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
              }}
            >
              <svg width="34" height="34" viewBox="0 0 24 24">
                <polyline points="4,12 10,18 20,6" fill="none" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 30,
                fontWeight: 600,
                color: '#609966',
                letterSpacing: 4,
                textTransform: 'uppercase',
                marginBottom: 16,
              }}
            >
              Section 504 Gives You
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 116,
                fontWeight: 900,
                color: '#F5F5F5',
                letterSpacing: -3,
                lineHeight: 1,
              }}
            >
              GRANT
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 28,
                fontWeight: 600,
                color: '#93B5C6',
                marginTop: 14,
              }}
            >
              Free money. Yours to keep.
            </div>
          </div>
        </div>

        {/* Big NO PAYBACK banner */}
        <div
          style={{
            position: 'absolute',
            bottom: 200,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: noPaybackOp,
            transform: `scale(${noPaybackScale})`,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '16px 60px',
              background: '#609966',
              borderRadius: 12,
              fontFamily: 'Inter, Poppins',
              fontSize: 64,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: 4,
              textTransform: 'uppercase',
              boxShadow: '0 0 50px rgba(96,153,102,0.5)',
              transform: `translateY(${Math.sin(frame * 0.08) * 4}px)`,
            }}
          >
            You Do NOT Pay It Back
          </div>
        </div>

        {/* Eligibility row */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: eligOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 500,
              color: '#93B5C6',
              letterSpacing: 4,
              textTransform: 'uppercase',
            }}
          >
            Eligibility: Very Low-Income Seniors · Rural Areas · Town Edges
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene32_GrantNotLoan;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33_TenThousandGrant',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene33_TenThousandGrant: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Count-up to 10000
  const countProgress = interpolate(sp(20), [0, 1], [0, 1]);
  const count = Math.floor(countProgress * 10000);

  // Label
  const labelOp = sp(10);

  // Big number
  const bigOp = sp(15);
  const bigScale = sp(15, { damping: 9, stiffness: 100 });

  // Three uses cards
  const card1 = sp(95);
  const card2 = sp(115);
  const card3 = sp(135);

  // Combine line
  const combineOp = sp(180);

  // Pulse
  const pulse = 1 + Math.sin(frame * 0.1) * 0.02;

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 35%, rgba(96,153,102,0.22) 0%, transparent 60%)',
        }}
      />

      {/* Shape Layer - concentric rings around center */}
      <AbsoluteFill>
        {[0, 1, 2].map((r) => (
          <div
            key={r}
            style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              width: 600 + r * 200,
              height: 600 + r * 200,
              borderRadius: '50%',
              border: '1.5px solid #609966',
              opacity: 0.06 - r * 0.015,
              transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.02 + r) * 0.04})`,
            }}
          />
        ))}
        <div
          style={{
            position: 'absolute',
            top: '8%',
            right: '8%',
            width: 200,
            height: 200,
            border: '2px solid #93B5C6',
            opacity: 0.1,
            transform: `rotate(${frame * 0.3}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Texture - radial lines */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(0deg, transparent 0, transparent 80px, rgba(96,153,102,0.04) 80px, rgba(96,153,102,0.04) 81px)`,
        }}
      />

      {/* Floating coins/particles */}
      <AbsoluteFill>
        {Array.from({ length: 28 }).map((_, i) => {
          const seed = seededRandom(i + 90);
          const x = seed * 1920;
          const y = (seededRandom(i + 300) * 1080 - frame * (0.5 + seed * 0.8)) % 1080;
          const adjustedY = y < 0 ? y + 1080 : y;
          const size = 4 + seed * 5;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: adjustedY,
                width: size,
                height: size,
                borderRadius: '50%',
                background: '#FFC107',
                opacity: 0.35,
                boxShadow: '0 0 10px #FFC107',
              }}
            />
          );
        })}
      </AbsoluteFill>

      <AbsoluteFill>
        {/* Top Label */}
        <div
          style={{
            position: 'absolute',
            top: 110,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: labelOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 600,
              color: '#93B5C6',
              letterSpacing: 8,
              textTransform: 'uppercase',
            }}
          >
            Maximum Grant Amount
          </div>
        </div>

        {/* HUGE Counter */}
        <div
          style={{
            position: 'absolute',
            top: 210,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: bigOp,
            transform: `scale(${bigScale * pulse})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 340,
              fontWeight: 900,
              color: '#609966',
              letterSpacing: -10,
              lineHeight: 1,
              textShadow: '0 0 60px rgba(96,153,102,0.5)',
            }}
          >
            ${count.toLocaleString()}
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 42,
              fontWeight: 700,
              color: '#F5F5F5',
              letterSpacing: 6,
              marginTop: -10,
              textTransform: 'uppercase',
            }}
          >
            Free Grant Money
          </div>
        </div>

        {/* Three Uses Cards */}
        <div
          style={{
            position: 'absolute',
            top: 700,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 50,
          }}
        >
          {/* Card 1 - New Roof */}
          <div
            style={{
              width: 360,
              opacity: card1,
              transform: `translateY(${interpolate(card1, [0, 1], [50, 0])}px)`,
              padding: '32px 28px',
              border: '2px solid #93B5C6',
              borderRadius: 16,
              background: 'rgba(147,181,198,0.06)',
              textAlign: 'center',
            }}
          >
            <svg width="80" height="80" viewBox="0 0 80 80" style={{ marginBottom: 14 }}>
              <polygon points="40,10 10,40 70,40" fill="none" stroke="#609966" strokeWidth="4" strokeLinejoin="round" />
              <rect x="20" y="40" width="40" height="30" fill="none" stroke="#609966" strokeWidth="3" />
              <rect x="34" y="52" width="12" height="18" fill="#609966" opacity="0.4" />
            </svg>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 42,
                fontWeight: 800,
                color: '#F5F5F5',
                letterSpacing: -0.5,
              }}
            >
              New Roof
            </div>
          </div>

          {/* Card 2 - Furnace */}
          <div
            style={{
              width: 360,
              opacity: card2,
              transform: `translateY(${interpolate(card2, [0, 1], [50, 0])}px)`,
              padding: '32px 28px',
              border: '2px solid #93B5C6',
              borderRadius: 16,
              background: 'rgba(147,181,198,0.06)',
              textAlign: 'center',
            }}
          >
            <svg width="80" height="80" viewBox="0 0 80 80" style={{ marginBottom: 14 }}>
              <rect x="18" y="18" width="44" height="50" fill="none" stroke="#609966" strokeWidth="4" rx="3" />
              <circle cx="40" cy="40" r="10" fill="none" stroke="#609966" strokeWidth="3" />
              <circle cx="40" cy="40" r="4" fill="#FFC107" />
              <line x1="28" y1="58" x2="52" y2="58" stroke="#609966" strokeWidth="3" />
              <path
                d={`M 36 10 Q 40 ${6 + Math.sin(frame * 0.2) * 2} 44 10`}
                fill="none"
                stroke="#FFC107"
                strokeWidth="3"
                strokeLinecap="round"
              />
            </svg>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 42,
                fontWeight: 800,
                color: '#F5F5F5',
                letterSpacing: -0.5,
              }}
            >
              Fixed Furnace
            </div>
          </div>

          {/* Card 3 - Wheelchair Ramps */}
          <div
            style={{
              width: 360,
              opacity: card3,
              transform: `translateY(${interpolate(card3, [0, 1], [50, 0])}px)`,
              padding: '32px 28px',
              border: '2px solid #93B5C6',
              borderRadius: 16,
              background: 'rgba(147,181,198,0.06)',
              textAlign: 'center',
            }}
          >
            <svg width="80" height="80" viewBox="0 0 80 80" style={{ marginBottom: 14 }}>
              <circle cx="30" cy="50" r="14" fill="none" stroke="#609966" strokeWidth="3" />
              <circle cx="30" cy="50" r="4" fill="#609966" />
              <line x1="30" y1="36" x2="30" y2="20" stroke="#609966" strokeWidth="3" />
              <circle cx="30" cy="18" r="5" fill="#609966" />
              <line x1="30" y1="36" x2="46" y2="36" stroke="#609966" strokeWidth="3" />
              <line x1="46" y1="36" x2="46" y2="50" stroke="#609966" strokeWidth="3" />
              <line x1="10" y1="68" x2="70" y2="50" stroke="#FFC107" strokeWidth="3" />
              <line x1="10" y1="68" x2="70" y2="68" stroke="#FFC107" strokeWidth="2" />
            </svg>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 42,
                fontWeight: 800,
                color: '#F5F5F5',
                letterSpacing: -0.5,
              }}
            >
              Wheelchair Ramps
            </div>
          </div>
        </div>

        {/* Combine hint at bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: combineOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              fontWeight: 600,
              color: '#FFC107',
              letterSpacing: 4,
              textTransform: 'uppercase',
            }}
          >
            + Combine With Weatherization Program
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene33_TenThousandGrant;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34_WeatherizationFree',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene34_WeatherizationFree: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleOp = sp(5);
  const titleY = interpolate(sp(5), [0, 1], [-40, 0]);

  // Checklist items stagger
  const item1 = sp(35);
  const item2 = sp(60);
  const item3 = sp(85);

  // Bill graph
  const graphOp = sp(120);
  const billBefore = interpolate(sp(125), [0, 1], [0, 380]);
  const billAfter = interpolate(sp(150), [0, 1], [0, 180]);

  // "Age in Place" tagline
  const taglineOp = sp(175);

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(160deg, rgba(96,153,102,0.12) 0%, transparent 45%, rgba(147,181,198,0.10) 100%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            top: '20%',
            right: '-5%',
            width: 380,
            height: 380,
            border: '2px solid #609966',
            borderRadius: 30,
            opacity: 0.07,
            transform: `rotate(${frame * 0.12}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '-8%',
            left: '12%',
            width: 320,
            height: 320,
            background: '#93B5C6',
            borderRadius: '50%',
            opacity: 0.05,
            transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            top: '10%',
            left: '8%',
            width: 150,
            height: 150,
            background: '#FFC107',
            opacity: 0.05,
            transform: `rotate(${frame * 0.2}deg)`,
            clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
          }}
        />
      </AbsoluteFill>

      {/* Texture - grid */}
      <AbsoluteFill
        style={{
          backgroundImage: `linear-gradient(rgba(147,181,198,0.04) 1px, transparent 1px), linear-gradient(90deg, rgba(147,181,198,0.04) 1px, transparent 1px)`,
          backgroundSize: '70px 70px',
        }}
      />

      {/* Floating particles - warm air */}
      <AbsoluteFill>
        {Array.from({ length: 24 }).map((_, i) => {
          const seed = seededRandom(i + 120);
          const baseX = seed * 1920;
          const x = baseX + Math.sin(frame * 0.04 + i) * 30;
          const y = (seededRandom(i + 400) * 1080 - frame * (0.6 + seed * 0.5)) % 1080;
          const adjustedY = y < 0 ? y + 1080 : y;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: adjustedY,
                width: 3 + seed * 3,
                height: 3 + seed * 3,
                borderRadius: '50%',
                background: i % 3 === 0 ? '#FFC107' : '#93B5C6',
                opacity: 0.35,
                boxShadow: `0 0 10px ${i % 3 === 0 ? '#FFC107' : '#93B5C6'}`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      <AbsoluteFill>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
            transform: `translateY(${titleY}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 600,
              color: '#93B5C6',
              letterSpacing: 8,
              textTransform: 'uppercase',
              marginBottom: 12,
            }}
          >
            Weatherization Assistance
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 112,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: -2,
              lineHeight: 1,
            }}
          >
            They Come To <span style={{ color: '#609966' }}>Your House</span>
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 36,
              fontWeight: 700,
              color: '#FFC107',
              marginTop: 12,
              letterSpacing: 4,
              textTransform: 'uppercase',
            }}
          >
            100% Free Service
          </div>
        </div>

        {/* Checklist - Left side */}
        <div
          style={{
            position: 'absolute',
            top: 480,
            left: 130,
            width: 700,
          }}
        >
          {[
            { delay: item1, icon: 'window', label: 'Seal Your Windows' },
            { delay: item2, icon: 'attic', label: 'Insulate Your Attic' },
            { delay: item3, icon: 'heater', label: 'Service Your Heater' },
          ].map((it, idx) => (
            <div
              key={idx}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 28,
                marginBottom: 36,
                opacity: it.delay,
                transform: `translateX(${interpolate(it.delay, [0, 1], [-60, 0])}px)`,
              }}
            >
              {/* Check circle */}
              <div
                style={{
                  width: 70,
                  height: 70,
                  borderRadius: '50%',
                  background: '#609966',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  flexShrink: 0,
                  boxShadow: '0 0 20px rgba(96,153,102,0.4)',
                }}
              >
                <svg width="38" height="38" viewBox="0 0 24 24">
                  <polyline points="4,12 10,18 20,6" fill="none" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
              </div>
              {/* Icon */}
              <div style={{ width: 70, display: 'flex', justifyContent: 'center' }}>
                {it.icon === 'window' && (
                  <svg width="60" height="60" viewBox="0 0 60 60">
                    <rect x="8" y="8" width="44" height="44" fill="none" stroke="#93B5C6" strokeWidth="3" />
                    <line x1="30" y1="8" x2="30" y2="52" stroke="#93B5C6" strokeWidth="2" />
                    <line x1="8" y1="30" x2="52" y2="30" stroke="#93B5C6" strokeWidth="2" />
                  </svg>
                )}
                {it.icon === 'attic' && (
                  <svg width="60" height="60" viewBox="0 0 60 60">
                    <polygon points="30,8 8,32 52,32" fill="none" stroke="#93B5C6" strokeWidth="3" strokeLinejoin="round" />
                    <line x1="14" y1="40" x2="46" y2="40" stroke="#93B5C6" strokeWidth="2" strokeDasharray="3,3" />
                    <line x1="14" y1="46" x2="46" y2="46" stroke="#93B5C6" strokeWidth="2" strokeDasharray="3,3" />
                    <line x1="14" y1="52" x2="46" y2="52" stroke="#93B5C6" strokeWidth="2" strokeDasharray="3,3" />
                  </svg>
                )}
                {it.icon === 'heater' && (
                  <svg width="60" height="60" viewBox="0 0 60 60">
                    <rect x="10" y="14" width="40" height="36" fill="none" stroke="#93B5C6" strokeWidth="3" rx="2" />
                    <line x1="18" y1="22" x2="18" y2="42" stroke="#93B5C6" strokeWidth="2" />
                    <line x1="26" y1="22" x2="26" y2="42" stroke="#93B5C6" strokeWidth="2" />
                    <line x1="34" y1="22" x2="34" y2="42" stroke="#93B5C6" strokeWidth="2" />
                    <line x1="42" y1="22" x2="42" y2="42" stroke="#93B5C6" strokeWidth="2" />
                  </svg>
                )}
              </div>
              {/* Label */}
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 52,
                  fontWeight: 800,
                  color: '#F5F5F5',
                  letterSpacing: -0.5,
                }}
              >
                {it.label}
              </div>
            </div>
          ))}
        </div>

        {/* Bill Comparison Graph - Right side */}
        <div
          style={{
            position: 'absolute',
            top: 480,
            right: 130,
            width: 460,
            opacity: graphOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              fontWeight: 600,
              color: '#93B5C6',
              letterSpacing: 4,
              textTransform: 'uppercase',
              marginBottom: 30,
              textAlign: 'center',
            }}
          >
            Utility Bill Impact
          </div>

          {/* Before bar */}
          <div style={{ marginBottom: 28 }}>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 26,
                fontWeight: 700,
                color: '#9E9E9E',
                marginBottom: 8,
                letterSpacing: 2,
              }}
            >
              BEFORE
            </div>
            <div
              style={{
                height: 60,
                width: billBefore,
                background: '#9E9E9E',
                borderRadius: 6,
                display: 'flex',
                alignItems: 'center',
                paddingRight: 16,
                justifyContent: 'flex-end',
                fontFamily: 'Inter, Poppins',
                fontSize: 34,
                fontWeight: 900,
                color: '#F5F5F5',
              }}
            >
              {billBefore > 100 ? '$320' : ''}
            </div>
          </div>

          {/* After bar */}
          <div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 26,
                fontWeight: 700,
                color: '#609966',
                marginBottom: 8,
                letterSpacing: 2,
              }}
            >
              AFTER
            </div>
            <div
              style={{
                height: 60,
                width: billAfter,
                background: '#609966',
                borderRadius: 6,
                display: 'flex',
                alignItems: 'center',
                paddingRight: 16,
                justifyContent: 'flex-end',
                fontFamily: 'Inter, Poppins',
                fontSize: 34,
                fontWeight: 900,
                color: '#F5F5F5',
                boxShadow: '0 0 25px rgba(96,153,102,0.4)',
              }}
            >
              {billAfter > 80 ? '$155' : ''}
            </div>
          </div>

          {/* Savings tag */}
          <div
            style={{
              marginTop: 30,
              padding: '14px 24px',
              background: '#FFC107',
              borderRadius: 10,
              textAlign: 'center',
              fontFamily: 'Inter, Poppins',
              fontSize: 36,
              fontWeight: 900,
              color: '#393E46',
              letterSpacing: 1,
              opacity: sp(170),
              transform: `scale(${sp(170, { damping: 8 })})`,
            }}
          >
            ↓ PERMANENT SAVINGS
          </div>
        </div>

        {/* Bottom Tagline */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: taglineOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 44,
              fontWeight: 800,
              color: '#609966',
              letterSpacing: 3,
              fontStyle: 'italic',
            }}
          >
            Age In Place · Without Going Into Debt
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene34_WeatherizationFree;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35_TheCatchWebsite',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35_TheCatchWebsite: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // CATCH stamp
  const catchOp = sp(5);
  const catchScale = sp(5, { damping: 7, stiffness: 200 });

  // Paperwork pile
  const paperOp = sp(40);
  const paperShake = Math.sin(frame * 0.4) * 2;

  // Transition arrow
  const arrowOp = sp(85);
  const arrowX = interpolate(sp(85), [0, 1], [-80, 0]);

  // URL block
  const urlOp = sp(115);
  const urlScale = sp(115, { damping: 9 });

  // Pulse
  const urlPulse = 1 + Math.sin(frame * 0.12) * 0.015;

  // Cursor blink
  const cursorBlink = Math.floor(frame / 15) % 2 === 0 ? 1 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 80% 60%, rgba(96,153,102,0.20) 0%, transparent 55%), radial-gradient(ellipse at 20% 30%, rgba(255,193,7,0.10) 0%, transparent 50%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            top: '5%',
            left: '-5%',
            width: 420,
            height: 420,
            background: '#FFC107',
            opacity: 0.05,
            transform: `rotate(${frame * 0.1}deg)`,
            clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '-10%',
            right: '-5%',
            width: 520,
            height: 520,
            border: '2px solid #609966',
            borderRadius: '50%',
            opacity: 0.08,
            transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
          }}
        />
      </AbsoluteFill>

      {/* Texture - diagonal */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(-45deg, transparent 0, transparent 50px, rgba(96,153,102,0.04) 50px, rgba(96,153,102,0.04) 51px)`,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 20 }).map((_, i) => {
          const seed = seededRandom(i + 150);
          const x = (seed * 1920 + frame * 0.4) % 1920;
          const y = seededRandom(i + 500) * 1080;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: y,
                width: 3,
                height: 3,
                borderRadius: '50%',
                background: '#93B5C6',
                opacity: 0.4,
                boxShadow: '0 0 8px #93B5C6',
              }}
            />
          );
        })}
      </AbsoluteFill>

      <AbsoluteFill>
        {/* THE CATCH stamp - top left */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 140,
            opacity: catchOp,
            transform: `scale(${catchScale}) rotate(-6deg)`,
            transformOrigin: 'left center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 600,
              color: '#FFC107',
              letterSpacing: 8,
              textTransform: 'uppercase',
              marginBottom: 4,
            }}
          >
            Here's The Catch
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 128,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: -3,
              lineHeight: 1,
            }}
          >
            You Have To Apply.
          </div>
        </div>

        {/* Paperwork stack */}
        <div
          style={{
            position: 'absolute',
            top: 400,
            left: 180,
            opacity: paperOp,
            transform: `translate(${paperShake}px, ${-paperShake}px)`,
          }}
        >
          {/* Stack of documents */}
          {[0, 1, 2, 3, 4].map((i) => (
            <div
              key={i}
              style={{
                position: 'absolute',
                top: i * -8,
                left: i * 6,
                width: 280,
                height: 360,
                background: '#F5F5F5',
                opacity: 0.85 - i * 0.1,
                border: '2px solid #9E9E9E',
                borderRadius: 4,
                transform: `rotate(${(i - 2) * 2}deg)`,
                boxShadow: '0 8px 20px rgba(0,0,0,0.3)',
              }}
            >
              {/* Lines simulating text */}
              {Array.from({ length: 8 }).map((_, l) => (
                <div
                  key={l}
                  style={{
                    height: 6,
                    background: '#9E9E9E',
                    margin: '20px 24px',
                    width: l % 3 === 0 ? '60%' : '80%',
                    opacity: 0.4,
                  }}
                />
              ))}
            </div>
          ))}
          {/* X mark */}
          <div
            style={{
              position: 'absolute',
              top: -30,
              right: -30,
              width: 90,
              height: 90,
              borderRadius: '50%',
              background: '#FFC107',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              transform: `rotate(15deg) scale(${1 + Math.sin(frame * 0.15) * 0.05})`,
              boxShadow: '0 0 30px rgba(255,193,7,0.5)',
            }}
          >
            <svg width="50" height="50" viewBox="0 0 24 24">
              <line x1="5" y1="5" x2="19" y2="19" stroke="#393E46" strokeWidth="4" strokeLinecap="round" />
              <line x1="19" y1="5" x2="5" y2="19" stroke="#393E46" strokeWidth="4" strokeLinecap="round" />
            </svg>
          </div>
          <div
            style={{
              position: 'absolute',
              top: 380,
              left: -20,
              fontFamily: 'Inter, Poppins',
              fontSize: 30,
              fontWeight: 700,
              color: '#9E9E9E',
              letterSpacing: 3,
              textTransform: 'uppercase',
            }}
          >
            Paperwork Nightmare
          </div>
        </div>

        {/* Big Arrow */}
        <div
          style={{
            position: 'absolute',
            top: 540,
            left: 600,
            opacity: arrowOp,
            transform: `translateX(${arrowX}px)`,
          }}
        >
          <svg width="180" height="80" viewBox="0 0 180 80">
            <line x1="10" y1="40" x2="160" y2="40" stroke="#609966" strokeWidth="8" strokeLinecap="round" />
            <polyline
              points="130,15 170,40 130,65"
              fill="none"
              stroke="#609966"
              strokeWidth="8"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          </svg>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 26,
              fontWeight: 700,
              color: '#609966',
              letterSpacing: 4,
              textTransform: 'uppercase',
              textAlign: 'center',
              marginTop: 6,
            }}
          >
            One Stop Secret
          </div>
        </div>

        {/* Browser URL Block */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            right: 120,
            width: 760,
            opacity: urlOp,
            transform: `scale(${urlScale * urlPulse})`,
            transformOrigin: 'center',
          }}
        >
          {/* Browser top */}
          <div
            style={{
              background: '#2D2D2D',
              borderRadius: '12px 12px 0 0',
              padding: '14px 20px',
              display: 'flex',
              alignItems: 'center',
              gap: 10,
              border: '2px solid #609966',
              borderBottom: 'none',
            }}
          >
            <div style={{ width: 14, height: 14, borderRadius: '50%', background: '#ff5f57' }} />
            <div style={{ width: 14, height: 14, borderRadius: '50%', background: '#febc2e' }} />
            <div style={{ width: 14, height: 14, borderRadius: '50%', background: '#28c840' }} />
            <div
              style={{
                marginLeft: 16,
                flex: 1,
                background: '#393E46',
                borderRadius: 8,
                padding: '8px 16px',
                fontFamily: 'JetBrains Mono',
                fontSize: 20,
                color: '#93B5C6',
                display: 'flex',
                alignItems: 'center',
                gap: 8,
              }}
            >
              <span style={{ color: '#609966' }}>●</span>
              <span>https://</span>
              <span style={{ color: '#F5F5F5', fontWeight: 700 }}>www.benefitscheckup.org</span>
              <span style={{ color: '#FFC107', opacity: cursorBlink }}>|</span>
            </div>
          </div>
          {/* Browser body */}
          <div
            style={{
              background: 'rgba(28,28,28,0.6)',
              border: '2px solid #609966',
              borderRadius: '0 0 12px 12px',
              padding: '50px 36px 44px',
              textAlign: 'center',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 30,
                fontWeight: 600,
                color: '#93B5C6',
                letterSpacing: 4,
                textTransform: 'uppercase',
                marginBottom: 16,
              }}
            >
              Run by National Council on Aging
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 92,
                fontWeight: 900,
                color: '#F5F5F5',
                letterSpacing: -2,
                lineHeight: 1.05,
              }}
            >
              benefitscheckup
              <span style={{ color: '#609966' }}>.org</span>
            </div>
            <div
              style={{
                marginTop: 22,
                display: 'inline-block',
                padding: '12px 32px',
                background: '#FFC107',
                borderRadius: 50,
                fontFamily: 'Inter, Poppins',
                fontSize: 28,
                fontWeight: 800,
                color: '#393E46',
                letterSpacing: 2,
              }}
            >
              100% FREE TOOL
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene35_TheCatchWebsite;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36_HowItWorks',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene36_HowItWorks: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleOp = sp(5);

  // Steps stagger
  const step1 = sp(30);
  const step2 = sp(70);
  const step3 = sp(110);

  // Scan number counter
  const scanCount = Math.floor(interpolate(sp(150), [0, 1], [0, 2847], { extrapolateRight: 'clamp' }));

  // Scanner line
  const scanY = (frame * 4) % 200;

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(96,153,102,0.10) 0%, transparent 40%, rgba(147,181,198,0.12) 100%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            top: '10%',
            right: '5%',
            width: 260,
            height: 260,
            border: '2px solid #93B5C6',
            opacity: 0.08,
            transform: `rotate(${frame * 0.15}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '5%',
            left: '5%',
            width: 380,
            height: 380,
            background: '#609966',
            opacity: 0.05,
            borderRadius: '50%',
            transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            top: '40%',
            left: '40%',
            width: 140,
            height: 140,
            border: '2px solid #FFC107',
            opacity: 0.06,
            borderRadius: '50%',
            transform: `scale(${1 + Math.cos(frame * 0.04) * 0.08})`,
          }}
        />
      </AbsoluteFill>

      {/* Texture - tech grid */}
      <AbsoluteFill
        style={{
          backgroundImage: `linear-gradient(rgba(96,153,102,0.05) 1px, transparent 1px), linear-gradient(90deg, rgba(96,153,102,0.05) 1px, transparent 1px)`,
          backgroundSize: '50px 50px',
        }}
      />

      {/* Floating particles - data flow */}
      <AbsoluteFill>
        {Array.from({ length: 30 }).map((_, i) => {
          const seed = seededRandom(i + 180);
          const x = (seed * 1920 + frame * 0.6) % 1920;
          const y = seededRandom(i + 600) * 1080;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: y,
                width: 2,
                height: 8,
                background: i % 2 === 0 ? '#609966' : '#93B5C6',
                opacity: 0.45,
              }}
            />
          );
        })}
      </AbsoluteFill>

      <AbsoluteFill>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
            transform: `translateY(${interpolate(titleOp, [0, 1], [-30, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 34,
              fontWeight: 600,
              color: '#93B5C6',
              letterSpacing: 8,
              textTransform: 'uppercase',
              marginBottom: 14,
            }}
          >
            How It Works · 3 Steps
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 102,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: -2,
              lineHeight: 1,
            }}
          >
            Free · Fast · Personal
          </div>
        </div>

        {/* THREE STEPS - horizontal */}
        <div
          style={{
            position: 'absolute',
            top: 360,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 30,
          }}
        >
          {/* STEP 1 - ZIP CODE */}
          <div
            style={{
              width: 380,
              opacity: step1,
              transform: `translateY(${interpolate(step1, [0, 1], [60, 0])}px)`,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 110,
                fontWeight: 900,
                color: '#609966',
                lineHeight: 1,
                marginBottom: 10,
                letterSpacing: -3,
              }}
            >
              01
            </div>
            <div
              style={{
                border: '2px solid #609966',
                borderRadius: 14,
                padding: '28px 24px',
                background: 'rgba(96,153,102,0.08)',
              }}
            >
              {/* SVG location icon */}
              <svg width="50" height="50" viewBox="0 0 50 50" style={{ marginBottom: 12 }}>
                <path
                  d="M25 5 C16 5, 10 12, 10 20 C10 30, 25 45, 25 45 C25 45, 40 30, 40 20 C40 12, 34 5, 25 5 Z"
                  fill="none"
                  stroke="#93B5C6"
                  strokeWidth="3"
                />
                <circle cx="25" cy="20" r="5" fill="#FFC107" />
              </svg>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 36,
                  fontWeight: 800,
                  color: '#F5F5F5',
                  marginBottom: 14,
                }}
              >
                Type Your ZIP
              </div>
              {/* ZIP input mock */}
              <div
                style={{
                  background: '#1C1C1C',
                  border: '2px solid #93B5C6',
                  borderRadius: 8,
                  padding: '12px 18px',
                  fontFamily: 'JetBrains Mono',
                  fontSize: 36,
                  fontWeight: 700,
                  color: '#FFC107',
                  letterSpacing: 4,
                }}
              >
                90210
              </div>
            </div>
          </div>

          {/* Arrow */}
          <div style={{ opacity: step2, fontSize: 60, color: '#FFC107', fontWeight: 900 }}>→</div>

          {/* STEP 2 - QUESTIONS */}
          <div
            style={{
              width: 380,
              opacity: step2,
              transform: `translateY(${interpolate(step2, [0, 1], [60, 0])}px)`,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 110,
                fontWeight: 900,
                color: '#609966',
                lineHeight: 1,
                marginBottom: 10,
                letterSpacing: -3,
              }}
            >
              02
            </div>
            <div
              style={{
                border: '2px solid #609966',
                borderRadius: 14,
                padding: '28px 24px',
                background: 'rgba(96,153,102,0.08)',
              }}
            >
              <svg width="50" height="50" viewBox="0 0 50 50" style={{ marginBottom: 12 }}>
                <circle cx="25" cy="25" r="20" fill="none" stroke="#93B5C6" strokeWidth="3" />
                <path
                  d="M18 20 Q18 14, 25 14 Q32 14, 32 20 Q32 24, 25 26 L25 30"
                  fill="none"
                  stroke="#93B5C6"
                  strokeWidth="3"
                  strokeLinecap="round"
                />
                <circle cx="25" cy="36" r="2.5" fill="#FFC107" />
              </svg>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 36,
                  fontWeight: 800,
                  color: '#F5F5F5',
                  marginBottom: 14,
                }}
              >
                Simple Questions
              </div>
              <div style={{ display: 'flex', flexDirection: 'column', gap: 8 }}>
                {['Income?', 'Expenses?', 'Age?'].map((q, i) => (
                  <div
                    key={i}
                    style={{
                      display: 'flex',
                      alignItems: 'center',
                      gap: 10,
                      fontFamily: 'Inter, Poppins',
                      fontSize: 24,
                      fontWeight: 600,
                      color: '#93B5C6',
                    }}
                  >
                    <div
                      style={{
                        width: 14,
                        height: 14,
                        background: '#609966',
                        borderRadius: 3,
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                      }}
                    >
                      <svg width="10" height="10" viewBox="0 0 24 24">
                        <polyline points="4,12 10,18 20,6" fill="none" stroke="#F5F5F5" strokeWidth="4" />
                      </svg>
                    </div>
                    {q}
                  </div>
                ))}
              </div>
            </div>
          </div>

          {/* Arrow */}
          <div style={{ opacity: step3, fontSize: 60, color: '#FFC107', fontWeight: 900 }}>→</div>

          {/* STEP 3 - SCAN */}
          <div
            style={{
              width: 380,
              opacity: step3,
              transform: `translateY(${interpolate(step3, [0, 1], [60, 0])}px)`,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 110,
                fontWeight: 900,
                color: '#609966',
                lineHeight: 1,
                marginBottom: 10,
                letterSpacing: -3,
              }}
            >
              03
            </div>
            <div
              style={{
                border: '2px solid #609966',
                borderRadius: 14,
                padding: '28px 24px',
                background: 'rgba(96,153,102,0.08)',
                position: 'relative',
                overflow: 'hidden',
              }}
            >
              {/* Scanner line */}
              <div
                style={{
                  position: 'absolute',
                  top: scanY,
                  left: 0,
                  right: 0,
                  height: 3,
                  background: '#FFC107',
                  boxShadow: '0 0 20px #FFC107',
                  opacity: 0.7,
                }}
              />
              <svg width="50" height="50" viewBox="0 0 50 50" style={{ marginBottom: 12 }}>
                <circle cx="22" cy="22" r="12" fill="none" stroke="#93B5C6" strokeWidth="3" />
                <line x1="32" y1="32" x2="42" y2="42" stroke="#93B5C6" strokeWidth="3" strokeLinecap="round" />
              </svg>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 36,
                  fontWeight: 800,
                  color: '#F5F5F5',
                  marginBottom: 14,
                }}
              >
                Instant Scan
              </div>
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 56,
                  fontWeight: 900,
                  color: '#FFC107',
                  letterSpacing: -1,
                }}
              >
                {scanCount.toLocaleString()}
              </div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 20,
                  fontWeight: 600,
                  color: '#93B5C6',
                  letterSpacing: 2,
                  textTransform: 'uppercase',
                  marginTop: 4,
                }}
              >
                Programs Scanned
              </div>
            </div>
          </div>
        </div>

        {/* Bottom line */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(180),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 38,
              fontWeight: 700,
              color: '#609966',
              letterSpacing: 4,
              textTransform: 'uppercase',
            }}
          >
            Federal · State · Local — All At Once
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene36_HowItWorks;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37_PersonalizedList',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene37_PersonalizedList: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleOp = sp(5);

  // Result card
  const cardOp = sp(30);
  const cardScale = sp(30, { damping: 11 });

  // List items
  const items = [
    { name: 'SNAP for Seniors', amount: '$250/mo' },
    { name: 'Medicare Extra Help', amount: '$2,400/yr' },
    { name: 'QMB Program', amount: '$174/mo' },
    { name: 'LIHEAP Energy', amount: '$600/yr' },
    { name: 'Section 504 Grant', amount: '$10,000' },
  ];

  // 5 minutes circle
  const timerOp = sp(170);
  const timerScale = sp(170, { damping: 9 });

  // Print stamp
  const printOp = sp(190);

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 30% 50%, rgba(96,153,102,0.18) 0%, transparent 55%), radial-gradient(ellipse at 85% 50%, rgba(255,193,7,0.10) 0%, transparent 50%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            top: '8%',
            left: '40%',
            width: 350,
            height: 350,
            border: '2px solid #609966',
            opacity: 0.06,
            borderRadius: '50%',
            transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '5%',
            right: '8%',
            width: 280,
            height: 280,
            background: '#93B5C6',
            opacity: 0.05,
            transform: `rotate(${frame * 0.1}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            top: '50%',
            left: '5%',
            width: 180,
            height: 180,
            background: '#FFC107',
            opacity: 0.05,
            borderRadius: '50%',
            transform: `scale(${1 + Math.cos(frame * 0.04) * 0.07})`,
          }}
        />
      </AbsoluteFill>

      {/* Texture */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(90deg, transparent 0, transparent 100px, rgba(147,181,198,0.04) 100px, rgba(147,181,198,0.04) 101px)`,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 22 }).map((_, i) => {
          const seed = seededRandom(i + 220);
          const x = seed * 1920;
          const y = (seededRandom(i + 700) * 1080 + frame * (0.3 + seed * 0.4)) % 1080;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: y,
                width: 3,
                height: 3,
                borderRadius: '50%',
                background: '#609966',
                opacity: 0.4,
                boxShadow: '0 0 8px #609966',
              }}
            />
          );
        })}
      </AbsoluteFill>

      <AbsoluteFill>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
            transform: `translateY(${interpolate(titleOp, [0, 1], [-30, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 600,
              color: '#93B5C6',
              letterSpacing: 8,
              textTransform: 'uppercase',
              marginBottom: 10,
            }}
          >
            Your Personalized Result
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 96,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: -2,
              lineHeight: 1,
            }}
          >
            Every Benefit You <span style={{ color: '#609966' }}>Qualify For</span>
          </div>
        </div>

        {/* Result Card - center left */}
        <div
          style={{
            position: 'absolute',
            top: 310,
            left: 140,
            width: 900,
            opacity: cardOp,
            transform: `scale(${cardScale})`,
            transformOrigin: 'left center',
          }}
        >
          <div
            style={{
              background: 'rgba(28,28,28,0.65)',
              border: '3px solid #609966',
              borderRadius: 18,
              padding: '30px 36px',
              boxShadow: '0 0 50px rgba(96,153,102,0.25)',
            }}
          >
            {/* Header */}
            <div
              style={{
                display: 'flex',
                justifyContent: 'space-between',
                alignItems: 'center',
                paddingBottom: 20,
                borderBottom: '2px solid rgba(147,181,198,0.3)',
                marginBottom: 20,
              }}
            >
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 32,
                  fontWeight: 800,
                  color: '#F5F5F5',
                  letterSpacing: 1,
                }}
              >
                MATCHED PROGRAMS
              </div>
              <div
                style={{
                  fontFamily: 'JetBrains Mono',
                  fontSize: 24,
                  fontWeight: 700,
                  color: '#FFC107',
                  background: 'rgba(255,193,7,0.1)',
                  padding: '6px 16px',
                  borderRadius: 8,
                }}
              >
                5 of 5
              </div>
            </div>

            {/* List items */}
            {items.map((it, i) => {
              const itemDelay = sp(55 + i * 22);
              return (
                <div
                  key={i}
                  style={{
                    display: 'flex',
                    justifyContent: 'space-between',
                    alignItems: 'center',
                    padding: '14px 0',
                    borderBottom: i < items.length - 1 ? '1px solid rgba(147,181,198,0.15)' : 'none',
                    opacity: itemDelay,
                    transform: `translateX(${interpolate(itemDelay, [0, 1], [-40, 0])}px)`,
                  }}
                >
                  <div style={{ display: 'flex', alignItems: 'center', gap: 18 }}>
                    <div
                      style={{
                        width: 36,
                        height: 36,
                        borderRadius: '50%',
                        background: '#609966',
                        display: 'flex',
                        alignItems: 'center',
                        justifyContent: 'center',
                      }}
                    >
                      <svg width="20" height="20" viewBox="0 0 24 24">
                        <polyline points="4,12 10,18 20,6" fill="none" stroke="#F5F5F5" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round" />
                      </svg>
                    </div>
                    <div
                      style={{
                        fontFamily: 'Inter, Poppins',
                        fontSize: 38,
                        fontWeight: 700,
                        color: '#F5F5F5',
                      }}
                    >
                      {it.name}
                    </div>
                  </div>
                  <div
                    style={{
                      fontFamily: 'JetBrains Mono',
                      fontSize: 36,
                      fontWeight: 900,
                      color: '#FFC107',
                      letterSpacing: -0.5,
                    }}
                  >
                    {it.amount}
                  </div>
                </div>
              );
            })}
          </div>
        </div>

        {/* Right side - 5 MINUTES */}
        <div
          style={{
            position: 'absolute',
            top: 340,
            right: 100,
            width: 360,
            textAlign: 'center',
            opacity: timerOp,
            transform: `scale(${timerScale})`,
            transformOrigin: 'center',
          }}
        >
          <div
            style={{
              width: 360,
              height: 360,
              borderRadius: '50%',
              border: '8px solid #FFC107',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              boxShadow: '0 0 50px rgba(255,193,7,0.3)',
              background: 'rgba(255,193,7,0.05)',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 220,
                fontWeight: 900,
                color: '#FFC107',
                lineHeight: 1,
                letterSpacing: -8,
              }}
            >
              5
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 38,
                fontWeight: 800,
                color: '#F5F5F5',
                letterSpacing: 4,
                textTransform: 'uppercase',
                marginTop: -10,
              }}
            >
              Minutes
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 22,
                fontWeight: 600,
                color: '#93B5C6',
                letterSpacing: 2,
                marginTop: 6,
              }}
            >
              That's All It Takes
            </div>
          </div>
        </div>

        {/* Print badge */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            right: 130,
            opacity: printOp,
            transform: `rotate(${interpolate(printOp, [0, 1], [10, -4])}deg) scale(${printOp})`,
          }}
        >
          <div
            style={{
              padding: '14px 28px',
              background: '#609966',
              borderRadius: 10,
              fontFamily: 'Inter, Poppins',
              fontSize: 28,
              fontWeight: 800,
              color: '#F5F5F5',
              letterSpacing: 3,
              textTransform: 'uppercase',
            }}
          >
            → Print The List
          </div>
        </div>

        {/* Left bottom hint */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: 140,
            opacity: sp(150),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 30,
              fontWeight: 600,
              color: '#93B5C6',
              letterSpacing: 3,
              textTransform: 'uppercase',
              fontStyle: 'italic',
            }}
          >
            + Direct Links To Apply
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene37_PersonalizedList;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38_RecapPart1',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene38_RecapPart1: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title
  const titleOp = sp(5);
  const yearScale = sp(15, { damping: 8, stiffness: 180 });

  // Three cards stagger
  const card1 = sp(45);
  const card2 = sp(90);
  const card3 = sp(135);

  // Camera subtle zoom
  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.03]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(96,153,102,0.14) 0%, transparent 50%, rgba(147,181,198,0.12) 100%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            top: '-10%',
            left: '-8%',
            width: 550,
            height: 550,
            background: '#609966',
            opacity: 0.05,
            borderRadius: '50%',
            transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '-12%',
            right: '-10%',
            width: 600,
            height: 600,
            border: '2px solid #93B5C6',
            opacity: 0.07,
            transform: `rotate(${frame * 0.08}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            top: '20%',
            right: '15%',
            width: 180,
            height: 180,
            background: '#FFC107',
            opacity: 0.05,
            transform: `rotate(${-frame * 0.15}deg)`,
            clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
          }}
        />
      </AbsoluteFill>

      {/* Texture */}
      <AbsoluteFill
        style={{
          backgroundImage: `radial-gradient(circle, rgba(96,153,102,0.06) 1.5px, transparent 1.5px)`,
          backgroundSize: '50px 50px',
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 24 }).map((_, i) => {
          const seed = seededRandom(i + 250);
          const x = seed * 1920;
          const y = (seededRandom(i + 800) * 1080 - frame * 0.5) % 1080;
          const adjustedY = y < 0 ? y + 1080 : y;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: adjustedY,
                width: 3,
                height: 3,
                borderRadius: '50%',
                background: i % 2 === 0 ? '#609966' : '#FFC107',
                opacity: 0.4,
                boxShadow: '0 0 8px currentColor',
              }}
            />
          );
        })}
      </AbsoluteFill>

      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>
        {/* Title */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: titleOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 600,
              color: '#93B5C6',
              letterSpacing: 8,
              textTransform: 'uppercase',
              marginBottom: 8,
            }}
          >
            Final Recap · Senior Benefits
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 110,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: -2,
              lineHeight: 1,
              display: 'flex',
              justifyContent: 'center',
              alignItems: 'baseline',
              gap: 24,
            }}
          >
            <span style={{ color: '#FFC107' }}>5 Programs</span>
            <span
              style={{
                fontSize: 90,
                color: '#93B5C6',
                transform: `scale(${yearScale})`,
                display: 'inline-block',
              }}
            >
              · 2026
            </span>
          </div>
        </div>

        {/* Three Cards Row */}
        <div
          style={{
            position: 'absolute',
            top: 320,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 40,
            padding: '0 100px',
          }}
        >
          {[
            {
              num: '01',
              icon: 'cart',
              title: 'SNAP for Seniors',
              tag: 'Grocery Card',
              amt: '$250/mo',
              delay: card1,
            },
            {
              num: '02',
              icon: 'pill',
              title: 'Medicare Extra Help',
              tag: 'Wipes Out Co-Pays',
              amt: 'LIS',
              delay: card2,
            },
            {
              num: '03',
              icon: 'shield',
              title: 'Medicare Savings',
              tag: 'State Pays Part B',
              amt: 'QMB',
              delay: card3,
            },
          ].map((c, i) => (
            <div
              key={i}
              style={{
                flex: 1,
                maxWidth: 540,
                opacity: c.delay,
                transform: `translateY(${interpolate(c.delay, [0, 1], [80, 0])}px) scale(${c.delay})`,
                transformOrigin: 'center top',
              }}
            >
              <div
                style={{
                  border: '2px solid #609966',
                  borderRadius: 20,
                  padding: '36px 28px',
                  background: 'rgba(96,153,102,0.08)',
                  height: 520,
                  display: 'flex',
                  flexDirection: 'column',
                  position: 'relative',
                  boxShadow: '0 0 30px rgba(96,153,102,0.15)',
                }}
              >
                {/* Number */}
                <div
                  style={{
                    fontFamily: 'Inter, Poppins',
                    fontSize: 96,
                    fontWeight: 900,
                    color: '#609966',
                    lineHeight: 1,
                    letterSpacing: -3,
                    opacity: 0.5,
                  }}
                >
                  {c.num}
                </div>

                {/* Icon */}
                <div style={{ margin: '10px 0 20px' }}>
                  {c.icon === 'cart' && (
                    <svg width="80" height="80" viewBox="0 0 80 80">
                      <path
                        d="M10 14 L20 14 L26 50 L62 50 L68 22 L24 22"
                        fill="none"
                        stroke="#93B5C6"
                        strokeWidth="4"
                        strokeLinecap="round"
                        strokeLinejoin="round"
                      />
                      <circle cx="32" cy="62" r="5" fill="#FFC107" />
                      <circle cx="56" cy="62" r="5" fill="#FFC107" />
                    </svg>
                  )}
                  {c.icon === 'pill' && (
                    <svg width="80" height="80" viewBox="0 0 80 80">
                      <rect
                        x="14"
                        y="32"
                        width="52"
                        height="22"
                        rx="11"
                        fill="none"
                        stroke="#93B5C6"
                        strokeWidth="4"
                        transform="rotate(-30 40 43)"
                      />
                      <line
                        x1="40"
                        y1="32"
                        x2="40"
                        y2="54"
                        stroke="#93B5C6"
                        strokeWidth="4"
                        transform="rotate(-30 40 43)"
                      />
                      <circle cx="55" cy="28" r="3" fill="#FFC107" />
                      <circle cx="62" cy="22" r="2" fill="#FFC107" />
                    </svg>
                  )}
                  {c.icon === 'shield' && (
                    <svg width="80" height="80" viewBox="0 0 80 80">
                      <path
                        d="M40 8 L65 18 L65 42 Q65 60, 40 72 Q15 60, 15 42 L15 18 Z"
                        fill="none"
                        stroke="#93B5C6"
                        strokeWidth="4"
                        strokeLinejoin="round"
                      />
                      <polyline
                        points="28,40 36,48 52,30"
                        fill="none"
                        stroke="#FFC107"
                        strokeWidth="5"
                        strokeLinecap="round"
                        strokeLinejoin="round"
                      />
                    </svg>
                  )}
                </div>

                {/* Title */}
                <div
                  style={{
                    fontFamily: 'Inter, Poppins',
                    fontSize: 44,
                    fontWeight: 900,
                    color: '#F5F5F5',
                    letterSpacing: -1,
                    lineHeight: 1.05,
                    marginBottom: 16,
                  }}
                >
                  {c.title}
                </div>

                {/* Tag line */}
                <div
                  style={{
                    fontFamily: 'Inter, Poppins',
                    fontSize: 28,
                    fontWeight: 600,
                    color: '#93B5C6',
                    letterSpacing: 1,
                    marginBottom: 'auto',
                  }}
                >
                  {c.tag}
                </div>

                {/* Bottom amount badge */}
                <div
                  style={{
                    marginTop: 20,
                    padding: '14px 0',
                    borderTop: '2px solid rgba(147,181,198,0.25)',
                    fontFamily: 'JetBrains Mono',
                    fontSize: 48,
                    fontWeight: 900,
                    color: '#FFC107',
                    textAlign: 'center',
                    letterSpacing: -1,
                  }}
                >
                  {c.amt}
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* Bottom indicator */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: sp(170),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 30,
              fontWeight: 600,
              color: '#93B5C6',
              letterSpacing: 6,
              textTransform: 'uppercase',
            }}
          >
            ● ● ● ○ ○  &nbsp; 3 of 5 Covered
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene38_RecapPart1;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39_RecapPart2_Earned',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene39_RecapPart2_Earned: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Two remaining cards
  const card4 = sp(5);
  const card5 = sp(40);

  // Divider
  const dividerWidth = interpolate(sp(85), [0, 1], [0, 1600]);

  // Statement reveal — three lines
  const line1 = sp(110);
  const line2 = sp(135);
  const line3 = sp(160);

  // Big EARNED stamp
  const earnedOp = sp(190);
  const earnedScale = sp(190, { damping: 7, stiffness: 200 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 80%, rgba(255,193,7,0.15) 0%, transparent 55%), radial-gradient(ellipse at 50% 20%, rgba(96,153,102,0.15) 0%, transparent 50%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            top: '50%',
            left: '50%',
            width: 700,
            height: 700,
            border: '2px solid #FFC107',
            opacity: 0.06,
            borderRadius: '50%',
            transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            top: '10%',
            left: '5%',
            width: 220,
            height: 220,
            background: '#609966',
            opacity: 0.05,
            transform: `rotate(${frame * 0.12}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '10%',
            right: '5%',
            width: 200,
            height: 200,
            background: '#93B5C6',
            opacity: 0.05,
            borderRadius: '50%',
            transform: `scale(${1 + Math.cos(frame * 0.04) * 0.06})`,
          }}
        />
      </AbsoluteFill>

      {/* Texture */}
      <AbsoluteFill
        style={{
          backgroundImage: `repeating-linear-gradient(45deg, transparent 0, transparent 70px, rgba(255,193,7,0.04) 70px, rgba(255,193,7,0.04) 71px)`,
        }}
      />

      {/* Floating particles - celebratory */}
      <AbsoluteFill>
        {Array.from({ length: 32 }).map((_, i) => {
          const seed = seededRandom(i + 300);
          const x = (seed * 1920 + Math.sin(frame * 0.03 + i) * 40) % 1920;
          const y = (seededRandom(i + 900) * 1080 - frame * (0.4 + seed * 0.6)) % 1080;
          const adjustedY = y < 0 ? y + 1080 : y;
          const size = 3 + seed * 4;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: adjustedY,
                width: size,
                height: size,
                borderRadius: '50%',
                background: i % 3 === 0 ? '#FFC107' : i % 3 === 1 ? '#609966' : '#93B5C6',
                opacity: 0.45,
                boxShadow: '0 0 10px currentColor',
              }}
            />
          );
        })}
      </AbsoluteFill>

      <AbsoluteFill>
        {/* Two Cards Row - top */}
        <div
          style={{
            position: 'absolute',
            top: 100,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 50,
            padding: '0 200px',
          }}
        >
          {[
            {
              num: '04',
              icon: 'flame',
              title: 'LIHEAP',
              tag: 'Pays Heating + Cooling',
              amt: 'Energy Aid',
              delay: card4,
            },
            {
              num: '05',
              icon: 'home',
              title: 'Section 504',
              tag: 'Fixes Roof + Plumbing Free',
              amt: '$10,000',
              delay: card5,
            },
          ].map((c, i) => (
            <div
              key={i}
              style={{
                flex: 1,
                maxWidth: 640,
                opacity: c.delay,
                transform: `translateY(${interpolate(c.delay, [0, 1], [60, 0])}px) scale(${c.delay})`,
                transformOrigin: 'center top',
              }}
            >
              <div
                style={{
                  border: '2px solid #FFC107',
                  borderRadius: 20,
                  padding: '28px 32px',
                  background: 'rgba(255,193,7,0.06)',
                  display: 'flex',
                  alignItems: 'center',
                  gap: 24,
                  boxShadow: '0 0 30px rgba(255,193,7,0.15)',
                }}
              >
                {/* Icon */}
                <div style={{ flexShrink: 0 }}>
                  {c.icon === 'flame' && (
                    <svg width="90" height="90" viewBox="0 0 90 90">
                      <path
                        d="M45 10 Q30 30, 32 48 Q34 60, 45 65 Q56 60, 58 48 Q60 30, 45 10 Z"
                        fill="none"
                        stroke="#FFC107"
                        strokeWidth="4"
                        strokeLinejoin="round"
                      />
                      <path
                        d="M45 30 Q38 42, 40 52 Q42 58, 45 60 Q48 58, 50 52 Q52 42, 45 30 Z"
                        fill="#FFC107"
                        opacity={0.4 + Math.sin(frame * 0.2) * 0.3}
                      />
                      <line x1="45" y1="68" x2="45" y2="78" stroke="#FFC107" strokeWidth="4" strokeLinecap="round" />
                    </svg>
                  )}
                  {c.icon === 'home' && (
                    <svg width="90" height="90" viewBox="0 0 90 90">
                      <polygon points="45,12 12,42 78,42" fill="none" stroke="#FFC107" strokeWidth="4" strokeLinejoin="round" />
                      <rect x="20" y="42" width="50" height="38" fill="none" stroke="#FFC107" strokeWidth="4" />
                      <rect x="38" y="56" width="14" height="24" fill="#FFC107" opacity="0.5" />
                      <polyline points="22,52 28,46 34,52" fill="none" stroke="#609966" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
                    </svg>
                  )}
                </div>

                {/* Text */}
                <div style={{ flex: 1 }}>
                  <div
                    style={{
                      fontFamily: 'Inter, Poppins',
                      fontSize: 22,
                      fontWeight: 700,
                      color: '#FFC107',
                      letterSpacing: 4,
                      textTransform: 'uppercase',
                      marginBottom: 4,
                    }}
                  >
                    Program {c.num}
                  </div>
                  <div
                    style={{
                      fontFamily: 'Inter, Poppins',
                      fontSize: 56,
                      fontWeight: 900,
                      color: '#F5F5F5',
                      letterSpacing: -1.5,
                      lineHeight: 1,
                      marginBottom: 8,
                    }}
                  >
                    {c.title}
                  </div>
                  <div
                    style={{
                      fontFamily: 'Inter, Poppins',
                      fontSize: 26,
                      fontWeight: 600,
                      color: '#93B5C6',
                      letterSpacing: 1,
                    }}
                  >
                    {c.tag}
                  </div>
                </div>

                {/* Amount */}
                <div
                  style={{
                    fontFamily: 'JetBrains Mono',
                    fontSize: 40,
                    fontWeight: 900,
                    color: '#609966',
                    letterSpacing: -1,
                    flexShrink: 0,
                  }}
                >
                  {c.amt}
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* Divider */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            left: '50%',
            width: dividerWidth,
            height: 2,
            background: 'linear-gradient(90deg, transparent, #FFC107, transparent)',
            transform: 'translateX(-50%)',
          }}
        />

        {/* Three-line Statement */}
        <div
          style={{
            position: 'absolute',
            top: 440,
            left: 0,
            right: 0,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 78,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: -1,
              lineHeight: 1.15,
              marginBottom: 14,
              opacity: line1,
              transform: `translateY(${interpolate(line1, [0, 1], [20, 0])}px)`,
            }}
          >
            You Paid Your Taxes.
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 78,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: -1,
              lineHeight: 1.15,
              marginBottom: 14,
              opacity: line2,
              transform: `translateY(${interpolate(line2, [0, 1], [20, 0])}px)`,
            }}
          >
            You Did Your Time.
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 88,
              fontWeight: 900,
              color: '#609966',
              letterSpacing: -1.5,
              lineHeight: 1.15,
              opacity: line3,
              transform: `translateY(${interpolate(line3, [0, 1], [20, 0])}px)`,
              textShadow: '0 0 30px rgba(96,153,102,0.4)',
            }}
          >
            You Earned These Benefits.
          </div>
        </div>

        {/* EARNED stamp - bottom right */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            right: 130,
            opacity: earnedOp,
            transform: `scale(${earnedScale}) rotate(-8deg)`,
            transformOrigin: 'center',
          }}
        >
          <div
            style={{
              border: '5px solid #FFC107',
              padding: '14px 36px',
              fontFamily: 'Inter, Poppins',
              fontSize: 56,
              fontWeight: 900,
              color: '#FFC107',
              letterSpacing: 8,
              borderRadius: 8,
              background: 'rgba(57,62,70,0.6)',
              textShadow: '0 0 20px rgba(255,193,7,0.4)',
            }}
          >
            EARNED
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene39_RecapPart2_Earned;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40_Outro_ShareSave',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene40_Outro_ShareSave: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Top line
  const topLineOp = sp(5);

  // Big headline
  const headOp = sp(25);
  const headScale = sp(25, { damping: 10 });

  // Three person circles
  const p1 = sp(70);
  const p2 = sp(90);
  const p3 = sp(110);

  // Subscribe CTA
  const subOp = sp(150);
  const subPulse = 1 + Math.sin(frame * 0.13) * 0.025;

  // See you next
  const seeOp = sp(190);

  return (
    <AbsoluteFill style={{ backgroundColor: '#393E46', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 50%, rgba(96,153,102,0.20) 0%, transparent 60%), linear-gradient(180deg, rgba(255,193,7,0.05) 0%, transparent 100%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        {[0, 1, 2, 3].map((r) => (
          <div
            key={r}
            style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              width: 400 + r * 220,
              height: 400 + r * 220,
              borderRadius: '50%',
              border: '1.5px solid #609966',
              opacity: 0.05 - r * 0.01,
              transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.02 + r * 0.5) * 0.04})`,
            }}
          />
        ))}
        <div
          style={{
            position: 'absolute',
            top: '15%',
            right: '10%',
            width: 200,
            height: 200,
            background: '#FFC107',
            opacity: 0.05,
            transform: `rotate(${frame * 0.12}deg)`,
            clipPath: 'polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)',
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '12%',
            left: '8%',
            width: 240,
            height: 240,
            background: '#93B5C6',
            opacity: 0.05,
            borderRadius: '50%',
          }}
        />
      </AbsoluteFill>

      {/* Texture */}
      <AbsoluteFill
        style={{
          backgroundImage: `radial-gradient(circle, rgba(96,153,102,0.06) 1.5px, transparent 1.5px)`,
          backgroundSize: '45px 45px',
        }}
      />

      {/* Floating particles - celebratory upward */}
      <AbsoluteFill>
        {Array.from({ length: 36 }).map((_, i) => {
          const seed = seededRandom(i + 350);
          const baseX = seed * 1920;
          const x = baseX + Math.sin(frame * 0.05 + i) * 25;
          const y = (seededRandom(i + 1000) * 1080 - frame * (0.5 + seed * 0.7)) % 1080;
          const adjustedY = y < 0 ? y + 1080 : y;
          const size = 3 + seed * 5;
          return (
            <div
              key={i}
              style={{
                position: 'absolute',
                left: x,
                top: adjustedY,
                width: size,
                height: size,
                borderRadius: '50%',
                background: i % 3 === 0 ? '#FFC107' : i % 3 === 1 ? '#609966' : '#93B5C6',
                opacity: 0.45,
                boxShadow: '0 0 12px currentColor',
              }}
            />
          );
        })}
      </AbsoluteFill>

      <AbsoluteFill>
        {/* Top line */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: topLineOp,
            transform: `translateY(${interpolate(topLineOp, [0, 1], [-20, 0])}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 34,
              fontWeight: 600,
              color: '#93B5C6',
              letterSpacing: 6,
              textTransform: 'uppercase',
            }}
          >
            Don't Let Pride Stop You
          </div>
        </div>

        {/* Big Headline */}
        <div
          style={{
            position: 'absolute',
            top: 170,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headOp,
            transform: `scale(${headScale})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 124,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: -3,
              lineHeight: 1,
            }}
          >
            Share This With
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 130,
              fontWeight: 900,
              color: '#FFC107',
              letterSpacing: -3,
              lineHeight: 1,
              marginTop: 10,
              textShadow: '0 0 40px rgba(255,193,7,0.4)',
            }}
          >
            Someone You Love
          </div>
        </div>

        {/* Three Person Circles */}
        <div
          style={{
            position: 'absolute',
            top: 510,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 80,
          }}
        >
          {[
            { label: 'A Parent', delay: p1 },
            { label: 'A Neighbor', delay: p2 },
            { label: 'A Friend', delay: p3 },
          ].map((p, i) => (
            <div
              key={i}
              style={{
                textAlign: 'center',
                opacity: p.delay,
                transform: `translateY(${interpolate(p.delay, [0, 1], [40, 0])}px) scale(${p.delay})`,
              }}
            >
              <div
                style={{
                  width: 180,
                  height: 180,
                  borderRadius: '50%',
                  border: '4px solid #609966',
                  background: 'rgba(96,153,102,0.12)',
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  margin: '0 auto 18px',
                  boxShadow: '0 0 30px rgba(96,153,102,0.25)',
                }}
              >
                {/* Person silhouette */}
                <svg width="90" height="90" viewBox="0 0 90 90">
                  <circle cx="45" cy="30" r="14" fill="none" stroke="#93B5C6" strokeWidth="4" />
                  <path
                    d="M20 75 Q20 52, 45 52 Q70 52, 70 75"
                    fill="none"
                    stroke="#93B5C6"
                    strokeWidth="4"
                    strokeLinecap="round"
                  />
                </svg>
              </div>
              <div
                style={{
                  fontFamily: 'Inter, Poppins',
                  fontSize: 36,
                  fontWeight: 800,
                  color: '#F5F5F5',
                  letterSpacing: 1,
                }}
              >
                {p.label}
              </div>
            </div>
          ))}
        </div>

        {/* SUBSCRIBE bar */}
        <div
          style={{
            position: 'absolute',
            bottom: 170,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: subOp,
            transform: `scale(${subPulse})`,
          }}
        >
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 20,
              padding: '18px 50px',
              background: '#ff0000',
              borderRadius: 14,
              boxShadow: '0 0 50px rgba(255,0,0,0.4)',
            }}
          >
            {/* Play triangle */}
            <svg width="40" height="40" viewBox="0 0 24 24">
              <polygon points="6,4 6,20 20,12" fill="#F5F5F5" />
            </svg>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 48,
                fontWeight: 900,
                color: '#F5F5F5',
                letterSpacing: 4,
                textTransform: 'uppercase',
              }}
            >
              Subscribe + Share
            </div>
          </div>
        </div>

        {/* See you in the next one */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: seeOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 34,
              fontWeight: 700,
              color: '#609966',
              letterSpacing: 6,
              textTransform: 'uppercase',
              fontStyle: 'italic',
            }}
          >
            See You In The Next One
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, Poppins',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
          letterSpacing: 1,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene40_Outro_ShareSave;
```

