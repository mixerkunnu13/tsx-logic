```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_HiddenEMI',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_HiddenEMI: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06], { extrapolateRight: 'clamp' });

  const q1 = sp(20);
  const q2 = sp(45);
  const emiPop = sp(75, { damping: 8, stiffness: 160 });
  const lineGrow = interpolate(frame, [60, 110], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const shake = frame > 75 && frame < 90 ? Math.sin(frame * 2) * (90 - frame) * 0.4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FDFFBC', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 40%, rgba(255,255,255,0.4), rgba(246,36,64,0.06))' }} />

      {/* Shape layers - drifting transparent geometry */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 1);
        const drift = Math.sin(frame * 0.012 + i) * 40;
        const rot = frame * 0.1 * (i % 2 === 0 ? 1 : -1);
        const breath = 1 + Math.sin(frame * 0.02 + i) * 0.08;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r * 80}%`,
            top: `${seededRandom(i + 9) * 70}%`,
            width: 320 + r * 200,
            height: 320 + r * 200,
            border: `3px solid rgba(0,0,0,${0.05 + r * 0.03})`,
            borderRadius: i % 2 === 0 ? '50%' : '20px',
            transform: `translateY(${drift}px) rotate(${rot}deg) scale(${breath})`,
          }} />
        );
      })}

      {/* Motion texture - vertical lines grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <line key={i} x1={i * 100 + (frame * 0.3 % 100)} y1="0" x2={i * 100 + (frame * 0.3 % 100)} y2="1080" stroke="#000000" strokeWidth="1" />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 30);
        const y = (r * 1080 - frame * (0.6 + r)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r * 100}%`,
            top: `${(y + 1080) % 1080}px`,
            width: 6 + r * 8,
            height: 6 + r * 8,
            borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#F62440' : '#000000',
            opacity: 0.18,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)`, justifyContent: 'center', alignItems: 'center' }}>
        <div style={{ textAlign: 'center', padding: '0 120px' }}>
          <div style={{
            fontFamily: 'Inter, Poppins, sans-serif',
            fontSize: 42,
            fontWeight: 700,
            color: '#F62440',
            letterSpacing: 4,
            opacity: q1,
            transform: `translateY(${(1 - q1) * 40}px)`,
            marginBottom: 30,
          }}>एक सीधा सवाल</div>

          <div style={{
            fontFamily: "'Yantra One', sans-serif",
            fontSize: 110,
            fontWeight: 800,
            color: '#000000',
            lineHeight: 1.1,
            opacity: q2,
            transform: `translateY(${(1 - q2) * 60}px)`,
          }}>
            Private School Fee<br />या छुपी हुई
          </div>

          {/* EMI big reveal */}
          <div style={{
            display: 'inline-block',
            marginTop: 40,
            transform: `scale(${emiPop})`,
          }}>
            <div style={{
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 200,
              fontWeight: 900,
              color: '#F62440',
              lineHeight: 1,
              textShadow: '6px 6px 0 rgba(0,0,0,0.15)',
            }}>EMI?</div>
            <div style={{
              height: 10,
              width: `${lineGrow * 100}%`,
              backgroundColor: '#000000',
              margin: '10px auto 0',
              borderRadius: 6,
            }} />
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.25)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene01_HiddenEMI;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_AprilEnvelope',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_AprilEnvelope: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [-30, 30], { extrapolateRight: 'clamp' });

  const envIn = sp(15, { damping: 10 });
  const flapOpen = interpolate(frame, [50, 80], [0, 180], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const paperUp = interpolate(frame, [75, 110], [60, -40], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.back(1.5)) });
  const paperOp = interpolate(frame, [75, 95], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const nameIn = sp(130);
  const heartBeat = 1 + Math.sin(frame * 0.3) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FDFFBC', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, rgba(255,255,255,0.5), rgba(246,36,64,0.05))' }} />

      {/* Shape layers */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 5);
        const breath = 1 + Math.sin(frame * 0.025 + i) * 0.1;
        return (
          <div key={i} style={{
            position: 'absolute',
            right: `${r * 60}%`,
            top: `${seededRandom(i + 12) * 60}%`,
            width: 280 + r * 250,
            height: 280 + r * 250,
            border: `4px solid rgba(246,36,64,${0.04 + r * 0.03})`,
            transform: `rotate(${45 + frame * 0.08}deg) scale(${breath})`,
          }} />
        );
      })}

      {/* Motion texture - diagonal dashes */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 15 }).map((_, i) => (
          <line key={i} x1="0" y1={i * 80 + (frame * 0.4 % 80)} x2="1920" y2={i * 80 - 200 + (frame * 0.4 % 80)} stroke="#000000" strokeWidth="2" strokeDasharray="20 30" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 40);
        const y = (r * 1080 - frame * (0.5 + r)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${(y + 1080) % 1080}px`,
            width: 5 + r * 7, height: 5 + r * 7, borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#F62440' : '#000000', opacity: 0.15,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* APRIL label */}
        <div style={{
          fontFamily: 'Inter, sans-serif', fontSize: 60, fontWeight: 800, color: '#F62440',
          letterSpacing: 8, marginBottom: 30, opacity: envIn,
        }}>हर APRIL</div>

        {/* Envelope SVG */}
        <div style={{ position: 'relative', transform: `scale(${envIn})`, marginBottom: 50 }}>
          {/* Paper coming out */}
          <div style={{
            position: 'absolute', left: 60, top: paperUp, width: 380, height: 200,
            backgroundColor: '#F5F5F5', border: '3px solid #000000', borderRadius: 8,
            opacity: paperOp, padding: 20, boxSizing: 'border-box', zIndex: flapOpen > 90 ? 1 : 3,
          }}>
            <div style={{ height: 14, width: '70%', backgroundColor: '#000000', borderRadius: 4, marginBottom: 14, opacity: 0.7 }} />
            <div style={{ height: 14, width: '90%', backgroundColor: '#000000', borderRadius: 4, marginBottom: 14, opacity: 0.5 }} />
            <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 56, fontWeight: 900, color: '#F62440' }}>FEE ↑</div>
          </div>
          {/* Envelope body */}
          <svg width="500" height="320" viewBox="0 0 500 320" style={{ position: 'relative', zIndex: 2 }}>
            <rect x="40" y="100" width="420" height="200" rx="10" fill="#F62440" stroke="#000000" strokeWidth="5" />
            <g style={{ transform: `rotateX(${flapOpen}deg)`, transformOrigin: '250px 100px' }}>
              <polygon points="40,100 250,230 460,100" fill="#000000" stroke="#000000" strokeWidth="5" />
            </g>
          </svg>
        </div>

        {/* Heart sinking */}
        <div style={{ display: 'flex', alignItems: 'center', gap: 20, transform: `scale(${heartBeat})`, opacity: paperOp }}>
          <svg width="70" height="70" viewBox="0 0 24 24" fill="#F62440" stroke="#000000" strokeWidth="1.5">
            <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z" />
          </svg>
          <div style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 64, fontWeight: 800, color: '#000000' }}>दिल बैठ जाता है</div>
        </div>

        {/* Name tag */}
        <div style={{
          marginTop: 50, opacity: nameIn, transform: `translateY(${(1 - nameIn) * 30}px)`,
          backgroundColor: '#000000', padding: '14px 40px', borderRadius: 50,
        }}>
          <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 38, fontWeight: 700, color: '#FDFFBC' }}>मैं कुनाल — ProBuffet HQ</span>
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.25)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene02_AprilEnvelope;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_SalariedStory',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_SalariedStory: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt
  const camRot = interpolate(frame, [0, durationInFrames], [-1.5, 1.5], { extrapolateRight: 'clamp' });

  const title = sp(15);
  const word = sp(50, { damping: 9, stiffness: 150 });
  const wordPush = interpolate(frame, [70, 100], [0, 400], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const brainFade = interpolate(frame, [70, 100], [1, 0.2], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const letterIn = sp(120);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FDFFBC', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 30% 50%, rgba(255,255,255,0.45), transparent)' }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 3);
        const breath = 1 + Math.sin(frame * 0.02 + i) * 0.09;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 75}%`, top: `${seededRandom(i + 20) * 65}%`,
            width: 250 + r * 220, height: 250 + r * 220,
            border: `3px solid rgba(0,0,0,${0.04 + r * 0.04})`,
            borderRadius: i % 3 === 0 ? '50%' : '0',
            transform: `rotate(${frame * 0.06 * (i % 2 ? 1 : -1)}deg) scale(${breath})`,
          }} />
        );
      })}

      {/* Motion texture - concentric circles */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05, left: '60%', top: '20%' }}>
        {[0, 1, 2, 3, 4].map((i) => (
          <circle key={i} cx="200" cy="300" r={60 + i * 70 + Math.sin(frame * 0.03) * 10} fill="none" stroke="#F62440" strokeWidth="2" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 50);
        const y = (r * 1080 - frame * (0.4 + r)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${(y + 1080) % 1080}px`,
            width: 5 + r * 6, height: 5 + r * 6, borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#F62440' : '#000000', opacity: 0.15,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)`, justifyContent: 'center', alignItems: 'center' }}>
        <div style={{
          fontFamily: "'Yantra One', sans-serif", fontSize: 90, fontWeight: 800, color: '#000000',
          opacity: title, transform: `translateY(${(1 - title) * 40}px)`, marginBottom: 60, textAlign: 'center',
        }}>ये Salaried पीढ़ी की कहानी</div>

        {/* "फ्यूचर" word pushing financial sense aside */}
        <div style={{ display: 'flex', alignItems: 'center', gap: 40, position: 'relative' }}>
          <div style={{
            transform: `scale(${word})`,
            backgroundColor: '#F62440', padding: '24px 60px', borderRadius: 20,
            boxShadow: '8px 8px 0 #000000',
          }}>
            <span style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 130, fontWeight: 900, color: '#FDFFBC' }}>"फ्यूचर"</span>
          </div>

          <div style={{
            transform: `translateX(${wordPush}px)`, opacity: brainFade,
            fontFamily: 'Inter, sans-serif', fontSize: 56, fontWeight: 700, color: '#000000',
            textAlign: 'left',
          }}>Financial<br />समझ →</div>
        </div>

        {/* Fee letter strip */}
        <div style={{
          marginTop: 70, opacity: letterIn, transform: `translateY(${(1 - letterIn) * 30}px)`,
          fontFamily: 'Inter, sans-serif', fontSize: 48, fontWeight: 600, color: '#000000',
          borderTop: '4px solid #F62440', paddingTop: 24,
        }}>मार्च-अप्रैल Fee Letter में सिर्फ <span style={{ color: '#F62440', fontWeight: 900 }}>tuition नहीं</span></div>
      </AbsoluteFill>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.25)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene03_SalariedStory;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_HiddenCharges',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_HiddenCharges: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1], { extrapolateRight: 'clamp' });

  const charges = ['Activity Charge', 'Transport', 'Smart Class', 'ID Card', 'Diary', 'Annual Function', 'Examination Fee', 'Enrichment'];
  const titleIn = sp(10);
  const smileIn = sp(170);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FDFFBC', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(160deg, rgba(255,255,255,0.5), rgba(246,36,64,0.04))' }} />

      {/* Shape layers */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 7);
        const breath = 1 + Math.sin(frame * 0.022 + i) * 0.08;
        return (
          <div key={i} style={{
            position: 'absolute', right: `${r * 50}%`, top: `${seededRandom(i + 15) * 55}%`,
            width: 300 + r * 200, height: 300 + r * 200,
            border: `3px solid rgba(0,0,0,${0.04 + r * 0.03})`, borderRadius: '50%',
            transform: `scale(${breath}) rotate(${frame * 0.05}deg)`,
          }} />
        );
      })}

      {/* Motion texture - grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        {Array.from({ length: 12 }).map((_, i) => (
          <line key={'h' + i} x1="0" y1={i * 90} x2="1920" y2={i * 90} stroke="#000000" strokeWidth="1" />
        ))}
        {Array.from({ length: 20 }).map((_, i) => (
          <line key={'v' + i} x1={i * 100} y1="0" x2={i * 100} y2="1080" stroke="#000000" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const r = seededRandom(i + 60);
        const y = (r * 1080 - frame * (0.5 + r)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${(y + 1080) % 1080}px`,
            width: 5 + r * 6, height: 5 + r * 6, borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#F62440' : '#000000', opacity: 0.14,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, justifyContent: 'flex-start', alignItems: 'center', paddingTop: 80 }}>
        <div style={{
          fontFamily: "'Yantra One', sans-serif", fontSize: 72, fontWeight: 800, color: '#F62440',
          opacity: titleIn, transform: `translateY(${(1 - titleIn) * 30}px)`, marginBottom: 50,
        }}>सिर्फ Tuition नहीं... ये सब भी</div>

        {/* Charges grid - staggered chips */}
        <div style={{ display: 'flex', flexWrap: 'wrap', gap: 24, justifyContent: 'center', maxWidth: 1400 }}>
          {charges.map((c, i) => {
            const chip = sp(20 + i * 14, { damping: 10, stiffness: 140 });
            return (
              <div key={i} style={{
                transform: `scale(${chip}) translateY(${(1 - chip) * 30}px)`,
                backgroundColor: i % 2 === 0 ? '#000000' : '#F62440',
                padding: '22px 42px', borderRadius: 16,
                boxShadow: '5px 5px 0 rgba(0,0,0,0.2)',
              }}>
                <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 46, fontWeight: 700, color: '#FDFFBC' }}>{c}</span>
              </div>
            );
          })}
        </div>

        {/* Cold smile reaction */}
        <div style={{
          marginTop: 60, opacity: smileIn, transform: `scale(${smileIn})`,
          display: 'flex', alignItems: 'center', gap: 24,
          border: '4px dashed #F62440', padding: '20px 50px', borderRadius: 20,
        }}>
          <svg width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="#000000" strokeWidth="2">
            <circle cx="12" cy="12" r="10" />
            <line x1="8" y1="15" x2="16" y2="15" />
            <line x1="9" y1="9" x2="9" y2="9.5" />
            <line x1="15" y1="9" x2="15" y2="9.5" />
          </svg>
          <span style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 50, fontWeight: 700, color: '#000000' }}>"कहीं और admission करवा लो"</span>
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.25)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene04_HiddenCharges;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_SilentInflation',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_SilentInflation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camX = interpolate(frame, [0, durationInFrames], [20, -20], { extrapolateRight: 'clamp' });

  const titleIn = sp(15);
  // Counters
  const c1 = Math.floor(interpolate(frame, [120, 170], [0, 27100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));
  const c2 = Math.floor(interpolate(frame, [140, 200], [0, 47000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));
  const cardA = sp(110, { damping: 11 });
  const cardB = sp(135, { damping: 11 });
  // Rising bar chart for inflation
  const barGrow = interpolate(frame, [30, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FDFFBC', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 70% 60%, rgba(246,36,64,0.07), rgba(255,255,255,0.3))' }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 11);
        const breath = 1 + Math.sin(frame * 0.02 + i) * 0.1;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 78}%`, top: `${seededRandom(i + 22) * 60}%`,
            width: 240 + r * 230, height: 240 + r * 230,
            border: `3px solid rgba(0,0,0,${0.04 + r * 0.03})`, borderRadius: i % 2 ? '30px' : '50%',
            transform: `rotate(${frame * 0.07 * (i % 2 ? -1 : 1)}deg) scale(${breath})`,
          }} />
        );
      })}

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 70);
        const y = (r * 1080 - frame * (0.6 + r)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${(y + 1080) % 1080}px`,
            width: 5 + r * 7, height: 5 + r * 7, borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#F62440' : '#000000', opacity: 0.15,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)`, justifyContent: 'center', alignItems: 'center' }}>
        <div style={{
          fontFamily: "'Yantra One', sans-serif", fontSize: 96, fontWeight: 900, color: '#000000',
          opacity: titleIn, transform: `translateY(${(1 - titleIn) * 40}px)`, textAlign: 'center', marginBottom: 20,
        }}>India का सबसे बड़ा<br /><span style={{ color: '#F62440' }}>Silent Inflation</span></div>

        {/* Rising bars visual */}
        <div style={{ display: 'flex', alignItems: 'flex-end', gap: 18, height: 200, marginBottom: 50 }}>
          {[0.3, 0.45, 0.6, 0.75, 1].map((h, i) => (
            <div key={i} style={{
              width: 60, height: `${barGrow * h * 200}px`,
              backgroundColor: i === 4 ? '#F62440' : '#000000', borderRadius: '8px 8px 0 0',
            }} />
          ))}
        </div>

        {/* Survey cards */}
        <div style={{ display: 'flex', gap: 50 }}>
          <div style={{
            transform: `scale(${cardA})`, backgroundColor: '#000000', padding: '30px 50px',
            borderRadius: 20, textAlign: 'center', boxShadow: '6px 6px 0 #F62440',
          }}>
            <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 90, fontWeight: 900, color: '#FDFFBC' }}>{c1.toLocaleString()}</div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 36, fontWeight: 600, color: '#F62440' }}>जिले</div>
          </div>
          <div style={{
            transform: `scale(${cardB})`, backgroundColor: '#F62440', padding: '30px 50px',
            borderRadius: 20, textAlign: 'center', boxShadow: '6px 6px 0 #000000',
          }}>
            <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 90, fontWeight: 900, color: '#FDFFBC' }}>{c2.toLocaleString()}+</div>
            <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 36, fontWeight: 600, color: '#000000' }}>Parents</div>
          </div>
        </div>

        <div style={{ marginTop: 36, fontFamily: 'Inter, sans-serif', fontSize: 34, fontWeight: 600, color: '#000000', opacity: 0.7 }}>Local Circles Survey</div>
      </AbsoluteFill>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.25)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene05_SilentInflation;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_SurveyStats',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_SurveyStats: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05], { extrapolateRight: 'clamp' });

  // Donut 70%
  const donutProg = interpolate(frame, [20, 90], [0, 0.7], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const count70 = Math.floor(interpolate(frame, [20, 90], [0, 70], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));
  const radius = 130;
  const circ = 2 * Math.PI * radius;

  const card33 = sp(110, { damping: 9, stiffness: 150 });
  const count33 = Math.floor(interpolate(frame, [110, 160], [0, 33], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#FDFFBC', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(120deg, rgba(255,255,255,0.5), rgba(246,36,64,0.05))' }} />

      {/* Shape layers */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 13);
        const breath = 1 + Math.sin(frame * 0.024 + i) * 0.09;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 70}%`, top: `${seededRandom(i + 25) * 60}%`,
            width: 280 + r * 200, height: 280 + r * 200,
            border: `3px solid rgba(0,0,0,${0.04 + r * 0.03})`, borderRadius: '40px',
            transform: `rotate(${frame * 0.06}deg) scale(${breath})`,
          }} />
        );
      })}

      {/* Motion texture - dots field */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 100 }).map((_, i) => (
          <circle key={i} cx={(i % 10) * 200 + 50} cy={Math.floor(i / 10) * 120 + 30} r={3 + Math.sin(frame * 0.05 + i) * 1.5} fill="#000000" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 80);
        const y = (r * 1080 - frame * (0.5 + r)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${(y + 1080) % 1080}px`,
            width: 5 + r * 6, height: 5 + r * 6, borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#F62440' : '#000000', opacity: 0.14,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, justifyContent: 'center', alignItems: 'center', gap: 80, flexDirection: 'row' }}>
        {/* Donut 70% */}
        <div style={{ textAlign: 'center' }}>
          <svg width="320" height="320" viewBox="0 0 320 320">
            <circle cx="160" cy="160" r={radius} fill="none" stroke="rgba(0,0,0,0.12)" strokeWidth="36" />
            <circle cx="160" cy="160" r={radius} fill="none" stroke="#F62440" strokeWidth="36"
              strokeDasharray={circ} strokeDashoffset={circ * (1 - donutProg)} strokeLinecap="round"
              transform="rotate(-90 160 160)" />
            <text x="160" y="150" textAnchor="middle" fontFamily="'JetBrains Mono', monospace" fontSize="90" fontWeight="900" fill="#000000">{count70}%</text>
            <text x="160" y="205" textAnchor="middle" fontFamily="Inter, sans-serif" fontSize="30" fontWeight="600" fill="#000000">Parents</text>
          </svg>
          <div style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 50, fontWeight: 800, color: '#000000', marginTop: 16 }}>2023-26: Fees <span style={{ color: '#F62440' }}>30%+</span> बढ़ीं</div>
        </div>

        {/* 33% card */}
        <div style={{
          transform: `scale(${card33})`, backgroundColor: '#000000', padding: '50px 60px',
          borderRadius: 24, textAlign: 'center', boxShadow: '8px 8px 0 #F62440',
        }}>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 150, fontWeight: 900, color: '#F62440', lineHeight: 1 }}>{count33}%</div>
          <div style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 56, fontWeight: 800, color: '#FDFFBC', marginTop: 16 }}>ने बताई</div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 60, fontWeight: 900, color: '#FDFFBC', marginTop: 8 }}>50-80% बढ़ोतरी</div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.25)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene06_SurveyStats;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_NoRescue',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_NoRescue: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camRot = interpolate(frame, [0, durationInFrames], [1, -1], { extrapolateRight: 'clamp' });

  const titleIn = sp(10);
  const items = ['कोई RBI Rate Cut नहीं', 'कोई Press Release नहीं', 'कोई "महंगाई कम" नहीं'];
  // Two lines: salary hike vs fee
  const lineProg = interpolate(frame, [110, 175], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FDFFBC', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 30%, rgba(255,255,255,0.45), rgba(246,36,64,0.05))' }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 17);
        const breath = 1 + Math.sin(frame * 0.02 + i) * 0.08;
        return (
          <div key={i} style={{
            position: 'absolute', right: `${r * 70}%`, top: `${seededRandom(i + 27) * 60}%`,
            width: 260 + r * 200, height: 260 + r * 200,
            border: `3px solid rgba(0,0,0,${0.04 + r * 0.03})`, borderRadius: i % 2 ? '50%' : '20px',
            transform: `rotate(${frame * 0.05 * (i % 2 ? 1 : -1)}deg) scale(${breath})`,
          }} />
        );
      })}

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 90);
        const y = (r * 1080 - frame * (0.5 + r)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${(y + 1080) % 1080}px`,
            width: 5 + r * 6, height: 5 + r * 6, borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#F62440' : '#000000', opacity: 0.15,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)`, justifyContent: 'center', alignItems: 'center' }}>
        <div style={{
          fontFamily: "'Yantra One', sans-serif", fontSize: 80, fontWeight: 800, color: '#000000',
          opacity: titleIn, transform: `translateY(${(1 - titleIn) * 30}px)`, marginBottom: 40, textAlign: 'center',
        }}>इस Inflation के खिलाफ...</div>

        {/* Crossed out rescue items */}
        <div style={{ display: 'flex', flexDirection: 'column', gap: 22, marginBottom: 50 }}>
          {items.map((it, i) => {
            const row = sp(30 + i * 20, { damping: 11 });
            const strike = interpolate(frame, [50 + i * 20, 80 + i * 20], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
            return (
              <div key={i} style={{
                position: 'relative', transform: `translateX(${(1 - row) * -60}px)`, opacity: row,
                fontFamily: 'Inter, sans-serif', fontSize: 56, fontWeight: 700, color: '#000000',
              }}>
                {it}
                <div style={{ position: 'absolute', left: 0, top: '50%', height: 6, width: `${strike * 100}%`, backgroundColor: '#F62440', borderRadius: 4 }} />
              </div>
            );
          })}
        </div>

        {/* Salary hike vs Fee divergence graph */}
        <svg width="900" height="300" viewBox="0 0 900 300">
          <line x1="60" y1="250" x2="860" y2="250" stroke="#000000" strokeWidth="3" />
          <line x1="60" y1="40" x2="60" y2="250" stroke="#000000" strokeWidth="3" />
          {/* salary slow */}
          <polyline points={`60,230 ${60 + lineProg * 800},${230 - lineProg * 40}`} fill="none" stroke="#000000" strokeWidth="6" strokeLinecap="round" />
          {/* fee steep */}
          <polyline points={`60,230 ${60 + lineProg * 800},${230 - lineProg * 180}`} fill="none" stroke="#F62440" strokeWidth="8" strokeLinecap="round" />
          <text x={70 + lineProg * 770} y={235 - lineProg * 40} fontFamily="Inter" fontSize="30" fontWeight="700" fill="#000000">Salary Hike</text>
          <text x={70 + lineProg * 770} y={235 - lineProg * 180} fontFamily="Inter" fontSize="32" fontWeight="800" fill="#F62440">Fee ↑↑</text>
        </svg>
      </AbsoluteFill>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.25)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene07_NoRescue;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_Tier1Fee',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_Tier1Fee: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1.06, 1], { extrapolateRight: 'clamp' });

  const titleIn = sp(12);
  const cardIn = sp(70, { damping: 9, stiffness: 140 });
  const amount = (interpolate(frame, [80, 150], [0, 2.1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) })).toFixed(1);
  const shake = frame > 145 && frame < 162 ? Math.sin(frame * 2.5) * (162 - frame) * 0.5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FDFFBC', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(150deg, rgba(255,255,255,0.5), rgba(246,36,64,0.06))' }} />

      {/* Shape layers - buildings vibe */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 19);
        const breath = 1 + Math.sin(frame * 0.022 + i) * 0.08;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 75}%`, bottom: 0,
            width: 120 + r * 80, height: 300 + r * 250,
            border: `3px solid rgba(0,0,0,${0.04 + r * 0.03})`,
            transform: `scale(${breath})`,
          }} />
        );
      })}

      {/* Motion texture - rising lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 16 }).map((_, i) => (
          <line key={i} x1={i * 120} y1="1080" x2={i * 120} y2={300 + Math.sin(frame * 0.04 + i) * 40} stroke="#000000" strokeWidth="2" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 100);
        const y = (r * 1080 - frame * (0.6 + r)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${(y + 1080) % 1080}px`,
            width: 5 + r * 7, height: 5 + r * 7, borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#F62440' : '#000000', opacity: 0.15,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)`, justifyContent: 'center', alignItems: 'center' }}>
        <div style={{
          fontFamily: "'Yantra One', sans-serif", fontSize: 76, fontWeight: 800, color: '#000000',
          opacity: titleIn, transform: `translateY(${(1 - titleIn) * 30}px)`, marginBottom: 50, textAlign: 'center',
        }}>Tier-1 शहरों में Number <span style={{ color: '#F62440' }}>कई गुना</span></div>

        {/* Big fee receipt card */}
        <div style={{
          transform: `scale(${cardIn})`, backgroundColor: '#F5F5F5', border: '5px solid #000000',
          borderRadius: 20, padding: '50px 70px', textAlign: 'center', boxShadow: '10px 10px 0 #F62440',
        }}>
          <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 16, marginBottom: 20 }}>
            <svg width="50" height="50" viewBox="0 0 24 24" fill="#F62440" stroke="#000000" strokeWidth="1.5">
              <path d="M12 3L2 9v12h6v-7h8v7h6V9L12 3z" />
            </svg>
            <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 44, fontWeight: 700, color: '#000000' }}>Noida · Shiv Nadar School</span>
          </div>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 38, fontWeight: 600, color: '#000000', opacity: 0.7 }}>Admission Fee अकेले</div>
          <div style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: 180, fontWeight: 900, color: '#F62440', lineHeight: 1.1 }}>₹{amount} लाख</div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.25)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene08_Tier1Fee;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_WhyProtest',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_WhyProtest: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camX = interpolate(frame, [0, durationInFrames], [-25, 25], { extrapolateRight: 'clamp' });

  const notIn = sp(20, { damping: 9 });
  const strike = interpolate(frame, [55, 85], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const realIn = sp(110, { damping: 10, stiffness: 140 });
  const dots = interpolate(frame, [170, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FDFFBC', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 40% 50%, rgba(255,255,255,0.45), rgba(246,36,64,0.05))' }} />

      {/* Shape layers */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 21);
        const breath = 1 + Math.sin(frame * 0.02 + i) * 0.09;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 76}%`, top: `${seededRandom(i + 30) * 62}%`,
            width: 240 + r * 220, height: 240 + r * 220,
            border: `3px solid rgba(0,0,0,${0.04 + r * 0.03})`, borderRadius: i % 2 ? '50%' : '30px',
            transform: `rotate(${frame * 0.06 * (i % 2 ? -1 : 1)}deg) scale(${breath})`,
          }} />
        );
      })}

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 110);
        const y = (r * 1080 - frame * (0.5 + r)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${(y + 1080) % 1080}px`,
            width: 5 + r * 6, height: 5 + r * 6, borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#F62440' : '#000000', opacity: 0.15,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)`, justifyContent: 'center', alignItems: 'center', padding: '0 140px' }}>
        {/* Crossed myth */}
        <div style={{ position: 'relative', opacity: notIn, transform: `scale(${notIn})`, marginBottom: 60 }}>
          <div style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 78, fontWeight: 800, color: '#000000', textAlign: 'center' }}>
            "लोग पढ़ाई नहीं चाहते"
          </div>
          <div style={{ position: 'absolute', left: 0, top: '50%', height: 8, width: `${strike * 100}%`, backgroundColor: '#F62440', borderRadius: 4, transform: 'rotate(-2deg)' }} />
        </div>

        {/* Real reason */}
        <div style={{
          transform: `scale(${realIn}) translateY(${(1 - realIn) * 40}px)`,
          backgroundColor: '#000000', padding: '40px 70px', borderRadius: 24,
          textAlign: 'center', boxShadow: '8px 8px 0 #F62440', maxWidth: 1300,
        }}>
          <div style={{ fontFamily: 'Inter, sans-serif', fontSize: 40, fontWeight: 600, color: '#F62440', marginBottom: 16 }}>असली बात</div>
          <div style={{ fontFamily: "'Yantra One', sans-serif", fontSize: 72, fontWeight: 800, color: '#FDFFBC', lineHeight: 1.2 }}>
            लोग समझ नहीं पा रहे कि<span style={{ opacity: dots }}>...</span>
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.25)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene09_WhyProtest;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_SqueezePoint',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_SqueezePoint: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05], { extrapolateRight: 'clamp' });

  const rows = [
    { label: 'Salary', val: '+7%', icon: 'up', color: '#000000' },
    { label: 'GST', val: 'जेब काट रहा', icon: 'cut', color: '#000000' },
    { label: 'Home Loan EMI', val: 'चढ़ी हुई', icon: 'load', color: '#000000' },
    { label: 'School Fee', val: '+40%', icon: 'jump', color: '#F62440' },
  ];

  const shake = frame > 130 && frame < 148 ? Math.sin(frame * 2.5) * (148 - frame) * 0.4 : 0;
  const govIn = sp(165, { damping: 9, stiffness: 150 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#FDFFBC', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, rgba(255,255,255,0.5), rgba(246,36,64,0.06))' }} />

      {/* Shape layers */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 23);
        const breath = 1 + Math.sin(frame * 0.024 + i) * 0.08;
        return (
          <div key={i} style={{
            position: 'absolute', right: `${r * 65}%`, top: `${seededRandom(i + 32) * 58}%`,
            width: 280 + r * 200, height: 280 + r * 200,
            border: `3px solid rgba(0,0,0,${0.04 + r * 0.03})`, borderRadius: '50%',
            transform: `rotate(${frame * 0.05}deg) scale(${breath})`,
          }} />
        );
      })}

      {/* Motion texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 14 }).map((_, i) => (
          <line key={i} x1="0" y1={i * 90 + (frame * 0.3 % 90)} x2="1920" y2={i * 90 + (frame * 0.3 % 90)} stroke="#000000" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const r = seededRandom(i + 120);
        const y = (r * 1080 - frame * (0.5 + r)) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${(y + 1080) % 1080}px`,
            width: 5 + r * 6, height: 5 + r * 6, borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#F62440' : '#000000', opacity: 0.14,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)`, justifyContent: 'center', alignItems: 'center' }}>
        {/* Squeeze rows */}
        <div style={{ display: 'flex', flexDirection: 'column', gap: 20, marginBottom: 50 }}>
          {rows.map((r, i) => {
            const row = sp(20 + i * 22, { damping: 10, stiffness: 140 });
            const isFee = i === 3;
            return (
              <div key={i} style={{
                transform: `translateX(${(1 - row) * (i % 2 ? 80 : -80)}px) scale(${isFee ? row * (1 + (frame > 130 ? 0.05 : 0)) : row})`,
                opacity: row,
                display: 'flex', alignItems: 'center', justifyContent: 'space-between',
                width: 900, padding: '24px 50px', borderRadius: 16,
                backgroundColor: isFee ? '#F62440' : '#000000',
                boxShadow: isFee ? '7px 7px 0 #000000' : '5px 5px 0 rgba(0,0,0,0.2)',
              }}>
                <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 52, fontWeight: 700, color: '#FDFFBC' }}>{r.label}</span>
                <span style={{ fontFamily: "'JetBrains Mono', monospace", fontSize: isFee ? 72 : 52, fontWeight: 900, color: '#FDFFBC' }}>{r.val}</span>
              </div>
            );
          })}
        </div>

        {/* Government question */}
        <div style={{
          opacity: govIn, transform: `scale(${govIn})`,
          fontFamily: "'Yantra One', sans-serif", fontSize: 76, fontWeight: 800, color: '#000000',
          borderTop: '5px solid #F62440', paddingTop: 24,
        }}>अब सरकार क्या कर रही है?</div>
      </AbsoluteFill>

      <AbsoluteFill style={{ boxShadow: 'inset 0 0 300px rgba(0,0,0,0.25)', pointerEvents: 'none' }} />
      <div style={{ position: 'absolute', bottom: 30, right: 45, fontFamily: 'Inter, sans-serif', fontSize: 22, fontWeight: 600, color: '#9E9E9E', opacity: 0.3 }}>ProBuffet HQ</div>
    </AbsoluteFill>
  );
};

export default Scene10_SqueezePoint;
```

