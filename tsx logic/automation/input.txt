```tsx
import React from 'react';
Audio: "नमस्ते दोस्तों, मैं कुनाल... 6 महीने से हर घर खरीदार के मन का सवाल — सच या बिकवाली की चाल?"

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_HookIntro',
  durationInSeconds: 23,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_HookIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06], { extrapolateRight: 'clamp' });

  // Multi-beat reveal
  const namasteIn = sp(8);
  const kunalIn = sp(35);
  const questionIn = sp(120);
  const wordHighlight = sp(220);
  const finalPunchIn = sp(380);

  // Floating particles
  const particles = Array.from({ length: 28 }, (_, i) => ({
    x: seededRandom(i + 1) * 1920,
    y: seededRandom(i + 50) * 1080,
    size: 3 + seededRandom(i + 100) * 6,
    speed: 0.3 + seededRandom(i + 150) * 0.6,
  }));

  // Floating shapes
  const shape1Rot = frame * 0.15;
  const shape2Rot = -frame * 0.1;
  const shape3Float = Math.sin(frame * 0.03) * 20;

  // Grid texture pulse
  const gridOpacity = 0.08 + Math.sin(frame * 0.05) * 0.02;

  // Underline draw
  const underlineWidth = interpolate(sp(260), [0, 1], [0, 720]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient layer */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 30%, rgba(25,211,218,0.18) 0%, rgba(238,238,238,0) 55%), radial-gradient(circle at 75% 80%, rgba(55,58,64,0.12) 0%, rgba(238,238,238,0) 60%)',
          transform: `scale(${camScale})`,
        }}
      />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: gridOpacity }}>
        <defs>
          <pattern id="gridA" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#373A40" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#gridA)" />
      </svg>

      {/* Shape layers — drifting geometric forms */}
      <div
        style={{
          position: 'absolute',
          top: 120 + shape3Float,
          right: 140,
          width: 320,
          height: 320,
          border: '2px solid #19D3DA',
          opacity: 0.08,
          transform: `rotate(${shape1Rot}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: 200,
          left: 100,
          width: 260,
          height: 260,
          borderRadius: '50%',
          border: '2px solid #373A40',
          opacity: 0.07,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
        }}
      />
      <svg
        style={{
          position: 'absolute',
          top: 400,
          left: 1500,
          opacity: 0.06,
          transform: `rotate(${shape2Rot}deg)`,
        }}
        width="280"
        height="280"
      >
        <polygon points="140,20 260,240 20,240" fill="none" stroke="#19D3DA" strokeWidth="2" />
      </svg>

      {/* Floating particles */}
      {particles.map((p, i) => {
        const py = (p.y + frame * p.speed) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: py,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#19D3DA' : '#373A40',
              opacity: 0.35,
            }}
          />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Top label bar */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 120,
            opacity: namasteIn,
            transform: `translateX(${(1 - namasteIn) * -40}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 18,
          }}
        >
          <div style={{ width: 12, height: 12, backgroundColor: '#19D3DA', borderRadius: '50%' }} />
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 30,
              fontWeight: 600,
              color: '#373A40',
              letterSpacing: 4,
              textTransform: 'uppercase',
            }}
          >
            ProBuffet HQ • Episode
          </div>
          <div style={{ width: 80, height: 2, backgroundColor: '#373A40', opacity: 0.5 }} />
        </div>

        {/* Greeting block */}
        <div
          style={{
            position: 'absolute',
            top: 200,
            left: 120,
            opacity: namasteIn,
            transform: `translateY(${(1 - namasteIn) * 30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 187,
              fontWeight: 700,
              color: '#373A40',
              lineHeight: 1,
            }}
          >
            नमस्ते
          </div>
        </div>

        {/* "मैं कुनाल" */}
        <div
          style={{
            position: 'absolute',
            top: 410,
            left: 130,
            opacity: kunalIn,
            transform: `translateX(${(1 - kunalIn) * -50}px)`,
            display: 'flex',
            alignItems: 'baseline',
            gap: 30,
          }}
        >
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 76,
              fontWeight: 500,
              color: '#373A40',
            }}
          >
            मैं
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 119,
              fontWeight: 700,
              color: '#19D3DA',
              position: 'relative',
            }}
          >
            कुनाल
            <div
              style={{
                position: 'absolute',
                bottom: -8,
                left: 0,
                width: underlineWidth,
                height: 8,
                backgroundColor: '#19D3DA',
                opacity: 0.6,
              }}
            />
          </div>
        </div>

        {/* Question block */}
        <div
          style={{
            position: 'absolute',
            top: 620,
            left: 120,
            right: 120,
            opacity: questionIn,
            transform: `translateY(${(1 - questionIn) * 40}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 51,
              fontWeight: 600,
              color: '#373A40',
              lineHeight: 1.3,
            }}
          >
            पिछले <span style={{ color: '#19D3DA', fontWeight: 800 }}>6 महीने</span> से हर घर खरीदार के मन में
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 51,
              fontWeight: 600,
              color: '#373A40',
              lineHeight: 1.3,
              marginTop: 8,
            }}
          >
            एक ही सवाल गूँज रहा है...
          </div>
        </div>

        {/* Final punch — सच या बिकवाली */}
        <div
          style={{
            position: 'absolute',
            bottom: 130,
            left: 120,
            right: 120,
            opacity: finalPunchIn,
            transform: `scale(${0.85 + finalPunchIn * 0.15})`,
            display: 'flex',
            alignItems: 'center',
            gap: 40,
          }}
        >
          {/* SVG icon — question mark in circle */}
          <svg width="100" height="100" viewBox="0 0 100 100">
            <circle cx="50" cy="50" r="46" fill="none" stroke="#19D3DA" strokeWidth="4" />
            <text x="50" y="68" textAnchor="middle" fontFamily="Inter" fontSize="58" fontWeight="800" fill="#373A40">
              ?
            </text>
          </svg>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 76,
              fontWeight: 700,
              color: '#373A40',
              lineHeight: 1.1,
            }}
          >
            सच है, या सिर्फ <span style={{ color: '#19D3DA' }}>बिकवाली की चाल?</span>
          </div>
        </div>

        {/* Accent corner brackets */}
        <div
          style={{
            position: 'absolute',
            top: 60,
            right: 60,
            width: 60,
            height: 60,
            borderTop: '4px solid #19D3DA',
            borderRight: '4px solid #19D3DA',
            opacity: namasteIn,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            left: 60,
            width: 60,
            height: 60,
            borderBottom: '4px solid #19D3DA',
            borderLeft: '4px solid #19D3DA',
            opacity: namasteIn,
          }}
        />
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.18) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#373A40',
          opacity: 0.4,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_HookIntro;
```

```tsx
import React from 'react';
Audio: "(intro pause) ...यानी पिछले साल इसी तिमाही के मुकाबले इस बार..."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_LastYearCompare',
  durationInSeconds: 23,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_LastYearCompare: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan slightly right
  const camX = interpolate(frame, [0, durationInFrames], [0, -25], { extrapolateRight: 'clamp' });

  const headerIn = sp(10);
  const leftCardIn = sp(60);
  const vsIn = sp(140);
  const rightCardIn = sp(180);
  const arrowIn = sp(300);
  const tagIn = sp(420);

  // Particles
  const particles = Array.from({ length: 22 }, (_, i) => ({
    x: seededRandom(i + 5) * 1920,
    y: seededRandom(i + 60) * 1080,
    size: 2 + seededRandom(i + 110) * 5,
    speed: 0.2 + seededRandom(i + 160) * 0.5,
  }));

  // Diagonal lines texture
  const texOpacity = 0.06;

  // Pulsing rings
  const ring1Scale = 1 + Math.sin(frame * 0.05) * 0.04;
  const ring2Scale = 1 + Math.sin(frame * 0.04 + 1) * 0.05;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, rgba(25,211,218,0.10) 0%, rgba(238,238,238,0) 50%, rgba(55,58,64,0.08) 100%)',
        }}
      />

      {/* Diagonal line texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: texOpacity }}>
        <defs>
          <pattern id="diagB" width="40" height="40" patternUnits="userSpaceOnUse" patternTransform="rotate(45)">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#373A40" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diagB)" />
      </svg>

      {/* Drifting shapes */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 80,
          width: 220,
          height: 220,
          border: '2px solid #19D3DA',
          opacity: 0.07,
          transform: `rotate(${frame * 0.2}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          right: 100,
          width: 280,
          height: 280,
          borderRadius: '50%',
          backgroundColor: '#19D3DA',
          opacity: 0.05,
          transform: `scale(${ring1Scale})`,
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => {
        const py = (p.y + frame * p.speed) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: py,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#19D3DA' : '#373A40',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headerIn,
            transform: `translateY(${(1 - headerIn) * -30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 30,
              fontWeight: 700,
              color: '#19D3DA',
              letterSpacing: 6,
              textTransform: 'uppercase',
            }}
          >
            QUARTERLY COMPARISON
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 102,
              fontWeight: 700,
              color: '#373A40',
              marginTop: 8,
              lineHeight: 1,
            }}
          >
            पिछले साल बनाम <span style={{ color: '#19D3DA' }}>इस बार</span>
          </div>
        </div>

        {/* LEFT CARD — Last Year */}
        <div
          style={{
            position: 'absolute',
            top: 360,
            left: 130,
            width: 700,
            height: 480,
            backgroundColor: '#FFFFFF',
            border: '3px solid #373A40',
            borderRadius: 18,
            padding: 50,
            opacity: leftCardIn,
            transform: `translateX(${(1 - leftCardIn) * -80}px) scale(${0.95 + leftCardIn * 0.05})`,
            boxShadow: '0 18px 50px rgba(55,58,64,0.15)',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 26,
              fontWeight: 700,
              color: '#373A40',
              opacity: 0.6,
              letterSpacing: 3,
            }}
          >
            LAST YEAR • Q
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 51,
              fontWeight: 700,
              color: '#373A40',
              marginTop: 20,
              lineHeight: 1.2,
            }}
          >
            पिछले साल<br />इसी तिमाही
          </div>
          {/* Bar visualization */}
          <div style={{ marginTop: 50, display: 'flex', alignItems: 'flex-end', gap: 14, height: 180 }}>
            {[1, 2, 3, 4, 5].map((b) => (
              <div
                key={b}
                style={{
                  width: 80,
                  height: interpolate(sp(80 + b * 20), [0, 1], [0, 120 + b * 10]),
                  backgroundColor: '#373A40',
                  borderRadius: 6,
                }}
              />
            ))}
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              color: '#373A40',
              marginTop: 24,
              opacity: 0.7,
            }}
          >
            Baseline Sales = 100%
          </div>
        </div>

        {/* VS */}
        <div
          style={{
            position: 'absolute',
            top: 530,
            left: 880,
            opacity: vsIn,
            transform: `scale(${0.6 + vsIn * 0.4}) rotate(${(1 - vsIn) * 90}deg)`,
          }}
        >
          <div
            style={{
              width: 140,
              height: 140,
              borderRadius: '50%',
              backgroundColor: '#19D3DA',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Inter',
              fontSize: 51,
              fontWeight: 900,
              color: '#373A40',
              transform: `scale(${ring2Scale})`,
              boxShadow: '0 0 40px rgba(25,211,218,0.5)',
            }}
          >
            VS
          </div>
        </div>

        {/* RIGHT CARD — This Year */}
        <div
          style={{
            position: 'absolute',
            top: 360,
            right: 130,
            width: 700,
            height: 480,
            backgroundColor: '#373A40',
            borderRadius: 18,
            padding: 50,
            opacity: rightCardIn,
            transform: `translateX(${(1 - rightCardIn) * 80}px) scale(${0.95 + rightCardIn * 0.05})`,
            boxShadow: '0 18px 50px rgba(25,211,218,0.25)',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 26,
              fontWeight: 700,
              color: '#19D3DA',
              letterSpacing: 3,
            }}
          >
            THIS YEAR • SAME Q
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 51,
              fontWeight: 700,
              color: '#EEEEEE',
              marginTop: 20,
              lineHeight: 1.2,
            }}
          >
            इस बार<br />इसी तिमाही
          </div>
          {/* Bar visualization — shorter */}
          <div style={{ marginTop: 50, display: 'flex', alignItems: 'flex-end', gap: 14, height: 180 }}>
            {[1, 2, 3, 4, 5].map((b) => (
              <div
                key={b}
                style={{
                  width: 80,
                  height: interpolate(sp(220 + b * 20), [0, 1], [0, (120 + b * 10) * 0.7]),
                  backgroundColor: '#19D3DA',
                  borderRadius: 6,
                }}
              />
            ))}
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 28,
              color: '#19D3DA',
              marginTop: 24,
            }}
          >
            Visibly lower output
          </div>
        </div>

        {/* Down arrow + tag */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: tagIn,
            transform: `translateY(${(1 - tagIn) * 30}px)`,
          }}
        >
          <svg width="60" height="60" viewBox="0 0 60 60" style={{ verticalAlign: 'middle' }}>
            <path d="M30 8 L30 44 M14 30 L30 46 L46 30" stroke="#19D3DA" strokeWidth="5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 47,
              fontWeight: 700,
              color: '#373A40',
              marginLeft: 24,
              verticalAlign: 'middle',
            }}
          >
            एक बड़ा फ़र्क सामने आया है
          </span>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#373A40',
          opacity: 0.4,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_LastYearCompare;
```

```tsx
import React from 'react';
Audio: "13% कम घर बिके — बड़ा संकेत, बाजार में कुछ बहुत बड़ा हिल रहा है।"

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_ThirteenPercent',
  durationInSeconds: 23,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_ThirteenPercent: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1], { extrapolateRight: 'clamp' });

  const labelIn = sp(10);
  const counterStart = sp(50);
  const numberPunch = sp(120);
  const shakeFrame = frame - 130;
  const shake = shakeFrame > 0 && shakeFrame < 20 ? Math.sin(shakeFrame * 1.5) * (1 - shakeFrame / 20) * 8 : 0;
  const signalIn = sp(260);
  const bottomIn = sp(400);

  // Counter animation 0 → 13
  const counterVal = Math.round(interpolate(sp(50, { damping: 18, stiffness: 80 }), [0, 1], [0, 13]));

  // Particles — falling
  const particles = Array.from({ length: 30 }, (_, i) => ({
    x: seededRandom(i + 3) * 1920,
    y: seededRandom(i + 70) * 1080,
    size: 3 + seededRandom(i + 120) * 5,
    speed: 0.4 + seededRandom(i + 170) * 0.7,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp, transform: `translate(${shake}px, 0)` }}>
      {/* Radial gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 45%, rgba(25,211,218,0.20) 0%, rgba(238,238,238,0) 60%)',
          transform: `scale(${camScale})`,
        }}
      />

      {/* Dot grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.12 }}>
        <defs>
          <pattern id="dotsC" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="1.6" fill="#373A40" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dotsC)" />
      </svg>

      {/* Drifting shapes */}
      <div
        style={{
          position: 'absolute',
          top: 100,
          left: 60,
          width: 200,
          height: 200,
          border: '2px solid #19D3DA',
          borderRadius: '50%',
          opacity: 0.08,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
        }}
      />
      <svg
        style={{
          position: 'absolute',
          bottom: 80,
          right: 100,
          opacity: 0.07,
          transform: `rotate(${frame * 0.18}deg)`,
        }}
        width="260"
        height="260"
      >
        <polygon points="130,15 245,225 15,225" fill="none" stroke="#373A40" strokeWidth="2" />
      </svg>

      {/* Particles */}
      {particles.map((p, i) => {
        const py = (p.y + frame * p.speed) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: py,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#19D3DA' : '#373A40',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 110,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: labelIn,
            transform: `translateY(${(1 - labelIn) * -25}px)`,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '14px 36px',
              border: '2px solid #373A40',
              borderRadius: 50,
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 700,
              color: '#373A40',
              letterSpacing: 5,
              textTransform: 'uppercase',
              backgroundColor: 'rgba(238,238,238,0.7)',
            }}
          >
            Sales Drop • Q-on-Q
          </div>
        </div>

        {/* GIANT 13% */}
        <div
          style={{
            position: 'absolute',
            top: 260,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: counterStart,
            transform: `scale(${0.5 + numberPunch * 0.5})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontWeight: 900,
              fontSize: 442,
              color: '#19D3DA',
              lineHeight: 1,
              display: 'inline-block',
              textShadow: '0 10px 0 rgba(55,58,64,0.15)',
            }}
          >
            {counterVal}<span style={{ fontSize: 221, color: '#373A40' }}>%</span>
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 38,
              fontWeight: 700,
              color: '#373A40',
              letterSpacing: 8,
              marginTop: -20,
            }}
          >
            ▼ FEWER HOMES SOLD
          </div>
        </div>

        {/* Signal label */}
        <div
          style={{
            position: 'absolute',
            top: 800,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: signalIn,
            transform: `translateY(${(1 - signalIn) * 30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 60,
              fontWeight: 700,
              color: '#373A40',
            }}
          >
            ये कोई छोटी संख्या नहीं — <span style={{ color: '#19D3DA' }}>एक बड़ा संकेत है</span>
          </div>
        </div>

        {/* Bottom: market shaking — wavy line */}
        <div
          style={{
            position: 'absolute',
            bottom: 100,
            left: 0,
            right: 0,
            opacity: bottomIn,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 28,
          }}
        >
          <svg width="900" height="80" viewBox="0 0 900 80">
            <path
              d={`M0 40 ${Array.from({ length: 90 }, (_, i) => {
                const x = i * 10;
                const y = 40 + Math.sin(i * 0.4 + frame * 0.2) * 22;
                return `L${x} ${y}`;
              }).join(' ')}`}
              stroke="#19D3DA"
              strokeWidth="4"
              fill="none"
              strokeLinecap="round"
            />
          </svg>
        </div>

        {/* Side accent */}
        <div
          style={{
            position: 'absolute',
            top: '50%',
            left: 60,
            transform: 'translateY(-50%)',
            width: 6,
            height: 240,
            backgroundColor: '#19D3DA',
            opacity: labelIn,
          }}
        />
        <div
          style={{
            position: 'absolute',
            top: '50%',
            right: 60,
            transform: 'translateY(-50%)',
            width: 6,
            height: 240,
            backgroundColor: '#373A40',
            opacity: labelIn,
          }}
        />
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#373A40',
          opacity: 0.4,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene03_ThirteenPercent;
```

```tsx
import React from 'react';
Audio: "बैंगलुरू, हैदराबाद, पुणे, गुरुग्राम, नॉएडा — रियल एस्टेट इंजन IT सैलरी पर चलता था।"

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_FiveCities',
  durationInSeconds: 23,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_FiveCities: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camY = interpolate(frame, [0, durationInFrames], [10, -10], { extrapolateRight: 'clamp' });

  const headerIn = sp(8);
  const cities = ['बैंगलुरू', 'हैदराबाद', 'पुणे', 'गुरुग्राम', 'नॉएडा'];
  const cityDelays = [60, 90, 120, 150, 180];

  const engineIn = sp(260);
  const arrowIn = sp(340);
  const fuelIn = sp(400);

  // Particles
  const particles = Array.from({ length: 25 }, (_, i) => ({
    x: seededRandom(i + 4) * 1920,
    y: seededRandom(i + 80) * 1080,
    size: 2 + seededRandom(i + 130) * 4,
    speed: 0.3 + seededRandom(i + 180) * 0.5,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(238,238,238,1) 0%, rgba(25,211,218,0.10) 100%)',
        }}
      />

      {/* Skyline silhouette texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="gridD" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#373A40" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#gridD)" />
      </svg>

      {/* Drifting shapes */}
      <div
        style={{
          position: 'absolute',
          top: 60,
          right: 80,
          width: 280,
          height: 280,
          backgroundColor: '#19D3DA',
          opacity: 0.06,
          transform: `rotate(${frame * 0.12}deg)`,
          borderRadius: 30,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          left: 100,
          width: 220,
          height: 220,
          border: '2px solid #373A40',
          opacity: 0.08,
          transform: `scale(${1 + Math.sin(frame * 0.05) * 0.05})`,
          borderRadius: '50%',
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => {
        const py = (p.y + frame * p.speed) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: py,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#19D3DA' : '#373A40',
              opacity: 0.28,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headerIn,
            transform: `translateY(${(1 - headerIn) * -25}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 700,
              color: '#19D3DA',
              letterSpacing: 6,
              textTransform: 'uppercase',
            }}
          >
            India's Real Estate Engine
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 94,
              fontWeight: 700,
              color: '#373A40',
              marginTop: 4,
            }}
          >
            5 शहर • <span style={{ color: '#19D3DA' }}>एक ही ईंधन</span>
          </div>
        </div>

        {/* City buildings row */}
        <div
          style={{
            position: 'absolute',
            top: 340,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'flex-end',
            gap: 28,
            padding: '0 80px',
          }}
        >
          {cities.map((c, i) => {
            const ci = sp(cityDelays[i]);
            const bH = 160 + i * 30;
            return (
              <div
                key={c}
                style={{
                  opacity: ci,
                  transform: `translateY(${(1 - ci) * 60}px)`,
                  display: 'flex',
                  flexDirection: 'column',
                  alignItems: 'center',
                }}
              >
                {/* Building SVG */}
                <svg width="160" height={bH + 40} viewBox={`0 0 160 ${bH + 40}`}>
                  <rect x="20" y="20" width="120" height={bH} fill="#373A40" rx="4" />
                  <rect x="65" y="0" width="30" height="22" fill="#19D3DA" />
                  {/* Windows */}
                  {Array.from({ length: Math.floor(bH / 30) }).map((_, r) =>
                    [0, 1, 2].map((cIdx) => (
                      <rect
                        key={`${r}-${cIdx}`}
                        x={35 + cIdx * 32}
                        y={35 + r * 30}
                        width="20"
                        height="16"
                        fill={(r + cIdx + Math.floor(frame / 25)) % 4 === 0 ? '#19D3DA' : '#EEEEEE'}
                        opacity="0.85"
                      />
                    ))
                  )}
                </svg>
                <div
                  style={{
                    fontFamily: 'Noto Sans Devanagari',
                    fontSize: 36,
                    fontWeight: 700,
                    color: '#373A40',
                    marginTop: 14,
                  }}
                >
                  {c}
                </div>
              </div>
            );
          })}
        </div>

        {/* Arrow down */}
        <div
          style={{
            position: 'absolute',
            top: 820,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: arrowIn,
          }}
        >
          <svg width="50" height="50" viewBox="0 0 50 50">
            <path d="M25 8 L25 38 M14 28 L25 40 L36 28" stroke="#373A40" strokeWidth="4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>

        {/* Fuel bar — IT Salary */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: fuelIn,
            transform: `translateY(${(1 - fuelIn) * 40}px)`,
          }}
        >
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 24,
              padding: '24px 56px',
              backgroundColor: '#373A40',
              borderRadius: 16,
              boxShadow: '0 14px 40px rgba(25,211,218,0.3)',
            }}
          >
            <svg width="56" height="56" viewBox="0 0 56 56">
              <rect x="6" y="14" width="32" height="38" rx="3" fill="none" stroke="#19D3DA" strokeWidth="3" />
              <path d="M40 22 L48 22 L48 40 Q48 46 42 46" fill="none" stroke="#19D3DA" strokeWidth="3" />
              <rect x="12" y="20" width="20" height="14" fill="#19D3DA" />
            </svg>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 51,
                fontWeight: 800,
                color: '#19D3DA',
                letterSpacing: 2,
              }}
            >
              FUEL = IT SALARIES
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.18) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#373A40',
          opacity: 0.4,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene04_FiveCities;
```

```tsx
import React from 'react';
Audio: "60-70 लाख का फ्लैट EMI पर — यही पक्के ग्राहक, नौकरी पक्की मानी जाती थी।"

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_SixtySeventyEMI',
  durationInSeconds: 23,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_SixtySeventyEMI: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04], { extrapolateRight: 'clamp' });

  const labelIn = sp(10);
  const priceIn = sp(60);
  const dashIn = sp(110);
  const emiIn = sp(180);
  const reasonIn = sp(310);
  const stampIn = sp(450);

  // Counter 0-70
  const priceLow = Math.round(interpolate(sp(60, { damping: 18 }), [0, 1], [0, 60]));
  const priceHigh = Math.round(interpolate(sp(110, { damping: 18 }), [0, 1], [0, 70]));

  // Particles
  const particles = Array.from({ length: 24 }, (_, i) => ({
    x: seededRandom(i + 6) * 1920,
    y: seededRandom(i + 90) * 1080,
    size: 2 + seededRandom(i + 140) * 5,
    speed: 0.3 + seededRandom(i + 190) * 0.6,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 50% 40%, rgba(25,211,218,0.15) 0%, rgba(238,238,238,0) 60%)',
          transform: `scale(${camScale})`,
        }}
      />

      {/* Diag stripe texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="stE" width="30" height="30" patternUnits="userSpaceOnUse" patternTransform="rotate(-30)">
            <line x1="0" y1="0" x2="0" y2="30" stroke="#373A40" strokeWidth="1.2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#stE)" />
      </svg>

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 60,
          width: 240,
          height: 240,
          borderRadius: '50%',
          border: '2px solid #19D3DA',
          opacity: 0.08,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          right: 80,
          width: 300,
          height: 300,
          border: '2px solid #373A40',
          opacity: 0.07,
          transform: `rotate(${frame * 0.15}deg)`,
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => {
        const py = (p.y + frame * p.speed) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: py,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#19D3DA' : '#373A40',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: labelIn,
            transform: `translateY(${(1 - labelIn) * -25}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 700,
              color: '#373A40',
              letterSpacing: 6,
              textTransform: 'uppercase',
            }}
          >
            THE TYPICAL FLAT BUYER
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 79,
              fontWeight: 700,
              color: '#373A40',
              marginTop: 4,
            }}
          >
            EMI पर बनता था <span style={{ color: '#19D3DA' }}>"पक्का ग्राहक"</span>
          </div>
        </div>

        {/* GIANT PRICE RANGE */}
        <div
          style={{
            position: 'absolute',
            top: 290,
            left: 0,
            right: 0,
            textAlign: 'center',
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 30,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontWeight: 900,
              fontSize: 272,
              color: '#373A40',
              opacity: priceIn,
              transform: `translateY(${(1 - priceIn) * 60}px)`,
              lineHeight: 1,
            }}
          >
            ₹{priceLow}
          </div>
          <div
            style={{
              width: 80,
              height: 12,
              backgroundColor: '#19D3DA',
              opacity: dashIn,
              transform: `scaleX(${dashIn})`,
            }}
          />
          <div
            style={{
              fontFamily: 'Inter',
              fontWeight: 900,
              fontSize: 272,
              color: '#19D3DA',
              opacity: dashIn,
              transform: `translateY(${(1 - dashIn) * 60}px)`,
              lineHeight: 1,
            }}
          >
            {priceHigh}
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 85,
              fontWeight: 700,
              color: '#373A40',
              opacity: dashIn,
              marginLeft: 12,
            }}
          >
            लाख
          </div>
        </div>

        {/* EMI tag */}
        <div
          style={{
            position: 'absolute',
            top: 640,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: emiIn,
            transform: `scale(${0.85 + emiIn * 0.15})`,
          }}
        >
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 20,
              padding: '20px 50px',
              border: '3px solid #19D3DA',
              borderRadius: 14,
              backgroundColor: 'rgba(255,255,255,0.6)',
            }}
          >
            <svg width="42" height="42" viewBox="0 0 42 42">
              <rect x="4" y="10" width="34" height="22" rx="3" fill="none" stroke="#373A40" strokeWidth="3" />
              <line x1="4" y1="18" x2="38" y2="18" stroke="#373A40" strokeWidth="3" />
              <circle cx="32" cy="25" r="3" fill="#19D3DA" />
            </svg>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 51,
                fontWeight: 800,
                color: '#373A40',
                letterSpacing: 4,
              }}
            >
              EMI MONTHLY
            </div>
          </div>
        </div>

        {/* Three reason chips */}
        <div
          style={{
            position: 'absolute',
            top: 790,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            gap: 24,
            opacity: reasonIn,
            transform: `translateY(${(1 - reasonIn) * 30}px)`,
          }}
        >
          {['नौकरी पक्की', 'सैलरी हर साल बढ़ती', 'डाउन पेमेंट तैयार'].map((t, i) => (
            <div
              key={i}
              style={{
                padding: '20px 32px',
                backgroundColor: '#373A40',
                borderRadius: 12,
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 37,
                fontWeight: 700,
                color: '#EEEEEE',
                borderTop: '4px solid #19D3DA',
              }}
            >
              {t}
            </div>
          ))}
        </div>

        {/* "PERFECT CUSTOMER" stamp */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            right: 100,
            opacity: stampIn,
            transform: `rotate(${-10 + (1 - stampIn) * 30}deg) scale(${0.6 + stampIn * 0.4})`,
          }}
        >
          <div
            style={{
              padding: '14px 30px',
              border: '4px solid #19D3DA',
              fontFamily: 'Inter',
              fontSize: 38,
              fontWeight: 900,
              color: '#19D3DA',
              letterSpacing: 4,
              backgroundColor: 'rgba(238,238,238,0.4)',
            }}
          >
            ★ PERFECT BUYER ★
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#373A40',
          opacity: 0.4,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene05_SixtySeventyEMI;
```

```tsx
import React from 'react';
Audio: "AI revolution ने पूरा समीकरण उलट दिया — IT कंपनियां वर्कफोर्स घटा रही हैं।"

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_AIRevolution',
  durationInSeconds: 23,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_AIRevolution: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headerIn = sp(10);
  const aiCoreIn = sp(60);
  const equationIn = sp(160);
  const flipShake = frame > 220 && frame < 240 ? Math.sin((frame - 220) * 1.5) * (1 - (frame - 220) / 20) * 6 : 0;
  const beforeIn = sp(250);
  const afterIn = sp(360);
  const downIn = sp(470);

  // Nodes orbiting AI core
  const orbitNodes = Array.from({ length: 8 }, (_, i) => {
    const angle = (i / 8) * Math.PI * 2 + frame * 0.02;
    const r = 180;
    return { x: Math.cos(angle) * r, y: Math.sin(angle) * r, i };
  });

  // Particles
  const particles = Array.from({ length: 30 }, (_, i) => ({
    x: seededRandom(i + 7) * 1920,
    y: seededRandom(i + 95) * 1080,
    size: 2 + seededRandom(i + 145) * 4,
    speed: 0.3 + seededRandom(i + 195) * 0.5,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp, transform: `translate(${flipShake}px, 0)` }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 35%, rgba(25,211,218,0.20) 0%, rgba(238,238,238,0) 55%)',
        }}
      />

      {/* Grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="gridF" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#373A40" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#gridF)" />
      </svg>

      {/* Drifting shapes */}
      <div
        style={{
          position: 'absolute',
          top: 120,
          right: 100,
          width: 260,
          height: 260,
          border: '2px solid #19D3DA',
          opacity: 0.07,
          transform: `rotate(${frame * 0.15}deg)`,
          borderRadius: 30,
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => {
        const py = (p.y + frame * p.speed) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: py,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#19D3DA' : '#373A40',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headerIn,
            transform: `translateY(${(1 - headerIn) * -25}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 32,
              fontWeight: 800,
              color: '#19D3DA',
              letterSpacing: 8,
            }}
          >
            ◆ AI REVOLUTION ◆
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 85,
              fontWeight: 700,
              color: '#373A40',
              marginTop: 4,
            }}
          >
            पूरा समीकरण <span style={{ color: '#19D3DA' }}>उलट गया</span>
          </div>
        </div>

        {/* AI Core node + orbiting */}
        <div
          style={{
            position: 'absolute',
            top: 350,
            left: '50%',
            transform: `translate(-50%, 0) scale(${aiCoreIn})`,
            opacity: aiCoreIn,
          }}
        >
          <div style={{ position: 'relative', width: 400, height: 400 }}>
            {/* Orbit ring */}
            <div
              style={{
                position: 'absolute',
                top: 20,
                left: 20,
                width: 360,
                height: 360,
                borderRadius: '50%',
                border: '2px dashed #19D3DA',
                opacity: 0.5,
                transform: `rotate(${frame * 0.5}deg)`,
              }}
            />
            {/* Orbit nodes */}
            {orbitNodes.map((n) => (
              <div
                key={n.i}
                style={{
                  position: 'absolute',
                  top: 200 + n.y - 12,
                  left: 200 + n.x - 12,
                  width: 24,
                  height: 24,
                  borderRadius: '50%',
                  backgroundColor: '#19D3DA',
                  boxShadow: '0 0 20px rgba(25,211,218,0.7)',
                }}
              />
            ))}
            {/* AI core */}
            <div
              style={{
                position: 'absolute',
                top: 120,
                left: 120,
                width: 160,
                height: 160,
                borderRadius: '50%',
                backgroundColor: '#373A40',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                fontFamily: 'Inter',
                fontSize: 64,
                fontWeight: 900,
                color: '#19D3DA',
                boxShadow: '0 0 50px rgba(25,211,218,0.6)',
                transform: `scale(${1 + Math.sin(frame * 0.1) * 0.04})`,
              }}
            >
              AI
            </div>
          </div>
        </div>

        {/* Equation flip — Before / After */}
        <div
          style={{
            position: 'absolute',
            bottom: 230,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 40,
          }}
        >
          {/* BEFORE */}
          <div
            style={{
              opacity: beforeIn,
              transform: `translateX(${(1 - beforeIn) * -80}px)`,
              backgroundColor: '#FFFFFF',
              border: '2px solid #373A40',
              borderRadius: 14,
              padding: '24px 36px',
              textAlign: 'center',
              minWidth: 420,
            }}
          >
            <div style={{ fontFamily: 'Inter', fontSize: 24, fontWeight: 700, color: '#373A40', opacity: 0.6, letterSpacing: 3 }}>BEFORE</div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 47, fontWeight: 700, color: '#373A40', marginTop: 6 }}>
              हजारों Engineers
            </div>
          </div>

          {/* Arrow */}
          <div style={{ opacity: equationIn }}>
            <svg width="80" height="50" viewBox="0 0 80 50">
              <path d="M5 25 L65 25 M55 12 L72 25 L55 38" stroke="#19D3DA" strokeWidth="5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </div>

          {/* AFTER */}
          <div
            style={{
              opacity: afterIn,
              transform: `translateX(${(1 - afterIn) * 80}px)`,
              backgroundColor: '#373A40',
              borderRadius: 14,
              padding: '24px 36px',
              textAlign: 'center',
              minWidth: 420,
              borderTop: '4px solid #19D3DA',
            }}
          >
            <div style={{ fontFamily: 'Inter', fontSize: 24, fontWeight: 700, color: '#19D3DA', letterSpacing: 3 }}>NOW</div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 47, fontWeight: 700, color: '#EEEEEE', marginTop: 6 }}>
              Automation + AI Tools
            </div>
          </div>
        </div>

        {/* Bottom — workforce down */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: downIn,
            transform: `translateY(${(1 - downIn) * 30}px)`,
          }}
        >
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 18,
            }}
          >
            <svg width="46" height="46" viewBox="0 0 46 46">
              <path d="M23 6 L23 36 M10 26 L23 38 L36 26" stroke="#19D3DA" strokeWidth="5" fill="none" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari',
                fontSize: 51,
                fontWeight: 700,
                color: '#373A40',
              }}
            >
              Workforce धीरे-धीरे <span style={{ color: '#19D3DA' }}>कम हो रही है</span>
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#373A40',
          opacity: 0.4,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene06_AIRevolution;
```

```tsx
import React from 'react';
Audio: "नौकरी के डर से लोग 20 साल की EMI का जोखिम नहीं उठाते — वो रुक जाते हैं।"

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_FearAndPause',
  durationInSeconds: 23,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_FearAndPause: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headerIn = sp(8);
  const fearIn = sp(50);
  const timelineIn = sp(140);
  const riskIn = sp(280);
  const pauseIn = sp(400);

  // Heartbeat pulse for "DAR"
  const beat = 1 + Math.sin(frame * 0.18) * 0.04;

  // Particles
  const particles = Array.from({ length: 25 }, (_, i) => ({
    x: seededRandom(i + 8) * 1920,
    y: seededRandom(i + 100) * 1080,
    size: 2 + seededRandom(i + 150) * 4,
    speed: 0.3 + seededRandom(i + 200) * 0.5,
  }));

  // EMI timeline — 20 segments
  const segments = Array.from({ length: 20 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(238,238,238,1) 0%, rgba(55,58,64,0.10) 100%)',
        }}
      />

      {/* Texture lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="gridG" width="100" height="100" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="100" y2="0" stroke="#373A40" strokeWidth="1" />
            <line x1="0" y1="0" x2="0" y2="100" stroke="#373A40" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#gridG)" />
      </svg>

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          right: 80,
          width: 280,
          height: 280,
          borderRadius: '50%',
          backgroundColor: '#19D3DA',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: 80,
          width: 240,
          height: 240,
          border: '2px solid #373A40',
          opacity: 0.06,
          transform: `rotate(${frame * 0.13}deg)`,
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => {
        const py = (p.y + frame * p.speed) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: py,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#19D3DA' : '#373A40',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headerIn,
            transform: `translateY(${(1 - headerIn) * -25}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 700,
              color: '#373A40',
              letterSpacing: 6,
            }}
          >
            INSIDE THE BUYER'S MIND
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 90,
              fontWeight: 700,
              color: '#373A40',
              marginTop: 4,
            }}
          >
            एक <span style={{ color: '#19D3DA' }}>डर</span> बैठ गया है
          </div>
        </div>

        {/* FEAR pulse circle */}
        <div
          style={{
            position: 'absolute',
            top: 320,
            left: 160,
            opacity: fearIn,
            transform: `scale(${fearIn * beat})`,
          }}
        >
          <div
            style={{
              width: 360,
              height: 360,
              borderRadius: '50%',
              border: '4px solid #19D3DA',
              backgroundColor: 'rgba(25,211,218,0.10)',
              display: 'flex',
              flexDirection: 'column',
              alignItems: 'center',
              justifyContent: 'center',
              boxShadow: '0 0 60px rgba(25,211,218,0.4)',
            }}
          >
            <div style={{ fontFamily: 'Yantra One', fontSize: 144, fontWeight: 800, color: '#373A40', lineHeight: 1 }}>डर</div>
            <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 700, color: '#19D3DA', letterSpacing: 4, marginTop: 8 }}>JOB INSECURITY</div>
          </div>
        </div>

        {/* EMI 20-year timeline */}
        <div
          style={{
            position: 'absolute',
            top: 380,
            right: 130,
            width: 800,
            opacity: timelineIn,
            transform: `translateX(${(1 - timelineIn) * 60}px)`,
          }}
        >
          <div style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 700, color: '#373A40', opacity: 0.7, letterSpacing: 4 }}>
            20 YEARS • EMI BURDEN
          </div>
          <div style={{ display: 'flex', gap: 4, marginTop: 18 }}>
            {segments.map((s) => {
              const segIn = sp(160 + s * 5);
              return (
                <div
                  key={s}
                  style={{
                    width: 36,
                    height: 48,
                    backgroundColor: '#373A40',
                    opacity: segIn * 0.85,
                    borderRadius: 3,
                  }}
                />
              );
            })}
          </div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 30, color: '#373A40', marginTop: 12 }}>
            Year 1 ────────────────── Year 20
          </div>

          {/* Risk badge */}
          <div
            style={{
              marginTop: 30,
              padding: '20px 28px',
              backgroundColor: '#373A40',
              borderLeft: '6px solid #19D3DA',
              borderRadius: 8,
              opacity: riskIn,
              transform: `translateX(${(1 - riskIn) * 40}px)`,
            }}
          >
            <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#19D3DA', letterSpacing: 3 }}>RISK</div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 40, fontWeight: 700, color: '#EEEEEE', marginTop: 4 }}>
              जोखिम नहीं उठाता
            </div>
          </div>
        </div>

        {/* PAUSE banner */}
        <div
          style={{
            position: 'absolute',
            bottom: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: pauseIn,
            transform: `scale(${0.85 + pauseIn * 0.15})`,
          }}
        >
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 28,
              padding: '24px 60px',
              backgroundColor: '#19D3DA',
              borderRadius: 14,
              boxShadow: '0 14px 40px rgba(25,211,218,0.35)',
            }}
          >
            {/* Pause icon */}
            <div style={{ display: 'flex', gap: 10 }}>
              <div style={{ width: 18, height: 56, backgroundColor: '#373A40', borderRadius: 3 }} />
              <div style={{ width: 18, height: 56, backgroundColor: '#373A40', borderRadius: 3 }} />
            </div>
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 64,
                fontWeight: 800,
                color: '#373A40',
                letterSpacing: 2,
              }}
            >
              वो रुक जाते हैं • वो इंतज़ार करते हैं
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#373A40',
          opacity: 0.4,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene07_FearAndPause;
```

```tsx
import React from 'react';
Audio: "बिक्री गिरे तो कीमतें भी गिरनी चाहिए — economics का बेसिक नियम।"

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_DemandSupplyLaw',
  durationInSeconds: 23,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_DemandSupplyLaw: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom in
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04], { extrapolateRight: 'clamp' });

  const headerIn = sp(10);
  const axesIn = sp(60);
  const demandLineProgress = interpolate(sp(120, { damping: 18 }), [0, 1], [0, 1]);
  const supplyLineProgress = interpolate(sp(180, { damping: 18 }), [0, 1], [0, 1]);
  const labelIn = sp(280);
  const lawIn = sp(420);

  // Particles
  const particles = Array.from({ length: 24 }, (_, i) => ({
    x: seededRandom(i + 9) * 1920,
    y: seededRandom(i + 110) * 1080,
    size: 2 + seededRandom(i + 160) * 4,
    speed: 0.3 + seededRandom(i + 210) * 0.5,
  }));

  // Chart dims
  const chartX = 540;
  const chartY = 320;
  const chartW = 840;
  const chartH = 500;

  // Demand line (downward to right) - going down
  const demandPoints = Array.from({ length: 60 }, (_, i) => {
    const t = i / 59;
    const x = chartX + t * chartW;
    const y = chartY + 80 + t * (chartH - 160);
    return { x, y };
  });
  const demandVisible = Math.floor(demandPoints.length * demandLineProgress);

  // Supply line (upward to right)
  const supplyPoints = Array.from({ length: 60 }, (_, i) => {
    const t = i / 59;
    const x = chartX + t * chartW;
    const y = chartY + (chartH - 80) - t * (chartH - 160);
    return { x, y };
  });
  const supplyVisible = Math.floor(supplyPoints.length * supplyLineProgress);

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at 60% 50%, rgba(25,211,218,0.12) 0%, rgba(238,238,238,0) 60%)',
          transform: `scale(${camScale})`,
        }}
      />

      {/* Grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="gridH" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#373A40" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#gridH)" />
      </svg>

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: 100,
          right: 80,
          width: 240,
          height: 240,
          border: '2px solid #19D3DA',
          opacity: 0.07,
          transform: `rotate(${frame * 0.12}deg)`,
          borderRadius: 20,
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => {
        const py = (p.y + frame * p.speed) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: py,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#19D3DA' : '#373A40',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headerIn,
            transform: `translateY(${(1 - headerIn) * -25}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 700,
              color: '#19D3DA',
              letterSpacing: 8,
              textTransform: 'uppercase',
            }}
          >
            Economics 101
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 90,
              fontWeight: 700,
              color: '#373A40',
              marginTop: 4,
            }}
          >
            बेसिक नियम — <span style={{ color: '#19D3DA' }}>Demand ↓ → Price ↓</span>
          </div>
        </div>

        {/* Y-axis label */}
        <div
          style={{
            position: 'absolute',
            top: chartY + 200,
            left: 130,
            opacity: axesIn,
            transform: 'rotate(-90deg)',
            transformOrigin: 'left top',
            fontFamily: 'Inter',
            fontSize: 30,
            fontWeight: 700,
            color: '#373A40',
            letterSpacing: 3,
          }}
        >
          PRICE →
        </div>

        {/* SVG Chart */}
        <svg
          width="1920"
          height="1080"
          style={{ position: 'absolute', top: 0, left: 0, opacity: axesIn }}
        >
          {/* X axis */}
          <line x1={chartX} y1={chartY + chartH} x2={chartX + chartW} y2={chartY + chartH} stroke="#373A40" strokeWidth="3" />
          {/* Y axis */}
          <line x1={chartX} y1={chartY} x2={chartX} y2={chartY + chartH} stroke="#373A40" strokeWidth="3" />

          {/* Axis arrows */}
          <path d={`M${chartX + chartW} ${chartY + chartH} L${chartX + chartW - 12} ${chartY + chartH - 8} L${chartX + chartW - 12} ${chartY + chartH + 8} Z`} fill="#373A40" />
          <path d={`M${chartX} ${chartY} L${chartX - 8} ${chartY + 12} L${chartX + 8} ${chartY + 12} Z`} fill="#373A40" />

          {/* Demand line */}
          {demandVisible > 1 && (
            <path
              d={`M ${demandPoints.slice(0, demandVisible).map((p) => `${p.x} ${p.y}`).join(' L ')}`}
              stroke="#19D3DA"
              strokeWidth="6"
              fill="none"
              strokeLinecap="round"
            />
          )}

          {/* Supply line */}
          {supplyVisible > 1 && (
            <path
              d={`M ${supplyPoints.slice(0, supplyVisible).map((p) => `${p.x} ${p.y}`).join(' L ')}`}
              stroke="#373A40"
              strokeWidth="6"
              fill="none"
              strokeLinecap="round"
              strokeDasharray="14 8"
            />
          )}

          {/* Intersection dot */}
          {demandLineProgress > 0.5 && supplyLineProgress > 0.5 && (
            <circle cx={chartX + chartW / 2} cy={chartY + chartH / 2} r="14" fill="#19D3DA" stroke="#373A40" strokeWidth="3" />
          )}
        </svg>

        {/* X-axis label */}
        <div
          style={{
            position: 'absolute',
            top: chartY + chartH + 18,
            left: chartX + chartW / 2 - 100,
            opacity: axesIn,
            fontFamily: 'Inter',
            fontSize: 30,
            fontWeight: 700,
            color: '#373A40',
            letterSpacing: 3,
          }}
        >
          QUANTITY →
        </div>

        {/* Line labels */}
        <div
          style={{
            position: 'absolute',
            top: chartY + 60,
            left: chartX + chartW + 30,
            opacity: labelIn,
            fontFamily: 'Inter',
            fontSize: 36,
            fontWeight: 800,
            color: '#19D3DA',
          }}
        >
          DEMAND
        </div>
        <div
          style={{
            position: 'absolute',
            top: chartY + chartH - 100,
            left: chartX + chartW + 30,
            opacity: labelIn,
            fontFamily: 'Inter',
            fontSize: 36,
            fontWeight: 800,
            color: '#373A40',
          }}
        >
          SUPPLY
        </div>

        {/* Bottom law statement */}
        <div
          style={{
            position: 'absolute',
            bottom: 60,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: lawIn,
            transform: `translateY(${(1 - lawIn) * 30}px)`,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '20px 50px',
              backgroundColor: '#373A40',
              borderRadius: 12,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 47,
              fontWeight: 700,
              color: '#EEEEEE',
              borderTop: '4px solid #19D3DA',
            }}
          >
            जब <span style={{ color: '#19D3DA' }}>demand कम</span>, तब supply की कीमत <span style={{ color: '#19D3DA' }}>अपने आप नीचे</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#373A40',
          opacity: 0.4,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene08_DemandSupplyLaw;
```

```tsx
import React from 'react';
Audio: "पर बिक्री 13% गिरी, कीमतें वही — ये असंतुलन खरीदार के लिए खतरा।"

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_PriceImbalance',
  durationInSeconds: 23,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_PriceImbalance: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headerIn = sp(10);
  const leftIn = sp(60);
  const rightIn = sp(120);
  const beamIn = sp(220);
  const tiltAngle = interpolate(sp(280, { damping: 14, stiffness: 60 }), [0, 1], [0, -10]);
  const warnIn = sp(420);

  // Particles
  const particles = Array.from({ length: 25 }, (_, i) => ({
    x: seededRandom(i + 10) * 1920,
    y: seededRandom(i + 120) * 1080,
    size: 2 + seededRandom(i + 170) * 4,
    speed: 0.3 + seededRandom(i + 220) * 0.5,
  }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, rgba(238,238,238,1) 0%, rgba(25,211,218,0.10) 100%)',
        }}
      />

      {/* Dot texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.10 }}>
        <defs>
          <pattern id="dotI" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#373A40" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dotI)" />
      </svg>

      {/* Shapes */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 70,
          width: 240,
          height: 240,
          borderRadius: '50%',
          border: '2px solid #19D3DA',
          opacity: 0.07,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: 80,
          right: 80,
          width: 260,
          height: 260,
          border: '2px solid #373A40',
          opacity: 0.07,
          transform: `rotate(${frame * 0.15}deg)`,
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => {
        const py = (p.y + frame * p.speed) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: py,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#19D3DA' : '#373A40',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headerIn,
            transform: `translateY(${(1 - headerIn) * -25}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 700,
              color: '#373A40',
              letterSpacing: 6,
            }}
          >
            MARKET IMBALANCE
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 85,
              fontWeight: 700,
              color: '#373A40',
              marginTop: 4,
            }}
          >
            नियम <span style={{ color: '#19D3DA' }}>टूट गया है</span>
          </div>
        </div>

        {/* SCALE BEAM container */}
        <div
          style={{
            position: 'absolute',
            top: 330,
            left: '50%',
            transform: `translateX(-50%)`,
            width: 1400,
            height: 480,
          }}
        >
          {/* Center pivot */}
          <div
            style={{
              position: 'absolute',
              top: 200,
              left: '50%',
              transform: 'translateX(-50%)',
              width: 80,
              height: 80,
              borderRadius: '50%',
              backgroundColor: '#373A40',
              border: '4px solid #19D3DA',
              zIndex: 5,
              opacity: beamIn,
            }}
          />
          {/* Center stand */}
          <div
            style={{
              position: 'absolute',
              top: 240,
              left: '50%',
              transform: 'translateX(-50%)',
              width: 14,
              height: 200,
              backgroundColor: '#373A40',
              opacity: beamIn,
            }}
          />

          {/* BEAM (tilted) */}
          <div
            style={{
              position: 'absolute',
              top: 230,
              left: 60,
              right: 60,
              height: 14,
              backgroundColor: '#373A40',
              transformOrigin: 'center',
              transform: `rotate(${tiltAngle}deg)`,
              opacity: beamIn,
              borderRadius: 4,
            }}
          />

          {/* LEFT PLATE — Sales -13% (going up because lighter) */}
          <div
            style={{
              position: 'absolute',
              top: 80 + tiltAngle * 8,
              left: 60,
              opacity: leftIn,
              transform: `translateY(${(1 - leftIn) * 50}px) rotate(${tiltAngle}deg)`,
              transformOrigin: 'center',
            }}
          >
            <div
              style={{
                width: 380,
                padding: '30px 24px',
                backgroundColor: '#FFFFFF',
                border: '3px solid #19D3DA',
                borderRadius: 14,
                textAlign: 'center',
              }}
            >
              <div style={{ fontFamily: 'Inter', fontSize: 24, fontWeight: 700, color: '#373A40', opacity: 0.6, letterSpacing: 3 }}>SALES</div>
              <div style={{ fontFamily: 'Inter', fontSize: 119, fontWeight: 900, color: '#19D3DA', lineHeight: 1, marginTop: 8 }}>
                -13%
              </div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 32, fontWeight: 700, color: '#373A40', marginTop: 6 }}>
                बिक्री गिरी
              </div>
            </div>
          </div>

          {/* RIGHT PLATE — Prices same/up (going down because heavier) */}
          <div
            style={{
              position: 'absolute',
              top: 80 - tiltAngle * 8,
              right: 60,
              opacity: rightIn,
              transform: `translateY(${(1 - rightIn) * 50}px) rotate(${tiltAngle}deg)`,
              transformOrigin: 'center',
            }}
          >
            <div
              style={{
                width: 380,
                padding: '30px 24px',
                backgroundColor: '#373A40',
                borderRadius: 14,
                textAlign: 'center',
                borderTop: '4px solid #19D3DA',
              }}
            >
              <div style={{ fontFamily: 'Inter', fontSize: 24, fontWeight: 700, color: '#19D3DA', letterSpacing: 3 }}>PRICES</div>
              <div style={{ fontFamily: 'Inter', fontSize: 119, fontWeight: 900, color: '#EEEEEE', lineHeight: 1, marginTop: 8 }}>
                SAME ↑
              </div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 32, fontWeight: 700, color: '#EEEEEE', marginTop: 6 }}>
                कीमत वही या बढ़ी
              </div>
            </div>
          </div>
        </div>

        {/* WARNING bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: warnIn,
            transform: `translateY(${(1 - warnIn) * 30}px)`,
          }}
        >
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 22,
              padding: '20px 44px',
              backgroundColor: '#19D3DA',
              borderRadius: 12,
            }}
          >
            <svg width="44" height="44" viewBox="0 0 44 44">
              <polygon points="22,4 42,38 2,38" fill="#373A40" />
              <text x="22" y="32" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#19D3DA">!</text>
            </svg>
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 55,
                fontWeight: 800,
                color: '#373A40',
              }}
            >
              खरीदार के लिए सबसे बड़ा खतरा
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#373A40',
          opacity: 0.4,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene09_PriceImbalance;
```

```tsx
import React from 'react';
Audio: "बिल्डर लॉबी की पुरानी तरकीब — artificial scarcity बनाकर कीमतें ऊंची रखना।"

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_ArtificialScarcity',
  durationInSeconds: 23,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_ArtificialScarcity: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom out then in
  const camScale = interpolate(frame, [0, durationInFrames / 2, durationInFrames], [1.04, 1, 1.05], { extrapolateRight: 'clamp' });

  const headerIn = sp(10);
  const trickIn = sp(70);
  const step1In = sp(160);
  const step2In = sp(240);
  const step3In = sp(320);
  const resultIn = sp(440);

  // Particles
  const particles = Array.from({ length: 26 }, (_, i) => ({
    x: seededRandom(i + 11) * 1920,
    y: seededRandom(i + 130) * 1080,
    size: 2 + seededRandom(i + 180) * 4,
    speed: 0.3 + seededRandom(i + 230) * 0.5,
  }));

  // Sliding "BLOCKED" indicators
  const slideX = Math.sin(frame * 0.08) * 6;

  return (
    <AbsoluteFill style={{ backgroundColor: '#EEEEEE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 40%, rgba(25,211,218,0.16) 0%, rgba(238,238,238,0) 55%), radial-gradient(circle at 70% 70%, rgba(55,58,64,0.10) 0%, rgba(238,238,238,0) 60%)',
          transform: `scale(${camScale})`,
        }}
      />

      {/* Texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="patJ" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M 50 0 L 0 0 0 50" fill="none" stroke="#373A40" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#patJ)" />
      </svg>

      {/* Drifting shapes */}
      <div
        style={{
          position: 'absolute',
          top: 100,
          right: 100,
          width: 260,
          height: 260,
          border: '2px solid #19D3DA',
          opacity: 0.07,
          transform: `rotate(${frame * 0.13}deg)`,
          borderRadius: 20,
        }}
      />
      <div
        style={{
          position: 'absolute',
          bottom: 100,
          left: 80,
          width: 240,
          height: 240,
          borderRadius: '50%',
          backgroundColor: '#373A40',
          opacity: 0.05,
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.05})`,
        }}
      />

      {/* Particles */}
      {particles.map((p, i) => {
        const py = (p.y + frame * p.speed) % 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: p.x,
              top: py,
              width: p.size,
              height: p.size,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#19D3DA' : '#373A40',
              opacity: 0.3,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headerIn,
            transform: `translateY(${(1 - headerIn) * -25}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 700,
              color: '#19D3DA',
              letterSpacing: 6,
            }}
          >
            BUILDER LOBBY • OLD PLAYBOOK
          </div>
          <div
            style={{
              fontFamily: 'Yantra One',
              fontSize: 85,
              fontWeight: 700,
              color: '#373A40',
              marginTop: 4,
            }}
          >
            पुरानी <span style={{ color: '#19D3DA' }}>तरकीब</span>
          </div>
        </div>

        {/* TRICK label */}
        <div
          style={{
            position: 'absolute',
            top: 280,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: trickIn,
            transform: `scale(${0.85 + trickIn * 0.15})`,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '12px 36px',
              border: '3px solid #373A40',
              borderRadius: 10,
              fontFamily: 'JetBrains Mono',
              fontSize: 38,
              fontWeight: 800,
              color: '#373A40',
              letterSpacing: 5,
            }}
          >
            ARTIFICIAL SCARCITY
          </div>
        </div>

        {/* 3 STEPS — horizontal flow */}
        <div
          style={{
            position: 'absolute',
            top: 410,
            left: 0,
            right: 0,
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            gap: 24,
            padding: '0 100px',
          }}
        >
          {/* Step 1 */}
          <div
            style={{
              width: 430,
              padding: '28px 24px',
              backgroundColor: '#FFFFFF',
              border: '3px solid #373A40',
              borderRadius: 14,
              opacity: step1In,
              transform: `translateY(${(1 - step1In) * 40}px) translateX(${slideX}px)`,
              textAlign: 'center',
              position: 'relative',
            }}
          >
            <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 800, color: '#19D3DA', letterSpacing: 3 }}>STEP 01</div>
            {/* SVG construction icon */}
            <svg width="64" height="64" viewBox="0 0 64 64" style={{ marginTop: 10 }}>
              <rect x="14" y="20" width="36" height="32" fill="#373A40" />
              <rect x="20" y="26" width="8" height="8" fill="#19D3DA" />
              <rect x="36" y="26" width="8" height="8" fill="#19D3DA" />
              <rect x="20" y="38" width="8" height="8" fill="#19D3DA" />
              <rect x="36" y="38" width="24" height="3" fill="#19D3DA" transform="rotate(-30 36 38)" />
            </svg>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 34, fontWeight: 700, color: '#373A40', marginTop: 8 }}>
              Construction धीमा करो
            </div>
          </div>

          {/* Arrow */}
          <svg width="40" height="40" viewBox="0 0 40 40" style={{ opacity: step2In }}>
            <path d="M5 20 L30 20 M22 12 L34 20 L22 28" stroke="#19D3DA" strokeWidth="4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>

          {/* Step 2 */}
          <div
            style={{
              width: 430,
              padding: '28px 24px',
              backgroundColor: '#373A40',
              borderRadius: 14,
              opacity: step2In,
              transform: `translateY(${(1 - step2In) * 40}px) translateX(${-slideX}px)`,
              textAlign: 'center',
              borderTop: '4px solid #19D3DA',
            }}
          >
            <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 800, color: '#19D3DA', letterSpacing: 3 }}>STEP 02</div>
            <svg width="64" height="64" viewBox="0 0 64 64" style={{ marginTop: 10 }}>
              <rect x="12" y="14" width="40" height="36" fill="none" stroke="#19D3DA" strokeWidth="3" rx="3" />
              <path d="M18 32 L46 32 M32 18 L32 46" stroke="#19D3DA" strokeWidth="3" />
              <line x1="14" y1="14" x2="50" y2="50" stroke="#19D3DA" strokeWidth="4" />
            </svg>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 34, fontWeight: 700, color: '#EEEEEE', marginTop: 8 }}>
              नए Launches रोको
            </div>
          </div>

          {/* Arrow */}
          <svg width="40" height="40" viewBox="0 0 40 40" style={{ opacity: step3In }}>
            <path d="M5 20 L30 20 M22 12 L34 20 L22 28" stroke="#19D3DA" strokeWidth="4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>

          {/* Step 3 */}
          <div
            style={{
              width: 430,
              padding: '28px 24px',
              backgroundColor: '#FFFFFF',
              border: '3px solid #19D3DA',
              borderRadius: 14,
              opacity: step3In,
              transform: `translateY(${(1 - step3In) * 40}px) translateX(${slideX}px)`,
              textAlign: 'center',
            }}
          >
            <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 800, color: '#19D3DA', letterSpacing: 3 }}>STEP 03</div>
            <svg width="64" height="64" viewBox="0 0 64 64" style={{ marginTop: 10 }}>
              <path d="M10 50 L20 36 L28 42 L40 22 L54 14" stroke="#373A40" strokeWidth="4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
              <path d="M44 14 L54 14 L54 24" stroke="#373A40" strokeWidth="4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 34, fontWeight: 700, color: '#373A40', marginTop: 8 }}>
              कीमतें ऊँची रखो
            </div>
          </div>
        </div>

        {/* Result banner */}
        <div
          style={{
            position: 'absolute',
            bottom: 70,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: resultIn,
            transform: `scale(${0.85 + resultIn * 0.15})`,
          }}
        >
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 20,
              padding: '20px 50px',
              backgroundColor: '#19D3DA',
              borderRadius: 12,
            }}
          >
            <div
              style={{
                fontFamily: 'Yantra One',
                fontSize: 55,
                fontWeight: 800,
                color: '#373A40',
              }}
            >
              नतीजा — Scarcity बनी, कीमत नीचे नहीं आई
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.2) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#373A40',
          opacity: 0.4,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_ArtificialScarcity;
```

