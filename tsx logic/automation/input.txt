```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene1_EmergingMarketShock',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_EmergingMarketShock: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Chart bar animation — bars drop with grief
  const bar1H = interpolate(sp(10), [0, 1], [0, 180]);
  const bar2H = interpolate(sp(18), [0, 1], [0, 130]);
  const bar3H = interpolate(sp(26), [0, 1], [0, 90]);
  const bar4H = interpolate(sp(34), [0, 1], [0, 55]);

  // Sad arrow swoop
  const arrowOp = interpolate(frame, [50, 70], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const arrowY = interpolate(sp(50), [0, 1], [40, 0]);

  // Headline stagger
  const line1X = interpolate(sp(5), [0, 1], [-120, 0]);
  const line2X = interpolate(sp(20), [0, 1], [-120, 0]);

  // Background slow pan
  const bgX = interpolate(frame, [0, durationInFrames], [0, -30]);

  // Floating circles parallax
  const fc1Y = Math.sin(frame * 0.03) * 18;
  const fc2Y = Math.cos(frame * 0.025) * 14;

  // Vignette pulse
  const vigOp = interpolate(frame, [0, 30], [0, 0.55], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#F5F5F5',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Slow-panning gradient wash */}
      <AbsoluteFill
        style={{
          background: `linear-gradient(135deg, #ffffff 0%, #f0f4ff 45%, #e8edf8 100%)`,
          transform: `translateX(${bgX}px)`,
          width: '110%',
        }}
      />

      {/* Large geometric shape — left circle */}
      <div
        style={{
          position: 'absolute',
          left: -200,
          top: -200,
          width: 700,
          height: 700,
          borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(255,0,0,0.06) 0%, transparent 70%)',
          transform: `translateY(${fc1Y}px) scale(${1 + Math.sin(frame * 0.02) * 0.015})`,
        }}
      />

      {/* Large geometric shape — right rectangle */}
      <div
        style={{
          position: 'absolute',
          right: -100,
          bottom: -100,
          width: 500,
          height: 500,
          borderRadius: 60,
          background: 'rgba(28, 28, 28, 0.03)',
          transform: `translateY(${fc2Y}px) rotate(${20 + Math.sin(frame * 0.018) * 3}deg)`,
        }}
      />

      {/* Grid texture */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 32 }).map((_, i) => (
            <line
              key={`v${i}`}
              x1={i * 60}
              y1={0}
              x2={i * 60}
              y2={1080}
              stroke="#1C1C1C"
              strokeWidth={1}
            />
          ))}
          {Array.from({ length: 20 }).map((_, i) => (
            <line
              key={`h${i}`}
              x1={0}
              y1={i * 60}
              x2={1920}
              y2={i * 60}
              stroke="#1C1C1C"
              strokeWidth={1}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = seededRandom(i * 7) * 1920;
        const py = seededRandom(i * 13) * 1080;
        const pSize = 3 + seededRandom(i * 5) * 6;
        const pOp = 0.08 + seededRandom(i * 11) * 0.12;
        const drift = Math.sin(frame * 0.02 + i) * 12;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py + drift,
              width: pSize,
              height: pSize,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#ff0000' : '#1C1C1C',
              opacity: pOp,
            }}
          />
        );
      })}

      {/* LEFT PANEL — Text */}
      <div
        style={{
          position: 'absolute',
          left: 100,
          top: 0,
          width: 900,
          height: 1080,
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          gap: 24,
        }}
      >
        {/* Tag */}
        <div
          style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 10,
            backgroundColor: '#ff0000',
            borderRadius: 8,
            padding: '8px 22px',
            width: 'fit-content',
            opacity: sp(0),
            transform: `translateY(${interpolate(sp(0), [0, 1], [20, 0])}px)`,
          }}
        >
          <span
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 22,
              fontWeight: 800,
              color: '#F5F5F5',
              letterSpacing: 3,
              textTransform: 'uppercase',
            }}
          >
            Emerging Market Reality
          </span>
        </div>

        {/* Main headline line 1 */}
        <div
          style={{
            overflow: 'hidden',
            transform: `translateX(${line1X}px)`,
            opacity: sp(5),
          }}
        >
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 96,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.05,
              display: 'block',
            }}
          >
            निराशा क्यों?
          </span>
        </div>

        {/* Main headline line 2 */}
        <div
          style={{
            overflow: 'hidden',
            transform: `translateX(${line2X}px)`,
            opacity: sp(20),
          }}
        >
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 54,
              fontWeight: 700,
              color: '#2D2D2D',
              lineHeight: 1.3,
              display: 'block',
            }}
          >
            इंटरनेशनल फंड्स ने
            <br />
            <span style={{ color: '#ff0000', fontWeight: 900 }}>expectations तोड़ीं</span>
          </span>
        </div>

        {/* Sub body */}
        <div
          style={{
            opacity: interpolate(frame, [40, 60], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
            transform: `translateY(${interpolate(
              frame,
              [40, 60],
              [20, 0],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            )}px)`,
          }}
        >
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 40,
              fontWeight: 500,
              color: '#9E9E9E',
              lineHeight: 1.5,
            }}
          >
            Emerging market fund में पैसा लगाया
            <br />
            और performance देख कर निराशा हुई?
          </span>
        </div>
      </div>

      {/* RIGHT PANEL — Bar Chart */}
      <div
        style={{
          position: 'absolute',
          right: 100,
          top: '50%',
          transform: 'translateY(-50%)',
          width: 650,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          gap: 20,
        }}
      >
        {/* Chart title */}
        <div
          style={{
            opacity: sp(5),
            fontFamily: 'Inter, sans-serif',
            fontSize: 28,
            fontWeight: 700,
            color: '#1C1C1C',
            letterSpacing: 1,
          }}
        >
          Emerging Market Fund Returns
        </div>

        {/* Bar chart */}
        <div
          style={{
            display: 'flex',
            alignItems: 'flex-end',
            gap: 28,
            height: 240,
            padding: '0 20px',
            borderBottom: '3px solid #1C1C1C',
          }}
        >
          {[
            { label: 'Year 1', h: bar1H, color: '#FFC107' },
            { label: 'Year 2', h: bar2H, color: '#e9d700' },
            { label: 'Year 3', h: bar3H, color: '#ff0000' },
            { label: 'Year 4', h: bar4H, color: '#ff0000' },
          ].map((b, i) => (
            <div
              key={i}
              style={{
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                gap: 8,
              }}
            >
              <div
                style={{
                  width: 80,
                  height: b.h,
                  backgroundColor: b.color,
                  borderRadius: '8px 8px 0 0',
                  position: 'relative',
                }}
              >
                <div
                  style={{
                    position: 'absolute',
                    top: -36,
                    left: '50%',
                    transform: 'translateX(-50%)',
                    fontFamily: 'JetBrains Mono, monospace',
                    fontSize: 22,
                    fontWeight: 800,
                    color: b.color === '#ff0000' ? '#ff0000' : '#1C1C1C',
                    whiteSpace: 'nowrap',
                  }}
                >
                  {i === 0 ? '+12%' : i === 1 ? '+8%' : i === 2 ? '-3%' : '-9%'}
                </div>
              </div>
              <span
                style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 20,
                  fontWeight: 600,
                  color: '#9E9E9E',
                }}
              >
                {b.label}
              </span>
            </div>
          ))}
        </div>

        {/* Down arrow */}
        <div
          style={{
            opacity: arrowOp,
            transform: `translateY(${arrowY}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 12,
          }}
        >
          <svg width="48" height="48" viewBox="0 0 48 48">
            <path
              d="M24 4 L24 36 M12 26 L24 38 L36 26"
              stroke="#ff0000"
              strokeWidth="4"
              strokeLinecap="round"
              strokeLinejoin="round"
              fill="none"
            />
          </svg>
          <span
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              fontWeight: 800,
              color: '#ff0000',
            }}
          >
            Declining Returns
          </span>
        </div>
      </div>

      {/* Stick Figure — disappointed pose */}
      <div
        style={{
          position: 'absolute',
          bottom: 90,
          left: 80,
          opacity: sp(40),
          transform: `translateY(${interpolate(sp(40), [0, 1], [60, 0])}px)`,
        }}
      >
        <svg width="90" height="130" viewBox="0 0 90 130">
          {/* Golden glow */}
          <circle cx="45" cy="30" r="28" fill="rgba(255,193,7,0.12)" />
          {/* Head */}
          <circle
            cx="45"
            cy="28"
            r="18"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="3.5"
          />
          {/* Glasses */}
          <circle cx="38" cy="26" r="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <circle cx="52" cy="26" r="6" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <line x1="44" y1="26" x2="46" y2="26" stroke="#FFC107" strokeWidth="2" />
          {/* Sad mouth */}
          <path
            d="M38 35 Q45 31 52 35"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="2.5"
            strokeLinecap="round"
          />
          {/* Body — slumped */}
          <line
            x1="45"
            y1="46"
            x2="45"
            y2={85 + Math.sin(frame * 0.05) * 2}
            stroke="#1C1C1C"
            strokeWidth="3.5"
            strokeLinecap="round"
          />
          {/* Arms drooping */}
          <line x1="45" y1="60" x2="22" y2="80" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="60" x2="68" y2="80" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          {/* Legs */}
          <line x1="45" y1="85" x2="28" y2="122" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="85" x2="62" y2="122" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.18) 100%)',
          opacity: vigOp,
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene1_EmergingMarketShock;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene2_StructuralDisadvantage',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_StructuralDisadvantage: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Trap net draw animation
  const netOp = interpolate(frame, [20, 50], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Word reveal
  const undervalued = interpolate(frame, [10, 30], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const structuralX = interpolate(sp(30), [0, 1], [200, 0]);

  // Strikethrough width
  const strikeW = interpolate(frame, [35, 60], [0, 320], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Pulsing warning ring
  const ringScale = 1 + Math.sin(frame * 0.08) * 0.05;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#F5F5F5',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Background gradient — sharp left light */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(160deg, #ffffff 0%, #f7f7f7 40%, #ffe8e8 100%)',
        }}
      />

      {/* Large triangle shape — background depth */}
      <div
        style={{
          position: 'absolute',
          right: -80,
          top: -80,
          width: 0,
          height: 0,
          borderLeft: '600px solid transparent',
          borderBottom: '600px solid rgba(255,0,0,0.04)',
          transform: `rotate(${Math.sin(frame * 0.015) * 2}deg)`,
        }}
      />

      {/* Horizontal line texture */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 36 }).map((_, i) => (
            <line
              key={i}
              x1={0}
              y1={i * 30}
              x2={1920}
              y2={i * 30}
              stroke="#1C1C1C"
              strokeWidth={1}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const px = seededRandom(i * 3) * 1920;
        const py = seededRandom(i * 7) * 1080;
        const sz = 4 + seededRandom(i * 11) * 5;
        const drift = Math.cos(frame * 0.022 + i * 0.5) * 10;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py + drift,
              width: sz,
              height: sz,
              borderRadius: '50%',
              backgroundColor: '#ff0000',
              opacity: 0.07 + seededRandom(i * 9) * 0.08,
            }}
          />
        );
      })}

      {/* LEFT: Comparison boxes */}
      <div
        style={{
          position: 'absolute',
          left: 100,
          top: '50%',
          transform: 'translateY(-50%)',
          display: 'flex',
          flexDirection: 'column',
          gap: 32,
          width: 780,
        }}
      >
        {/* Label */}
        <div
          style={{
            opacity: sp(0),
            transform: `translateY(${interpolate(sp(0), [0, 1], [-20, 0])}px)`,
          }}
        >
          <span
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 26,
              fontWeight: 800,
              color: '#9E9E9E',
              letterSpacing: 4,
              textTransform: 'uppercase',
            }}
          >
            The Real Diagnosis
          </span>
        </div>

        {/* Wrong diagnosis box — UNDERVALUED crossed */}
        <div
          style={{
            opacity: undervalued,
            background: '#ffffff',
            border: '3px solid #e0e0e0',
            borderRadius: 20,
            padding: '28px 36px',
            position: 'relative',
            boxShadow: '0 4px 30px rgba(0,0,0,0.06)',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 52,
              fontWeight: 900,
              color: '#9E9E9E',
              letterSpacing: 1,
              position: 'relative',
              display: 'inline-block',
            }}
          >
            Undervalued
            {/* Strikethrough */}
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                height: 5,
                width: strikeW,
                backgroundColor: '#ff0000',
                borderRadius: 3,
              }}
            />
          </div>
          <div
            style={{
              marginTop: 10,
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 34,
              fontWeight: 500,
              color: '#9E9E9E',
            }}
          >
            ❌ यह गलत diagnosis है
          </div>
        </div>

        {/* Correct diagnosis — STRUCTURALLY DISADVANTAGED */}
        <div
          style={{
            transform: `translateX(${structuralX}px)`,
            opacity: sp(30),
            background: '#1C1C1C',
            borderRadius: 20,
            padding: '28px 36px',
            boxShadow: '0 8px 40px rgba(28,28,28,0.18)',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 46,
              fontWeight: 900,
              color: '#e9d700',
              letterSpacing: 0.5,
            }}
          >
            Structurally
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 46,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: 0.5,
            }}
          >
            Disadvantaged
          </div>
          <div
            style={{
              marginTop: 12,
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 32,
              fontWeight: 600,
              color: '#9E9E9E',
            }}
          >
            ✓ यही असली सच्चाई है
          </div>
        </div>
      </div>

      {/* RIGHT: Trap SVG illustration */}
      <div
        style={{
          position: 'absolute',
          right: 120,
          top: '50%',
          transform: 'translateY(-50%)',
          opacity: netOp,
        }}
      >
        <svg width="560" height="560" viewBox="0 0 560 560">
          {/* Pulsing outer ring */}
          <circle
            cx="280"
            cy="280"
            r={200 * ringScale}
            fill="none"
            stroke="rgba(255,0,0,0.12)"
            strokeWidth="2"
          />
          <circle
            cx="280"
            cy="280"
            r={160 * ringScale}
            fill="none"
            stroke="rgba(255,0,0,0.08)"
            strokeWidth="2"
          />

          {/* Trap net lines */}
          {Array.from({ length: 9 }).map((_, i) => (
            <line
              key={`v${i}`}
              x1={100 + i * 45}
              y1={100}
              x2={100 + i * 45}
              y2={460}
              stroke="rgba(28,28,28,0.15)"
              strokeWidth={1.5}
            />
          ))}
          {Array.from({ length: 9 }).map((_, i) => (
            <line
              key={`h${i}`}
              x1={100}
              y1={100 + i * 45}
              x2={460}
              y2={100 + i * 45}
              stroke="rgba(28,28,28,0.15)"
              strokeWidth={1.5}
            />
          ))}

          {/* Center country circle */}
          <circle cx="280" cy="280" r="60" fill="#ff0000" opacity="0.18" />
          <circle cx="280" cy="280" r="44" fill="#ff0000" opacity="0.28" />

          {/* Country flag-like text */}
          <text
            x="280"
            y="268"
            textAnchor="middle"
            fontFamily="Inter, sans-serif"
            fontSize="20"
            fontWeight="800"
            fill="#1C1C1C"
          >
            Developing
          </text>
          <text
            x="280"
            y="294"
            textAnchor="middle"
            fontFamily="Inter, sans-serif"
            fontSize="20"
            fontWeight="800"
            fill="#1C1C1C"
          >
            Countries
          </text>

          {/* Chain links to edges */}
          {[0, 90, 180, 270].map((angle, i) => {
            const rad = (angle * Math.PI) / 180;
            const x1 = 280 + Math.cos(rad) * 44;
            const y1 = 280 + Math.sin(rad) * 44;
            const x2 = 280 + Math.cos(rad) * 180;
            const y2 = 280 + Math.sin(rad) * 180;
            return (
              <line
                key={i}
                x1={x1}
                y1={y1}
                x2={x2}
                y2={y2}
                stroke="#ff0000"
                strokeWidth="3"
                strokeDasharray="8 6"
              />
            );
          })}

          {/* Anchor icons at corners */}
          {['WTO', 'IMF', 'World Bank', 'Dollar'].map((label, i) => {
            const angles = [45, 135, 225, 315];
            const rad = (angles[i] * Math.PI) / 180;
            const x = 280 + Math.cos(rad) * 200;
            const y = 280 + Math.sin(rad) * 200;
            return (
              <g key={i}>
                <circle cx={x} cy={y} r="34" fill="#1C1C1C" opacity="0.9" />
                <text
                  x={x}
                  y={y + 6}
                  textAnchor="middle"
                  fontFamily="Inter, sans-serif"
                  fontSize="13"
                  fontWeight="800"
                  fill="#e9d700"
                >
                  {label}
                </text>
              </g>
            );
          })}
        </svg>
      </div>

      {/* Stick figure — trapped gesture */}
      <div
        style={{
          position: 'absolute',
          bottom: 60,
          right: 80,
          opacity: sp(45),
          transform: `translateY(${interpolate(sp(45), [0, 1], [50, 0])}px)`,
        }}
      >
        <svg width="85" height="125" viewBox="0 0 85 125">
          <circle cx="42" cy="26" r="24" fill="rgba(255,193,7,0.15)" />
          <circle cx="42" cy="26" r="17" fill="none" stroke="#1C1C1C" strokeWidth="3.5" />
          <circle cx="36" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <circle cx="48" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <line x1="41.5" y1="24" x2="42.5" y2="24" stroke="#FFC107" strokeWidth="2" />
          <path d="M36 33 Q42 37 48 33" fill="none" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          {/* Body */}
          <line
            x1="42"
            y1="43"
            x2="42"
            y2={82 + Math.sin(frame * 0.05) * 2}
            stroke="#1C1C1C"
            strokeWidth="3.5"
            strokeLinecap="round"
          />
          {/* Arms raised in frustration */}
          <line x1="42" y1="55" x2="18" y2="40" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="42" y1="55" x2="66" y2="40" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="42" y1="82" x2="28" y2="118" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="42" y1="82" x2="56" y2="118" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(28,28,28,0.15) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene2_StructuralDisadvantage;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene3_IndiaEscape',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_IndiaEscape: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Arrow escape animation
  const arrowProgress = interpolate(frame, [30, 90], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Headline
  const h1Y = interpolate(sp(5), [0, 1], [60, 0]);
  const h2Y = interpolate(sp(20), [0, 1], [60, 0]);

  // Country chips
  const chipsOp = interpolate(frame, [50, 70], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Glow pulse
  const glowR = 120 + Math.sin(frame * 0.06) * 15;

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#F5F5F5',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Background */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(120deg, #f8fff8 0%, #ffffff 50%, #fffde8 100%)',
        }}
      />

      {/* Large circle shape — hope glow */}
      <div
        style={{
          position: 'absolute',
          right: -100,
          top: '50%',
          transform: `translateY(-50%) scale(${1 + Math.sin(frame * 0.02) * 0.02})`,
          width: 700,
          height: 700,
          borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(233,215,0,0.1) 0%, transparent 70%)',
        }}
      />

      {/* Diagonal line texture */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 40 }).map((_, i) => (
            <line
              key={i}
              x1={i * 50 - 400}
              y1={0}
              x2={i * 50}
              y2={1080}
              stroke="#1C1C1C"
              strokeWidth={1}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const px = seededRandom(i * 5) * 1920;
        const py = seededRandom(i * 9) * 1080;
        const sz = 3 + seededRandom(i * 3) * 7;
        const drift = Math.sin(frame * 0.025 + i) * 14;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py + drift,
              width: sz,
              height: sz,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#e9d700' : '#1C1C1C',
              opacity: 0.06 + seededRandom(i * 7) * 0.1,
            }}
          />
        );
      })}

      {/* LEFT content */}
      <div
        style={{
          position: 'absolute',
          left: 100,
          top: '50%',
          transform: 'translateY(-50%)',
          width: 840,
        }}
      >
        {/* Badge */}
        <div
          style={{
            opacity: sp(0),
            marginBottom: 24,
            display: 'inline-flex',
            alignItems: 'center',
            gap: 10,
            background: '#1C1C1C',
            borderRadius: 10,
            padding: '10px 24px',
          }}
        >
          <svg width="20" height="20" viewBox="0 0 20 20">
            <circle cx="10" cy="10" r="9" fill="none" stroke="#e9d700" strokeWidth="2" />
            <path d="M6 10 L9 13 L14 7" stroke="#e9d700" strokeWidth="2" strokeLinecap="round" fill="none" />
          </svg>
          <span
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 22,
              fontWeight: 800,
              color: '#e9d700',
              letterSpacing: 3,
            }}
          >
            SELECT FEW
          </span>
        </div>

        {/* Main headline */}
        <div
          style={{
            transform: `translateY(${h1Y}px)`,
            opacity: sp(5),
            marginBottom: 8,
          }}
        >
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 88,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.08,
              display: 'block',
            }}
          >
            भारत उन
          </span>
        </div>
        <div
          style={{
            transform: `translateY(${h2Y}px)`,
            opacity: sp(20),
            marginBottom: 32,
          }}
        >
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 88,
              fontWeight: 900,
              lineHeight: 1.08,
              display: 'block',
            }}
          >
            <span style={{ color: '#e9d700', WebkitTextStroke: '2px #1C1C1C' }}>
              गिने चुने
            </span>{' '}
            <span style={{ color: '#1C1C1C' }}>देशों में</span>
          </span>
        </div>

        {/* Subtext */}
        <div
          style={{
            opacity: interpolate(frame, [35, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            marginBottom: 40,
          }}
        >
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 42,
              fontWeight: 600,
              color: '#2D2D2D',
              lineHeight: 1.5,
            }}
          >
            जिन्होंने इस जाल से बाहर निकलने की
            <br />
            <span style={{ color: '#1C1C1C', fontWeight: 800 }}>कोशिश की है</span>
          </span>
        </div>

        {/* Country chips */}
        <div
          style={{
            opacity: chipsOp,
            display: 'flex',
            gap: 16,
            flexWrap: 'wrap',
          }}
        >
          {['🇮🇳 India', '🇨🇳 China', '🇰🇷 S. Korea', '🇩🇪 Germany'].map((c, i) => (
            <div
              key={i}
              style={{
                padding: '10px 22px',
                borderRadius: 40,
                background: i === 0 ? '#1C1C1C' : '#ffffff',
                border: i === 0 ? '3px solid #1C1C1C' : '2px solid #e0e0e0',
                fontFamily: 'Inter, sans-serif',
                fontSize: 26,
                fontWeight: 700,
                color: i === 0 ? '#e9d700' : '#1C1C1C',
                boxShadow: i === 0 ? '0 4px 20px rgba(0,0,0,0.12)' : 'none',
                transform: `translateY(${interpolate(
                  sp(50 + i * 8),
                  [0, 1],
                  [20, 0]
                )}px)`,
              }}
            >
              {c}
            </div>
          ))}
        </div>
      </div>

      {/* RIGHT: Escape arrow visualization */}
      <div
        style={{
          position: 'absolute',
          right: 80,
          top: '50%',
          transform: 'translateY(-50%)',
        }}
      >
        <svg width="620" height="500" viewBox="0 0 620 500">
          {/* Trap box */}
          <rect
            x="30"
            y="200"
            width="220"
            height="200"
            fill="rgba(255,0,0,0.08)"
            stroke="rgba(255,0,0,0.3)"
            strokeWidth="2.5"
            strokeDasharray="8 5"
            rx="12"
          />
          <text x="140" y="295" textAnchor="middle" fontFamily="Noto Sans Devanagari" fontSize="20" fontWeight="700" fill="#ff0000">
            Global Financial
          </text>
          <text x="140" y="320" textAnchor="middle" fontFamily="Noto Sans Devanagari" fontSize="20" fontWeight="700" fill="#ff0000">
            Trap
          </text>

          {/* Escape arrow path */}
          <path
            d={`M 250 300 Q 400 ${300 - arrowProgress * 180} ${250 + arrowProgress * 320} ${300 - arrowProgress * 220}`}
            fill="none"
            stroke="#e9d700"
            strokeWidth="5"
            strokeLinecap="round"
            strokeDasharray={`${arrowProgress * 800} 800`}
          />

          {/* Arrow head — only shows when arrow is fully drawn */}
          {arrowProgress > 0.85 && (
            <g transform={`translate(${250 + arrowProgress * 320 - 16}, ${300 - arrowProgress * 220 - 8}) rotate(-45)`}>
              <polygon points="0,0 24,10 10,24" fill="#e9d700" />
            </g>
          )}

          {/* Destination star */}
          <g
            opacity={arrowProgress}
            transform={`translate(${250 + arrowProgress * 300}, ${300 - arrowProgress * 210})`}
          >
            <circle r={glowR * 0.28} fill="rgba(233,215,0,0.15)" />
            <circle r="28" fill="#1C1C1C" />
            <text textAnchor="middle" y="6" fontFamily="Inter" fontSize="15" fontWeight="800" fill="#e9d700">
              FREE
            </text>
          </g>
        </svg>
      </div>

      {/* Stick figure — walking out pose */}
      <div
        style={{
          position: 'absolute',
          bottom: 60,
          left: 80,
          opacity: sp(40),
          transform: `translateX(${interpolate(sp(40), [0, 1], [-60, 0])}px)`,
        }}
      >
        <svg width="90" height="130" viewBox="0 0 90 130">
          <circle cx="45" cy="26" r="26" fill="rgba(255,193,7,0.15)" />
          <circle cx="45" cy="26" r="17" fill="none" stroke="#1C1C1C" strokeWidth="3.5" />
          <circle cx="39" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <circle cx="51" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <line x1="44.5" y1="24" x2="45.5" y2="24" stroke="#FFC107" strokeWidth="2" />
          {/* Smile */}
          <path d="M39 33 Q45 39 51 33" fill="none" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="45" y1="43" x2="45" y2={82 + Math.sin(frame * 0.05) * 2} stroke="#1C1C1C" strokeWidth="3.5" strokeLinecap="round" />
          {/* Walking arms */}
          <line x1="45" y1="55" x2="24" y2="70" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="55" x2="64" y2="62" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          {/* Walking legs */}
          <line x1="45" y1="82" x2="32" y2="118" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="82" x2="60" y2="116" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(28,28,28,0.12) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene3_IndiaEscape;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene4_CapitalControl',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4_CapitalControl: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Gate door animation
  const leftDoor = interpolate(sp(20), [0, 1], [0, -180]);
  const rightDoor = interpolate(sp(20), [0, 1], [0, 180]);

  // Money flow arrows
  const flowOp = interpolate(frame, [45, 65], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const arrowX1 = interpolate(frame, [45, 80], [0, 200], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const arrowX2 = interpolate(frame, [55, 90], [0, -200], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Headline
  const h1Sc = sp(0);
  const h2Y = interpolate(sp(15), [0, 1], [40, 0]);

  // Year badge
  const yearSc = sp(5);

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#F5F5F5',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Background */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(145deg, #ffffff 0%, #f5f5f5 50%, #f0f0f0 100%)',
        }}
      />

      {/* Large hexagon shape */}
      <div
        style={{
          position: 'absolute',
          left: -150,
          bottom: -150,
          opacity: 0.04,
          transform: `rotate(${frame * 0.05}deg)`,
        }}
      >
        <svg width="600" height="600" viewBox="0 0 600 600">
          <polygon
            points="300,30 570,165 570,435 300,570 30,435 30,165"
            fill="#1C1C1C"
          />
        </svg>
      </div>

      {/* Dot grid texture */}
      <AbsoluteFill style={{ opacity: 0.06 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 32 }).map((_, r) =>
            Array.from({ length: 55 }).map((_, c) => (
              <circle
                key={`${r}-${c}`}
                cx={c * 36}
                cy={r * 36}
                r={2}
                fill="#1C1C1C"
              />
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const px = seededRandom(i * 11) * 1920;
        const py = seededRandom(i * 17) * 1080;
        const sz = 4 + seededRandom(i * 3) * 5;
        const drift = Math.sin(frame * 0.03 + i * 0.7) * 12;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py + drift,
              width: sz,
              height: sz,
              borderRadius: '50%',
              backgroundColor: '#FFC107',
              opacity: 0.08 + seededRandom(i * 5) * 0.1,
            }}
          />
        );
      })}

      {/* TOP: Year + Headline */}
      <div
        style={{
          position: 'absolute',
          left: 100,
          top: 80,
          width: 900,
        }}
      >
        {/* Year badge */}
        <div
          style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 16,
            marginBottom: 20,
            opacity: yearSc,
            transform: `scale(${0.5 + yearSc * 0.5})`,
          }}
        >
          <div
            style={{
              background: '#1C1C1C',
              borderRadius: 12,
              padding: '12px 28px',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 52,
              fontWeight: 900,
              color: '#e9d700',
              letterSpacing: 3,
            }}
          >
            1991
          </div>
          <span
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              fontWeight: 700,
              color: '#9E9E9E',
            }}
          >
            Post-Liberalization
          </span>
        </div>

        {/* Headline */}
        <div
          style={{
            opacity: h1Sc,
            transform: `scale(${0.7 + h1Sc * 0.3})`,
            marginBottom: 12,
          }}
        >
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 80,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.1,
              display: 'block',
            }}
          >
            उदारीकरण हुआ —
          </span>
        </div>
        <div
          style={{
            transform: `translateY(${h2Y}px)`,
            opacity: sp(15),
            marginBottom: 28,
          }}
        >
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 80,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.1,
              display: 'block',
            }}
          >
            लेकिन{' '}
            <span
              style={{
                background: '#1C1C1C',
                color: '#e9d700',
                padding: '4px 16px',
                borderRadius: 8,
              }}
            >
              नियंत्रण
            </span>{' '}
            रहा
          </span>
        </div>

        <div
          style={{
            opacity: interpolate(frame, [30, 50], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 38,
              fontWeight: 500,
              color: '#2D2D2D',
              lineHeight: 1.5,
            }}
          >
            Capital Account Convertibility पूरी तरह नहीं की
            <br />
            पूंजी की आने-जाने पर{' '}
            <span style={{ color: '#1C1C1C', fontWeight: 800 }}>strategic control बनाए रखा</span>
          </span>
        </div>
      </div>

      {/* CENTER: Gate visualization */}
      <div
        style={{
          position: 'absolute',
          right: 100,
          top: '50%',
          transform: 'translateY(-50%)',
        }}
      >
        <svg width="700" height="480" viewBox="0 0 700 480" overflow="visible">
          {/* Gate frame */}
          <rect x="150" y="60" width="400" height="360" rx="8" fill="none" stroke="#e0e0e0" strokeWidth="3" />

          {/* Left door */}
          <g transform={`translate(${150 + leftDoor}, 60)`}>
            <rect width="190" height="360" rx="4" fill="#1C1C1C" opacity="0.9" />
            {/* Door bars */}
            {[40, 80, 120, 160, 200, 240, 280, 320].map((y, i) => (
              <line key={i} x1="20" y1={y} x2="170" y2={y} stroke="#e9d700" strokeWidth="2" opacity="0.4" />
            ))}
            <text x="95" y="190" textAnchor="middle" fontFamily="Inter" fontSize="18" fontWeight="700" fill="#e9d700">
              CONTROL
            </text>
          </g>

          {/* Right door */}
          <g transform={`translate(${360 + rightDoor}, 60)`}>
            <rect width="190" height="360" rx="4" fill="#1C1C1C" opacity="0.9" />
            {[40, 80, 120, 160, 200, 240, 280, 320].map((y, i) => (
              <line key={i} x1="20" y1={y} x2="170" y2={y} stroke="#e9d700" strokeWidth="2" opacity="0.4" />
            ))}
            <text x="95" y="190" textAnchor="middle" fontFamily="Inter" fontSize="18" fontWeight="700" fill="#e9d700">
              GUARD
            </text>
          </g>

          {/* Money in arrow — allowed */}
          <g opacity={flowOp} transform={`translate(${arrowX1}, 0)`}>
            <text x="350" y="35" textAnchor="middle" fontFamily="Inter" fontSize="16" fontWeight="700" fill="#1C1C1C">
              Strategic Inflow ✓
            </text>
            <path
              d="M280 50 L420 50"
              stroke="#e9d700"
              strokeWidth="4"
              strokeLinecap="round"
              markerEnd="url(#arrowY)"
            />
          </g>

          {/* Money out arrow — restricted */}
          <g opacity={flowOp} transform={`translate(${arrowX2}, 0)`}>
            <text x="350" y="450" textAnchor="middle" fontFamily="Inter" fontSize="16" fontWeight="700" fill="#ff0000">
              Speculative Outflow ✗
            </text>
            <path
              d="M420 435 L280 435"
              stroke="#ff0000"
              strokeWidth="4"
              strokeLinecap="round"
              strokeDasharray="8 5"
            />
          </g>

          <defs>
            <marker id="arrowY" markerWidth="10" markerHeight="7" refX="10" refY="3.5" orient="auto">
              <polygon points="0 0, 10 3.5, 0 7" fill="#e9d700" />
            </marker>
          </defs>
        </svg>
      </div>

      {/* Stick Figure — guarding stance */}
      <div
        style={{
          position: 'absolute',
          bottom: 60,
          left: 80,
          opacity: sp(45),
          transform: `translateY(${interpolate(sp(45), [0, 1], [40, 0])}px)`,
        }}
      >
        <svg width="90" height="130" viewBox="0 0 90 130">
          <circle cx="45" cy="26" r="26" fill="rgba(255,193,7,0.15)" />
          <circle cx="45" cy="26" r="17" fill="none" stroke="#1C1C1C" strokeWidth="3.5" />
          <circle cx="39" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <circle cx="51" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <line x1="44.5" y1="24" x2="45.5" y2="24" stroke="#FFC107" strokeWidth="2" />
          <path d="M39 32 Q45 36 51 32" fill="none" stroke="#1C1C1C" strokeWidth="2" strokeLinecap="round" />
          <line x1="45" y1="43" x2="45" y2={84 + Math.sin(frame * 0.05) * 2} stroke="#1C1C1C" strokeWidth="3.5" strokeLinecap="round" />
          {/* Arms out — guarding */}
          <line x1="45" y1="55" x2="14" y2="55" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="55" x2="76" y2="55" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="84" x2="30" y2="120" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="84" x2="60" y2="120" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(28,28,28,0.1) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene4_CapitalControl;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene5_AsianCrisis',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5_AsianCrisis: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Screen shake for crisis impact (frames 25-35)
  const shakeX = frame >= 25 && frame <= 35
    ? Math.sin(frame * 2.5) * 8 * (1 - (frame - 25) / 10)
    : 0;
  const shakeY = frame >= 25 && frame <= 35
    ? Math.cos(frame * 3) * 5 * (1 - (frame - 25) / 10)
    : 0;

  // Country collapse animations
  const th = interpolate(frame, [25, 45], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const id = interpolate(frame, [30, 50], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const my = interpolate(frame, [35, 55], [1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // India shield
  const shieldSc = sp(55);

  // Headline
  const yearOp = sp(0);
  const taglineSc = sp(10);

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#F5F5F5',
        overflow: 'hidden',
        opacity: exitOp,
        transform: `translate(${shakeX}px, ${shakeY}px)`,
      }}
    >
      {/* Background — crisis red left half */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(to right, #fff0f0 0%, #ffffff 55%, #f0fff0 100%)',
        }}
      />

      {/* Large shapes */}
      <div
        style={{
          position: 'absolute',
          left: -100,
          top: -100,
          width: 600,
          height: 600,
          borderRadius: '50%',
          background: 'radial-gradient(circle, rgba(255,0,0,0.07) 0%, transparent 70%)',
          transform: `scale(${1 + Math.sin(frame * 0.02) * 0.02})`,
        }}
      />

      {/* Circuit lines texture */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 12 }).map((_, i) => (
            <g key={i}>
              <line x1={i * 160} y1={0} x2={i * 160} y2={1080} stroke="#1C1C1C" strokeWidth={1} />
              <line x1={0} y1={i * 96} x2={1920} y2={i * 96} stroke="#1C1C1C" strokeWidth={1} />
            </g>
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const px = seededRandom(i * 7) * 1920;
        const py = seededRandom(i * 13) * 1080;
        const sz = 3 + seededRandom(i * 9) * 6;
        const drift = Math.sin(frame * 0.02 + i) * 11;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py + drift,
              width: sz,
              height: sz,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#ff0000' : '#1C1C1C',
              opacity: 0.06 + seededRandom(i * 5) * 0.1,
            }}
          />
        );
      })}

      {/* Year + Title — top */}
      <div
        style={{
          position: 'absolute',
          left: 100,
          top: 60,
          opacity: yearOp,
        }}
      >
        <div
          style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 16,
            background: '#ff0000',
            borderRadius: 12,
            padding: '12px 30px',
            marginBottom: 16,
          }}
        >
          <span
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 36,
              fontWeight: 900,
              color: '#F5F5F5',
              letterSpacing: 3,
            }}
          >
            1997-98
          </span>
          <span
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              fontWeight: 700,
              color: '#F5F5F5',
            }}
          >
            ASIAN FINANCIAL CRISIS
          </span>
        </div>
      </div>

      {/* LEFT: Falling countries */}
      <div
        style={{
          position: 'absolute',
          left: 80,
          top: 180,
          width: 720,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 44,
            fontWeight: 800,
            color: '#ff0000',
            marginBottom: 30,
            opacity: interpolate(frame, [10, 25], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          ये देश तबाह हो गए ↓
        </div>

        {[
          { country: '🇹🇭 Thailand', drop: '-44%', op: th },
          { country: '🇮🇩 Indonesia', drop: '-83%', op: id },
          { country: '🇲🇾 Malaysia', drop: '-60%', op: my },
        ].map((c, i) => (
          <div
            key={i}
            style={{
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'space-between',
              background: '#1C1C1C',
              borderRadius: 16,
              padding: '18px 28px',
              marginBottom: 16,
              transform: `translateX(${interpolate(1 - c.op, [0, 1], [0, 400])}px) rotate(${(1 - c.op) * -8}deg)`,
              opacity: c.op,
            }}
          >
            <span
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 34,
                fontWeight: 700,
                color: '#F5F5F5',
              }}
            >
              {c.country}
            </span>
            <span
              style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 38,
                fontWeight: 900,
                color: '#ff0000',
              }}
            >
              {c.drop}
            </span>
          </div>
        ))}

        {/* Currency drop label */}
        <div
          style={{
            opacity: interpolate(frame, [20, 35], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            fontFamily: 'Inter, sans-serif',
            fontSize: 24,
            fontWeight: 600,
            color: '#9E9E9E',
            marginTop: 8,
          }}
        >
          Currency crash vs USD
        </div>
      </div>

      {/* RIGHT: India Shield */}
      <div
        style={{
          position: 'absolute',
          right: 100,
          top: '50%',
          transform: `translateY(-50%) scale(${0.2 + shieldSc * 0.8})`,
          opacity: shieldSc,
        }}
      >
        <svg width="520" height="520" viewBox="0 0 520 520">
          {/* Shield glow */}
          <circle cx="260" cy="260" r="200" fill="rgba(233,215,0,0.1)" />
          <circle cx="260" cy="260" r="170" fill="rgba(233,215,0,0.07)" />

          {/* Shield body */}
          <path
            d="M260 60 L420 130 L420 300 Q420 420 260 470 Q100 420 100 300 L100 130 Z"
            fill="#1C1C1C"
          />
          <path
            d="M260 90 L390 150 L390 295 Q390 395 260 440 Q130 395 130 295 L130 150 Z"
            fill="#2D2D2D"
          />
          <path
            d="M260 120 L360 170 L360 290 Q360 370 260 410 Q160 370 160 290 L160 170 Z"
            fill="none"
            stroke="#e9d700"
            strokeWidth="3"
          />

          {/* India text */}
          <text
            x="260"
            y="250"
            textAnchor="middle"
            fontFamily="Inter, sans-serif"
            fontSize="28"
            fontWeight="900"
            fill="#e9d700"
          >
            🇮🇳 INDIA
          </text>
          <text
            x="260"
            y="290"
            textAnchor="middle"
            fontFamily="Noto Sans Devanagari, sans-serif"
            fontSize="22"
            fontWeight="700"
            fill="#F5F5F5"
          >
            बचा रहा
          </text>
          <text
            x="260"
            y="322"
            textAnchor="middle"
            fontFamily="Inter, sans-serif"
            fontSize="20"
            fontWeight="600"
            fill="#9E9E9E"
          >
            Capital Controls = Protection
          </text>

          {/* Checkmark */}
          <circle cx="260" cy="175" r="28" fill="#e9d700" />
          <path
            d="M248 175 L257 184 L274 166"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="4"
            strokeLinecap="round"
            strokeLinejoin="round"
          />
        </svg>
      </div>

      {/* BOTTOM tagline */}
      <div
        style={{
          position: 'absolute',
          bottom: 120,
          left: 80,
          opacity: sp(65),
          transform: `translateY(${interpolate(sp(65), [0, 1], [30, 0])}px)`,
        }}
      >
        <div
          style={{
            background: '#e9d700',
            borderRadius: 12,
            padding: '16px 32px',
            display: 'inline-block',
          }}
        >
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 38,
              fontWeight: 900,
              color: '#1C1C1C',
            }}
          >
            नियंत्रण = सुरक्षा — यही था भारत का स्मार्ट move
          </span>
        </div>
      </div>

      {/* Stick figure — arms raised in triumph */}
      <div
        style={{
          position: 'absolute',
          bottom: 55,
          right: 80,
          opacity: sp(60),
          transform: `translateY(${interpolate(sp(60), [0, 1], [50, 0])}px)`,
        }}
      >
        <svg width="90" height="130" viewBox="0 0 90 130">
          <circle cx="45" cy="26" r="26" fill="rgba(255,193,7,0.2)" />
          <circle cx="45" cy="26" r="17" fill="none" stroke="#1C1C1C" strokeWidth="3.5" />
          <circle cx="39" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <circle cx="51" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <line x1="44.5" y1="24" x2="45.5" y2="24" stroke="#FFC107" strokeWidth="2" />
          <path d="M38 34 Q45 40 52 34" fill="none" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="45" y1="43" x2="45" y2={82 + Math.sin(frame * 0.05) * 2} stroke="#1C1C1C" strokeWidth="3.5" strokeLinecap="round" />
          {/* Arms raised */}
          <line x1="45" y1="55" x2="16" y2="32" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="55" x2="74" y2="32" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="82" x2="30" y2="118" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="82" x2="60" y2="118" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(28,28,28,0.14) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene5_AsianCrisis;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene6_ChinaStateLed',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6_ChinaStateLed: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // GDP bar growth
  const gdpH = interpolate(sp(20), [0, 1], [0, 320]);

  // Strategy pillars — stagger in
  const p1 = sp(15);
  const p2 = sp(28);
  const p3 = sp(41);

  // Number counter — $18T
  const counter = interpolate(frame, [30, 80], [0, 18], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Background pan
  const bgPan = interpolate(frame, [0, durationInFrames], [-20, 20]);

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#F5F5F5',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Background */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(160deg, #f8f8f8 0%, #ffffff 50%, #f5f8ff 100%)',
          transform: `translateX(${bgPan}px)`,
          width: '110%',
        }}
      />

      {/* Large shape — background pentagon */}
      <div
        style={{
          position: 'absolute',
          right: -200,
          top: -200,
          opacity: 0.03,
          transform: `rotate(${frame * 0.04}deg)`,
        }}
      >
        <svg width="800" height="800" viewBox="0 0 800 800">
          <polygon
            points="400,50 740,270 610,650 190,650 60,270"
            fill="#1C1C1C"
          />
        </svg>
      </div>

      {/* Concentric circle texture */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 10 }).map((_, i) => (
            <circle
              key={i}
              cx={1400}
              cy={540}
              r={80 + i * 80}
              fill="none"
              stroke="#1C1C1C"
              strokeWidth={1}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const px = seededRandom(i * 6) * 1920;
        const py = seededRandom(i * 14) * 1080;
        const sz = 4 + seededRandom(i * 2) * 5;
        const drift = Math.cos(frame * 0.025 + i) * 13;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py + drift,
              width: sz,
              height: sz,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#FFC107' : '#1C1C1C',
              opacity: 0.07 + seededRandom(i * 8) * 0.09,
            }}
          />
        );
      })}

      {/* LEFT: Strategy Pillars */}
      <div
        style={{
          position: 'absolute',
          left: 100,
          top: '50%',
          transform: 'translateY(-50%)',
          width: 800,
        }}
      >
        {/* Label */}
        <div
          style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 12,
            background: '#1C1C1C',
            borderRadius: 10,
            padding: '10px 22px',
            marginBottom: 28,
            opacity: sp(0),
          }}
        >
          <span style={{ fontSize: 28 }}>🇨🇳</span>
          <span
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 24,
              fontWeight: 800,
              color: '#e9d700',
              letterSpacing: 2,
            }}
          >
            CHINA'S STATE-LED FORMULA
          </span>
        </div>

        {/* Three pillars */}
        {[
          {
            icon: '🏛️',
            title: 'State Industrial Policy',
            desc: 'पूरी economy को State की guidance में',
            op: p1,
            color: '#e9d700',
          },
          {
            icon: '🚪',
            title: 'Strategic Market Opening',
            desc: 'बाजार खोला — लेकिन अपनी शर्तों पर',
            op: p2,
            color: '#FFC107',
          },
          {
            icon: '⚙️',
            title: 'Mandatory Tech Transfer',
            desc: 'Foreign companies को तकनीक share करनी पड़ी',
            op: p3,
            color: '#1C1C1C',
          },
        ].map((p, i) => (
          <div
            key={i}
            style={{
              display: 'flex',
              alignItems: 'flex-start',
              gap: 20,
              background: i === 2 ? '#1C1C1C' : '#ffffff',
              border: `3px solid ${p.color}`,
              borderRadius: 18,
              padding: '20px 24px',
              marginBottom: 18,
              opacity: p.op,
              transform: `translateX(${interpolate(p.op, [0, 1], [-80, 0])}px)`,
              boxShadow:
                i === 2
                  ? '0 6px 30px rgba(28,28,28,0.15)'
                  : '0 4px 20px rgba(0,0,0,0.06)',
            }}
          >
            <div
              style={{
                width: 56,
                height: 56,
                borderRadius: 12,
                background: p.color,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                fontSize: 30,
                flexShrink: 0,
              }}
            >
              {p.icon}
            </div>
            <div>
              <div
                style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 30,
                  fontWeight: 800,
                  color: i === 2 ? '#F5F5F5' : '#1C1C1C',
                  marginBottom: 4,
                }}
              >
                {p.title}
              </div>
              <div
                style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: 26,
                  fontWeight: 500,
                  color: i === 2 ? '#9E9E9E' : '#2D2D2D',
                }}
              >
                {p.desc}
              </div>
            </div>
          </div>
        ))}
      </div>

      {/* RIGHT: GDP visualization */}
      <div
        style={{
          position: 'absolute',
          right: 80,
          top: '50%',
          transform: 'translateY(-50%)',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          gap: 24,
        }}
      >
        {/* Counter */}
        <div
          style={{
            opacity: sp(15),
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 130,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1,
            }}
          >
            ${Math.round(counter)}T
          </div>
          <div
            style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 28,
              fontWeight: 600,
              color: '#9E9E9E',
              marginTop: 8,
            }}
          >
            China's GDP — World #2
          </div>
        </div>

        {/* Bar comparison */}
        <div
          style={{
            display: 'flex',
            alignItems: 'flex-end',
            gap: 32,
            height: 360,
            padding: '0 20px',
            borderBottom: '3px solid #e0e0e0',
          }}
        >
          {[
            { label: '1980', h: 30, color: '#9E9E9E' },
            { label: '1990', h: 70, color: '#9E9E9E' },
            { label: '2000', h: 130, color: '#FFC107' },
            { label: '2010', h: 210, color: '#e9d700' },
            { label: '2023', h: gdpH, color: '#1C1C1C' },
          ].map((b, i) => (
            <div
              key={i}
              style={{
                display: 'flex',
                flexDirection: 'column',
                alignItems: 'center',
                gap: 8,
              }}
            >
              <div
                style={{
                  width: 72,
                  height: b.h,
                  backgroundColor: b.color,
                  borderRadius: '8px 8px 0 0',
                }}
              />
              <span
                style={{
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 20,
                  fontWeight: 700,
                  color: '#9E9E9E',
                }}
              >
                {b.label}
              </span>
            </div>
          ))}
        </div>

        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 26,
            fontWeight: 700,
            color: '#1C1C1C',
            opacity: sp(55),
          }}
        >
          State-led growth = दूसरी सबसे बड़ी economy
        </div>
      </div>

      {/* Stick figure — confident pose */}
      <div
        style={{
          position: 'absolute',
          bottom: 55,
          left: 80,
          opacity: sp(50),
          transform: `translateY(${interpolate(sp(50), [0, 1], [40, 0])}px)`,
        }}
      >
        <svg width="90" height="130" viewBox="0 0 90 130">
          <circle cx="45" cy="26" r="26" fill="rgba(255,193,7,0.18)" />
          <circle cx="45" cy="26" r="17" fill="none" stroke="#1C1C1C" strokeWidth="3.5" />
          <circle cx="39" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <circle cx="51" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <line x1="44.5" y1="24" x2="45.5" y2="24" stroke="#FFC107" strokeWidth="2" />
          <path d="M39 34 Q45 40 51 34" fill="none" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="45" y1="43" x2="45" y2={82 + Math.sin(frame * 0.05) * 2} stroke="#1C1C1C" strokeWidth="3.5" strokeLinecap="round" />
          <line x1="45" y1="55" x2="20" y2="44" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="55" x2="70" y2="44" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="82" x2="30" y2="118" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="82" x2="60" y2="118" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 52%, rgba(28,28,28,0.12) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene6_ChinaStateLed;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene7_Rulemakers',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7_Rulemakers: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // HERO text — mega scale in
  const heroSc = sp(0, { damping: 10, stiffness: 100 });
  const heroRotate = interpolate(sp(0), [0, 1], [-5, 0]);

  // Second line slide
  const line2X = interpolate(sp(20), [0, 1], [300, 0]);

  // Chess board reveal
  const boardOp = interpolate(frame, [30, 55], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Crown bounce
  const crownY = Math.sin(frame * 0.08) * 6;

  // Divider line
  const divW = interpolate(frame, [15, 40], [0, 800], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#F5F5F5',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Background — bold split */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, #1C1C1C 0%, #1C1C1C 48%, #F5F5F5 48%, #F5F5F5 100%)',
        }}
      />

      {/* Diagonal geometric shapes */}
      <div
        style={{
          position: 'absolute',
          left: -100,
          top: -100,
          width: 800,
          height: 800,
          borderRadius: 80,
          background: 'rgba(233,215,0,0.04)',
          transform: `rotate(${25 + Math.sin(frame * 0.015) * 2}deg)`,
        }}
      />

      {/* Dot grid — bottom half */}
      <AbsoluteFill style={{ opacity: 0.05 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 16 }).map((_, r) =>
            Array.from({ length: 54 }).map((_, c) => (
              <circle
                key={`${r}-${c}`}
                cx={c * 36}
                cy={540 + r * 36}
                r={2}
                fill="#1C1C1C"
              />
            ))
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const px = seededRandom(i * 4) * 1920;
        const py = seededRandom(i * 8) * 1080;
        const sz = 3 + seededRandom(i * 6) * 6;
        const drift = Math.sin(frame * 0.028 + i * 0.6) * 12;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py + drift,
              width: sz,
              height: sz,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#e9d700' : '#F5F5F5',
              opacity: 0.06 + seededRandom(i * 12) * 0.1,
            }}
          />
        );
      })}

      {/* TOP HALF — Dark: Main hero statement */}
      <div
        style={{
          position: 'absolute',
          left: 0,
          top: 0,
          width: '100%',
          height: '48%',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          flexDirection: 'column',
          gap: 16,
        }}
      >
        {/* Hero line 1 */}
        <div
          style={{
            transform: `scale(${0.3 + heroSc * 0.7}) rotate(${heroRotate}deg)`,
            opacity: heroSc,
            textAlign: 'center',
          }}
        >
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 120,
              fontWeight: 900,
              color: '#e9d700',
              lineHeight: 1,
              display: 'block',
              textShadow: '0 4px 30px rgba(233,215,0,0.3)',
            }}
          >
            नियम वही जीतते हैं
          </span>
        </div>

        {/* Divider */}
        <div
          style={{
            width: divW,
            height: 4,
            background: 'linear-gradient(to right, transparent, #e9d700, transparent)',
            borderRadius: 2,
          }}
        />

        {/* Hero line 2 */}
        <div
          style={{
            transform: `translateX(${line2X}px)`,
            opacity: sp(20),
            textAlign: 'center',
          }}
        >
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 72,
              fontWeight: 800,
              color: '#F5F5F5',
              lineHeight: 1.1,
              display: 'block',
            }}
          >
            जो अपने{' '}
            <span
              style={{
                background: '#e9d700',
                color: '#1C1C1C',
                padding: '2px 16px',
                borderRadius: 8,
              }}
            >
              खेल के rules
            </span>{' '}
            खुद बनाते हैं
          </span>
        </div>
      </div>

      {/* BOTTOM HALF — Light: Investor insight */}
      <div
        style={{
          position: 'absolute',
          bottom: 0,
          left: 0,
          width: '100%',
          height: '52%',
          display: 'flex',
          alignItems: 'center',
          paddingLeft: 100,
          paddingRight: 100,
          gap: 60,
          boxSizing: 'border-box',
        }}
      >
        {/* Chess board illustration */}
        <div style={{ opacity: boardOp, flexShrink: 0 }}>
          <svg width="280" height="280" viewBox="0 0 280 280">
            {/* Chess board */}
            {Array.from({ length: 8 }).map((_, r) =>
              Array.from({ length: 8 }).map((_, c) => (
                <rect
                  key={`${r}-${c}`}
                  x={c * 35}
                  y={r * 35}
                  width="35"
                  height="35"
                  fill={(r + c) % 2 === 0 ? '#1C1C1C' : '#F5F5F5'}
                  stroke="#e0e0e0"
                  strokeWidth="0.5"
                />
              ))
            )}
            {/* Crown on king square */}
            <g transform={`translate(140, ${140 + crownY})`}>
              <circle r="20" fill="#e9d700" />
              <text
                textAnchor="middle"
                y="8"
                fontFamily="Inter"
                fontSize="22"
                fontWeight="900"
              >
                ♛
              </text>
            </g>
          </svg>
        </div>

        {/* Investor insight text */}
        <div style={{ flex: 1 }}>
          <div
            style={{
              opacity: sp(40),
              transform: `translateY(${interpolate(sp(40), [0, 1], [30, 0])}px)`,
              marginBottom: 20,
            }}
          >
            <div
              style={{
                display: 'inline-flex',
                alignItems: 'center',
                gap: 10,
                background: '#1C1C1C',
                borderRadius: 10,
                padding: '8px 20px',
                marginBottom: 16,
              }}
            >
              <span
                style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 22,
                  fontWeight: 800,
                  color: '#e9d700',
                  letterSpacing: 2,
                }}
              >
                INVESTOR INSIGHT
              </span>
            </div>

            <span
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 52,
                fontWeight: 800,
                color: '#1C1C1C',
                lineHeight: 1.3,
                display: 'block',
              }}
            >
              एक निवेशक के तौर पर
            </span>
            <span
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 52,
                fontWeight: 800,
                color: '#1C1C1C',
                lineHeight: 1.3,
                display: 'block',
              }}
            >
              तुम्हें ये समझना{' '}
              <span
                style={{
                  color: '#ff0000',
                  fontWeight: 900,
                  textDecoration: 'underline',
                  textDecorationColor: '#ff0000',
                  textDecorationThickness: 4,
                }}
              >
                होगा
              </span>
            </span>
          </div>

          {/* Two-column quick insight */}
          <div
            style={{
              display: 'flex',
              gap: 20,
              opacity: sp(55),
            }}
          >
            {[
              { icon: '❌', text: 'Rule Follower = पीछे रहता है' },
              { icon: '✅', text: 'Rule Maker = आगे निकलता है' },
            ].map((item, i) => (
              <div
                key={i}
                style={{
                  flex: 1,
                  background: i === 1 ? '#1C1C1C' : '#f0f0f0',
                  borderRadius: 16,
                  padding: '16px 20px',
                  display: 'flex',
                  alignItems: 'center',
                  gap: 12,
                }}
              >
                <span style={{ fontSize: 32 }}>{item.icon}</span>
                <span
                  style={{
                    fontFamily: 'Noto Sans Devanagari, sans-serif',
                    fontSize: 28,
                    fontWeight: 700,
                    color: i === 1 ? '#e9d700' : '#9E9E9E',
                  }}
                >
                  {item.text}
                </span>
              </div>
            ))}
          </div>
        </div>
      </div>

      {/* Stick figure — pointing up */}
      <div
        style={{
          position: 'absolute',
          bottom: 55,
          right: 80,
          opacity: sp(55),
          transform: `translateY(${interpolate(sp(55), [0, 1], [40, 0])}px)`,
        }}
      >
        <svg width="90" height="130" viewBox="0 0 90 130">
          <circle cx="45" cy="26" r="26" fill="rgba(233,215,0,0.2)" />
          <circle cx="45" cy="26" r="17" fill="none" stroke="#1C1C1C" strokeWidth="3.5" />
          <circle cx="39" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <circle cx="51" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <line x1="44.5" y1="24" x2="45.5" y2="24" stroke="#FFC107" strokeWidth="2" />
          <path d="M39 34 Q45 40 51 34" fill="none" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="45" y1="43" x2="45" y2={82 + Math.sin(frame * 0.05) * 2} stroke="#1C1C1C" strokeWidth="3.5" strokeLinecap="round" />
          {/* One arm pointing up */}
          <line x1="45" y1="55" x2="22" y2="28" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="55" x2="68" y2="68" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="82" x2="30" y2="118" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="82" x2="60" y2="118" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(28,28,28,0.12) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene7_Rulemakers;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene8_IndiaInvestmentThesis',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8_IndiaInvestmentThesis: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Three pillars animate in
  const p1sc = sp(10);
  const p2sc = sp(25);
  const p3sc = sp(40);

  // Triangle formation animation
  const triOp = interpolate(frame, [50, 70], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // NIFTY line blink
  const blinkOp = Math.sin(frame * 0.3) > 0 ? 1 : 0.4;

  // Background zoom
  const bgScale = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#F5F5F5',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Background */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(130deg, #ffffff 0%, #f8fffe 50%, #f5f8ff 100%)',
          transform: `scale(${bgScale})`,
        }}
      />

      {/* Large diamond shape */}
      <div
        style={{
          position: 'absolute',
          right: -80,
          top: '50%',
          transform: `translateY(-50%) rotate(${45 + Math.sin(frame * 0.015) * 2}deg) scale(${1 + Math.sin(frame * 0.025) * 0.015})`,
          width: 500,
          height: 500,
          background: 'rgba(28,28,28,0.03)',
          borderRadius: 20,
        }}
      />

      {/* Wave texture */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 18 }).map((_, i) => (
            <path
              key={i}
              d={`M 0 ${100 + i * 60} Q 480 ${80 + i * 60} 960 ${100 + i * 60} Q 1440 ${120 + i * 60} 1920 ${100 + i * 60}`}
              fill="none"
              stroke="#1C1C1C"
              strokeWidth={1}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const px = seededRandom(i * 5) * 1920;
        const py = seededRandom(i * 11) * 1080;
        const sz = 3 + seededRandom(i * 7) * 6;
        const drift = Math.cos(frame * 0.022 + i * 0.8) * 11;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py + drift,
              width: sz,
              height: sz,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#e9d700' : i % 3 === 1 ? '#1C1C1C' : '#FFC107',
              opacity: 0.06 + seededRandom(i * 9) * 0.09,
            }}
          />
        );
      })}

      {/* TOP: Market tickers */}
      <div
        style={{
          position: 'absolute',
          top: 50,
          left: 100,
          display: 'flex',
          gap: 20,
          opacity: sp(0),
          transform: `translateY(${interpolate(sp(0), [0, 1], [-30, 0])}px)`,
        }}
      >
        {[
          { name: 'NIFTY 50', val: '22,456', up: true },
          { name: 'BSE SENSEX', val: '73,882', up: true },
          { name: 'Sectoral Funds', val: '↑ Focus', up: true },
        ].map((t, i) => (
          <div
            key={i}
            style={{
              background: '#1C1C1C',
              borderRadius: 12,
              padding: '12px 22px',
              display: 'flex',
              alignItems: 'center',
              gap: 12,
              opacity: i === 0 ? blinkOp : 1,
            }}
          >
            <div>
              <div
                style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 18,
                  fontWeight: 600,
                  color: '#9E9E9E',
                }}
              >
                {t.name}
              </div>
              <div
                style={{
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 24,
                  fontWeight: 900,
                  color: '#e9d700',
                }}
              >
                {t.val}
              </div>
            </div>
            <svg width="20" height="20" viewBox="0 0 20 20">
              <path d="M4 14 L10 6 L16 14" fill="#e9d700" />
            </svg>
          </div>
        ))}
      </div>

      {/* Main content — 3 pillars */}
      <div
        style={{
          position: 'absolute',
          left: 80,
          top: 180,
          width: 900,
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 58,
            fontWeight: 900,
            color: '#1C1C1C',
            lineHeight: 1.2,
            marginBottom: 32,
            opacity: sp(5),
          }}
        >
          भारत की{' '}
          <span style={{ color: '#e9d700', WebkitTextStroke: '1px #1C1C1C' }}>
            3 driving forces
          </span>{' '}
          जो
          <br />
          इस जाल से बाहर ले जाएंगी
        </div>

        {/* Three pillar cards */}
        {[
          {
            number: '01',
            title: 'Domestic Consumption',
            hindi: 'भारत की internal demand engine',
            icon: '🛒',
            op: p1sc,
            color: '#e9d700',
          },
          {
            number: '02',
            title: 'IT Sector',
            hindi: 'Silicon Valley का भारतीय दिमाग',
            icon: '💻',
            op: p2sc,
            color: '#FFC107',
          },
          {
            number: '03',
            title: 'Manufacturing Push',
            hindi: 'Make in India — for the world',
            icon: '🏭',
            op: p3sc,
            color: '#1C1C1C',
          },
        ].map((p, i) => (
          <div
            key={i}
            style={{
              display: 'flex',
              alignItems: 'center',
              gap: 24,
              background: i === 2 ? '#1C1C1C' : '#ffffff',
              border: `3px solid ${p.color}`,
              borderRadius: 20,
              padding: '18px 28px',
              marginBottom: 16,
              opacity: p.op,
              transform: `translateX(${interpolate(p.op, [0, 1], [-100, 0])}px)`,
              boxShadow: i === 2 ? '0 6px 28px rgba(0,0,0,0.14)' : '0 3px 18px rgba(0,0,0,0.06)',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono, monospace',
                fontSize: 44,
                fontWeight: 900,
                color: p.color,
                minWidth: 60,
              }}
            >
              {p.number}
            </div>
            <div style={{ fontSize: 36, flexShrink: 0 }}>{p.icon}</div>
            <div>
              <div
                style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 32,
                  fontWeight: 800,
                  color: i === 2 ? '#F5F5F5' : '#1C1C1C',
                }}
              >
                {p.title}
              </div>
              <div
                style={{
                  fontFamily: 'Noto Sans Devanagari, sans-serif',
                  fontSize: 24,
                  fontWeight: 500,
                  color: i === 2 ? '#9E9E9E' : '#2D2D2D',
                }}
              >
                {p.hindi}
              </div>
            </div>
          </div>
        ))}
      </div>

      {/* RIGHT: Triangle convergence */}
      <div
        style={{
          position: 'absolute',
          right: 60,
          top: '50%',
          transform: 'translateY(-40%)',
          opacity: triOp,
        }}
      >
        <svg width="520" height="480" viewBox="0 0 520 480">
          {/* Triangle */}
          <path
            d="M260 40 L460 400 L60 400 Z"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="3"
            strokeDasharray="12 8"
          />
          {/* Three nodes */}
          <circle cx="260" cy="40" r="42" fill="#e9d700" />
          <text x="260" y="48" textAnchor="middle" fontFamily="Inter" fontSize="16" fontWeight="800" fill="#1C1C1C">
            Consumption
          </text>

          <circle cx="460" cy="400" r="42" fill="#1C1C1C" />
          <text x="460" y="396" textAnchor="middle" fontFamily="Inter" fontSize="14" fontWeight="800" fill="#e9d700">
            Manufacturing
          </text>
          <text x="460" y="414" textAnchor="middle" fontFamily="Inter" fontSize="14" fontWeight="800" fill="#e9d700">
            Push
          </text>

          <circle cx="60" cy="400" r="42" fill="#FFC107" />
          <text x="60" y="404" textAnchor="middle" fontFamily="Inter" fontSize="16" fontWeight="800" fill="#1C1C1C">
            IT
          </text>

          {/* Center result */}
          <circle cx="260" cy="280" r="70" fill="#1C1C1C" />
          <text x="260" y="272" textAnchor="middle" fontFamily="Noto Sans Devanagari, sans-serif" fontSize="18" fontWeight="700" fill="#e9d700">
            जाल से
          </text>
          <text x="260" y="296" textAnchor="middle" fontFamily="Noto Sans Devanagari, sans-serif" fontSize="18" fontWeight="700" fill="#e9d700">
            बाहर
          </text>
          <text x="260" y="318" textAnchor="middle" fontFamily="Inter" fontSize="14" fontWeight="600" fill="#9E9E9E">
            Breaking free
          </text>
        </svg>
      </div>

      {/* Stick figure — thinking pose */}
      <div
        style={{
          position: 'absolute',
          bottom: 55,
          right: 80,
          opacity: sp(55),
          transform: `translateY(${interpolate(sp(55), [0, 1], [40, 0])}px)`,
        }}
      >
        <svg width="90" height="130" viewBox="0 0 90 130">
          <circle cx="45" cy="26" r="26" fill="rgba(255,193,7,0.16)" />
          <circle
            cx="45"
            cy={26 + Math.sin(frame * 0.04) * 1.5}
            r="17"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="3.5"
          />
          <circle cx="39" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <circle cx="51" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <line x1="44.5" y1="24" x2="45.5" y2="24" stroke="#FFC107" strokeWidth="2" />
          <path d="M39 33 Q45 37 51 33" fill="none" stroke="#1C1C1C" strokeWidth="2" strokeLinecap="round" />
          <line x1="45" y1="43" x2="45" y2={82 + Math.sin(frame * 0.05) * 2} stroke="#1C1C1C" strokeWidth="3.5" strokeLinecap="round" />
          {/* Thinking arm to chin */}
          <line x1="45" y1="55" x2="32" y2="40" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="55" x2="68" y2="64" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="82" x2="30" y2="118" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="82" x2="60" y2="118" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 52%, rgba(28,28,28,0.12) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene8_IndiaInvestmentThesis;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene9_IndiaSchemes',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9_IndiaSchemes: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Schemes slide in from right
  const s1 = sp(8);
  const s2 = sp(22);
  const s3 = sp(36);

  // Progress bars
  const pli = interpolate(sp(15), [0, 1], [0, 75]);
  const semi = interpolate(sp(28), [0, 1], [0, 55]);
  const def = interpolate(sp(41), [0, 1], [0, 68]);

  // Connecting line to history
  const lineW = interpolate(frame, [55, 85], [0, 500], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Background shift
  const bgShift = interpolate(frame, [0, durationInFrames], [0, -25]);

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#F5F5F5',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Background */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(to bottom right, #f0f8ff 0%, #ffffff 50%, #f8fff0 100%)',
          transform: `translateX(${bgShift}px)`,
          width: '110%',
        }}
      />

      {/* Large ring shape */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) rotate(${frame * 0.06}deg)`,
          width: 900,
          height: 900,
          borderRadius: '50%',
          border: '2px solid rgba(28,28,28,0.04)',
          pointerEvents: 'none',
        }}
      />
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) rotate(${-frame * 0.04}deg)`,
          width: 700,
          height: 700,
          borderRadius: '50%',
          border: '2px solid rgba(233,215,0,0.05)',
          pointerEvents: 'none',
        }}
      />

      {/* Hexagon grid texture */}
      <AbsoluteFill style={{ opacity: 0.03 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 8 }).map((_, r) =>
            Array.from({ length: 12 }).map((_, c) => {
              const cx = c * 170 + (r % 2) * 85;
              const cy = r * 150;
              return (
                <polygon
                  key={`${r}-${c}`}
                  points={`${cx},${cy + 50} ${cx + 44},${cy + 25} ${cx + 44},${cy - 25} ${cx},${cy - 50} ${cx - 44},${cy - 25} ${cx - 44},${cy + 25}`}
                  fill="none"
                  stroke="#1C1C1C"
                  strokeWidth={1}
                />
              );
            })
          )}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const px = seededRandom(i * 6) * 1920;
        const py = seededRandom(i * 10) * 1080;
        const sz = 3 + seededRandom(i * 4) * 7;
        const drift = Math.sin(frame * 0.02 + i * 0.7) * 13;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py + drift,
              width: sz,
              height: sz,
              borderRadius: '50%',
              backgroundColor: i % 2 === 0 ? '#e9d700' : '#1C1C1C',
              opacity: 0.07 + seededRandom(i * 8) * 0.09,
            }}
          />
        );
      })}

      {/* LEFT: Scheme cards with progress bars */}
      <div
        style={{
          position: 'absolute',
          left: 80,
          top: '50%',
          transform: 'translateY(-50%)',
          width: 860,
        }}
      >
        {/* Header */}
        <div
          style={{
            opacity: sp(0),
            marginBottom: 28,
          }}
        >
          <div
            style={{
              display: 'inline-flex',
              gap: 12,
              alignItems: 'center',
              background: '#1C1C1C',
              borderRadius: 12,
              padding: '12px 26px',
              marginBottom: 16,
            }}
          >
            <span style={{ fontSize: 32 }}>🇮🇳</span>
            <span
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 26,
                fontWeight: 900,
                color: '#e9d700',
                letterSpacing: 2,
              }}
            >
              INDIA'S STRATEGIC PLAYBOOK
            </span>
          </div>
          <div
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 48,
              fontWeight: 900,
              color: '#1C1C1C',
              lineHeight: 1.2,
            }}
          >
            Korea + China की नीति का
            <br />
            <span style={{ color: '#e9d700' }}>भारतीय version</span>
          </div>
        </div>

        {/* Three schemes */}
        {[
          {
            name: 'PLI Scheme',
            full: 'Production Linked Incentive',
            desc: 'Manufacturing को global-competitive बनाना',
            progress: pli,
            op: s1,
            color: '#e9d700',
            icon: '🏭',
          },
          {
            name: 'Semiconductor Mission',
            full: '₹76,000 Cr Investment',
            desc: 'Chip dependency खत्म करना',
            progress: semi,
            op: s2,
            color: '#FFC107',
            icon: '🔬',
          },
          {
            name: 'Defence Indigenization',
            full: '75% indigenization target',
            desc: 'खुद की defence industry खड़ी करना',
            progress: def,
            op: s3,
            color: '#1C1C1C',
            icon: '🛡️',
          },
        ].map((s, i) => (
          <div
            key={i}
            style={{
              background: i === 2 ? '#1C1C1C' : '#ffffff',
              border: `3px solid ${s.color}`,
              borderRadius: 20,
              padding: '20px 26px',
              marginBottom: 16,
              opacity: s.op,
              transform: `translateX(${interpolate(s.op, [0, 1], [120, 0])}px)`,
              boxShadow: i === 2 ? '0 6px 28px rgba(0,0,0,0.14)' : '0 3px 18px rgba(0,0,0,0.06)',
            }}
          >
            <div
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 16,
                marginBottom: 12,
              }}
            >
              <span style={{ fontSize: 34 }}>{s.icon}</span>
              <div>
                <div
                  style={{
                    fontFamily: 'Inter, sans-serif',
                    fontSize: 30,
                    fontWeight: 900,
                    color: i === 2 ? s.color : '#1C1C1C',
                  }}
                >
                  {s.name}
                </div>
                <div
                  style={{
                    fontFamily: 'Inter, sans-serif',
                    fontSize: 20,
                    fontWeight: 600,
                    color: '#9E9E9E',
                  }}
                >
                  {s.full}
                </div>
              </div>
            </div>
            {/* Progress bar */}
            <div
              style={{
                height: 10,
                background: i === 2 ? '#2D2D2D' : '#f0f0f0',
                borderRadius: 5,
                marginBottom: 8,
              }}
            >
              <div
                style={{
                  height: '100%',
                  width: `${s.progress}%`,
                  background: s.color,
                  borderRadius: 5,
                  transition: 'width 0.1s ease',
                }}
              />
            </div>
            <div
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 22,
                fontWeight: 500,
                color: i === 2 ? '#9E9E9E' : '#2D2D2D',
              }}
            >
              {s.desc}
            </div>
          </div>
        ))}
      </div>

      {/* RIGHT: Historical parallel */}
      <div
        style={{
          position: 'absolute',
          right: 60,
          top: '50%',
          transform: 'translateY(-50%)',
          width: 560,
          opacity: interpolate(frame, [50, 70], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
        }}
      >
        <div
          style={{
            fontFamily: 'Noto Sans Devanagari, sans-serif',
            fontSize: 34,
            fontWeight: 800,
            color: '#9E9E9E',
            textAlign: 'center',
            marginBottom: 24,
          }}
        >
          Historical Parallel
        </div>

        {/* Comparison */}
        {[
          { flag: '🇰🇷', country: 'South Korea', approach: 'Chaebols + State funding', era: '1970s-90s' },
          { flag: '🇨🇳', country: 'China', approach: 'State-led industrial policy', era: '1980s-2000s' },
          { flag: '🇮🇳', country: 'India', approach: 'PLI + Missions + Indigenization', era: '2020s →', highlight: true },
        ].map((c, i) => (
          <div
            key={i}
            style={{
              background: c.highlight ? '#1C1C1C' : '#ffffff',
              border: c.highlight ? '3px solid #e9d700' : '2px solid #e0e0e0',
              borderRadius: 16,
              padding: '16px 22px',
              marginBottom: 14,
              transform: `translateX(${interpolate(sp(55 + i * 8), [0, 1], [80, 0])}px)`,
              opacity: sp(55 + i * 8),
            }}
          >
            <div
              style={{
                display: 'flex',
                justifyContent: 'space-between',
                alignItems: 'center',
              }}
            >
              <div
                style={{
                  display: 'flex',
                  alignItems: 'center',
                  gap: 12,
                }}
              >
                <span style={{ fontSize: 30 }}>{c.flag}</span>
                <div>
                  <div
                    style={{
                      fontFamily: 'Inter, sans-serif',
                      fontSize: 24,
                      fontWeight: 800,
                      color: c.highlight ? '#e9d700' : '#1C1C1C',
                    }}
                  >
                    {c.country}
                  </div>
                  <div
                    style={{
                      fontFamily: 'Inter, sans-serif',
                      fontSize: 18,
                      fontWeight: 500,
                      color: '#9E9E9E',
                    }}
                  >
                    {c.approach}
                  </div>
                </div>
              </div>
              <div
                style={{
                  background: c.highlight ? '#e9d700' : '#f0f0f0',
                  borderRadius: 8,
                  padding: '6px 14px',
                  fontFamily: 'JetBrains Mono, monospace',
                  fontSize: 18,
                  fontWeight: 700,
                  color: c.highlight ? '#1C1C1C' : '#9E9E9E',
                }}
              >
                {c.era}
              </div>
            </div>
          </div>
        ))}

        {/* Connection line */}
        <div
          style={{
            marginTop: 20,
            display: 'flex',
            alignItems: 'center',
            gap: 12,
            opacity: sp(80),
          }}
        >
          <div
            style={{
              height: 4,
              width: lineW * 0.4,
              background: 'linear-gradient(to right, #e9d700, #1C1C1C)',
              borderRadius: 2,
            }}
          />
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 24,
              fontWeight: 700,
              color: '#1C1C1C',
            }}
          >
            Same strategy, new era
          </span>
        </div>
      </div>

      {/* Stick figure — building pose */}
      <div
        style={{
          position: 'absolute',
          bottom: 55,
          left: 80,
          opacity: sp(50),
          transform: `translateY(${interpolate(sp(50), [0, 1], [40, 0])}px)`,
        }}
      >
        <svg width="90" height="130" viewBox="0 0 90 130">
          <circle cx="45" cy="26" r="26" fill="rgba(233,215,0,0.16)" />
          <circle cx="45" cy="26" r="17" fill="none" stroke="#1C1C1C" strokeWidth="3.5" />
          <circle cx="39" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <circle cx="51" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <line x1="44.5" y1="24" x2="45.5" y2="24" stroke="#FFC107" strokeWidth="2" />
          <path d="M39 34 Q45 40 51 34" fill="none" stroke="#1C1C1C" strokeWidth="2.5" strokeLinecap="round" />
          <line x1="45" y1="43" x2="45" y2={82 + Math.sin(frame * 0.05) * 2} stroke="#1C1C1C" strokeWidth="3.5" strokeLinecap="round" />
          <line x1="45" y1="55" x2="18" y2="45" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="55" x2="68" y2="38" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="82" x2="30" y2="118" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="82" x2="60" y2="118" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 52%, rgba(28,28,28,0.12) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene9_IndiaSchemes;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'Scene10_DirectionVsGuarantee',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_DirectionVsGuarantee: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: { damping: 12, mass: 0.4, stiffness: 120, ...config },
    });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  // Hero contrast words
  const dirOp = sp(0);
  const vsOp = sp(18);
  const grtOp = sp(30);

  // Bottom CTA
  const ctaSc = sp(55);

  // Arrow animation
  const arrowProgress = interpolate(frame, [40, 80], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  // Compass rotation
  const compassAngle = interpolate(frame, [0, durationInFrames], [0, 15]);

  // Slow zoom background
  const bgScale = interpolate(frame, [0, durationInFrames], [1.02, 1]);

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#F5F5F5',
        overflow: 'hidden',
        opacity: exitOp,
      }}
    >
      {/* Background */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(145deg, #ffffff 0%, #f5f5f5 60%, #fafaf0 100%)',
          transform: `scale(${bgScale})`,
        }}
      />

      {/* Large geometric — rotated rectangle */}
      <div
        style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          transform: `translate(-50%, -50%) rotate(${12 + Math.sin(frame * 0.01) * 1.5}deg)`,
          width: 1100,
          height: 600,
          borderRadius: 40,
          background: 'rgba(28,28,28,0.025)',
          pointerEvents: 'none',
        }}
      />

      {/* Radial line texture */}
      <AbsoluteFill style={{ opacity: 0.04 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 24 }).map((_, i) => {
            const angle = (i * 15 * Math.PI) / 180;
            return (
              <line
                key={i}
                x1={960}
                y1={540}
                x2={960 + Math.cos(angle) * 1000}
                y2={540 + Math.sin(angle) * 800}
                stroke="#1C1C1C"
                strokeWidth={1}
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const px = seededRandom(i * 4) * 1920;
        const py = seededRandom(i * 12) * 1080;
        const sz = 3 + seededRandom(i * 8) * 6;
        const drift = Math.cos(frame * 0.02 + i * 0.9) * 12;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: px,
              top: py + drift,
              width: sz,
              height: sz,
              borderRadius: '50%',
              backgroundColor: i % 3 === 0 ? '#e9d700' : i % 3 === 1 ? '#1C1C1C' : '#ff0000',
              opacity: 0.05 + seededRandom(i * 6) * 0.09,
            }}
          />
        );
      })}

      {/* MAIN — Center dominant layout */}
      <div
        style={{
          position: 'absolute',
          top: 0,
          left: 0,
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 0,
        }}
      >
        {/* Row 1: DIRECTION + VS + GUARANTEE */}
        <div
          style={{
            display: 'flex',
            alignItems: 'center',
            gap: 32,
            marginBottom: 20,
          }}
        >
          {/* DIRECTION — Green */}
          <div
            style={{
              opacity: dirOp,
              transform: `translateX(${interpolate(dirOp, [0, 1], [-200, 0])}px)`,
              background: '#1C1C1C',
              borderRadius: 24,
              padding: '24px 44px',
            }}
          >
            <span
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 100,
                fontWeight: 900,
                color: '#e9d700',
                letterSpacing: -2,
                display: 'block',
                lineHeight: 1,
              }}
            >
              Direction
            </span>
            <span
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 40,
                fontWeight: 700,
                color: '#F5F5F5',
                display: 'block',
                marginTop: 8,
              }}
            >
              ✓ सही है
            </span>
          </div>

          {/* VS */}
          <div
            style={{
              opacity: vsOp,
              transform: `scale(${0.5 + vsOp * 0.5})`,
              textAlign: 'center',
            }}
          >
            <div
              style={{
                width: 70,
                height: 70,
                borderRadius: '50%',
                background: '#ff0000',
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
              }}
            >
              <span
                style={{
                  fontFamily: 'Inter, sans-serif',
                  fontSize: 28,
                  fontWeight: 900,
                  color: '#F5F5F5',
                }}
              >
                VS
              </span>
            </div>
          </div>

          {/* GUARANTEE — Struck through */}
          <div
            style={{
              opacity: grtOp,
              transform: `translateX(${interpolate(grtOp, [0, 1], [200, 0])}px)`,
              background: '#f8f8f8',
              border: '3px dashed #e0e0e0',
              borderRadius: 24,
              padding: '24px 44px',
              position: 'relative',
            }}
          >
            <span
              style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 100,
                fontWeight: 900,
                color: '#9E9E9E',
                letterSpacing: -2,
                display: 'block',
                lineHeight: 1,
              }}
            >
              Guarantee
            </span>
            <span
              style={{
                fontFamily: 'Noto Sans Devanagari, sans-serif',
                fontSize: 40,
                fontWeight: 700,
                color: '#ff0000',
                display: 'block',
                marginTop: 8,
              }}
            >
              ✗ नहीं है
            </span>
            {/* Big X */}
            <div
              style={{
                position: 'absolute',
                top: 0,
                left: 0,
                right: 0,
                bottom: 0,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                pointerEvents: 'none',
              }}
            >
              <svg width="100%" height="100%" viewBox="0 0 400 180">
                <line
                  x1="20"
                  y1="20"
                  x2="380"
                  y2="160"
                  stroke="#ff0000"
                  strokeWidth="5"
                  strokeLinecap="round"
                  opacity="0.3"
                />
                <line
                  x1="380"
                  y1="20"
                  x2="20"
                  y2="160"
                  stroke="#ff0000"
                  strokeWidth="5"
                  strokeLinecap="round"
                  opacity="0.3"
                />
              </svg>
            </div>
          </div>
        </div>

        {/* Divider */}
        <div
          style={{
            width: interpolate(frame, [38, 60], [0, 900], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            height: 3,
            background: 'linear-gradient(to right, transparent, #1C1C1C, transparent)',
            marginBottom: 28,
            borderRadius: 2,
          }}
        />

        {/* Bottom message */}
        <div
          style={{
            textAlign: 'center',
            opacity: interpolate(frame, [48, 68], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
            transform: `translateY(${interpolate(
              frame,
              [48, 68],
              [30, 0],
              { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }
            )}px)`,
          }}
        >
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 56,
              fontWeight: 800,
              color: '#1C1C1C',
              lineHeight: 1.3,
              display: 'block',
            }}
          >
            Direction समझ कर portfolio के{' '}
            <span
              style={{
                background: '#e9d700',
                padding: '2px 14px',
                borderRadius: 8,
              }}
            >
              फैसले लो
            </span>
          </span>
        </div>

        {/* Hot tip warning */}
        <div
          style={{
            marginTop: 20,
            display: 'flex',
            alignItems: 'center',
            gap: 16,
            background: '#fff0f0',
            border: '2px solid #ff0000',
            borderRadius: 16,
            padding: '14px 32px',
            opacity: sp(65),
            transform: `scale(${0.6 + sp(65) * 0.4})`,
          }}
        >
          <svg width="32" height="32" viewBox="0 0 32 32">
            <circle cx="16" cy="16" r="14" fill="#ff0000" opacity="0.15" />
            <path
              d="M16 8 L16 18 M16 22 L16 24"
              stroke="#ff0000"
              strokeWidth="3"
              strokeLinecap="round"
            />
          </svg>
          <span
            style={{
              fontFamily: 'Noto Sans Devanagari, sans-serif',
              fontSize: 32,
              fontWeight: 700,
              color: '#ff0000',
            }}
          >
            Hot Tips पर नहीं — समझ पर invest करो
          </span>
        </div>
      </div>

      {/* Compass — bottom right */}
      <div
        style={{
          position: 'absolute',
          bottom: 110,
          right: 160,
          opacity: sp(45),
        }}
      >
        <svg
          width="100"
          height="100"
          viewBox="0 0 100 100"
          style={{ transform: `rotate(${compassAngle}deg)` }}
        >
          <circle cx="50" cy="50" r="44" fill="none" stroke="#e0e0e0" strokeWidth="2" />
          <path d="M50 18 L56 50 L50 44 L44 50 Z" fill="#e9d700" />
          <path d="M50 82 L44 50 L50 56 L56 50 Z" fill="#9E9E9E" />
          <circle cx="50" cy="50" r="6" fill="#1C1C1C" />
          <text x="50" y="14" textAnchor="middle" fontFamily="Inter" fontSize="11" fontWeight="800" fill="#1C1C1C">N</text>
        </svg>
      </div>

      {/* Stick figure — confident investor pose */}
      <div
        style={{
          position: 'absolute',
          bottom: 55,
          left: 80,
          opacity: sp(60),
          transform: `translateY(${interpolate(sp(60), [0, 1], [40, 0])}px)`,
        }}
      >
        <svg width="90" height="130" viewBox="0 0 90 130">
          <circle cx="45" cy="26" r="26" fill="rgba(233,215,0,0.2)" />
          <circle
            cx="45"
            cy={26 + Math.sin(frame * 0.04) * 1.5}
            r="17"
            fill="none"
            stroke="#1C1C1C"
            strokeWidth="3.5"
          />
          <circle cx="39" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <circle cx="51" cy="24" r="5.5" fill="none" stroke="#FFC107" strokeWidth="2.5" />
          <line x1="44.5" y1="24" x2="45.5" y2="24" stroke="#FFC107" strokeWidth="2" />
          {/* Confident smile */}
          <path d="M38 34 Q45 42 52 34" fill="none" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          {/* Body */}
          <line
            x1="45"
            y1="43"
            x2="45"
            y2={82 + Math.sin(frame * 0.05) * 2}
            stroke="#1C1C1C"
            strokeWidth="3.5"
            strokeLinecap="round"
          />
          {/* Arms — one up one out */}
          <line x1="45" y1="55" x2="20" y2="36" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="55" x2="70" y2="55" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="82" x2="30" y2="118" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
          <line x1="45" y1="82" x2="60" y2="118" stroke="#1C1C1C" strokeWidth="3" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(28,28,28,0.13) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene10_DirectionVsGuarantee;
```

