```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31ResidenceSecondaire',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const fmt = (n: number): string => n.toFixed(0).replace(/\B(?=(\d{3})+(?!\d))/g, ' ');

const COSTS = [
  { label: 'Taxe foncière', value: 2200 },
  { label: 'Charges & copropriété', value: 1800 },
  { label: 'Assurance multirisque', value: 900 },
  { label: 'Chauffage hors gel', value: 1400 },
  { label: 'Jardinier', value: 1600 },
  { label: 'Déplacements', value: 2100 },
  { label: 'Réparations & meubles', value: 2000 },
];

const Scene31ResidenceSecondaire: React.FC = () => {
  // Use the current frame to drive every animation of this scene
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  // Multi-beat window helper (in -> hold -> out)
  const seg = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Slow camera push used across the whole scene
  const camZoom = interpolate(frame, [0, durationInFrames], [1.06, 1.14], { extrapolateRight: 'clamp' });
  const camPan = interpolate(frame, [0, durationInFrames], [30, -30], { extrapolateRight: 'clamp' });

  const total = Math.round(
    interpolate(frame, [330, 470], [0, 12000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) })
  );
  const perNight = Math.round(
    interpolate(frame, [620, 720], [0, 343], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) })
  );

  // Impact shake when the per-night number lands
  const shake = frame > 620 && frame < 660 ? Math.sin(frame * 1.4) * (660 - frame) * 0.18 : 0;

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 22% 25%, #6B57C4 0%, rgba(107,87,196,0) 55%), radial-gradient(circle at 82% 78%, #3A2E73 0%, rgba(58,46,115,0) 60%), linear-gradient(160deg, #504099 0%, #40317E 100%)',
        }}
      />

      {/* Animated shape layers - large transparent geometry drifting */}
      <AbsoluteFill>
        {[0, 1, 2, 3].map((i) => {
          const s = seededRandom(i + 7);
          const drift = Math.sin(frame * 0.006 + i) * 60;
          return (
            <div
              key={`shape-${i}`}
              style={{
                position: 'absolute',
                left: 120 + s * 1500,
                top: 60 + seededRandom(i + 31) * 800,
                width: 420 + i * 120,
                height: 420 + i * 120,
                borderRadius: i % 2 === 0 ? '50%' : 40,
                border: '3px solid #F5F5F5',
                opacity: 0.06,
                transform: `translate(${drift}px, ${-drift * 0.5}px) rotate(${frame * 0.05 + i * 30}deg) scale(${1 + Math.sin(frame * 0.01 + i) * 0.06})`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Motion texture - punk halftone dot field */}
      <AbsoluteFill style={{ opacity: 0.12 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 260 }).map((_, i) => {
            const x = (i % 26) * 76 + 20;
            const y = Math.floor(i / 26) * 108 + 20;
            const r = 3 + Math.sin(frame * 0.04 + i) * 2;
            return <circle key={`dot-${i}`} cx={x} cy={y} r={Math.max(0.5, r)} fill="#F5F5F5" />;
          })}
        </svg>
      </AbsoluteFill>

      {/* Punk collage torn paper strips */}
      <div
        style={{
          position: 'absolute',
          left: -90,
          top: 150,
          width: 520,
          height: 120,
          background: '#000000',
          opacity: 0.35,
          transform: `rotate(${-8 + Math.sin(frame * 0.01) * 1.5}deg)`,
        }}
      />
      <div
        style={{
          position: 'absolute',
          right: -120,
          bottom: 190,
          width: 620,
          height: 150,
          background: '#FFC107',
          opacity: 0.16,
          transform: `rotate(${6 + Math.cos(frame * 0.012) * 1.5}deg)`,
        }}
      />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 34 }).map((_, i) => {
          const s = seededRandom(i + 3);
          const y = (seededRandom(i + 11) * 1080 - frame * (0.5 + s) + 2000) % 1180;
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: s * 1900,
                top: y - 60,
                width: 5 + s * 5,
                height: 5 + s * 5,
                background: i % 4 === 0 ? '#FFC107' : '#F5F5F5',
                opacity: 0.35,
                borderRadius: i % 3 === 0 ? 0 : '50%',
                transform: `rotate(${frame * (0.6 + s)}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content with camera movement */}
      <AbsoluteFill style={{ transform: `scale(${camZoom}) translateX(${camPan + shake}px)` }}>
        {/* BEAT 1 - Title + house icon */}
        <div style={{ position: 'absolute', left: 130, top: 120, opacity: seg(0, 25, 560, 600) }}>
          <div
            style={{
              display: 'inline-block',
              background: '#000000',
              padding: '10px 26px',
              transform: `translateY(${(1 - sp(0)) * 60}px) rotate(-2deg)`,
            }}
          >
            <span style={{ fontFamily: 'Space Mono', fontSize: 30, color: '#FFC107', letterSpacing: 4 }}>LE RÊVE FACTURÉ</span>
          </div>
          <div
            style={{
              fontFamily: 'Anton',
              fontSize: 100,
              lineHeight: 1,
              color: '#F5F5F5',
              marginTop: 22,
              maxWidth: 900,
              transform: `translateY(${(1 - sp(12)) * 80}px)`,
            }}
          >
            RÉSIDENCE<br />SECONDAIRE
          </div>

          <svg width="330" height="260" style={{ marginTop: 30, transform: `scale(${sp(30)})`, transformOrigin: 'left top' }}>
            <path d="M20 120 L165 20 L310 120" stroke="#F5F5F5" strokeWidth="10" fill="none" strokeLinecap="round" />
            <rect x="55" y="120" width="220" height="120" stroke="#F5F5F5" strokeWidth="10" fill="none" />
            <rect x="140" y="170" width="55" height="70" fill="#FFC107" opacity={0.9} />
            <rect x="80" y="145" width="42" height="34" stroke="#FFC107" strokeWidth="6" fill="none" />
            <rect x="215" y="145" width="42" height="34" stroke="#FFC107" strokeWidth="6" fill="none" />
          </svg>
        </div>

        {/* BEAT 2 - Receipt strips stacking */}
        <div style={{ position: 'absolute', right: 150, top: 150, width: 700, opacity: seg(80, 110, 560, 600) }}>
          {COSTS.map((c, i) => {
            const d = 90 + i * 26;
            const s = sp(d);
            return (
              <div
                key={c.label}
                style={{
                  display: 'flex',
                  justifyContent: 'space-between',
                  alignItems: 'center',
                  background: '#F5F5F5',
                  marginBottom: 14,
                  padding: '16px 24px',
                  transform: `translateX(${(1 - s) * 420}px) rotate(${i % 2 === 0 ? -0.8 : 0.8}deg)`,
                  opacity: s,
                }}
              >
                <span style={{ fontFamily: 'Montserrat', fontWeight: 700, fontSize: 34, color: '#000000' }}>{c.label}</span>
                <span style={{ fontFamily: 'Space Mono', fontSize: 34, color: '#504099' }}>{fmt(c.value)} €</span>
              </div>
            );
          })}
        </div>

        {/* BEAT 3 - Annual total counter */}
        <div style={{ position: 'absolute', right: 150, bottom: 150, textAlign: 'right', opacity: seg(320, 350, 560, 600) }}>
          <div style={{ fontFamily: 'Montserrat', fontWeight: 600, fontSize: 30, color: '#F5F5F5', letterSpacing: 6 }}>COÛT RÉEL ANNUEL</div>
          <div style={{ fontFamily: 'Anton', fontSize: 170, lineHeight: 1, color: '#FFC107' }}>{fmt(total)} €</div>
        </div>

        {/* BEAT 4 - The division payoff */}
        <AbsoluteFill style={{ opacity: seg(600, 640, 850, 880), justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ background: '#000000', padding: '18px 40px', transform: 'rotate(-1.5deg)' }}>
            <span style={{ fontFamily: 'Space Mono', fontSize: 44, color: '#F5F5F5' }}>12 000 € ÷ 35 NUITS</span>
          </div>
          <div
            style={{
              fontFamily: 'Anton',
              fontSize: 210,
              lineHeight: 1,
              color: '#F5F5F5',
              marginTop: 24,
              transform: `scale(${sp(620, { damping: 10 })})`,
            }}
          >
            {fmt(perNight)} € <span style={{ fontSize: 90, color: '#FFC107' }}>LA NUIT</span>
          </div>
          <div style={{ fontFamily: 'Bricolage Grotesque', fontSize: 46, color: '#F5F5F5', marginTop: 18, opacity: sp(700) }}>
            Le prix affiché n'était que la surface.
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill
        style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 45%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene31ResidenceSecondaire;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32EcosystemeDepenses',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const fmt = (n: number): string => n.toFixed(0).replace(/\B(?=(\d{3})+(?!\d))/g, ' ');

const SATELLITES = [
  { label: 'Assurance', angle: -90 },
  { label: 'Entretien', angle: -38 },
  { label: 'Carburant', angle: 14 },
  { label: 'Stationnement', angle: 66 },
  { label: 'Accessoires', angle: 130 },
  { label: 'Révisions', angle: 190 },
];

const Scene32EcosystemeDepenses: React.FC = () => {
  // Use the current frame to drive every animation of this scene
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const seg = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Orbit camera - slow rotation + tilt
  const camRot = interpolate(frame, [0, durationInFrames], [-2.5, 2.5], { extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, 300, durationInFrames], [1.18, 1.02, 1.1], { extrapolateRight: 'clamp' });

  const counter = Math.round(
    interpolate(frame, [300, 520], [5000, 12000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad) })
  );

  const water = interpolate(frame, [560, 700], [1080, 470], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 50% 45%, #6E5AD0 0%, rgba(110,90,208,0) 60%), linear-gradient(200deg, #4A3A8F 0%, #504099 45%, #2E245C 100%)',
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        {[0, 1, 2].map((i) => (
          <div
            key={`sh-${i}`}
            style={{
              position: 'absolute',
              left: 200 + i * 620,
              top: 120 + Math.sin(frame * 0.008 + i) * 70,
              width: 520,
              height: 520,
              background: '#F5F5F5',
              opacity: 0.04,
              borderRadius: i === 1 ? 0 : '50%',
              transform: `rotate(${frame * 0.08 + i * 45}deg) scale(${1 + Math.cos(frame * 0.012 + i) * 0.08})`,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Motion texture - concentric rings */}
      <AbsoluteFill style={{ opacity: 0.14, justifyContent: 'center', alignItems: 'center' }}>
        <svg width="1400" height="1080" viewBox="0 0 1400 1080">
          {[0, 1, 2, 3, 4].map((i) => (
            <circle
              key={`ring-${i}`}
              cx="700"
              cy="540"
              r={160 + i * 95 + Math.sin(frame * 0.03 + i) * 8}
              stroke="#F5F5F5"
              strokeWidth="2"
              strokeDasharray="14 18"
              fill="none"
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Punk collage blocks */}
      <div style={{ position: 'absolute', left: 60, top: 60, width: 360, height: 90, background: '#000000', opacity: 0.4, transform: 'rotate(-3deg)' }} />
      <div style={{ position: 'absolute', right: 80, top: 820, width: 440, height: 110, background: '#FFC107', opacity: 0.14, transform: 'rotate(4deg)' }} />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 30 }).map((_, i) => {
          const s = seededRandom(i + 21);
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: (s * 1900 + Math.sin(frame * 0.01 + i) * 40) % 1900,
                top: (seededRandom(i + 5) * 1080 + frame * (0.4 + s * 0.7)) % 1080,
                width: 6,
                height: 6,
                borderRadius: '50%',
                background: '#F5F5F5',
                opacity: 0.3,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)` }}>
        {/* BEAT 1 - Headline */}
        <div style={{ position: 'absolute', left: 90, top: 70, opacity: seg(0, 25, 540, 580) }}>
          <div style={{ fontFamily: 'Space Mono', fontSize: 30, color: '#FFC107', letterSpacing: 6, transform: `translateX(${(1 - sp(0)) * -60}px)` }}>
            EFFET MULTIPLICATEUR
          </div>
          <div style={{ fontFamily: 'Anton', fontSize: 96, color: '#F5F5F5', lineHeight: 1, transform: `translateY(${(1 - sp(10)) * 50}px)` }}>
            LE PRIX VISIBLE MENT
          </div>
        </div>

        {/* BEAT 2 - Central purchase + satellites */}
        <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', opacity: seg(40, 80, 540, 580) }}>
          <div style={{ position: 'relative', width: 900, height: 620 }}>
            {/* connection lines */}
            <svg width="900" height="620" style={{ position: 'absolute', left: 0, top: 0 }}>
              {SATELLITES.map((s, i) => {
                const grow = sp(110 + i * 22);
                const rad = ((s.angle + frame * 0.06) * Math.PI) / 180;
                const x = 450 + Math.cos(rad) * 320 * grow;
                const y = 310 + Math.sin(rad) * 230 * grow;
                return <line key={`l-${i}`} x1="450" y1="310" x2={x} y2={y} stroke="#FFC107" strokeWidth="3" opacity={0.55 * grow} />;
              })}
            </svg>

            {/* satellites */}
            {SATELLITES.map((s, i) => {
              const grow = sp(110 + i * 22);
              const rad = ((s.angle + frame * 0.06) * Math.PI) / 180;
              const x = 450 + Math.cos(rad) * 320 * grow;
              const y = 310 + Math.sin(rad) * 230 * grow;
              return (
                <div
                  key={s.label}
                  style={{
                    position: 'absolute',
                    left: x,
                    top: y,
                    transform: `translate(-50%, -50%) scale(${grow})`,
                    background: '#F5F5F5',
                    padding: '12px 22px',
                    fontFamily: 'Montserrat',
                    fontWeight: 700,
                    fontSize: 32,
                    color: '#000000',
                    whiteSpace: 'nowrap',
                  }}
                >
                  {s.label}
                </div>
              );
            })}

            {/* center card */}
            <div
              style={{
                position: 'absolute',
                left: 450,
                top: 310,
                transform: `translate(-50%, -50%) scale(${sp(50, { damping: 10 })})`,
                background: '#000000',
                border: '5px solid #FFC107',
                padding: '30px 46px',
                textAlign: 'center',
              }}
            >
              <div style={{ fontFamily: 'Montserrat', fontWeight: 600, fontSize: 28, color: '#9E9E9E', letterSpacing: 4 }}>ACHAT AFFICHÉ</div>
              <div style={{ fontFamily: 'Anton', fontSize: 110, color: '#F5F5F5', lineHeight: 1.05 }}>5 000 €</div>
            </div>
          </div>
        </AbsoluteFill>

        {/* BEAT 3 - Counter climbing */}
        <div style={{ position: 'absolute', left: 0, right: 0, bottom: 90, textAlign: 'center', opacity: seg(300, 340, 540, 580) }}>
          <span style={{ fontFamily: 'Montserrat', fontWeight: 600, fontSize: 32, color: '#F5F5F5', letterSpacing: 4 }}>COÛT RÉEL DE L'ÉCOSYSTÈME&nbsp;&nbsp;</span>
          <span style={{ fontFamily: 'Anton', fontSize: 120, color: '#FFC107' }}>{fmt(counter)} €</span>
        </div>

        {/* BEAT 4 - Iceberg payoff */}
        <AbsoluteFill style={{ opacity: seg(560, 600, 850, 880) }}>
          <svg width="1920" height="1080">
            <polygon points="960,250 1120,470 800,470" fill="#F5F5F5" />
            <polygon points="800,470 1120,470 1330,980 620,980" fill="#F5F5F5" opacity="0.28" />
            <line x1="360" y1={water} x2="1560" y2={water} stroke="#FFC107" strokeWidth="6" strokeDasharray="26 16" />
          </svg>
          <div style={{ position: 'absolute', left: 250, top: water - 110, fontFamily: 'Anton', fontSize: 62, color: '#F5F5F5' }}>PRIX AFFICHÉ</div>
          <div style={{ position: 'absolute', left: 250, top: water + 40, fontFamily: 'Anton', fontSize: 62, color: '#FFC107' }}>
            DÉPENSES RÉCURRENTES
          </div>
          <div
            style={{
              position: 'absolute',
              right: 140,
              bottom: 150,
              fontFamily: 'Bricolage Grotesque',
              fontSize: 48,
              color: '#F5F5F5',
              maxWidth: 560,
              textAlign: 'right',
              opacity: sp(700),
            }}
          >
            Tu n'achètes jamais un objet seul.
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 42%, rgba(0,0,0,0.58) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene32EcosystemeDepenses;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33LiberationConstellation',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const fmt = (n: number): string => n.toFixed(0).replace(/\B(?=(\d{3})+(?!\d))/g, ' ');

const CHAINS = [
  { label: 'Assurance', x: -430, y: -220 },
  { label: 'Contrôle technique', x: 380, y: -250 },
  { label: 'Parking', x: -480, y: 60 },
  { label: 'Carburant', x: 430, y: 60 },
  { label: 'Révisions', x: -330, y: 260 },
  { label: 'Taxes', x: 350, y: 280 },
];

const Scene33LiberationConstellation: React.FC = () => {
  // Use the current frame to drive every animation of this scene
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const seg = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera tilt + pull back
  const camScale = interpolate(frame, [0, 400, durationInFrames], [1.2, 1.0, 1.08], { extrapolateRight: 'clamp' });
  const camY = interpolate(frame, [0, durationInFrames], [40, -40], { extrapolateRight: 'clamp' });

  const saved = Math.round(
    interpolate(frame, [600, 730], [0, 5500], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) })
  );

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(120deg, #2E245C 0%, #504099 40%, #6B57C4 100%), radial-gradient(circle at 70% 20%, rgba(255,193,7,0.18) 0%, rgba(255,193,7,0) 45%)',
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        {[0, 1, 2, 3].map((i) => (
          <div
            key={`sh-${i}`}
            style={{
              position: 'absolute',
              left: 80 + seededRandom(i + 2) * 1600,
              top: 40 + seededRandom(i + 9) * 900,
              width: 300 + i * 130,
              height: 300 + i * 130,
              border: '2px solid #FFC107',
              opacity: 0.07,
              transform: `rotate(${45 + frame * 0.04 * (i % 2 === 0 ? 1 : -1)}deg)`,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Motion texture - diagonal scan lines */}
      <AbsoluteFill style={{ opacity: 0.1 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 40 }).map((_, i) => {
            const off = (i * 60 + frame * 0.8) % 2400;
            return <line key={`sl-${i}`} x1={off - 400} y1="0" x2={off} y2="1080" stroke="#F5F5F5" strokeWidth="2" />;
          })}
        </svg>
      </AbsoluteFill>

      {/* Punk collage strips */}
      <div style={{ position: 'absolute', left: 0, bottom: 240, width: 700, height: 130, background: '#000000', opacity: 0.32, transform: 'rotate(-4deg)' }} />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 28 }).map((_, i) => {
          const s = seededRandom(i + 41);
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: s * 1900,
                top: (seededRandom(i + 13) * 1080 - frame * (0.6 + s)) < 0
                  ? ((seededRandom(i + 13) * 1080 - frame * (0.6 + s)) % 1080) + 1080
                  : (seededRandom(i + 13) * 1080 - frame * (0.6 + s)) % 1080,
                width: 4,
                height: 16,
                background: '#F5F5F5',
                opacity: 0.28,
                transform: `rotate(${25 + s * 20}deg)`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${camY}px)` }}>
        {/* BEAT 1 - Title */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 80, textAlign: 'center', opacity: seg(0, 25, 560, 600) }}>
          <div
            style={{
              display: 'inline-block',
              background: '#FFC107',
              padding: '8px 26px',
              transform: `rotate(-1.5deg) scale(${sp(0)})`,
            }}
          >
            <span style={{ fontFamily: 'Space Mono', fontSize: 30, color: '#000000', letterSpacing: 5 }}>SUPPRIMER UN OBJET MAJEUR</span>
          </div>
        </div>

        {/* BEAT 2 - Central car icon with attached costs, then release */}
        <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', opacity: seg(20, 60, 700, 760) }}>
          <div style={{ position: 'relative', width: 1200, height: 700 }}>
            {CHAINS.map((c, i) => {
              const inSp = sp(70 + i * 20);
              const release = interpolate(frame, [330 + i * 18, 470 + i * 18], [0, 1], {
                extrapolateLeft: 'clamp',
                extrapolateRight: 'clamp',
                easing: Easing.in(Easing.cubic),
              });
              const x = 600 + c.x * inSp + c.x * release * 2.2;
              const y = 350 + c.y * inSp + c.y * release * 1.6;
              const op = (1 - release) * inSp;
              return (
                <div key={c.label} style={{ position: 'absolute', left: x, top: y, transform: `translate(-50%,-50%) rotate(${release * 25}deg)`, opacity: op }}>
                  <div
                    style={{
                      background: '#F5F5F5',
                      border: '3px solid #000000',
                      padding: '12px 22px',
                      fontFamily: 'Montserrat',
                      fontWeight: 700,
                      fontSize: 32,
                      color: '#000000',
                      whiteSpace: 'nowrap',
                    }}
                  >
                    {c.label}
                  </div>
                </div>
              );
            })}

            {/* central object icon */}
            <div
              style={{
                position: 'absolute',
                left: 600,
                top: 350,
                transform: `translate(-50%,-50%) scale(${sp(30, { damping: 10 })})`,
                opacity: interpolate(frame, [480, 560], [1, 0.25], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              }}
            >
              <svg width="340" height="180">
                <rect x="20" y="70" width="300" height="70" rx="18" stroke="#F5F5F5" strokeWidth="8" fill="none" />
                <path d="M70 70 L110 25 L235 25 L275 70" stroke="#F5F5F5" strokeWidth="8" fill="none" strokeLinejoin="round" />
                <circle cx="95" cy="145" r="26" fill="#FFC107" />
                <circle cx="250" cy="145" r="26" fill="#FFC107" />
              </svg>
            </div>
          </div>
        </AbsoluteFill>

        {/* BEAT 3 - Payoff */}
        <AbsoluteFill style={{ opacity: seg(590, 640, 850, 880), justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ fontFamily: 'Montserrat', fontWeight: 600, fontSize: 36, color: '#F5F5F5', letterSpacing: 6 }}>LIBÉRÉ CHAQUE ANNÉE</div>
          <div style={{ fontFamily: 'Anton', fontSize: 200, lineHeight: 1, color: '#FFC107', transform: `scale(${sp(610, { damping: 10 })})` }}>
            {fmt(saved)} €
          </div>
          <div
            style={{
              fontFamily: 'Bricolage Grotesque',
              fontSize: 48,
              color: '#F5F5F5',
              marginTop: 12,
              maxWidth: 1200,
              textAlign: 'center',
              opacity: sp(700),
            }}
          >
            L'économie dépasse presque toujours la valeur du bien.
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 44%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene33LiberationConstellation;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34ScanDeTaVie',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const ITEMS = [
  { label: 'Voiture', flagged: true },
  { label: 'Abonnements', flagged: true },
  { label: 'Vélo', flagged: false },
  { label: 'Bateau', flagged: true },
  { label: 'Console', flagged: false },
  { label: 'Piscine', flagged: true },
  { label: 'Cave', flagged: false },
  { label: 'Garage', flagged: true },
];

const Scene34ScanDeTaVie: React.FC = () => {
  // Use the current frame to drive every animation of this scene
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const seg = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera pan across the inventory grid
  const camX = interpolate(frame, [0, 500, durationInFrames], [-50, 40, -20], { extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1.12], { extrapolateRight: 'clamp' });

  // Scanning beam sweeping the grid
  const scanX = interpolate(frame, [120, 420], [120, 1800], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad) });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(180deg, #5B4AAD 0%, #504099 55%, #372A6B 100%), radial-gradient(circle at 15% 85%, rgba(255,193,7,0.16) 0%, rgba(255,193,7,0) 50%)',
        }}
      />

      {/* Motion texture - technical grid */}
      <AbsoluteFill style={{ opacity: 0.14 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 20 }).map((_, i) => (
            <line key={`h-${i}`} x1="0" y1={i * 56 + (frame * 0.3) % 56} x2="1920" y2={i * 56 + (frame * 0.3) % 56} stroke="#F5F5F5" strokeWidth="1" />
          ))}
          {Array.from({ length: 34 }).map((_, i) => (
            <line key={`v-${i}`} x1={i * 58} y1="0" x2={i * 58} y2="1080" stroke="#F5F5F5" strokeWidth="1" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Animated shape layers */}
      <AbsoluteFill>
        {[0, 1, 2].map((i) => (
          <div
            key={`sh-${i}`}
            style={{
              position: 'absolute',
              right: 100 + i * 380,
              top: 80 + Math.cos(frame * 0.01 + i) * 60,
              width: 460,
              height: 460,
              borderRadius: '50%',
              background: '#000000',
              opacity: 0.05,
              transform: `scale(${1 + Math.sin(frame * 0.014 + i) * 0.07})`,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Punk collage tape */}
      <div style={{ position: 'absolute', left: 140, top: 40, width: 300, height: 60, background: '#FFC107', opacity: 0.25, transform: 'rotate(-6deg)' }} />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 26 }).map((_, i) => {
          const s = seededRandom(i + 61);
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: (s * 1900 + frame * 0.4) % 1900,
                top: seededRandom(i + 17) * 1000,
                width: 7,
                height: 7,
                background: '#FFC107',
                opacity: 0.32,
                transform: `rotate(45deg) scale(${0.6 + Math.sin(frame * 0.05 + i) * 0.4})`,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${camX}px)` }}>
        {/* BEAT 1 - Title */}
        <div style={{ position: 'absolute', left: 110, top: 90, opacity: seg(0, 25, 620, 660) }}>
          <div style={{ fontFamily: 'Space Mono', fontSize: 30, color: '#FFC107', letterSpacing: 6, opacity: sp(0) }}>INVENTAIRE PERSONNEL</div>
          <div style={{ fontFamily: 'Anton', fontSize: 104, color: '#F5F5F5', lineHeight: 1, transform: `translateY(${(1 - sp(8)) * 60}px)` }}>
            OBSERVE TA PROPRE VIE
          </div>
        </div>

        {/* BEAT 2 - Inventory cards + scan beam */}
        <div
          style={{
            position: 'absolute',
            left: 110,
            top: 330,
            width: 1700,
            display: 'flex',
            flexWrap: 'wrap',
            gap: 26,
            opacity: seg(60, 100, 620, 660),
          }}
        >
          {ITEMS.map((it, i) => {
            const s = sp(70 + i * 14);
            const cardX = 110 + (i % 4) * 431;
            const scanned = scanX > cardX + 120;
            const flag = it.flagged && scanned;
            return (
              <div
                key={it.label}
                style={{
                  width: 405,
                  height: 210,
                  background: flag ? '#000000' : '#F5F5F5',
                  border: flag ? '4px solid #FFC107' : '4px solid #000000',
                  padding: 22,
                  transform: `translateY(${(1 - s) * 70}px) rotate(${i % 2 === 0 ? -1 : 1}deg) scale(${flag ? 1.03 : 1})`,
                  opacity: s,
                }}
              >
                <svg width="70" height="70">
                  <rect x="8" y="8" width="54" height="54" stroke={flag ? '#FFC107' : '#000000'} strokeWidth="6" fill="none" />
                  <line x1="8" y1="30" x2="62" y2="30" stroke={flag ? '#FFC107' : '#000000'} strokeWidth="6" />
                </svg>
                <div style={{ fontFamily: 'Anton', fontSize: 52, color: flag ? '#F5F5F5' : '#000000', marginTop: 8 }}>{it.label}</div>
                <div style={{ fontFamily: 'Space Mono', fontSize: 26, color: flag ? '#FFC107' : '#504099' }}>
                  {flag ? 'DÉPENSES SATELLITES' : 'à vérifier'}
                </div>
              </div>
            );
          })}
        </div>

        {/* Scan beam */}
        <div
          style={{
            position: 'absolute',
            left: scanX,
            top: 300,
            width: 8,
            height: 560,
            background: '#FFC107',
            opacity: frame > 110 && frame < 440 ? 0.9 : 0,
            boxShadow: '0 0 40px #FFC107',
          }}
        />

        {/* BEAT 3 - Question payoff */}
        <AbsoluteFill style={{ opacity: seg(640, 690, 850, 880), justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ background: '#000000', padding: '40px 60px', transform: `rotate(-1deg) scale(${sp(660, { damping: 11 })})`, maxWidth: 1450 }}>
            <div style={{ fontFamily: 'Anton', fontSize: 92, color: '#F5F5F5', lineHeight: 1.05, textAlign: 'center' }}>
              QUEL OBJET TRAÎNE<br />UN CORTÈGE DE FRAIS ?
            </div>
          </div>
          <div style={{ fontFamily: 'Bricolage Grotesque', fontSize: 46, color: '#FFC107', marginTop: 26, opacity: sp(740) }}>
            La réponse est souvent devant toi.
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 45%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene34ScanDeTaVie;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35DefinirAssez',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35DefinirAssez: React.FC = () => {
  // Use the current frame to drive every animation of this scene
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const seg = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera dolly along the track
  const camScale = interpolate(frame, [0, 450, durationInFrames], [1.25, 1.02, 1.12], { extrapolateRight: 'clamp' });

  // Marker travelling toward the finish line
  const markerX = interpolate(frame, [140, 470], [180, 1360], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic) });

  // Impact shake when marker hits the finish line
  const shake = frame > 460 && frame < 500 ? Math.sin(frame * 1.6) * (500 - frame) * 0.2 : 0;

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 78% 30%, #7362D6 0%, rgba(115,98,214,0) 55%), linear-gradient(210deg, #504099 0%, #322663 100%)',
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        {[0, 1, 2, 3, 4].map((i) => (
          <div
            key={`sh-${i}`}
            style={{
              position: 'absolute',
              left: seededRandom(i + 4) * 1700,
              top: seededRandom(i + 23) * 850,
              width: 260 + i * 90,
              height: 260 + i * 90,
              background: i % 2 === 0 ? '#FFC107' : '#F5F5F5',
              opacity: 0.045,
              transform: `rotate(${frame * 0.05 + i * 25}deg)`,
              clipPath: i % 3 === 0 ? 'polygon(50% 0%, 100% 100%, 0% 100%)' : 'none',
              borderRadius: i % 3 === 1 ? '50%' : 0,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Motion texture - vertical measure ticks */}
      <AbsoluteFill style={{ opacity: 0.16 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 60 }).map((_, i) => (
            <line key={`t-${i}`} x1={i * 32} y1={i % 5 === 0 ? 940 : 975} x2={i * 32} y2="1010" stroke="#F5F5F5" strokeWidth="3" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Punk collage blocks */}
      <div style={{ position: 'absolute', right: 60, top: 120, width: 380, height: 100, background: '#000000', opacity: 0.35, transform: 'rotate(5deg)' }} />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 30 }).map((_, i) => {
          const s = seededRandom(i + 71);
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: s * 1900,
                top: ((seededRandom(i + 29) * 1080 + frame * (0.5 + s)) % 1080),
                width: 5,
                height: 5,
                borderRadius: '50%',
                background: '#F5F5F5',
                opacity: 0.3,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateX(${shake}px)` }}>
        {/* BEAT 1 - Principle tag */}
        <div style={{ position: 'absolute', left: 130, top: 100, opacity: seg(0, 25, 620, 660) }}>
          <div style={{ display: 'inline-block', background: '#FFC107', padding: '10px 24px', transform: `rotate(-2deg) scale(${sp(0)})` }}>
            <span style={{ fontFamily: 'Space Mono', fontSize: 30, color: '#000000', letterSpacing: 5 }}>PRINCIPE 08</span>
          </div>
          <div style={{ fontFamily: 'Anton', fontSize: 110, color: '#F5F5F5', lineHeight: 1, marginTop: 20, transform: `translateY(${(1 - sp(14)) * 70}px)` }}>
            DÉFINIS TON<br />« ASSEZ »
          </div>
        </div>

        {/* BEAT 2 - Track with finish line */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 560, opacity: seg(100, 140, 620, 660) }}>
          <svg width="1920" height="300">
            <line x1="150" y1="150" x2="1600" y2="150" stroke="#F5F5F5" strokeWidth="8" opacity="0.5" />
            {/* finish line */}
            <g transform="translate(1400,40)" opacity={sp(120)}>
              {Array.from({ length: 12 }).map((_, i) => (
                <rect
                  key={`chk-${i}`}
                  x={(i % 2) * 30}
                  y={Math.floor(i / 2) * 37}
                  width="30"
                  height="37"
                  fill={i % 3 === 0 ? '#000000' : '#F5F5F5'}
                />
              ))}
            </g>
            <text x="1400" y="290" fontFamily="Anton" fontSize="56" fill="#FFC107">
              LIGNE D'ARRIVÉE
            </text>
          </svg>

          {/* travelling marker */}
          <div style={{ position: 'absolute', left: markerX, top: 108, transform: 'translateX(-50%)' }}>
            <svg width="120" height="120">
              <polygon points="60,10 110,95 10,95" fill="#FFC107" stroke="#000000" strokeWidth="5" />
              <circle cx="60" cy="72" r="10" fill="#000000" />
            </svg>
          </div>
        </div>

        {/* BEAT 3 - Contrast cards */}
        <div style={{ position: 'absolute', right: 130, top: 200, opacity: seg(220, 270, 620, 660) }}>
          {[
            { t: 'SANS SEUIL', d: 'Chaque euro gagné trouve une dépense.', c: '#000000', f: '#F5F5F5' },
            { t: 'AVEC SEUIL', d: 'Chaque euro en trop devient du capital.', c: '#F5F5F5', f: '#000000' },
          ].map((card, i) => {
            const s = sp(230 + i * 40);
            return (
              <div
                key={card.t}
                style={{
                  width: 620,
                  background: card.c,
                  padding: '26px 30px',
                  marginBottom: 24,
                  transform: `translateX(${(1 - s) * 400}px) rotate(${i === 0 ? 1.2 : -1.2}deg)`,
                  opacity: s,
                  borderLeft: '10px solid #FFC107',
                }}
              >
                <div style={{ fontFamily: 'Anton', fontSize: 62, color: card.f }}>{card.t}</div>
                <div style={{ fontFamily: 'Montserrat', fontWeight: 600, fontSize: 36, color: card.f, opacity: 0.85 }}>{card.d}</div>
              </div>
            );
          })}
        </div>

        {/* BEAT 4 - Hero payoff */}
        <AbsoluteFill style={{ opacity: seg(640, 690, 850, 880), justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ fontFamily: 'Clash Display', fontSize: 200, color: '#F5F5F5', lineHeight: 1, transform: `scale(${sp(660, { damping: 10 })})` }}>
            ASSEZ
          </div>
          <div style={{ width: interpolate(frame, [700, 780], [0, 700], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }), height: 12, background: '#FFC107' }} />
          <div style={{ fontFamily: 'Bricolage Grotesque', fontSize: 48, color: '#F5F5F5', marginTop: 26, opacity: sp(760) }}>
            Un chiffre décidé avant l'augmentation.
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 43%, rgba(0,0,0,0.56) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene35DefinirAssez;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36BoucleInfinie',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const fmt = (n: number): string => n.toFixed(0).replace(/\B(?=(\d{3})+(?!\d))/g, ' ');

const Scene36BoucleInfinie: React.FC = () => {
  // Use the current frame to drive every animation of this scene
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const seg = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera slow tilt
  const camRot = interpolate(frame, [0, durationInFrames], [1.8, -1.8], { extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1.13], { extrapolateRight: 'clamp' });

  // Both bars rise together - the gap never opens
  const rise = interpolate(frame, [120, 460], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.quad) });
  const income = 3400 + rise * 11600;
  const spendv = income * 0.965;
  const barH = 520;

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(135deg, #6B57C4 0%, #504099 45%, #251C4A 100%), radial-gradient(circle at 30% 80%, rgba(0,0,0,0.35) 0%, rgba(0,0,0,0) 55%)',
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        {[0, 1, 2].map((i) => (
          <div
            key={`sh-${i}`}
            style={{
              position: 'absolute',
              left: 140 + i * 700,
              top: 100 + Math.sin(frame * 0.009 + i * 2) * 80,
              width: 560,
              height: 560,
              border: '4px solid #F5F5F5',
              borderRadius: '50%',
              opacity: 0.05,
              transform: `scale(${1 + Math.sin(frame * 0.013 + i) * 0.09})`,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Motion texture - rotating loop arrows */}
      <AbsoluteFill style={{ opacity: 0.12, justifyContent: 'center', alignItems: 'center' }}>
        <svg width="900" height="900" style={{ transform: `rotate(${frame * 0.6}deg)` }}>
          <circle cx="450" cy="450" r="380" stroke="#F5F5F5" strokeWidth="6" fill="none" strokeDasharray="60 40" />
          <circle cx="450" cy="450" r="290" stroke="#FFC107" strokeWidth="4" fill="none" strokeDasharray="20 26" />
        </svg>
      </AbsoluteFill>

      {/* Punk collage strips */}
      <div style={{ position: 'absolute', left: 0, top: 300, width: 480, height: 96, background: '#FFC107', opacity: 0.18, transform: 'rotate(-7deg)' }} />
      <div style={{ position: 'absolute', right: 0, bottom: 120, width: 520, height: 96, background: '#000000', opacity: 0.3, transform: 'rotate(3deg)' }} />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 32 }).map((_, i) => {
          const s = seededRandom(i + 91);
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: (s * 1900 + Math.sin(frame * 0.02 + i) * 60 + 1900) % 1900,
                top: ((seededRandom(i + 37) * 1080 - frame * (0.4 + s) + 2160) % 1080),
                width: 6,
                height: 6,
                background: i % 3 === 0 ? '#FFC107' : '#F5F5F5',
                opacity: 0.32,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)` }}>
        {/* BEAT 1 - Title */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 80, textAlign: 'center', opacity: seg(0, 25, 600, 640) }}>
          <div style={{ fontFamily: 'Space Mono', fontSize: 30, color: '#FFC107', letterSpacing: 8, opacity: sp(0) }}>LIFESTYLE CREEP</div>
          <div style={{ fontFamily: 'Anton', fontSize: 100, color: '#F5F5F5', lineHeight: 1, transform: `translateY(${(1 - sp(10)) * 60}px)` }}>
            GAGNER PLUS, DÉPENSER PLUS
          </div>
        </div>

        {/* BEAT 2 - Twin bars rising together */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 300, display: 'flex', justifyContent: 'center', gap: 160, opacity: seg(80, 130, 600, 640) }}>
          {[
            { label: 'REVENUS', v: income, color: '#F5F5F5' },
            { label: 'DÉPENSES', v: spendv, color: '#FFC107' },
          ].map((b, i) => {
            const h = (b.v / 15000) * barH * sp(90 + i * 20);
            return (
              <div key={b.label} style={{ width: 250, textAlign: 'center' }}>
                <div style={{ height: barH, display: 'flex', alignItems: 'flex-end', justifyContent: 'center' }}>
                  <div style={{ width: 200, height: Math.max(4, h), background: b.color, border: '4px solid #000000' }} />
                </div>
                <div style={{ fontFamily: 'Anton', fontSize: 58, color: '#F5F5F5', marginTop: 14 }}>{fmt(b.v)} €</div>
                <div style={{ fontFamily: 'Montserrat', fontWeight: 700, fontSize: 32, color: b.color, letterSpacing: 3 }}>{b.label}</div>
              </div>
            );
          })}
        </div>

        {/* BEAT 3 - Gap callout */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 880, textAlign: 'center', opacity: seg(360, 400, 600, 640) }}>
          <div style={{ display: 'inline-block', background: '#000000', padding: '16px 36px', transform: `scale(${sp(370, { damping: 10 })})` }}>
            <span style={{ fontFamily: 'Anton', fontSize: 66, color: '#F5F5F5' }}>ÉCART CONSERVÉ : </span>
            <span style={{ fontFamily: 'Anton', fontSize: 66, color: '#FFC107' }}>PRESQUE NUL</span>
          </div>
        </div>

        {/* BEAT 4 - Infinite loop payoff */}
        <AbsoluteFill style={{ opacity: seg(620, 670, 850, 880), justifyContent: 'center', alignItems: 'center' }}>
          <svg width="620" height="330" style={{ transform: `scale(${sp(640, { damping: 11 })})` }}>
            <path
              d="M120 165 C120 70, 260 70, 310 165 C360 260, 500 260, 500 165 C500 70, 360 70, 310 165 C260 260, 120 260, 120 165 Z"
              stroke="#FFC107"
              strokeWidth="16"
              fill="none"
              strokeLinecap="round"
              strokeDasharray="1200"
              strokeDashoffset={interpolate(frame, [640, 780], [1200, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })}
            />
          </svg>
          <div style={{ fontFamily: 'Anton', fontSize: 92, color: '#F5F5F5', marginTop: 10, textAlign: 'center' }}>UN CYCLE SANS FIN</div>
          <div style={{ fontFamily: 'Bricolage Grotesque', fontSize: 46, color: '#FFC107', marginTop: 8, opacity: sp(760) }}>
            Sans ligne d'arrivée, il n'y a jamais de victoire.
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 44%, rgba(0,0,0,0.57) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene36BoucleInfinie;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37SeuilDeSuffisance',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const fmt = (n: number): string => n.toFixed(0).replace(/\B(?=(\d{3})+(?!\d))/g, ' ');

const Scene37SeuilDeSuffisance: React.FC = () => {
  // Use the current frame to drive every animation of this scene
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const seg = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const camScale = interpolate(frame, [0, durationInFrames], [1.12, 1.02], { extrapolateRight: 'clamp' });

  // Income climbing above a locked threshold
  const salaryStep = interpolate(frame, [150, 300, 430, 560], [3400, 3400, 4800, 6200], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
    easing: Easing.inOut(Easing.quad),
  });
  const threshold = 2800;
  const scaleY = 0.075;
  const baseY = 860;
  const thrY = baseY - threshold * scaleY;
  const salY = baseY - salaryStep * scaleY;
  const surplus = Math.max(0, salaryStep - threshold);

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(0deg, #2B2158 0%, #504099 60%, #6E5AD0 100%), radial-gradient(circle at 85% 15%, rgba(255,193,7,0.2) 0%, rgba(255,193,7,0) 45%)',
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        {[0, 1, 2, 3].map((i) => (
          <div
            key={`sh-${i}`}
            style={{
              position: 'absolute',
              left: seededRandom(i + 12) * 1600,
              top: seededRandom(i + 44) * 800,
              width: 340 + i * 110,
              height: 340 + i * 110,
              background: '#F5F5F5',
              opacity: 0.04,
              transform: `rotate(${frame * 0.03 * (i % 2 ? -1 : 1)}deg)`,
              clipPath: i % 2 === 0 ? 'polygon(50% 0%, 100% 38%, 82% 100%, 18% 100%, 0% 38%)' : 'none',
              borderRadius: i % 2 === 1 ? 32 : 0,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Motion texture - horizontal measure lines */}
      <AbsoluteFill style={{ opacity: 0.12 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 12 }).map((_, i) => (
            <line key={`ml-${i}`} x1="140" y1={140 + i * 70} x2="1780" y2={140 + i * 70} stroke="#F5F5F5" strokeWidth="1.5" strokeDasharray="10 16" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Punk collage tape */}
      <div style={{ position: 'absolute', left: 90, top: 60, width: 420, height: 74, background: '#000000', opacity: 0.4, transform: 'rotate(-2deg)' }} />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 26 }).map((_, i) => {
          const s = seededRandom(i + 101);
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: s * 1900,
                top: ((seededRandom(i + 53) * 1080 - frame * (0.7 + s) + 2160) % 1080),
                width: 8,
                height: 8,
                background: '#FFC107',
                opacity: 0.3,
                transform: 'rotate(45deg)',
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* BEAT 1 - Title */}
        <div style={{ position: 'absolute', left: 120, top: 70, opacity: seg(0, 25, 700, 740) }}>
          <div style={{ fontFamily: 'Space Mono', fontSize: 30, color: '#FFC107', letterSpacing: 6, opacity: sp(0) }}>SEUIL DE SUFFISANCE</div>
          <div style={{ fontFamily: 'Anton', fontSize: 96, color: '#F5F5F5', lineHeight: 1, transform: `translateY(${(1 - sp(10)) * 50}px)` }}>
            LA LIGNE QU'ON NE BOUGE PAS
          </div>
        </div>

        {/* BEAT 2 - Threshold chart */}
        <div style={{ position: 'absolute', left: 0, top: 0, opacity: seg(90, 130, 700, 740) }}>
          <svg width="1920" height="1080">
            {/* surplus zone */}
            <rect x="330" y={salY} width="1180" height={Math.max(0, thrY - salY)} fill="#FFC107" opacity="0.25" />
            {/* income line */}
            <line x1="330" y1={salY} x2="1510" y2={salY} stroke="#F5F5F5" strokeWidth="10" />
            {/* locked threshold line */}
            <line x1="330" y1={thrY} x2="1510" y2={thrY} stroke="#FFC107" strokeWidth="10" strokeDasharray="28 16" opacity={sp(110)} />
            {/* baseline */}
            <line x1="330" y1={baseY} x2="1510" y2={baseY} stroke="#F5F5F5" strokeWidth="4" opacity="0.4" />
          </svg>

          {/* padlock icon on the threshold */}
          <div style={{ position: 'absolute', left: 1540, top: thrY - 70, transform: `scale(${sp(150, { damping: 10 })})` }}>
            <svg width="120" height="140">
              <rect x="20" y="55" width="80" height="70" rx="10" fill="#FFC107" />
              <path d="M38 55 V40 a22 22 0 0 1 44 0 V55" stroke="#FFC107" strokeWidth="10" fill="none" />
              <circle cx="60" cy="90" r="9" fill="#000000" />
            </svg>
          </div>

          <div style={{ position: 'absolute', left: 340, top: thrY + 14, fontFamily: 'Space Mono', fontSize: 34, color: '#FFC107' }}>
            SEUIL FIXÉ : {fmt(threshold)} € / MOIS
          </div>
          <div style={{ position: 'absolute', left: 340, top: salY - 62, fontFamily: 'Anton', fontSize: 58, color: '#F5F5F5' }}>
            REVENU : {fmt(salaryStep)} €
          </div>
        </div>

        {/* BEAT 3 - Surplus counter */}
        <div style={{ position: 'absolute', right: 140, bottom: 120, textAlign: 'right', opacity: seg(300, 340, 700, 740) }}>
          <div style={{ fontFamily: 'Montserrat', fontWeight: 600, fontSize: 32, color: '#F5F5F5', letterSpacing: 5 }}>ÉPARGNE MENSUELLE</div>
          <div style={{ fontFamily: 'Anton', fontSize: 150, lineHeight: 1, color: '#FFC107' }}>{fmt(surplus)} €</div>
        </div>

        {/* BEAT 4 - Payoff */}
        <AbsoluteFill style={{ opacity: seg(720, 770, 855, 880), justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ background: '#F5F5F5', padding: '34px 54px', transform: `rotate(-1deg) scale(${sp(740, { damping: 11 })})` }}>
            <div style={{ fontFamily: 'Anton', fontSize: 86, color: '#000000', textAlign: 'center', lineHeight: 1.05 }}>
              LE REVENU MONTE.<br />LE MODE DE VIE RESTE.
            </div>
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 45%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene37SeuilDeSuffisance;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38FluxVersLaLiberte',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const OUTLETS = [
  { title: 'ÉPARGNE', sub: 'Livret A · LDDS', x: 300 },
  { title: 'INVESTISSEMENT', sub: 'PEA · Assurance-vie', x: 830 },
  { title: 'LIBERTÉ', sub: 'Choix · Options', x: 1360 },
];

const Scene38FluxVersLaLiberte: React.FC = () => {
  // Use the current frame to drive every animation of this scene
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const seg = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.12], { extrapolateRight: 'clamp' });
  const camY = interpolate(frame, [0, durationInFrames], [-25, 25], { extrapolateRight: 'clamp' });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 50% 0%, #7563DC 0%, rgba(117,99,220,0) 55%), linear-gradient(180deg, #504099 0%, #2B2158 100%)',
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        {[0, 1, 2, 3].map((i) => (
          <div
            key={`sh-${i}`}
            style={{
              position: 'absolute',
              left: 60 + i * 480,
              top: 620 + Math.sin(frame * 0.01 + i) * 50,
              width: 420,
              height: 420,
              borderRadius: '50%',
              border: '3px solid #FFC107',
              opacity: 0.06,
              transform: `scale(${1 + Math.cos(frame * 0.011 + i) * 0.07})`,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Motion texture - flowing dashes */}
      <AbsoluteFill style={{ opacity: 0.13 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 16 }).map((_, i) => (
            <line
              key={`fd-${i}`}
              x1="0"
              y1={70 + i * 66}
              x2="1920"
              y2={70 + i * 66}
              stroke="#F5F5F5"
              strokeWidth="2"
              strokeDasharray="30 40"
              strokeDashoffset={-frame * (1 + (i % 3))}
            />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Punk collage blocks */}
      <div style={{ position: 'absolute', right: 100, top: 70, width: 300, height: 90, background: '#FFC107', opacity: 0.2, transform: 'rotate(6deg)' }} />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 24 }).map((_, i) => {
          const s = seededRandom(i + 121);
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: (s * 1900 + frame * (0.6 + s)) % 1900,
                top: seededRandom(i + 67) * 1000,
                width: 6,
                height: 6,
                borderRadius: '50%',
                background: '#F5F5F5',
                opacity: 0.3,
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) translateY(${camY}px)` }}>
        {/* BEAT 1 - Title */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 70, textAlign: 'center', opacity: seg(0, 25, 700, 740) }}>
          <div style={{ fontFamily: 'Space Mono', fontSize: 30, color: '#FFC107', letterSpacing: 7, opacity: sp(0) }}>APRÈS LA LIGNE D'ARRIVÉE</div>
          <div style={{ fontFamily: 'Anton', fontSize: 98, color: '#F5F5F5', lineHeight: 1, transform: `translateY(${(1 - sp(10)) * 55}px)` }}>
            OÙ VA CHAQUE EURO EN PLUS
          </div>
        </div>

        {/* BEAT 2 - Source + pipes + outlets */}
        <div style={{ position: 'absolute', left: 0, top: 260, opacity: seg(70, 120, 700, 740) }}>
          {/* source */}
          <div
            style={{
              position: 'absolute',
              left: 760,
              top: 0,
              width: 400,
              background: '#000000',
              border: '5px solid #FFC107',
              padding: '20px 24px',
              textAlign: 'center',
              transform: `scale(${sp(60, { damping: 10 })})`,
            }}
          >
            <div style={{ fontFamily: 'Montserrat', fontWeight: 600, fontSize: 28, color: '#9E9E9E', letterSpacing: 4 }}>REVENU SUPPLÉMENTAIRE</div>
            <div style={{ fontFamily: 'Anton', fontSize: 78, color: '#F5F5F5', lineHeight: 1.1 }}>+ 1 400 €</div>
          </div>

          {/* pipes */}
          <svg width="1920" height="420" style={{ position: 'absolute', left: 0, top: 130 }}>
            {OUTLETS.map((o, i) => {
              const grow = sp(140 + i * 30);
              return (
                <path
                  key={`pipe-${i}`}
                  d={`M960 20 C960 130, ${o.x + 210} 130, ${o.x + 210} 300`}
                  stroke="#FFC107"
                  strokeWidth="7"
                  fill="none"
                  strokeDasharray="600"
                  strokeDashoffset={600 * (1 - grow)}
                  opacity="0.9"
                />
              );
            })}
            {/* flowing tokens */}
            {OUTLETS.map((o, i) =>
              [0, 1, 2].map((k) => {
                const t = ((frame * 0.9 + k * 40 + i * 22) % 120) / 120;
                const x = 960 + (o.x + 210 - 960) * (t * t);
                const y = 20 + 280 * t;
                return <circle key={`tok-${i}-${k}`} cx={x} cy={y} r="10" fill="#F5F5F5" opacity={frame > 160 ? 0.9 : 0} />;
              })
            )}
          </svg>

          {/* outlet cards */}
          {OUTLETS.map((o, i) => {
            const s = sp(200 + i * 35);
            return (
              <div
                key={o.title}
                style={{
                  position: 'absolute',
                  left: o.x,
                  top: 460,
                  width: 420,
                  background: i === 2 ? '#FFC107' : '#F5F5F5',
                  border: '5px solid #000000',
                  padding: '26px 24px',
                  transform: `translateY(${(1 - s) * 90}px) rotate(${i === 1 ? 0 : i === 0 ? -1.5 : 1.5}deg)`,
                  opacity: s,
                }}
              >
                <svg width="60" height="60">
                  {i === 0 && <path d="M10 45 L30 12 L50 45 Z" fill="#000000" />}
                  {i === 1 && (
                    <g>
                      <rect x="10" y="34" width="12" height="20" fill="#000000" />
                      <rect x="26" y="22" width="12" height="32" fill="#000000" />
                      <rect x="42" y="8" width="12" height="46" fill="#000000" />
                    </g>
                  )}
                  {i === 2 && (
                    <g>
                      <circle cx="24" cy="30" r="14" stroke="#000000" strokeWidth="7" fill="none" />
                      <rect x="34" y="26" width="24" height="8" fill="#000000" />
                      <rect x="50" y="34" width="8" height="12" fill="#000000" />
                    </g>
                  )}
                </svg>
                <div style={{ fontFamily: 'Anton', fontSize: 62, color: '#000000', lineHeight: 1.05, marginTop: 8 }}>{o.title}</div>
                <div style={{ fontFamily: 'Space Mono', fontSize: 28, color: '#504099' }}>{o.sub}</div>
              </div>
            );
          })}
        </div>

        {/* BEAT 3 - Payoff */}
        <AbsoluteFill style={{ opacity: seg(720, 770, 855, 880), justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ fontFamily: 'Clash Display', fontSize: 170, color: '#F5F5F5', lineHeight: 1, transform: `scale(${sp(740, { damping: 10 })})` }}>
            100 % CAPITAL
          </div>
          <div style={{ fontFamily: 'Bricolage Grotesque', fontSize: 48, color: '#FFC107', marginTop: 16, opacity: sp(790) }}>
            L'augmentation ne finance plus le train de vie.
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 44%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene38FluxVersLaLiberte;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39VingtAnsDeDivergence',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const fmt = (n: number): string => n.toFixed(0).replace(/\B(?=(\d{3})+(?!\d))/g, ' ');

const Scene39VingtAnsDeDivergence: React.FC = () => {
  // Use the current frame to drive every animation of this scene
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const seg = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const camScale = interpolate(frame, [0, 500, durationInFrames], [1.14, 1.0, 1.09], { extrapolateRight: 'clamp' });

  // Chart drawing progress
  const draw = interpolate(frame, [140, 520], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  const x0 = 300;
  const x1 = 1560;
  const yBase = 860;
  const pointsA: string[] = [];
  const pointsB: string[] = [];
  for (let i = 0; i <= 20; i++) {
    const t = i / 20;
    const x = x0 + (x1 - x0) * t;
    const growth = Math.pow(1.07, i);
    const yA = yBase - Math.min(560, 22 * (growth - 1) * 5.6);
    const yB = yBase - t * 70;
    pointsA.push(`${x},${yA}`);
    pointsB.push(`${x},${yB}`);
  }
  const shownA = pointsA.slice(0, Math.max(2, Math.round(draw * pointsA.length))).join(' ');
  const shownB = pointsB.slice(0, Math.max(2, Math.round(draw * pointsB.length))).join(' ');

  const capital = Math.round(
    interpolate(frame, [430, 600], [0, 900000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) })
  );

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background:
            'linear-gradient(160deg, #3A2E73 0%, #504099 50%, #6B57C4 100%), radial-gradient(circle at 90% 90%, rgba(255,193,7,0.16) 0%, rgba(255,193,7,0) 50%)',
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        {[0, 1, 2].map((i) => (
          <div
            key={`sh-${i}`}
            style={{
              position: 'absolute',
              left: 120 + i * 640,
              top: 90 + Math.cos(frame * 0.008 + i) * 60,
              width: 500,
              height: 500,
              background: '#000000',
              opacity: 0.05,
              borderRadius: i === 2 ? '50%' : 26,
              transform: `rotate(${frame * 0.04 + i * 20}deg)`,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Motion texture - chart grid */}
      <AbsoluteFill style={{ opacity: 0.16 }}>
        <svg width="1920" height="1080">
          {Array.from({ length: 9 }).map((_, i) => (
            <line key={`g-${i}`} x1="300" y1={300 + i * 70} x2="1560" y2={300 + i * 70} stroke="#F5F5F5" strokeWidth="1.5" />
          ))}
          {Array.from({ length: 11 }).map((_, i) => (
            <line key={`gv-${i}`} x1={300 + i * 126} y1="300" x2={300 + i * 126} y2="860" stroke="#F5F5F5" strokeWidth="1.5" />
          ))}
        </svg>
      </AbsoluteFill>

      {/* Punk collage strip */}
      <div style={{ position: 'absolute', left: 60, bottom: 70, width: 560, height: 88, background: '#000000', opacity: 0.34, transform: 'rotate(-3deg)' }} />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 26 }).map((_, i) => {
          const s = seededRandom(i + 141);
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: s * 1900,
                top: ((seededRandom(i + 71) * 1080 - frame * (0.5 + s) + 2160) % 1080),
                width: 5,
                height: 5,
                background: '#FFC107',
                opacity: 0.3,
                borderRadius: '50%',
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* BEAT 1 - Title */}
        <div style={{ position: 'absolute', left: 130, top: 90, opacity: seg(0, 25, 700, 740) }}>
          <div style={{ fontFamily: 'Space Mono', fontSize: 30, color: '#FFC107', letterSpacing: 6, opacity: sp(0) }}>HORIZON 20 ANS · 7 % NET</div>
          <div style={{ fontFamily: 'Anton', fontSize: 100, color: '#F5F5F5', lineHeight: 1, transform: `translateY(${(1 - sp(10)) * 60}px)` }}>
            MÊME SALAIRE, DEUX DESTINS
          </div>
        </div>

        {/* BEAT 2 - Diverging curves */}
        <div style={{ position: 'absolute', left: 0, top: 0, opacity: seg(110, 150, 700, 740) }}>
          <svg width="1920" height="1080">
            <polyline points={shownB} stroke="#9E9E9E" strokeWidth="9" fill="none" strokeLinecap="round" />
            <polyline points={shownA} stroke="#FFC107" strokeWidth="12" fill="none" strokeLinecap="round" />
            <line x1="300" y1="860" x2="1560" y2="860" stroke="#F5F5F5" strokeWidth="5" />
          </svg>

          <div style={{ position: 'absolute', left: 1180, top: 250, fontFamily: 'Anton', fontSize: 56, color: '#FFC107', opacity: sp(420) }}>
            SEUIL MAINTENU
          </div>
          <div style={{ position: 'absolute', left: 1180, top: 790, fontFamily: 'Anton', fontSize: 52, color: '#9E9E9E', opacity: sp(300) }}>
            MODE DE VIE DILATÉ
          </div>
        </div>

        {/* BEAT 3 - Final capital */}
        <div style={{ position: 'absolute', left: 130, bottom: 120, opacity: seg(420, 460, 700, 740) }}>
          <div style={{ fontFamily: 'Montserrat', fontWeight: 600, fontSize: 32, color: '#F5F5F5', letterSpacing: 5 }}>CAPITAL APRÈS 20 ANS</div>
          <div style={{ fontFamily: 'Anton', fontSize: 160, lineHeight: 1, color: '#F5F5F5' }}>{fmt(capital)} €</div>
        </div>

        {/* BEAT 4 - Payoff */}
        <AbsoluteFill style={{ opacity: seg(720, 770, 855, 880), justifyContent: 'center', alignItems: 'center' }}>
          <div style={{ background: '#000000', padding: '36px 56px', transform: `rotate(1deg) scale(${sp(740, { damping: 11 })})` }}>
            <div style={{ fontFamily: 'Anton', fontSize: 90, color: '#F5F5F5', textAlign: 'center', lineHeight: 1.05 }}>
              LA DIFFÉRENCE N'EST PAS<br />DANS LE SALAIRE
            </div>
          </div>
          <div style={{ fontFamily: 'Bricolage Grotesque', fontSize: 48, color: '#FFC107', marginTop: 24, opacity: sp(800) }}>
            Elle est dans la définition préalable de « assez ».
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 45%, rgba(0,0,0,0.55) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene39VingtAnsDeDivergence;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40Optionnalite',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const OPTIONS = [
  'Démissionner demain',
  'Six mois pour un proche',
  'Refuser un projet',
  'Déménager par amour',
  'Créer sa propre boîte',
];

const Scene40Optionnalite: React.FC = () => {
  // Use the current frame to drive every animation of this scene
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const seg = (a: number, b: number, c: number, d: number) =>
    interpolate(frame, [a, b, c, d], [0, 1, 1, 0], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Orbit camera for the radial option map
  const camRot = interpolate(frame, [0, durationInFrames], [-2, 2], { extrapolateRight: 'clamp' });
  const camScale = interpolate(frame, [0, 420, durationInFrames], [1.16, 1.0, 1.1], { extrapolateRight: 'clamp' });

  // Padlock opening at the payoff
  const unlock = interpolate(frame, [640, 700], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.back(2)) });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#504099', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at 50% 55%, #7C69E4 0%, rgba(124,105,228,0) 58%), linear-gradient(140deg, #504099 0%, #241B49 100%)',
        }}
      />

      {/* Animated shape layers */}
      <AbsoluteFill>
        {[0, 1, 2, 3, 4].map((i) => (
          <div
            key={`sh-${i}`}
            style={{
              position: 'absolute',
              left: seededRandom(i + 15) * 1650,
              top: seededRandom(i + 55) * 820,
              width: 280 + i * 100,
              height: 280 + i * 100,
              border: '3px solid #F5F5F5',
              opacity: 0.055,
              borderRadius: i % 2 === 0 ? '50%' : 20,
              transform: `rotate(${frame * 0.045 * (i % 2 ? 1 : -1)}deg) scale(${1 + Math.sin(frame * 0.01 + i) * 0.06})`,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Motion texture - radial spokes */}
      <AbsoluteFill style={{ opacity: 0.11, justifyContent: 'center', alignItems: 'center' }}>
        <svg width="1400" height="1080" style={{ transform: `rotate(${frame * 0.15}deg)` }}>
          {Array.from({ length: 36 }).map((_, i) => {
            const a = (i * 10 * Math.PI) / 180;
            return (
              <line
                key={`sp-${i}`}
                x1={700 + Math.cos(a) * 180}
                y1={540 + Math.sin(a) * 180}
                x2={700 + Math.cos(a) * 640}
                y2={540 + Math.sin(a) * 640}
                stroke="#F5F5F5"
                strokeWidth="2"
              />
            );
          })}
        </svg>
      </AbsoluteFill>

      {/* Punk collage blocks */}
      <div style={{ position: 'absolute', left: 70, top: 80, width: 420, height: 92, background: '#FFC107', opacity: 0.2, transform: 'rotate(-4deg)' }} />
      <div style={{ position: 'absolute', right: 70, bottom: 90, width: 380, height: 92, background: '#000000', opacity: 0.35, transform: 'rotate(3deg)' }} />

      {/* Floating particles */}
      <AbsoluteFill>
        {Array.from({ length: 30 }).map((_, i) => {
          const s = seededRandom(i + 161);
          return (
            <div
              key={`p-${i}`}
              style={{
                position: 'absolute',
                left: (s * 1900 + Math.cos(frame * 0.015 + i) * 50 + 1900) % 1900,
                top: ((seededRandom(i + 83) * 1080 - frame * (0.45 + s) + 2160) % 1080),
                width: 6,
                height: 6,
                background: i % 4 === 0 ? '#FFC107' : '#F5F5F5',
                opacity: 0.32,
                borderRadius: '50%',
              }}
            />
          );
        })}
      </AbsoluteFill>

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale}) rotate(${camRot}deg)` }}>
        {/* BEAT 1 - Principle tag */}
        <div style={{ position: 'absolute', left: 0, right: 0, top: 80, textAlign: 'center', opacity: seg(0, 25, 600, 640) }}>
          <div style={{ fontFamily: 'Space Mono', fontSize: 30, color: '#FFC107', letterSpacing: 8, opacity: sp(0) }}>PRINCIPE 09</div>
          <div style={{ fontFamily: 'Anton', fontSize: 96, color: '#F5F5F5', lineHeight: 1, transform: `translateY(${(1 - sp(10)) * 55}px)` }}>
            RICHESSE = CE QUE TU PEUX CHOISIR
          </div>
        </div>

        {/* BEAT 2 - Radial option map */}
        <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', opacity: seg(60, 110, 600, 640) }}>
          <div style={{ position: 'relative', width: 1500, height: 620 }}>
            <svg width="1500" height="620" style={{ position: 'absolute', left: 0, top: 0 }}>
              {OPTIONS.map((_, i) => {
                const g = sp(120 + i * 26);
                const a = ((-160 + i * 40) * Math.PI) / 180;
                const x = 750 + Math.cos(a) * 560 * g;
                const y = 330 + Math.sin(a) * 250 * g;
                return <line key={`ol-${i}`} x1="750" y1="330" x2={x} y2={y} stroke="#F5F5F5" strokeWidth="3" opacity={0.5 * g} />;
              })}
            </svg>

            {OPTIONS.map((o, i) => {
              const g = sp(120 + i * 26);
              const a = ((-160 + i * 40) * Math.PI) / 180;
              const x = 750 + Math.cos(a) * 560 * g;
              const y = 330 + Math.sin(a) * 250 * g;
              const pulse = 1 + Math.sin(frame * 0.06 + i) * 0.02;
              return (
                <div
                  key={o}
                  style={{
                    position: 'absolute',
                    left: x,
                    top: y,
                    transform: `translate(-50%,-50%) scale(${g * pulse}) rotate(${i % 2 === 0 ? -1 : 1}deg)`,
                    background: '#F5F5F5',
                    border: '4px solid #000000',
                    padding: '14px 22px',
                    fontFamily: 'Montserrat',
                    fontWeight: 700,
                    fontSize: 34,
                    color: '#000000',
                    whiteSpace: 'nowrap',
                  }}
                >
                  {o}
                </div>
              );
            })}

            <div
              style={{
                position: 'absolute',
                left: 750,
                top: 330,
                transform: `translate(-50%,-50%) scale(${sp(70, { damping: 10 })})`,
                background: '#000000',
                border: '5px solid #FFC107',
                padding: '28px 40px',
                textAlign: 'center',
              }}
            >
              <div style={{ fontFamily: 'Anton', fontSize: 84, color: '#FFC107', lineHeight: 1 }}>OPTIONNALITÉ</div>
              <div style={{ fontFamily: 'Space Mono', fontSize: 28, color: '#F5F5F5', letterSpacing: 3 }}>REVENUS − DÉPENSES</div>
            </div>
          </div>
        </AbsoluteFill>

        {/* BEAT 3 - Payoff with opening padlock */}
        <AbsoluteFill style={{ opacity: seg(620, 670, 855, 880), justifyContent: 'center', alignItems: 'center' }}>
          <svg width="200" height="230" style={{ transform: `scale(${sp(640, { damping: 11 })})` }}>
            <rect x="40" y="105" width="120" height="105" rx="14" fill="#FFC107" />
            <path
              d="M68 105 V78 a32 32 0 0 1 64 0 V95"
              stroke="#F5F5F5"
              strokeWidth="14"
              fill="none"
              transform={`rotate(${unlock * 22} 68 105) translate(${unlock * 16} ${-unlock * 6})`}
            />
            <circle cx="100" cy="150" r="13" fill="#000000" />
          </svg>
          <div style={{ fontFamily: 'Anton', fontSize: 92, color: '#F5F5F5', marginTop: 18, textAlign: 'center', lineHeight: 1.05 }}>
            CHAQUE ACHAT ÉCHANGE<br />UNE OPTION CONTRE UN OBJET
          </div>
          <div style={{ fontFamily: 'Bricolage Grotesque', fontSize: 46, color: '#FFC107', marginTop: 20, opacity: sp(760) }}>
            Parfois oui. Souvent non.
          </div>
        </AbsoluteFill>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 50%, rgba(0,0,0,0) 43%, rgba(0,0,0,0.56) 100%)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Montserrat',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: interpolate(frame, [0, 40], [0, 0.3], { extrapolateRight: 'clamp' }),
        }}
      >
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene40Optionnalite;
```

