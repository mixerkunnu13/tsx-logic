```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene31: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.08]);

  // Title reveals
  const titleY = interpolate(sp(5), [0, 1], [80, 0]);
  const titleOp = sp(5);

  // 3000 number pop
  const numScale = sp(30, { damping: 8, stiffness: 140 });
  const numGlow = 20 + Math.sin(frame * 0.1) * 10;

  // Weekend chips
  const chip1 = sp(60);
  const chip2 = sp(75);
  const chip3 = sp(90);

  // Arrow transformation
  const arrowProgress = interpolate(frame, [120, 180], [0, 1], { extrapolateRight: 'clamp' });
  const arrowOp = interpolate(frame, [120, 140], [0, 1], { extrapolateRight: 'clamp' });

  // Fund container reveal
  const fundOp = interpolate(frame, [180, 210], [0, 1], { extrapolateRight: 'clamp' });
  const fundY = interpolate(sp(180), [0, 1], [60, 0]);

  // Growing bar chart
  const barProgress = interpolate(frame, [240, 360], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Bottom callout
  const calloutOp = interpolate(frame, [380, 420], [0, 1], { extrapolateRight: 'clamp' });
  const calloutY = interpolate(sp(380), [0, 1], [40, 0]);

  // Particles
  const particles = Array.from({ length: 35 }, (_, i) => i);

  // Shapes
  const shapes = Array.from({ length: 6 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4A4466', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 20%, rgba(238,238,238,0.08) 0%, transparent 50%), radial-gradient(circle at 70% 80%, rgba(233,215,0,0.12) 0%, transparent 55%), linear-gradient(135deg, #4A4466 0%, #3A3556 100%)',
      }} />

      {/* Shape Layers */}
      {shapes.map((i) => {
        const s = seededRandom(i + 1);
        const size = 200 + s * 300;
        const x = seededRandom(i + 10) * 1920;
        const y = seededRandom(i + 20) * 1080;
        const rot = frame * 0.1 * (i % 2 === 0 ? 1 : -1);
        const breath = 1 + Math.sin(frame * 0.02 + i) * 0.05;
        return (
          <div key={`shape-${i}`} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid rgba(238,238,238,${0.04 + s * 0.04})`,
            borderRadius: i % 3 === 0 ? '50%' : i % 3 === 1 ? '0' : '20%',
            transform: `rotate(${rot}deg) scale(${breath})`,
          }} />
        );
      })}

      {/* Grid Texture */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(rgba(238,238,238,0.04) 1px, transparent 1px), linear-gradient(90deg, rgba(238,238,238,0.04) 1px, transparent 1px)',
        backgroundSize: '80px 80px',
        opacity: 0.6,
      }} />

      {/* Floating Particles */}
      {particles.map((i) => {
        const s = seededRandom(i);
        const px = (s * 1920 + frame * (0.3 + s)) % 1920;
        const py = (seededRandom(i + 100) * 1080 + Math.sin(frame * 0.02 + i) * 30) % 1080;
        const psize = 2 + seededRandom(i + 200) * 4;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: psize,
            height: psize,
            borderRadius: '50%',
            backgroundColor: i % 3 === 0 ? '#e9d700' : '#EEEEEE',
            opacity: 0.4,
            boxShadow: `0 0 ${psize * 2}px currentColor`,
          }} />
        );
      })}

      {/* Main Content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>
        {/* Top Title */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 38,
            fontWeight: 600,
            color: '#e9d700',
            letterSpacing: 8,
            textTransform: 'uppercase',
            marginBottom: 12,
          }}>
            ── WEEKEND vs WEALTH ──
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 76,
            color: '#EEEEEE',
            lineHeight: 1.1,
          }}>
            वही पैसा, दो रास्ते
          </div>
        </div>

        {/* The 3000 Number - Hero */}
        <div style={{
          position: 'absolute',
          top: 280,
          left: '50%',
          transform: `translateX(-50%) scale(${numScale})`,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 28,
            color: '#EEEEEE',
            opacity: 0.7,
            letterSpacing: 4,
            marginBottom: 8,
          }}>
            ₹ AMOUNT
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 220,
            color: '#e9d700',
            lineHeight: 1,
            textShadow: `0 0 ${numGlow}px rgba(233,215,0,0.6)`,
            letterSpacing: -4,
          }}>
            ₹3,000
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 42,
            color: '#EEEEEE',
            marginTop: 4,
            fontWeight: 600,
          }}>
            हर weekend
          </div>
        </div>

        {/* Left Path - Weekend Chips */}
        <div style={{
          position: 'absolute',
          top: 640,
          left: 120,
          width: 720,
        }}>
          <div style={{
            opacity: chip1,
            transform: `translateX(${(1 - chip1) * -40}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
            marginBottom: 18,
            padding: '20px 30px',
            background: 'rgba(255,0,0,0.12)',
            border: '2px solid rgba(255,0,0,0.4)',
            borderRadius: 16,
          }}>
            <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
              <circle cx="12" cy="12" r="10" stroke="#ff0000" strokeWidth="2" />
              <path d="M8 12h8M12 8v8" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" transform="rotate(45 12 12)" />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 36, color: '#EEEEEE', fontWeight: 600 }}>
              Swiggy / Zomato
            </div>
            <div style={{ marginLeft: 'auto', fontFamily: 'JetBrains Mono', fontSize: 32, color: '#ff0000', fontWeight: 700 }}>
              −₹1200
            </div>
          </div>

          <div style={{
            opacity: chip2,
            transform: `translateX(${(1 - chip2) * -40}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
            marginBottom: 18,
            padding: '20px 30px',
            background: 'rgba(255,0,0,0.12)',
            border: '2px solid rgba(255,0,0,0.4)',
            borderRadius: 16,
          }}>
            <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
              <rect x="3" y="6" width="18" height="14" rx="2" stroke="#ff0000" strokeWidth="2" />
              <path d="M3 10h18M8 6V4M16 6V4" stroke="#ff0000" strokeWidth="2" />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 36, color: '#EEEEEE', fontWeight: 600 }}>
              Mall Shopping
            </div>
            <div style={{ marginLeft: 'auto', fontFamily: 'JetBrains Mono', fontSize: 32, color: '#ff0000', fontWeight: 700 }}>
              −₹1000
            </div>
          </div>

          <div style={{
            opacity: chip3,
            transform: `translateX(${(1 - chip3) * -40}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
            padding: '20px 30px',
            background: 'rgba(255,0,0,0.12)',
            border: '2px solid rgba(255,0,0,0.4)',
            borderRadius: 16,
          }}>
            <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
              <path d="M3 12c2-4 6-6 9-6s7 2 9 6c-2 4-6 6-9 6s-7-2-9-6z" stroke="#ff0000" strokeWidth="2" />
              <circle cx="12" cy="12" r="3" stroke="#ff0000" strokeWidth="2" />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 36, color: '#EEEEEE', fontWeight: 600 }}>
              Weekend Plan
            </div>
            <div style={{ marginLeft: 'auto', fontFamily: 'JetBrains Mono', fontSize: 32, color: '#ff0000', fontWeight: 700 }}>
              −₹800
            </div>
          </div>
        </div>

        {/* Center Arrow - Transformation */}
        <div style={{
          position: 'absolute',
          top: 720,
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: arrowOp,
        }}>
          <svg width="180" height="180" viewBox="0 0 180 180">
            <defs>
              <linearGradient id="arrGrad" x1="0%" y1="0%" x2="100%" y2="0%">
                <stop offset="0%" stopColor="#ff0000" />
                <stop offset={`${arrowProgress * 100}%`} stopColor="#e9d700" />
                <stop offset="100%" stopColor="#e9d700" />
              </linearGradient>
            </defs>
            <circle cx="90" cy="90" r="70" stroke="url(#arrGrad)" strokeWidth="3" fill="none" 
              strokeDasharray="440" strokeDashoffset={(1 - arrowProgress) * 440} 
              transform="rotate(-90 90 90)" />
            <path 
              d={`M ${50} ${90} L ${130} ${90} M ${110} ${70} L ${130} ${90} L ${110} ${110}`}
              stroke="url(#arrGrad)" strokeWidth="6" fill="none" strokeLinecap="round" strokeLinejoin="round"
              strokeDasharray="200" strokeDashoffset={(1 - arrowProgress) * 200}
            />
          </svg>
          <div style={{
            position: 'absolute',
            top: -45,
            left: '50%',
            transform: 'translateX(-50%)',
            fontFamily: 'Yantra One',
            fontSize: 32,
            color: '#EEEEEE',
            whiteSpace: 'nowrap',
            opacity: arrowProgress,
          }}>
            SHIFT
          </div>
        </div>

        {/* Right Path - Index Fund */}
        <div style={{
          position: 'absolute',
          top: 640,
          right: 120,
          width: 720,
          opacity: fundOp,
          transform: `translateY(${fundY}px)`,
        }}>
          <div style={{
            padding: '30px 40px',
            background: 'rgba(233,215,0,0.12)',
            border: '3px solid #e9d700',
            borderRadius: 20,
            boxShadow: '0 0 40px rgba(233,215,0,0.2)',
          }}>
            <div style={{
              display: 'flex',
              alignItems: 'center',
              gap: 20,
              marginBottom: 20,
            }}>
              <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
                <path d="M3 17l6-6 4 4 8-8M14 7h7v7" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
              <div>
                <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#e9d700', fontWeight: 600, letterSpacing: 2 }}>
                  INDEX FUND SIP
                </div>
                <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 38, color: '#EEEEEE', fontWeight: 700, marginTop: 4 }}>
                  ₹3,000 / महीना
                </div>
              </div>
            </div>

            {/* Growing Bar Chart */}
            <div style={{ display: 'flex', alignItems: 'flex-end', gap: 12, height: 160, padding: '0 10px', borderBottom: '2px solid rgba(238,238,238,0.2)' }}>
              {[20, 32, 45, 58, 72, 88, 105, 125].map((h, i) => {
                const localProgress = Math.max(0, Math.min(1, barProgress * 8 - i));
                return (
                  <div key={`bar-${i}`} style={{
                    flex: 1,
                    height: h * localProgress,
                    background: `linear-gradient(180deg, #e9d700 0%, #FFC107 100%)`,
                    borderRadius: '6px 6px 0 0',
                    boxShadow: '0 0 12px rgba(233,215,0,0.4)',
                  }} />
                );
              })}
            </div>
            <div style={{
              display: 'flex',
              justifyContent: 'space-between',
              marginTop: 8,
              fontFamily: 'JetBrains Mono',
              fontSize: 18,
              color: '#EEEEEE',
              opacity: 0.6,
            }}>
              <span>Y1</span>
              <span>Y5</span>
              <span>Y10</span>
              <span>Y15</span>
              <span>Y20</span>
              <span>Y25</span>
            </div>
          </div>
        </div>

        {/* Bottom Callout */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: '50%',
          transform: `translateX(-50%) translateY(${calloutY}px)`,
          opacity: calloutOp,
          textAlign: 'center',
          padding: '18px 50px',
          background: 'rgba(8,6,22,0.6)',
          border: '2px solid rgba(233,215,0,0.3)',
          borderRadius: 50,
          backdropFilter: 'blur(10px)',
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 40,
            color: '#EEEEEE',
            fontWeight: 700,
          }}>
            12% औसत return × 25 साल = <span style={{ color: '#e9d700' }}>compounding magic</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(8,6,22,0.6) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene31;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [0, -20]);

  // Nifty badge
  const niftyScale = sp(10, { damping: 10, stiffness: 130 });
  
  // 12% reveal
  const pctScale = sp(50, { damping: 8 });
  const pctRotate = interpolate(sp(50), [0, 1], [-15, 0]);

  // Years counter
  const yearsOp = sp(110);
  const yearsCounter = Math.floor(interpolate(frame, [120, 220], [0, 25], { extrapolateRight: 'clamp' }));

  // Growth curve animation
  const curveProgress = interpolate(frame, [240, 380], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.quad) });

  // Final 56 lakh reveal
  const lakhOp = interpolate(frame, [380, 420], [0, 1], { extrapolateRight: 'clamp' });
  const lakhScale = sp(380, { damping: 7, stiffness: 110 });
  const lakhCounter = Math.floor(interpolate(frame, [390, 480], [0, 56], { extrapolateRight: 'clamp' }));

  // Shake on impact
  const shakeX = frame > 380 && frame < 410 ? Math.sin(frame * 2) * 4 : 0;

  const particles = Array.from({ length: 30 }, (_, i) => i);
  const shapes = Array.from({ length: 5 }, (_, i) => i);

  // Generate curve path
  const curvePoints: string[] = [];
  const totalPoints = 60;
  for (let i = 0; i <= totalPoints * curveProgress; i++) {
    const t = i / totalPoints;
    const x = 100 + t * 1100;
    const y = 400 - Math.pow(t, 2.2) * 320;
    curvePoints.push(`${i === 0 ? 'M' : 'L'} ${x} ${y}`);
  }

  return (
    <AbsoluteFill style={{ backgroundColor: '#4A4466', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 80% 30%, rgba(233,215,0,0.15) 0%, transparent 50%), radial-gradient(ellipse at 20% 70%, rgba(238,238,238,0.06) 0%, transparent 50%), linear-gradient(160deg, #4A4466 0%, #38334F 100%)',
      }} />

      {/* Shapes */}
      {shapes.map((i) => {
        const s = seededRandom(i + 5);
        const size = 250 + s * 350;
        const x = seededRandom(i + 15) * 1920;
        const y = seededRandom(i + 25) * 1080;
        const rot = frame * 0.08 * (i % 2 === 0 ? 1 : -1);
        return (
          <div key={`sh-${i}`} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid rgba(238,238,238,0.05)`,
            borderRadius: i % 2 === 0 ? '50%' : '30%',
            transform: `rotate(${rot}deg)`,
          }} />
        );
      })}

      {/* Dot Grid */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(circle, rgba(238,238,238,0.1) 1.5px, transparent 1.5px)',
        backgroundSize: '50px 50px',
        opacity: 0.5,
      }} />

      {/* Particles */}
      {particles.map((i) => {
        const s = seededRandom(i + 50);
        const px = (s * 1920 + frame * (0.4 + s * 0.5)) % 1920;
        const py = (seededRandom(i + 150) * 1080 + Math.cos(frame * 0.03 + i) * 25) % 1080;
        return (
          <div key={`pt-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 3,
            height: 3,
            borderRadius: '50%',
            backgroundColor: '#e9d700',
            opacity: 0.5,
            boxShadow: '0 0 8px #e9d700',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX + shakeX}px)` }}>
        {/* Top Bar - Nifty Badge & 12% */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          gap: 80,
        }}>
          {/* Nifty 50 Badge */}
          <div style={{
            transform: `scale(${niftyScale})`,
            padding: '24px 40px',
            background: 'rgba(238,238,238,0.08)',
            border: '2px solid #EEEEEE',
            borderRadius: 16,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
          }}>
            <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
              <rect x="3" y="13" width="3" height="8" fill="#e9d700" />
              <rect x="8" y="9" width="3" height="12" fill="#e9d700" />
              <rect x="13" y="5" width="3" height="16" fill="#e9d700" />
              <rect x="18" y="2" width="3" height="19" fill="#e9d700" />
            </svg>
            <div>
              <div style={{ fontFamily: 'Inter', fontSize: 20, color: '#EEEEEE', opacity: 0.7, letterSpacing: 3 }}>INDEX</div>
              <div style={{ fontFamily: 'Yantra One', fontSize: 56, color: '#EEEEEE', lineHeight: 1 }}>NIFTY 50</div>
            </div>
          </div>

          {/* 12% */}
          <div style={{
            transform: `scale(${pctScale}) rotate(${pctRotate}deg)`,
            textAlign: 'center',
          }}>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 160,
              color: '#e9d700',
              lineHeight: 1,
              textShadow: '0 0 30px rgba(233,215,0,0.5)',
            }}>
              12%
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#EEEEEE', letterSpacing: 4, marginTop: -8 }}>
              AVG RETURN / YEAR
            </div>
          </div>
        </div>

        {/* Growth Curve Chart */}
        <div style={{
          position: 'absolute',
          top: 320,
          left: 350,
          width: 1220,
          height: 480,
          opacity: yearsOp,
        }}>
          {/* Chart frame */}
          <svg width="1220" height="480" viewBox="0 0 1220 480" style={{ position: 'absolute' }}>
            {/* Grid lines */}
            {[0, 1, 2, 3, 4].map((i) => (
              <line key={`gl-${i}`} x1="80" y1={80 + i * 80} x2="1200" y2={80 + i * 80} stroke="rgba(238,238,238,0.1)" strokeWidth="1" strokeDasharray="4 6" />
            ))}
            {/* Axis */}
            <line x1="80" y1="80" x2="80" y2="420" stroke="#EEEEEE" strokeWidth="2" opacity="0.4" />
            <line x1="80" y1="420" x2="1200" y2="420" stroke="#EEEEEE" strokeWidth="2" opacity="0.4" />

            {/* Curve area fill */}
            <path
              d={curvePoints.join(' ') + ` L ${100 + (totalPoints * curveProgress / totalPoints) * 1100} 420 L 100 420 Z`}
              fill="url(#areaGrad)"
              opacity="0.3"
            />
            <defs>
              <linearGradient id="areaGrad" x1="0%" y1="0%" x2="0%" y2="100%">
                <stop offset="0%" stopColor="#e9d700" />
                <stop offset="100%" stopColor="#e9d700" stopOpacity="0" />
              </linearGradient>
            </defs>

            {/* Curve line */}
            <path
              d={curvePoints.join(' ')}
              stroke="#e9d700"
              strokeWidth="5"
              fill="none"
              strokeLinecap="round"
              strokeLinejoin="round"
              style={{ filter: 'drop-shadow(0 0 8px rgba(233,215,0,0.6))' }}
            />

            {/* End dot */}
            {curveProgress > 0 && (
              <circle
                cx={100 + curveProgress * 1100}
                cy={400 - Math.pow(curveProgress, 2.2) * 320}
                r="12"
                fill="#e9d700"
                style={{ filter: 'drop-shadow(0 0 12px rgba(233,215,0,0.8))' }}
              />
            )}

            {/* Year labels */}
            <text x="100" y="455" fill="#EEEEEE" fontSize="20" fontFamily="JetBrains Mono" opacity="0.6">2026</text>
            <text x="600" y="455" fill="#EEEEEE" fontSize="20" fontFamily="JetBrains Mono" opacity="0.6" textAnchor="middle">+12 YRS</text>
            <text x="1180" y="455" fill="#EEEEEE" fontSize="20" fontFamily="JetBrains Mono" opacity="0.6" textAnchor="end">2051</text>
          </svg>

          {/* Years counter top-left */}
          <div style={{
            position: 'absolute',
            top: 0,
            left: 90,
            fontFamily: 'JetBrains Mono',
            fontSize: 24,
            color: '#EEEEEE',
            opacity: 0.7,
          }}>
            TIME: <span style={{ color: '#e9d700', fontWeight: 700 }}>{yearsCounter} YEARS</span>
          </div>
        </div>

        {/* 56 Lakh Final Reveal - Bottom */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: lakhOp,
          transform: `scale(${lakhScale})`,
        }}>
          <div style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 30,
            padding: '24px 60px',
            background: 'linear-gradient(135deg, rgba(233,215,0,0.2) 0%, rgba(255,193,7,0.15) 100%)',
            border: '3px solid #e9d700',
            borderRadius: 24,
            boxShadow: '0 0 60px rgba(233,215,0,0.3)',
          }}>
            <div style={{ textAlign: 'left' }}>
              <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#EEEEEE', letterSpacing: 3, opacity: 0.8 }}>
                25 साल बाद
              </div>
              <div style={{ fontFamily: 'Yantra One', fontSize: 110, color: '#e9d700', lineHeight: 1 }}>
                ₹{lakhCounter} लाख
              </div>
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 32,
              color: '#EEEEEE',
              borderLeft: '2px solid rgba(238,238,238,0.3)',
              paddingLeft: 30,
              maxWidth: 360,
              lineHeight: 1.3,
            }}>
              सिर्फ ₹3,000 महीने से
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(8,6,22,0.65) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene32;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene33: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera tilt
  const camRot = interpolate(frame, [0, 60], [-2, 0]);

  // Hook reveal
  const hookOp = sp(5);
  const hookY = interpolate(sp(5), [0, 1], [-40, 0]);

  // VS scene
  const leftOp = sp(40);
  const leftX = interpolate(sp(40), [0, 1], [-100, 0]);
  const rightOp = sp(60);
  const rightX = interpolate(sp(60), [0, 1], [100, 0]);

  // VS badge
  const vsScale = sp(90, { damping: 7, stiffness: 130 });
  const vsRotate = Math.sin(frame * 0.05) * 5;

  // Question reveal
  const qOp = interpolate(frame, [180, 220], [0, 1], { extrapolateRight: 'clamp' });
  const qY = interpolate(sp(180), [0, 1], [30, 0]);

  // Math stamp
  const mathOp = interpolate(frame, [300, 340], [0, 1], { extrapolateRight: 'clamp' });
  const mathScale = sp(300, { damping: 6, stiffness: 100 });
  const mathRot = interpolate(sp(300), [0, 1], [-8, -4]);

  // Pulse on choice
  const pulse = 1 + Math.sin(frame * 0.15) * 0.02;

  const particles = Array.from({ length: 32 }, (_, i) => i);
  const shapes = Array.from({ length: 6 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4A4466', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 25% 50%, rgba(255,0,0,0.1) 0%, transparent 45%), radial-gradient(circle at 75% 50%, rgba(233,215,0,0.15) 0%, transparent 45%), linear-gradient(180deg, #4A4466 0%, #3A3556 100%)',
      }} />

      {/* Shapes */}
      {shapes.map((i) => {
        const s = seededRandom(i + 7);
        const size = 220 + s * 320;
        const x = seededRandom(i + 17) * 1920;
        const y = seededRandom(i + 27) * 1080;
        const rot = frame * 0.07 * (i % 2 === 0 ? 1 : -1);
        const breath = 1 + Math.sin(frame * 0.025 + i) * 0.04;
        return (
          <div key={`s33-${i}`} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid rgba(238,238,238,0.05)`,
            borderRadius: i % 3 === 0 ? '0' : '50%',
            transform: `rotate(${rot}deg) scale(${breath})`,
          }} />
        );
      })}

      {/* Diagonal lines texture */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(45deg, transparent, transparent 60px, rgba(238,238,238,0.025) 60px, rgba(238,238,238,0.025) 61px)',
        opacity: 0.8,
      }} />

      {/* Particles */}
      {particles.map((i) => {
        const s = seededRandom(i + 77);
        const px = (s * 1920 + frame * (0.35 + s * 0.6)) % 1920;
        const py = (seededRandom(i + 177) * 1080 + Math.sin(frame * 0.025 + i * 0.5) * 35) % 1080;
        const c = i % 3 === 0 ? '#ff0000' : i % 3 === 1 ? '#e9d700' : '#EEEEEE';
        return (
          <div key={`pt33-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 3,
            height: 3,
            borderRadius: '50%',
            backgroundColor: c,
            opacity: 0.5,
            boxShadow: `0 0 6px ${c}`,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRot}deg)`, transformOrigin: 'center' }}>
        {/* Top Hook Line */}
        <div style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: hookOp,
          transform: `translateY(${hookY}px)`,
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#e9d700', letterSpacing: 6, marginBottom: 8 }}>
            ── एक छोटी सी आदत ──
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 86, color: '#EEEEEE' }}>
            अब बताइए, क्या चुनेंगे?
          </div>
        </div>

        {/* LEFT: Pizza */}
        <div style={{
          position: 'absolute',
          top: 320,
          left: 180,
          width: 600,
          opacity: leftOp,
          transform: `translateX(${leftX}px) scale(${pulse})`,
          textAlign: 'center',
        }}>
          <div style={{
            width: 320,
            height: 320,
            margin: '0 auto',
            background: 'radial-gradient(circle, rgba(255,0,0,0.15) 0%, transparent 70%)',
            borderRadius: '50%',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            border: '4px dashed rgba(255,0,0,0.4)',
          }}>
            <svg width="220" height="220" viewBox="0 0 100 100">
              <circle cx="50" cy="50" r="45" fill="#ff0000" opacity="0.2" />
              <circle cx="50" cy="50" r="42" stroke="#ff0000" strokeWidth="3" fill="none" />
              <path d="M50 8 L88 70 L12 70 Z" fill="rgba(255,0,0,0.3)" />
              <circle cx="40" cy="40" r="5" fill="#ff0000" />
              <circle cx="60" cy="45" r="4" fill="#ff0000" />
              <circle cx="45" cy="58" r="5" fill="#ff0000" />
              <circle cx="58" cy="60" r="4" fill="#ff0000" />
            </svg>
          </div>
          <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#EEEEEE', opacity: 0.6, letterSpacing: 4, marginTop: 16 }}>
            OPTION A
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 72, color: '#ff0000', lineHeight: 1, marginTop: 8 }}>
            एक Pizza
          </div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 36, color: '#EEEEEE', marginTop: 12 }}>
            ₹500 · 30 मिनट
          </div>
        </div>

        {/* VS Badge - Center */}
        <div style={{
          position: 'absolute',
          top: 480,
          left: '50%',
          transform: `translateX(-50%) scale(${vsScale}) rotate(${vsRotate}deg)`,
          width: 140,
          height: 140,
          borderRadius: '50%',
          background: 'linear-gradient(135deg, #080616 0%, #2D2D2D 100%)',
          border: '4px solid #e9d700',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          boxShadow: '0 0 40px rgba(233,215,0,0.5)',
        }}>
          <div style={{ fontFamily: 'Yantra One', fontSize: 56, color: '#e9d700' }}>VS</div>
        </div>

        {/* RIGHT: 56 Lakh */}
        <div style={{
          position: 'absolute',
          top: 320,
          right: 180,
          width: 600,
          opacity: rightOp,
          transform: `translateX(${rightX}px) scale(${pulse})`,
          textAlign: 'center',
        }}>
          <div style={{
            width: 320,
            height: 320,
            margin: '0 auto',
            background: 'radial-gradient(circle, rgba(233,215,0,0.2) 0%, transparent 70%)',
            borderRadius: '50%',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            border: '4px solid #e9d700',
            boxShadow: '0 0 40px rgba(233,215,0,0.3)',
            flexDirection: 'column',
          }}>
            <div style={{ fontFamily: 'Yantra One', fontSize: 110, color: '#e9d700', lineHeight: 1 }}>56</div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 42, color: '#EEEEEE', marginTop: 4 }}>लाख</div>
          </div>
          <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#EEEEEE', opacity: 0.6, letterSpacing: 4, marginTop: 16 }}>
            OPTION B
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 72, color: '#e9d700', lineHeight: 1, marginTop: 8 }}>
            ₹56 लाख
          </div>
          <div style={{ fontFamily: 'JetBrains Mono', fontSize: 36, color: '#EEEEEE', marginTop: 12 }}>
            ₹3K/mo · 25 YEARS
          </div>
        </div>

        {/* Bottom Question */}
        <div style={{
          position: 'absolute',
          bottom: 180,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: qOp,
          transform: `translateY(${qY}px)`,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 50,
            color: '#EEEEEE',
            fontWeight: 700,
          }}>
            एक pizza और एक छप्पन लाख में — <span style={{ color: '#e9d700' }}>क्या चुनेंगे?</span>
          </div>
        </div>

        {/* Math Stamp - Bottom Right */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: '50%',
          transform: `translateX(-50%) rotate(${mathRot}deg) scale(${mathScale})`,
          opacity: mathOp,
          padding: '14px 36px',
          background: 'rgba(8,6,22,0.7)',
          border: '3px solid #EEEEEE',
          borderRadius: 8,
          backdropFilter: 'blur(8px)',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 32,
            color: '#EEEEEE',
            letterSpacing: 4,
            fontWeight: 700,
          }}>
            MATHEMATICS — NOT MAGIC
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(8,6,22,0.65) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene33;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene34: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom out
  const camScale = interpolate(frame, [0, durationInFrames], [1.05, 1]);

  // Heritage tag
  const tagOp = sp(5);
  const tagY = interpolate(sp(5), [0, 1], [-30, 0]);

  // Title reveal
  const titleOp = sp(20);
  const titleScale = interpolate(sp(20), [0, 1], [0.85, 1]);

  // Left panel — eaten grain
  const leftOp = sp(80);
  const leftY = interpolate(sp(80), [0, 1], [50, 0]);

  // Right panel — sown grain
  const rightOp = sp(120);
  const rightY = interpolate(sp(120), [0, 1], [50, 0]);

  // Growing tree on right
  const treeProgress = interpolate(frame, [200, 360], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // 100x reveal
  const multOp = interpolate(frame, [320, 360], [0, 1], { extrapolateRight: 'clamp' });
  const multScale = sp(320, { damping: 7 });

  // Bottom wisdom
  const wisdomOp = interpolate(frame, [400, 440], [0, 1], { extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 28 }, (_, i) => i);
  const shapes = Array.from({ length: 5 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4A4466', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 100%, rgba(233,215,0,0.12) 0%, transparent 60%), linear-gradient(180deg, #4A4466 0%, #2D2A40 100%)',
      }} />

      {/* Shapes */}
      {shapes.map((i) => {
        const s = seededRandom(i + 9);
        const size = 280 + s * 320;
        const x = seededRandom(i + 19) * 1920;
        const y = seededRandom(i + 29) * 1080;
        const rot = frame * 0.06 * (i % 2 === 0 ? 1 : -1);
        return (
          <div key={`s34-${i}`} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid rgba(238,238,238,0.05)`,
            borderRadius: i % 2 === 0 ? '50%' : '0',
            transform: `rotate(${rot}deg)`,
          }} />
        );
      })}

      {/* Horizontal lines */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(0deg, transparent, transparent 90px, rgba(238,238,238,0.03) 90px, rgba(238,238,238,0.03) 91px)',
      }} />

      {/* Particles - floating grain dust */}
      {particles.map((i) => {
        const s = seededRandom(i + 99);
        const px = (s * 1920 + frame * (0.3 + s * 0.5)) % 1920;
        const py = (seededRandom(i + 199) * 1080 - frame * 0.4 + 1080) % 1080;
        return (
          <div key={`pt34-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 4,
            height: 4,
            borderRadius: '50%',
            backgroundColor: '#e9d700',
            opacity: 0.4,
            boxShadow: '0 0 8px #e9d700',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center' }}>
        {/* Heritage Tag */}
        <div style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: tagOp,
          transform: `translateY(${tagY}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            padding: '8px 30px',
            background: 'rgba(233,215,0,0.15)',
            border: '2px solid #e9d700',
            borderRadius: 30,
            fontFamily: 'Inter',
            fontSize: 24,
            color: '#e9d700',
            letterSpacing: 5,
            fontWeight: 600,
          }}>
            पुराना किसानी सिद्धांत
          </div>
        </div>

        {/* Main Title */}
        <div style={{
          position: 'absolute',
          top: 130,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleOp,
          transform: `scale(${titleScale})`,
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 80,
            color: '#EEEEEE',
            lineHeight: 1.1,
          }}>
            खेत में सबसे कीमती <span style={{ color: '#e9d700' }}>दाना</span>
          </div>
        </div>

        {/* LEFT - Eaten Grain */}
        <div style={{
          position: 'absolute',
          top: 340,
          left: 140,
          width: 720,
          opacity: leftOp,
          transform: `translateY(${leftY}px)`,
          padding: 40,
          background: 'rgba(255,0,0,0.08)',
          border: '2px solid rgba(255,0,0,0.4)',
          borderRadius: 24,
        }}>
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 16,
            marginBottom: 24,
          }}>
            <div style={{
              width: 50,
              height: 50,
              borderRadius: '50%',
              background: '#ff0000',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Yantra One',
              fontSize: 28,
              color: '#EEEEEE',
            }}>
              1
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 26, color: '#EEEEEE', letterSpacing: 4, opacity: 0.7 }}>
              CONSUMPTION
            </div>
          </div>

          <div style={{ fontFamily: 'Yantra One', fontSize: 64, color: '#EEEEEE', lineHeight: 1.15, marginBottom: 24 }}>
            खाया हुआ <span style={{ color: '#ff0000' }}>दाना</span>
          </div>

          {/* Visual: single grain → empty */}
          <div style={{ display: 'flex', alignItems: 'center', gap: 20, marginBottom: 24 }}>
            <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
              <ellipse cx="12" cy="12" rx="5" ry="9" fill="#e9d700" />
              <line x1="12" y1="3" x2="12" y2="21" stroke="#080616" strokeWidth="0.8" />
            </svg>
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
              <path d="M5 12h14M14 7l5 5-5 5" stroke="#EEEEEE" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" opacity="0.5" />
            </svg>
            <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
              <circle cx="12" cy="12" r="10" stroke="#ff0000" strokeWidth="2" strokeDasharray="3 3" />
              <path d="M8 8l8 8M16 8l-8 8" stroke="#ff0000" strokeWidth="2" strokeLinecap="round" />
            </svg>
          </div>

          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 32, color: '#EEEEEE', opacity: 0.85, lineHeight: 1.3 }}>
            एक बार में खत्म।
          </div>
        </div>

        {/* RIGHT - Sown Grain */}
        <div style={{
          position: 'absolute',
          top: 340,
          right: 140,
          width: 720,
          opacity: rightOp,
          transform: `translateY(${rightY}px)`,
          padding: 40,
          background: 'rgba(233,215,0,0.1)',
          border: '3px solid #e9d700',
          borderRadius: 24,
          boxShadow: '0 0 40px rgba(233,215,0,0.2)',
        }}>
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 16,
            marginBottom: 24,
          }}>
            <div style={{
              width: 50,
              height: 50,
              borderRadius: '50%',
              background: '#e9d700',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              fontFamily: 'Yantra One',
              fontSize: 28,
              color: '#080616',
            }}>
              2
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 26, color: '#e9d700', letterSpacing: 4, fontWeight: 600 }}>
              INVESTMENT
            </div>
          </div>

          <div style={{ fontFamily: 'Yantra One', fontSize: 64, color: '#EEEEEE', lineHeight: 1.15, marginBottom: 24 }}>
            बोया हुआ <span style={{ color: '#e9d700' }}>दाना</span>
          </div>

          {/* Visual: grain → growing tree */}
          <div style={{ display: 'flex', alignItems: 'flex-end', gap: 16, height: 100, marginBottom: 16 }}>
            <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
              <ellipse cx="12" cy="12" rx="5" ry="9" fill="#e9d700" />
            </svg>
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 10 }}>
              <path d="M5 12h14M14 7l5 5-5 5" stroke="#e9d700" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
            </svg>
            {/* Growing tree */}
            <svg width={80 + treeProgress * 60} height={80 + treeProgress * 60} viewBox="0 0 60 80">
              <rect x="27" y="50" width="6" height={30 * treeProgress} fill="#9E9E9E" />
              <circle cx="30" cy="40" r={5 + treeProgress * 8} fill="#e9d700" opacity="0.4" />
              <circle cx="20" cy="35" r={4 + treeProgress * 7} fill="#e9d700" opacity="0.5" />
              <circle cx="40" cy="35" r={4 + treeProgress * 7} fill="#e9d700" opacity="0.5" />
              <circle cx="30" cy="25" r={5 + treeProgress * 9} fill="#e9d700" />
              <circle cx="22" cy="22" r={3 + treeProgress * 5} fill="#FFC107" />
              <circle cx="38" cy="22" r={3 + treeProgress * 5} fill="#FFC107" />
            </svg>
          </div>

          <div style={{
            display: 'flex',
            alignItems: 'baseline',
            gap: 12,
            opacity: multOp,
            transform: `scale(${multScale})`,
            transformOrigin: 'left',
          }}>
            <div style={{ fontFamily: 'Yantra One', fontSize: 90, color: '#e9d700', lineHeight: 1 }}>100×</div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 28, color: '#EEEEEE', fontWeight: 600 }}>
              होकर लौटता है
            </div>
          </div>
        </div>

        {/* Bottom Wisdom */}
        <div style={{
          position: 'absolute',
          bottom: 50,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: wisdomOp,
        }}>
          <div style={{
            display: 'inline-block',
            padding: '14px 50px',
            background: 'rgba(8,6,22,0.6)',
            border: '1px solid rgba(238,238,238,0.2)',
            borderRadius: 50,
            backdropFilter: 'blur(8px)',
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 36,
            color: '#EEEEEE',
            fontWeight: 600,
          }}>
            कौन से दाने आप <span style={{ color: '#ff0000' }}>खा रहे हो</span>, कौन से <span style={{ color: '#e9d700' }}>बो रहे हो</span>?
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(8,6,22,0.65) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene34;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera drift
  const camY = interpolate(frame, [0, durationInFrames], [10, -10]);

  // Multiplication waves - 1× → 100×
  const counterValue = Math.floor(interpolate(frame, [10, 160], [1, 100], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));
  const counterScale = sp(10, { damping: 9 });
  const counterPulse = 1 + Math.sin(frame * 0.18) * 0.03;

  // Rule banner reveal
  const banOp = interpolate(frame, [200, 240], [0, 1], { extrapolateRight: 'clamp' });
  const banScale = sp(200, { damping: 10, stiffness: 110 });

  // Buffett name plate
  const nameOp = interpolate(frame, [280, 320], [0, 1], { extrapolateRight: 'clamp' });
  const nameY = interpolate(sp(280), [0, 1], [40, 0]);

  // Quote setup tag
  const ruleOp = interpolate(frame, [380, 420], [0, 1], { extrapolateRight: 'clamp' });

  // Concentric pulse rings around 100×
  const ring1 = (frame * 3) % 200;
  const ring2 = ((frame * 3) + 70) % 200;
  const ring3 = ((frame * 3) + 140) % 200;

  const particles = Array.from({ length: 36 }, (_, i) => i);
  const shapes = Array.from({ length: 6 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4A4466', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 40%, rgba(233,215,0,0.15) 0%, transparent 50%), linear-gradient(135deg, #4A4466 0%, #38334F 100%)',
      }} />

      {/* Shapes */}
      {shapes.map((i) => {
        const s = seededRandom(i + 11);
        const size = 260 + s * 360;
        const x = seededRandom(i + 21) * 1920;
        const y = seededRandom(i + 31) * 1080;
        const rot = frame * 0.08 * (i % 2 === 0 ? 1 : -1);
        const breath = 1 + Math.sin(frame * 0.03 + i) * 0.05;
        return (
          <div key={`s35-${i}`} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid rgba(238,238,238,0.05)`,
            borderRadius: i % 3 === 0 ? '50%' : '20%',
            transform: `rotate(${rot}deg) scale(${breath})`,
          }} />
        );
      })}

      {/* Grid */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(rgba(238,238,238,0.04) 1px, transparent 1px), linear-gradient(90deg, rgba(238,238,238,0.04) 1px, transparent 1px)',
        backgroundSize: '100px 100px',
        opacity: 0.7,
      }} />

      {/* Particles */}
      {particles.map((i) => {
        const s = seededRandom(i + 111);
        const px = (s * 1920 + frame * (0.4 + s)) % 1920;
        const py = (seededRandom(i + 211) * 1080 + Math.cos(frame * 0.02 + i) * 30) % 1080;
        return (
          <div key={`pt35-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 3,
            height: 3,
            borderRadius: '50%',
            backgroundColor: '#e9d700',
            opacity: 0.45,
            boxShadow: '0 0 8px #e9d700',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        {/* Top Tag */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 26,
            color: '#EEEEEE',
            letterSpacing: 8,
            opacity: 0.7,
          }}>
            COMPOUNDING — THE EIGHTH WONDER
          </div>
        </div>

        {/* Center 100× with pulse rings */}
        <div style={{
          position: 'absolute',
          top: 200,
          left: '50%',
          transform: `translateX(-50%)`,
          width: 700,
          height: 500,
        }}>
          {/* Pulse rings */}
          {[ring1, ring2, ring3].map((r, i) => (
            <div key={`ring-${i}`} style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              width: 200 + r * 2,
              height: 200 + r * 2,
              transform: 'translate(-50%, -50%)',
              borderRadius: '50%',
              border: '2px solid #e9d700',
              opacity: 1 - r / 200,
            }} />
          ))}

          {/* Center number */}
          <div style={{
            position: 'absolute',
            top: '50%',
            left: '50%',
            transform: `translate(-50%, -50%) scale(${counterScale * counterPulse})`,
            textAlign: 'center',
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#EEEEEE', letterSpacing: 4, opacity: 0.8, marginBottom: 4 }}>
              बोया हुआ दाना
            </div>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 280,
              color: '#e9d700',
              lineHeight: 1,
              textShadow: '0 0 50px rgba(233,215,0,0.6)',
            }}>
              {counterValue}×
            </div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 36, color: '#EEEEEE', marginTop: 8, fontWeight: 700 }}>
              होकर लौटता है
            </div>
          </div>
        </div>

        {/* Rule Banner - Bottom */}
        <div style={{
          position: 'absolute',
          bottom: 200,
          left: '50%',
          transform: `translateX(-50%) scale(${banScale})`,
          opacity: banOp,
          display: 'flex',
          alignItems: 'center',
          gap: 24,
          padding: '20px 50px',
          background: 'linear-gradient(135deg, #080616 0%, #2D2D2D 100%)',
          border: '3px solid #e9d700',
          borderRadius: 16,
        }}>
          <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
            <circle cx="12" cy="12" r="10" stroke="#e9d700" strokeWidth="2" />
            <path d="M9 12l2 2 4-4" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div>
            <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#e9d700', letterSpacing: 5, fontWeight: 600 }}>
              BUFFETT'S FAMOUS RULE
            </div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 54, color: '#EEEEEE', lineHeight: 1.1, marginTop: 4 }}>
              एक मशहूर सिद्धांत आ रहा है...
            </div>
          </div>
        </div>

        {/* Buffett Name Plate - Bottom */}
        <div style={{
          position: 'absolute',
          bottom: 90,
          left: '50%',
          transform: `translateX(-50%) translateY(${nameY}px)`,
          opacity: nameOp,
          display: 'flex',
          alignItems: 'center',
          gap: 20,
        }}>
          <div style={{
            width: 70,
            height: 70,
            borderRadius: '50%',
            background: 'linear-gradient(135deg, #FFC107 0%, #e9d700 100%)',
            border: '3px solid #EEEEEE',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            fontFamily: 'Yantra One',
            fontSize: 36,
            color: '#080616',
          }}>
            WB
          </div>
          <div>
            <div style={{ fontFamily: 'Inter', fontSize: 32, color: '#EEEEEE', fontWeight: 700 }}>
              WARREN BUFFETT
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 20, color: '#9E9E9E', letterSpacing: 3, opacity: 0.9 }}>
              ORACLE OF OMAHA · BERKSHIRE HATHAWAY
            </div>
          </div>
        </div>

        {/* Left side number line */}
        <div style={{
          position: 'absolute',
          left: 100,
          top: 380,
          fontFamily: 'JetBrains Mono',
          fontSize: 36,
          color: '#EEEEEE',
          opacity: 0.6,
        }}>
          1<br />×
        </div>
        <svg style={{ position: 'absolute', left: 160, top: 410 }} width="200" height="40" viewBox="0 0 200 40">
          <path d="M0 20 L180 20 M170 10 L180 20 L170 30" stroke="#EEEEEE" strokeWidth="2" fill="none" opacity="0.5" strokeLinecap="round" />
        </svg>

        {/* Right side TIME label */}
        <div style={{
          position: 'absolute',
          right: 100,
          top: 380,
          fontFamily: 'Inter',
          fontSize: 24,
          color: '#EEEEEE',
          opacity: 0.6,
          letterSpacing: 4,
          textAlign: 'right',
        }}>
          TIME +<br />PATIENCE
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(8,6,22,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene35;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene36: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.06]);

  // WRONG flow
  const wrongOp = sp(10);
  const wrongX = interpolate(sp(10), [0, 1], [-80, 0]);

  // Strike-through animation
  const strikeProgress = interpolate(frame, [80, 130], [0, 1], { extrapolateRight: 'clamp' });

  // Flip transition
  const flipScale = sp(160, { damping: 9, stiffness: 110 });
  const flipRot = interpolate(sp(160), [0, 1], [180, 0]);

  // RIGHT flow
  const rightOp = sp(180);
  const rightX = interpolate(sp(180), [0, 1], [80, 0]);

  // Stamp
  const stampOp = interpolate(frame, [280, 320], [0, 1], { extrapolateRight: 'clamp' });
  const stampScale = sp(280, { damping: 6, stiffness: 110 });
  const stampRot = interpolate(sp(280), [0, 1], [-15, -8]);

  // Bottom payoff
  const payoffOp = interpolate(frame, [380, 420], [0, 1], { extrapolateRight: 'clamp' });
  const payoffY = interpolate(sp(380), [0, 1], [30, 0]);

  // Battle bar progress
  const battleProgress = interpolate(frame, [400, 480], [0, 0.5], { extrapolateRight: 'clamp' });

  const particles = Array.from({ length: 30 }, (_, i) => i);
  const shapes = Array.from({ length: 5 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4A4466', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(255,0,0,0.08) 0%, transparent 50%, rgba(233,215,0,0.12) 100%), linear-gradient(180deg, #4A4466 0%, #3A3556 100%)',
      }} />

      {/* Shapes */}
      {shapes.map((i) => {
        const s = seededRandom(i + 13);
        const size = 250 + s * 320;
        const x = seededRandom(i + 23) * 1920;
        const y = seededRandom(i + 33) * 1080;
        const rot = frame * 0.06 * (i % 2 === 0 ? 1 : -1);
        return (
          <div key={`s36-${i}`} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid rgba(238,238,238,0.04)`,
            borderRadius: i % 2 === 0 ? '50%' : '0',
            transform: `rotate(${rot}deg)`,
          }} />
        );
      })}

      {/* Dot pattern */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(circle, rgba(238,238,238,0.08) 1px, transparent 1px)',
        backgroundSize: '60px 60px',
      }} />

      {/* Particles */}
      {particles.map((i) => {
        const s = seededRandom(i + 133);
        const px = (s * 1920 + frame * (0.3 + s * 0.6)) % 1920;
        const py = (seededRandom(i + 233) * 1080 + Math.sin(frame * 0.025 + i) * 30) % 1080;
        const c = px < 960 ? '#ff0000' : '#e9d700';
        return (
          <div key={`pt36-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 3,
            height: 3,
            borderRadius: '50%',
            backgroundColor: c,
            opacity: 0.4,
            boxShadow: `0 0 6px ${c}`,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center' }}>
        {/* Top Title */}
        <div style={{
          position: 'absolute',
          top: 50,
          left: 0,
          right: 0,
          textAlign: 'center',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 26, color: '#e9d700', letterSpacing: 6, marginBottom: 6 }}>
            ── BUFFETT'S CORE PRINCIPLE ──
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 72, color: '#EEEEEE' }}>
            एक sentence — आधी लड़ाई
          </div>
        </div>

        {/* LEFT - WRONG WAY */}
        <div style={{
          position: 'absolute',
          top: 240,
          left: 100,
          width: 800,
          opacity: wrongOp,
          transform: `translateX(${wrongX}px)`,
        }}>
          <div style={{
            padding: '32px 40px',
            background: 'rgba(255,0,0,0.1)',
            border: '2px solid rgba(255,0,0,0.4)',
            borderRadius: 20,
            position: 'relative',
          }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginBottom: 20 }}>
              <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
                <circle cx="12" cy="12" r="10" stroke="#ff0000" strokeWidth="2.5" />
                <path d="M8 8l8 8M16 8l-8 8" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" />
              </svg>
              <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#ff0000', letterSpacing: 5, fontWeight: 700 }}>
                गलत तरीका
              </div>
            </div>

            <div style={{ position: 'relative' }}>
              <div style={{ fontFamily: 'Yantra One', fontSize: 48, color: '#EEEEEE', lineHeight: 1.3 }}>
                कमाई <span style={{ color: '#e9d700' }}>→</span> खर्च <span style={{ color: '#e9d700' }}>→</span> जो बचे उसे बचाओ
              </div>
              {/* Strike line */}
              <div style={{
                position: 'absolute',
                top: '50%',
                left: 0,
                width: `${strikeProgress * 100}%`,
                height: 5,
                background: '#ff0000',
                transform: 'translateY(-50%)',
                borderRadius: 3,
                boxShadow: '0 0 10px rgba(255,0,0,0.6)',
              }} />
            </div>

            {/* Flow boxes */}
            <div style={{ display: 'flex', gap: 12, marginTop: 28, alignItems: 'center' }}>
              <div style={{ padding: '12px 20px', background: 'rgba(238,238,238,0.1)', borderRadius: 10, fontFamily: 'Inter', fontSize: 22, color: '#EEEEEE', flex: 1, textAlign: 'center' }}>
                INCOME<br /><span style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: '#e9d700' }}>₹50K</span>
              </div>
              <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#EEEEEE' }}>→</div>
              <div style={{ padding: '12px 20px', background: 'rgba(255,0,0,0.15)', borderRadius: 10, fontFamily: 'Inter', fontSize: 22, color: '#EEEEEE', flex: 1.4, textAlign: 'center' }}>
                EXPENSES<br /><span style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: '#ff0000' }}>₹48K</span>
              </div>
              <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#EEEEEE' }}>→</div>
              <div style={{ padding: '12px 20px', background: 'rgba(158,158,158,0.15)', borderRadius: 10, fontFamily: 'Inter', fontSize: 22, color: '#EEEEEE', flex: 0.8, textAlign: 'center' }}>
                SAVE<br /><span style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: '#9E9E9E' }}>₹2K</span>
              </div>
            </div>
          </div>
        </div>

        {/* Flip arrow center */}
        <div style={{
          position: 'absolute',
          top: 480,
          left: '50%',
          transform: `translateX(-50%) scale(${flipScale}) rotateY(${flipRot}deg)`,
          width: 120,
          height: 120,
          borderRadius: '50%',
          background: 'linear-gradient(135deg, #e9d700 0%, #FFC107 100%)',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          boxShadow: '0 0 50px rgba(233,215,0,0.6)',
          zIndex: 5,
        }}>
          <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
            <path d="M3 7h13M13 4l3 3-3 3M21 17H8M11 14l-3 3 3 3" stroke="#080616" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>

        {/* RIGHT - CORRECT WAY */}
        <div style={{
          position: 'absolute',
          top: 240,
          right: 100,
          width: 800,
          opacity: rightOp,
          transform: `translateX(${rightX}px)`,
        }}>
          <div style={{
            padding: '32px 40px',
            background: 'rgba(233,215,0,0.12)',
            border: '3px solid #e9d700',
            borderRadius: 20,
            boxShadow: '0 0 40px rgba(233,215,0,0.25)',
          }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginBottom: 20 }}>
              <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
                <circle cx="12" cy="12" r="10" stroke="#e9d700" strokeWidth="2.5" />
                <path d="M8 12l3 3 5-6" stroke="#e9d700" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
              <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#e9d700', letterSpacing: 5, fontWeight: 700 }}>
                सही तरीका
              </div>
            </div>

            <div style={{ fontFamily: 'Yantra One', fontSize: 48, color: '#EEEEEE', lineHeight: 1.3 }}>
              कमाई <span style={{ color: '#e9d700' }}>→</span> बचत <span style={{ color: '#e9d700' }}>→</span> जो बचे उसे खर्च करो
            </div>

            {/* Flow boxes */}
            <div style={{ display: 'flex', gap: 12, marginTop: 28, alignItems: 'center' }}>
              <div style={{ padding: '12px 20px', background: 'rgba(238,238,238,0.1)', borderRadius: 10, fontFamily: 'Inter', fontSize: 22, color: '#EEEEEE', flex: 1, textAlign: 'center' }}>
                INCOME<br /><span style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: '#e9d700' }}>₹50K</span>
              </div>
              <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#EEEEEE' }}>→</div>
              <div style={{ padding: '12px 20px', background: 'rgba(233,215,0,0.2)', borderRadius: 10, fontFamily: 'Inter', fontSize: 22, color: '#EEEEEE', flex: 1, textAlign: 'center' }}>
                SAVE FIRST<br /><span style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: '#e9d700' }}>₹15K</span>
              </div>
              <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#EEEEEE' }}>→</div>
              <div style={{ padding: '12px 20px', background: 'rgba(238,238,238,0.1)', borderRadius: 10, fontFamily: 'Inter', fontSize: 22, color: '#EEEEEE', flex: 1.2, textAlign: 'center' }}>
                SPEND<br /><span style={{ fontFamily: 'JetBrains Mono', fontSize: 26, color: '#EEEEEE' }}>₹35K</span>
              </div>
            </div>
          </div>
        </div>

        {/* PAY YOURSELF FIRST stamp */}
        <div style={{
          position: 'absolute',
          top: 460,
          right: 120,
          transform: `rotate(${stampRot}deg) scale(${stampScale})`,
          opacity: stampOp,
          padding: '12px 30px',
          background: 'rgba(8,6,22,0.85)',
          border: '3px solid #e9d700',
          borderRadius: 8,
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 24, color: '#e9d700', letterSpacing: 3, fontWeight: 700 }}>
            PAY YOURSELF FIRST
          </div>
        </div>

        {/* Bottom Payoff Bar */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: 100,
          right: 100,
          opacity: payoffOp,
          transform: `translateY(${payoffY}px)`,
        }}>
          <div style={{
            display: 'flex',
            justifyContent: 'space-between',
            alignItems: 'center',
            marginBottom: 12,
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 24, color: '#EEEEEE', letterSpacing: 3, opacity: 0.8 }}>
              आधी लड़ाई जीत जाओगे
            </div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 28, color: '#e9d700', fontWeight: 700 }}>
              {Math.floor(battleProgress * 100)}% WON
            </div>
          </div>
          <div style={{
            height: 30,
            background: 'rgba(8,6,22,0.6)',
            borderRadius: 15,
            border: '2px solid rgba(238,238,238,0.2)',
            overflow: 'hidden',
            position: 'relative',
          }}>
            <div style={{
              height: '100%',
              width: `${battleProgress * 100}%`,
              background: 'linear-gradient(90deg, #e9d700 0%, #FFC107 100%)',
              borderRadius: 15,
              boxShadow: '0 0 20px rgba(233,215,0,0.6)',
              transition: 'width 0.3s',
            }} />
            {/* Center marker */}
            <div style={{
              position: 'absolute',
              top: -8,
              left: '50%',
              transform: 'translateX(-50%)',
              width: 4,
              height: 46,
              background: '#EEEEEE',
              borderRadius: 2,
            }} />
            <div style={{
              position: 'absolute',
              top: -34,
              left: '50%',
              transform: 'translateX(-50%)',
              fontFamily: 'JetBrains Mono',
              fontSize: 18,
              color: '#EEEEEE',
              opacity: 0.7,
            }}>
              50%
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(8,6,22,0.65) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene36;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene37: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera shake on chapter mark
  const camShake = frame < 30 ? Math.sin(frame * 1.5) * 3 : 0;

  // Chapter mark "4"
  const fourScale = sp(0, { damping: 8, stiffness: 130 });
  const fourOp = interpolate(frame, [0, 40], [0, 0.15], { extrapolateRight: 'clamp' });

  // Chapter label
  const labelOp = sp(20);

  // Quote bubble reveal
  const quoteOp = sp(60);
  const quoteScale = interpolate(sp(60), [0, 1], [0.92, 1]);

  // Skill label scratched out
  const skillScratch = interpolate(frame, [180, 230], [0, 1], { extrapolateRight: 'clamp' });

  // Identity reveal
  const idOp = interpolate(frame, [240, 290], [0, 1], { extrapolateRight: 'clamp' });
  const idScale = sp(240, { damping: 7, stiffness: 120 });

  // Tag self stamp
  const tagOp = interpolate(frame, [340, 380], [0, 1], { extrapolateRight: 'clamp' });
  const tagY = interpolate(sp(340), [0, 1], [30, 0]);

  const particles = Array.from({ length: 30 }, (_, i) => i);
  const shapes = Array.from({ length: 5 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4A4466', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, rgba(238,238,238,0.06) 0%, transparent 60%), linear-gradient(135deg, #4A4466 0%, #38334F 100%)',
      }} />

      {/* Giant 4 in background */}
      <div style={{
        position: 'absolute',
        top: '50%',
        left: '50%',
        transform: `translate(-50%, -50%) scale(${fourScale})`,
        fontFamily: 'Yantra One',
        fontSize: 1400,
        color: '#EEEEEE',
        opacity: fourOp,
        lineHeight: 1,
        pointerEvents: 'none',
        userSelect: 'none',
      }}>
        04
      </div>

      {/* Shapes */}
      {shapes.map((i) => {
        const s = seededRandom(i + 15);
        const size = 240 + s * 320;
        const x = seededRandom(i + 25) * 1920;
        const y = seededRandom(i + 35) * 1080;
        const rot = frame * 0.07 * (i % 2 === 0 ? 1 : -1);
        return (
          <div key={`s37-${i}`} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid rgba(238,238,238,0.04)`,
            borderRadius: i % 2 === 0 ? '50%' : '0',
            transform: `rotate(${rot}deg)`,
          }} />
        );
      })}

      {/* Texture */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(90deg, rgba(238,238,238,0.025) 1px, transparent 1px)',
        backgroundSize: '40px 100%',
      }} />

      {/* Particles */}
      {particles.map((i) => {
        const s = seededRandom(i + 155);
        const px = (s * 1920 + frame * (0.3 + s * 0.4)) % 1920;
        const py = (seededRandom(i + 255) * 1080 + Math.cos(frame * 0.02 + i) * 25) % 1080;
        return (
          <div key={`pt37-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 3,
            height: 3,
            borderRadius: '50%',
            backgroundColor: '#EEEEEE',
            opacity: 0.35,
            boxShadow: '0 0 6px #EEEEEE',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camShake}px)` }}>
        {/* Chapter Label */}
        <div style={{
          position: 'absolute',
          top: 70,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: labelOp,
        }}>
          <div style={{
            display: 'inline-block',
            padding: '10px 30px',
            background: '#e9d700',
            color: '#080616',
            fontFamily: 'Inter',
            fontSize: 26,
            letterSpacing: 6,
            fontWeight: 700,
            borderRadius: 4,
            transform: 'rotate(-1deg)',
          }}>
            चौथी ज़ंजीर — IDENTITY TRAP
          </div>
        </div>

        {/* Quote Bubble - Center Top */}
        <div style={{
          position: 'absolute',
          top: 200,
          left: '50%',
          transform: `translateX(-50%) scale(${quoteScale})`,
          opacity: quoteOp,
          width: 1200,
        }}>
          <div style={{
            background: 'rgba(8,6,22,0.7)',
            border: '2px solid rgba(238,238,238,0.3)',
            borderRadius: 24,
            padding: '36px 60px',
            position: 'relative',
            backdropFilter: 'blur(10px)',
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E', letterSpacing: 4, marginBottom: 16 }}>
              ── रोज़ बोली जाने वाली लाइन ──
            </div>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 78,
              color: '#EEEEEE',
              lineHeight: 1.2,
              textAlign: 'center',
            }}>
              "मुझे पैसे की समझ नहीं है,<br />ये सब अपने बस का नहीं।"
            </div>
            {/* Speech bubble pointer */}
            <div style={{
              position: 'absolute',
              bottom: -22,
              left: '50%',
              transform: 'translateX(-50%) rotate(45deg)',
              width: 40,
              height: 40,
              background: 'rgba(8,6,22,0.7)',
              borderRight: '2px solid rgba(238,238,238,0.3)',
              borderBottom: '2px solid rgba(238,238,238,0.3)',
            }} />
          </div>
        </div>

        {/* Reveal Boxes - SKILL vs IDENTITY */}
        <div style={{
          position: 'absolute',
          bottom: 220,
          left: 0,
          right: 0,
          display: 'flex',
          justifyContent: 'center',
          gap: 40,
        }}>
          {/* NOT SKILL */}
          <div style={{
            padding: '28px 50px',
            background: 'rgba(255,0,0,0.12)',
            border: '2px solid rgba(255,0,0,0.5)',
            borderRadius: 16,
            position: 'relative',
            minWidth: 480,
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#ff0000', letterSpacing: 4, marginBottom: 8, fontWeight: 600 }}>
              NOT A
            </div>
            <div style={{ position: 'relative', display: 'inline-block' }}>
              <div style={{ fontFamily: 'Yantra One', fontSize: 96, color: '#EEEEEE', lineHeight: 1 }}>
                SKILL
              </div>
              <div style={{
                position: 'absolute',
                top: '55%',
                left: -10,
                width: `${skillScratch * 110}%`,
                height: 6,
                background: '#ff0000',
                transform: 'translateY(-50%) rotate(-3deg)',
                borderRadius: 3,
                boxShadow: '0 0 12px rgba(255,0,0,0.7)',
              }} />
            </div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 24, color: '#EEEEEE', opacity: 0.7, marginTop: 4 }}>
              जो सीखी जा सकती है
            </div>
          </div>

          {/* IS IDENTITY */}
          <div style={{
            padding: '28px 50px',
            background: 'rgba(233,215,0,0.15)',
            border: '3px solid #e9d700',
            borderRadius: 16,
            position: 'relative',
            minWidth: 480,
            opacity: idOp,
            transform: `scale(${idScale})`,
            boxShadow: '0 0 40px rgba(233,215,0,0.3)',
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#e9d700', letterSpacing: 4, marginBottom: 8, fontWeight: 600 }}>
              BUT AN
            </div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 96, color: '#e9d700', lineHeight: 1, textShadow: '0 0 20px rgba(233,215,0,0.5)' }}>
              IDENTITY
            </div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 24, color: '#EEEEEE', opacity: 0.85, marginTop: 4 }}>
              जो आपने खुद को दे दी है
            </div>
          </div>
        </div>

        {/* Bottom Warning Tag */}
        <div style={{
          position: 'absolute',
          bottom: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: tagOp,
          transform: `translateY(${tagY}px)`,
        }}>
          <div style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 16,
            padding: '14px 36px',
            background: 'rgba(8,6,22,0.7)',
            border: '2px solid #ff0000',
            borderRadius: 50,
          }}>
            <svg width="36" height="36" viewBox="0 0 24 24" fill="none">
              <path d="M12 2L2 22h20L12 2z" stroke="#ff0000" strokeWidth="2.5" strokeLinejoin="round" />
              <path d="M12 9v6M12 18v.5" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" />
            </svg>
            <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#EEEEEE', letterSpacing: 3, fontWeight: 600 }}>
              IDENTITY = DANGEROUS LABEL YOU GAVE YOURSELF
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(8,6,22,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene37;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene38: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.04, 1]);

  // Declaration scroll
  const declarOp = sp(5);
  const declarY = interpolate(sp(5), [0, 1], [-40, 0]);

  // Brain illustration
  const brainOp = sp(50);
  const brainScale = sp(50, { damping: 8, stiffness: 110 });

  // Defense shield
  const shieldOp = sp(120);
  const shieldRot = frame * 0.5;

  // Loop arrows pulse
  const loopPulse = 1 + Math.sin(frame * 0.15) * 0.05;

  // Identity tag
  const idTagOp = interpolate(frame, [200, 240], [0, 1], { extrapolateRight: 'clamp' });

  // Bottom result
  const resultOp = interpolate(frame, [280, 320], [0, 1], { extrapolateRight: 'clamp' });
  const resultY = interpolate(sp(280), [0, 1], [30, 0]);

  const particles = Array.from({ length: 28 }, (_, i) => i);
  const shapes = Array.from({ length: 5 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4A4466', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 45%, rgba(255,0,0,0.1) 0%, transparent 50%), linear-gradient(180deg, #4A4466 0%, #38334F 100%)',
      }} />

      {/* Shapes */}
      {shapes.map((i) => {
        const s = seededRandom(i + 17);
        const size = 240 + s * 340;
        const x = seededRandom(i + 27) * 1920;
        const y = seededRandom(i + 37) * 1080;
        const rot = frame * 0.06 * (i % 2 === 0 ? 1 : -1);
        return (
          <div key={`s38-${i}`} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid rgba(238,238,238,0.04)`,
            borderRadius: i % 3 === 0 ? '50%' : '15%',
            transform: `rotate(${rot}deg)`,
          }} />
        );
      })}

      {/* Concentric circles texture */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(circle at center, transparent 200px, rgba(238,238,238,0.03) 201px, rgba(238,238,238,0.03) 202px, transparent 203px), radial-gradient(circle at center, transparent 400px, rgba(238,238,238,0.03) 401px, rgba(238,238,238,0.03) 402px, transparent 403px), radial-gradient(circle at center, transparent 600px, rgba(238,238,238,0.03) 601px, rgba(238,238,238,0.03) 602px, transparent 603px)',
      }} />

      {/* Particles */}
      {particles.map((i) => {
        const s = seededRandom(i + 177);
        const px = (s * 1920 + frame * (0.3 + s)) % 1920;
        const py = (seededRandom(i + 277) * 1080 + Math.sin(frame * 0.02 + i) * 30) % 1080;
        return (
          <div key={`pt38-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 3,
            height: 3,
            borderRadius: '50%',
            backgroundColor: i % 2 === 0 ? '#ff0000' : '#EEEEEE',
            opacity: 0.4,
            boxShadow: '0 0 6px currentColor',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center' }}>
        {/* Top Declaration Quote */}
        <div style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: declarOp,
          transform: `translateY(${declarY}px)`,
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 24, color: '#9E9E9E', letterSpacing: 5, marginBottom: 8 }}>
            THE DECLARATION YOU MADE
          </div>
          <div style={{
            display: 'inline-block',
            padding: '14px 40px',
            background: 'rgba(255,0,0,0.12)',
            border: '2px solid #ff0000',
            borderRadius: 12,
            fontFamily: 'Yantra One',
            fontSize: 56,
            color: '#EEEEEE',
          }}>
            "मैं पैसे का आदमी नहीं हूँ"
          </div>
        </div>

        {/* Center Brain with Shield */}
        <div style={{
          position: 'absolute',
          top: 260,
          left: '50%',
          transform: `translateX(-50%) scale(${brainScale * loopPulse})`,
          opacity: brainOp,
          width: 460,
          height: 460,
        }}>
          {/* Outer protective shield ring */}
          <svg width="460" height="460" viewBox="0 0 460 460" style={{ position: 'absolute', opacity: shieldOp }}>
            <g transform={`rotate(${shieldRot} 230 230)`}>
              <circle cx="230" cy="230" r="220" stroke="#ff0000" strokeWidth="2" fill="none" strokeDasharray="20 15" opacity="0.6" />
            </g>
            <g transform={`rotate(${-shieldRot * 0.7} 230 230)`}>
              <circle cx="230" cy="230" r="195" stroke="#e9d700" strokeWidth="1.5" fill="none" strokeDasharray="8 10" opacity="0.5" />
            </g>
          </svg>

          {/* Brain SVG */}
          <svg width="460" height="460" viewBox="0 0 200 200" style={{ position: 'absolute' }}>
            <defs>
              <linearGradient id="brainGrad" x1="0%" y1="0%" x2="100%" y2="100%">
                <stop offset="0%" stopColor="#EEEEEE" />
                <stop offset="100%" stopColor="#9E9E9E" />
              </linearGradient>
            </defs>
            <path 
              d="M100 40 C 70 40, 50 60, 50 90 C 50 100, 55 110, 60 115 C 55 125, 60 140, 75 145 C 80 155, 95 160, 100 155 C 105 160, 120 155, 125 145 C 140 140, 145 125, 140 115 C 145 110, 150 100, 150 90 C 150 60, 130 40, 100 40 Z"
              fill="url(#brainGrad)"
              stroke="#080616"
              strokeWidth="2"
              opacity="0.95"
            />
            {/* Brain folds */}
            <path d="M75 70 Q 85 80, 75 95 M 100 60 Q 100 90, 100 120 M 125 70 Q 115 80, 125 95 M 70 110 Q 85 115, 90 125 M 130 110 Q 115 115, 110 125" 
              stroke="#080616" strokeWidth="1.5" fill="none" opacity="0.6" />
            {/* Identity lock symbol in center */}
            <circle cx="100" cy="100" r="14" fill="#ff0000" />
            <rect x="93" y="97" width="14" height="11" rx="2" fill="#EEEEEE" />
            <path d="M96 97 v-3 a4 4 0 0 1 8 0 v3" stroke="#EEEEEE" strokeWidth="2" fill="none" />
          </svg>

          {/* Defense arrows around */}
          {[0, 60, 120, 180, 240, 300].map((angle) => {
            const rad = (angle * Math.PI) / 180;
            const x = 230 + Math.cos(rad) * 250;
            const y = 230 + Math.sin(rad) * 250;
            return (
              <div key={`arr-${angle}`} style={{
                position: 'absolute',
                left: x - 25,
                top: y - 25,
                opacity: shieldOp * 0.8,
                transform: `rotate(${angle + 180}deg)`,
              }}>
                <svg width="50" height="50" viewBox="0 0 24 24" fill="none">
                  <path d="M5 12h14M14 7l5 5-5 5" stroke="#ff0000" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
                </svg>
              </div>
            );
          })}
        </div>

        {/* IDENTITY DEFENSE label */}
        <div style={{
          position: 'absolute',
          top: 760,
          left: '50%',
          transform: 'translateX(-50%)',
          opacity: idTagOp,
          padding: '12px 32px',
          background: 'rgba(8,6,22,0.7)',
          border: '2px solid #ff0000',
          borderRadius: 50,
          backdropFilter: 'blur(8px)',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 26, color: '#EEEEEE', letterSpacing: 4, fontWeight: 600 }}>
            BRAIN MODE: <span style={{ color: '#ff0000' }}>PROTECT IDENTITY</span>
          </div>
        </div>

        {/* Side Labels */}
        <div style={{
          position: 'absolute',
          top: 380,
          left: 120,
          opacity: shieldOp,
          textAlign: 'right',
          maxWidth: 260,
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#ff0000', letterSpacing: 3, marginBottom: 6, fontWeight: 700 }}>
            INPUT
          </div>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 30, color: '#EEEEEE', lineHeight: 1.3, fontWeight: 600 }}>
            कोई investment की बात
          </div>
        </div>

        <div style={{
          position: 'absolute',
          top: 380,
          right: 120,
          opacity: shieldOp,
          maxWidth: 260,
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#ff0000', letterSpacing: 3, marginBottom: 6, fontWeight: 700 }}>
            REJECTED
          </div>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 30, color: '#EEEEEE', lineHeight: 1.3, fontWeight: 600 }}>
            "मुझे नहीं आता" — auto reply
          </div>
        </div>

        {/* Bottom Result */}
        <div style={{
          position: 'absolute',
          bottom: 40,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: resultOp,
          transform: `translateY(${resultY}px)`,
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 42,
            color: '#EEEEEE',
            fontWeight: 700,
          }}>
            दिमाग identity बचाने में लग जाता है — <span style={{ color: '#e9d700' }}>सीखने का दरवाज़ा बंद</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(8,6,22,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene38;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39',
  durationInSeconds: 12,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene39: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camY = interpolate(frame, [0, durationInFrames], [-5, 5]);

  // Title
  const titleOp = sp(5);

  // Rejected items (3 dodges)
  const item1Op = sp(20);
  const item1X = interpolate(sp(20), [0, 1], [-60, 0]);

  const item2Op = sp(60);
  const item2X = interpolate(sp(60), [0, 1], [-60, 0]);

  const item3Op = sp(100);
  const item3X = interpolate(sp(100), [0, 1], [-60, 0]);

  // Acronym chips floating in
  const acronyms = ['SIP', 'MUTUAL FUND', 'NPS', 'PPF', 'ELSS'];
  
  const blameOp = interpolate(frame, [240, 280], [0, 1], { extrapolateRight: 'clamp' });
  const blameScale = sp(240, { damping: 7 });

  const particles = Array.from({ length: 30 }, (_, i) => i);
  const shapes = Array.from({ length: 5 }, (_, i) => i);

  return (
    <AbsoluteFill style={{ backgroundColor: '#4A4466', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(255,0,0,0.06) 0%, transparent 60%), linear-gradient(180deg, #4A4466 0%, #38334F 100%)',
      }} />

      {/* Shapes */}
      {shapes.map((i) => {
        const s = seededRandom(i + 19);
        const size = 240 + s * 320;
        const x = seededRandom(i + 29) * 1920;
        const y = seededRandom(i + 39) * 1080;
        const rot = frame * 0.06 * (i % 2 === 0 ? 1 : -1);
        return (
          <div key={`s39-${i}`} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid rgba(238,238,238,0.04)`,
            borderRadius: i % 2 === 0 ? '50%' : '20%',
            transform: `rotate(${rot}deg)`,
          }} />
        );
      })}

      {/* Diagonal stripes */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(-45deg, transparent, transparent 70px, rgba(238,238,238,0.025) 70px, rgba(238,238,238,0.025) 71px)',
      }} />

      {/* Particles */}
      {particles.map((i) => {
        const s = seededRandom(i + 199);
        const px = (s * 1920 + frame * (0.35 + s * 0.5)) % 1920;
        const py = (seededRandom(i + 299) * 1080 + Math.cos(frame * 0.02 + i) * 25) % 1080;
        return (
          <div key={`pt39-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 3,
            height: 3,
            borderRadius: '50%',
            backgroundColor: '#EEEEEE',
            opacity: 0.4,
            boxShadow: '0 0 6px #EEEEEE',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        {/* Top Title */}
        <div style={{
          position: 'absolute',
          top: 60,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleOp,
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 26, color: '#ff0000', letterSpacing: 6, marginBottom: 8, fontWeight: 600 }}>
            ── ROZ KI DODGING ──
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 76, color: '#EEEEEE' }}>
            कन्नी काटने की कला
          </div>
        </div>

        {/* 3 Dodge Items - Stacked */}
        <div style={{
          position: 'absolute',
          top: 250,
          left: 120,
          width: 900,
        }}>
          {/* Item 1 */}
          <div style={{
            opacity: item1Op,
            transform: `translateX(${item1X}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
            padding: '24px 32px',
            background: 'rgba(255,0,0,0.1)',
            border: '2px solid rgba(255,0,0,0.4)',
            borderRadius: 16,
            marginBottom: 24,
          }}>
            <div style={{
              minWidth: 60,
              fontFamily: 'Yantra One',
              fontSize: 50,
              color: '#ff0000',
            }}>01</div>
            <div style={{ flex: 1 }}>
              <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E', letterSpacing: 3, marginBottom: 6 }}>
                कोई बोले:
              </div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 38, color: '#EEEEEE', fontWeight: 700 }}>
                "Investment की बात है..."
              </div>
            </div>
            <div style={{
              padding: '10px 24px',
              background: '#ff0000',
              borderRadius: 8,
              fontFamily: 'Inter',
              fontSize: 24,
              color: '#EEEEEE',
              letterSpacing: 2,
              fontWeight: 700,
            }}>
              कन्नी ✕
            </div>
          </div>

          {/* Item 2 */}
          <div style={{
            opacity: item2Op,
            transform: `translateX(${item2X}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
            padding: '24px 32px',
            background: 'rgba(255,0,0,0.1)',
            border: '2px solid rgba(255,0,0,0.4)',
            borderRadius: 16,
            marginBottom: 24,
          }}>
            <div style={{
              minWidth: 60,
              fontFamily: 'Yantra One',
              fontSize: 50,
              color: '#ff0000',
            }}>02</div>
            <div style={{ flex: 1 }}>
              <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E', letterSpacing: 3, marginBottom: 6 }}>
                कोई बोले:
              </div>
              <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 38, color: '#EEEEEE', fontWeight: 700 }}>
                "ये किताब पढ़ो..."
              </div>
            </div>
            <div style={{
              padding: '10px 24px',
              background: '#ff0000',
              borderRadius: 8,
              fontFamily: 'Inter',
              fontSize: 24,
              color: '#EEEEEE',
              letterSpacing: 2,
              fontWeight: 700,
            }}>
              टाल ✕
            </div>
          </div>

          {/* Item 3 - Acronyms */}
          <div style={{
            opacity: item3Op,
            transform: `translateX(${item3X}px)`,
            display: 'flex',
            alignItems: 'center',
            gap: 24,
            padding: '24px 32px',
            background: 'rgba(255,0,0,0.1)',
            border: '2px solid rgba(255,0,0,0.4)',
            borderRadius: 16,
          }}>
            <div style={{
              minWidth: 60,
              fontFamily: 'Yantra One',
              fontSize: 50,
              color: '#ff0000',
            }}>03</div>
            <div style={{ flex: 1 }}>
              <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E', letterSpacing: 3, marginBottom: 6 }}>
                कोई बोले:
              </div>
              <div style={{ display: 'flex', flexWrap: 'wrap', gap: 8, marginTop: 4 }}>
                {acronyms.map((a, i) => {
                  const op = sp(110 + i * 12);
                  return (
                    <div key={a} style={{
                      opacity: op,
                      transform: `scale(${op})`,
                      padding: '6px 14px',
                      background: 'rgba(238,238,238,0.15)',
                      border: '1.5px solid #EEEEEE',
                      borderRadius: 6,
                      fontFamily: 'JetBrains Mono',
                      fontSize: 22,
                      color: '#EEEEEE',
                      fontWeight: 700,
                    }}>
                      {a}
                    </div>
                  );
                })}
              </div>
            </div>
            <div style={{
              padding: '10px 24px',
              background: '#ff0000',
              borderRadius: 8,
              fontFamily: 'Inter',
              fontSize: 24,
              color: '#EEEEEE',
              letterSpacing: 2,
              fontWeight: 700,
            }}>
              BHAGO ✕
            </div>
          </div>
        </div>

        {/* Right side BLAME panel */}
        <div style={{
          position: 'absolute',
          top: 320,
          right: 100,
          width: 360,
          opacity: blameOp,
          transform: `scale(${blameScale})`,
          padding: '32px 28px',
          background: 'rgba(8,6,22,0.7)',
          border: '3px dashed #e9d700',
          borderRadius: 20,
          textAlign: 'center',
          backdropFilter: 'blur(8px)',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 22, color: '#9E9E9E', letterSpacing: 3, marginBottom: 12 }}>
            FINAL EXCUSE
          </div>
          <svg width="60" height="60" viewBox="0 0 24 24" fill="none" style={{ margin: '0 auto 16px' }}>
            <path d="M5 12h14M14 7l5 5-5 5" stroke="#e9d700" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div style={{ fontFamily: 'Yantra One', fontSize: 44, color: '#e9d700', lineHeight: 1.2 }}>
            "ये CA वालों<br />का काम है"
          </div>
          <div style={{
            marginTop: 16,
            padding: '6px 16px',
            background: 'rgba(255,0,0,0.2)',
            border: '1px solid #ff0000',
            borderRadius: 20,
            display: 'inline-block',
            fontFamily: 'Inter',
            fontSize: 18,
            color: '#ff0000',
            letterSpacing: 2,
            fontWeight: 600,
          }}>
            BLAME SHIFT COMPLETE
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 45%, rgba(8,6,22,0.65) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene39;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene40: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slow zoom
  const camScale = interpolate(frame, [0, durationInFrames], [0.96, 1.02]);

  // Top title
  const titleOp = sp(5);
  const titleY = interpolate(sp(5), [0, 1], [-40, 0]);

  // Loop circle drawing
  const loopProgress = interpolate(frame, [40, 160], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Node 1 - नहीं सीखते
  const node1Op = sp(60);
  // Node 2 - नहीं आता
  const node2Op = sp(120);

  // Center JAIL label
  const jailOp = interpolate(frame, [180, 220], [0, 1], { extrapolateRight: 'clamp' });
  const jailScale = sp(180, { damping: 7, stiffness: 110 });

  // Bottom warning
  const warnOp = interpolate(frame, [240, 280], [0, 1], { extrapolateRight: 'clamp' });
  const warnY = interpolate(sp(240), [0, 1], [30, 0]);

  // Rotating loop arrows
  const loopRot = frame * 1.2;

  const particles = Array.from({ length: 28 }, (_, i) => i);
  const shapes = Array.from({ length: 5 }, (_, i) => i);

  // Generate circular path
  const cx = 960;
  const cy = 530;
  const r = 260;

  return (
    <AbsoluteFill style={{ backgroundColor: '#4A4466', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 55%, rgba(255,0,0,0.12) 0%, transparent 50%), linear-gradient(180deg, #4A4466 0%, #2D2A40 100%)',
      }} />

      {/* Shapes */}
      {shapes.map((i) => {
        const s = seededRandom(i + 21);
        const size = 250 + s * 340;
        const x = seededRandom(i + 31) * 1920;
        const y = seededRandom(i + 41) * 1080;
        const rot = frame * 0.06 * (i % 2 === 0 ? 1 : -1);
        return (
          <div key={`s40-${i}`} style={{
            position: 'absolute',
            left: x - size / 2,
            top: y - size / 2,
            width: size,
            height: size,
            border: `2px solid rgba(238,238,238,0.04)`,
            borderRadius: '50%',
            transform: `rotate(${rot}deg)`,
          }} />
        );
      })}

      {/* Cell bars texture */}
      <AbsoluteFill style={{
        backgroundImage: 'repeating-linear-gradient(90deg, transparent, transparent 120px, rgba(238,238,238,0.025) 120px, rgba(238,238,238,0.025) 122px)',
      }} />

      {/* Particles */}
      {particles.map((i) => {
        const s = seededRandom(i + 211);
        const px = (s * 1920 + frame * (0.3 + s * 0.4)) % 1920;
        const py = (seededRandom(i + 311) * 1080 + Math.sin(frame * 0.02 + i) * 30) % 1080;
        return (
          <div key={`pt40-${i}`} style={{
            position: 'absolute',
            left: px,
            top: py,
            width: 3,
            height: 3,
            borderRadius: '50%',
            backgroundColor: '#ff0000',
            opacity: 0.35,
            boxShadow: '0 0 6px #ff0000',
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center' }}>
        {/* Top Title */}
        <div style={{
          position: 'absolute',
          top: 50,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: titleOp,
          transform: `translateY(${titleY}px)`,
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 26, color: '#ff0000', letterSpacing: 6, marginBottom: 8, fontWeight: 600 }}>
            ── THE CLOSED LOOP ──
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 76, color: '#EEEEEE' }}>
            खुद बनाई गई जेल
          </div>
        </div>

        {/* Closed Loop SVG */}
        <div style={{
          position: 'absolute',
          top: 220,
          left: 0,
          right: 0,
          height: 600,
        }}>
          <svg width="1920" height="600" viewBox="0 0 1920 600" style={{ overflow: 'visible' }}>
            {/* Outer rotating arrows */}
            <g transform={`rotate(${loopRot} ${cx} ${cy - 220})`} opacity={loopProgress}>
              <circle cx={cx} cy={cy - 220} r={r + 30} fill="none" stroke="#ff0000" strokeWidth="2" strokeDasharray="40 30" opacity="0.5" />
            </g>

            {/* Main loop circle */}
            <circle
              cx={cx}
              cy={cy - 220}
              r={r}
              fill="none"
              stroke="#ff0000"
              strokeWidth="6"
              strokeDasharray={2 * Math.PI * r}
              strokeDashoffset={2 * Math.PI * r * (1 - loopProgress)}
              transform={`rotate(-90 ${cx} ${cy - 220})`}
              style={{ filter: 'drop-shadow(0 0 12px rgba(255,0,0,0.5))' }}
            />

            {/* Direction arrows on loop */}
            {loopProgress > 0.5 && [45, 135, 225, 315].map((a) => {
              const rad = (a * Math.PI) / 180;
              const ax = cx + Math.cos(rad) * r;
              const ay = (cy - 220) + Math.sin(rad) * r;
              return (
                <g key={`la-${a}`} transform={`translate(${ax} ${ay}) rotate(${a + 90})`}>
                  <path d="M-12 -8 L0 0 L-12 8" stroke="#ff0000" strokeWidth="4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
                </g>
              );
            })}
          </svg>

          {/* Node 1 - Top */}
          <div style={{
            position: 'absolute',
            top: 30,
            left: '50%',
            transform: 'translateX(-50%)',
            opacity: node1Op,
            padding: '20px 36px',
            background: '#080616',
            border: '3px solid #ff0000',
            borderRadius: 16,
            textAlign: 'center',
            minWidth: 320,
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 20, color: '#ff0000', letterSpacing: 3, marginBottom: 4, fontWeight: 600 }}>
              CAUSE
            </div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 44, color: '#EEEEEE', lineHeight: 1.1 }}>
              सीखते नहीं
            </div>
          </div>

          {/* Node 2 - Bottom */}
          <div style={{
            position: 'absolute',
            bottom: 30,
            left: '50%',
            transform: 'translateX(-50%)',
            opacity: node2Op,
            padding: '20px 36px',
            background: '#080616',
            border: '3px solid #ff0000',
            borderRadius: 16,
            textAlign: 'center',
            minWidth: 320,
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 20, color: '#ff0000', letterSpacing: 3, marginBottom: 4, fontWeight: 600 }}>
              EFFECT
            </div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 44, color: '#EEEEEE', lineHeight: 1.1 }}>
              नहीं आता
            </div>
          </div>

          {/* Left arrow text */}
          <div style={{
            position: 'absolute',
            top: 270,
            left: 460,
            opacity: loopProgress,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 28,
            color: '#EEEEEE',
            textAlign: 'right',
            maxWidth: 200,
            fontWeight: 600,
          }}>
            इसलिए<br />नहीं आता
          </div>

          {/* Right arrow text */}
          <div style={{
            position: 'absolute',
            top: 270,
            right: 460,
            opacity: loopProgress,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 28,
            color: '#EEEEEE',
            maxWidth: 200,
            fontWeight: 600,
          }}>
            इसलिए<br />सीखते नहीं
          </div>

          {/* Center JAIL stamp */}
          <div style={{
            position: 'absolute',
            top: 250,
            left: '50%',
            transform: `translateX(-50%) scale(${jailScale})`,
            opacity: jailOp,
            textAlign: 'center',
          }}>
            <svg width="80" height="80" viewBox="0 0 24 24" fill="none" style={{ margin: '0 auto 4px' }}>
              <rect x="4" y="10" width="16" height="11" rx="2" stroke="#e9d700" strokeWidth="2.5" />
              <path d="M8 10V7a4 4 0 0 1 8 0v3" stroke="#e9d700" strokeWidth="2.5" />
              <line x1="9" y1="13" x2="9" y2="18" stroke="#e9d700" strokeWidth="2" />
              <line x1="15" y1="13" x2="15" y2="18" stroke="#e9d700" strokeWidth="2" />
            </svg>
            <div style={{ fontFamily: 'Yantra One', fontSize: 48, color: '#e9d700', lineHeight: 1, textShadow: '0 0 15px rgba(233,215,0,0.5)' }}>
              JAIL
            </div>
          </div>
        </div>

        {/* Bottom Warning */}
        <div style={{
          position: 'absolute',
          bottom: 50,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: warnOp,
          transform: `translateY(${warnY}px)`,
        }}>
          <div style={{
            display: 'inline-flex',
            alignItems: 'center',
            gap: 16,
            padding: '12px 32px',
            background: 'rgba(8,6,22,0.7)',
            border: '2px solid rgba(255,0,0,0.5)',
            borderRadius: 50,
            backdropFilter: 'blur(8px)',
          }}>
            <div style={{ fontFamily: 'Inter', fontSize: 28, color: '#EEEEEE', letterSpacing: 3, fontWeight: 600 }}>
              हर दिन एक नई <span style={{ color: '#ff0000' }}>ईंट</span> लग रही है
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 40%, rgba(8,6,22,0.7) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#9E9E9E',
        opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene40;
```

