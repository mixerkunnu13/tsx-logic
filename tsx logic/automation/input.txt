```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene1_HeizungOptimieren',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene1_HeizungOptimieren: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Title reveal
  const titleY = interpolate(sp(8), [0, 1], [80, 0]);
  const titleOp = sp(8);

  // Radiator scale
  const radiatorScale = sp(30);
  const radiatorRot = Math.sin(frame * 0.03) * 1.5;

  // Heat waves
  const waveOp = interpolate(frame, [50, 80], [0, 1], { extrapolateRight: 'clamp' });

  // Number 15% reveal
  const percentScale = sp(120, { damping: 8, stiffness: 100 });
  const percentY = interpolate(sp(120), [0, 1], [60, 0]);

  // Temperature gauge animation
  const gaugeFill = interpolate(frame, [150, 280], [0, 0.85], { extrapolateRight: 'clamp' });

  // Money saved counter
  const moneyCount = Math.floor(interpolate(frame, [200, 330], [0, 200], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Tool icons
  const tool1 = sp(250);
  const tool2 = sp(275);
  const tool3 = sp(300);

  // Camera breathing
  const camScale = 1 + Math.sin(frame * 0.015) * 0.008;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A5CF83', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 30%, rgba(238,238,238,0.35) 0%, transparent 55%), radial-gradient(circle at 75% 75%, rgba(0,0,0,0.18) 0%, transparent 50%)',
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        <div style={{
          position: 'absolute',
          width: 800, height: 800,
          left: -200, top: -200,
          borderRadius: '50%',
          border: '2px solid #000000',
          opacity: 0.05,
          transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }} />
        <div style={{
          position: 'absolute',
          width: 600, height: 600,
          right: -150, bottom: -150,
          background: '#EEEEEE',
          opacity: 0.07,
          transform: `rotate(${-frame * 0.1}deg)`,
          borderRadius: 40,
        }} />
        <div style={{
          position: 'absolute',
          width: 400, height: 400,
          right: 200, top: 100,
          border: '3px solid #000000',
          opacity: 0.04,
          transform: `rotate(${frame * 0.2}deg)`,
        }} />
      </AbsoluteFill>

      {/* Grid texture */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(rgba(0,0,0,0.06) 1px, transparent 1px), linear-gradient(90deg, rgba(0,0,0,0.06) 1px, transparent 1px)',
        backgroundSize: '80px 80px',
        opacity: 0.5,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = seededRandom(i + 1);
        const x = seed * 1920;
        const y = (seededRandom(i + 100) * 1080 + frame * 0.4) % 1080;
        const size = 4 + seededRandom(i + 50) * 6;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: size, height: size,
            borderRadius: '50%',
            backgroundColor: '#EEEEEE',
            opacity: 0.4,
          }} />
        );
      })}

      {/* TOP TAG */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: '50%',
        transform: `translateX(-50%) translateY(${titleY}px)`,
        opacity: titleOp,
        display: 'flex',
        alignItems: 'center',
        gap: 20,
        backgroundColor: '#000000',
        padding: '14px 36px',
        borderRadius: 6,
      }}>
        <div style={{ width: 14, height: 14, borderRadius: '50%', backgroundColor: '#A5CF83' }} />
        <span style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 28,
          fontWeight: 800,
          color: '#EEEEEE',
          letterSpacing: 4,
        }}>HACK NR. 04 — HEIZUNG OPTIMIEREN</span>
      </div>

      {/* MAIN HEADLINE */}
      <div style={{
        position: 'absolute',
        top: 160,
        left: 100,
        transform: `translateY(${interpolate(sp(25), [0, 1], [40, 0])}px)`,
        opacity: sp(25),
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 178,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 0.9,
          letterSpacing: -3,
        }}>
          HYDRAULISCHER
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 178,
          fontWeight: 900,
          color: '#EEEEEE',
          lineHeight: 0.9,
          letterSpacing: -3,
          marginTop: -10,
          WebkitTextStroke: '3px #000000',
        }}>
          ABGLEICH
        </div>
      </div>

      {/* LEFT: RADIATOR SVG */}
      <div style={{
        position: 'absolute',
        left: 140,
        top: 540,
        transform: `scale(${radiatorScale}) rotate(${radiatorRot}deg)`,
        transformOrigin: 'center',
      }}>
        <svg width="380" height="340" viewBox="0 0 380 340">
          {/* Top pipe */}
          <rect x="20" y="40" width="340" height="22" fill="#000000" rx="4" />
          {/* Bottom pipe */}
          <rect x="20" y="280" width="340" height="22" fill="#000000" rx="4" />
          {/* Fins */}
          {[0, 1, 2, 3, 4, 5, 6, 7].map((i) => (
            <rect key={i} x={40 + i * 40} y="60" width="28" height="220" fill="#EEEEEE" stroke="#000000" strokeWidth="3" rx="2" />
          ))}
          {/* Valve */}
          <circle cx="40" cy="320" r="22" fill="#000000" />
          <circle cx="40" cy="320" r="10" fill="#A5CF83" />
          {/* Thermometer reading */}
          <rect x="160" y="140" width="60" height="60" fill="#A5CF83" stroke="#000000" strokeWidth="4" rx="6" />
        </svg>
      </div>

      {/* Heat waves */}
      <div style={{ position: 'absolute', left: 230, top: 460, opacity: waveOp }}>
        {[0, 1, 2].map((i) => {
          const waveY = -((frame * 1.5 + i * 40) % 120);
          const waveOpacity = interpolate(Math.abs(waveY), [0, 120], [0.8, 0]);
          return (
            <svg key={i} width="200" height="60" viewBox="0 0 200 60" style={{
              position: 'absolute',
              top: waveY,
              opacity: waveOpacity,
            }}>
              <path d="M 0 30 Q 25 10, 50 30 T 100 30 T 150 30 T 200 30" fill="none" stroke="#000000" strokeWidth="3" />
            </svg>
          );
        })}
      </div>

      {/* RIGHT: 15% BIG NUMBER */}
      <div style={{
        position: 'absolute',
        right: 120,
        top: 470,
        transform: `translateY(${percentY}px) scale(${percentScale})`,
        transformOrigin: 'center',
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 28,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 6,
          marginBottom: 10,
        }}>
          BIS ZU
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 360,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 0.85,
          letterSpacing: -10,
        }}>
          15<span style={{ color: '#EEEEEE', WebkitTextStroke: '4px #000000' }}>%</span>
        </div>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 32,
          fontWeight: 800,
          color: '#000000',
          marginTop: 6,
          letterSpacing: 2,
        }}>
          WENIGER ENERGIE
        </div>
      </div>

      {/* TEMPERATURE GAUGE */}
      <div style={{
        position: 'absolute',
        left: 580,
        top: 760,
        opacity: interpolate(frame, [140, 170], [0, 1], { extrapolateRight: 'clamp' }),
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 700,
          color: '#000000',
          marginBottom: 10,
          letterSpacing: 2,
        }}>
          EFFIZIENZ-LEVEL
        </div>
        <div style={{
          width: 420,
          height: 36,
          backgroundColor: '#EEEEEE',
          border: '3px solid #000000',
          borderRadius: 4,
          overflow: 'hidden',
          position: 'relative',
        }}>
          <div style={{
            width: `${gaugeFill * 100}%`,
            height: '100%',
            background: 'repeating-linear-gradient(45deg, #000000 0px, #000000 10px, #2D2D2D 10px, #2D2D2D 20px)',
          }} />
        </div>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 26,
          fontWeight: 700,
          color: '#000000',
          marginTop: 8,
        }}>
          {Math.floor(gaugeFill * 100)}% optimiert
        </div>
      </div>

      {/* MONEY SAVED COUNTER */}
      <div style={{
        position: 'absolute',
        left: 100,
        bottom: 90,
        opacity: interpolate(frame, [200, 230], [0, 1], { extrapolateRight: 'clamp' }),
        backgroundColor: '#000000',
        padding: '20px 36px',
        borderRadius: 8,
        display: 'flex',
        alignItems: 'center',
        gap: 20,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 24,
          fontWeight: 700,
          color: '#A5CF83',
          letterSpacing: 2,
        }}>
          ERSPARNIS / JAHR
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 64,
          fontWeight: 900,
          color: '#EEEEEE',
          letterSpacing: -1,
        }}>
          {moneyCount}€
        </div>
      </div>

      {/* CHECKLIST RIGHT BOTTOM */}
      <div style={{
        position: 'absolute',
        right: 80,
        bottom: 90,
        display: 'flex',
        flexDirection: 'column',
        gap: 12,
      }}>
        {[
          { label: 'HEIZKÖRPER ENTLÜFTEN', spr: tool1 },
          { label: 'THERMOSTAT EINSTELLEN', spr: tool2 },
          { label: 'ABGLEICH BEAUFTRAGEN', spr: tool3 },
        ].map((item, i) => (
          <div key={i} style={{
            display: 'flex',
            alignItems: 'center',
            gap: 14,
            transform: `translateX(${interpolate(item.spr, [0, 1], [60, 0])}px)`,
            opacity: item.spr,
          }}>
            <div style={{
              width: 30, height: 30,
              backgroundColor: '#000000',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}>
              <svg width="18" height="14" viewBox="0 0 18 14">
                <path d="M 2 7 L 7 12 L 16 2" fill="none" stroke="#A5CF83" strokeWidth="3" strokeLinecap="round" />
              </svg>
            </div>
            <span style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 24,
              fontWeight: 800,
              color: '#000000',
              letterSpacing: 1,
            }}>{item.label}</span>
          </div>
        ))}
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.35,
      }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene1_HeizungOptimieren;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene2_KriseChance',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene2_KriseChance: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Headline word reveals
  const word1 = sp(10);
  const word2 = sp(35);
  const word3 = sp(60);

  // Three-pillar cards
  const card1 = sp(110);
  const card2 = sp(135);
  const card3 = sp(160);

  // Förder-Stamp
  const stampScale = sp(280, { damping: 6, stiffness: 80 });
  const stampRot = interpolate(sp(280), [0, 1], [-30, -8]);

  // Camera pan
  const camY = Math.sin(frame * 0.02) * 4;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A5CF83', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(238,238,238,0.2) 0%, transparent 50%, rgba(0,0,0,0.15) 100%)',
      }} />

      {/* Shape layers */}
      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        <svg style={{ position: 'absolute', left: 1200, top: -100 }} width="700" height="700" viewBox="0 0 700 700">
          <polygon points="350,50 650,500 50,500" fill="none" stroke="#000000" strokeWidth="2" opacity="0.06"
            transform={`rotate(${frame * 0.15} 350 350)`} />
        </svg>
        <div style={{
          position: 'absolute',
          left: -100, bottom: -100,
          width: 500, height: 500,
          backgroundColor: '#EEEEEE',
          opacity: 0.08,
          borderRadius: 30,
          transform: `rotate(${frame * 0.1}deg)`,
        }} />
        <div style={{
          position: 'absolute',
          left: 800, top: 200,
          width: 300, height: 300,
          border: '4px solid #EEEEEE',
          opacity: 0.1,
          borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.04) * 0.1})`,
        }} />
      </AbsoluteFill>

      {/* Dot texture */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(circle, rgba(0,0,0,0.1) 1.5px, transparent 1.5px)',
        backgroundSize: '40px 40px',
        opacity: 0.6,
      }} />

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const seed = seededRandom(i + 5);
        const x = (seed * 1920 + frame * 0.3) % 1920;
        const y = seededRandom(i + 50) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 6, height: 6,
            backgroundColor: '#000000',
            opacity: 0.2,
            transform: 'rotate(45deg)',
          }} />
        );
      })}

      {/* HEADLINE */}
      <div style={{
        position: 'absolute',
        top: 100,
        left: 100,
        right: 100,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 28,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 8,
          marginBottom: 20,
          opacity: word1,
        }}>
          MINDSET-SHIFT
        </div>
        <div style={{
          display: 'flex',
          flexWrap: 'wrap',
          gap: 30,
          alignItems: 'baseline',
        }}>
          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 195,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 0.9,
            opacity: word1,
            transform: `translateY(${interpolate(word1, [0, 1], [60, 0])}px)`,
            display: 'inline-block',
          }}>
            KRISE
          </span>
          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 110,
            fontWeight: 400,
            color: '#000000',
            opacity: word2,
            fontStyle: 'italic',
            transform: `translateY(${interpolate(word2, [0, 1], [60, 0])}px)`,
            display: 'inline-block',
          }}>
            wird zur
          </span>
          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 195,
            fontWeight: 900,
            color: '#EEEEEE',
            lineHeight: 0.9,
            WebkitTextStroke: '4px #000000',
            opacity: word3,
            transform: `translateY(${interpolate(word3, [0, 1], [60, 0])}px) scale(${interpolate(word3, [0, 1], [0.7, 1])})`,
            display: 'inline-block',
          }}>
            CHANCE
          </span>
        </div>
      </div>

      {/* Diagonal divider line */}
      <div style={{
        position: 'absolute',
        top: 480,
        left: 100,
        right: 100,
        height: 4,
        backgroundColor: '#000000',
        transformOrigin: 'left',
        transform: `scaleX(${interpolate(sp(95), [0, 1], [0, 1])})`,
      }} />

      {/* THREE PILLAR CARDS */}
      <div style={{
        position: 'absolute',
        top: 530,
        left: 100,
        right: 100,
        display: 'flex',
        gap: 30,
        justifyContent: 'space-between',
      }}>
        {[
          {
            num: '01',
            title: 'WÄRME-\nPUMPE',
            desc: 'Effiziente Wärme aus der Umwelt',
            icon: 'pump',
            spr: card1,
          },
          {
            num: '02',
            title: 'GEBÄUDE-\nDÄMMUNG',
            desc: 'Schutz gegen Energieverlust',
            icon: 'house',
            spr: card2,
          },
          {
            num: '03',
            title: 'KfW-\nFÖRDERUNG',
            desc: 'Staatliche Unterstützung sichern',
            icon: 'bank',
            spr: card3,
          },
        ].map((item, i) => (
          <div key={i} style={{
            width: 540,
            height: 440,
            backgroundColor: i === 1 ? '#000000' : '#EEEEEE',
            border: '4px solid #000000',
            padding: '36px 32px',
            transform: `translateY(${interpolate(item.spr, [0, 1], [120, 0])}px) rotate(${interpolate(item.spr, [0, 1], [3, 0])}deg)`,
            opacity: item.spr,
            position: 'relative',
            overflow: 'hidden',
          }}>
            {/* Number */}
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 64,
              fontWeight: 900,
              color: i === 1 ? '#A5CF83' : '#000000',
              opacity: 0.5,
            }}>
              {item.num}
            </div>

            {/* Icon */}
            <div style={{ position: 'absolute', top: 110, right: 30 }}>
              {item.icon === 'pump' && (
                <svg width="130" height="130" viewBox="0 0 130 130">
                  <rect x="20" y="30" width="90" height="70" fill="none" stroke="#000000" strokeWidth="5" rx="6" />
                  <circle cx="65" cy="65" r="22" fill="none" stroke="#000000" strokeWidth="5" />
                  <line x1="65" y1="50" x2={65 + Math.cos(frame * 0.2) * 15} y2={65 + Math.sin(frame * 0.2) * 15} stroke="#000000" strokeWidth="5" strokeLinecap="round" />
                  <circle cx="65" cy="65" r="4" fill="#000000" />
                </svg>
              )}
              {item.icon === 'house' && (
                <svg width="130" height="130" viewBox="0 0 130 130">
                  <polygon points="65,15 115,65 105,65 105,110 25,110 25,65 15,65" fill="none" stroke="#A5CF83" strokeWidth="5" />
                  <rect x="50" y="75" width="30" height="35" fill="none" stroke="#A5CF83" strokeWidth="5" />
                  {[0, 1, 2].map((j) => (
                    <line key={j} x1={30 + j * 5} y1={70 - j * 3} x2={100 - j * 5} y2={70 - j * 3} stroke="#A5CF83" strokeWidth="2" opacity={0.5 + j * 0.15} />
                  ))}
                </svg>
              )}
              {item.icon === 'bank' && (
                <svg width="130" height="130" viewBox="0 0 130 130">
                  <polygon points="65,15 115,40 15,40" fill="none" stroke="#000000" strokeWidth="5" />
                  <rect x="20" y="40" width="90" height="6" fill="#000000" />
                  <rect x="25" y="50" width="10" height="50" fill="none" stroke="#000000" strokeWidth="4" />
                  <rect x="50" y="50" width="10" height="50" fill="none" stroke="#000000" strokeWidth="4" />
                  <rect x="75" y="50" width="10" height="50" fill="none" stroke="#000000" strokeWidth="4" />
                  <rect x="100" y="50" width="10" height="50" fill="none" stroke="#000000" strokeWidth="4" />
                  <rect x="15" y="105" width="100" height="8" fill="#000000" />
                </svg>
              )}
            </div>

            {/* Title */}
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 75,
              fontWeight: 900,
              color: i === 1 ? '#EEEEEE' : '#000000',
              lineHeight: 0.9,
              marginTop: 90,
              whiteSpace: 'pre-line',
              letterSpacing: -1,
            }}>
              {item.title}
            </div>

            {/* Description */}
            <div style={{
              position: 'absolute',
              bottom: 30,
              left: 32,
              right: 32,
              fontFamily: 'Inter, sans-serif',
              fontSize: 22,
              fontWeight: 600,
              color: i === 1 ? '#A5CF83' : '#2D2D2D',
              lineHeight: 1.3,
            }}>
              {item.desc}
            </div>

            {/* Corner accent */}
            <div style={{
              position: 'absolute',
              bottom: 0, right: 0,
              width: 70, height: 70,
              backgroundColor: '#A5CF83',
              clipPath: 'polygon(100% 0, 100% 100%, 0 100%)',
              border: '4px solid #000000',
            }} />
          </div>
        ))}
      </div>

      {/* FÖRDER STAMP */}
      <div style={{
        position: 'absolute',
        top: 60,
        right: 100,
        transform: `scale(${stampScale}) rotate(${stampRot}deg)`,
        transformOrigin: 'center',
      }}>
        <div style={{
          width: 220, height: 220,
          borderRadius: '50%',
          border: '6px solid #000000',
          backgroundColor: '#A5CF83',
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          position: 'relative',
        }}>
          <div style={{
            position: 'absolute',
            inset: 14,
            border: '3px dashed #000000',
            borderRadius: '50%',
          }} />
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 18,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: 3,
          }}>STAATLICHE</div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 64,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 0.9,
          }}>BAFA</div>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 18,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: 3,
          }}>ZUSCHUSS</div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.35,
      }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene2_KriseChance;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene3_Heizoel30Prozent',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene3_Heizoel30Prozent: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Chart drawing
  const chartProgress = interpolate(frame, [60, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const chartOp = sp(50);

  // 30% number
  const bigNumScale = sp(220, { damping: 7, stiffness: 90 });

  // Brutal stamp
  const brutalRot = interpolate(sp(310), [0, 1], [-25, -5]);
  const brutalScale = sp(310, { damping: 8 });

  // Screen shake on impact
  const shake = frame > 220 && frame < 240 ? Math.sin(frame * 3) * 6 : 0;

  // Chart data points (12 months, rising)
  const dataPoints = [140, 142, 145, 148, 152, 158, 162, 165, 170, 173, 176, 179];

  return (
    <AbsoluteFill style={{ backgroundColor: '#A5CF83', overflow: 'hidden', opacity: exitOp, transform: `translateX(${shake}px)` }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at top right, rgba(0,0,0,0.25) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        right: -200, top: -200,
        width: 700, height: 700,
        borderRadius: '50%',
        backgroundColor: '#000000',
        opacity: 0.05,
      }} />
      <div style={{
        position: 'absolute',
        left: -100, bottom: -100,
        width: 500, height: 500,
        border: '3px solid #EEEEEE',
        opacity: 0.1,
        borderRadius: 20,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Grid texture */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(rgba(0,0,0,0.08) 1px, transparent 1px), linear-gradient(90deg, rgba(0,0,0,0.08) 1px, transparent 1px)',
        backgroundSize: '60px 60px',
      }} />

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const seed = seededRandom(i + 3);
        const x = seed * 1920;
        const y = ((seededRandom(i + 30) * 1080) + frame * 0.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 5, height: 5,
            backgroundColor: '#EEEEEE',
            opacity: 0.5,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* TOP LABEL */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 100,
        opacity: sp(5),
        transform: `translateY(${interpolate(sp(5), [0, 1], [-30, 0])}px)`,
        display: 'flex',
        alignItems: 'center',
        gap: 16,
      }}>
        <div style={{
          width: 12, height: 12,
          backgroundColor: '#000000',
          animation: 'pulse 1s infinite',
        }} />
        <span style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 26,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 4,
        }}>
          DESTATIS // VERBRAUCHERPREISINDEX // HEIZÖL
        </span>
      </div>

      {/* MAIN HEADLINE */}
      <div style={{
        position: 'absolute',
        top: 130,
        left: 100,
        opacity: sp(15),
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 122,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 0.95,
          letterSpacing: -2,
        }}>
          IN NUR 12 MONATEN.
        </div>
      </div>

      {/* LEFT: CHART */}
      <div style={{
        position: 'absolute',
        left: 100,
        top: 330,
        opacity: chartOp,
      }}>
        <svg width="900" height="450" viewBox="0 0 900 450">
          {/* Y axis labels */}
          {[140, 150, 160, 170, 180].map((v, i) => (
            <g key={i}>
              <text x="10" y={420 - i * 90} fontFamily="JetBrains Mono" fontSize="20" fontWeight="700" fill="#000000">{v}</text>
              <line x1="70" y1={415 - i * 90} x2="880" y2={415 - i * 90} stroke="#000000" strokeWidth="1" opacity="0.2" />
            </g>
          ))}

          {/* X axis */}
          <line x1="70" y1="415" x2="880" y2="415" stroke="#000000" strokeWidth="3" />

          {/* Chart line */}
          <polyline
            points={dataPoints.map((v, i) => `${70 + i * 70},${415 - (v - 140) * 9}`).slice(0, Math.ceil(dataPoints.length * chartProgress)).join(' ')}
            fill="none"
            stroke="#000000"
            strokeWidth="6"
            strokeLinecap="round"
            strokeLinejoin="round"
          />

          {/* Area under line */}
          <polygon
            points={`70,415 ${dataPoints.map((v, i) => `${70 + i * 70},${415 - (v - 140) * 9}`).slice(0, Math.ceil(dataPoints.length * chartProgress)).join(' ')} ${70 + (Math.ceil(dataPoints.length * chartProgress) - 1) * 70},415`}
            fill="#000000"
            opacity="0.15"
          />

          {/* Data points */}
          {dataPoints.slice(0, Math.ceil(dataPoints.length * chartProgress)).map((v, i) => (
            <circle key={i} cx={70 + i * 70} cy={415 - (v - 140) * 9} r="8" fill="#EEEEEE" stroke="#000000" strokeWidth="3" />
          ))}

          {/* Start label */}
          {chartProgress > 0.1 && (
            <g>
              <rect x="40" y="280" width="80" height="38" fill="#EEEEEE" stroke="#000000" strokeWidth="3" rx="4" />
              <text x="80" y="306" fontFamily="JetBrains Mono" fontSize="22" fontWeight="700" fill="#000000" textAnchor="middle">140</text>
            </g>
          )}

          {/* End label */}
          {chartProgress > 0.9 && (
            <g>
              <rect x="800" y="50" width="100" height="44" fill="#000000" rx="4" />
              <text x="850" y="80" fontFamily="JetBrains Mono" fontSize="24" fontWeight="900" fill="#A5CF83" textAnchor="middle">179</text>
            </g>
          )}

          {/* Arrow up */}
          {chartProgress > 0.95 && (
            <g transform={`translate(${70 + 11 * 70 + 30}, ${415 - (179 - 140) * 9})`}>
              <path d="M 0 0 L -15 15 M 0 0 L 15 15" stroke="#000000" strokeWidth="5" strokeLinecap="round" />
              <path d="M 0 0 L 0 30" stroke="#000000" strokeWidth="5" strokeLinecap="round" />
            </g>
          )}
        </svg>
      </div>

      {/* RIGHT: 30% MEGA NUMBER */}
      <div style={{
        position: 'absolute',
        right: 100,
        top: 360,
        transform: `scale(${bigNumScale})`,
        transformOrigin: 'center',
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 28,
          fontWeight: 800,
          color: '#000000',
          letterSpacing: 6,
        }}>
          ANSTIEG VON
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 380,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 0.85,
          letterSpacing: -12,
          position: 'relative',
        }}>
          +30
          <span style={{
            fontSize: 220,
            color: '#EEEEEE',
            WebkitTextStroke: '4px #000000',
          }}>%</span>
        </div>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 28,
          fontWeight: 800,
          color: '#000000',
          letterSpacing: 4,
          marginTop: 10,
        }}>
          INNERHALB EINES JAHRES
        </div>
      </div>

      {/* BRUTAL STAMP */}
      <div style={{
        position: 'absolute',
        bottom: 120,
        right: 220,
        transform: `rotate(${brutalRot}deg) scale(${brutalScale})`,
        transformOrigin: 'center',
      }}>
        <div style={{
          backgroundColor: '#000000',
          padding: '20px 60px',
          border: '5px solid #000000',
          boxShadow: '8px 8px 0 #EEEEEE',
        }}>
          <span style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 95,
            fontWeight: 900,
            color: '#A5CF83',
            letterSpacing: 8,
          }}>BRUTAL.</span>
        </div>
      </div>

      {/* Bottom info bar */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        left: 100,
        opacity: sp(280),
        fontFamily: 'JetBrains Mono, monospace',
        fontSize: 22,
        fontWeight: 700,
        color: '#000000',
        letterSpacing: 2,
      }}>
        QUELLE: STATISTISCHES BUNDESAMT 2026
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.35,
      }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene3_Heizoel30Prozent;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene4_Kaufkraftverlust',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene4_Kaufkraftverlust: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Bar fills
  const bar1Height = interpolate(sp(70), [0, 1], [0, 60]);
  const bar2Height = interpolate(sp(140), [0, 1], [0, 540]);

  // Result reveal
  const resultOp = sp(240);
  const resultScale = sp(240, { damping: 7, stiffness: 90 });

  // Equation reveal
  const eqOp = sp(290);

  // Camera shake on result
  const shake = frame > 240 && frame < 260 ? Math.sin(frame * 4) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A5CF83', overflow: 'hidden', opacity: exitOp, transform: `translateY(${shake}px)` }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, transparent 0%, rgba(0,0,0,0.15) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        left: '50%', top: '50%',
        width: 1000, height: 1000,
        marginLeft: -500, marginTop: -500,
        border: '2px solid #000000',
        borderRadius: '50%',
        opacity: 0.04,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute',
        right: -200, top: -200,
        width: 600, height: 600,
        backgroundColor: '#EEEEEE',
        opacity: 0.08,
        borderRadius: 40,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Dot pattern */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(circle, rgba(0,0,0,0.08) 1.5px, transparent 1.5px)',
        backgroundSize: '50px 50px',
      }} />

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = seededRandom(i + 7);
        const x = seed * 1920;
        const y = ((seededRandom(i + 70) * 1080) - frame * 0.4 + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 4, height: 4,
            backgroundColor: '#000000',
            opacity: 0.25,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* HEADLINE */}
      <div style={{
        position: 'absolute',
        top: 70,
        left: 100,
        opacity: sp(10),
        transform: `translateY(${interpolate(sp(10), [0, 1], [-30, 0])}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 26,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 6,
          marginBottom: 14,
        }}>
          MATHEMATIK DES VERLUSTS
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 130,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 0.9,
          letterSpacing: -3,
        }}>
          DEIN GEHALT VERLIERT.
        </div>
      </div>

      {/* COMPARISON BARS */}
      <div style={{
        position: 'absolute',
        left: 140,
        top: 360,
        display: 'flex',
        alignItems: 'flex-end',
        gap: 80,
        height: 580,
      }}>
        {/* Bar 1: Gehalt +3% */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 24,
            fontWeight: 800,
            color: '#000000',
            marginBottom: 14,
            letterSpacing: 2,
          }}>
            DEIN GEHALT
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 90,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 1,
            marginBottom: 18,
            opacity: sp(65),
          }}>
            +3%
          </div>
          <div style={{
            width: 220,
            height: bar1Height,
            backgroundColor: '#EEEEEE',
            border: '5px solid #000000',
            position: 'relative',
          }}>
            <div style={{
              position: 'absolute',
              inset: 0,
              background: 'repeating-linear-gradient(45deg, transparent, transparent 12px, rgba(0,0,0,0.08) 12px, rgba(0,0,0,0.08) 24px)',
            }} />
          </div>
        </div>

        {/* VS sign */}
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 110,
          fontWeight: 900,
          color: '#000000',
          marginBottom: 200,
          opacity: sp(100),
          transform: `rotate(${interpolate(sp(100), [0, 1], [-20, 0])}deg)`,
        }}>
          vs.
        </div>

        {/* Bar 2: Heizöl +30% */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 24,
            fontWeight: 800,
            color: '#000000',
            marginBottom: 14,
            letterSpacing: 2,
          }}>
            DIE PREISE
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 90,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 1,
            marginBottom: 18,
            opacity: sp(135),
          }}>
            +30%
          </div>
          <div style={{
            width: 220,
            height: bar2Height,
            backgroundColor: '#000000',
            border: '5px solid #000000',
            position: 'relative',
            overflow: 'hidden',
          }}>
            {/* Rising arrow inside */}
            <svg width="220" height={bar2Height} style={{ position: 'absolute', top: 0 }}>
              <path d={`M 110 ${bar2Height - 30} L 110 30 M 110 30 L 80 60 M 110 30 L 140 60`}
                stroke="#A5CF83" strokeWidth="8" fill="none" strokeLinecap="round" />
            </svg>
          </div>
        </div>
      </div>

      {/* RESULT BOX RIGHT */}
      <div style={{
        position: 'absolute',
        right: 100,
        top: 360,
        opacity: resultOp,
        transform: `scale(${resultScale})`,
        transformOrigin: 'center',
        width: 600,
      }}>
        <div style={{
          backgroundColor: '#000000',
          padding: '40px 40px',
          border: '6px solid #000000',
          boxShadow: '12px 12px 0 #EEEEEE',
        }}>
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 24,
            fontWeight: 800,
            color: '#A5CF83',
            letterSpacing: 4,
            marginBottom: 10,
          }}>
            REALE KAUFKRAFT
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 240,
            fontWeight: 900,
            color: '#EEEEEE',
            lineHeight: 0.9,
            letterSpacing: -5,
          }}>
            -27%
          </div>
          <div style={{
            width: '100%',
            height: 6,
            backgroundColor: '#A5CF83',
            margin: '14px 0',
          }} />
          <div style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 26,
            fontWeight: 700,
            color: '#EEEEEE',
            lineHeight: 1.3,
          }}>
            ...das verlierst du bei Heizenergie. Real. Pro Jahr.
          </div>
        </div>

        {/* Equation below */}
        <div style={{
          marginTop: 28,
          opacity: eqOp,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 32,
          fontWeight: 700,
          color: '#000000',
          textAlign: 'center',
          backgroundColor: '#EEEEEE',
          padding: '16px 20px',
          border: '4px solid #000000',
        }}>
          +3% – 30% = <span style={{ color: '#000000', fontWeight: 900 }}>–27%</span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.35,
      }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene4_Kaufkraftverlust;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene5_MittelschichtBroeckelt',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene5_MittelschichtBroeckelt: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Crack pieces animation
  const crack1 = sp(180);
  const crack2 = sp(210);
  const crack3 = sp(240);

  // Three element cards
  const elem1 = sp(80);
  const elem2 = sp(110);
  const elem3 = sp(140);

  // Pressure bar
  const pressureFill = interpolate(frame, [200, 350], [0.4, 0.95], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A5CF83', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, transparent 0%, rgba(0,0,0,0.18) 100%), radial-gradient(circle at 50% 30%, rgba(238,238,238,0.2) 0%, transparent 50%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        left: 200, top: 100,
        width: 400, height: 400,
        border: '2px solid #000000',
        opacity: 0.06,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        right: 100, bottom: 100,
        width: 500, height: 500,
        borderRadius: '50%',
        backgroundColor: '#000000',
        opacity: 0.05,
      }} />

      {/* Cross-hatched texture */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(45deg, rgba(0,0,0,0.06) 1px, transparent 1px), linear-gradient(-45deg, rgba(0,0,0,0.06) 1px, transparent 1px)',
        backgroundSize: '40px 40px',
      }} />

      {/* Falling particles (debris) */}
      {Array.from({ length: 35 }).map((_, i) => {
        const seed = seededRandom(i + 11);
        const x = seed * 1920;
        const y = ((seededRandom(i + 90) * 1080) + frame * 0.8) % 1080;
        const size = 3 + seededRandom(i + 200) * 6;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: size, height: size,
            backgroundColor: '#000000',
            opacity: 0.3,
            transform: `rotate(${seed * 360}deg)`,
          }} />
        );
      })}

      {/* TOP TAG */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 100,
        display: 'flex',
        alignItems: 'center',
        gap: 16,
        opacity: sp(5),
      }}>
        <div style={{ width: 60, height: 4, backgroundColor: '#000000' }} />
        <span style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 28,
          fontWeight: 800,
          color: '#000000',
          letterSpacing: 6,
        }}>
          DIE MITTE GERÄT UNTER DRUCK
        </span>
      </div>

      {/* HEADLINE WITH CRACK */}
      <div style={{
        position: 'absolute',
        top: 130,
        left: 100,
        right: 100,
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 230,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 0.9,
          letterSpacing: -5,
          opacity: sp(15),
          position: 'relative',
          display: 'inline-block',
        }}>
          MITTEL
          <span style={{ color: '#EEEEEE', WebkitTextStroke: '5px #000000' }}>SCHICHT</span>
        </div>

        {/* Crack overlay SVG */}
        <svg style={{ position: 'absolute', top: 50, left: 0, width: 1700, height: 230, pointerEvents: 'none' }}>
          <path d="M 200 100 L 380 70 L 420 130 L 560 90"
            stroke="#000000" strokeWidth="4" fill="none"
            strokeDasharray="400"
            strokeDashoffset={400 - 400 * crack1} />
          <path d="M 900 110 L 1050 80 L 1100 140 L 1250 100"
            stroke="#000000" strokeWidth="4" fill="none"
            strokeDasharray="400"
            strokeDashoffset={400 - 400 * crack2} />
          <path d="M 1300 60 L 1380 120 L 1420 90 L 1500 150"
            stroke="#000000" strokeWidth="4" fill="none"
            strokeDasharray="300"
            strokeDashoffset={300 - 300 * crack3} />
        </svg>
      </div>

      {/* Subtitle */}
      <div style={{
        position: 'absolute',
        top: 340,
        left: 100,
        fontFamily: 'Inter, sans-serif',
        fontSize: 42,
        fontWeight: 700,
        color: '#000000',
        opacity: sp(40),
        fontStyle: 'italic',
      }}>
        bröckelt unter der Last. Stück für Stück.
      </div>

      {/* THREE ELEMENT CARDS - HOUSE, CAR, MUSIC */}
      <div style={{
        position: 'absolute',
        top: 460,
        left: 100,
        right: 100,
        display: 'flex',
        gap: 40,
        justifyContent: 'space-between',
      }}>
        {[
          {
            label: 'HAUS',
            sub: 'Tilgung läuft weiter',
            icon: 'house',
            spr: elem1,
          },
          {
            label: 'AUTO',
            sub: 'Leasingrate steigt',
            icon: 'car',
            spr: elem2,
          },
          {
            label: 'MUSIKSCHULE',
            sub: 'Wird zum Luxus',
            icon: 'music',
            spr: elem3,
          },
        ].map((item, i) => (
          <div key={i} style={{
            width: 560,
            height: 420,
            backgroundColor: i === 1 ? '#000000' : '#EEEEEE',
            border: '5px solid #000000',
            padding: '34px',
            position: 'relative',
            transform: `translateY(${interpolate(item.spr, [0, 1], [80, 0])}px) rotate(${interpolate(item.spr, [0, 1], [-3 + i * 3, 0])}deg)`,
            opacity: item.spr,
            display: 'flex',
            flexDirection: 'column',
            justifyContent: 'space-between',
          }}>
            {/* Icon */}
            <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', height: 180 }}>
              {item.icon === 'house' && (
                <svg width="180" height="180" viewBox="0 0 180 180">
                  <polygon points="90,20 160,90 145,90 145,160 35,160 35,90 20,90" fill="none" stroke="#000000" strokeWidth="7" />
                  <rect x="70" y="100" width="40" height="60" fill="none" stroke="#000000" strokeWidth="6" />
                  <line x1="35" y1="160" x2="145" y2="160" stroke="#000000" strokeWidth="7" />
                </svg>
              )}
              {item.icon === 'car' && (
                <svg width="220" height="160" viewBox="0 0 220 160">
                  <path d="M 20 100 L 50 60 L 170 60 L 200 100 L 200 130 L 20 130 Z" fill="none" stroke="#A5CF83" strokeWidth="7" />
                  <line x1="80" y1="60" x2="80" y2="100" stroke="#A5CF83" strokeWidth="5" />
                  <line x1="140" y1="60" x2="140" y2="100" stroke="#A5CF83" strokeWidth="5" />
                  <circle cx="60" cy="135" r="18" fill="none" stroke="#A5CF83" strokeWidth="7" />
                  <circle cx="160" cy="135" r="18" fill="none" stroke="#A5CF83" strokeWidth="7" />
                </svg>
              )}
              {item.icon === 'music' && (
                <svg width="180" height="180" viewBox="0 0 180 180">
                  <path d="M 70 30 L 70 130" stroke="#000000" strokeWidth="7" strokeLinecap="round" />
                  <path d="M 130 20 L 130 120" stroke="#000000" strokeWidth="7" strokeLinecap="round" />
                  <path d="M 70 30 Q 100 20, 130 20" stroke="#000000" strokeWidth="7" fill="none" />
                  <ellipse cx="55" cy="135" rx="22" ry="16" fill="#000000" transform="rotate(-15 55 135)" />
                  <ellipse cx="115" cy="125" rx="22" ry="16" fill="#000000" transform="rotate(-15 115 125)" />
                </svg>
              )}
            </div>

            {/* Label */}
            <div>
              <div style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 78,
                fontWeight: 900,
                color: i === 1 ? '#A5CF83' : '#000000',
                lineHeight: 0.9,
                letterSpacing: -1,
              }}>
                {item.label}
              </div>
              <div style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 24,
                fontWeight: 700,
                color: i === 1 ? '#EEEEEE' : '#2D2D2D',
                marginTop: 8,
                letterSpacing: 1,
              }}>
                {item.sub}
              </div>
            </div>

            {/* Tension indicator */}
            <div style={{
              position: 'absolute',
              top: 20,
              right: 20,
              backgroundColor: '#A5CF83',
              border: '3px solid #000000',
              padding: '4px 12px',
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 18,
              fontWeight: 800,
              color: '#000000',
            }}>
              ENG
            </div>
          </div>
        ))}
      </div>

      {/* PRESSURE METER bottom */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        left: 100,
        right: 250,
        opacity: sp(200),
      }}>
        <div style={{
          display: 'flex',
          justifyContent: 'space-between',
          marginBottom: 6,
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 18,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 2,
        }}>
          <span>FINANZIELLER DRUCK</span>
          <span>KRITISCH</span>
        </div>
        <div style={{
          height: 18,
          backgroundColor: '#EEEEEE',
          border: '3px solid #000000',
          overflow: 'hidden',
        }}>
          <div style={{
            width: `${pressureFill * 100}%`,
            height: '100%',
            background: 'repeating-linear-gradient(45deg, #000000 0px, #000000 8px, #2D2D2D 8px, #2D2D2D 16px)',
          }} />
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.35,
      }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene5_MittelschichtBroeckelt;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene6_FinanzielleBildung',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene6_FinanzielleBildung: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Three emotions
  const em1 = sp(20);
  const em2 = sp(50);
  const em3 = sp(80);

  // Strike through
  const strikeWidth = interpolate(sp(160), [0, 1], [0, 1]);

  // Solution reveal
  const solutionScale = sp(220, { damping: 7, stiffness: 90 });
  const solutionOp = sp(220);

  // Brain/knowledge icon
  const brainPulse = 1 + Math.sin(frame * 0.1) * 0.05;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A5CF83', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 100%, rgba(238,238,238,0.25) 0%, transparent 50%), linear-gradient(180deg, rgba(0,0,0,0.15) 0%, transparent 50%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        left: -200, top: -200,
        width: 700, height: 700,
        borderRadius: '50%',
        border: '3px solid #000000',
        opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute',
        right: -150, bottom: -150,
        width: 500, height: 500,
        backgroundColor: '#000000',
        opacity: 0.06,
        borderRadius: 50,
        transform: `rotate(${frame * 0.08}deg)`,
      }} />

      {/* Wave texture */}
      <svg style={{ position: 'absolute', width: '100%', height: '100%', opacity: 0.1 }}>
        {[0, 1, 2, 3, 4].map((i) => (
          <path key={i}
            d={`M 0 ${200 + i * 200 + Math.sin(frame * 0.05 + i) * 20} Q 480 ${180 + i * 200} 960 ${200 + i * 200} T 1920 ${200 + i * 200}`}
            stroke="#000000" strokeWidth="2" fill="none" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = seededRandom(i + 13);
        const x = seed * 1920;
        const y = ((seededRandom(i + 80) * 1080) - frame * 0.5 + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 5, height: 5,
            backgroundColor: '#EEEEEE',
            opacity: 0.4,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* TOP: THREE EMOTIONS - stacked diagonal */}
      <div style={{
        position: 'absolute',
        top: 80,
        left: 100,
        right: 100,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 26,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 6,
          marginBottom: 16,
          opacity: sp(5),
        }}>
          DIE LAST AUF DEN SCHULTERN
        </div>

        <div style={{ position: 'relative', height: 380 }}>
          {/* Angst */}
          <div style={{
            position: 'absolute',
            left: 0, top: 0,
            transform: `translateX(${interpolate(em1, [0, 1], [-100, 0])}px)`,
            opacity: em1,
            display: 'flex',
            alignItems: 'baseline',
            gap: 30,
          }}>
            <span style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 30,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.6,
            }}>01 //</span>
            <span style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 168,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 0.9,
              letterSpacing: -3,
              position: 'relative',
            }}>
              ANGST
              <div style={{
                position: 'absolute',
                left: 0, top: '52%',
                height: 12,
                width: `${strikeWidth * 100}%`,
                backgroundColor: '#A5CF83',
                border: '3px solid #000000',
              }} />
            </span>
          </div>

          {/* Wut */}
          <div style={{
            position: 'absolute',
            left: 380, top: 110,
            transform: `translateX(${interpolate(em2, [0, 1], [-100, 0])}px)`,
            opacity: em2,
            display: 'flex',
            alignItems: 'baseline',
            gap: 30,
          }}>
            <span style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 30,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.6,
            }}>02 //</span>
            <span style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 168,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 0.9,
              letterSpacing: -3,
              WebkitTextStroke: '4px #000000',
              position: 'relative',
            }}>
              WUT
              <div style={{
                position: 'absolute',
                left: 0, top: '52%',
                height: 12,
                width: `${strikeWidth * 100}%`,
                backgroundColor: '#A5CF83',
                border: '3px solid #000000',
              }} />
            </span>
          </div>

          {/* Müdigkeit */}
          <div style={{
            position: 'absolute',
            left: 700, top: 220,
            transform: `translateX(${interpolate(em3, [0, 1], [-100, 0])}px)`,
            opacity: em3,
            display: 'flex',
            alignItems: 'baseline',
            gap: 30,
          }}>
            <span style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 30,
              fontWeight: 700,
              color: '#000000',
              opacity: 0.6,
            }}>03 //</span>
            <span style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 168,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 0.9,
              letterSpacing: -3,
              fontStyle: 'italic',
              position: 'relative',
            }}>
              MÜDIGKEIT
              <div style={{
                position: 'absolute',
                left: 0, top: '52%',
                height: 12,
                width: `${strikeWidth * 100}%`,
                backgroundColor: '#A5CF83',
                border: '3px solid #000000',
              }} />
            </span>
          </div>
        </div>
      </div>

      {/* TRANSITION ARROW */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: 540,
        transform: 'translateX(-50%)',
        opacity: sp(195),
      }}>
        <svg width="80" height="80" viewBox="0 0 80 80">
          <path d="M 40 10 L 40 60 M 40 60 L 20 40 M 40 60 L 60 40"
            stroke="#000000" strokeWidth="7" fill="none" strokeLinecap="round" />
        </svg>
      </div>

      {/* SOLUTION BOX */}
      <div style={{
        position: 'absolute',
        bottom: 100,
        left: 100,
        right: 100,
        opacity: solutionOp,
        transform: `scale(${solutionScale})`,
        transformOrigin: 'center',
      }}>
        <div style={{
          backgroundColor: '#000000',
          padding: '40px 60px',
          border: '6px solid #000000',
          boxShadow: '14px 14px 0 #EEEEEE',
          display: 'flex',
          alignItems: 'center',
          gap: 50,
        }}>
          {/* Brain/knowledge icon */}
          <div style={{ transform: `scale(${brainPulse})`, flexShrink: 0 }}>
            <svg width="180" height="180" viewBox="0 0 180 180">
              {/* Book open */}
              <path d="M 30 60 Q 30 50, 40 50 L 85 50 Q 90 50, 90 55 L 90 145 Q 90 150, 85 150 L 40 150 Q 30 150, 30 140 Z"
                fill="none" stroke="#A5CF83" strokeWidth="6" />
              <path d="M 150 60 Q 150 50, 140 50 L 95 50 Q 90 50, 90 55 L 90 145 Q 90 150, 95 150 L 140 150 Q 150 150, 150 140 Z"
                fill="none" stroke="#A5CF83" strokeWidth="6" />
              {/* Lines on pages */}
              {[0, 1, 2].map((i) => (
                <line key={`l${i}`} x1="45" y1={75 + i * 18} x2="80" y2={75 + i * 18} stroke="#A5CF83" strokeWidth="4" opacity={0.6 + i * 0.1} />
              ))}
              {[0, 1, 2].map((i) => (
                <line key={`r${i}`} x1="100" y1={75 + i * 18} x2="135" y2={75 + i * 18} stroke="#A5CF83" strokeWidth="4" opacity={0.6 + i * 0.1} />
              ))}
              {/* Glow circle */}
              <circle cx="90" cy="30" r="18" fill="none" stroke="#A5CF83" strokeWidth="4" />
              <line x1="90" y1="5" x2="90" y2="12" stroke="#A5CF83" strokeWidth="4" strokeLinecap="round" />
              <line x1="65" y1="30" x2="72" y2="30" stroke="#A5CF83" strokeWidth="4" strokeLinecap="round" />
              <line x1="108" y1="30" x2="115" y2="30" stroke="#A5CF83" strokeWidth="4" strokeLinecap="round" />
            </svg>
          </div>

          {/* Text */}
          <div>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 26,
              fontWeight: 800,
              color: '#A5CF83',
              letterSpacing: 4,
              marginBottom: 8,
            }}>
              DIE LÖSUNG
            </div>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 132,
              fontWeight: 900,
              color: '#EEEEEE',
              lineHeight: 0.9,
              letterSpacing: -2,
            }}>
              FINANZIELLE BILDUNG
            </div>
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 32,
              fontWeight: 700,
              color: '#A5CF83',
              marginTop: 14,
              fontStyle: 'italic',
            }}>
              = deine Überlebensstrategie.
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.35,
      }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene6_FinanzielleBildung;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene7_SparplanAnpassen',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene7_SparplanAnpassen: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Bad path
  const badOp = sp(50);
  const badX = interpolate(sp(50), [0, 1], [-80, 0]);

  // Good path
  const goodOp = sp(110);
  const goodX = interpolate(sp(110), [0, 1], [80, 0]);

  // Counter animation
  const counter200 = Math.floor(interpolate(frame, [180, 240], [200, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Divider
  const divH = interpolate(sp(40), [0, 1], [0, 600]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#A5CF83', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(0,0,0,0.12) 0%, transparent 50%, rgba(238,238,238,0.18) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        left: '50%', top: '50%',
        width: 1200, height: 1200,
        marginLeft: -600, marginTop: -600,
        border: '2px solid #EEEEEE',
        borderRadius: '50%',
        opacity: 0.08,
      }} />
      <div style={{
        position: 'absolute',
        left: 100, top: 100,
        width: 400, height: 400,
        backgroundColor: '#000000',
        opacity: 0.04,
        borderRadius: 20,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Vertical lines texture */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(90deg, rgba(0,0,0,0.06) 1px, transparent 1px)',
        backgroundSize: '50px 100%',
      }} />

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = seededRandom(i + 17);
        const x = ((seed * 1920) + frame * 0.4) % 1920;
        const y = seededRandom(i + 60) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 4, height: 4,
            backgroundColor: '#000000',
            opacity: 0.2,
            transform: 'rotate(45deg)',
          }} />
        );
      })}

      {/* TOP HEADLINE */}
      <div style={{
        position: 'absolute',
        top: 70,
        left: '50%',
        transform: 'translateX(-50%)',
        textAlign: 'center',
        opacity: sp(5),
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 26,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 6,
          marginBottom: 12,
        }}>
          ZWEI WEGE — EINE ENTSCHEIDUNG
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 130,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 0.9,
          letterSpacing: -3,
        }}>
          WIE REAGIERST <span style={{ fontStyle: 'italic' }}>du</span>?
        </div>
      </div>

      {/* CENTER DIVIDER */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: 320,
        width: 6,
        height: divH,
        backgroundColor: '#000000',
        marginLeft: -3,
      }} />

      {/* VS CIRCLE */}
      <div style={{
        position: 'absolute',
        left: '50%',
        top: 590,
        marginLeft: -65,
        opacity: sp(140),
        transform: `scale(${sp(140, { damping: 7 })})`,
      }}>
        <div style={{
          width: 130, height: 130,
          borderRadius: '50%',
          backgroundColor: '#A5CF83',
          border: '6px solid #000000',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          fontFamily: 'Anton, sans-serif',
          fontSize: 56,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -2,
        }}>
          VS
        </div>
      </div>

      {/* LEFT SIDE: PANIK */}
      <div style={{
        position: 'absolute',
        left: 100,
        top: 380,
        width: 780,
        opacity: badOp,
        transform: `translateX(${badX}px)`,
      }}>
        {/* Tag */}
        <div style={{
          display: 'inline-block',
          backgroundColor: '#EEEEEE',
          border: '4px solid #000000',
          padding: '8px 22px',
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 22,
          fontWeight: 800,
          color: '#000000',
          marginBottom: 20,
          letterSpacing: 2,
        }}>
          OPTION A — PANIK
        </div>

        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 120,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 0.9,
          letterSpacing: -2,
          position: 'relative',
          display: 'inline-block',
        }}>
          KÜNDIGEN
          {/* Strike-through */}
          <div style={{
            position: 'absolute',
            left: -10,
            top: '50%',
            width: interpolate(sp(80), [0, 1], [0, 540]),
            height: 10,
            backgroundColor: '#000000',
            transform: 'rotate(-3deg)',
          }} />
        </div>

        {/* Down arrow chart */}
        <div style={{ marginTop: 30 }}>
          <svg width="500" height="200" viewBox="0 0 500 200">
            <line x1="20" y1="20" x2="20" y2="180" stroke="#000000" strokeWidth="3" />
            <line x1="20" y1="180" x2="480" y2="180" stroke="#000000" strokeWidth="3" />
            <polyline points="40,40 120,60 200,80 280,110 360,150 440,170"
              fill="none" stroke="#000000" strokeWidth="6" strokeLinecap="round" strokeDasharray="800"
              strokeDashoffset={interpolate(sp(90), [0, 1], [800, 0])} />
            <text x="440" y="195" fontFamily="JetBrains Mono" fontSize="22" fontWeight="700" fill="#000000" textAnchor="end">REALE VERLUSTE</text>
          </svg>
        </div>

        {/* Result */}
        <div style={{
          marginTop: 20,
          backgroundColor: '#000000',
          padding: '14px 24px',
          display: 'inline-block',
          border: '4px solid #000000',
        }}>
          <span style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 28,
            fontWeight: 800,
            color: '#A5CF83',
            letterSpacing: 1,
          }}>
            Resultat: 2022 verkauft = verloren
          </span>
        </div>
      </div>

      {/* RIGHT SIDE: VERSTEHEN */}
      <div style={{
        position: 'absolute',
        right: 100,
        top: 380,
        width: 780,
        opacity: goodOp,
        transform: `translateX(${goodX}px)`,
        textAlign: 'left',
      }}>
        {/* Tag */}
        <div style={{
          display: 'inline-block',
          backgroundColor: '#000000',
          border: '4px solid #000000',
          padding: '8px 22px',
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 22,
          fontWeight: 800,
          color: '#A5CF83',
          marginBottom: 20,
          letterSpacing: 2,
        }}>
          OPTION B — VERSTEHEN
        </div>

        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 120,
          fontWeight: 900,
          color: '#EEEEEE',
          lineHeight: 0.9,
          letterSpacing: -2,
          WebkitTextStroke: '4px #000000',
        }}>
          ANPASSEN
        </div>

        {/* Slider visualization */}
        <div style={{ marginTop: 30 }}>
          <div style={{
            display: 'flex',
            justifyContent: 'space-between',
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 24,
            fontWeight: 700,
            color: '#000000',
            marginBottom: 10,
          }}>
            <span>SPARPLAN</span>
            <span style={{ fontFamily: 'Anton', fontSize: 40, fontWeight: 900 }}>{counter200}€</span>
          </div>
          <div style={{
            width: 500,
            height: 22,
            backgroundColor: '#EEEEEE',
            border: '4px solid #000000',
            position: 'relative',
          }}>
            <div style={{
              width: `${interpolate(counter200, [100, 200], [40, 100])}%`,
              height: '100%',
              backgroundColor: '#000000',
            }} />
            {/* Handle */}
            <div style={{
              position: 'absolute',
              left: `${interpolate(counter200, [100, 200], [40, 100])}%`,
              top: -8,
              width: 32,
              height: 32,
              backgroundColor: '#A5CF83',
              border: '4px solid #000000',
              borderRadius: '50%',
              marginLeft: -16,
            }} />
          </div>
          <div style={{
            marginTop: 14,
            fontFamily: 'Inter, sans-serif',
            fontSize: 22,
            fontWeight: 700,
            color: '#000000',
            fontStyle: 'italic',
          }}>
            200 → 100 € weiter investieren statt aufgeben
          </div>
        </div>

        {/* Result */}
        <div style={{
          marginTop: 30,
          backgroundColor: '#EEEEEE',
          padding: '14px 24px',
          display: 'inline-block',
          border: '4px solid #000000',
        }}>
          <span style={{
            fontFamily: 'Inter, sans-serif',
            fontSize: 28,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: 1,
          }}>
            Resultat: durchgehalten = gewonnen
          </span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.35,
      }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene7_SparplanAnpassen;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene8_WissenIstMacht',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene8_WissenIstMacht: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Word zoom
  const wissenScale = sp(15, { damping: 8, stiffness: 100 });
  const machtScale = sp(60, { damping: 8, stiffness: 100 });

  // Forecast graph
  const forecastDraw = interpolate(frame, [180, 320], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Warning blink
  const warningOp = 0.6 + Math.sin(frame * 0.2) * 0.4;

  return (
    <AbsoluteFill style={{ backgroundColor: '#A5CF83', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 30%, rgba(238,238,238,0.25) 0%, transparent 50%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        right: -100, top: -100,
        width: 600, height: 600,
        border: '4px solid #000000',
        opacity: 0.05,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute',
        left: -150, bottom: -150,
        width: 500, height: 500,
        backgroundColor: '#EEEEEE',
        opacity: 0.08,
        borderRadius: '50%',
      }} />

      {/* Grid + dot texture */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(circle, rgba(0,0,0,0.1) 1.5px, transparent 1.5px)',
        backgroundSize: '60px 60px',
      }} />

      {/* Particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const seed = seededRandom(i + 19);
        const x = seed * 1920;
        const y = ((seededRandom(i + 90) * 1080) + frame * 0.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 5, height: 5,
            backgroundColor: '#000000',
            opacity: 0.2,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* TOP STATEMENT — WISSEN = MACHT */}
      <div style={{
        position: 'absolute',
        top: 90,
        left: '50%',
        transform: 'translateX(-50%)',
        textAlign: 'center',
        display: 'flex',
        alignItems: 'center',
        gap: 40,
      }}>
        <span style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 220,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 0.9,
          letterSpacing: -4,
          transform: `scale(${wissenScale})`,
          transformOrigin: 'right center',
          display: 'inline-block',
        }}>
          WISSEN
        </span>
        <span style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 180,
          fontWeight: 900,
          color: '#000000',
          opacity: sp(45),
          transform: `rotate(${Math.sin(frame * 0.05) * 5}deg)`,
        }}>
          =
        </span>
        <span style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 220,
          fontWeight: 900,
          color: '#EEEEEE',
          lineHeight: 0.9,
          letterSpacing: -4,
          WebkitTextStroke: '5px #000000',
          transform: `scale(${machtScale})`,
          transformOrigin: 'left center',
          display: 'inline-block',
        }}>
          MACHT
        </span>
      </div>

      {/* Divider */}
      <div style={{
        position: 'absolute',
        top: 360,
        left: 100,
        right: 100,
        height: 4,
        backgroundColor: '#000000',
        transformOrigin: 'center',
        transform: `scaleX(${interpolate(sp(120), [0, 1], [0, 1])})`,
      }} />

      {/* QUESTION */}
      <div style={{
        position: 'absolute',
        top: 410,
        left: 100,
        opacity: sp(140),
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 30,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 4,
          marginBottom: 10,
          fontStyle: 'italic',
        }}>
          Die unbequeme Frage:
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 100,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 0.95,
          letterSpacing: -2,
        }}>
          Liegt das Schlimmste hinter uns?
        </div>
      </div>

      {/* LEFT BOTTOM: FORECAST GRAPH */}
      <div style={{
        position: 'absolute',
        left: 100,
        bottom: 90,
        width: 900,
        opacity: sp(170),
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 22,
          fontWeight: 700,
          color: '#000000',
          marginBottom: 14,
          letterSpacing: 2,
        }}>
          PROGNOSE-KURVE / ÖLPREIS-ENTWICKLUNG
        </div>

        <svg width="900" height="320" viewBox="0 0 900 320">
          {/* Grid */}
          {[0, 1, 2, 3, 4].map((i) => (
            <line key={i} x1="0" y1={60 + i * 60} x2="900" y2={60 + i * 60} stroke="#000000" strokeWidth="1" opacity="0.15" />
          ))}

          {/* Past data (solid) */}
          <polyline points="20,260 100,250 180,230 260,210 340,180 420,160"
            fill="none" stroke="#000000" strokeWidth="6" strokeLinecap="round" />

          {/* Vertical now-line */}
          <line x1="420" y1="40" x2="420" y2="280" stroke="#000000" strokeWidth="3" strokeDasharray="8 8" />
          <rect x="380" y="20" width="80" height="30" fill="#000000" />
          <text x="420" y="42" fontFamily="JetBrains Mono" fontSize="20" fontWeight="800" fill="#A5CF83" textAnchor="middle">JETZT</text>

          {/* Future (dashed up) */}
          <polyline points={`420,160 ${500 + 380 * forecastDraw - 380},${160 - 30 * forecastDraw} ${580 + 300 * forecastDraw - 300},${160 - 55 * forecastDraw} ${660 + 220 * forecastDraw - 220},${160 - 80 * forecastDraw} ${740 + 140 * forecastDraw - 140},${160 - 100 * forecastDraw} ${820 + 60 * forecastDraw - 60},${160 - 115 * forecastDraw} 880,${160 - 130 * forecastDraw}`}
            fill="none" stroke="#000000" strokeWidth="6" strokeLinecap="round" strokeDasharray="14 10" opacity={forecastDraw} />

          {/* Arrow */}
          {forecastDraw > 0.9 && (
            <g transform={`translate(880, ${160 - 130 * forecastDraw})`}>
              <path d="M 0 0 L -18 18 M 0 0 L -18 -18 M 0 0 L 0 -25" stroke="#000000" strokeWidth="6" strokeLinecap="round" fill="none" />
            </g>
          )}

          {/* X labels */}
          <text x="20" y="305" fontFamily="JetBrains Mono" fontSize="18" fontWeight="700" fill="#000000">2024</text>
          <text x="420" y="305" fontFamily="JetBrains Mono" fontSize="18" fontWeight="700" fill="#000000" textAnchor="middle">2026</text>
          <text x="880" y="305" fontFamily="JetBrains Mono" fontSize="18" fontWeight="700" fill="#000000" textAnchor="end">2027+</text>
        </svg>
      </div>

      {/* RIGHT BOTTOM: ANSWER BOX */}
      <div style={{
        position: 'absolute',
        right: 100,
        bottom: 120,
        opacity: sp(250),
        transform: `scale(${sp(250, { damping: 7 })})`,
      }}>
        {/* Warning sign */}
        <div style={{
          display: 'flex',
          alignItems: 'center',
          gap: 16,
          marginBottom: 14,
          opacity: warningOp,
        }}>
          <svg width="50" height="44" viewBox="0 0 50 44">
            <polygon points="25,4 48,40 2,40" fill="#000000" stroke="#000000" strokeWidth="2" />
            <text x="25" y="35" fontFamily="Anton" fontSize="28" fontWeight="900" fill="#A5CF83" textAnchor="middle">!</text>
          </svg>
          <span style={{
            fontFamily: 'JetBrains Mono, monospace',
            fontSize: 22,
            fontWeight: 800,
            color: '#000000',
            letterSpacing: 3,
          }}>
            EHRLICHE ANTWORT
          </span>
        </div>

        <div style={{
          backgroundColor: '#000000',
          padding: '36px 50px',
          border: '6px solid #000000',
          boxShadow: '12px 12px 0 #EEEEEE',
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 120,
            fontWeight: 900,
            color: '#EEEEEE',
            lineHeight: 0.9,
            letterSpacing: -2,
          }}>
            WAHRSCHEIN-
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif',
            fontSize: 120,
            fontWeight: 900,
            color: '#A5CF83',
            lineHeight: 0.9,
            letterSpacing: -2,
          }}>
            LICH NICHT.
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.35,
      }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene8_WissenIstMacht;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene9_EZBZinsen',
  durationInSeconds: 20,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene9_EZBZinsen: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // EZB building
  const buildingScale = sp(20);

  // Lock animation
  const lockShake = frame > 80 && frame < 150 ? Math.sin(frame * 0.8) * 3 : 0;

  // Interest rate dial
  const dialAngle = interpolate(frame, [150, 280], [-90, 45], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Three consequence rows
  const row1 = sp(180);
  const row2 = sp(220);
  const row3 = sp(260);

  return (
    <AbsoluteFill style={{ backgroundColor: '#A5CF83', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(238,238,238,0.2) 0%, transparent 50%, rgba(0,0,0,0.15) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        left: '50%', top: 200,
        width: 800, height: 800,
        marginLeft: -400,
        border: '2px solid #000000',
        opacity: 0.05,
        borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute',
        right: -200, top: -200,
        width: 500, height: 500,
        backgroundColor: '#EEEEEE',
        opacity: 0.07,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Crosshatch texture */}
      <AbsoluteFill style={{
        backgroundImage: 'linear-gradient(0deg, rgba(0,0,0,0.06) 1px, transparent 1px)',
        backgroundSize: '100% 50px',
      }} />

      {/* Particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const seed = seededRandom(i + 21);
        const x = seed * 1920;
        const y = ((seededRandom(i + 100) * 1080) + frame * 0.3) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 4, height: 4,
            backgroundColor: '#000000',
            opacity: 0.2,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* TOP TAG */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 100,
        opacity: sp(5),
        display: 'flex',
        alignItems: 'center',
        gap: 16,
      }}>
        <div style={{
          width: 50, height: 50,
          backgroundColor: '#000000',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          fontFamily: 'Anton',
          fontSize: 26,
          fontWeight: 900,
          color: '#A5CF83',
        }}>€</div>
        <span style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 28,
          fontWeight: 800,
          color: '#000000',
          letterSpacing: 4,
        }}>
          EUROPÄISCHE ZENTRALBANK / EZB
        </span>
      </div>

      {/* HEADLINE */}
      <div style={{
        position: 'absolute',
        top: 130,
        left: 100,
        opacity: sp(15),
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 160,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 0.9,
          letterSpacing: -3,
        }}>
          ZINSEN BLEIBEN.
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 160,
          fontWeight: 900,
          color: '#EEEEEE',
          lineHeight: 0.9,
          letterSpacing: -3,
          WebkitTextStroke: '5px #000000',
          marginTop: -8,
        }}>
          EINGEFROREN.
        </div>
      </div>

      {/* LEFT: LOCKED ZINS DIAL */}
      <div style={{
        position: 'absolute',
        left: 120,
        bottom: 100,
        opacity: sp(50),
        transform: `scale(${buildingScale}) translateX(${lockShake}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 800,
          color: '#000000',
          letterSpacing: 3,
          marginBottom: 14,
        }}>
          LEITZINS-REGLER
        </div>

        <svg width="360" height="360" viewBox="0 0 360 360">
          {/* Outer circle */}
          <circle cx="180" cy="180" r="160" fill="#EEEEEE" stroke="#000000" strokeWidth="8" />
          <circle cx="180" cy="180" r="160" fill="none" stroke="#000000" strokeWidth="2" strokeDasharray="6 8" opacity="0.4" />

          {/* Tick marks */}
          {[-90, -60, -30, 0, 30, 60, 90].map((deg, i) => {
            const rad = (deg * Math.PI) / 180;
            const x1 = 180 + Math.cos(rad - Math.PI / 2) * 140;
            const y1 = 180 + Math.sin(rad - Math.PI / 2) * 140;
            const x2 = 180 + Math.cos(rad - Math.PI / 2) * 120;
            const y2 = 180 + Math.sin(rad - Math.PI / 2) * 120;
            return <line key={i} x1={x1} y1={y1} x2={x2} y2={y2} stroke="#000000" strokeWidth="4" />;
          })}

          {/* Labels */}
          <text x="40" y="200" fontFamily="JetBrains Mono" fontSize="20" fontWeight="800" fill="#000000">LOW</text>
          <text x="290" y="200" fontFamily="JetBrains Mono" fontSize="20" fontWeight="800" fill="#000000">HIGH</text>

          {/* Needle */}
          <g transform={`rotate(${dialAngle} 180 180)`}>
            <line x1="180" y1="180" x2="180" y2="60" stroke="#000000" strokeWidth="10" strokeLinecap="round" />
            <polygon points="180,55 170,75 190,75" fill="#000000" />
          </g>

          {/* Center hub */}
          <circle cx="180" cy="180" r="30" fill="#000000" />
          <circle cx="180" cy="180" r="14" fill="#A5CF83" />

          {/* Lock icon */}
          <g transform="translate(150, 240)">
            <rect x="0" y="20" width="60" height="40" fill="#000000" rx="4" />
            <path d="M 12 20 L 12 10 Q 12 -2, 30 -2 Q 48 -2, 48 10 L 48 20" stroke="#000000" strokeWidth="6" fill="none" />
            <circle cx="30" cy="40" r="4" fill="#A5CF83" />
          </g>
        </svg>
      </div>

      {/* RIGHT: THREE CONSEQUENCES */}
      <div style={{
        position: 'absolute',
        right: 100,
        bottom: 90,
        width: 880,
      }}>
        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 26,
          fontWeight: 800,
          color: '#000000',
          letterSpacing: 4,
          marginBottom: 20,
          opacity: sp(170),
        }}>
          DIE FOLGEN FÜR DEINEN ALLTAG
        </div>

        {[
          {
            label: 'KREDITE',
            value: 'TEUER',
            desc: 'Immobilien­finanzierung bleibt schwer',
            icon: 'credit',
            spr: row1,
          },
          {
            label: 'KONSUM',
            value: 'GEDÄMPFT',
            desc: 'Weniger Kaufkraft im Alltag',
            icon: 'cart',
            spr: row2,
          },
          {
            label: 'WIRTSCHAFT',
            value: 'STAGNIERT',
            desc: 'Wachstumsmotor stockt weiter',
            icon: 'gear',
            spr: row3,
          },
        ].map((item, i) => (
          <div key={i} style={{
            display: 'flex',
            alignItems: 'center',
            backgroundColor: i === 1 ? '#000000' : '#EEEEEE',
            border: '5px solid #000000',
            padding: '16px 24px',
            marginBottom: 14,
            transform: `translateX(${interpolate(item.spr, [0, 1], [100, 0])}px)`,
            opacity: item.spr,
            gap: 24,
          }}>
            {/* Icon */}
            <div style={{
              width: 70, height: 70,
              backgroundColor: i === 1 ? '#A5CF83' : '#000000',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              flexShrink: 0,
            }}>
              {item.icon === 'credit' && (
                <svg width="40" height="30" viewBox="0 0 40 30">
                  <rect x="2" y="2" width="36" height="26" fill="none" stroke={i === 1 ? '#000000' : '#A5CF83'} strokeWidth="3" rx="3" />
                  <line x1="2" y1="10" x2="38" y2="10" stroke={i === 1 ? '#000000' : '#A5CF83'} strokeWidth="3" />
                  <line x1="8" y1="20" x2="20" y2="20" stroke={i === 1 ? '#000000' : '#A5CF83'} strokeWidth="3" />
                </svg>
              )}
              {item.icon === 'cart' && (
                <svg width="40" height="36" viewBox="0 0 40 36">
                  <path d="M 4 4 L 10 4 L 14 24 L 34 24 L 38 10 L 12 10" fill="none" stroke={i === 1 ? '#000000' : '#A5CF83'} strokeWidth="3" />
                  <circle cx="16" cy="30" r="3" fill={i === 1 ? '#000000' : '#A5CF83'} />
                  <circle cx="30" cy="30" r="3" fill={i === 1 ? '#000000' : '#A5CF83'} />
                </svg>
              )}
              {item.icon === 'gear' && (
                <svg width="40" height="40" viewBox="0 0 40 40" style={{ transform: `rotate(${frame * 2}deg)` }}>
                  <path d="M 20 4 L 22 10 L 28 8 L 28 14 L 34 16 L 32 22 L 36 26 L 30 28 L 30 34 L 24 32 L 20 36 L 16 32 L 10 34 L 10 28 L 4 26 L 8 22 L 6 16 L 12 14 L 12 8 L 18 10 Z"
                    fill="none" stroke={i === 1 ? '#000000' : '#A5CF83'} strokeWidth="3" />
                  <circle cx="20" cy="20" r="5" fill="none" stroke={i === 1 ? '#000000' : '#A5CF83'} strokeWidth="3" />
                </svg>
              )}
            </div>

            {/* Label */}
            <div style={{ minWidth: 220 }}>
              <div style={{
                fontFamily: 'Inter, sans-serif',
                fontSize: 22,
                fontWeight: 700,
                color: i === 1 ? '#A5CF83' : '#2D2D2D',
                letterSpacing: 2,
                marginBottom: 2,
              }}>
                {item.label}
              </div>
              <div style={{
                fontFamily: 'Anton, sans-serif',
                fontSize: 60,
                fontWeight: 900,
                color: i === 1 ? '#EEEEEE' : '#000000',
                lineHeight: 1,
                letterSpacing: -1,
              }}>
                {item.value}
              </div>
            </div>

            {/* Desc */}
            <div style={{
              fontFamily: 'Inter, sans-serif',
              fontSize: 22,
              fontWeight: 600,
              color: i === 1 ? '#EEEEEE' : '#000000',
              flex: 1,
              lineHeight: 1.3,
            }}>
              {item.desc}
            </div>
          </div>
        ))}
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(0,0,0,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.35,
      }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene9_EZBZinsen;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_SparsamkeitBasis',
  durationInSeconds: 21,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_SparsamkeitBasis: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pyramid blocks rising
  const block1 = sp(40);
  const block2 = sp(90);
  const block3 = sp(140);
  const block4 = sp(190);

  // Title
  const titleOp = sp(10);

  // Final stamp
  const stampScale = sp(280, { damping: 7, stiffness: 90 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#A5CF83', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(238,238,238,0.15) 0%, transparent 40%, rgba(0,0,0,0.18) 100%)',
      }} />

      {/* Shape layers */}
      <div style={{
        position: 'absolute',
        left: -150, top: -150,
        width: 600, height: 600,
        border: '3px solid #EEEEEE',
        opacity: 0.1,
        borderRadius: '50%',
      }} />
      <div style={{
        position: 'absolute',
        right: 100, bottom: 100,
        width: 400, height: 400,
        backgroundColor: '#000000',
        opacity: 0.06,
        borderRadius: 20,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Dot texture */}
      <AbsoluteFill style={{
        backgroundImage: 'radial-gradient(circle, rgba(0,0,0,0.08) 1.5px, transparent 1.5px)',
        backgroundSize: '50px 50px',
      }} />

      {/* Particles - rising upward */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = seededRandom(i + 23);
        const x = seed * 1920;
        const y = ((seededRandom(i + 100) * 1080) - frame * 0.6 + 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y,
            width: 5, height: 5,
            backgroundColor: '#EEEEEE',
            opacity: 0.45,
            borderRadius: '50%',
          }} />
        );
      })}

      {/* TOP TAG */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: '50%',
        transform: `translateX(-50%)`,
        opacity: titleOp,
        display: 'flex',
        alignItems: 'center',
        gap: 16,
      }}>
        <div style={{ width: 40, height: 4, backgroundColor: '#000000' }} />
        <span style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 26,
          fontWeight: 800,
          color: '#000000',
          letterSpacing: 6,
        }}>
          KAPITEL-FAZIT
        </span>
        <div style={{ width: 40, height: 4, backgroundColor: '#000000' }} />
      </div>

      {/* HEADLINE */}
      <div style={{
        position: 'absolute',
        top: 120,
        left: '50%',
        transform: 'translateX(-50%)',
        textAlign: 'center',
        opacity: titleOp,
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 168,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 0.9,
          letterSpacing: -3,
        }}>
          SPARSAMKEIT
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 88,
          fontWeight: 400,
          color: '#000000',
          fontStyle: 'italic',
          lineHeight: 1,
          marginTop: -4,
        }}>
          ist kluge Voraussicht.
        </div>
      </div>

      {/* LEFT: PYRAMID OF WEALTH */}
      <div style={{
        position: 'absolute',
        left: 140,
        bottom: 80,
        width: 760,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono, monospace',
          fontSize: 22,
          fontWeight: 700,
          color: '#000000',
          marginBottom: 14,
          letterSpacing: 2,
          opacity: sp(30),
        }}>
          DEINE FINANZ-PYRAMIDE
        </div>

        {/* Pyramid blocks - bottom to top */}
        <div style={{ display: 'flex', flexDirection: 'column-reverse', alignItems: 'center', gap: 6 }}>
          {/* Block 1 - widest - BASIS */}
          <div style={{
            width: 760,
            height: 90,
            backgroundColor: '#000000',
            border: '5px solid #000000',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'space-between',
            padding: '0 30px',
            transform: `translateY(${interpolate(block1, [0, 1], [80, 0])}px)`,
            opacity: block1,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              fontWeight: 800,
              color: '#A5CF83',
              letterSpacing: 3,
            }}>01 / BASIS</div>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 58,
              fontWeight: 900,
              color: '#EEEEEE',
              letterSpacing: -1,
            }}>FIXKOSTEN SENKEN</div>
          </div>

          {/* Block 2 */}
          <div style={{
            width: 620,
            height: 78,
            backgroundColor: '#EEEEEE',
            border: '5px solid #000000',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'space-between',
            padding: '0 30px',
            transform: `translateY(${interpolate(block2, [0, 1], [80, 0])}px)`,
            opacity: block2,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              fontWeight: 800,
              color: '#000000',
              letterSpacing: 3,
            }}>02</div>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 48,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -1,
            }}>NOTGROSCHEN</div>
          </div>

          {/* Block 3 */}
          <div style={{
            width: 480,
            height: 70,
            backgroundColor: '#000000',
            border: '5px solid #000000',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'space-between',
            padding: '0 26px',
            transform: `translateY(${interpolate(block3, [0, 1], [80, 0])}px)`,
            opacity: block3,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 22,
              fontWeight: 800,
              color: '#A5CF83',
              letterSpacing: 3,
            }}>03</div>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 40,
              fontWeight: 900,
              color: '#EEEEEE',
              letterSpacing: -1,
            }}>SPARPLAN</div>
          </div>

          {/* Block 4 - tip */}
          <div style={{
            width: 340,
            height: 64,
            backgroundColor: '#EEEEEE',
            border: '5px solid #000000',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'space-between',
            padding: '0 24px',
            transform: `translateY(${interpolate(block4, [0, 1], [80, 0])}px)`,
            opacity: block4,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono, monospace',
              fontSize: 20,
              fontWeight: 800,
              color: '#000000',
              letterSpacing: 3,
            }}>04</div>
            <div style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 34,
              fontWeight: 900,
              color: '#000000',
              letterSpacing: -1,
            }}>FREIHEIT</div>
          </div>
        </div>

        {/* Foundation label */}
        <div style={{
          marginTop: 18,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 700,
          color: '#000000',
          textAlign: 'center',
          opacity: sp(230),
          fontStyle: 'italic',
        }}>
          ↑ Alles baut auf der Basis auf.
        </div>
      </div>

      {/* RIGHT: BIG QUOTE */}
      <div style={{
        position: 'absolute',
        right: 100,
        top: 460,
        width: 720,
        opacity: sp(60),
      }}>
        {/* Quote marks */}
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 220,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 0.6,
          letterSpacing: -4,
          marginBottom: -20,
        }}>
          „
        </div>

        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 88,
          fontWeight: 900,
          color: '#000000',
          lineHeight: 0.95,
          letterSpacing: -2,
          marginBottom: 14,
        }}>
          OHNE BASIS<br />
          KEIN <span style={{ color: '#EEEEEE', WebkitTextStroke: '4px #000000' }}>AUFBAU</span>.
        </div>

        <div style={{
          fontFamily: 'Inter, sans-serif',
          fontSize: 28,
          fontWeight: 700,
          color: '#000000',
          lineHeight: 1.4,
          paddingLeft: 20,
          borderLeft: '5px solid #000000',
          marginTop: 30,
        }}>
          Die beste Aktie nützt nichts, wenn deine Fixkosten dich auffressen.
        </div>

        {/* Stamp */}
        <div style={{
          position: 'absolute',
          right: -20,
          bottom: -80,
          transform: `scale(${stampScale}) rotate(-10deg)`,
          transformOrigin: 'center',
        }}>
          <div style={{
            backgroundColor: '#000000',
            padding: '12px 30px',
            border: '5px solid #000000',
            boxShadow: '8px 8px 0 #EEEEEE',
          }}>
            <span style={{
              fontFamily: 'Anton, sans-serif',
              fontSize: 60,
              fontWeight: 900,
              color: '#A5CF83',
              letterSpacing: 4,
            }}>FUNDAMENT.</span>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(0,0,0,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter, sans-serif',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.35,
      }}>Kux legt an</div>
    </AbsoluteFill>
  );
};

export default Scene10_SparsamkeitBasis;
```

