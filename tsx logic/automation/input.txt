```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31_RuleTwoIntro',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene31_RuleTwoIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Number 02 reveal
  const numScale = sp(10, { damping: 10 });
  const numRotate = interpolate(sp(10), [0, 1], [-20, 0]);

  // "RULE" label
  const labelOp = interpolate(sp(25), [0, 1], [0, 1]);
  const labelX = interpolate(sp(25), [0, 1], [-100, 0]);

  // Title slide
  const titleY = interpolate(sp(45), [0, 1], [80, 0]);
  const titleOp = interpolate(sp(45), [0, 1], [0, 1]);

  // Underline draw
  const underline = interpolate(sp(70), [0, 1], [0, 720]);

  // Subtitle
  const subOp = interpolate(sp(90), [0, 1], [0, 1]);

  // Trap word emphasis
  const trapPulse = 1 + Math.sin(frame * 0.12) * 0.03;

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.06]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient base */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(233,215,0,0.10) 0%, transparent 55%), linear-gradient(135deg, #F5F5F5 0%, #EAEAEA 100%)',
      }} />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid31" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#303841" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid31)" />
      </svg>

      {/* Floating shape layers */}
      <div style={{
        position: 'absolute', top: 100, right: 150, width: 380, height: 380,
        border: '2px solid #303841', opacity: 0.06,
        transform: `rotate(${frame * 0.15}deg)`, borderRadius: 20,
      }} />
      <div style={{
        position: 'absolute', bottom: 80, left: 100, width: 280, height: 280,
        borderRadius: '50%', backgroundColor: '#e9d700', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.1})`,
      }} />
      <div style={{
        position: 'absolute', top: '50%', left: '70%', width: 200, height: 200,
        border: '3px solid #ff0000', opacity: 0.05,
        transform: `rotate(${45 + frame * 0.2}deg)`,
      }} />

      {/* Particles */}
      {[...Array(20)].map((_, i) => {
        const px = seededRandom(i + 1) * 1920;
        const py = seededRandom(i + 50) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 15;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + drift,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#303841', opacity: 0.2,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ textAlign: 'center', maxWidth: 1500 }}>
          {/* RULE label */}
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 51,
            fontWeight: 700,
            color: '#303841',
            letterSpacing: 12,
            opacity: labelOp,
            transform: `translateX(${labelX}px)`,
            marginBottom: 20,
          }}>
            ━━ RULE ━━
          </div>

          {/* Big 02 */}
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 221,
            fontWeight: 900,
            color: '#ff0000',
            transform: `scale(${numScale}) rotate(${numRotate}deg)`,
            lineHeight: 1,
            textShadow: '6px 6px 0px #303841',
            marginBottom: 10,
          }}>
            02
          </div>

          {/* Title */}
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 119,
            fontWeight: 900,
            color: '#000000',
            opacity: titleOp,
            transform: `translateY(${titleY}px) scale(${trapPulse})`,
            lineHeight: 1.1,
          }}>
            COMBINED INCOME
          </div>

          {/* Underline */}
          <div style={{
            width: underline, height: 8,
            backgroundColor: '#e9d700',
            margin: '15px auto',
          }} />

          {/* TRAP word */}
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 153,
            fontWeight: 900,
            color: '#ff0000',
            opacity: subOp,
            letterSpacing: 8,
            transform: `scale(${trapPulse})`,
          }}>
            TRAP
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.15) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene31_RuleTwoIntro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32_SmartTrap',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32_SmartTrap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Brain icon
  const brainScale = sp(10);
  const brainRotate = Math.sin(frame * 0.05) * 3;

  // "SMART" text
  const smartOp = interpolate(sp(30), [0, 1], [0, 1]);
  const smartY = interpolate(sp(30), [0, 1], [40, 0]);

  // Strike-through smart
  const strikeWidth = interpolate(sp(70), [0, 1], [0, 380]);

  // "CAUGHT" appears
  const caughtScale = sp(85, { damping: 8 });
  const caughtShake = frame > 100 && frame < 115 ? Math.sin(frame * 2) * 5 : 0;

  // Trap snap
  const trapSnap = interpolate(sp(120), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #F5F5F5 0%, #E8E8E8 100%)',
      }} />

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="diag32" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 0 40 L 40 0" stroke="#303841" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag32)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: -100, left: -100, width: 500, height: 500,
        borderRadius: '50%', backgroundColor: '#e9d700', opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
      }} />
      <div style={{
        position: 'absolute', bottom: -150, right: -150, width: 600, height: 600,
        border: '3px solid #ff0000', opacity: 0.05, borderRadius: 40,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Particles */}
      {[...Array(15)].map((_, i) => {
        const px = seededRandom(i + 100) * 1920;
        const py = seededRandom(i + 200) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.03 + i) * 20,
            width: 6, height: 6, borderRadius: '50%',
            backgroundColor: '#303841', opacity: 0.15,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', padding: 80 }}>
        {/* Brain SVG */}
        <div style={{ transform: `scale(${brainScale}) rotate(${brainRotate}deg)`, marginBottom: 30 }}>
          <svg width="180" height="180" viewBox="0 0 100 100">
            <path d="M30 35 Q20 35 20 45 Q15 50 20 58 Q15 65 25 70 Q25 80 38 78 Q45 85 50 78 Q55 85 62 78 Q75 80 75 70 Q85 65 80 58 Q85 50 80 45 Q80 35 70 35 Q65 25 50 28 Q35 25 30 35 Z"
              fill="none" stroke="#303841" strokeWidth="3" />
            <path d="M50 28 L50 78 M35 45 Q45 50 35 60 M65 45 Q55 50 65 60"
              fill="none" stroke="#303841" strokeWidth="2" />
          </svg>
        </div>

        {/* "SMART" with strikethrough */}
        <div style={{ position: 'relative', marginBottom: 40 }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 153,
            fontWeight: 900,
            color: '#303841',
            opacity: smartOp,
            transform: `translateY(${smartY}px)`,
            letterSpacing: 8,
          }}>
            "SMART"
          </div>
          <div style={{
            position: 'absolute', top: '50%', left: 0,
            height: 10, width: strikeWidth,
            backgroundColor: '#ff0000',
            transform: 'translateY(-50%)',
          }} />
        </div>

        {/* CAUGHT IN THE TRAP */}
        <div style={{
          transform: `scale(${caughtScale}) translateX(${caughtShake}px)`,
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 51,
            fontWeight: 700,
            color: '#303841',
            letterSpacing: 4,
            marginBottom: 10,
          }}>
            BUT STILL
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 178,
            fontWeight: 900,
            color: '#ff0000',
            lineHeight: 1,
            textShadow: '4px 4px 0px #303841',
          }}>
            CAUGHT!
          </div>
        </div>

        {/* Trap jaws SVG */}
        <div style={{ marginTop: 30, opacity: trapSnap }}>
          <svg width="500" height="60" viewBox="0 0 500 60">
            <path d={`M 10 30 L 50 ${30 - trapSnap * 20} L 90 30 L 130 ${30 - trapSnap * 20} L 170 30 L 210 ${30 - trapSnap * 20} L 250 30 L 290 ${30 - trapSnap * 20} L 330 30 L 370 ${30 - trapSnap * 20} L 410 30 L 450 ${30 - trapSnap * 20} L 490 30`}
              fill="none" stroke="#303841" strokeWidth="4" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.18) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene32_SmartTrap;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33_MunicipalBonds',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene33_MunicipalBonds: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Advisor speech
  const advisorOp = interpolate(sp(10), [0, 1], [0, 1]);
  const advisorX = interpolate(sp(10), [0, 1], [-150, 0]);

  // Bond certificate
  const bondScale = sp(50, { damping: 10 });
  const bondRotate = interpolate(sp(50), [0, 1], [-8, 0]);

  // TAX-FREE stamp
  const stampScale = sp(80, { damping: 7 });
  const stampRotate = interpolate(sp(80), [0, 1], [-30, -12]);

  // Caption
  const captionOp = interpolate(sp(110), [0, 1], [0, 1]);

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 30%, rgba(255,193,7,0.10) 0%, transparent 60%), linear-gradient(135deg, #F5F5F5 0%, #EDEDED 100%)',
      }} />

      {/* Dot grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.12 }}>
        <defs>
          <pattern id="dots33" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#303841" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots33)" />
      </svg>

      {/* Floating shapes */}
      <div style={{
        position: 'absolute', top: 150, right: 100, width: 350, height: 350,
        borderRadius: '50%', border: '3px solid #FFC107', opacity: 0.08,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute', bottom: 100, left: 50, width: 280, height: 280,
        backgroundColor: '#303841', opacity: 0.04, borderRadius: 30,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />

      {/* Particles */}
      {[...Array(18)].map((_, i) => {
        const px = seededRandom(i + 300) * 1920;
        const py = seededRandom(i + 400) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.025 + i) * 18,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: '#FFC107', opacity: 0.25,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 80 }}>

          {/* Advisor speech bubble */}
          <div style={{
            opacity: advisorOp,
            transform: `translateX(${advisorX}px)`,
            position: 'relative',
          }}>
            <div style={{
              backgroundColor: '#303841',
              borderRadius: 30,
              padding: '40px 50px',
              maxWidth: 540,
              boxShadow: '8px 8px 0 #000000',
            }}>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 36,
                color: '#FFC107',
                fontWeight: 700,
                marginBottom: 15,
                letterSpacing: 2,
              }}>
                ADVISOR SAYS:
              </div>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 56,
                color: '#F5F5F5',
                fontWeight: 800,
                lineHeight: 1.2,
              }}>
                "Buy Municipal Bonds —<br/>they're TAX-FREE!"
              </div>
            </div>
            {/* Bubble tail */}
            <div style={{
              position: 'absolute', right: -25, bottom: 50,
              width: 0, height: 0,
              borderTop: '20px solid transparent',
              borderBottom: '20px solid transparent',
              borderLeft: '30px solid #303841',
            }} />
          </div>

          {/* Bond Certificate */}
          <div style={{
            transform: `scale(${bondScale}) rotate(${bondRotate}deg)`,
            position: 'relative',
          }}>
            <div style={{
              width: 480,
              height: 340,
              backgroundColor: '#F5F5F5',
              border: '6px double #303841',
              borderRadius: 12,
              padding: 30,
              boxShadow: '10px 10px 0 rgba(48,56,65,0.3)',
              display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center',
            }}>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 26,
                color: '#303841',
                fontWeight: 700,
                letterSpacing: 3,
                marginBottom: 10,
              }}>
                CERTIFICATE
              </div>
              <div style={{
                fontFamily: 'Yantra One',
                fontSize: 64,
                color: '#000000',
                fontWeight: 900,
                lineHeight: 1,
                textAlign: 'center',
              }}>
                MUNICIPAL<br/>BOND
              </div>
              <div style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 28,
                color: '#303841',
                fontWeight: 600,
                marginTop: 15,
              }}>
                $50,000
              </div>
            </div>

            {/* TAX-FREE Stamp */}
            <div style={{
              position: 'absolute', top: -20, right: -40,
              transform: `scale(${stampScale}) rotate(${stampRotate}deg)`,
              border: '6px solid #ff0000',
              borderRadius: 12,
              padding: '15px 25px',
              backgroundColor: 'rgba(255,255,255,0.92)',
            }}>
              <div style={{
                fontFamily: 'Inter',
                fontSize: 43,
                color: '#ff0000',
                fontWeight: 900,
                letterSpacing: 3,
              }}>
                TAX-FREE!
              </div>
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Bottom caption */}
      <div style={{
        position: 'absolute', bottom: 90, left: 0, right: 0,
        textAlign: 'center', opacity: captionOp,
      }}>
        <div style={{
          fontFamily: 'Inter',
          fontSize: 43,
          color: '#303841',
          fontWeight: 700,
          letterSpacing: 2,
        }}>
          ...but is it really tax-free?
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.15) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene33_MunicipalBonds;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34_TrueForFederal',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene34_TrueForFederal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Checkmark draw
  const checkDraw = interpolate(sp(15), [0, 1], [0, 100]);
  const checkScale = sp(15, { damping: 9 });

  // "TRUE" big text
  const trueScale = sp(35, { damping: 10 });
  
  // Federal label
  const fedOp = interpolate(sp(60), [0, 1], [0, 1]);
  const fedY = interpolate(sp(60), [0, 1], [30, 0]);

  // Federal Income Tax box
  const boxScale = sp(85, { damping: 10 });

  // Asterisk warning
  const astOp = interpolate(sp(120), [0, 1], [0, 1]);
  const astPulse = 1 + Math.sin(frame * 0.15) * 0.05;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #F5F5F5 0%, #EEEEEE 50%, #E5E5E5 100%)',
      }} />

      {/* Vertical lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="vlines34" width="60" height="60" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="0" y2="60" stroke="#303841" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#vlines34)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: 80, left: 80, width: 320, height: 320,
        border: '3px solid #303841', opacity: 0.06,
        transform: `rotate(${frame * 0.1}deg)`, borderRadius: 12,
      }} />
      <div style={{
        position: 'absolute', bottom: 120, right: 120, width: 400, height: 400,
        borderRadius: '50%', backgroundColor: '#e9d700', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.07})`,
      }} />

      {/* Particles */}
      {[...Array(16)].map((_, i) => {
        const px = seededRandom(i + 500) * 1920;
        const py = seededRandom(i + 600) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.02 + i) * 12,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#303841', opacity: 0.2,
          }} />
        );
      })}

      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', padding: 80 }}>

        {/* Big Checkmark */}
        <div style={{ transform: `scale(${checkScale})`, marginBottom: 30 }}>
          <svg width="180" height="180" viewBox="0 0 100 100">
            <circle cx="50" cy="50" r="45" fill="none" stroke="#2D8B3E" strokeWidth="6" />
            <path d={`M 28 50 L 45 ${checkDraw > 50 ? 65 : 50 + (checkDraw / 50) * 15} L ${checkDraw > 50 ? 72 : 45 + ((checkDraw - 50) / 50) * 27} ${checkDraw > 50 ? 35 : 65}`}
              fill="none" stroke="#2D8B3E" strokeWidth="8" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>

        {/* TRUE text */}
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 178,
          fontWeight: 900,
          color: '#2D8B3E',
          transform: `scale(${trueScale})`,
          lineHeight: 1,
          letterSpacing: 6,
        }}>
          TRUE
        </div>

        {/* Subtitle */}
        <div style={{
          fontFamily: 'Inter',
          fontSize: 43,
          fontWeight: 600,
          color: '#303841',
          opacity: fedOp,
          transform: `translateY(${fedY}px)`,
          marginTop: 20,
          marginBottom: 40,
          letterSpacing: 2,
        }}>
          for one specific tax only:
        </div>

        {/* Federal box */}
        <div style={{
          transform: `scale(${boxScale})`,
          backgroundColor: '#303841',
          padding: '35px 70px',
          borderRadius: 16,
          boxShadow: '8px 8px 0 #000000',
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 102,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: 4,
            lineHeight: 1.1,
            textAlign: 'center',
          }}>
            FEDERAL INCOME TAX
          </div>
        </div>

        {/* Asterisk warning */}
        <div style={{
          opacity: astOp,
          marginTop: 35,
          transform: `scale(${astPulse})`,
          display: 'flex', alignItems: 'center', gap: 15,
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 55,
            color: '#ff0000',
            fontWeight: 900,
          }}>
            *
          </div>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 38,
            color: '#ff0000',
            fontWeight: 700,
            letterSpacing: 2,
          }}>
            (...but there's a catch coming)
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.15) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene34_TrueForFederal;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35_NotTrueForProvisional',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35_NotTrueForProvisional: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // BUT word
  const butScale = sp(8, { damping: 8 });

  // X mark
  const xScale = sp(35, { damping: 7 });
  const xShake = frame > 50 && frame < 70 ? Math.sin(frame * 2) * 4 : 0;

  // NOT TRUE
  const notOp = interpolate(sp(60), [0, 1], [0, 1]);
  const notY = interpolate(sp(60), [0, 1], [30, 0]);

  // Provisional income box
  const boxScale = sp(90, { damping: 10 });

  // Warning underline pulse
  const warnPulse = 1 + Math.sin(frame * 0.12) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient - red tinted */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(255,0,0,0.08) 0%, transparent 65%), linear-gradient(180deg, #F5F5F5 0%, #EAEAEA 100%)',
      }} />

      {/* Hatched texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="hatch35" width="20" height="20" patternUnits="userSpaceOnUse">
            <path d="M 0 0 L 20 20 M 0 20 L 20 0" stroke="#ff0000" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hatch35)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: -50, right: -50, width: 450, height: 450,
        borderRadius: '50%', backgroundColor: '#ff0000', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.08})`,
      }} />
      <div style={{
        position: 'absolute', bottom: -100, left: -100, width: 500, height: 500,
        border: '4px solid #303841', opacity: 0.05, borderRadius: 30,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Particles */}
      {[...Array(20)].map((_, i) => {
        const px = seededRandom(i + 700) * 1920;
        const py = seededRandom(i + 800) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.025 + i) * 15,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: '#ff0000', opacity: 0.18,
          }} />
        );
      })}

      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', padding: 70 }}>

        {/* BUT */}
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 119,
          fontWeight: 900,
          color: '#303841',
          transform: `scale(${butScale})`,
          letterSpacing: 8,
          marginBottom: 10,
        }}>
          BUT...
        </div>

        {/* X Mark with NOT TRUE */}
        <div style={{ display: 'flex', alignItems: 'center', gap: 30, marginBottom: 30 }}>
          {/* X */}
          <div style={{
            transform: `scale(${xScale}) translateX(${xShake}px)`,
          }}>
            <svg width="140" height="140" viewBox="0 0 100 100">
              <circle cx="50" cy="50" r="45" fill="none" stroke="#ff0000" strokeWidth="6" />
              <line x1="30" y1="30" x2="70" y2="70" stroke="#ff0000" strokeWidth="8" strokeLinecap="round" />
              <line x1="70" y1="30" x2="30" y2="70" stroke="#ff0000" strokeWidth="8" strokeLinecap="round" />
            </svg>
          </div>

          {/* NOT TRUE */}
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 170,
            fontWeight: 900,
            color: '#ff0000',
            opacity: notOp,
            transform: `translateY(${notY}px)`,
            lineHeight: 1,
            textShadow: '5px 5px 0 #303841',
          }}>
            NOT TRUE!
          </div>
        </div>

        {/* For Provisional Income */}
        <div style={{
          fontFamily: 'Inter',
          fontSize: 47,
          color: '#303841',
          fontWeight: 600,
          opacity: notOp,
          marginBottom: 25,
          letterSpacing: 2,
        }}>
          when calculating →
        </div>

        {/* Provisional Income box */}
        <div style={{
          transform: `scale(${boxScale}) scale(${warnPulse})`,
          backgroundColor: '#303841',
          padding: '30px 60px',
          borderRadius: 16,
          border: '5px solid #ff0000',
          boxShadow: '8px 8px 0 #000000',
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 102,
            fontWeight: 900,
            color: '#e9d700',
            letterSpacing: 4,
            lineHeight: 1.1,
            textAlign: 'center',
          }}>
            PROVISIONAL INCOME
          </div>
        </div>

        {/* Bottom note */}
        <div style={{
          marginTop: 40,
          fontFamily: 'Inter',
          fontSize: 36,
          color: '#303841',
          fontWeight: 600,
          opacity: notOp,
          textAlign: 'center',
          letterSpacing: 1,
        }}>
          The IRS uses a different rule here →
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.18) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene35_NotTrueForProvisional;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36_IRSAddsBack',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene36_IRSAddsBack: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Left box (tax-exempt)
  const leftScale = sp(10);

  // Arrow animation
  const arrowProgress = interpolate(sp(50), [0, 1], [0, 1]);
  const arrowX = interpolate(arrowProgress, [0, 1], [0, 400]);

  // Right box (provisional income)
  const rightScale = sp(80, { damping: 10 });

  // PLUS sign pulse
  const plusPulse = 1 + Math.sin(frame * 0.18) * 0.08;

  // Bottom IRS label
  const irsOp = interpolate(sp(110), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #F5F5F5 0%, #EBEBEB 100%)',
      }} />

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid36" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#303841" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid36)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: 100, left: '50%', width: 300, height: 300,
        borderRadius: '50%', backgroundColor: '#FFC107', opacity: 0.06,
        transform: `translateX(-50%) scale(${1 + Math.sin(frame * 0.04) * 0.08})`,
      }} />
      <div style={{
        position: 'absolute', bottom: 50, left: 150, width: 250, height: 250,
        border: '3px solid #303841', opacity: 0.05, borderRadius: 20,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: 50, right: 150, width: 250, height: 250,
        border: '3px solid #ff0000', opacity: 0.05, borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.05) * 0.08})`,
      }} />

      {/* Particles - flowing right */}
      {[...Array(14)].map((_, i) => {
        const baseX = (frame * 3 + i * 130) % 2000 - 100;
        const py = 400 + seededRandom(i + 900) * 280;
        return (
          <div key={i} style={{
            position: 'absolute', left: baseX, top: py,
            width: 6, height: 6, borderRadius: '50%',
            backgroundColor: '#e9d700', opacity: 0.4,
          }} />
        );
      })}

      {/* Title at top */}
      <div style={{
        position: 'absolute', top: 80, left: 0, right: 0, textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 81,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: 3,
        }}>
          THE IRS ADDS IT RIGHT BACK
        </div>
      </div>

      <AbsoluteFill style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', marginTop: 50 }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 50 }}>

          {/* Left box - Tax-Exempt Interest */}
          <div style={{
            transform: `scale(${leftScale})`,
            backgroundColor: '#F5F5F5',
            border: '5px solid #2D8B3E',
            borderRadius: 16,
            padding: '30px 35px',
            minWidth: 380,
            textAlign: 'center',
            boxShadow: '6px 6px 0 rgba(48,56,65,0.2)',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 700,
              color: '#2D8B3E',
              letterSpacing: 2,
              marginBottom: 12,
            }}>
              "TAX-FREE"
            </div>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 51,
              fontWeight: 900,
              color: '#303841',
              lineHeight: 1.1,
            }}>
              MUNI BOND<br/>INTEREST
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 38,
              fontWeight: 700,
              color: '#000000',
              marginTop: 15,
            }}>
              $8,000
            </div>
          </div>

          {/* PLUS sign + arrow */}
          <div style={{
            display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 15,
            transform: `scale(${plusPulse})`,
          }}>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 119,
              fontWeight: 900,
              color: '#ff0000',
              lineHeight: 1,
            }}>
              +
            </div>
            <svg width="120" height="40" viewBox="0 0 120 40">
              <line x1="10" y1="20" x2={10 + arrowProgress * 90} y2="20" stroke="#ff0000" strokeWidth="6" strokeLinecap="round" />
              {arrowProgress > 0.7 && (
                <>
                  <line x1={10 + arrowProgress * 90} y1="20" x2={10 + arrowProgress * 90 - 15} y2="10" stroke="#ff0000" strokeWidth="6" strokeLinecap="round" />
                  <line x1={10 + arrowProgress * 90} y1="20" x2={10 + arrowProgress * 90 - 15} y2="30" stroke="#ff0000" strokeWidth="6" strokeLinecap="round" />
                </>
              )}
            </svg>
          </div>

          {/* Right box - Provisional Income */}
          <div style={{
            transform: `scale(${rightScale})`,
            backgroundColor: '#303841',
            border: '5px solid #ff0000',
            borderRadius: 16,
            padding: '30px 35px',
            minWidth: 420,
            textAlign: 'center',
            boxShadow: '8px 8px 0 #000000',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 700,
              color: '#e9d700',
              letterSpacing: 2,
              marginBottom: 12,
            }}>
              COUNTED AS:
            </div>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 55,
              fontWeight: 900,
              color: '#F5F5F5',
              lineHeight: 1.1,
            }}>
              PROVISIONAL<br/>INCOME
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 38,
              fontWeight: 700,
              color: '#ff0000',
              marginTop: 15,
            }}>
              + $8,000
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Bottom IRS warning */}
      <div style={{
        position: 'absolute', bottom: 90, left: 0, right: 0, textAlign: 'center', opacity: irsOp,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#ff0000',
          padding: '12px 32px',
          borderRadius: 8,
        }}>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 38,
            color: '#F5F5F5',
            fontWeight: 800,
            letterSpacing: 3,
          }}>
            THE IRS COUNTS IT ANYWAY
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.15) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene36_IRSAddsBack;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37_SameWithSocialSecurity',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene37_SameWithSocialSecurity: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Header
  const headerOp = interpolate(sp(10), [0, 1], [0, 1]);
  const headerY = interpolate(sp(10), [0, 1], [-30, 0]);

  // SS check card
  const cardScale = sp(35, { damping: 9 });

  // Formula 50%
  const formulaScale = sp(70, { damping: 8 });
  const formulaPulse = 1 + Math.sin(frame * 0.12) * 0.04;

  // Equation reveal
  const eqOp = interpolate(sp(100), [0, 1], [0, 1]);

  // Camera
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.03]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 30%, rgba(233,215,0,0.10) 0%, transparent 60%), linear-gradient(180deg, #F5F5F5 0%, #E8E8E8 100%)',
      }} />

      {/* Circles texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="circles37" width="80" height="80" patternUnits="userSpaceOnUse">
            <circle cx="40" cy="40" r="2" fill="#303841" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#circles37)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: -80, right: -80, width: 400, height: 400,
        border: '3px solid #303841', opacity: 0.06,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: -100, left: -100, width: 450, height: 450,
        borderRadius: '50%', backgroundColor: '#e9d700', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.07})`,
      }} />

      {/* Particles */}
      {[...Array(18)].map((_, i) => {
        const px = seededRandom(i + 1100) * 1920;
        const py = seededRandom(i + 1200) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.03 + i) * 18,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: '#303841', opacity: 0.2,
          }} />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})`, display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', padding: 60 }}>

        {/* Header */}
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 81,
          fontWeight: 900,
          color: '#000000',
          opacity: headerOp,
          transform: `translateY(${headerY}px)`,
          letterSpacing: 3,
          marginBottom: 40,
          textAlign: 'center',
        }}>
          SAME RULE → SOCIAL SECURITY
        </div>

        {/* SS Check card */}
        <div style={{
          transform: `scale(${cardScale})`,
          backgroundColor: '#F5F5F5',
          border: '5px solid #303841',
          borderRadius: 16,
          padding: '25px 50px',
          minWidth: 750,
          boxShadow: '8px 8px 0 rgba(48,56,65,0.3)',
          marginBottom: 35,
        }}>
          <div style={{
            display: 'flex', justifyContent: 'space-between', alignItems: 'center',
            borderBottom: '2px dashed #303841', paddingBottom: 12, marginBottom: 12,
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 30,
              fontWeight: 700,
              color: '#303841',
              letterSpacing: 2,
            }}>
              SOCIAL SECURITY CHECK
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 26,
              color: '#9E9E9E',
            }}>
              ANNUAL
            </div>
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 85,
            fontWeight: 700,
            color: '#000000',
            textAlign: 'center',
          }}>
            $20,000
          </div>
        </div>

        {/* Down arrow */}
        <svg width="50" height="60" viewBox="0 0 50 60" style={{ marginBottom: 20 }}>
          <line x1="25" y1="5" x2="25" y2="45" stroke="#ff0000" strokeWidth="5" strokeLinecap="round" />
          <line x1="25" y1="50" x2="10" y2="35" stroke="#ff0000" strokeWidth="5" strokeLinecap="round" />
          <line x1="25" y1="50" x2="40" y2="35" stroke="#ff0000" strokeWidth="5" strokeLinecap="round" />
        </svg>

        {/* 50% Formula */}
        <div style={{
          transform: `scale(${formulaScale}) scale(${formulaPulse})`,
          display: 'flex', alignItems: 'center', gap: 30,
        }}>
          <div style={{
            backgroundColor: '#ff0000',
            padding: '20px 35px',
            borderRadius: 14,
            boxShadow: '6px 6px 0 #303841',
          }}>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 119,
              fontWeight: 900,
              color: '#F5F5F5',
              lineHeight: 1,
            }}>
              50%
            </div>
          </div>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 51,
            fontWeight: 800,
            color: '#303841',
            lineHeight: 1.1,
          }}>
            ADDED TO<br/>YOUR INCOME
          </div>
        </div>

        {/* Equation */}
        <div style={{
          opacity: eqOp,
          marginTop: 30,
          fontFamily: 'JetBrains Mono',
          fontSize: 47,
          color: '#303841',
          fontWeight: 700,
          backgroundColor: 'rgba(255,193,7,0.20)',
          padding: '15px 40px',
          borderRadius: 10,
          border: '2px solid #FFC107',
        }}>
          $20,000 × 50% = +$10,000 counted
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.15) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene37_SameWithSocialSecurity;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38_TwentyThousandExample',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene38_TwentyThousandExample: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Step rows
  const step1Op = interpolate(sp(10), [0, 1], [0, 1]);
  const step1X = interpolate(sp(10), [0, 1], [-100, 0]);

  const step2Op = interpolate(sp(40), [0, 1], [0, 1]);
  const step2X = interpolate(sp(40), [0, 1], [-100, 0]);

  const step3Op = interpolate(sp(75), [0, 1], [0, 1]);
  const step3X = interpolate(sp(75), [0, 1], [-100, 0]);

  // Final highlight
  const finalScale = sp(105, { damping: 8 });
  const finalPulse = 1 + Math.sin(frame * 0.15) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #F5F5F5 0%, #EDEDED 100%)',
      }} />

      {/* Grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="grid38" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M 50 0 L 0 0 0 50" fill="none" stroke="#303841" strokeWidth="0.8" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid38)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: 60, right: 80, width: 280, height: 280,
        borderRadius: '50%', backgroundColor: '#FFC107', opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.08})`,
      }} />
      <div style={{
        position: 'absolute', bottom: 60, left: 60, width: 320, height: 320,
        border: '3px solid #ff0000', opacity: 0.05,
        transform: `rotate(${frame * 0.12}deg)`, borderRadius: 12,
      }} />

      {/* Particles */}
      {[...Array(15)].map((_, i) => {
        const px = seededRandom(i + 1300) * 1920;
        const py = seededRandom(i + 1400) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.025 + i) * 14,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#303841', opacity: 0.2,
          }} />
        );
      })}

      {/* Header */}
      <div style={{ position: 'absolute', top: 70, left: 0, right: 0, textAlign: 'center' }}>
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 76,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: 3,
        }}>
          HOW THE MATH WORKS
        </div>
      </div>

      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', padding: 60, gap: 25, marginTop: 50 }}>

        {/* Step 1 */}
        <div style={{
          opacity: step1Op,
          transform: `translateX(${step1X}px)`,
          backgroundColor: '#F5F5F5',
          border: '4px solid #303841',
          borderRadius: 14,
          padding: '22px 40px',
          width: 1100,
          display: 'flex', alignItems: 'center', gap: 30,
          boxShadow: '5px 5px 0 rgba(48,56,65,0.2)',
        }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 64, fontWeight: 900,
            color: '#FFC107', minWidth: 90,
          }}>01</div>
          <div style={{
            fontFamily: 'Inter', fontSize: 43, fontWeight: 700, color: '#303841',
            flex: 1,
          }}>You receive Social Security:</div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 55, fontWeight: 700, color: '#000000',
          }}>$20,000</div>
        </div>

        {/* Step 2 */}
        <div style={{
          opacity: step2Op,
          transform: `translateX(${step2X}px)`,
          backgroundColor: '#F5F5F5',
          border: '4px solid #303841',
          borderRadius: 14,
          padding: '22px 40px',
          width: 1100,
          display: 'flex', alignItems: 'center', gap: 30,
          boxShadow: '5px 5px 0 rgba(48,56,65,0.2)',
        }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 64, fontWeight: 900,
            color: '#FFC107', minWidth: 90,
          }}>02</div>
          <div style={{
            fontFamily: 'Inter', fontSize: 43, fontWeight: 700, color: '#303841',
            flex: 1,
          }}>IRS takes 50% of it:</div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 55, fontWeight: 700, color: '#000000',
          }}>× 0.50</div>
        </div>

        {/* Step 3 - the result */}
        <div style={{
          opacity: step3Op,
          transform: `translateX(${step3X}px) scale(${finalScale * finalPulse})`,
          backgroundColor: '#ff0000',
          border: '5px solid #303841',
          borderRadius: 14,
          padding: '25px 40px',
          width: 1100,
          display: 'flex', alignItems: 'center', gap: 30,
          boxShadow: '7px 7px 0 #000000',
        }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 64, fontWeight: 900,
            color: '#e9d700', minWidth: 90,
          }}>03</div>
          <div style={{
            fontFamily: 'Inter', fontSize: 43, fontWeight: 800, color: '#F5F5F5',
            flex: 1,
            letterSpacing: 1,
          }}>Added to your income count:</div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 64, fontWeight: 700, color: '#F5F5F5',
          }}>+$10,000</div>
        </div>

        {/* Bottom note */}
        <div style={{
          opacity: step3Op,
          marginTop: 15,
          fontFamily: 'Inter',
          fontSize: 36,
          color: '#303841',
          fontWeight: 700,
          fontStyle: 'italic',
          letterSpacing: 1,
        }}>
          ...even though you never received that $10,000 in your bank!
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.15) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene38_TwentyThousandExample;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39_PhantomIncome',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene39_PhantomIncome: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Ghost SVG floating
  const ghostScale = sp(8, { damping: 9 });
  const ghostFloat = Math.sin(frame * 0.06) * 12;
  const ghostOp = 0.6 + Math.sin(frame * 0.08) * 0.2;

  // PHANTOM word
  const phantomScale = sp(30, { damping: 10 });
  const phantomGlow = 0.5 + Math.sin(frame * 0.1) * 0.3;

  // INCOME word
  const incomeOp = interpolate(sp(55), [0, 1], [0, 1]);

  // Bar chart - bars rising
  const bar1Height = interpolate(sp(85), [0, 1], [0, 150]);
  const bar2Height = interpolate(sp(105), [0, 1], [0, 280]);

  // Inflation arrow
  const inflateOp = interpolate(sp(125), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(158,158,158,0.15) 0%, transparent 60%), linear-gradient(180deg, #F5F5F5 0%, #E5E5E5 100%)',
      }} />

      {/* Wavy texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="wave39" width="100" height="20" patternUnits="userSpaceOnUse">
            <path d="M 0 10 Q 25 0 50 10 T 100 10" fill="none" stroke="#303841" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#wave39)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: 100, left: 100, width: 350, height: 350,
        borderRadius: '50%', backgroundColor: '#9E9E9E', opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.08})`,
      }} />
      <div style={{
        position: 'absolute', bottom: 80, right: 80, width: 300, height: 300,
        border: '3px solid #303841', opacity: 0.06, borderRadius: '50%',
        transform: `rotate(${frame * 0.15}deg)`,
      }} />

      {/* Floating particles - ghostly */}
      {[...Array(22)].map((_, i) => {
        const px = seededRandom(i + 1500) * 1920;
        const py = seededRandom(i + 1600) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 25;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + drift,
            width: 6, height: 6, borderRadius: '50%',
            backgroundColor: '#9E9E9E', opacity: 0.3 + Math.sin(frame * 0.05 + i) * 0.15,
          }} />
        );
      })}

      <AbsoluteFill style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', padding: 60 }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 80 }}>

          {/* Left side - Ghost + Phantom Income */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
            {/* Ghost SVG */}
            <div style={{
              transform: `scale(${ghostScale}) translateY(${ghostFloat}px)`,
              opacity: ghostOp,
              marginBottom: 20,
            }}>
              <svg width="160" height="180" viewBox="0 0 100 110">
                <path d="M50 10 Q20 10 20 40 L20 90 Q20 100 30 95 Q35 100 45 95 Q50 100 55 95 Q65 100 70 95 Q80 100 80 90 L80 40 Q80 10 50 10 Z"
                  fill="#9E9E9E" opacity="0.85" />
                <circle cx="40" cy="40" r="6" fill="#303841" />
                <circle cx="60" cy="40" r="6" fill="#303841" />
                <path d="M40 60 Q50 68 60 60" fill="none" stroke="#303841" strokeWidth="2.5" strokeLinecap="round" />
              </svg>
            </div>

            {/* PHANTOM text */}
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 153,
              fontWeight: 900,
              color: '#303841',
              transform: `scale(${phantomScale})`,
              letterSpacing: 6,
              lineHeight: 1,
              textShadow: `0 0 ${30 * phantomGlow}px rgba(158,158,158,${phantomGlow})`,
            }}>
              PHANTOM
            </div>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 119,
              fontWeight: 900,
              color: '#ff0000',
              opacity: incomeOp,
              letterSpacing: 4,
              lineHeight: 1,
            }}>
              INCOME
            </div>
          </div>

          {/* Right side - Bar Chart */}
          <div style={{
            display: 'flex', flexDirection: 'column', alignItems: 'center',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 36,
              color: '#303841',
              fontWeight: 700,
              marginBottom: 20,
              letterSpacing: 2,
            }}>
              YOUR INCOME COUNT
            </div>

            <div style={{ display: 'flex', alignItems: 'flex-end', gap: 40, height: 320 }}>
              {/* Bar 1 - Real */}
              <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
                <div style={{
                  width: 130,
                  height: bar1Height,
                  backgroundColor: '#2D8B3E',
                  borderRadius: '8px 8px 0 0',
                  boxShadow: '4px 4px 0 #303841',
                }} />
                <div style={{
                  marginTop: 12,
                  fontFamily: 'Inter', fontSize: 26, fontWeight: 700, color: '#303841',
                }}>REAL</div>
              </div>

              {/* + sign */}
              <div style={{
                fontFamily: 'Yantra One', fontSize: 68, color: '#ff0000', fontWeight: 900,
                alignSelf: 'center',
              }}>+</div>

              {/* Bar 2 - Inflated */}
              <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
                <div style={{
                  width: 130,
                  height: bar2Height,
                  background: 'repeating-linear-gradient(45deg, #ff0000, #ff0000 10px, #b80000 10px, #b80000 20px)',
                  borderRadius: '8px 8px 0 0',
                  boxShadow: '4px 4px 0 #303841',
                  border: '2px dashed #303841',
                }} />
                <div style={{
                  marginTop: 12,
                  fontFamily: 'Inter', fontSize: 26, fontWeight: 700, color: '#ff0000',
                }}>INFLATED</div>
              </div>
            </div>

            {/* Inflation note */}
            <div style={{
              opacity: inflateOp,
              marginTop: 25,
              backgroundColor: '#303841',
              padding: '10px 25px',
              borderRadius: 8,
            }}>
              <div style={{
                fontFamily: 'Inter', fontSize: 30, fontWeight: 700, color: '#e9d700',
                letterSpacing: 2,
              }}>
                ARTIFICIALLY INFLATED
              </div>
            </div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.18) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene39_PhantomIncome;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40_HoldingMuniBonds',
  durationInSeconds: 5,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene40_HoldingMuniBonds: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Magnifying glass
  const glassScale = sp(5, { damping: 9 });
  const glassRotate = Math.sin(frame * 0.08) * 8;

  // Main text
  const headerOp = interpolate(sp(20), [0, 1], [0, 1]);
  const headerY = interpolate(sp(20), [0, 1], [-25, 0]);

  // Look at... text
  const lookScale = sp(45, { damping: 8 });

  // Whole picture badge
  const badgeScale = sp(75, { damping: 8 });
  const badgePulse = 1 + Math.sin(frame * 0.15) * 0.05;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F5F5F5', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, rgba(255,193,7,0.10) 0%, transparent 60%), linear-gradient(135deg, #F5F5F5 0%, #EAEAEA 100%)',
      }} />

      {/* Dots texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dots40" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="1.5" fill="#303841" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots40)" />
      </svg>

      {/* Shape layers */}
      <div style={{
        position: 'absolute', top: 80, right: 100, width: 380, height: 380,
        borderRadius: '50%', border: '4px solid #FFC107', opacity: 0.08,
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute', bottom: 80, left: 80, width: 320, height: 320,
        backgroundColor: '#303841', opacity: 0.05, borderRadius: 24,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />

      {/* Particles */}
      {[...Array(14)].map((_, i) => {
        const px = seededRandom(i + 1700) * 1920;
        const py = seededRandom(i + 1800) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.03 + i) * 15,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: '#FFC107', opacity: 0.3,
          }} />
        );
      })}

      <AbsoluteFill style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', padding: 60 }}>

        {/* Magnifying glass icon */}
        <div style={{
          transform: `scale(${glassScale}) rotate(${glassRotate}deg)`,
          marginBottom: 25,
        }}>
          <svg width="140" height="140" viewBox="0 0 100 100">
            <circle cx="42" cy="42" r="28" fill="none" stroke="#303841" strokeWidth="6" />
            <circle cx="42" cy="42" r="22" fill="#FFC107" opacity="0.3" />
            <line x1="62" y1="62" x2="85" y2="85" stroke="#303841" strokeWidth="8" strokeLinecap="round" />
          </svg>
        </div>

        {/* Header */}
        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 81,
          fontWeight: 900,
          color: '#000000',
          opacity: headerOp,
          transform: `translateY(${headerY}px)`,
          letterSpacing: 3,
          textAlign: 'center',
          marginBottom: 20,
        }}>
          HOLDING MUNI BONDS?
        </div>

        {/* Action text */}
        <div style={{
          transform: `scale(${lookScale})`,
          fontFamily: 'Inter',
          fontSize: 51,
          fontWeight: 700,
          color: '#303841',
          marginBottom: 30,
          letterSpacing: 2,
          textAlign: 'center',
        }}>
          You must look at the →
        </div>

        {/* Whole picture badge */}
        <div style={{
          transform: `scale(${badgeScale}) scale(${badgePulse})`,
          backgroundColor: '#ff0000',
          padding: '25px 60px',
          borderRadius: 16,
          border: '5px solid #303841',
          boxShadow: '8px 8px 0 #000000',
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 119,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: 4,
            lineHeight: 1,
          }}>
            WHOLE PICTURE
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.15) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.5,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene40_HoldingMuniBonds;
```

