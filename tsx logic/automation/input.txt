```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31_RakeshIntro',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene31_RakeshIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.02, 1.08]);
  const labelIn = sp(20);
  const nameIn = sp(45, { stiffness: 90 });
  const cityIn = sp(75);
  const tagIn = sp(120);
  const lineWidth = interpolate(sp(60), [0, 1], [0, 720]);
  const stampRot = interpolate(sp(150), [0, 1], [-25, -8]);
  const stampScale = sp(150, { stiffness: 200 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#B4E1EB', overflow: 'hidden' }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 20%, rgba(254,236,65,0.35) 0%, rgba(180,225,235,0) 55%)',
      }} />
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 80% 90%, rgba(0,0,0,0.10) 0%, rgba(180,225,235,0) 50%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', top: '-8%', right: '-6%',
        width: 720, height: 720, borderRadius: '50%',
        border: '3px solid #000000', opacity: 0.06,
        transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-10%', left: '-5%',
        width: 560, height: 560,
        border: '2px solid #000000', opacity: 0.05,
        transform: `rotate(${45 + frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '40%', left: '55%',
        width: 320, height: 320, borderRadius: '50%',
        backgroundColor: '#FEEC41', opacity: 0.08,
        transform: `translateY(${Math.sin(frame * 0.03) * 20}px)`,
      }} />

      {/* Motion texture - dotted grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.12 }} width="1920" height="1080">
        <defs>
          <pattern id="dots31" x="0" y="0" width="48" height="48" patternUnits="userSpaceOnUse">
            <circle cx="2" cy="2" r="1.8" fill="#000000" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dots31)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const s = seededRandom(i + 1);
        const x = s * 1920;
        const y = seededRandom(i + 50) * 1080;
        const drift = Math.sin(frame * 0.02 + i) * 15;
        return (
          <div key={i} style={{
            position: 'absolute', left: x + drift, top: y,
            width: 6, height: 6, borderRadius: '50%',
            backgroundColor: '#000000', opacity: 0.25,
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ opacity: exitOp, transform: `scale(${camZoom})` }}>
        <div style={{
          position: 'absolute', top: 180, left: 140,
          opacity: labelIn, transform: `translateY(${(1 - labelIn) * -30}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            padding: '14px 28px',
            backgroundColor: '#000000',
            fontFamily: 'Inter, Poppins',
            fontSize: 34, fontWeight: 800,
            color: '#FEEC41',
            letterSpacing: '4px',
          }}>
            REAL LIFE CASE • असल किरदार
          </div>
        </div>

        <div style={{
          position: 'absolute', top: 260, left: 140,
          fontFamily: 'Yantra One',
          fontSize: 187, // 220 * 0.85
          fontWeight: 900,
          color: '#000000',
          lineHeight: 1,
          opacity: nameIn,
          transform: `translateX(${(1 - nameIn) * -60}px)`,
        }}>
          राकेश जी
        </div>

        <div style={{
          position: 'absolute', top: 470, left: 140,
          height: 6, width: lineWidth,
          backgroundColor: '#FEEC41',
        }} />

        <div style={{
          position: 'absolute', top: 510, left: 140,
          display: 'flex', alignItems: 'center', gap: 20,
          opacity: cityIn, transform: `translateY(${(1 - cityIn) * 20}px)`,
        }}>
          {/* Location pin SVG */}
          <svg width="52" height="52" viewBox="0 0 24 24" fill="none">
            <path d="M12 2C7.58 2 4 5.58 4 10c0 5.5 8 12 8 12s8-6.5 8-12c0-4.42-3.58-8-8-8zm0 11a3 3 0 110-6 3 3 0 010 6z" fill="#000000"/>
          </svg>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 68, fontWeight: 700, color: '#000000',
          }}>
            मेरठ, उत्तर प्रदेश
          </div>
        </div>

        <div style={{
          position: 'absolute', bottom: 220, left: 140,
          opacity: tagIn, transform: `translateY(${(1 - tagIn) * 20}px)`,
          maxWidth: 1400,
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 51, fontWeight: 600,
            color: '#000000', lineHeight: 1.3,
          }}>
            काल्पनिक किरदार <span style={{ backgroundColor: '#FEEC41', padding: '0 12px' }}>पर कहानी असली भारत की</span>
          </div>
        </div>

        {/* Stamp */}
        <div style={{
          position: 'absolute', top: 240, right: 200,
          transform: `rotate(${stampRot}deg) scale(${stampScale})`,
          border: '6px solid #000000',
          padding: '18px 34px',
          backgroundColor: 'rgba(180,225,235,0.2)',
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 32, fontWeight: 900,
            color: '#000000', letterSpacing: '3px',
          }}>
            CASE STUDY
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 20, fontWeight: 700,
            color: '#000000', marginTop: 4, letterSpacing: '2px',
          }}>
            #001 / BHARAT
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark "ProBuffet HQ" bottom-right */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene31_RakeshIntro;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32_TwentyTwoLakh',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32_TwentyTwoLakh: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter animation 0 -> 22
  const countProgress = interpolate(frame, [30, 130], [0, 22], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });
  const displayCount = countProgress.toFixed(1);

  const labelIn = sp(15);
  const numIn = sp(30, { stiffness: 90 });
  const barIn = sp(150);
  const quoteIn = sp(230);
  const warnIn = sp(310);

  // Shake on impact at frame 130
  const shake = frame > 128 && frame < 145
    ? Math.sin(frame * 2) * (145 - frame) * 0.5
    : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B4E1EB', overflow: 'hidden' }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(254,236,65,0.15) 0%, rgba(180,225,235,0) 60%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', top: '20%', right: '-15%',
        width: 900, height: 900,
        border: '4px solid #000000', opacity: 0.05,
        borderRadius: 40,
        transform: `rotate(${20 + frame * 0.08}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-20%', left: '30%',
        width: 700, height: 700, borderRadius: '50%',
        border: '2px solid #000000', opacity: 0.07,
        transform: `scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
      }} />

      {/* Motion texture - vertical lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="1920" height="1080">
        {Array.from({ length: 40 }).map((_, i) => (
          <line key={i} x1={i * 48} y1={0} x2={i * 48} y2={1080} stroke="#000000" strokeWidth="1" />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 100);
        const x = s * 1920;
        const y = ((seededRandom(i + 150) * 1080) + frame * 0.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#000000', opacity: 0.3,
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ opacity: exitOp, transform: `translateX(${shake}px)` }}>
        {/* Top label */}
        <div style={{
          position: 'absolute', top: 100, left: 140,
          opacity: labelIn, transform: `translateY(${(1 - labelIn) * -20}px)`,
          display: 'flex', alignItems: 'center', gap: 20,
        }}>
          <div style={{
            width: 14, height: 14, backgroundColor: '#000000',
          }} />
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 46, fontWeight: 700, color: '#000000',
            letterSpacing: '2px',
          }}>
            रिटायर्ड मास्टर • जमापूंजी
          </div>
        </div>

        {/* Big number */}
        <div style={{
          position: 'absolute', top: 210, left: 140,
          display: 'flex', alignItems: 'baseline', gap: 30,
          opacity: numIn, transform: `scale(${numIn})`,
          transformOrigin: 'left center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 340, // 400*0.85
            fontWeight: 900, color: '#000000',
            lineHeight: 0.9,
            textShadow: '8px 8px 0 #FEEC41',
          }}>
            ₹{displayCount}
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 119, // 140*0.85
            fontWeight: 900, color: '#000000',
          }}>
            लाख
          </div>
        </div>

        {/* Bar chart - single bank */}
        <div style={{
          position: 'absolute', top: 640, left: 140,
          opacity: barIn, transform: `translateX(${(1 - barIn) * -40}px)`,
          display: 'flex', alignItems: 'flex-end', gap: 24, height: 200,
        }}>
          <div style={{
            width: 120, height: 200,
            backgroundColor: '#000000',
            display: 'flex', flexDirection: 'column', alignItems: 'center',
            justifyContent: 'flex-end', paddingBottom: 20,
            position: 'relative',
          }}>
            <div style={{
              position: 'absolute', top: -50,
              fontFamily: 'JetBrains Mono', fontSize: 34, fontWeight: 900,
              color: '#000000',
            }}>100%</div>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 800,
              color: '#FEEC41', letterSpacing: '2px',
            }}>ONE BANK</div>
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 51, fontWeight: 800, color: '#000000',
            marginLeft: 30, lineHeight: 1.15,
          }}>
            एक ही बैंक के<br/>
            <span style={{ backgroundColor: '#FEEC41', padding: '0 14px' }}>Savings Account</span>
          </div>
        </div>

        {/* Quote box */}
        <div style={{
          position: 'absolute', top: 210, right: 100,
          width: 620,
          opacity: quoteIn, transform: `translateY(${(1 - quoteIn) * 30}px)`,
          padding: '40px 44px',
          backgroundColor: '#FEEC41',
          border: '5px solid #000000',
          transform: `translateY(${(1 - quoteIn) * 30}px) rotate(-2deg)`,
        }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 120,
            color: '#000000', lineHeight: 0.5, marginBottom: 10,
          }}>“</div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 44, fontWeight: 700, color: '#000000',
            lineHeight: 1.3,
          }}>
            बैंक में पैसा सुरक्षित रहता है बेटा
          </div>
          <div style={{
            marginTop: 24,
            fontFamily: 'Inter, Poppins', fontSize: 24, fontWeight: 700,
            color: '#000000', letterSpacing: '2px',
          }}>
            — राकेश जी की सोच
          </div>
        </div>

        {/* Warning strip */}
        <div style={{
          position: 'absolute', bottom: 80, left: 0, right: 0,
          opacity: warnIn,
          backgroundColor: '#000000',
          padding: '22px 140px',
          display: 'flex', alignItems: 'center', gap: 24,
        }}>
          <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
            <path d="M12 2L1 21h22L12 2zm0 6l7.5 13h-15L12 8zm-1 4v4h2v-4h-2zm0 5v2h2v-2h-2z" fill="#FEEC41"/>
          </svg>
          <div style={{
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 36, fontWeight: 700, color: '#FEEC41',
            letterSpacing: '1px',
          }}>
            Concentration Risk — पूरा अंडा एक ही टोकरी में
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 60%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark "ProBuffet HQ" bottom-right */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene32_TwentyTwoLakh;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33_SonDelhi',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene33_SonDelhi: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(15);
  const phoneIn = sp(40, { stiffness: 90 });
  const arrowIn = sp(120);
  const usaIn = sp(160);
  const dividerIn = sp(220);
  const bottomIn = sp(280);

  const phoneFloat = Math.sin(frame * 0.05) * 8;
  const arrowPulse = 1 + Math.sin(frame * 0.15) * 0.05;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B4E1EB', overflow: 'hidden' }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 30%, rgba(254,236,65,0.30) 0%, rgba(180,225,235,0) 55%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', top: '10%', left: '-8%',
        width: 640, height: 640,
        border: '3px solid #000000', opacity: 0.06,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '5%', right: '-10%',
        width: 800, height: 800, borderRadius: '50%',
        backgroundColor: '#FEEC41', opacity: 0.08,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
      }} />

      {/* Motion texture - concentric circles top-left */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="1920" height="1080">
        {[1, 2, 3, 4, 5].map((i) => (
          <circle key={i} cx="960" cy="540" r={i * 140 + frame * 0.3}
            fill="none" stroke="#000000" strokeWidth="1" strokeDasharray="6 10" />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const s = seededRandom(i + 200);
        const x = s * 1920;
        const y = seededRandom(i + 250) * 1080 + Math.sin(frame * 0.03 + i) * 20;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 5, height: 5,
            backgroundColor: '#000000', opacity: 0.25,
            transform: 'rotate(45deg)',
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ opacity: exitOp }}>
        {/* Header */}
        <div style={{
          position: 'absolute', top: 80, left: 140,
          opacity: titleIn, transform: `translateY(${(1 - titleIn) * -20}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 30, fontWeight: 800, color: '#000000',
            letterSpacing: '4px', marginBottom: 12,
          }}>
            NEXT GENERATION • अगली पीढ़ी
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 136, // 160*0.85
            fontWeight: 900, color: '#000000',
            lineHeight: 1,
          }}>
            बेटा — <span style={{ color: '#000000', backgroundColor: '#FEEC41', padding: '0 20px' }}>दिल्ली में</span>
          </div>
        </div>

        {/* Phone illustration */}
        <div style={{
          position: 'absolute', top: 340, left: 200,
          opacity: phoneIn, transform: `scale(${phoneIn}) translateY(${phoneFloat}px)`,
        }}>
          <div style={{
            width: 340, height: 620,
            border: '10px solid #000000',
            borderRadius: 44,
            backgroundColor: '#B4E1EB',
            padding: 20,
            position: 'relative',
            boxShadow: '14px 14px 0 #000000',
          }}>
            <div style={{
              width: 100, height: 8, backgroundColor: '#000000',
              borderRadius: 4, margin: '0 auto 20px',
            }} />
            <div style={{
              backgroundColor: '#FEEC41', padding: '18px 20px',
              border: '3px solid #000000', marginBottom: 14,
            }}>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 900, color: '#000000' }}>SIP</div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 700, color: '#000000' }}>₹15,000/mo</div>
            </div>
            <div style={{
              backgroundColor: '#000000', padding: '18px 20px', marginBottom: 14,
            }}>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 800, color: '#FEEC41' }}>NIFTY 50</div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 20, color: '#FEEC41' }}>+ 12.4%</div>
            </div>
            <div style={{
              backgroundColor: '#B4E1EB', padding: '18px 20px',
              border: '3px solid #000000',
            }}>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 22, fontWeight: 800, color: '#000000' }}>US STOCKS</div>
              <div style={{ fontFamily: 'JetBrains Mono', fontSize: 18, color: '#000000' }}>AAPL • TSLA • NVDA</div>
            </div>
          </div>
        </div>

        {/* Arrow flow */}
        <div style={{
          position: 'absolute', top: 560, left: 620,
          opacity: arrowIn, transform: `scale(${arrowPulse})`,
        }}>
          <svg width="200" height="80" viewBox="0 0 200 80" fill="none">
            <path d="M0 40 L160 40 M140 20 L170 40 L140 60" stroke="#000000" strokeWidth="8" strokeLinecap="round" strokeLinejoin="round"/>
          </svg>
        </div>

        {/* USA / Globe icon */}
        <div style={{
          position: 'absolute', top: 400, right: 200,
          opacity: usaIn, transform: `translateX(${(1 - usaIn) * 40}px)`,
        }}>
          <div style={{
            width: 380, height: 380, borderRadius: '50%',
            border: '8px solid #000000',
            backgroundColor: '#FEEC41',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            flexDirection: 'column',
            boxShadow: '12px 12px 0 #000000',
          }}>
            <svg width="140" height="140" viewBox="0 0 24 24" fill="none">
              <circle cx="12" cy="12" r="10" stroke="#000000" strokeWidth="1.8"/>
              <path d="M2 12h20M12 2a15 15 0 010 20M12 2a15 15 0 000 20" stroke="#000000" strokeWidth="1.5"/>
            </svg>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 40, fontWeight: 900, color: '#000000',
              marginTop: 12, letterSpacing: '3px',
            }}>
              USA MARKET
            </div>
          </div>
        </div>

        {/* Divider */}
        <div style={{
          position: 'absolute', bottom: 240, left: 140, right: 140,
          height: 4, backgroundColor: '#000000',
          transform: `scaleX(${dividerIn})`, transformOrigin: 'left',
        }} />

        {/* Bottom line */}
        <div style={{
          position: 'absolute', bottom: 90, left: 140, right: 140,
          opacity: bottomIn, transform: `translateY(${(1 - bottomIn) * 20}px)`,
          display: 'flex', justifyContent: 'space-between', alignItems: 'center',
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 51,
            fontWeight: 800, color: '#000000', lineHeight: 1.2,
          }}>
            एक घर • दो पीढ़ियाँ<br/>
            <span style={{ backgroundColor: '#FEEC41', padding: '0 14px' }}>दो अलग नज़रिए</span>
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 200, fontWeight: 900,
            color: '#000000', lineHeight: 1,
          }}>
            2
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark "ProBuffet HQ" bottom-right */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene33_SonDelhi;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34_InflationVsCompound',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene34_InflationVsCompound: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(15);
  const leftIn = sp(50);
  const rightIn = sp(80);

  // Down curve (Rakesh) - starts at frame 100
  const downProg = interpolate(frame, [100, 260], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });
  // Up curve (Beta) - starts at frame 130
  const upProg = interpolate(frame, [130, 290], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.inOut(Easing.cubic),
  });

  // Chart dimensions
  const chartX = 140, chartY = 380, chartW = 1640, chartH = 480;
  const midY = chartY + chartH / 2;

  // Down curve path (Rakesh - purchasing power)
  const downEndX = chartX + chartW * downProg;
  const downEndY = midY + (downProg * 200);
  const downPath = `M ${chartX} ${midY} Q ${chartX + chartW * 0.5 * downProg} ${midY + 40 * downProg}, ${downEndX} ${downEndY}`;

  // Up curve (Beta - compound)
  const upEndX = chartX + chartW * upProg;
  const upEndY = midY - Math.pow(upProg, 1.8) * 260;
  const upPath = `M ${chartX} ${midY} Q ${chartX + chartW * 0.6 * upProg} ${midY - 20 * upProg}, ${upEndX} ${upEndY}`;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B4E1EB', overflow: 'hidden' }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(254,236,65,0.15) 0%, rgba(180,225,235,0) 40%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', top: '-15%', left: '30%',
        width: 700, height: 700, borderRadius: '50%',
        border: '3px solid #000000', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-10%', right: '10%',
        width: 500, height: 500,
        border: '2px solid #000000', opacity: 0.06,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Motion texture - graph grid */}
      <svg style={{ position: 'absolute', left: chartX, top: chartY, opacity: 0.18 }} width={chartW} height={chartH}>
        {Array.from({ length: 11 }).map((_, i) => (
          <line key={`v${i}`} x1={i * (chartW / 10)} y1={0} x2={i * (chartW / 10)} y2={chartH} stroke="#000000" strokeWidth="1" strokeDasharray="4 6"/>
        ))}
        {Array.from({ length: 7 }).map((_, i) => (
          <line key={`h${i}`} x1={0} y1={i * (chartH / 6)} x2={chartW} y2={i * (chartH / 6)} stroke="#000000" strokeWidth="1" strokeDasharray="4 6"/>
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const s = seededRandom(i + 300);
        const x = s * 1920;
        const y = (seededRandom(i + 350) * 1080 + frame * 0.4) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#000000', opacity: 0.3,
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ opacity: exitOp }}>
        {/* Title */}
        <div style={{
          position: 'absolute', top: 70, left: 140,
          opacity: titleIn, transform: `translateY(${(1 - titleIn) * -20}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 30, fontWeight: 900,
            color: '#000000', letterSpacing: '4px', marginBottom: 10,
          }}>
            SAME COUNTRY • SAME TIME • TWO DESTINIES
          </div>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 102, // 120*0.85
            fontWeight: 900, color: '#000000', lineHeight: 1,
          }}>
            एक चुपचाप घटता है • दूसरा <span style={{ backgroundColor: '#FEEC41', padding: '0 16px' }}>चुपचाप बढ़ता है</span>
          </div>
        </div>

        {/* Chart area */}
        <svg style={{ position: 'absolute', left: 0, top: 0, pointerEvents: 'none' }} width="1920" height="1080">
          {/* Zero line */}
          <line x1={chartX} y1={midY} x2={chartX + chartW} y2={midY} stroke="#000000" strokeWidth="3" strokeDasharray="12 8"/>

          {/* Down curve area fill */}
          <path
            d={`${downPath} L ${downEndX} ${midY} Z`}
            fill="#000000"
            opacity="0.15"
          />
          {/* Down curve line */}
          <path d={downPath} stroke="#000000" strokeWidth="7" fill="none" strokeLinecap="round"/>

          {/* Up curve area fill */}
          <path
            d={`${upPath} L ${upEndX} ${midY} Z`}
            fill="#FEEC41"
            opacity="0.6"
          />
          {/* Up curve line */}
          <path d={upPath} stroke="#000000" strokeWidth="7" fill="none" strokeLinecap="round"/>

          {/* End dots */}
          {downProg > 0.05 && (
            <circle cx={downEndX} cy={downEndY} r="14" fill="#000000"/>
          )}
          {upProg > 0.05 && (
            <circle cx={upEndX} cy={upEndY} r="14" fill="#FEEC41" stroke="#000000" strokeWidth="4"/>
          )}
        </svg>

        {/* Left label - Rakesh */}
        <div style={{
          position: 'absolute', bottom: 100, left: 140,
          opacity: leftIn, transform: `translateY(${(1 - leftIn) * 20}px)`,
          padding: '20px 26px', backgroundColor: '#000000',
          maxWidth: 700,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 800,
            color: '#FEEC41', letterSpacing: '2px', marginBottom: 8,
          }}>
            राकेश जी • Savings only
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 42, fontWeight: 800, color: '#FEEC41',
          }}>
            महंगाई से पिट रहा — Real Return ↓
          </div>
        </div>

        {/* Right label - Beta */}
        <div style={{
          position: 'absolute', top: 320, right: 140,
          opacity: rightIn, transform: `translateY(${(1 - rightIn) * -20}px)`,
          padding: '20px 26px',
          backgroundColor: '#FEEC41',
          border: '4px solid #000000',
          maxWidth: 620,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 800,
            color: '#000000', letterSpacing: '2px', marginBottom: 8,
          }}>
            बेटा • SIP + Equity
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 42, fontWeight: 800, color: '#000000',
          }}>
            Compound हो रहा — Real Return ↑
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 60%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark "ProBuffet HQ" bottom-right */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene34_InflationVsCompound;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35_KnowledgeGap',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35_KnowledgeGap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const strike1 = sp(20);
  const strike2 = sp(50);
  const wordIn = sp(90, { stiffness: 90 });
  const bridgeIn = sp(160);
  const bottomIn = sp(240);
  const chIn = sp(300);

  // Strike-through width for "रिटर्न"
  const strikeWidth = interpolate(sp(30), [0, 1], [0, 460]);
  const strikeWidth2 = interpolate(sp(60), [0, 1], [0, 460]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#B4E1EB', overflow: 'hidden' }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 40%, rgba(254,236,65,0.25) 0%, rgba(180,225,235,0) 60%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', top: '25%', left: '-6%',
        width: 500, height: 500,
        border: '3px solid #000000', opacity: 0.06,
        borderRadius: 30,
        transform: `rotate(${15 + frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '10%', right: '-8%',
        width: 620, height: 620, borderRadius: '50%',
        border: '2px solid #000000', opacity: 0.07,
        transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-8%', left: '35%',
        width: 400, height: 400,
        backgroundColor: '#FEEC41', opacity: 0.10,
        transform: `rotate(${45 + frame * 0.08}deg)`,
      }} />

      {/* Motion texture - diagonal lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.09 }} width="1920" height="1080">
        {Array.from({ length: 30 }).map((_, i) => (
          <line key={i} x1={-200 + i * 100} y1={0} x2={200 + i * 100} y2={1080}
            stroke="#000000" strokeWidth="1"/>
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 400);
        const x = s * 1920;
        const y = seededRandom(i + 450) * 1080;
        const bob = Math.sin(frame * 0.03 + i) * 12;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y + bob,
            width: 6, height: 6, borderRadius: '50%',
            backgroundColor: '#000000', opacity: 0.22,
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ opacity: exitOp }}>
        {/* Label top */}
        <div style={{
          position: 'absolute', top: 100, left: '50%', transform: 'translateX(-50%)',
          padding: '12px 32px', backgroundColor: '#000000',
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 30, fontWeight: 900,
            color: '#FEEC41', letterSpacing: '5px',
          }}>
            THE REAL GAP • असली फासला
          </div>
        </div>

        {/* Central struck lines */}
        <div style={{
          position: 'absolute', top: 240, left: 0, right: 0,
          textAlign: 'center',
        }}>
          <div style={{
            display: 'inline-block', position: 'relative',
            fontFamily: 'Yantra One', fontSize: 119, fontWeight: 900, color: '#000000',
            lineHeight: 1.1,
          }}>
            <span style={{ opacity: 0.4 }}>रिटर्न का फासला नहीं</span>
            <div style={{
              position: 'absolute', top: '50%', left: '50%',
              transform: 'translate(-50%, -50%) rotate(-3deg)',
              width: strikeWidth * 2.3, height: 10,
              backgroundColor: '#000000',
            }} />
          </div>
        </div>

        {/* Cross X */}
        <div style={{
          position: 'absolute', top: 380, left: '50%', transform: 'translateX(-50%)',
          opacity: strike2,
        }}>
          <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
            <path d="M6 6L18 18M18 6L6 18" stroke="#000000" strokeWidth="4" strokeLinecap="round"/>
          </svg>
        </div>

        {/* Big answer */}
        <div style={{
          position: 'absolute', top: 470, left: 0, right: 0,
          textAlign: 'center',
          opacity: wordIn,
          transform: `translateY(${(1 - wordIn) * 30}px) scale(${wordIn})`,
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 187,
            fontWeight: 900, color: '#000000',
            lineHeight: 1,
          }}>
            जानकारी का
          </div>
          <div style={{
            display: 'inline-block',
            fontFamily: 'Yantra One',
            fontSize: 187,
            fontWeight: 900, color: '#000000',
            backgroundColor: '#FEEC41',
            padding: '0 40px',
            marginTop: 10,
            border: '6px solid #000000',
            boxShadow: '12px 12px 0 #000000',
          }}>
            समझ का फासला
          </div>
        </div>

        {/* Bridge line - illustration */}
        <div style={{
          position: 'absolute', bottom: 220, left: '50%', transform: 'translateX(-50%)',
          opacity: bridgeIn, width: 800,
        }}>
          <svg width="800" height="80" viewBox="0 0 800 80" fill="none">
            <line x1="20" y1="40" x2="780" y2="40" stroke="#000000" strokeWidth="4" strokeDasharray="12 8"/>
            <circle cx="20" cy="40" r="14" fill="#000000"/>
            <circle cx="780" cy="40" r="14" fill="#FEEC41" stroke="#000000" strokeWidth="4"/>
            <text x="20" y="78" textAnchor="middle" fontFamily="Inter, Poppins" fontSize="22" fontWeight="800" fill="#000000">पिता</text>
            <text x="780" y="78" textAnchor="middle" fontFamily="Inter, Poppins" fontSize="22" fontWeight="800" fill="#000000">बेटा</text>
          </svg>
        </div>

        {/* Bottom pledge */}
        <div style={{
          position: 'absolute', bottom: 80, left: 0, right: 0,
          textAlign: 'center',
          opacity: bottomIn, transform: `translateY(${(1 - bottomIn) * 20}px)`,
        }}>
          <div style={{
            display: 'inline-block',
            backgroundColor: '#000000', padding: '18px 40px',
            fontFamily: 'Noto Sans Devanagari', fontSize: 44, fontWeight: 800,
            color: '#FEEC41',
          }}>
            यही खाई इस चैनल से पाटना है
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark "ProBuffet HQ" bottom-right */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene35_KnowledgeGap;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36_SEBIDisclaimer',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene36_SEBIDisclaimer: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const badgeIn = sp(15, { stiffness: 100 });
  const headIn = sp(40);
  const notIn = sp(90);
  const cardIn = sp(150);
  const purposeIn = sp(220);

  const badgeRot = interpolate(sp(15), [0, 1], [-40, -5]);
  const pulse = 1 + Math.sin(frame * 0.1) * 0.03;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B4E1EB', overflow: 'hidden' }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, rgba(254,236,65,0.20) 0%, rgba(180,225,235,0) 55%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', top: '-8%', right: '20%',
        width: 500, height: 500,
        border: '3px solid #000000', opacity: 0.06,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-15%', left: '-10%',
        width: 700, height: 700, borderRadius: '50%',
        border: '2px solid #000000', opacity: 0.05,
      }} />

      {/* Motion texture - warning stripes on sides */}
      <div style={{
        position: 'absolute', top: 0, left: 0, width: 60, height: '100%',
        background: 'repeating-linear-gradient(45deg, #000000, #000000 20px, #FEEC41 20px, #FEEC41 40px)',
        opacity: 0.5,
      }} />
      <div style={{
        position: 'absolute', top: 0, right: 0, width: 60, height: '100%',
        background: 'repeating-linear-gradient(45deg, #000000, #000000 20px, #FEEC41 20px, #FEEC41 40px)',
        opacity: 0.5,
      }} />

      {/* Floating particles */}
      {Array.from({ length: 10 }).map((_, i) => {
        const s = seededRandom(i + 500);
        const x = 100 + s * 1720;
        const y = seededRandom(i + 550) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: '#000000', opacity: 0.22,
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ opacity: exitOp }}>
        {/* Info badge */}
        <div style={{
          position: 'absolute', top: 90, left: 200,
          opacity: badgeIn, transform: `rotate(${badgeRot}deg) scale(${pulse})`,
          transformOrigin: 'left center',
        }}>
          <div style={{
            display: 'flex', alignItems: 'center', gap: 20,
            backgroundColor: '#000000', padding: '18px 30px',
          }}>
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
              <circle cx="12" cy="12" r="10" stroke="#FEEC41" strokeWidth="2.5"/>
              <path d="M12 8v5M12 16v.5" stroke="#FEEC41" strokeWidth="2.5" strokeLinecap="round"/>
            </svg>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 30, fontWeight: 900,
              color: '#FEEC41', letterSpacing: '4px',
            }}>
              IMPORTANT DISCLAIMER
            </div>
          </div>
        </div>

        {/* Main heading */}
        <div style={{
          position: 'absolute', top: 200, left: 200,
          opacity: headIn, transform: `translateY(${(1 - headIn) * -20}px)`,
        }}>
          <div style={{
            fontFamily: 'Yantra One',
            fontSize: 102,
            fontWeight: 900, color: '#000000', lineHeight: 1,
          }}>
            एक बात साफ़ रखो —
          </div>
        </div>

        {/* NOT badge - large */}
        <div style={{
          position: 'absolute', top: 380, left: 200,
          opacity: notIn, transform: `translateX(${(1 - notIn) * -40}px)`,
          display: 'flex', alignItems: 'center', gap: 30,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 187,
            fontWeight: 900, color: '#000000',
            lineHeight: 1,
          }}>
            NOT
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 76, fontWeight: 900,
            color: '#000000', lineHeight: 1.05,
          }}>
            SEBI Registered<br/>
            <span style={{ backgroundColor: '#FEEC41', padding: '0 14px' }}>निवेश सलाहकार</span>
          </div>
        </div>

        {/* Card - what this content is */}
        <div style={{
          position: 'absolute', top: 380, right: 160,
          width: 620,
          opacity: cardIn, transform: `translateX(${(1 - cardIn) * 40}px) rotate(2deg)`,
          padding: '34px 36px',
          backgroundColor: '#FEEC41',
          border: '5px solid #000000',
          boxShadow: '14px 14px 0 #000000',
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 26, fontWeight: 900,
            color: '#000000', letterSpacing: '3px', marginBottom: 14,
          }}>
            ये है क्या ↓
          </div>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 42, fontWeight: 800,
            color: '#000000', lineHeight: 1.25,
          }}>
            सिर्फ़ जागरूकता<br/>
            और समझ बढ़ाने<br/>
            के लिए
          </div>
        </div>

        {/* Bottom - three checkmarks */}
        <div style={{
          position: 'absolute', bottom: 100, left: 200, right: 200,
          opacity: purposeIn, transform: `translateY(${(1 - purposeIn) * 20}px)`,
          display: 'flex', justifyContent: 'space-between', gap: 20,
        }}>
          {[
            { l: 'EDUCATION', h: 'शिक्षा' },
            { l: 'AWARENESS', h: 'जागरूकता' },
            { l: 'DISCUSSION', h: 'चर्चा' },
          ].map((item, i) => (
            <div key={i} style={{
              flex: 1, padding: '20px 24px',
              backgroundColor: '#000000',
              display: 'flex', alignItems: 'center', gap: 16,
            }}>
              <svg width="40" height="40" viewBox="0 0 24 24" fill="none">
                <path d="M4 12l5 5L20 6" stroke="#FEEC41" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round"/>
              </svg>
              <div>
                <div style={{ fontFamily: 'Inter, Poppins', fontSize: 20, fontWeight: 800, color: '#FEEC41', letterSpacing: '2px' }}>{item.l}</div>
                <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 34, fontWeight: 800, color: '#FEEC41' }}>{item.h}</div>
              </div>
            </div>
          ))}
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 60%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark "ProBuffet HQ" bottom-right */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene36_SEBIDisclaimer;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37_ConsultAdvisor',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene37_ConsultAdvisor: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(15);
  const p1 = sp(50, { stiffness: 90 });
  const p2 = sp(80, { stiffness: 90 });
  const p3 = sp(110, { stiffness: 90 });
  const bottomIn = sp(200);

  return (
    <AbsoluteFill style={{ backgroundColor: '#B4E1EB', overflow: 'hidden' }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 20% 80%, rgba(254,236,65,0.25) 0%, rgba(180,225,235,0) 60%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', top: '30%', left: '40%',
        width: 500, height: 500, borderRadius: '50%',
        border: '3px solid #000000', opacity: 0.06,
        transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute', top: '50%', right: '5%',
        width: 380, height: 380,
        border: '2px solid #000000', opacity: 0.06,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-10%', left: '10%',
        width: 400, height: 400,
        backgroundColor: '#FEEC41', opacity: 0.10,
        transform: `rotate(${45 + frame * 0.06}deg)`,
      }} />

      {/* Motion texture - hex grid dots */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.12 }} width="1920" height="1080">
        <defs>
          <pattern id="hex37" x="0" y="0" width="60" height="52" patternUnits="userSpaceOnUse">
            <circle cx="0" cy="0" r="2" fill="#000000"/>
            <circle cx="30" cy="26" r="2" fill="#000000"/>
            <circle cx="60" cy="0" r="2" fill="#000000"/>
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hex37)"/>
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const s = seededRandom(i + 600);
        const x = s * 1920;
        const y = seededRandom(i + 650) * 1080 + Math.sin(frame * 0.02 + i) * 15;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 5, height: 5,
            backgroundColor: '#000000', opacity: 0.25,
            transform: 'rotate(45deg)',
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ opacity: exitOp }}>
        {/* Header */}
        <div style={{
          position: 'absolute', top: 80, left: 140,
          opacity: titleIn, transform: `translateY(${(1 - titleIn) * -20}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 30, fontWeight: 900,
            color: '#000000', letterSpacing: '4px', marginBottom: 10,
          }}>
            EVERY INVESTOR • अलग कहानी
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 102, fontWeight: 900,
            color: '#000000', lineHeight: 1.05,
          }}>
            तुम्हारी कमाई • लक्ष्य • जोखिम<br/>
            <span style={{ backgroundColor: '#FEEC41', padding: '0 16px' }}>सब अलग है</span>
          </div>
        </div>

        {/* 3 pillar cards */}
        <div style={{
          position: 'absolute', top: 400, left: 140, right: 140,
          display: 'flex', gap: 40, justifyContent: 'space-between',
        }}>
          {[
            { icon: 'money', l: 'INCOME', h: 'कमाई', op: p1 },
            { icon: 'target', l: 'GOAL', h: 'लक्ष्य', op: p2 },
            { icon: 'risk', l: 'RISK', h: 'जोखिम', op: p3 },
          ].map((c, i) => (
            <div key={i} style={{
              flex: 1,
              opacity: c.op,
              transform: `translateY(${(1 - c.op) * 40}px) scale(${c.op})`,
              backgroundColor: i === 1 ? '#FEEC41' : '#B4E1EB',
              border: '6px solid #000000',
              padding: '40px 30px',
              boxShadow: '12px 12px 0 #000000',
              minHeight: 340,
            }}>
              <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
                {c.icon === 'money' && (
                  <>
                    <rect x="2" y="6" width="20" height="12" rx="1" stroke="#000000" strokeWidth="2.2"/>
                    <circle cx="12" cy="12" r="3" stroke="#000000" strokeWidth="2.2"/>
                    <circle cx="5" cy="12" r="1" fill="#000000"/>
                    <circle cx="19" cy="12" r="1" fill="#000000"/>
                  </>
                )}
                {c.icon === 'target' && (
                  <>
                    <circle cx="12" cy="12" r="10" stroke="#000000" strokeWidth="2.2"/>
                    <circle cx="12" cy="12" r="6" stroke="#000000" strokeWidth="2.2"/>
                    <circle cx="12" cy="12" r="2" fill="#000000"/>
                  </>
                )}
                {c.icon === 'risk' && (
                  <>
                    <path d="M12 2L1 21h22L12 2z" stroke="#000000" strokeWidth="2.2" strokeLinejoin="round"/>
                    <path d="M12 10v4M12 17v.5" stroke="#000000" strokeWidth="2.5" strokeLinecap="round"/>
                  </>
                )}
              </svg>
              <div style={{
                fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 900,
                color: '#000000', letterSpacing: '3px', marginTop: 24,
              }}>{c.l}</div>
              <div style={{
                fontFamily: 'Yantra One', fontSize: 85, fontWeight: 900,
                color: '#000000', marginTop: 8, lineHeight: 1,
              }}>{c.h}</div>
              <div style={{
                marginTop: 20, height: 6, width: '60%',
                backgroundColor: '#000000',
              }} />
            </div>
          ))}
        </div>

        {/* Bottom instruction */}
        <div style={{
          position: 'absolute', bottom: 60, left: 140, right: 140,
          opacity: bottomIn, transform: `translateY(${(1 - bottomIn) * 20}px)`,
          backgroundColor: '#000000',
          padding: '26px 40px',
          display: 'flex', alignItems: 'center', gap: 30,
        }}>
          <svg width="56" height="56" viewBox="0 0 24 24" fill="none">
            <path d="M4 12l5 5L20 6" stroke="#FEEC41" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round"/>
          </svg>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 44, fontWeight: 800,
            color: '#FEEC41', lineHeight: 1.15,
          }}>
            बड़ा वित्तीय फैसला — <span style={{ backgroundColor: '#FEEC41', color: '#000000', padding: '0 14px' }}>मान्यता प्राप्त सलाहकार से</span> बात करके ही
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 60%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark "ProBuffet HQ" bottom-right */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene37_ConsultAdvisor;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38_RecapMap',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene38_RecapMap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(15);
  const c1 = sp(50, { stiffness: 90 });
  const c2 = sp(85, { stiffness: 90 });
  const c3 = sp(120, { stiffness: 90 });
  const c4 = sp(155, { stiffness: 90 });

  const arrowFlow = interpolate(sp(200), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#B4E1EB', overflow: 'hidden' }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(180,225,235,1) 0%, rgba(254,236,65,0.18) 100%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', top: '-10%', left: '10%',
        width: 600, height: 600,
        border: '3px solid #000000', opacity: 0.05,
        borderRadius: 40,
        transform: `rotate(${25 + frame * 0.08}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-10%', right: '5%',
        width: 550, height: 550, borderRadius: '50%',
        border: '2px solid #000000', opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />

      {/* Motion texture - grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.09 }} width="1920" height="1080">
        {Array.from({ length: 20 }).map((_, i) => (
          <line key={`v${i}`} x1={i * 100} y1={0} x2={i * 100} y2={1080} stroke="#000000" strokeWidth="1"/>
        ))}
        {Array.from({ length: 12 }).map((_, i) => (
          <line key={`h${i}`} x1={0} y1={i * 100} x2={1920} y2={i * 100} stroke="#000000" strokeWidth="1"/>
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const s = seededRandom(i + 700);
        const x = s * 1920;
        const y = (seededRandom(i + 750) * 1080 + frame * 0.3) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 4, height: 4, borderRadius: '50%',
            backgroundColor: '#000000', opacity: 0.25,
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ opacity: exitOp }}>
        {/* Header */}
        <div style={{
          position: 'absolute', top: 70, left: 140, right: 140,
          opacity: titleIn, transform: `translateY(${(1 - titleIn) * -20}px)`,
          display: 'flex', alignItems: 'center', gap: 30,
        }}>
          <svg width="70" height="70" viewBox="0 0 24 24" fill="none">
            <path d="M9 2L3 5v17l6-3 6 3 6-3V2l-6 3-6-3z" stroke="#000000" strokeWidth="2.2" strokeLinejoin="round"/>
            <path d="M9 2v17M15 5v17" stroke="#000000" strokeWidth="2.2"/>
          </svg>
          <div>
            <div style={{ fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 900, color: '#000000', letterSpacing: '4px' }}>
              PART 1 — RECAP MAP
            </div>
            <div style={{ fontFamily: 'Yantra One', fontSize: 85, fontWeight: 900, color: '#000000', lineHeight: 1 }}>
              पहले हिस्से का <span style={{ backgroundColor: '#FEEC41', padding: '0 14px' }}>नक्शा</span>
            </div>
          </div>
        </div>

        {/* 4 pillars in flow */}
        <div style={{
          position: 'absolute', top: 340, left: 100, right: 100,
          display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 10,
        }}>
          {[
            { label: 'कर्ज़', tag: 'तेज़', dir: 'up', op: c1, bg: '#000000', tc: '#FEEC41' },
            { label: 'जमा', tag: 'सुस्त', dir: 'down', op: c2, bg: '#FEEC41', tc: '#000000' },
            { label: 'सस्ता पैसा', tag: 'घटता', dir: 'down', op: c3, bg: '#000000', tc: '#FEEC41' },
            { label: 'होड़', tag: 'बढ़ती', dir: 'up', op: c4, bg: '#FEEC41', tc: '#000000' },
          ].map((p, i) => (
            <React.Fragment key={i}>
              <div style={{
                width: 360, height: 420,
                opacity: p.op,
                transform: `translateY(${(1 - p.op) * 40}px)`,
                backgroundColor: p.bg,
                border: '6px solid #000000',
                boxShadow: '10px 10px 0 #000000',
                padding: '30px 24px',
                display: 'flex', flexDirection: 'column',
                justifyContent: 'space-between',
              }}>
                <div style={{
                  fontFamily: 'Inter, Poppins', fontSize: 24, fontWeight: 900,
                  color: p.tc, letterSpacing: '3px',
                }}>
                  0{i + 1}
                </div>
                <div>
                  <div style={{
                    fontFamily: 'Yantra One', fontSize: 85, fontWeight: 900,
                    color: p.tc, lineHeight: 1,
                  }}>
                    {p.label}
                  </div>
                  <div style={{
                    marginTop: 12,
                    fontFamily: 'Yantra One', fontSize: 68, fontWeight: 900,
                    color: p.tc, display: 'flex', alignItems: 'center', gap: 12,
                  }}>
                    {p.tag}
                    <svg width="60" height="60" viewBox="0 0 24 24" fill="none">
                      {p.dir === 'up' ? (
                        <path d="M12 4l7 8h-4v8h-6v-8H5l7-8z" fill={p.tc}/>
                      ) : (
                        <path d="M12 20l-7-8h4V4h6v8h4l-7 8z" fill={p.tc}/>
                      )}
                    </svg>
                  </div>
                </div>
              </div>
              {i < 3 && (
                <div style={{
                  opacity: p.op,
                  transform: `scale(${0.8 + 0.2 * Math.sin(frame * 0.15 + i)})`,
                }}>
                  <svg width="60" height="30" viewBox="0 0 60 30" fill="none">
                    <path d="M0 15h50M40 5l15 10-15 10" stroke="#000000" strokeWidth="5" strokeLinecap="round" strokeLinejoin="round"/>
                  </svg>
                </div>
              )}
            </React.Fragment>
          ))}
        </div>

        {/* Bottom track line */}
        <div style={{
          position: 'absolute', bottom: 100, left: 140, right: 140,
          height: 8, backgroundColor: '#000000',
          transform: `scaleX(${arrowFlow})`, transformOrigin: 'left',
        }} />
        <div style={{
          position: 'absolute', bottom: 60, left: 140, right: 140,
          textAlign: 'center',
          fontFamily: 'Inter, Poppins', fontSize: 28, fontWeight: 900,
          color: '#000000', letterSpacing: '6px',
          opacity: arrowFlow,
        }}>
          PART 1 COMPLETE → NEXT: ACTION PLAN
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 60%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark "ProBuffet HQ" bottom-right */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene38_RecapMap;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39_GameChanged',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene39_GameChanged: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const l1 = sp(20);
  const l2 = sp(70);
  const trophyIn = sp(160, { stiffness: 100 });
  const yearIn = sp(220);
  const bottomIn = sp(280);

  const trophyFloat = Math.sin(frame * 0.06) * 6;

  // Year counter animation
  const yearProg = interpolate(frame, [220, 320], [0, 5], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
    easing: Easing.out(Easing.cubic),
  });

  return (
    <AbsoluteFill style={{ backgroundColor: '#B4E1EB', overflow: 'hidden' }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(254,236,65,0.30) 0%, rgba(180,225,235,0) 55%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', top: '20%', left: '50%',
        width: 800, height: 800, borderRadius: '50%',
        border: '4px solid #000000', opacity: 0.06,
        transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.02) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute', top: '15%', left: '50%',
        width: 500, height: 500, borderRadius: '50%',
        border: '2px solid #000000', opacity: 0.08,
        transform: `translate(-50%, -50%) rotate(${frame * 0.15}deg)`,
      }} />

      {/* Motion texture - radial rays */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.10 }} width="1920" height="1080">
        {Array.from({ length: 16 }).map((_, i) => {
          const angle = (i * Math.PI * 2) / 16 + frame * 0.005;
          const x2 = 960 + Math.cos(angle) * 1200;
          const y2 = 400 + Math.sin(angle) * 1200;
          return (
            <line key={i} x1="960" y1="400" x2={x2} y2={y2}
              stroke="#000000" strokeWidth="1.5" strokeDasharray="8 12"/>
          );
        })}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const s = seededRandom(i + 800);
        const x = s * 1920;
        const y = seededRandom(i + 850) * 1080 + Math.sin(frame * 0.04 + i) * 20;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: '#000000', opacity: 0.28,
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ opacity: exitOp }}>
        {/* Line 1 - System stable */}
        <div style={{
          position: 'absolute', top: 100, left: 140,
          opacity: l1, transform: `translateX(${(1 - l1) * -40}px)`,
          display: 'flex', alignItems: 'center', gap: 24,
        }}>
          <div style={{
            width: 24, height: 24, borderRadius: '50%',
            backgroundColor: '#000000',
            transform: `scale(${1 + Math.sin(frame * 0.2) * 0.15})`,
          }} />
          <div style={{
            fontFamily: 'Yantra One', fontSize: 76, fontWeight: 900,
            color: '#000000',
          }}>
            सिस्टम स्थिर है
          </div>
          <div style={{ opacity: 0.6, fontFamily: 'Inter, Poppins', fontSize: 34, fontWeight: 700, color: '#000000' }}>
            (SYSTEM STABLE)
          </div>
        </div>

        {/* Line 2 - But game changed */}
        <div style={{
          position: 'absolute', top: 230, left: 140,
          opacity: l2, transform: `translateX(${(1 - l2) * -40}px)`,
        }}>
          <div style={{
            display: 'flex', alignItems: 'center', gap: 20, marginBottom: 12,
          }}>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 44, fontWeight: 900,
              color: '#000000',
            }}>
              पर →
            </div>
            <div style={{
              fontFamily: 'Yantra One',
              fontSize: 136,
              fontWeight: 900, color: '#000000', lineHeight: 1,
            }}>
              <span style={{ backgroundColor: '#FEEC41', padding: '0 20px', border: '5px solid #000000' }}>
                खेल बदल चुका
              </span>
            </div>
          </div>
        </div>

        {/* Trophy + years */}
        <div style={{
          position: 'absolute', bottom: 180, left: 140,
          display: 'flex', alignItems: 'center', gap: 50,
        }}>
          <div style={{
            opacity: trophyIn, transform: `scale(${trophyIn}) translateY(${trophyFloat}px)`,
          }}>
            <svg width="180" height="180" viewBox="0 0 24 24" fill="none">
              <path d="M7 4V2h10v2h4v3a4 4 0 01-4 4h-.5A5.5 5.5 0 0113 14.9V17h3v2H8v-2h3v-2.1A5.5 5.5 0 017.5 11H7a4 4 0 01-4-4V4h4zm0 2H5v1a2 2 0 002 2V6zm10 3a2 2 0 002-2V6h-2v3z"
                fill="#FEEC41" stroke="#000000" strokeWidth="1.5"/>
            </svg>
          </div>

          <div style={{
            opacity: yearIn, transform: `translateX(${(1 - yearIn) * 40}px)`,
            display: 'flex', alignItems: 'baseline', gap: 20,
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 272, // 320*0.85
              fontWeight: 900, color: '#000000', lineHeight: 0.9,
              textShadow: '10px 10px 0 #FEEC41',
            }}>
              {Math.floor(yearProg)}
            </div>
            <div style={{
              fontFamily: 'Yantra One', fontSize: 85, fontWeight: 900,
              color: '#000000',
            }}>
              साल के<br/>असली विजेता
            </div>
          </div>
        </div>

        {/* Bottom strip */}
        <div style={{
          position: 'absolute', bottom: 60, left: 0, right: 0,
          opacity: bottomIn,
          backgroundColor: '#000000',
          padding: '18px 140px',
          display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 20,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 34, fontWeight: 800,
            color: '#FEEC41', letterSpacing: '4px',
          }}>
            जो समझ लेगा — वही जीतेगा
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 55%, rgba(0,0,0,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark "ProBuffet HQ" bottom-right */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene39_GameChanged;
```

```tsx
import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40_NextChapterTeaser',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene40_NextChapterTeaser: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const subIn = sp(20, { stiffness: 100 });
  const teaseIn = sp(60);
  const card1 = sp(110, { stiffness: 90 });
  const card2 = sp(145, { stiffness: 90 });
  const card3 = sp(180, { stiffness: 90 });
  const nextIn = sp(240);

  const bellShake = frame > 20 && frame < 70
    ? Math.sin((frame - 20) * 0.5) * 12
    : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B4E1EB', overflow: 'hidden' }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(254,236,65,0.20) 0%, rgba(180,225,235,0) 55%)',
      }} />

      {/* Animated shape layers */}
      <div style={{
        position: 'absolute', top: '-12%', right: '-8%',
        width: 780, height: 780, borderRadius: '50%',
        border: '3px solid #000000', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-15%', left: '-8%',
        width: 640, height: 640,
        border: '2px solid #000000', opacity: 0.06,
        transform: `rotate(${frame * 0.09}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '35%', right: '5%',
        width: 300, height: 300,
        backgroundColor: '#FEEC41', opacity: 0.10,
        transform: `rotate(${45 + frame * 0.1}deg)`,
      }} />

      {/* Motion texture */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.10 }} width="1920" height="1080">
        <defs>
          <pattern id="grid40" x="0" y="0" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#000000" strokeWidth="1"/>
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid40)"/>
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const s = seededRandom(i + 900);
        const x = s * 1920;
        const y = (seededRandom(i + 950) * 1080 + frame * 0.4) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: x, top: y,
            width: 5, height: 5, borderRadius: '50%',
            backgroundColor: '#000000', opacity: 0.28,
          }} />
        );
      })}

      {/* MAIN CONTENT */}
      <AbsoluteFill style={{ opacity: exitOp }}>
        {/* Top - Subscribe callout */}
        <div style={{
          position: 'absolute', top: 90, left: 140,
          opacity: subIn, transform: `translateY(${(1 - subIn) * -20}px)`,
          display: 'flex', alignItems: 'center', gap: 26,
        }}>
          <div style={{
            transform: `rotate(${bellShake}deg)`,
            transformOrigin: 'top center',
          }}>
            <svg width="80" height="80" viewBox="0 0 24 24" fill="none">
              <path d="M12 2a6 6 0 00-6 6c0 4-2 5-2 7h16c0-2-2-3-2-7a6 6 0 00-6-6zm0 20a2 2 0 002-2h-4a2 2 0 002 2z"
                fill="#FEEC41" stroke="#000000" strokeWidth="2"/>
            </svg>
          </div>
          <div>
            <div style={{
              fontFamily: 'Inter, Poppins', fontSize: 26, fontWeight: 900,
              color: '#000000', letterSpacing: '4px',
            }}>SUBSCRIBE + BELL</div>
            <div style={{
              fontFamily: 'Yantra One', fontSize: 76, fontWeight: 900,
              color: '#000000', lineHeight: 1,
            }}>
              चैनल से <span style={{ backgroundColor: '#FEEC41', padding: '0 14px' }}>जुड़ जाओ</span>
            </div>
          </div>
        </div>

        {/* Tease line */}
        <div style={{
          position: 'absolute', top: 300, left: 140,
          opacity: teaseIn, transform: `translateY(${(1 - teaseIn) * 20}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter, Poppins', fontSize: 32, fontWeight: 900,
            color: '#000000', letterSpacing: '4px',
          }}>
            COMING NEXT • आगे मिलेगा
          </div>
          <div style={{
            marginTop: 8, height: 6, width: 340,
            backgroundColor: '#000000',
          }} />
        </div>

        {/* 3 preview cards */}
        <div style={{
          position: 'absolute', top: 420, left: 140, right: 140,
          display: 'flex', gap: 30,
        }}>
          {[
            { n: '01', icon: 'ladder', l: 'FD LADDER', h: 'कैसे बनाएं', op: card1, bg: '#000000', tc: '#FEEC41' },
            { n: '02', icon: 'shield', l: 'DEPOSIT INSURANCE', h: 'का सच', op: card2, bg: '#FEEC41', tc: '#000000' },
            { n: '03', icon: 'x', l: '5 MISTAKES', h: 'पांच गलतियाँ', op: card3, bg: '#000000', tc: '#FEEC41' },
          ].map((c, i) => (
            <div key={i} style={{
              flex: 1,
              opacity: c.op,
              transform: `translateY(${(1 - c.op) * 50}px)`,
              backgroundColor: c.bg,
              border: '6px solid #000000',
              boxShadow: '12px 12px 0 #000000',
              padding: '30px 28px',
              minHeight: 380,
              display: 'flex', flexDirection: 'column',
              justifyContent: 'space-between',
            }}>
              <div style={{
                fontFamily: 'JetBrains Mono', fontSize: 40, fontWeight: 900,
                color: c.tc,
              }}>
                {c.n}
              </div>
              <div>
                <svg width="90" height="90" viewBox="0 0 24 24" fill="none">
                  {c.icon === 'ladder' && (
                    <>
                      <path d="M6 2v20M18 2v20M6 6h12M6 11h12M6 16h12M6 21h12" stroke={c.tc} strokeWidth="2" strokeLinecap="round"/>
                    </>
                  )}
                  {c.icon === 'shield' && (
                    <>
                      <path d="M12 2L3 5v7c0 5 4 9 9 10 5-1 9-5 9-10V5l-9-3z" stroke={c.tc} strokeWidth="2" strokeLinejoin="round"/>
                      <path d="M8 12l3 3 5-5" stroke={c.tc} strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round"/>
                    </>
                  )}
                  {c.icon === 'x' && (
                    <>
                      <circle cx="12" cy="12" r="10" stroke={c.tc} strokeWidth="2"/>
                      <path d="M8 8l8 8M16 8l-8 8" stroke={c.tc} strokeWidth="2.5" strokeLinecap="round"/>
                    </>
                  )}
                </svg>
                <div style={{
                  marginTop: 24,
                  fontFamily: 'Inter, Poppins', fontSize: 26, fontWeight: 900,
                  color: c.tc, letterSpacing: '2px',
                }}>
                  {c.l}
                </div>
                <div style={{
                  marginTop: 6,
                  fontFamily: 'Yantra One', fontSize: 68, fontWeight: 900,
                  color: c.tc, lineHeight: 1,
                }}>
                  {c.h}
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* Bottom - CTA */}
        <div style={{
          position: 'absolute', bottom: 60, left: 140, right: 140,
          opacity: nextIn, transform: `translateY(${(1 - nextIn) * 20}px)`,
          display: 'flex', alignItems: 'center', justifyContent: 'space-between',
        }}>
          <div style={{
            fontFamily: 'Noto Sans Devanagari', fontSize: 42, fontWeight: 800,
            color: '#000000',
          }}>
            साथ हो? → <span style={{ backgroundColor: '#FEEC41', padding: '0 14px' }}>Action Plan शुरू</span>
          </div>
          <div style={{
            transform: `translateX(${Math.sin(frame * 0.15) * 8}px)`,
          }}>
            <svg width="120" height="60" viewBox="0 0 120 60" fill="none">
              <path d="M0 30h95M85 12l25 18-25 18" stroke="#000000" strokeWidth="8" strokeLinecap="round" strokeLinejoin="round"/>
            </svg>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(0,0,0,0) 60%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark "ProBuffet HQ" bottom-right */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter, Poppins', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene40_NextChapterTeaser;
```

