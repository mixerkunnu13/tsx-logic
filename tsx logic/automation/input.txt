```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene31: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleY = interpolate(sp(8), [0, 1], [80, 0]);
  const titleOp = interpolate(sp(8), [0, 1], [0, 1]);
  const camZoom = interpolate(frame, [0, durationInFrames], [1.04, 1.1]);

  // Profile cards stagger
  const profiles = [
    { label: 'JEUNES ACTIFS', sub: 'qui débutent', delay: 30 },
    { label: 'CADRES', sub: 'qui optimisent', delay: 48 },
    { label: 'RETRAITÉS', sub: 'qui transmettent', delay: 66 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5B9F0', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 20%, rgba(251,245,167,0.35), transparent 55%), radial-gradient(circle at 80% 90%, rgba(29,33,40,0.18), transparent 50%)',
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>
        <div style={{
          position: 'absolute', top: '12%', left: '8%', width: 420, height: 420,
          borderRadius: '50%', border: '2px solid #1D2128', opacity: 0.06,
          transform: `rotate(${frame * 0.3}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
        }} />
        <div style={{
          position: 'absolute', bottom: '8%', right: '10%', width: 360, height: 360,
          background: '#FBF5A7', opacity: 0.07,
          transform: `rotate(${-frame * 0.2}deg)`,
        }} />
        <div style={{
          position: 'absolute', top: '40%', right: '22%', width: 0, height: 0,
          borderLeft: '160px solid transparent', borderRight: '160px solid transparent',
          borderBottom: '280px solid #1D2128', opacity: 0.05,
          transform: `rotate(${frame * 0.15}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — vertical lines */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        {Array.from({ length: 14 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', left: `${i * 7.5}%`, top: 0, width: 1, height: '100%',
            background: '#1D2128',
            transform: `translateY(${Math.sin(frame * 0.03 + i) * 14}px)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 1);
        const r2 = seededRandom(i + 50);
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${r2 * 100}%`,
            width: 6, height: 6, borderRadius: '50%', background: '#1D2128', opacity: 0.25,
            transform: `translateY(${Math.sin(frame * 0.04 + i) * 20}px)`,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', flexDirection: 'column' }}>
        <div style={{
          fontFamily: "'Anton', sans-serif", fontSize: 110, color: '#1D2128',
          textAlign: 'center', lineHeight: 1, letterSpacing: -1,
          transform: `translateY(${titleY}px)`, opacity: titleOp,
        }}>
          À TOI DE<br />DÉCIDER
        </div>

        <div style={{
          fontFamily: "'Montserrat', sans-serif", fontSize: 38, fontWeight: 600,
          color: '#1D2128', marginTop: 18, opacity: interpolate(sp(20), [0, 1], [0, 0.8]),
        }}>
          As-tu un PEA ? Une assurance-vie ?
        </div>

        <div style={{ display: 'flex', gap: 34, marginTop: 60 }}>
          {profiles.map((p, i) => {
            const s = sp(p.delay);
            return (
              <div key={i} style={{
                width: 300, padding: '30px 24px', borderRadius: 18,
                background: '#1D2128', border: '3px solid #FBF5A7',
                transform: `translateY(${interpolate(s, [0, 1], [60, 0])}px) scale(${interpolate(s, [0, 1], [0.85, 1])})`,
                opacity: s, textAlign: 'center',
              }}>
                <svg width="56" height="56" viewBox="0 0 24 24" fill="none" style={{ marginBottom: 14 }}>
                  <circle cx="12" cy="8" r="4" stroke="#FBF5A7" strokeWidth="2" />
                  <path d="M4 21c0-4 4-6 8-6s8 2 8 6" stroke="#FBF5A7" strokeWidth="2" strokeLinecap="round" />
                </svg>
                <div style={{ fontFamily: "'Anton', sans-serif", fontSize: 32, color: '#FBF5A7', letterSpacing: 0.5 }}>{p.label}</div>
                <div style={{ fontFamily: "'Montserrat', sans-serif", fontSize: 24, color: '#B5B9F0', marginTop: 6 }}>{p.sub}</div>
              </div>
            );
          })}
        </div>
      </AbsoluteFill>

      {/* Vignette overlay */}
      <AbsoluteFill style={{
        boxShadow: 'inset 0 0 320px rgba(29,33,40,0.45)', pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: "'Montserrat', sans-serif", fontSize: 22, fontWeight: 600,
        color: '#1D2128', opacity: interpolate(frame, [0, 30], [0, 0.3], { extrapolateRight: 'clamp' }),
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene31;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [-30, 30]);

  // Rising bond-yield line graph progress
  const lineProgress = interpolate(sp(30), [0, 1], [0, 1]);
  const points = [
    [120, 620], [320, 560], [520, 590], [720, 480], [920, 430], [1120, 320], [1320, 220], [1520, 140],
  ];
  const totalPts = points.length;
  const visiblePts = Math.floor(lineProgress * (totalPts - 1)) + 1;
  const pathD = points.slice(0, visiblePts).map((p, i) => `${i === 0 ? 'M' : 'L'} ${p[0]} ${p[1]}`).join(' ');

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5B9F0', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(29,33,40,0.12), transparent 40%), radial-gradient(circle at 70% 30%, rgba(251,245,167,0.3), transparent 50%)',
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill style={{ transform: `translateX(${camPan}px)` }}>
        <div style={{
          position: 'absolute', top: '15%', right: '12%', width: 480, height: 480,
          borderRadius: 40, border: '2px solid #1D2128', opacity: 0.05,
          transform: `rotate(${frame * 0.12}deg) scale(${1 + Math.sin(frame * 0.025) * 0.04})`,
        }} />
        <div style={{
          position: 'absolute', bottom: '10%', left: '6%', width: 300, height: 300,
          borderRadius: '50%', background: '#FBF5A7', opacity: 0.08,
        }} />
      </AbsoluteFill>

      {/* Motion texture — grid */}
      <AbsoluteFill style={{ opacity: 0.07 }}>
        {Array.from({ length: 10 }).map((_, i) => (
          <div key={`h${i}`} style={{
            position: 'absolute', top: `${i * 11}%`, left: 0, width: '100%', height: 1, background: '#1D2128',
          }} />
        ))}
        {Array.from({ length: 16 }).map((_, i) => (
          <div key={`v${i}`} style={{
            position: 'absolute', left: `${i * 6.5}%`, top: 0, width: 1, height: '100%', background: '#1D2128',
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 5);
        const r2 = seededRandom(i + 80);
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${r2 * 100}%`,
            width: 5, height: 5, borderRadius: '50%', background: '#FBF5A7', opacity: 0.5,
            transform: `translateY(${Math.cos(frame * 0.03 + i) * 18}px)`,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 70, left: 90,
        fontFamily: "'Anton', sans-serif", fontSize: 80, color: '#1D2128', lineHeight: 0.95,
        transform: `translateX(${interpolate(sp(6), [0, 1], [-60, 0])}px)`, opacity: sp(6),
      }}>
        LA PRIME<br /><span style={{ color: '#1D2128', background: '#FBF5A7', padding: '0 12px' }}>DE RISQUE</span>
      </div>

      {/* Rising yield graph */}
      <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
        <path d={pathD} stroke="#1D2128" strokeWidth="8" fill="none" strokeLinecap="round" strokeLinejoin="round" />
        {points.slice(0, visiblePts).map((p, i) => (
          <circle key={i} cx={p[0]} cy={p[1]} r="10" fill="#FBF5A7" stroke="#1D2128" strokeWidth="4" />
        ))}
      </svg>

      {/* Big stat */}
      <div style={{
        position: 'absolute', bottom: 120, right: 120, textAlign: 'right',
        transform: `scale(${interpolate(sp(70), [0, 1], [0.7, 1])})`, opacity: sp(70),
      }}>
        <div style={{ fontFamily: "'Space Mono', monospace", fontSize: 150, fontWeight: 700, color: '#1D2128', lineHeight: 1 }}>
          2,2%
        </div>
        <div style={{ fontFamily: "'Montserrat', sans-serif", fontSize: 34, fontWeight: 600, color: '#1D2128' }}>
          des intérêts / PIB
        </div>
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(29,33,40,0.4)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: "'Montserrat', sans-serif", fontSize: 22, fontWeight: 600,
        color: '#1D2128', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene32;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene33: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.06, 1.0]);

  const bars = [
    { year: '2025', val: '2,2%', h: 220, delay: 30 },
    { year: '2026', val: '2,6%', h: 300, delay: 48 },
    { year: '2027', val: '2,8%', h: 360, delay: 66 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5B9F0', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 0%, rgba(251,245,167,0.32), transparent 55%), radial-gradient(circle at 20% 100%, rgba(29,33,40,0.15), transparent 50%)',
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>
        <div style={{
          position: 'absolute', top: '20%', left: '15%', width: 0, height: 0,
          borderLeft: '140px solid transparent', borderRight: '140px solid transparent',
          borderBottom: '240px solid #1D2128', opacity: 0.05,
          transform: `rotate(${frame * 0.2}deg)`,
        }} />
        <div style={{
          position: 'absolute', bottom: '15%', right: '8%', width: 380, height: 380,
          borderRadius: '50%', border: '3px solid #FBF5A7', opacity: 0.18,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — concentric circles */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', opacity: 0.05 }}>
        {Array.from({ length: 6 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', width: 200 + i * 180, height: 200 + i * 180,
            borderRadius: '50%', border: '1px solid #1D2128',
            transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.03})`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 9);
        const r2 = seededRandom(i + 33);
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${r2 * 100}%`,
            width: 5, height: 5, borderRadius: '50%', background: '#1D2128', opacity: 0.22,
            transform: `translateY(${Math.sin(frame * 0.035 + i) * 16}px)`,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 80, width: '100%', textAlign: 'center',
        fontFamily: "'Anton', sans-serif", fontSize: 76, color: '#1D2128',
        transform: `translateY(${interpolate(sp(6), [0, 1], [-50, 0])}px)`, opacity: sp(6),
      }}>
        LA CHARGE QUI GRIMPE
      </div>

      {/* Bar chart */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'flex-end', flexDirection: 'row', gap: 90, paddingBottom: 180 }}>
        {bars.map((b, i) => {
          const s = sp(b.delay);
          const barH = interpolate(s, [0, 1], [0, b.h]);
          return (
            <div key={i} style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
              <div style={{
                fontFamily: "'Space Mono', monospace", fontSize: 56, fontWeight: 700, color: '#1D2128',
                marginBottom: 16, opacity: s,
              }}>{b.val}</div>
              <div style={{
                width: 150, height: barH, borderRadius: '12px 12px 0 0',
                background: i === 2 ? '#1D2128' : '#FBF5A7',
                border: '4px solid #1D2128',
              }} />
              <div style={{
                fontFamily: "'Anton', sans-serif", fontSize: 40, color: '#1D2128', marginTop: 18, opacity: s,
              }}>{b.year}</div>
            </div>
          );
        })}
      </AbsoluteFill>

      {/* Caption */}
      <div style={{
        position: 'absolute', bottom: 90, width: '100%', textAlign: 'center',
        fontFamily: "'Montserrat', sans-serif", fontSize: 32, fontWeight: 600, color: '#1D2128',
        opacity: interpolate(sp(90), [0, 1], [0, 0.85]),
      }}>
        Une part croissante part aux créanciers
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(29,33,40,0.42)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: "'Montserrat', sans-serif", fontSize: 22, fontWeight: 600, color: '#1D2128', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene33;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene34: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camTilt = interpolate(frame, [0, durationInFrames], [-1.5, 1.5]);

  // Money flow particles moving left → right toward destinations
  const flowDest = [
    { label: 'FONDS US', x: '78%', y: '28%', delay: 40 },
    { label: 'GOLFE', x: '82%', y: '52%', delay: 56 },
    { label: 'ASSUREURS JP', x: '78%', y: '76%', delay: 72 },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5B9F0', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(29,33,40,0.18), transparent 45%, rgba(251,245,167,0.28))',
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill style={{ transform: `rotate(${camTilt}deg)` }}>
        <div style={{
          position: 'absolute', top: '10%', left: '4%', width: 360, height: 360,
          borderRadius: 30, border: '2px solid #1D2128', opacity: 0.06,
          transform: `rotate(${frame * 0.18}deg)`,
        }} />
        <div style={{
          position: 'absolute', bottom: '12%', right: '5%', width: 320, height: 320,
          borderRadius: '50%', background: '#FBF5A7', opacity: 0.07,
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — diagonal flow lines */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        {Array.from({ length: 10 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', top: `${i * 10}%`, left: '-10%', width: '120%', height: 2,
            background: '#1D2128',
            transform: `translateX(${(frame * 2 + i * 30) % 200 - 100}px) rotate(-8deg)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Flow particles (money leaving France) */}
      {Array.from({ length: 24 }).map((_, i) => {
        const phase = (frame * 4 + i * 40) % 1000;
        const t = phase / 1000;
        const x = interpolate(t, [0, 1], [18, 80]);
        const y = 35 + (i % 3) * 18 + Math.sin(frame * 0.05 + i) * 4;
        return (
          <div key={i} style={{
            position: 'absolute', left: `${x}%`, top: `${y}%`,
            width: 12, height: 12, borderRadius: '50%', background: '#FBF5A7',
            border: '2px solid #1D2128', opacity: interpolate(t, [0, 0.1, 0.9, 1], [0, 1, 1, 0]),
          }} />
        );
      })}

      {/* Source: FRANCE */}
      <div style={{
        position: 'absolute', left: '6%', top: '42%',
        transform: `translateY(${interpolate(sp(10), [0, 1], [60, 0])}px)`, opacity: sp(10),
      }}>
        <div style={{
          width: 220, height: 220, borderRadius: 24, background: '#1D2128',
          display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center',
          border: '4px solid #FBF5A7',
        }}>
          <svg width="70" height="70" viewBox="0 0 24 24" fill="none">
            <path d="M3 21h18M5 21V8l7-5 7 5v13M9 21v-6h6v6" stroke="#FBF5A7" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
          <div style={{ fontFamily: "'Anton', sans-serif", fontSize: 40, color: '#FBF5A7', marginTop: 10 }}>FRANCE</div>
        </div>
      </div>

      {/* Title */}
      <div style={{
        position: 'absolute', top: 70, width: '100%', textAlign: 'center',
        fontFamily: "'Anton', sans-serif", fontSize: 70, color: '#1D2128',
        opacity: sp(4),
      }}>
        UN TRANSFERT SILENCIEUX
      </div>

      {/* Destinations */}
      {flowDest.map((d, i) => {
        const s = sp(d.delay);
        return (
          <div key={i} style={{
            position: 'absolute', left: d.x, top: d.y,
            transform: `translate(-50%,-50%) scale(${interpolate(s, [0, 1], [0.7, 1])})`, opacity: s,
            background: '#FBF5A7', border: '3px solid #1D2128', borderRadius: 14,
            padding: '14px 26px',
            fontFamily: "'Anton', sans-serif", fontSize: 34, color: '#1D2128', whiteSpace: 'nowrap',
          }}>
            {d.label}
          </div>
        );
      })}

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(29,33,40,0.42)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: "'Montserrat', sans-serif", fontSize: 22, fontWeight: 600, color: '#1D2128', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene34;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.08]);
  const shake = frame > 66 && frame < 78 ? Math.sin(frame * 3) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5B9F0', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 80% 20%, rgba(251,245,167,0.3), transparent 50%), radial-gradient(circle at 10% 90%, rgba(29,33,40,0.16), transparent 50%)',
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camZoom}) translateX(${shake}px)` }}>
        <div style={{
          position: 'absolute', top: '14%', right: '10%', width: 440, height: 440,
          borderRadius: '50%', border: '2px solid #1D2128', opacity: 0.05,
          transform: `rotate(${-frame * 0.2}deg)`,
        }} />
        <div style={{
          position: 'absolute', bottom: '8%', left: '8%', width: 300, height: 300,
          background: '#FBF5A7', opacity: 0.07, borderRadius: 20,
          transform: `rotate(${frame * 0.15}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — dotted grid */}
      <AbsoluteFill style={{ opacity: 0.1 }}>
        {Array.from({ length: 8 }).map((_, row) =>
          Array.from({ length: 16 }).map((_, col) => (
            <div key={`${row}-${col}`} style={{
              position: 'absolute', left: `${col * 6.5 + 3}%`, top: `${row * 13 + 5}%`,
              width: 4, height: 4, borderRadius: '50%', background: '#1D2128',
              opacity: 0.5 + Math.sin(frame * 0.05 + row + col) * 0.3,
            }} />
          ))
        )}
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const r = seededRandom(i + 11);
        const r2 = seededRandom(i + 44);
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${r2 * 100}%`,
            width: 6, height: 6, borderRadius: '50%', background: '#FBF5A7', opacity: 0.45,
            transform: `translateY(${Math.cos(frame * 0.04 + i) * 18}px)`,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 90, left: 90,
        fontFamily: "'Anton', sans-serif", fontSize: 84, color: '#1D2128', lineHeight: 0.95,
        transform: `translateX(${interpolate(sp(6), [0, 1], [-70, 0])}px)`, opacity: sp(6),
      }}>
        LE PROBLÈME EST<br /><span style={{ background: '#1D2128', color: '#FBF5A7', padding: '0 14px' }}>STRUCTUREL</span>
      </div>

      {/* Huge deficit stat */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', flexDirection: 'column' }}>
        <div style={{
          fontFamily: "'Space Mono', monospace", fontSize: 280, fontWeight: 700, color: '#1D2128', lineHeight: 1,
          transform: `scale(${interpolate(sp(40), [0, 1], [0.5, 1])})`, opacity: sp(40),
        }}>
          3,4%
        </div>
        <div style={{
          fontFamily: "'Montserrat', sans-serif", fontSize: 40, fontWeight: 700, color: '#1D2128',
          opacity: interpolate(sp(60), [0, 1], [0, 1]), marginTop: -10,
          background: '#FBF5A7', padding: '8px 24px', borderRadius: 10,
        }}>
          DÉFICIT PRIMAIRE — SANS LES INTÉRÊTS
        </div>
      </AbsoluteFill>

      {/* Caption */}
      <div style={{
        position: 'absolute', bottom: 80, width: '100%', textAlign: 'center',
        fontFamily: "'Montserrat', sans-serif", fontSize: 34, fontWeight: 600, color: '#1D2128',
        opacity: interpolate(sp(80), [0, 1], [0, 0.85]),
      }}>
        L'État dépense trop. Point final.
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(29,33,40,0.42)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: "'Montserrat', sans-serif", fontSize: 22, fontWeight: 600, color: '#1D2128', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene35;
```

```tsx
import React from 'react';
[
    {
        "domain": "use.ai",
        "expirationDate": 1782539699.268874,
        "hostOnly": true,
        "httpOnly": false,
        "name": "user-geo",
        "path": "/",
        "sameSite": "lax",
        "secure": true,
        "session": false,
        "storeId": null,
        "value": "%7B%22country%22%3A%22IN%22%2C%22currency%22%3A%22INR%22%2C%22currencyCode%22%3A%22INR%22%2C%22currencySymbol%22%3A%22%24%22%2C%22ip%22%3A%2249.15.247.5%22%2C%22region%22%3A%22MH%22%2C%22regionName%22%3A%22Maharashtra%22%7D"
    },
    {
        "domain": "use.ai",
        "expirationDate": 1783140925.485906,
        "hostOnly": true,
        "httpOnly": false,
        "name": "mp_device_id",
        "path": "/",
        "sameSite": "lax",
        "secure": true,
        "session": false,
        "storeId": null,
        "value": "d10aa4cb-70f8-409d-ac8f-7a18e6057f3d"
    },
    {
        "domain": ".use.ai",
        "expirationDate": 1798088122.691155,
        "hostOnly": false,
        "httpOnly": true,
        "name": "__Secure-better-auth.session_token",
        "path": "/",
        "sameSite": "lax",
        "secure": true,
        "session": false,
        "storeId": null,
        "value": "meDFrOH6I0VlvHWj75aiGmxp1RAyc1kt.A2zwM8h9XqyKFLwg5PBUWTvyeMkkU38o63VrpvWc3S4%3D"
    },
    {
        "domain": "use.ai",
        "expirationDate": 1783140925.485776,
        "hostOnly": true,
        "httpOnly": false,
        "name": "mp_mid",
        "path": "/",
        "sameSite": "lax",
        "secure": true,
        "session": false,
        "storeId": null,
        "value": "2342acf8-c085-4565-959d-b7b9c1932938"
    },
    {
        "domain": ".use.ai",
        "expirationDate": 1785122973.289361,
        "hostOnly": false,
        "httpOnly": false,
        "name": "guest_mixpanel_id",
        "path": "/",
        "sameSite": "lax",
        "secure": true,
        "session": false,
        "storeId": null,
        "value": "2342acf8-c085-4565-959d-b7b9c1932938"
    },
    {
        "domain": ".use.ai",
        "expirationDate": 1782538860,
        "hostOnly": false,
        "httpOnly": false,
        "name": "__stripe_sid",
        "path": "/",
        "sameSite": "strict",
        "secure": true,
        "session": false,
        "storeId": null,
        "value": "270a7df1-f996-4402-974c-6a6bde159eac408d02"
    },
    {
        "domain": ".use.ai",
        "expirationDate": 1783141860.670682,
        "hostOnly": false,
        "httpOnly": false,
        "name": "__stripe_mid",
        "path": "/",
        "sameSite": "strict",
        "secure": true,
        "session": false,
        "storeId": null,
        "value": "81d9d1c4-d418-4556-9df6-7c1fca4b58e813f51b"
    },
    {
        "domain": ".use.ai",
        "expirationDate": 1785122973.289335,
        "hostOnly": false,
        "httpOnly": false,
        "name": "guest_user_id",
        "path": "/",
        "sameSite": "lax",
        "secure": true,
        "session": false,
        "storeId": null,
        "value": "f10b576d-4642-48f0-823c-4dd8eee83af5"
    }
]
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene37: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.05, 1.0]);

  // Growing snowball-style debt accumulation
  const ballScale = interpolate(sp(40), [0, 1], [0.3, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5B9F0', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 25% 30%, rgba(251,245,167,0.32), transparent 50%), radial-gradient(circle at 90% 80%, rgba(29,33,40,0.18), transparent 50%)',
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>
        <div style={{
          position: 'absolute', top: '20%', left: '12%', width: 380, height: 380,
          borderRadius: 30, border: '2px solid #1D2128', opacity: 0.05,
          transform: `rotate(${frame * 0.14}deg)`,
        }} />
        <div style={{
          position: 'absolute', bottom: '16%', right: '14%', width: 280, height: 280,
          background: '#FBF5A7', opacity: 0.07, borderRadius: '50%',
        }} />
      </AbsoluteFill>

      {/* Motion texture — diagonal rain lines */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        {Array.from({ length: 18 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', left: `${i * 6}%`, top: '-10%', width: 2, height: '40%',
            background: '#1D2128',
            transform: `translateY(${(frame * 4 + i * 50) % 1200}px) rotate(12deg)`,
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const r = seededRandom(i + 13);
        const r2 = seededRandom(i + 66);
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${r2 * 100}%`,
            width: 6, height: 6, borderRadius: '50%', background: '#FBF5A7', opacity: 0.45,
            transform: `translateY(${Math.sin(frame * 0.04 + i) * 18}px)`,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 80, width: '100%', textAlign: 'center',
        fontFamily: "'Anton', sans-serif", fontSize: 80, color: '#1D2128', lineHeight: 0.95,
        opacity: sp(5),
      }}>
        LA FRANCE REFUSE<br />
        <span style={{ background: '#1D2128', color: '#FBF5A7', padding: '0 14px' }}>L'AUSTÉRITÉ</span>
      </div>

      {/* Growing debt circle */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center' }}>
        <div style={{
          width: 360, height: 360, borderRadius: '50%', background: '#1D2128',
          border: '6px solid #FBF5A7', display: 'flex', justifyContent: 'center', alignItems: 'center',
          transform: `scale(${ballScale})`,
          boxShadow: '0 20px 60px rgba(29,33,40,0.4)',
        }}>
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: "'Anton', sans-serif", fontSize: 56, color: '#FBF5A7', lineHeight: 1 }}>LE DÉFICIT</div>
            <div style={{ fontFamily: "'Montserrat', sans-serif", fontSize: 30, color: '#B5B9F0', marginTop: 8 }}>se repousse</div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Caption */}
      <div style={{
        position: 'absolute', bottom: 80, width: '100%', textAlign: 'center',
        fontFamily: "'Montserrat', sans-serif", fontSize: 36, fontWeight: 700, color: '#1D2128',
        opacity: interpolate(sp(75), [0, 1], [0, 0.9]),
      }}>
        Loi mathématique : l'addition sera plus salée
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(29,33,40,0.42)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: "'Montserrat', sans-serif", fontSize: 22, fontWeight: 600, color: '#1D2128', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene37;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene38: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camPan = interpolate(frame, [0, durationInFrames], [-25, 25]);

  // Exponential curve drawing
  const curveProgress = interpolate(sp(28), [0, 1], [0, 1]);
  const expPoints: [number, number][] = [];
  for (let i = 0; i <= 50; i++) {
    const t = i / 50;
    const x = 150 + t * 1500;
    const y = 850 - Math.pow(t, 2.4) * 680;
    expPoints.push([x, y]);
  }
  const visible = Math.floor(curveProgress * expPoints.length);
  const pathD = expPoints.slice(0, Math.max(1, visible)).map((p, i) => `${i === 0 ? 'M' : 'L'} ${p[0]} ${p[1]}`).join(' ');

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5B9F0', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(29,33,40,0.14), transparent 40%), radial-gradient(circle at 80% 20%, rgba(251,245,167,0.3), transparent 50%)',
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill style={{ transform: `translateX(${camPan}px)` }}>
        <div style={{
          position: 'absolute', top: '12%', right: '8%', width: 420, height: 420,
          borderRadius: '50%', border: '2px solid #1D2128', opacity: 0.05,
          transform: `rotate(${frame * 0.18}deg)`,
        }} />
        <div style={{
          position: 'absolute', bottom: '10%', left: '6%', width: 280, height: 280,
          background: '#FBF5A7', opacity: 0.07, borderRadius: 24,
          transform: `rotate(${-frame * 0.12}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — grid */}
      <AbsoluteFill style={{ opacity: 0.07 }}>
        {Array.from({ length: 10 }).map((_, i) => (
          <div key={`h${i}`} style={{ position: 'absolute', top: `${i * 11}%`, left: 0, width: '100%', height: 1, background: '#1D2128' }} />
        ))}
        {Array.from({ length: 16 }).map((_, i) => (
          <div key={`v${i}`} style={{ position: 'absolute', left: `${i * 6.5}%`, top: 0, width: 1, height: '100%', background: '#1D2128' }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 12 }).map((_, i) => {
        const r = seededRandom(i + 17);
        const r2 = seededRandom(i + 88);
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${r2 * 100}%`,
            width: 5, height: 5, borderRadius: '50%', background: '#1D2128', opacity: 0.22,
            transform: `translateY(${Math.cos(frame * 0.04 + i) * 16}px)`,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 70, left: 90,
        fontFamily: "'Anton', sans-serif", fontSize: 72, color: '#1D2128', lineHeight: 0.95,
        transform: `translateX(${interpolate(sp(6), [0, 1], [-60, 0])}px)`, opacity: sp(6),
      }}>
        LA DETTE GRANDIT<br /><span style={{ background: '#FBF5A7', padding: '0 12px' }}>EXPONENTIELLEMENT</span>
      </div>

      {/* Exponential curve */}
      <svg width="1920" height="1080" style={{ position: 'absolute', top: 0, left: 0 }}>
        <line x1="150" y1="850" x2="1650" y2="850" stroke="#1D2128" strokeWidth="3" opacity="0.4" />
        <line x1="150" y1="850" x2="150" y2="150" stroke="#1D2128" strokeWidth="3" opacity="0.4" />
        <path d={pathD} stroke="#1D2128" strokeWidth="10" fill="none" strokeLinecap="round" />
        {visible > 1 && (
          <circle cx={expPoints[Math.min(visible - 1, expPoints.length - 1)][0]} cy={expPoints[Math.min(visible - 1, expPoints.length - 1)][1]} r="14" fill="#FBF5A7" stroke="#1D2128" strokeWidth="5" />
        )}
      </svg>

      {/* Scope alert tag */}
      <div style={{
        position: 'absolute', bottom: 90, right: 110,
        background: '#1D2128', color: '#FBF5A7', borderRadius: 14, padding: '20px 30px',
        fontFamily: "'Bricolage Grotesque', sans-serif", fontSize: 32, fontWeight: 600,
        maxWidth: 560, textAlign: 'right',
        transform: `scale(${interpolate(sp(75), [0, 1], [0.7, 1])})`, opacity: sp(75),
        border: '3px solid #FBF5A7',
      }}>
        SCOPE ALERTE : un effort soutenu sur plusieurs années
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(29,33,40,0.42)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: "'Montserrat', sans-serif", fontSize: 22, fontWeight: 600, color: '#1D2128', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene38;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene39: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1.0, 1.07]);

  // Question morph: "SI ?" struck, "LESQUELS & COMMENT" reveal
  const strikeW = interpolate(sp(35), [0, 1], [0, 100]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5B9F0', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 30%, rgba(251,245,167,0.32), transparent 55%), linear-gradient(180deg, transparent, rgba(29,33,40,0.14))',
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill style={{ transform: `scale(${camZoom})` }}>
        <div style={{
          position: 'absolute', top: '15%', left: '10%', width: 400, height: 400,
          borderRadius: 36, border: '2px solid #1D2128', opacity: 0.05,
          transform: `rotate(${frame * 0.16}deg)`,
        }} />
        <div style={{
          position: 'absolute', bottom: '12%', right: '10%', width: 320, height: 320,
          background: '#FBF5A7', opacity: 0.08, borderRadius: '50%',
          transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — radiating lines from center */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', opacity: 0.06 }}>
        {Array.from({ length: 24 }).map((_, i) => (
          <div key={i} style={{
            position: 'absolute', width: 2, height: 700, background: '#1D2128',
            transform: `rotate(${i * 15 + frame * 0.15}deg)`, transformOrigin: 'center',
          }} />
        ))}
      </AbsoluteFill>

      {/* Floating particles */}
      {Array.from({ length: 14 }).map((_, i) => {
        const r = seededRandom(i + 19);
        const r2 = seededRandom(i + 55);
        return (
          <div key={i} style={{
            position: 'absolute', left: `${r * 100}%`, top: `${r2 * 100}%`,
            width: 5, height: 5, borderRadius: '50%', background: '#FBF5A7', opacity: 0.4,
            transform: `translateY(${Math.sin(frame * 0.04 + i) * 16}px)`,
          }} />
        );
      })}

      {/* Main typographic statement */}
      <AbsoluteFill style={{ justifyContent: 'center', alignItems: 'center', flexDirection: 'column', gap: 20 }}>
        <div style={{
          fontFamily: "'Montserrat', sans-serif", fontSize: 46, fontWeight: 600, color: '#1D2128',
          opacity: sp(8),
        }}>
          La question n'est plus
        </div>

        {/* Struck-through SI */}
        <div style={{ position: 'relative', opacity: sp(14) }}>
          <span style={{ fontFamily: "'Anton', sans-serif", fontSize: 130, color: '#1D2128', opacity: 0.4 }}>SI ?</span>
          <div style={{
            position: 'absolute', top: '50%', left: 0, height: 10, width: `${strikeW}%`,
            background: '#1D2128', transform: 'translateY(-50%) rotate(-4deg)',
          }} />
        </div>

        <div style={{
          fontFamily: "'Anton', sans-serif", fontSize: 92, color: '#1D2128', textAlign: 'center', lineHeight: 0.95,
          transform: `scale(${interpolate(sp(45), [0, 1], [0.7, 1])})`, opacity: sp(45),
        }}>
          <span style={{ background: '#1D2128', color: '#FBF5A7', padding: '0 16px' }}>LESQUELS</span> & COMMENT<br />TE PRÉPARER
        </div>
      </AbsoluteFill>

      {/* Caption */}
      <div style={{
        position: 'absolute', bottom: 80, width: '100%', textAlign: 'center',
        fontFamily: "'Bricolage Grotesque', sans-serif", fontSize: 36, fontWeight: 600, color: '#1D2128',
        opacity: interpolate(sp(80), [0, 1], [0, 0.9]),
      }}>
        Place aux solutions concrètes
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(29,33,40,0.42)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: "'Montserrat', sans-serif", fontSize: 22, fontWeight: 600, color: '#1D2128', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene39;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene40: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const splitWidth = interpolate(sp(20), [0, 1], [0, 6]);

  const sanctuarises = ['ASSURANCE-VIE', 'SCPI', 'IMMOBILIER NU'];
  const alourdis = ['PEA', 'PER', 'CRYPTO', 'CTO'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#B5B9F0', overflow: 'hidden', opacity: exitOp }}>
      {/* Dynamic gradient background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(251,245,167,0.28) 0%, transparent 45%, transparent 55%, rgba(29,33,40,0.16) 100%)',
      }} />

      {/* Animated shape layers */}
      <AbsoluteFill>
        <div style={{
          position: 'absolute', top: '14%', left: '20%', width: 320, height: 320,
          borderRadius: '50%', border: '2px solid #1D2128', opacity: 0.05,
          transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.025) * 0.04})`,
        }} />
        <div style={{
          position: 'absolute', bottom: '12%', right: '22%', width: 280, height: 280,
          background: '#FBF5A7', opacity: 0.06, borderRadius: 24,
          transform: `rotate(${-frame * 0.13}deg)`,
        }} />
      </AbsoluteFill>

      {/* Motion texture — small dots */}
      <AbsoluteFill style={{ opacity: 0.08 }}>
        {Array.from({ length: 30 }).map((_, i) => {
          const r = seededRandom(i + 3);
          const r2 = seededRandom(i + 70);
          return (
            <div key={i} style={{
              position: 'absolute', left: `${r * 100}%`, top: `${r2 * 100}%`,
              width: 4, height: 4, borderRadius: '50%', background: '#1D2128',
              transform: `translateY(${Math.sin(frame * 0.04 + i) * 12}px)`,
            }} />
          );
        })}
      </AbsoluteFill>

      {/* Title */}
      <div style={{
        position: 'absolute', top: 50, width: '100%', textAlign: 'center',
        fontFamily: "'Anton', sans-serif", fontSize: 70, color: '#1D2128',
        opacity: sp(4),
      }}>
        FISCALITÉ 2026 : DEUX CAMPS
      </div>

      {/* Central divider */}
      <div style={{
        position: 'absolute', top: '20%', left: '50%', width: splitWidth, height: '70%',
        background: '#1D2128', transform: 'translateX(-50%)', borderRadius: 4,
      }} />

      {/* LEFT — Sanctuarisés */}
      <div style={{
        position: 'absolute', left: '6%', top: '24%', width: '38%',
        transform: `translateX(${interpolate(sp(28), [0, 1], [-80, 0])}px)`, opacity: sp(28),
      }}>
        <div style={{
          background: '#FBF5A7', border: '4px solid #1D2128', borderRadius: 18, padding: '26px',
        }}>
          <div style={{ fontFamily: "'Anton', sans-serif", fontSize: 44, color: '#1D2128', marginBottom: 6 }}>SANCTUARISÉS</div>
          <div style={{ fontFamily: "'Space Mono', monospace", fontSize: 56, fontWeight: 700, color: '#1D2128', marginBottom: 18 }}>17,2 %</div>
          {sanctuarises.map((s, i) => (
            <div key={i} style={{
              display: 'flex', alignItems: 'center', gap: 12, marginBottom: 12,
              opacity: sp(40 + i * 8),
            }}>
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
                <path d="M20 6L9 17l-5-5" stroke="#1D2128" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />
              </svg>
              <span style={{ fontFamily: "'Montserrat', sans-serif", fontSize: 34, fontWeight: 600, color: '#1D2128' }}>{s}</span>
            </div>
          ))}
        </div>
      </div>

      {/* RIGHT — Alourdis */}
      <div style={{
        position: 'absolute', right: '6%', top: '24%', width: '38%',
        transform: `translateX(${interpolate(sp(28), [0, 1], [80, 0])}px)`, opacity: sp(28),
      }}>
        <div style={{
          background: '#1D2128', border: '4px solid #FBF5A7', borderRadius: 18, padding: '26px',
        }}>
          <div style={{ fontFamily: "'Anton', sans-serif", fontSize: 44, color: '#FBF5A7', marginBottom: 6 }}>ALOURDIS</div>
          <div style={{ fontFamily: "'Space Mono', monospace", fontSize: 56, fontWeight: 700, color: '#B5B9F0', marginBottom: 18 }}>18,6 % / 31,4 %</div>
          {alourdis.map((a, i) => (
            <div key={i} style={{
              display: 'flex', alignItems: 'center', gap: 12, marginBottom: 12,
              opacity: sp(40 + i * 8),
            }}>
              <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
                <path d="M12 5v8m0 4h.01" stroke="#FBF5A7" strokeWidth="3" strokeLinecap="round" />
              </svg>
              <span style={{ fontFamily: "'Montserrat', sans-serif", fontSize: 34, fontWeight: 600, color: '#FBF5A7' }}>{a}</span>
            </div>
          ))}
        </div>
      </div>

      {/* Caption */}
      <div style={{
        position: 'absolute', bottom: 70, width: '100%', textAlign: 'center',
        fontFamily: "'Bricolage Grotesque', sans-serif", fontSize: 36, fontWeight: 600, color: '#1D2128',
        opacity: interpolate(sp(85), [0, 1], [0, 0.9]),
      }}>
        Mais le PEA reste incontournable
      </div>

      {/* Vignette overlay */}
      <AbsoluteFill style={{ boxShadow: 'inset 0 0 320px rgba(29,33,40,0.42)', pointerEvents: 'none' }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: "'Montserrat', sans-serif", fontSize: 22, fontWeight: 600, color: '#1D2128', opacity: 0.3,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene40;
```

