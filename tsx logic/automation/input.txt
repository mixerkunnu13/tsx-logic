```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31_HigherEarnerMax',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene31_HigherEarnerMax: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom in slowly
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05], { extrapolateRight: 'clamp' });

  // Age progression 62 -> 70
  const ageProgress = interpolate(frame, [30, 180], [62, 70], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const currentAge = Math.floor(ageProgress);

  // Benefit growth $2000 -> $2640
  const benefitVal = interpolate(frame, [30, 180], [2000, 2640], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Bar growth
  const barHeight = interpolate(frame, [40, 200], [0, 480], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Title entrance
  const titleY = sp(5) * 1;
  const titleOp = interpolate(sp(5), [0, 1], [0, 1]);

  // MAX stamp pop at end
  const maxStamp = sp(190, { damping: 8, stiffness: 180 });

  // Headline reveal
  const headlineOp = interpolate(sp(15), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient background */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 30% 30%, #5A3E1F 0%, #412D15 50%, #2A1C0D 100%)',
        }}
      />

      {/* Shape Layer - drifting geometric forms */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            top: -200,
            left: -150,
            width: 700,
            height: 700,
            borderRadius: '50%',
            border: '2px solid #FAB95B',
            opacity: 0.06,
            transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: -180,
            right: -200,
            width: 600,
            height: 600,
            border: '2px solid #E8EDF2',
            opacity: 0.05,
            transform: `rotate(${-frame * 0.12}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            top: '40%',
            left: '70%',
            width: 280,
            height: 280,
            borderRadius: 20,
            border: '1.5px solid #FAB95B',
            opacity: 0.04,
            transform: `translate(-50%,-50%) rotate(${frame * 0.2}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Motion texture - dotted grid */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="dotgrid31" x="0" y="0" width="60" height="60" patternUnits="userSpaceOnUse">
            <circle cx="2" cy="2" r="1.5" fill="#FAB95B" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#dotgrid31)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const seed = seededRandom(i + 31);
        const x = seed * 1920;
        const y = (seededRandom(i + 90) * 1080 + frame * (0.4 + seed * 0.6)) % 1080;
        const size = 2 + seed * 3;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: size,
              height: size,
              borderRadius: '50%',
              background: i % 3 === 0 ? '#FAB95B' : '#E8EDF2',
              opacity: 0.3 + seed * 0.4,
            }}
          />
        );
      })}

      {/* MAIN CONTENT WRAPPER */}
      <AbsoluteFill style={{ transform: `scale(${camScale})`, transformOrigin: 'center center' }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: '50%',
            transform: `translateX(-50%) translateY(${(1 - titleY) * -40}px)`,
            opacity: titleOp,
            display: 'flex',
            alignItems: 'center',
            gap: 18,
          }}
        >
          <div style={{ width: 60, height: 4, background: '#FAB95B' }} />
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 32,
              fontWeight: 700,
              letterSpacing: 8,
              color: '#FAB95B',
              textTransform: 'uppercase',
            }}
          >
            The Delay Reward
          </div>
          <div style={{ width: 60, height: 4, background: '#FAB95B' }} />
        </div>

        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 145,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: headlineOp,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 95,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1.05,
              letterSpacing: -2,
            }}
          >
            Wait Until <span style={{ color: '#FAB95B' }}>70</span>
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 42,
              fontWeight: 500,
              color: '#E8EDF2',
              opacity: 0.7,
              marginTop: 8,
            }}
          >
            Check Becomes Maximized
          </div>
        </div>

        {/* Bar chart visualization */}
        <div
          style={{
            position: 'absolute',
            left: 200,
            bottom: 180,
            width: 700,
            height: 520,
            display: 'flex',
            alignItems: 'flex-end',
            gap: 60,
            justifyContent: 'center',
          }}
        >
          {/* Age 62 bar - small */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 14 }}>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 36,
                fontWeight: 700,
                color: '#9E8770',
              }}
            >
              $1,500
            </div>
            <div
              style={{
                width: 140,
                height: 180,
                background: 'linear-gradient(180deg, #6B5538 0%, #4A3A26 100%)',
                border: '2px solid #9E8770',
                borderRadius: 4,
              }}
            />
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 38,
                fontWeight: 800,
                color: '#9E8770',
                letterSpacing: 2,
              }}
            >
              AGE 62
            </div>
          </div>

          {/* Animated growing bar - Age 70 */}
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 14, position: 'relative' }}>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 44,
                fontWeight: 800,
                color: '#FAB95B',
                opacity: interpolate(frame, [60, 90], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
              }}
            >
              ${Math.floor(benefitVal).toLocaleString()}
            </div>
            <div
              style={{
                width: 140,
                height: barHeight,
                background: 'linear-gradient(180deg, #FAB95B 0%, #D89A3F 100%)',
                border: '3px solid #FAB95B',
                borderRadius: 4,
                boxShadow: '0 0 40px rgba(250,185,91,0.5)',
                position: 'relative',
              }}
            >
              {/* growth arrow on bar */}
              <div
                style={{
                  position: 'absolute',
                  top: -20,
                  left: '50%',
                  transform: `translateX(-50%) translateY(${Math.sin(frame * 0.15) * 4}px)`,
                  fontSize: 24,
                  color: '#412D15',
                  fontWeight: 900,
                  opacity: interpolate(frame, [120, 150], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
                }}
              >
                ▲ +8%/yr
              </div>
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 44,
                fontWeight: 900,
                color: '#FAB95B',
                letterSpacing: 2,
              }}
            >
              AGE {currentAge}
            </div>
          </div>
        </div>

        {/* Right side - Age counter card */}
        <div
          style={{
            position: 'absolute',
            right: 110,
            top: 380,
            width: 520,
            padding: '40px 50px',
            background: 'rgba(232,237,242,0.04)',
            border: '2px solid rgba(250,185,91,0.3)',
            borderRadius: 16,
            opacity: interpolate(sp(25), [0, 1], [0, 1]),
            transform: `translateY(${(1 - sp(25)) * 30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 4,
              textTransform: 'uppercase',
              marginBottom: 16,
            }}
          >
            Delayed Retirement
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 72,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1,
              marginBottom: 20,
            }}
          >
            +32% Boost
          </div>
          <div
            style={{
              width: '100%',
              height: 2,
              background: 'rgba(250,185,91,0.3)',
              marginBottom: 20,
            }}
          />
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 32,
              fontWeight: 500,
              color: '#E8EDF2',
              opacity: 0.8,
              lineHeight: 1.3,
            }}
          >
            Every year of waiting adds 8% to your lifetime check.
          </div>
        </div>

        {/* MAXIMIZED stamp */}
        <div
          style={{
            position: 'absolute',
            right: 140,
            bottom: 200,
            transform: `scale(${maxStamp}) rotate(-8deg)`,
            opacity: maxStamp,
          }}
        >
          <div
            style={{
              padding: '20px 40px',
              border: '4px solid #FAB95B',
              borderRadius: 8,
              fontFamily: 'Inter',
              fontSize: 56,
              fontWeight: 900,
              color: '#FAB95B',
              letterSpacing: 6,
              background: 'rgba(65,45,21,0.7)',
            }}
          >
            MAXIMIZED
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene31_HigherEarnerMax;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32_SurvivorStepUp',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32_SurvivorStepUp: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slight pan
  const camX = interpolate(frame, [0, durationInFrames], [0, -15], { extrapolateRight: 'clamp' });

  // Step-up animation - the survivor benefit jumps up
  const stepUp = sp(90, { damping: 10, stiffness: 100 });
  const arrowProgress = interpolate(frame, [90, 150], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, #412D15 0%, #5A3E1F 50%, #2A1C0D 100%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            top: '20%',
            left: '10%',
            width: 500,
            height: 500,
            border: '2px solid #E8EDF2',
            opacity: 0.05,
            transform: `rotate(45deg) scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '15%',
            right: '8%',
            width: 0,
            height: 0,
            borderLeft: '180px solid transparent',
            borderRight: '180px solid transparent',
            borderBottom: '320px solid #FAB95B',
            opacity: 0.04,
            transform: `rotate(${frame * 0.1}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Diagonal line texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="diag32" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M0,40 L40,0" stroke="#FAB95B" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#diag32)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = seededRandom(i + 132);
        const x = (seed * 1920 + frame * 0.5) % 1920;
        const y = seededRandom(i + 200) * 1080;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y + Math.sin(frame * 0.03 + i) * 8,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: '#FAB95B',
              opacity: 0.4,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${camX}px)` }}>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 80,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(5), [0, 1], [0, 1]),
            transform: `translateY(${(1 - sp(5)) * -30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 30,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 10,
              textTransform: 'uppercase',
              marginBottom: 12,
            }}
          >
            The Survivor Step-Up
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 90,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1,
              letterSpacing: -2,
            }}
          >
            Surviving Spouse <span style={{ color: '#FAB95B' }}>Steps Up</span>
          </div>
        </div>

        {/* Two-tier visualization */}
        <div
          style={{
            position: 'absolute',
            top: 340,
            left: '50%',
            transform: 'translateX(-50%)',
            width: 1400,
            height: 580,
          }}
        >
          {/* Lower tier - survivor's own benefit */}
          <div
            style={{
              position: 'absolute',
              left: 80,
              bottom: 80,
              width: 480,
              padding: '40px',
              background: 'rgba(232,237,242,0.05)',
              border: '2px solid rgba(232,237,242,0.2)',
              borderRadius: 16,
              opacity: interpolate(sp(20), [0, 1], [0, 1]),
              transform: `translateY(${(1 - sp(20)) * 40}px)`,
            }}
          >
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 26,
                fontWeight: 600,
                color: '#9E8770',
                letterSpacing: 4,
                textTransform: 'uppercase',
                marginBottom: 14,
              }}
            >
              Survivor's Own Check
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 68,
                fontWeight: 800,
                color: '#E8EDF2',
                lineHeight: 1,
              }}
            >
              $1,500
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 28,
                fontWeight: 500,
                color: '#E8EDF2',
                opacity: 0.6,
                marginTop: 10,
              }}
            >
              per month
            </div>
          </div>

          {/* Animated arrow - step up */}
          <svg
            width="220"
            height="400"
            style={{
              position: 'absolute',
              left: 590,
              bottom: 120,
              opacity: arrowProgress,
            }}
          >
            <defs>
              <marker id="arrowhead32" markerWidth="14" markerHeight="14" refX="0" refY="7" orient="auto">
                <polygon points="0 0, 14 7, 0 14" fill="#FAB95B" />
              </marker>
            </defs>
            <path
              d={`M 20 380 L ${20 + 180 * arrowProgress} ${380 - 340 * arrowProgress}`}
              stroke="#FAB95B"
              strokeWidth="5"
              strokeLinecap="round"
              fill="none"
              markerEnd="url(#arrowhead32)"
              strokeDasharray="8 6"
            />
          </svg>

          {/* Upper tier - maximized benefit (step up target) */}
          <div
            style={{
              position: 'absolute',
              right: 80,
              top: 20,
              width: 560,
              padding: '50px',
              background: 'linear-gradient(135deg, rgba(250,185,91,0.18) 0%, rgba(250,185,91,0.08) 100%)',
              border: '3px solid #FAB95B',
              borderRadius: 20,
              opacity: stepUp,
              transform: `translateY(${(1 - stepUp) * -50}px) scale(${0.9 + stepUp * 0.1})`,
              boxShadow: '0 0 60px rgba(250,185,91,0.3)',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 28,
                fontWeight: 700,
                color: '#FAB95B',
                letterSpacing: 4,
                textTransform: 'uppercase',
                marginBottom: 14,
              }}
            >
              Deceased Spouse's Max Check
            </div>
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 92,
                fontWeight: 900,
                color: '#FAB95B',
                lineHeight: 1,
                textShadow: '0 0 30px rgba(250,185,91,0.5)',
              }}
            >
              $2,640
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 30,
                fontWeight: 500,
                color: '#E8EDF2',
                marginTop: 14,
              }}
            >
              per month — for life
            </div>
            <div
              style={{
                marginTop: 24,
                padding: '14px 24px',
                background: '#FAB95B',
                color: '#412D15',
                fontFamily: 'Inter',
                fontSize: 28,
                fontWeight: 800,
                letterSpacing: 3,
                borderRadius: 6,
                display: 'inline-block',
              }}
            >
              +76% INCREASE
            </div>
          </div>
        </div>

        {/* Bottom caption */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(frame, [160, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 38,
              fontWeight: 600,
              color: '#E8EDF2',
              letterSpacing: 1,
            }}
          >
            The larger benefit transfers to the survivor for the rest of their life
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.5) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene32_SurvivorStepUp;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33_DoubleValue',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene33_DoubleValue: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter from 1x to 2x
  const multiplier = interpolate(frame, [40, 180], [1, 2], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Camera punch zoom
  const camScale = interpolate(frame, [0, 30, durationInFrames], [1.1, 1, 1.02], { extrapolateRight: 'clamp' });

  // Big X2 reveal
  const x2Scale = sp(60, { damping: 8, stiffness: 140 });

  // Screen shake on impact
  const shake = frame > 60 && frame < 75 ? Math.sin(frame * 3) * 6 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, #6B4824 0%, #412D15 60%, #1F1408 100%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        {[0, 1, 2, 3].map((i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              width: 400 + i * 200,
              height: 400 + i * 200,
              borderRadius: '50%',
              border: `2px solid ${i % 2 === 0 ? '#FAB95B' : '#E8EDF2'}`,
              opacity: 0.04,
              transform: `translate(-50%,-50%) rotate(${frame * (0.1 + i * 0.05)}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.04})`,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Hexagon texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="hex33" x="0" y="0" width="80" height="70" patternUnits="userSpaceOnUse">
            <polygon points="40,5 75,25 75,55 40,75 5,55 5,25" fill="none" stroke="#FAB95B" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#hex33)" />
      </svg>

      {/* Particles - radial burst */}
      {Array.from({ length: 35 }).map((_, i) => {
        const seed = seededRandom(i + 233);
        const angle = (i / 35) * Math.PI * 2;
        const radius = 300 + seed * 500 + frame * 1.2;
        const x = 960 + Math.cos(angle) * radius;
        const y = 540 + Math.sin(angle) * radius;
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: x,
              top: y,
              width: 4,
              height: 4,
              borderRadius: '50%',
              background: '#FAB95B',
              opacity: Math.max(0, 0.6 - (radius - 300) / 800),
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale}) translate(${shake}px, ${shake * 0.5}px)` }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 100,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(5), [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 32,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 12,
              textTransform: 'uppercase',
            }}
          >
            The Result
          </div>
        </div>

        {/* Main X2 reveal */}
        <div
          style={{
            position: 'absolute',
            top: '50%',
            left: '50%',
            transform: `translate(-50%,-50%) scale(${x2Scale})`,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 320,
              fontWeight: 900,
              color: '#FAB95B',
              lineHeight: 0.9,
              letterSpacing: -10,
              textShadow: '0 0 60px rgba(250,185,91,0.4)',
            }}
          >
            {multiplier.toFixed(2)}<span style={{ fontSize: 180 }}>x</span>
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 70,
              fontWeight: 800,
              color: '#E8EDF2',
              marginTop: -20,
              letterSpacing: -1,
            }}
          >
            LIFETIME VALUE
          </div>
        </div>

        {/* Left side stat */}
        <div
          style={{
            position: 'absolute',
            left: 120,
            top: '50%',
            transform: `translateY(-50%) translateX(${(1 - sp(30)) * -60}px)`,
            opacity: interpolate(sp(30), [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 26,
              fontWeight: 600,
              color: '#9E8770',
              letterSpacing: 4,
              textTransform: 'uppercase',
              marginBottom: 12,
            }}
          >
            Without Strategy
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 56,
              fontWeight: 800,
              color: '#E8EDF2',
              opacity: 0.6,
            }}
          >
            $360k
          </div>
          <div
            style={{
              width: 200,
              height: 3,
              background: '#9E8770',
              marginTop: 10,
            }}
          />
        </div>

        {/* Right side stat */}
        <div
          style={{
            position: 'absolute',
            right: 120,
            top: '50%',
            transform: `translateY(-50%) translateX(${(1 - sp(40)) * 60}px)`,
            opacity: interpolate(sp(40), [0, 1], [0, 1]),
            textAlign: 'right',
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 26,
              fontWeight: 700,
              color: '#FAB95B',
              letterSpacing: 4,
              textTransform: 'uppercase',
              marginBottom: 12,
            }}
          >
            With Strategy
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 72,
              fontWeight: 900,
              color: '#FAB95B',
              textShadow: '0 0 20px rgba(250,185,91,0.5)',
            }}
          >
            $720k+
          </div>
          <div
            style={{
              width: 240,
              height: 4,
              background: '#FAB95B',
              marginTop: 10,
              marginLeft: 'auto',
            }}
          />
        </div>

        {/* Bottom tagline */}
        <div
          style={{
            position: 'absolute',
            bottom: 100,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(frame, [140, 180], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 44,
              fontWeight: 700,
              color: '#E8EDF2',
              letterSpacing: 1,
            }}
          >
            Double the value. <span style={{ color: '#FAB95B' }}>Same Social Security system.</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene33_DoubleValue;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34_FinancialFortress',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene34_FinancialFortress: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Fortress brick build-up
  const brickReveal = (i: number) => sp(20 + i * 4, { damping: 14, stiffness: 110 });

  // Camera tilt up
  const camY = interpolate(frame, [0, durationInFrames], [20, -10], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, #2A1C0D 0%, #412D15 40%, #5A3E1F 100%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            top: '8%',
            right: '12%',
            width: 380,
            height: 380,
            border: '2px solid #FAB95B',
            opacity: 0.05,
            transform: `rotate(${frame * 0.1}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '20%',
            left: '6%',
            width: 320,
            height: 320,
            borderRadius: '50%',
            border: '2px solid #E8EDF2',
            opacity: 0.04,
            transform: `scale(${1 + Math.sin(frame * 0.03) * 0.06})`,
          }}
        />
      </AbsoluteFill>

      {/* Cross-hatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="cross34" x="0" y="0" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M 0 25 L 50 25 M 25 0 L 25 50" stroke="#FAB95B" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#cross34)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const seed = seededRandom(i + 334);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: (seed * 1920 + frame * 0.3) % 1920,
              top: 1080 - ((seededRandom(i + 400) * 1080 + frame * 0.7) % 1080),
              width: 2 + seed * 2,
              height: 2 + seed * 2,
              borderRadius: '50%',
              background: '#FAB95B',
              opacity: 0.5,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateY(${camY}px)` }}>
        {/* Headline */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(5), [0, 1], [0, 1]),
            transform: `translateY(${(1 - sp(5)) * -30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 30,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 10,
              textTransform: 'uppercase',
              marginBottom: 14,
            }}
          >
            What You're Really Building
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 100,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1,
              letterSpacing: -2,
            }}
          >
            A <span style={{ color: '#FAB95B' }}>Financial Fortress</span>
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 40,
              fontWeight: 500,
              color: '#E8EDF2',
              opacity: 0.7,
              marginTop: 10,
            }}
          >
            For The Spouse You Leave Behind
          </div>
        </div>

        {/* Fortress visualization - SVG castle/shield */}
        <div
          style={{
            position: 'absolute',
            top: 400,
            left: '50%',
            transform: 'translateX(-50%)',
            width: 1400,
            height: 550,
          }}
        >
          {/* Brick wall stacking up */}
          <div
            style={{
              position: 'absolute',
              left: '50%',
              bottom: 60,
              transform: 'translateX(-50%)',
              display: 'flex',
              flexDirection: 'column',
              gap: 6,
            }}
          >
            {[
              ['Maximum Benefit', '$2,640/mo'],
              ['Survivor Step-Up', 'For Life'],
              ['Delayed Credits', '+32% Boost'],
              ['Spousal Strategy', 'Locked In'],
              ['SSA Filing Plan', 'Optimized'],
            ].map(([label, val], i) => {
              const r = brickReveal(i);
              return (
                <div
                  key={i}
                  style={{
                    width: 700,
                    height: 70,
                    background: i === 0
                      ? 'linear-gradient(90deg, #FAB95B 0%, #D89A3F 100%)'
                      : 'linear-gradient(90deg, rgba(250,185,91,0.18) 0%, rgba(250,185,91,0.08) 100%)',
                    border: i === 0 ? '3px solid #FAB95B' : '2px solid rgba(250,185,91,0.4)',
                    borderRadius: 6,
                    display: 'flex',
                    alignItems: 'center',
                    justifyContent: 'space-between',
                    padding: '0 32px',
                    opacity: r,
                    transform: `translateY(${(1 - r) * 30}px) scale(${0.95 + r * 0.05})`,
                    boxShadow: i === 0 ? '0 0 40px rgba(250,185,91,0.5)' : 'none',
                  }}
                >
                  <div
                    style={{
                      fontFamily: 'Inter',
                      fontSize: 30,
                      fontWeight: 700,
                      color: i === 0 ? '#412D15' : '#E8EDF2',
                      letterSpacing: 1,
                    }}
                  >
                    {label}
                  </div>
                  <div
                    style={{
                      fontFamily: 'JetBrains Mono',
                      fontSize: 28,
                      fontWeight: 800,
                      color: i === 0 ? '#412D15' : '#FAB95B',
                    }}
                  >
                    {val}
                  </div>
                </div>
              );
            })}
          </div>

          {/* Shield icon overlay (left) */}
          <svg
            width="160"
            height="200"
            viewBox="0 0 160 200"
            style={{
              position: 'absolute',
              left: 60,
              top: 60,
              opacity: interpolate(sp(80), [0, 1], [0, 0.85]),
              transform: `scale(${sp(80)})`,
            }}
          >
            <path
              d="M 80 10 L 150 40 L 150 110 Q 150 170 80 195 Q 10 170 10 110 L 10 40 Z"
              fill="none"
              stroke="#FAB95B"
              strokeWidth="4"
            />
            <path
              d="M 50 95 L 72 117 L 115 75"
              fill="none"
              stroke="#FAB95B"
              strokeWidth="6"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          </svg>

          {/* Shield icon overlay (right) */}
          <svg
            width="160"
            height="200"
            viewBox="0 0 160 200"
            style={{
              position: 'absolute',
              right: 60,
              top: 60,
              opacity: interpolate(sp(90), [0, 1], [0, 0.85]),
              transform: `scale(${sp(90)})`,
            }}
          >
            <path
              d="M 80 10 L 150 40 L 150 110 Q 150 170 80 195 Q 10 170 10 110 L 10 40 Z"
              fill="none"
              stroke="#E8EDF2"
              strokeWidth="4"
            />
            <circle cx="80" cy="95" r="22" fill="none" stroke="#E8EDF2" strokeWidth="4" />
            <path d="M 80 73 L 80 117" stroke="#E8EDF2" strokeWidth="4" strokeLinecap="round" />
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene34_FinancialFortress;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35_BringTogether',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35_BringTogether: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Three lines converging to center
  const converge = interpolate(sp(50), [0, 1], [0, 1]);

  // Center burst
  const burst = sp(120, { damping: 9, stiffness: 130 });

  // Camera orbit feel
  const orbitRot = interpolate(frame, [0, durationInFrames], [-3, 3], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at 50% 50%, #5A3E1F 0%, #412D15 50%, #1F1408 100%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        {[0, 1, 2].map((i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              width: 600 + i * 250,
              height: 600 + i * 250,
              border: `1.5px solid ${i % 2 === 0 ? '#FAB95B' : '#E8EDF2'}`,
              borderRadius: '50%',
              opacity: 0.05,
              transform: `translate(-50%,-50%) rotate(${frame * 0.08 * (i + 1)}deg)`,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="grid35" x="0" y="0" width="60" height="60" patternUnits="userSpaceOnUse">
            <rect width="60" height="60" fill="none" stroke="#FAB95B" strokeWidth="0.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#grid35)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const seed = seededRandom(i + 535);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: (seed * 1920 + frame * 0.4) % 1920,
              top: (seededRandom(i + 600) * 1080 + Math.sin(frame * 0.02 + i) * 30) % 1080,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: i % 2 === 0 ? '#FAB95B' : '#E8EDF2',
              opacity: 0.4,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${orbitRot * 0.1}deg)` }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(5), [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 30,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 12,
              textTransform: 'uppercase',
              marginBottom: 16,
            }}
          >
            The Complete Picture
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 120,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1,
              letterSpacing: -3,
            }}
          >
            Let's Bring It <span style={{ color: '#FAB95B' }}>All Together</span>
          </div>
        </div>

        {/* Three converging pillars */}
        <div
          style={{
            position: 'absolute',
            top: 430,
            left: '50%',
            transform: 'translateX(-50%)',
            width: 1500,
            height: 380,
          }}
        >
          {/* Pillar 1 - left */}
          <div
            style={{
              position: 'absolute',
              left: interpolate(converge, [0, 1], [-200, 60]),
              top: 50,
              width: 380,
              padding: '36px',
              background: 'rgba(232,237,242,0.05)',
              border: '2px solid rgba(250,185,91,0.4)',
              borderRadius: 16,
              opacity: interpolate(sp(30), [0, 1], [0, 1]),
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 56,
                fontWeight: 800,
                color: '#FAB95B',
                marginBottom: 14,
              }}
            >
              01
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 32,
                fontWeight: 700,
                color: '#E8EDF2',
                lineHeight: 1.2,
              }}
            >
              Restricted Application
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 24,
                fontWeight: 500,
                color: '#E8EDF2',
                opacity: 0.6,
                marginTop: 8,
              }}
            >
              Pre-1954 strategy
            </div>
          </div>

          {/* Pillar 2 - center */}
          <div
            style={{
              position: 'absolute',
              left: '50%',
              transform: `translateX(-50%) translateY(${interpolate(converge, [0, 1], [-100, 0])}px)`,
              top: 50,
              width: 380,
              padding: '36px',
              background: 'rgba(250,185,91,0.12)',
              border: '3px solid #FAB95B',
              borderRadius: 16,
              opacity: interpolate(sp(40), [0, 1], [0, 1]),
              boxShadow: '0 0 50px rgba(250,185,91,0.3)',
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 56,
                fontWeight: 800,
                color: '#FAB95B',
                marginBottom: 14,
              }}
            >
              02
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 32,
                fontWeight: 700,
                color: '#E8EDF2',
                lineHeight: 1.2,
              }}
            >
              Higher Earner Delay
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 24,
                fontWeight: 500,
                color: '#E8EDF2',
                opacity: 0.6,
                marginTop: 8,
              }}
            >
              Modern strategy
            </div>
          </div>

          {/* Pillar 3 - right */}
          <div
            style={{
              position: 'absolute',
              right: interpolate(converge, [0, 1], [-200, 60]),
              top: 50,
              width: 380,
              padding: '36px',
              background: 'rgba(232,237,242,0.05)',
              border: '2px solid rgba(250,185,91,0.4)',
              borderRadius: 16,
              opacity: interpolate(sp(50), [0, 1], [0, 1]),
            }}
          >
            <div
              style={{
                fontFamily: 'JetBrains Mono',
                fontSize: 56,
                fontWeight: 800,
                color: '#FAB95B',
                marginBottom: 14,
              }}
            >
              03
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 32,
                fontWeight: 700,
                color: '#E8EDF2',
                lineHeight: 1.2,
              }}
            >
              Survivor Multiplier
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 24,
                fontWeight: 500,
                color: '#E8EDF2',
                opacity: 0.6,
                marginTop: 8,
              }}
            >
              Lifetime protection
            </div>
          </div>
        </div>

        {/* Bottom convergence text */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: burst,
            transform: `scale(${0.9 + burst * 0.1})`,
          }}
        >
          <div
            style={{
              display: 'inline-block',
              padding: '18px 50px',
              background: '#FAB95B',
              color: '#412D15',
              fontFamily: 'Inter',
              fontSize: 44,
              fontWeight: 900,
              letterSpacing: 3,
              borderRadius: 8,
            }}
          >
            ONE UNIFIED PLAN
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene35_BringTogether;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36_ThreeElements',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene36_ThreeElements: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const card = (delay: number) => sp(delay, { damping: 11, stiffness: 130 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(225deg, #5A3E1F 0%, #412D15 50%, #2A1C0D 100%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            top: '15%',
            left: '20%',
            width: 280,
            height: 280,
            borderRadius: 20,
            border: '2px solid #FAB95B',
            opacity: 0.06,
            transform: `rotate(${15 + frame * 0.1}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '10%',
            right: '15%',
            width: 360,
            height: 360,
            border: '2px solid #E8EDF2',
            opacity: 0.05,
            transform: `rotate(${-frame * 0.08}deg)`,
            borderRadius: '50%',
          }}
        />
      </AbsoluteFill>

      {/* Vertical line texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="vlines36" x="0" y="0" width="40" height="40" patternUnits="userSpaceOnUse">
            <line x1="0" y1="0" x2="0" y2="40" stroke="#FAB95B" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#vlines36)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = seededRandom(i + 636);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: (seed * 1920) % 1920,
              top: (seededRandom(i + 700) * 1080 + frame * 0.6) % 1080,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: '#FAB95B',
              opacity: 0.45,
            }}
          />
        );
      })}

      {/* Header */}
      <div
        style={{
          position: 'absolute',
          top: 90,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(sp(5), [0, 1], [0, 1]),
          transform: `translateY(${(1 - sp(5)) * -30}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 30,
            fontWeight: 600,
            color: '#FAB95B',
            letterSpacing: 10,
            textTransform: 'uppercase',
            marginBottom: 14,
          }}
        >
          The Three Pillars
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 92,
            fontWeight: 900,
            color: '#E8EDF2',
            lineHeight: 1,
            letterSpacing: -2,
          }}
        >
          Your Recap In <span style={{ color: '#FAB95B' }}>3 Parts</span>
        </div>
      </div>

      {/* Three cards row */}
      <div
        style={{
          position: 'absolute',
          top: 380,
          left: '50%',
          transform: 'translateX(-50%)',
          display: 'flex',
          gap: 40,
          width: 1700,
          justifyContent: 'center',
        }}
      >
        {/* Card 1 - Restricted Application */}
        <div
          style={{
            width: 500,
            height: 540,
            padding: '50px 40px',
            background: 'rgba(232,237,242,0.04)',
            border: '2px solid rgba(250,185,91,0.4)',
            borderRadius: 20,
            opacity: card(20),
            transform: `translateY(${(1 - card(20)) * 60}px)`,
          }}
        >
          {/* Icon - document */}
          <svg width="80" height="90" viewBox="0 0 80 90" style={{ marginBottom: 24 }}>
            <rect x="10" y="5" width="60" height="80" rx="4" fill="none" stroke="#FAB95B" strokeWidth="3" />
            <line x1="22" y1="25" x2="58" y2="25" stroke="#FAB95B" strokeWidth="3" strokeLinecap="round" />
            <line x1="22" y1="40" x2="58" y2="40" stroke="#FAB95B" strokeWidth="3" strokeLinecap="round" />
            <line x1="22" y1="55" x2="45" y2="55" stroke="#FAB95B" strokeWidth="3" strokeLinecap="round" />
          </svg>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 24,
              fontWeight: 700,
              color: '#FAB95B',
              letterSpacing: 4,
              marginBottom: 12,
            }}
          >
            PART 01
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 44,
              fontWeight: 800,
              color: '#E8EDF2',
              lineHeight: 1.1,
              marginBottom: 20,
            }}
          >
            Restricted Application
          </div>
          <div
            style={{
              width: 80,
              height: 3,
              background: '#FAB95B',
              marginBottom: 20,
            }}
          />
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 500,
              color: '#E8EDF2',
              opacity: 0.75,
              lineHeight: 1.4,
            }}
          >
            File only for the spousal benefit while your own grows.
          </div>
        </div>

        {/* Card 2 - Magic Date (HIGHLIGHTED) */}
        <div
          style={{
            width: 500,
            height: 540,
            padding: '50px 40px',
            background: 'linear-gradient(180deg, rgba(250,185,91,0.18) 0%, rgba(250,185,91,0.08) 100%)',
            border: '3px solid #FAB95B',
            borderRadius: 20,
            opacity: card(35),
            transform: `translateY(${(1 - card(35)) * 60}px)`,
            boxShadow: '0 0 60px rgba(250,185,91,0.3)',
          }}
        >
          {/* Icon - calendar */}
          <svg width="80" height="90" viewBox="0 0 80 90" style={{ marginBottom: 24 }}>
            <rect x="8" y="14" width="64" height="68" rx="4" fill="none" stroke="#FAB95B" strokeWidth="3" />
            <line x1="8" y1="32" x2="72" y2="32" stroke="#FAB95B" strokeWidth="3" />
            <line x1="22" y1="6" x2="22" y2="22" stroke="#FAB95B" strokeWidth="3" strokeLinecap="round" />
            <line x1="58" y1="6" x2="58" y2="22" stroke="#FAB95B" strokeWidth="3" strokeLinecap="round" />
            <circle cx="40" cy="55" r="8" fill="#FAB95B" />
          </svg>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 24,
              fontWeight: 700,
              color: '#FAB95B',
              letterSpacing: 4,
              marginBottom: 12,
            }}
          >
            PART 02
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 44,
              fontWeight: 800,
              color: '#FAB95B',
              lineHeight: 1.1,
              marginBottom: 20,
            }}
          >
            The Magic Date
          </div>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 36,
              fontWeight: 900,
              color: '#E8EDF2',
              marginBottom: 18,
            }}
          >
            Jan 1, 1954
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 500,
              color: '#E8EDF2',
              opacity: 0.85,
              lineHeight: 1.4,
            }}
          >
            The single cutoff that decides your eligibility.
          </div>
        </div>

        {/* Card 3 - Spousal Math */}
        <div
          style={{
            width: 500,
            height: 540,
            padding: '50px 40px',
            background: 'rgba(232,237,242,0.04)',
            border: '2px solid rgba(250,185,91,0.4)',
            borderRadius: 20,
            opacity: card(50),
            transform: `translateY(${(1 - card(50)) * 60}px)`,
          }}
        >
          {/* Icon - chart bars */}
          <svg width="80" height="90" viewBox="0 0 80 90" style={{ marginBottom: 24 }}>
            <line x1="10" y1="80" x2="70" y2="80" stroke="#FAB95B" strokeWidth="3" strokeLinecap="round" />
            <rect x="14" y="55" width="14" height="25" fill="#FAB95B" />
            <rect x="33" y="38" width="14" height="42" fill="#FAB95B" />
            <rect x="52" y="20" width="14" height="60" fill="#FAB95B" />
          </svg>
          <div
            style={{
              fontFamily: 'JetBrains Mono',
              fontSize: 24,
              fontWeight: 700,
              color: '#FAB95B',
              letterSpacing: 4,
              marginBottom: 12,
            }}
          >
            PART 03
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 44,
              fontWeight: 800,
              color: '#E8EDF2',
              lineHeight: 1.1,
              marginBottom: 20,
            }}
          >
            Spousal Math
          </div>
          <div
            style={{
              width: 80,
              height: 3,
              background: '#FAB95B',
              marginBottom: 20,
            }}
          />
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 500,
              color: '#E8EDF2',
              opacity: 0.75,
              lineHeight: 1.4,
            }}
          >
            Collect 50% of spouse's check while yours keeps growing.
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene36_ThreeElements;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37_EightPercentGrowth',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene37_EightPercentGrowth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Line graph progress
  const graphProgress = interpolate(frame, [40, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // 8% counter
  const pctCounter = interpolate(frame, [50, 110], [0, 8], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Camera slight zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1], { extrapolateRight: 'clamp' });

  // Graph data points - exponential growth
  const points = [
    { x: 0, y: 0, label: '66', val: '$2,000' },
    { x: 0.25, y: 0.22, label: '67', val: '$2,160' },
    { x: 0.5, y: 0.46, label: '68', val: '$2,320' },
    { x: 0.75, y: 0.72, label: '69', val: '$2,480' },
    { x: 1, y: 1, label: '70', val: '$2,640' },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(135deg, #412D15 0%, #5A3E1F 60%, #2A1C0D 100%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            top: '10%',
            right: '8%',
            width: 400,
            height: 400,
            borderRadius: '50%',
            border: '2px solid #FAB95B',
            opacity: 0.05,
            transform: `scale(${1 + Math.sin(frame * 0.025) * 0.07})`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '12%',
            left: '8%',
            width: 280,
            height: 280,
            border: '2px solid #E8EDF2',
            opacity: 0.05,
            transform: `rotate(${frame * 0.12}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="g37" x="0" y="0" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#FAB95B" strokeWidth="0.5" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#g37)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const seed = seededRandom(i + 737);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: (seed * 1920) % 1920,
              top: 1080 - ((seededRandom(i + 800) * 1080 + frame * 0.5) % 1080),
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: '#FAB95B',
              opacity: 0.5,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `scale(${camScale})` }}>
        {/* Header */}
        <div
          style={{
            position: 'absolute',
            top: 70,
            left: 100,
            opacity: interpolate(sp(5), [0, 1], [0, 1]),
            transform: `translateX(${(1 - sp(5)) * -40}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 30,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 8,
              textTransform: 'uppercase',
              marginBottom: 14,
            }}
          >
            Delayed Retirement Credits
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 88,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1,
              letterSpacing: -2,
            }}
          >
            Your Own Benefit Grows
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 50,
              fontWeight: 600,
              color: '#FAB95B',
              marginTop: 10,
            }}
          >
            +{pctCounter.toFixed(1)}% Every Year
          </div>
        </div>

        {/* Big 8% on top right */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            right: 120,
            textAlign: 'right',
            opacity: interpolate(sp(40), [0, 1], [0, 1]),
            transform: `scale(${sp(40)})`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 260,
              fontWeight: 900,
              color: '#FAB95B',
              lineHeight: 0.85,
              letterSpacing: -8,
              textShadow: '0 0 50px rgba(250,185,91,0.4)',
            }}
          >
            8%
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 32,
              fontWeight: 700,
              color: '#E8EDF2',
              letterSpacing: 6,
              opacity: 0.8,
            }}
          >
            PER YEAR
          </div>
        </div>

        {/* Line Graph */}
        <div
          style={{
            position: 'absolute',
            bottom: 90,
            left: '50%',
            transform: 'translateX(-50%)',
            width: 1500,
            height: 420,
          }}
        >
          <svg width="1500" height="420" viewBox="0 0 1500 420">
            {/* Y-axis line */}
            <line x1="80" y1="20" x2="80" y2="360" stroke="#E8EDF2" strokeWidth="2" opacity="0.4" />
            {/* X-axis line */}
            <line x1="80" y1="360" x2="1450" y2="360" stroke="#E8EDF2" strokeWidth="2" opacity="0.4" />

            {/* Horizontal grid lines */}
            {[0, 0.25, 0.5, 0.75, 1].map((p, i) => (
              <line
                key={i}
                x1="80"
                y1={360 - p * 320}
                x2="1450"
                y2={360 - p * 320}
                stroke="#E8EDF2"
                strokeWidth="1"
                opacity="0.1"
                strokeDasharray="4 4"
              />
            ))}

            {/* Line path */}
            <path
              d={`M ${points.map((p, i) => `${80 + p.x * 1300} ${360 - p.y * 320}`).join(' L ')}`}
              stroke="#FAB95B"
              strokeWidth="5"
              fill="none"
              strokeLinecap="round"
              strokeLinejoin="round"
              strokeDasharray="2000"
              strokeDashoffset={2000 - 2000 * graphProgress}
            />

            {/* Area fill */}
            <path
              d={`M 80 360 L ${points.map((p) => `${80 + p.x * 1300} ${360 - p.y * 320}`).join(' L ')} L ${80 + 1300} 360 Z`}
              fill="url(#areagrad37)"
              opacity={graphProgress * 0.3}
            />
            <defs>
              <linearGradient id="areagrad37" x1="0" x2="0" y1="0" y2="1">
                <stop offset="0%" stopColor="#FAB95B" stopOpacity="0.6" />
                <stop offset="100%" stopColor="#FAB95B" stopOpacity="0" />
              </linearGradient>
            </defs>

            {/* Data points */}
            {points.map((p, i) => {
              const visible = graphProgress >= p.x;
              const pulse = 1 + Math.sin(frame * 0.15 + i) * 0.1;
              return (
                <g key={i} opacity={visible ? 1 : 0}>
                  <circle
                    cx={80 + p.x * 1300}
                    cy={360 - p.y * 320}
                    r={i === points.length - 1 ? 14 * pulse : 9}
                    fill="#412D15"
                    stroke="#FAB95B"
                    strokeWidth="4"
                  />
                  <text
                    x={80 + p.x * 1300}
                    y="395"
                    fontFamily="Inter"
                    fontSize="26"
                    fontWeight="700"
                    fill="#E8EDF2"
                    textAnchor="middle"
                  >
                    Age {p.label}
                  </text>
                  <text
                    x={80 + p.x * 1300}
                    y={360 - p.y * 320 - 24}
                    fontFamily="JetBrains Mono"
                    fontSize="24"
                    fontWeight="800"
                    fill="#FAB95B"
                    textAnchor="middle"
                  >
                    {p.val}
                  </text>
                </g>
              );
            })}
          </svg>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 55%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene37_EightPercentGrowth;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38_BornAfterDate',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene38_BornAfterDate: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Timeline progress arrow moving forward
  const timelineProgress = interpolate(sp(40), [0, 1], [0, 1]);
  // Strategy reveal
  const strategyReveal = sp(110, { damping: 11, stiffness: 120 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(90deg, #2A1C0D 0%, #412D15 50%, #5A3E1F 100%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            top: '20%',
            left: '5%',
            width: 320,
            height: 320,
            border: '2px solid #FAB95B',
            opacity: 0.05,
            transform: `rotate(${frame * 0.1}deg)`,
            borderRadius: 30,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '15%',
            right: '10%',
            width: 0,
            height: 0,
            borderLeft: '150px solid transparent',
            borderRight: '150px solid transparent',
            borderBottom: '260px solid #E8EDF2',
            opacity: 0.04,
            transform: `rotate(${-frame * 0.08}deg)`,
          }}
        />
      </AbsoluteFill>

      {/* Diagonal texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="d38" x="0" y="0" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M0,60 L60,0" stroke="#FAB95B" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#d38)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = seededRandom(i + 838);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: ((seed * 1920) + frame * 0.7) % 1920,
              top: seededRandom(i + 900) * 1080,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: '#FAB95B',
              opacity: 0.45,
            }}
          />
        );
      })}

      {/* Header */}
      <div
        style={{
          position: 'absolute',
          top: 80,
          left: 0,
          right: 0,
          textAlign: 'center',
          opacity: interpolate(sp(5), [0, 1], [0, 1]),
          transform: `translateY(${(1 - sp(5)) * -30}px)`,
        }}
      >
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 30,
            fontWeight: 600,
            color: '#FAB95B',
            letterSpacing: 10,
            textTransform: 'uppercase',
            marginBottom: 14,
          }}
        >
          For Everyone Else
        </div>
        <div
          style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 86,
            fontWeight: 900,
            color: '#E8EDF2',
            lineHeight: 1.05,
            letterSpacing: -2,
          }}
        >
          Born After Jan 1, 1954?
        </div>
        <div
          style={{
            fontFamily: 'Inter',
            fontSize: 50,
            fontWeight: 700,
            color: '#FAB95B',
            marginTop: 14,
          }}
        >
          Use the Higher Earner Delay Strategy
        </div>
      </div>

      {/* Timeline visualization */}
      <div
        style={{
          position: 'absolute',
          top: 480,
          left: '50%',
          transform: 'translateX(-50%)',
          width: 1500,
          height: 220,
        }}
      >
        <svg width="1500" height="220" viewBox="0 0 1500 220">
          {/* Base line */}
          <line x1="60" y1="110" x2="1440" y2="110" stroke="#E8EDF2" strokeWidth="3" opacity="0.3" />

          {/* Cutoff marker */}
          <line x1="380" y1="40" x2="380" y2="180" stroke="#FAB95B" strokeWidth="3" strokeDasharray="6 6" />
          <text x="380" y="30" fontFamily="JetBrains Mono" fontSize="22" fontWeight="800" fill="#FAB95B" textAnchor="middle">
            JAN 1, 1954
          </text>

          {/* Forbidden zone (before / left side ghost) */}
          <rect x="60" y="92" width="320" height="36" fill="#9E8770" opacity="0.2" />
          <text x="220" y="160" fontFamily="Inter" fontSize="24" fontWeight="600" fill="#9E8770" textAnchor="middle">
            Restricted App Era
          </text>

          {/* New strategy zone */}
          <rect
            x="380"
            y="92"
            width={1060 * timelineProgress}
            height="36"
            fill="#FAB95B"
            opacity="0.85"
          />
          <text x="910" y="160" fontFamily="Inter" fontSize="26" fontWeight="800" fill="#FAB95B" textAnchor="middle">
            Higher Earner Delay Strategy
          </text>

          {/* Moving arrow tip */}
          <polygon
            points={`${380 + 1060 * timelineProgress - 12},92 ${380 + 1060 * timelineProgress + 12},110 ${380 + 1060 * timelineProgress - 12},128`}
            fill="#FAB95B"
          />
        </svg>
      </div>

      {/* Strategy summary box */}
      <div
        style={{
          position: 'absolute',
          bottom: 90,
          left: '50%',
          transform: `translateX(-50%) scale(${strategyReveal})`,
          width: 1200,
          padding: '36px 50px',
          background: 'linear-gradient(135deg, rgba(250,185,91,0.18) 0%, rgba(250,185,91,0.08) 100%)',
          border: '3px solid #FAB95B',
          borderRadius: 16,
          display: 'flex',
          justifyContent: 'space-around',
          alignItems: 'center',
          opacity: strategyReveal,
          boxShadow: '0 0 50px rgba(250,185,91,0.25)',
        }}
      >
        <div style={{ textAlign: 'center' }}>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 24,
              fontWeight: 700,
              color: '#9E8770',
              letterSpacing: 3,
              textTransform: 'uppercase',
              marginBottom: 8,
            }}
          >
            Lower Earner
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 44,
              fontWeight: 900,
              color: '#E8EDF2',
            }}
          >
            Claims Early
          </div>
        </div>

        <div style={{ fontFamily: 'Inter', fontSize: 60, fontWeight: 900, color: '#FAB95B' }}>+</div>

        <div style={{ textAlign: 'center' }}>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 24,
              fontWeight: 700,
              color: '#FAB95B',
              letterSpacing: 3,
              textTransform: 'uppercase',
              marginBottom: 8,
            }}
          >
            Higher Earner
          </div>
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 44,
              fontWeight: 900,
              color: '#FAB95B',
            }}
          >
            Waits Until 70
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.55) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene38_BornAfterDate;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39_UltimateSurvivor',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene39_UltimateSurvivor: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Big number ramp
  const dollarCount = interpolate(frame, [40, 180], [0, 2640], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Camera tilt
  const camRot = interpolate(frame, [0, durationInFrames], [-1, 1], { extrapolateRight: 'clamp' });

  // Ring pulse
  const ringPulse = 1 + Math.sin(frame * 0.1) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(circle at center, #6B4824 0%, #412D15 50%, #1F1408 100%)',
        }}
      />

      {/* Shape Layer - concentric rings */}
      <AbsoluteFill>
        {[0, 1, 2, 3].map((i) => (
          <div
            key={i}
            style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              width: 500 + i * 220,
              height: 500 + i * 220,
              borderRadius: '50%',
              border: `2px solid #FAB95B`,
              opacity: 0.05 - i * 0.005,
              transform: `translate(-50%,-50%) scale(${ringPulse})`,
            }}
          />
        ))}
      </AbsoluteFill>

      {/* Star burst lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {Array.from({ length: 24 }).map((_, i) => {
          const angle = (i / 24) * Math.PI * 2;
          const x2 = 960 + Math.cos(angle) * 1200;
          const y2 = 540 + Math.sin(angle) * 1200;
          return <line key={i} x1="960" y1="540" x2={x2} y2={y2} stroke="#FAB95B" strokeWidth="1" />;
        })}
      </svg>

      {/* Particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = seededRandom(i + 939);
        const angle = seed * Math.PI * 2;
        const radius = 200 + (seededRandom(i + 1000) * 400);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: 960 + Math.cos(angle + frame * 0.005) * radius,
              top: 540 + Math.sin(angle + frame * 0.005) * radius,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: '#FAB95B',
              opacity: 0.5,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `rotate(${camRot * 0.05}deg)` }}>
        {/* Top label */}
        <div
          style={{
            position: 'absolute',
            top: 90,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(5), [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 32,
              fontWeight: 600,
              color: '#FAB95B',
              letterSpacing: 12,
              textTransform: 'uppercase',
              marginBottom: 14,
            }}
          >
            The Ultimate Outcome
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 96,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1,
              letterSpacing: -2,
            }}
          >
            Maximized <span style={{ color: '#FAB95B' }}>Survivor Benefit</span>
          </div>
        </div>

        {/* Center big number with halo */}
        <div
          style={{
            position: 'absolute',
            top: '50%',
            left: '50%',
            transform: 'translate(-50%, -50%)',
            textAlign: 'center',
          }}
        >
          {/* Halo */}
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              transform: `translate(-50%, -50%) scale(${ringPulse})`,
              width: 680,
              height: 680,
              borderRadius: '50%',
              border: '4px solid #FAB95B',
              opacity: 0.4,
              boxShadow: '0 0 80px rgba(250,185,91,0.4) inset',
            }}
          />
          <div
            style={{
              position: 'absolute',
              top: '50%',
              left: '50%',
              transform: `translate(-50%, -50%) scale(${ringPulse * 1.08})`,
              width: 760,
              height: 760,
              borderRadius: '50%',
              border: '2px solid #FAB95B',
              opacity: 0.2,
            }}
          />

          <div style={{ position: 'relative', zIndex: 2 }}>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 30,
                fontWeight: 700,
                color: '#FAB95B',
                letterSpacing: 6,
                textTransform: 'uppercase',
                marginBottom: 8,
              }}
            >
              Lifetime Monthly Check
            </div>
            <div
              style={{
                fontFamily: 'Inter, Poppins',
                fontSize: 230,
                fontWeight: 900,
                color: '#FAB95B',
                lineHeight: 1,
                letterSpacing: -6,
                textShadow: '0 0 60px rgba(250,185,91,0.5)',
              }}
            >
              ${Math.floor(dollarCount).toLocaleString()}
            </div>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 38,
                fontWeight: 600,
                color: '#E8EDF2',
                marginTop: 10,
              }}
            >
              transferred to surviving spouse
            </div>
          </div>
        </div>

        {/* Bottom comparison */}
        <div
          style={{
            position: 'absolute',
            bottom: 100,
            left: '50%',
            transform: 'translateX(-50%)',
            display: 'flex',
            gap: 60,
            alignItems: 'center',
            opacity: interpolate(sp(140), [0, 1], [0, 1]),
          }}
        >
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 600, color: '#9E8770', letterSpacing: 3, marginBottom: 6 }}>
              CLAIMED AT 62
            </div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 42, fontWeight: 800, color: '#E8EDF2', opacity: 0.6, textDecoration: 'line-through' }}>
              $1,500
            </div>
          </div>
          <div style={{ fontFamily: 'Inter', fontSize: 48, fontWeight: 900, color: '#FAB95B' }}>→</div>
          <div style={{ textAlign: 'center' }}>
            <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#FAB95B', letterSpacing: 3, marginBottom: 6 }}>
              DELAYED TO 70
            </div>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 50, fontWeight: 900, color: '#FAB95B' }}>$2,640</div>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 45%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene39_UltimateSurvivor;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40_NotAutomatic',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene40_NotAutomatic: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Warning stamp
  const stampScale = sp(60, { damping: 8, stiffness: 150 });
  // Cross line draw
  const crossProgress = interpolate(sp(100), [0, 1], [0, 1]);

  // Subtle shake on warning
  const shake = frame > 60 && frame < 80 ? Math.sin(frame * 4) * 4 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#412D15', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill
        style={{
          background: 'linear-gradient(180deg, #5A3E1F 0%, #412D15 50%, #2A1C0D 100%)',
        }}
      />

      {/* Shape Layer */}
      <AbsoluteFill>
        <div
          style={{
            position: 'absolute',
            top: '12%',
            left: '8%',
            width: 360,
            height: 360,
            border: '2px solid #FAB95B',
            opacity: 0.06,
            transform: `rotate(${45 + frame * 0.1}deg)`,
          }}
        />
        <div
          style={{
            position: 'absolute',
            bottom: '15%',
            right: '8%',
            width: 320,
            height: 320,
            borderRadius: '50%',
            border: '2px solid #E8EDF2',
            opacity: 0.05,
            transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
          }}
        />
      </AbsoluteFill>

      {/* Warning stripe texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="warn40" x="0" y="0" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M0,50 L50,0" stroke="#FAB95B" strokeWidth="2" />
          </pattern>
        </defs>
        <rect width="1920" height="1080" fill="url(#warn40)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const seed = seededRandom(i + 1040);
        return (
          <div
            key={i}
            style={{
              position: 'absolute',
              left: (seed * 1920 + frame * 0.4) % 1920,
              top: (seededRandom(i + 1100) * 1080 + frame * 0.6) % 1080,
              width: 3,
              height: 3,
              borderRadius: '50%',
              background: '#FAB95B',
              opacity: 0.5,
            }}
          />
        );
      })}

      <AbsoluteFill style={{ transform: `translateX(${shake}px)` }}>
        {/* Warning label */}
        <div
          style={{
            position: 'absolute',
            top: 110,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(5), [0, 1], [0, 1]),
            transform: `translateY(${(1 - sp(5)) * -30}px)`,
          }}
        >
          <div
            style={{
              display: 'inline-flex',
              alignItems: 'center',
              gap: 18,
              padding: '14px 32px',
              border: '2px solid #FAB95B',
              borderRadius: 50,
            }}
          >
            <svg width="34" height="34" viewBox="0 0 34 34">
              <polygon points="17,3 32,30 2,30" fill="none" stroke="#FAB95B" strokeWidth="3" strokeLinejoin="round" />
              <line x1="17" y1="13" x2="17" y2="22" stroke="#FAB95B" strokeWidth="3" strokeLinecap="round" />
              <circle cx="17" cy="26" r="1.8" fill="#FAB95B" />
            </svg>
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 28,
                fontWeight: 700,
                color: '#FAB95B',
                letterSpacing: 6,
                textTransform: 'uppercase',
              }}
            >
              Critical Warning
            </div>
          </div>
        </div>

        {/* Main headline */}
        <div
          style={{
            position: 'absolute',
            top: 260,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(20), [0, 1], [0, 1]),
            transform: `translateY(${(1 - sp(20)) * 30}px)`,
          }}
        >
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 140,
              fontWeight: 900,
              color: '#E8EDF2',
              lineHeight: 1,
              letterSpacing: -3,
            }}
          >
            NONE Of This
          </div>
          <div
            style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 140,
              fontWeight: 900,
              color: '#FAB95B',
              lineHeight: 1,
              letterSpacing: -3,
              marginTop: 4,
              textShadow: '0 0 40px rgba(250,185,91,0.4)',
            }}
          >
            Happens Automatically
          </div>
        </div>

        {/* The "AUTOMATIC" crossed out badge */}
        <div
          style={{
            position: 'absolute',
            bottom: 230,
            left: '50%',
            transform: `translateX(-50%) scale(${stampScale}) rotate(-4deg)`,
            opacity: stampScale,
          }}
        >
          <div
            style={{
              position: 'relative',
              padding: '24px 60px',
              border: '4px solid #E8EDF2',
              borderRadius: 8,
              background: 'rgba(46,28,13,0.6)',
            }}
          >
            <div
              style={{
                fontFamily: 'Inter',
                fontSize: 64,
                fontWeight: 900,
                color: '#E8EDF2',
                letterSpacing: 6,
              }}
            >
              AUTO-OPTIMIZED
            </div>
            {/* Strike-through line */}
            <svg
              width="100%"
              height="100%"
              style={{
                position: 'absolute',
                top: 0,
                left: 0,
                pointerEvents: 'none',
              }}
              preserveAspectRatio="none"
              viewBox="0 0 100 100"
            >
              <line
                x1="2"
                y1="50"
                x2={2 + 96 * crossProgress}
                y2="50"
                stroke="#FAB95B"
                strokeWidth="6"
                strokeLinecap="round"
              />
            </svg>
          </div>
        </div>

        {/* Bottom message */}
        <div
          style={{
            position: 'absolute',
            bottom: 110,
            left: 0,
            right: 0,
            textAlign: 'center',
            opacity: interpolate(sp(140), [0, 1], [0, 1]),
          }}
        >
          <div
            style={{
              fontFamily: 'Inter',
              fontSize: 38,
              fontWeight: 600,
              color: '#E8EDF2',
              opacity: 0.85,
              letterSpacing: 1,
            }}
          >
            You must take action. <span style={{ color: '#FAB95B', fontWeight: 800 }}>Knowledge is the only edge.</span>
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill
        style={{
          background: 'radial-gradient(ellipse at center, transparent 50%, rgba(0,0,0,0.6) 100%)',
          pointerEvents: 'none',
        }}
      />

      {/* Watermark */}
      <div
        style={{
          position: 'absolute',
          bottom: 30,
          right: 45,
          fontFamily: 'Inter',
          fontSize: 22,
          fontWeight: 600,
          color: '#9E9E9E',
          opacity: 0.3,
        }}
      >
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene40_NotAutomatic;
```

