```tsx
import React from 'react';
Audio: "What if I told you that the tax return you're going to file in early 2027 could look completely different from anything you've ever filed before, and most Americans have absolutely no clue what's coming?"

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene01_HookReveal',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene01_HookReveal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const whatIfOp = sp(8);
  const ifToldOp = sp(28);
  const taxReturnOp = sp(60);
  const year2027Scale = sp(120);
  const differentOp = sp(180);
  const noClueOp = sp(260);
  const stampScale = sp(340, { damping: 8, stiffness: 180 });

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.08]);
  const camPanX = Math.sin(frame * 0.008) * 12;

  return (
    <AbsoluteFill style={{ backgroundColor: '#CBE4DE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient base */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 30% 20%, rgba(70,138,154,0.18) 0%, rgba(203,228,222,0) 55%), radial-gradient(ellipse at 75% 80%, rgba(44,51,51,0.10) 0%, rgba(203,228,222,0) 60%)',
      }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', left: '8%', top: '12%', width: 520, height: 520,
        border: '2px solid #468A9A', borderRadius: '50%', opacity: 0.06,
        transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.04})`,
      }} />
      <div style={{
        position: 'absolute', right: '6%', top: '20%', width: 380, height: 380,
        backgroundColor: '#2C3333', opacity: 0.04,
        transform: `rotate(${45 + frame * 0.08}deg)`,
      }} />
      <div style={{
        position: 'absolute', left: '60%', bottom: '8%', width: 0, height: 0,
        borderLeft: '180px solid transparent', borderRight: '180px solid transparent',
        borderBottom: '300px solid #468A9A', opacity: 0.05,
        transform: `rotate(${-frame * 0.1}deg)`,
      }} />

      {/* Grid Texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid1" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#2C3333" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid1)" />
      </svg>

      {/* Floating Particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const r = seededRandom(i + 1);
        const r2 = seededRandom(i + 50);
        const px = r * 1920;
        const py = r2 * 1080 + Math.sin(frame * 0.02 + i) * 15;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 4 + r * 6, height: 4 + r * 6,
            backgroundColor: '#468A9A', borderRadius: '50%',
            opacity: 0.25 + r * 0.3,
          }} />
        );
      })}

      {/* Main Camera Wrapper */}
      <div style={{
        position: 'absolute', width: '100%', height: '100%',
        transform: `scale(${camZoom}) translateX(${camPanX}px)`,
      }}>
        {/* Top Tag */}
        <div style={{
          position: 'absolute', top: 80, left: '50%', transform: 'translateX(-50%)',
          display: 'flex', alignItems: 'center', gap: 14, opacity: whatIfOp,
        }}>
          <div style={{ width: 50, height: 3, backgroundColor: '#468A9A' }} />
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 28, color: '#468A9A',
            letterSpacing: 6, fontWeight: 700,
          }}>TAX ALERT // 2027</div>
          <div style={{ width: 50, height: 3, backgroundColor: '#468A9A' }} />
        </div>

        {/* WHAT IF */}
        <div style={{
          position: 'absolute', top: 180, left: '50%',
          transform: `translateX(-50%) translateY(${(1 - whatIfOp) * 30}px)`,
          opacity: whatIfOp,
          fontFamily: 'Mont Heavy', fontSize: 119, color: '#2C3333',
          fontWeight: 900, letterSpacing: -3,
        }}>
          WHAT IF...
        </div>

        {/* Main Statement Box */}
        <div style={{
          position: 'absolute', top: 340, left: '50%',
          transform: `translateX(-50%) scale(${0.9 + ifToldOp * 0.1})`,
          opacity: ifToldOp,
          width: 1500, padding: '32px 50px',
          backgroundColor: 'rgba(44,51,51,0.04)',
          borderLeft: '6px solid #468A9A',
          fontFamily: 'Inter', fontSize: 47, color: '#2C3333',
          fontWeight: 500, lineHeight: 1.3,
        }}>
          Your <span style={{ fontWeight: 800, color: '#468A9A' }}>tax return</span> in early
        </div>

        {/* Big 2027 Number */}
        <div style={{
          position: 'absolute', top: 470, left: '50%',
          transform: `translateX(-50%) scale(${year2027Scale})`,
          opacity: Math.min(1, year2027Scale),
        }}>
          <div style={{
            fontFamily: 'Nexa Bold', fontSize: 221, color: '#2C3333',
            fontWeight: 900, letterSpacing: -8, lineHeight: 1,
            position: 'relative',
          }}>
            2027
            <div style={{
              position: 'absolute', bottom: -8, left: 0, right: 0, height: 12,
              backgroundColor: '#468A9A', opacity: 0.4,
              transform: `scaleX(${interpolate(frame, [150, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' })})`,
              transformOrigin: 'left',
            }} />
          </div>
        </div>

        {/* DIFFERENT label */}
        <div style={{
          position: 'absolute', top: 760, left: '50%',
          transform: `translateX(-50%) translateY(${(1 - differentOp) * 20}px)`,
          opacity: differentOp,
          display: 'flex', alignItems: 'center', gap: 24,
        }}>
          <svg width="60" height="60" viewBox="0 0 60 60">
            <path d="M10 30 L25 30 M35 30 L50 30 M30 10 L30 25 M30 35 L30 50"
              stroke="#468A9A" strokeWidth="4" strokeLinecap="round" />
            <circle cx="30" cy="30" r="6" fill="#2C3333" />
          </svg>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 76, color: '#2C3333',
            fontWeight: 900, letterSpacing: -1,
          }}>
            COMPLETELY <span style={{ color: '#468A9A' }}>DIFFERENT</span>
          </div>
        </div>

        {/* Bottom Stat: No Clue */}
        <div style={{
          position: 'absolute', bottom: 110, left: '50%',
          transform: `translateX(-50%) scale(${stampScale})`,
          opacity: noClueOp,
          display: 'flex', alignItems: 'center', gap: 30,
          padding: '20px 50px',
          backgroundColor: '#2C3333',
          border: '3px solid #468A9A',
        }}>
          <svg width="50" height="50" viewBox="0 0 50 50">
            <circle cx="25" cy="25" r="22" fill="none" stroke="#CBE4DE" strokeWidth="3" />
            <path d="M18 20 Q25 14 32 20 Q32 26 25 28 L25 33" stroke="#CBE4DE" strokeWidth="3" fill="none" strokeLinecap="round" />
            <circle cx="25" cy="40" r="2" fill="#CBE4DE" />
          </svg>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 51, color: '#CBE4DE',
            fontWeight: 800, letterSpacing: 2,
          }}>
            MOST AMERICANS HAVE <span style={{ color: '#468A9A' }}>NO CLUE</span>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(44,51,51,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#2C3333', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene01_HookReveal;
```

```tsx
import React from 'react';
Audio: "I'm Kunal, behind Failed Finance, and I've spent weeks buried in the fine print of the One Big Beautiful Bill Act, signed into law on July 4th of 2025."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene02_BillReveal',
  durationInSeconds: 27,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene02_BillReveal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const nameOp = sp(10);
  const fineprintOp = sp(70);
  const billRevealScale = sp(140, { damping: 10, stiffness: 100 });
  const stampOp = sp(240, { damping: 8, stiffness: 200 });
  const dateOp = sp(320);
  const camTilt = Math.sin(frame * 0.01) * 1.2;

  return (
    <AbsoluteFill style={{ backgroundColor: '#CBE4DE', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(70,138,154,0.12) 0%, rgba(203,228,222,0) 60%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute', left: '-5%', bottom: '-10%', width: 700, height: 700,
        border: '2px solid #2C3333', borderRadius: '50%', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.03) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', right: '-8%', top: '-5%', width: 500, height: 500,
        backgroundColor: '#468A9A', opacity: 0.06,
        transform: `rotate(${30 + frame * 0.1}deg)`,
        borderRadius: 40,
      }} />

      {/* Diagonal lines texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="diag1" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M0 40 L40 0" stroke="#2C3333" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diag1)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 10);
        const r2 = seededRandom(i + 200);
        const px = r * 1920;
        const py = (r2 * 1080 + frame * 0.5) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 3 + r * 5, height: 3 + r * 5,
            backgroundColor: '#2C3333', borderRadius: '50%',
            opacity: 0.2 + r * 0.2,
          }} />
        );
      })}

      <div style={{ position: 'absolute', width: '100%', height: '100%', transform: `rotate(${camTilt}deg)` }}>
        {/* Top name banner */}
        <div style={{
          position: 'absolute', top: 70, left: 80,
          opacity: nameOp,
          transform: `translateX(${(1 - nameOp) * -40}px)`,
        }}>
          <div style={{
            display: 'flex', alignItems: 'center', gap: 18,
            padding: '14px 28px',
            backgroundColor: '#2C3333',
          }}>
            <div style={{ width: 14, height: 14, backgroundColor: '#468A9A', borderRadius: '50%' }} />
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 26, color: '#CBE4DE',
              fontWeight: 700, letterSpacing: 4,
            }}>KUNAL // FAILED FINANCE</div>
          </div>
        </div>

        {/* Fine Print label */}
        <div style={{
          position: 'absolute', top: 200, left: 80,
          opacity: fineprintOp,
          transform: `translateY(${(1 - fineprintOp) * 20}px)`,
        }}>
          <div style={{
            fontFamily: 'Inter', fontSize: 38, color: '#468A9A',
            fontWeight: 600, letterSpacing: 3,
          }}>WEEKS BURIED IN THE FINE PRINT OF...</div>
        </div>

        {/* Massive Bill Title */}
        <div style={{
          position: 'absolute', top: 290, left: 80, right: 80,
          transform: `scale(${billRevealScale}) translateY(${(1 - billRevealScale) * 40}px)`,
          opacity: Math.min(1, billRevealScale),
          transformOrigin: 'left center',
        }}>
          <div style={{
            fontFamily: 'Nexa Bold', fontSize: 187, color: '#2C3333',
            fontWeight: 900, letterSpacing: -6, lineHeight: 0.95,
          }}>
            ONE BIG
          </div>
          <div style={{
            fontFamily: 'Nexa Bold', fontSize: 187,
            fontWeight: 900, letterSpacing: -6, lineHeight: 0.95,
            color: '#468A9A',
          }}>
            BEAUTIFUL
          </div>
          <div style={{
            fontFamily: 'Nexa Bold', fontSize: 187, color: '#2C3333',
            fontWeight: 900, letterSpacing: -6, lineHeight: 0.95,
            display: 'flex', alignItems: 'center', gap: 30,
          }}>
            BILL ACT
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 60, color: '#468A9A',
              fontWeight: 700, paddingTop: 80,
            }}>[OBBB]</div>
          </div>
        </div>

        {/* Signed Stamp */}
        <div style={{
          position: 'absolute', bottom: 130, right: 100,
          transform: `scale(${stampOp}) rotate(${-8 + (1 - stampOp) * 30}deg)`,
          opacity: stampOp,
        }}>
          <div style={{
            border: '6px solid #468A9A',
            padding: '20px 36px',
            backgroundColor: 'rgba(203,228,222,0.6)',
          }}>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 51, color: '#468A9A',
              fontWeight: 900, letterSpacing: 4, lineHeight: 1,
            }}>SIGNED INTO LAW</div>
          </div>
        </div>

        {/* Date Banner */}
        <div style={{
          position: 'absolute', bottom: 60, left: 80,
          opacity: dateOp,
          display: 'flex', alignItems: 'center', gap: 24,
        }}>
          <svg width="70" height="70" viewBox="0 0 70 70">
            <rect x="8" y="14" width="54" height="50" rx="3" fill="none" stroke="#2C3333" strokeWidth="3.5" />
            <line x1="8" y1="26" x2="62" y2="26" stroke="#2C3333" strokeWidth="3.5" />
            <line x1="20" y1="8" x2="20" y2="20" stroke="#2C3333" strokeWidth="3.5" strokeLinecap="round" />
            <line x1="50" y1="8" x2="50" y2="20" stroke="#2C3333" strokeWidth="3.5" strokeLinecap="round" />
            <text x="35" y="52" fontFamily="Mont Heavy" fontSize="20" fill="#468A9A" textAnchor="middle" fontWeight="900">4</text>
          </svg>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 60, color: '#2C3333',
            fontWeight: 900, letterSpacing: -1,
          }}>
            JULY 4 <span style={{ color: '#468A9A' }}>// 2025</span>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(44,51,51,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#2C3333', opacity: 0.3,
      }}>
        Failed Finances
      </div>
    </AbsoluteFill>
  );
};

export default Scene02_BillReveal;
```

```tsx
import React from 'react';
Audio: "If you donate to charity, if you're over 50 catching up on retirement, or if you're a high earner who thought your tax strategy was locked in — stop scrolling and pay attention."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene03_ThreeTargets',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene03_ThreeTargets: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headOp = sp(10);
  const card1 = sp(50);
  const card2 = sp(110);
  const card3 = sp(170);
  const stopOp = sp(260, { damping: 8, stiffness: 160 });
  const shakeAmt = frame > 270 && frame < 300 ? Math.sin(frame * 1.5) * 6 : 0;

  const cards = [
    {
      icon: 'heart', label: 'CHARITY GIVERS',
      sub: 'You donate to charity',
      anim: card1, x: 100,
    },
    {
      icon: 'clock', label: 'OVER 50',
      sub: 'Catching up on retirement',
      anim: card2, x: 660,
    },
    {
      icon: 'crown', label: 'HIGH EARNERS',
      sub: 'Strategy "locked in"',
      anim: card3, x: 1220,
    },
  ];

  return (
    <AbsoluteFill style={{ backgroundColor: '#CBE4DE', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 0%, rgba(70,138,154,0.15), rgba(203,228,222,0) 60%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute', left: '50%', top: '50%',
        transform: `translate(-50%, -50%) rotate(${frame * 0.1}deg)`,
        width: 900, height: 900,
        border: '2px solid #468A9A', borderRadius: '50%', opacity: 0.04,
      }} />
      <div style={{
        position: 'absolute', left: '50%', top: '50%',
        transform: `translate(-50%, -50%) rotate(${-frame * 0.15}deg)`,
        width: 600, height: 600,
        border: '2px solid #2C3333', opacity: 0.05,
      }} />

      {/* Dots texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dots3" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="1.5" fill="#2C3333" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots3)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 33);
        const px = r * 1920;
        const py = (seededRandom(i + 99) * 1080 + Math.sin(frame * 0.03 + i) * 20) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 4, height: 4, backgroundColor: '#468A9A',
            borderRadius: '50%', opacity: 0.3,
          }} />
        );
      })}

      <div style={{ transform: `translateX(${shakeAmt}px)`, width: '100%', height: '100%' }}>
        {/* Header */}
        <div style={{
          position: 'absolute', top: 80, left: '50%', transform: `translateX(-50%) translateY(${(1 - headOp) * -20}px)`,
          opacity: headOp, textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 26, color: '#468A9A',
            letterSpacing: 6, fontWeight: 700, marginBottom: 14,
          }}>// THIS IS FOR YOU IF</div>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 102, color: '#2C3333',
            fontWeight: 900, letterSpacing: -3, lineHeight: 1,
          }}>
            ARE YOU <span style={{ color: '#468A9A' }}>ONE</span> OF THESE?
          </div>
        </div>

        {/* 3 Cards */}
        {cards.map((c, i) => (
          <div key={i} style={{
            position: 'absolute', top: 340, left: c.x,
            width: 600, height: 540,
            transform: `translateY(${(1 - c.anim) * 60}px) scale(${c.anim})`,
            opacity: c.anim,
            backgroundColor: '#2C3333',
            border: '4px solid #468A9A',
            padding: 40,
            display: 'flex', flexDirection: 'column', justifyContent: 'space-between',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 32, color: '#468A9A',
              fontWeight: 700, letterSpacing: 4,
            }}>0{i + 1}</div>

            <div style={{ display: 'flex', justifyContent: 'center', alignItems: 'center', flex: 1 }}>
              <svg width="180" height="180" viewBox="0 0 100 100">
                {c.icon === 'heart' && (
                  <path d="M50 85 C20 65 10 45 20 30 C28 18 42 22 50 35 C58 22 72 18 80 30 C90 45 80 65 50 85 Z"
                    fill="#468A9A" />
                )}
                {c.icon === 'clock' && (
                  <>
                    <circle cx="50" cy="52" r="36" fill="none" stroke="#468A9A" strokeWidth="6" />
                    <line x1="50" y1="52" x2="50" y2="30" stroke="#468A9A" strokeWidth="6" strokeLinecap="round" />
                    <line x1="50" y1="52" x2="65" y2="60" stroke="#468A9A" strokeWidth="6" strokeLinecap="round" />
                    <text x="50" y="14" fontFamily="Mont Heavy" fontSize="16" fill="#CBE4DE" textAnchor="middle" fontWeight="900">50+</text>
                  </>
                )}
                {c.icon === 'crown' && (
                  <>
                    <path d="M20 70 L25 35 L40 55 L50 25 L60 55 L75 35 L80 70 Z"
                      fill="#468A9A" />
                    <rect x="18" y="72" width="64" height="10" fill="#468A9A" />
                  </>
                )}
              </svg>
            </div>

            <div>
              <div style={{
                fontFamily: 'Mont Heavy', fontSize: 47, color: '#CBE4DE',
                fontWeight: 900, letterSpacing: -1, marginBottom: 10,
              }}>{c.label}</div>
              <div style={{
                fontFamily: 'Inter', fontSize: 30, color: '#468A9A',
                fontWeight: 500,
              }}>{c.sub}</div>
            </div>
          </div>
        ))}

        {/* Bottom Stop Banner */}
        <div style={{
          position: 'absolute', bottom: 50, left: '50%',
          transform: `translateX(-50%) scale(${stopOp})`,
          opacity: stopOp,
          padding: '20px 60px',
          backgroundColor: '#468A9A',
          display: 'flex', alignItems: 'center', gap: 26,
        }}>
          <svg width="50" height="50" viewBox="0 0 50 50">
            <polygon points="25,3 47,15 47,35 25,47 3,35 3,15" fill="#2C3333" stroke="#CBE4DE" strokeWidth="2" />
            <text x="25" y="32" fontFamily="Mont Heavy" fontSize="16" fill="#CBE4DE" textAnchor="middle" fontWeight="900">STOP</text>
          </svg>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 64, color: '#CBE4DE',
            fontWeight: 900, letterSpacing: 2,
          }}>STOP SCROLLING. PAY ATTENTION.</div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(44,51,51,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#2C3333', opacity: 0.3,
      }}>Failed Finances</div>
    </AbsoluteFill>
  );
};

export default Scene03_ThreeTargets;
```

```tsx
import React from 'react';
Audio: "The 2026 tax year is bringing changes that could either save you thousands or cost you thousands, depending on whether you understand the new rules or not."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene04_SaveOrCost',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene04_SaveOrCost: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const yearOp = sp(8);
  const splitOp = sp(70);
  const saveOp = sp(130);
  const costOp = sp(200);
  const arrowSwingL = sp(260);
  const arrowSwingR = sp(290);
  const bottomOp = sp(360);

  const saveCount = Math.round(interpolate(frame, [130, 250], [0, 5000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));
  const costCount = Math.round(interpolate(frame, [200, 320], [0, 5000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  return (
    <AbsoluteFill style={{ backgroundColor: '#CBE4DE', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(70,138,154,0.10) 0%, rgba(203,228,222,0) 50%, rgba(44,51,51,0.08) 100%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute', left: '10%', top: '30%', width: 400, height: 400,
        backgroundColor: '#468A9A', opacity: 0.05, borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.04) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute', right: '10%', top: '30%', width: 400, height: 400,
        backgroundColor: '#2C3333', opacity: 0.05, borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.04 + Math.PI) * 0.06})`,
      }} />

      {/* Center line texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="lin4" width="60" height="60" patternUnits="userSpaceOnUse">
            <line x1="0" y1="30" x2="60" y2="30" stroke="#2C3333" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#lin4)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 4);
        const px = r * 1920;
        const py = (seededRandom(i + 77) * 1080 + frame * 0.4) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 3, height: 3, backgroundColor: '#468A9A',
            borderRadius: '50%', opacity: 0.3,
          }} />
        );
      })}

      {/* Top: 2026 */}
      <div style={{
        position: 'absolute', top: 60, left: '50%',
        transform: `translateX(-50%) scale(${yearOp})`,
        opacity: yearOp, textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 26, color: '#468A9A',
          letterSpacing: 6, fontWeight: 700,
        }}>TAX YEAR</div>
        <div style={{
          fontFamily: 'Nexa Bold', fontSize: 153, color: '#2C3333',
          fontWeight: 900, letterSpacing: -5, lineHeight: 1,
        }}>2026</div>
      </div>

      {/* Center Divider Line */}
      <div style={{
        position: 'absolute', left: '50%', top: 320, width: 4, height: 600,
        backgroundColor: '#2C3333', opacity: splitOp * 0.4,
        transform: 'translateX(-50%)',
      }} />

      {/* LEFT: SAVE */}
      <div style={{
        position: 'absolute', left: 100, top: 380, width: 720,
        transform: `translateX(${(1 - saveOp) * -80}px)`,
        opacity: saveOp,
      }}>
        <div style={{
          display: 'flex', alignItems: 'center', gap: 18, marginBottom: 24,
        }}>
          <svg width="80" height="80" viewBox="0 0 80 80" style={{ transform: `rotate(${(1 - arrowSwingL) * -30}deg)` }}>
            <path d="M40 12 L40 60 M22 42 L40 60 L58 42" stroke="#468A9A" strokeWidth="8" strokeLinecap="round" strokeLinejoin="round" fill="none" />
          </svg>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 119, color: '#468A9A',
            fontWeight: 900, letterSpacing: -3,
          }}>SAVE</div>
        </div>
        <div style={{
          fontFamily: 'Nexa Bold', fontSize: 136, color: '#2C3333',
          fontWeight: 900, letterSpacing: -4, lineHeight: 1,
        }}>
          ${saveCount.toLocaleString()}+
        </div>
        <div style={{
          marginTop: 18, padding: '14px 22px',
          backgroundColor: 'rgba(70,138,154,0.18)',
          fontFamily: 'Inter', fontSize: 32, color: '#2C3333',
          fontWeight: 600, display: 'inline-block',
        }}>
          IF YOU UNDERSTAND THE RULES
        </div>
      </div>

      {/* RIGHT: COST */}
      <div style={{
        position: 'absolute', right: 100, top: 380, width: 720,
        transform: `translateX(${(1 - costOp) * 80}px)`,
        opacity: costOp, textAlign: 'right',
      }}>
        <div style={{
          display: 'flex', alignItems: 'center', gap: 18, marginBottom: 24,
          justifyContent: 'flex-end',
        }}>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 119, color: '#2C3333',
            fontWeight: 900, letterSpacing: -3,
          }}>COST</div>
          <svg width="80" height="80" viewBox="0 0 80 80" style={{ transform: `rotate(${(1 - arrowSwingR) * 30}deg)` }}>
            <path d="M40 60 L40 12 M22 30 L40 12 L58 30" stroke="#2C3333" strokeWidth="8" strokeLinecap="round" strokeLinejoin="round" fill="none" />
          </svg>
        </div>
        <div style={{
          fontFamily: 'Nexa Bold', fontSize: 136, color: '#468A9A',
          fontWeight: 900, letterSpacing: -4, lineHeight: 1,
        }}>
          -${costCount.toLocaleString()}
        </div>
        <div style={{
          marginTop: 18, padding: '14px 22px',
          backgroundColor: '#2C3333',
          fontFamily: 'Inter', fontSize: 32, color: '#CBE4DE',
          fontWeight: 600, display: 'inline-block',
        }}>
          IF YOU IGNORE THEM
        </div>
      </div>

      {/* Bottom: The Choice */}
      <div style={{
        position: 'absolute', bottom: 60, left: '50%',
        transform: `translateX(-50%) translateY(${(1 - bottomOp) * 30}px)`,
        opacity: bottomOp, textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Mont Heavy', fontSize: 47, color: '#2C3333',
          fontWeight: 900, letterSpacing: 1,
        }}>
          THE DIFFERENCE? <span style={{ color: '#468A9A' }}>KNOWING THE RULES.</span>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(44,51,51,0.30) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#2C3333', opacity: 0.3,
      }}>Failed Finances</div>
    </AbsoluteFill>
  );
};

export default Scene04_SaveOrCost;
```

```tsx
import React from 'react';
Audio: "This isn't the recycled stuff you've been seeing on every other channel. This is the actual mechanics — the new charity floor, 401k catch-up taxed up front, deduction hacks the wealthy use."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene05_Mechanics',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene05_Mechanics: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const xMark = sp(10);
  const recycledFade = interpolate(frame, [20, 80], [1, 0.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const actualOp = sp(90);
  const item1 = sp(160);
  const item2 = sp(220);
  const item3 = sp(280);

  return (
    <AbsoluteFill style={{ backgroundColor: '#CBE4DE', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 20% 50%, rgba(70,138,154,0.12), rgba(203,228,222,0) 60%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute', left: '70%', top: '5%', width: 600, height: 600,
        border: '3px solid #2C3333', opacity: 0.05, borderRadius: 50,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />
      <div style={{
        position: 'absolute', left: '5%', bottom: '5%', width: 350, height: 350,
        backgroundColor: '#468A9A', opacity: 0.06, borderRadius: '50%',
      }} />

      {/* Crosshatch */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="cross5" width="30" height="30" patternUnits="userSpaceOnUse">
            <path d="M0 30 L30 0 M15 30 L30 15" stroke="#2C3333" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#cross5)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 11);
        const px = r * 1920;
        const py = (seededRandom(i + 88) * 1080) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.03 + i) * 12,
            width: 4, height: 4, backgroundColor: '#468A9A',
            borderRadius: '50%', opacity: 0.3,
          }} />
        );
      })}

      {/* Top: Recycled (crossed out) */}
      <div style={{
        position: 'absolute', top: 90, left: 100, right: 100,
        opacity: recycledFade,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 26, color: '#2C3333',
          letterSpacing: 4, fontWeight: 700, opacity: 0.6, marginBottom: 12,
        }}>NOT ANOTHER...</div>
        <div style={{
          fontFamily: 'Nexa Bold', fontSize: 110, color: '#2C3333',
          fontWeight: 900, letterSpacing: -3, position: 'relative', display: 'inline-block',
        }}>
          RECYCLED TAX VIDEO
          <div style={{
            position: 'absolute', left: -10, right: -10, top: '50%', height: 8,
            backgroundColor: '#468A9A',
            transform: `scaleX(${xMark}) translateY(-50%) rotate(-2deg)`,
            transformOrigin: 'left',
          }} />
        </div>
      </div>

      {/* Header: Actual Mechanics */}
      <div style={{
        position: 'absolute', top: 320, left: 100,
        transform: `translateY(${(1 - actualOp) * 30}px)`,
        opacity: actualOp,
      }}>
        <div style={{
          display: 'inline-block', padding: '10px 22px',
          backgroundColor: '#2C3333',
          fontFamily: 'JetBrains Mono', fontSize: 28, color: '#CBE4DE',
          fontWeight: 700, letterSpacing: 4, marginBottom: 18,
        }}>// THE ACTUAL MECHANICS</div>
        <div style={{
          fontFamily: 'Mont Heavy', fontSize: 119, color: '#2C3333',
          fontWeight: 900, letterSpacing: -3, lineHeight: 1,
        }}>
          WHAT THEY <span style={{ color: '#468A9A' }}>WON'T</span> TELL YOU
        </div>
      </div>

      {/* 3 Mechanic Rows */}
      {[
        { num: '01', label: 'NEW CHARITY FLOOR', sub: '0.5% AGI threshold', anim: item1 },
        { num: '02', label: '401(K) CATCH-UP TAXED UP-FRONT', sub: 'Roth forced for high earners', anim: item2 },
        { num: '03', label: 'DEDUCTION HACKS', sub: 'What the wealthy quietly use', anim: item3 },
      ].map((row, i) => (
        <div key={i} style={{
          position: 'absolute', top: 580 + i * 130, left: 100, right: 100,
          transform: `translateX(${(1 - row.anim) * -60}px)`,
          opacity: row.anim,
          display: 'flex', alignItems: 'center', gap: 32,
          padding: '20px 30px',
          backgroundColor: 'rgba(44,51,51,0.06)',
          borderLeft: '6px solid #468A9A',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 60, color: '#468A9A',
            fontWeight: 800, minWidth: 100,
          }}>{row.num}</div>
          <div style={{ flex: 1 }}>
            <div style={{
              fontFamily: 'Mont Heavy', fontSize: 60, color: '#2C3333',
              fontWeight: 900, letterSpacing: -1, lineHeight: 1,
            }}>{row.label}</div>
            <div style={{
              fontFamily: 'Inter', fontSize: 30, color: '#468A9A',
              fontWeight: 600, marginTop: 6,
            }}>{row.sub}</div>
          </div>
          <svg width="50" height="50" viewBox="0 0 50 50">
            <circle cx="25" cy="25" r="22" fill="#2C3333" />
            <path d="M16 25 L23 32 L36 18" stroke="#CBE4DE" strokeWidth="4" fill="none" strokeLinecap="round" strokeLinejoin="round" />
          </svg>
        </div>
      ))}

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(44,51,51,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#2C3333', opacity: 0.3,
      }}>Failed Finances</div>
    </AbsoluteFill>
  );
};

export default Scene05_Mechanics;
```

```tsx
import React from 'react';
Audio: "I'm not your CPA. I'm not your financial advisor. I'm a guy who got burned badly by my own financial ignorance and made it my mission to break this stuff down in plain English."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene06_NotYourCPA',
  durationInSeconds: 27,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene06_NotYourCPA: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const tag1 = sp(15);
  const cross1 = sp(50);
  const tag2 = sp(80);
  const cross2 = sp(115);
  const burnedOp = sp(180);
  const missionOp = sp(280);

  return (
    <AbsoluteFill style={{ backgroundColor: '#CBE4DE', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(160deg, rgba(70,138,154,0.10), rgba(203,228,222,0) 60%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute', right: '-10%', top: '20%', width: 700, height: 700,
        border: '2px solid #468A9A', opacity: 0.05, borderRadius: '50%',
        transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', left: '-5%', bottom: '-10%', width: 500, height: 500,
        backgroundColor: '#2C3333', opacity: 0.04,
        transform: `rotate(${frame * 0.08}deg)`,
        borderRadius: 60,
      }} />

      {/* Horizontal lines texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="hline6" width="100" height="100" patternUnits="userSpaceOnUse">
            <line x1="0" y1="50" x2="100" y2="50" stroke="#2C3333" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hline6)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 60);
        const px = r * 1920;
        const py = (seededRandom(i + 200) * 1080 + Math.sin(frame * 0.02 + i) * 15) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 4, height: 4, backgroundColor: '#468A9A',
            borderRadius: '50%', opacity: 0.3,
          }} />
        );
      })}

      {/* Header label */}
      <div style={{
        position: 'absolute', top: 80, left: 100,
        fontFamily: 'JetBrains Mono', fontSize: 28, color: '#468A9A',
        letterSpacing: 5, fontWeight: 700,
        opacity: tag1,
      }}>// REAL TALK</div>

      {/* Crossed out CPA */}
      <div style={{
        position: 'absolute', top: 140, left: 100,
        transform: `translateX(${(1 - tag1) * -40}px)`,
        opacity: tag1,
      }}>
        <div style={{
          display: 'inline-block', position: 'relative',
          padding: '14px 36px',
          border: '4px solid #2C3333',
        }}>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 102, color: '#2C3333',
            fontWeight: 900, letterSpacing: -2, lineHeight: 1,
          }}>NOT YOUR CPA</div>
          <div style={{
            position: 'absolute', left: -10, right: -10, top: '50%', height: 8,
            backgroundColor: '#468A9A',
            transform: `scaleX(${cross1}) translateY(-50%) rotate(-3deg)`,
            transformOrigin: 'left',
          }} />
        </div>
      </div>

      {/* Crossed out Advisor */}
      <div style={{
        position: 'absolute', top: 320, left: 100,
        transform: `translateX(${(1 - tag2) * -40}px)`,
        opacity: tag2,
      }}>
        <div style={{
          display: 'inline-block', position: 'relative',
          padding: '14px 36px',
          border: '4px solid #2C3333',
        }}>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 102, color: '#2C3333',
            fontWeight: 900, letterSpacing: -2, lineHeight: 1,
          }}>NOT YOUR ADVISOR</div>
          <div style={{
            position: 'absolute', left: -10, right: -10, top: '50%', height: 8,
            backgroundColor: '#468A9A',
            transform: `scaleX(${cross2}) translateY(-50%) rotate(2deg)`,
            transformOrigin: 'left',
          }} />
        </div>
      </div>

      {/* Burned visual */}
      <div style={{
        position: 'absolute', top: 530, left: 100, right: 100,
        opacity: burnedOp,
        transform: `translateY(${(1 - burnedOp) * 30}px)`,
        display: 'flex', alignItems: 'center', gap: 30,
      }}>
        <svg width="120" height="120" viewBox="0 0 100 100">
          <path d="M50 10 Q35 30 35 45 Q35 60 50 70 Q65 60 65 45 Q65 30 50 10 Z"
            fill="#468A9A" />
          <path d="M50 25 Q42 40 45 55 Q50 65 55 55 Q58 40 50 25 Z"
            fill="#2C3333" opacity="0.6" />
          <ellipse cx="50" cy="85" rx="30" ry="6" fill="#2C3333" opacity="0.3" />
        </svg>
        <div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 28, color: '#468A9A',
            letterSpacing: 4, fontWeight: 700, marginBottom: 8,
          }}>JUST A GUY WHO...</div>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 94, color: '#2C3333',
            fontWeight: 900, letterSpacing: -2, lineHeight: 1,
          }}>
            GOT <span style={{ color: '#468A9A' }}>BURNED BADLY</span>
          </div>
          <div style={{
            fontFamily: 'Inter', fontSize: 36, color: '#2C3333',
            fontWeight: 500, marginTop: 10, opacity: 0.85,
          }}>by my own financial ignorance</div>
        </div>
      </div>

      {/* Mission Banner */}
      <div style={{
        position: 'absolute', bottom: 60, left: 100, right: 100,
        transform: `scale(${0.95 + missionOp * 0.05})`,
        opacity: missionOp,
        padding: '24px 40px',
        backgroundColor: '#2C3333',
        display: 'flex', alignItems: 'center', justifyContent: 'space-between',
      }}>
        <div style={{
          fontFamily: 'Mont Heavy', fontSize: 51, color: '#CBE4DE',
          fontWeight: 900, letterSpacing: 1,
        }}>
          MY MISSION: <span style={{ color: '#468A9A' }}>PLAIN ENGLISH</span>
        </div>
        <svg width="60" height="60" viewBox="0 0 60 60">
          <path d="M30 5 L36 22 L54 22 L40 33 L46 50 L30 40 L14 50 L20 33 L6 22 L24 22 Z"
            fill="#468A9A" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(44,51,51,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#2C3333', opacity: 0.3,
      }}>Failed Finances</div>
    </AbsoluteFill>
  );
};

export default Scene06_NotYourCPA;
```

```tsx
import React from 'react';
Audio: "Treat everything I say as a starting point for your own research, not gospel. Before you make any big moves, sit down with a qualified tax professional. Cool? Let's get into it."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene07_StartingPoint',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene07_StartingPoint: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const startOp = sp(15);
  const pathDraw = interpolate(frame, [40, 160], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const dot1 = sp(50);
  const dot2 = sp(110);
  const dot3 = sp(170);
  const proOp = sp(240);
  const coolOp = sp(330, { damping: 8, stiffness: 180 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#CBE4DE', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 50%, rgba(70,138,154,0.15), rgba(203,228,222,0) 65%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute', left: '50%', top: '50%',
        transform: `translate(-50%,-50%) rotate(${frame * 0.1}deg)`,
        width: 1200, height: 1200,
        border: '1px solid #2C3333', opacity: 0.04,
        borderRadius: 100,
      }} />
      <div style={{
        position: 'absolute', left: '50%', top: '50%',
        transform: `translate(-50%,-50%) rotate(${-frame * 0.08}deg)`,
        width: 800, height: 800,
        border: '1px solid #468A9A', opacity: 0.06,
        borderRadius: '50%',
      }} />

      {/* Dot grid */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.1 }}>
        <defs>
          <pattern id="dotg7" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1" fill="#2C3333" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dotg7)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const r = seededRandom(i + 71);
        const px = r * 1920;
        const py = (seededRandom(i + 130) * 1080 + frame * 0.3) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 4, height: 4, backgroundColor: '#468A9A',
            borderRadius: '50%', opacity: 0.3,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 70, left: '50%', transform: `translateX(-50%) translateY(${(1 - startOp) * -20}px)`,
        opacity: startOp, textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 26, color: '#468A9A',
          letterSpacing: 6, fontWeight: 700, marginBottom: 10,
        }}>// YOUR ROADMAP</div>
        <div style={{
          fontFamily: 'Mont Heavy', fontSize: 102, color: '#2C3333',
          fontWeight: 900, letterSpacing: -3, lineHeight: 1,
        }}>
          THIS IS A <span style={{ color: '#468A9A' }}>STARTING POINT</span>
        </div>
      </div>

      {/* Journey Path */}
      <svg width="1600" height="280" style={{
        position: 'absolute', top: 380, left: '50%',
        transform: 'translateX(-50%)',
      }} viewBox="0 0 1600 280">
        <path
          d="M 60 140 Q 400 40 800 140 T 1540 140"
          stroke="#468A9A" strokeWidth="6" fill="none"
          strokeDasharray="2000"
          strokeDashoffset={2000 - pathDraw * 2000}
          strokeLinecap="round"
        />
      </svg>

      {/* 3 Stops on the Path */}
      {[
        { label: 'WATCH', sub: 'This video', x: 100, anim: dot1 },
        { label: 'RESEARCH', sub: 'Verify yourself', x: 760, anim: dot2 },
        { label: 'CONSULT', sub: 'A pro', x: 1420, anim: dot3 },
      ].map((stop, i) => (
        <div key={i} style={{
          position: 'absolute', top: 470, left: stop.x,
          transform: `translateX(-50%) scale(${stop.anim})`,
          opacity: stop.anim,
          textAlign: 'center',
        }}>
          <div style={{
            width: 80, height: 80, borderRadius: '50%',
            backgroundColor: '#2C3333',
            border: '5px solid #468A9A',
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            margin: '0 auto 14px',
            fontFamily: 'Mont Heavy', fontSize: 34, color: '#CBE4DE',
            fontWeight: 900,
          }}>{i + 1}</div>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 47, color: '#2C3333',
            fontWeight: 900, letterSpacing: -1,
          }}>{stop.label}</div>
          <div style={{
            fontFamily: 'Inter', fontSize: 26, color: '#468A9A',
            fontWeight: 600,
          }}>{stop.sub}</div>
        </div>
      ))}

      {/* Pro Quote Card */}
      <div style={{
        position: 'absolute', bottom: 200, left: '50%',
        transform: `translateX(-50%) scale(${proOp})`,
        opacity: proOp,
        padding: '20px 40px',
        backgroundColor: 'rgba(44,51,51,0.08)',
        borderLeft: '5px solid #468A9A',
        fontFamily: 'Inter', fontSize: 36, color: '#2C3333',
        fontWeight: 600, fontStyle: 'italic',
      }}>
        "Before any big moves — sit down with a qualified pro."
      </div>

      {/* Cool? Cool. */}
      <div style={{
        position: 'absolute', bottom: 70, left: '50%',
        transform: `translateX(-50%) scale(${coolOp})`,
        opacity: coolOp,
        display: 'flex', alignItems: 'center', gap: 24,
      }}>
        <div style={{
          fontFamily: 'Mont Heavy', fontSize: 76, color: '#2C3333',
          fontWeight: 900, letterSpacing: -1,
        }}>COOL?</div>
        <div style={{
          padding: '12px 30px', backgroundColor: '#468A9A',
          fontFamily: 'Mont Heavy', fontSize: 76, color: '#CBE4DE',
          fontWeight: 900, letterSpacing: -1,
        }}>COOL.</div>
        <div style={{
          fontFamily: 'Mont Heavy', fontSize: 76, color: '#2C3333',
          fontWeight: 900, letterSpacing: -1,
        }}>LET'S GO.</div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(44,51,51,0.30) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#2C3333', opacity: 0.3,
      }}>Failed Finances</div>
    </AbsoluteFill>
  );
};

export default Scene07_StartingPoint;
```

```tsx
import React from 'react';
Audio: "Nobody is talking loudly enough about this new charitable giving floor — and this one stings if you give consistently throughout the year."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene08_CharityFloorIntro',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene08_CharityFloorIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const silentOp = sp(10);
  const titleOp = sp(60);
  const floorRise = sp(140, { damping: 14, stiffness: 80 });
  const stingOp = sp(260, { damping: 8, stiffness: 200 });
  const shake = frame > 270 && frame < 310 ? Math.sin(frame * 1.8) * 5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#CBE4DE', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(70,138,154,0.10) 0%, rgba(203,228,222,0) 50%, rgba(44,51,51,0.12) 100%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute', left: '5%', top: '10%', width: 350, height: 350,
        borderRadius: '50%', border: '2px solid #2C3333', opacity: 0.05,
      }} />
      <div style={{
        position: 'absolute', right: '8%', top: '15%', width: 280, height: 280,
        backgroundColor: '#468A9A', opacity: 0.05, borderRadius: 30,
        transform: `rotate(${15 + frame * 0.1}deg)`,
      }} />

      {/* Grid */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="g8" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M60 0 L0 0 0 60" fill="none" stroke="#2C3333" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#g8)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 91);
        const px = r * 1920;
        const py = (seededRandom(i + 22) * 1080 + Math.sin(frame * 0.025 + i) * 18) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 4, height: 4, backgroundColor: '#468A9A',
            borderRadius: '50%', opacity: 0.3,
          }} />
        );
      })}

      <div style={{ transform: `translateX(${shake}px)`, width: '100%', height: '100%' }}>
        {/* Silent megaphone metaphor */}
        <div style={{
          position: 'absolute', top: 80, left: '50%',
          transform: `translateX(-50%) scale(${silentOp})`,
          opacity: silentOp,
          display: 'flex', alignItems: 'center', gap: 20,
          padding: '14px 28px',
          backgroundColor: '#2C3333',
        }}>
          <svg width="40" height="40" viewBox="0 0 40 40">
            <path d="M5 16 L15 16 L28 8 L28 32 L15 24 L5 24 Z" fill="#CBE4DE" />
            <line x1="32" y1="12" x2="38" y2="6" stroke="#468A9A" strokeWidth="3" strokeLinecap="round" />
            <line x1="32" y1="28" x2="38" y2="34" stroke="#468A9A" strokeWidth="3" strokeLinecap="round" />
          </svg>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 26, color: '#CBE4DE',
            letterSpacing: 5, fontWeight: 700,
          }}>NOBODY IS SAYING IT LOUDLY ENOUGH</div>
        </div>

        {/* Main title */}
        <div style={{
          position: 'absolute', top: 200, left: 80, right: 80,
          textAlign: 'center',
          transform: `translateY(${(1 - titleOp) * 40}px)`,
          opacity: titleOp,
        }}>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 144, color: '#2C3333',
            fontWeight: 900, letterSpacing: -5, lineHeight: 0.95,
          }}>
            THE NEW
          </div>
          <div style={{
            fontFamily: 'Nexa Bold', fontSize: 187, color: '#468A9A',
            fontWeight: 900, letterSpacing: -7, lineHeight: 0.95,
          }}>
            CHARITY FLOOR
          </div>
        </div>

        {/* Floor visualization */}
        <div style={{
          position: 'absolute', bottom: 180, left: '50%',
          transform: `translateX(-50%) scaleY(${floorRise})`,
          transformOrigin: 'bottom',
          opacity: floorRise,
          width: 1400, height: 80,
          backgroundColor: '#2C3333',
          display: 'flex', alignItems: 'center', justifyContent: 'space-around',
          paddingLeft: 30, paddingRight: 30,
        }}>
          {Array.from({ length: 14 }).map((_, i) => (
            <div key={i} style={{
              width: 4, height: 50, backgroundColor: '#468A9A', opacity: 0.7,
            }} />
          ))}
          <div style={{
            position: 'absolute', top: '50%', left: '50%',
            transform: 'translate(-50%, -50%)',
            fontFamily: 'Mont Heavy', fontSize: 47, color: '#CBE4DE',
            fontWeight: 900, letterSpacing: 4,
          }}>FLOOR // DEDUCTION BLOCKED BELOW THIS LINE</div>
        </div>

        {/* Sting Banner */}
        <div style={{
          position: 'absolute', bottom: 70, left: '50%',
          transform: `translateX(-50%) scale(${stingOp})`,
          opacity: stingOp,
          display: 'flex', alignItems: 'center', gap: 20,
          padding: '14px 38px',
          backgroundColor: '#468A9A',
        }}>
          <svg width="40" height="40" viewBox="0 0 40 40">
            <path d="M20 5 L25 18 L38 18 L28 26 L32 38 L20 30 L8 38 L12 26 L2 18 L15 18 Z" fill="#CBE4DE" />
          </svg>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 51, color: '#CBE4DE',
            fontWeight: 900, letterSpacing: 2,
          }}>THIS ONE STINGS IF YOU GIVE CONSISTENTLY</div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(44,51,51,0.32) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#2C3333', opacity: 0.3,
      }}>Failed Finances</div>
    </AbsoluteFill>
  );
};

export default Scene08_CharityFloorIntro;
```

```tsx
import React from 'react';
Audio: "Starting in 2026, before any charitable contributions become deductible on Schedule A, you have to clear a floor equal to half a percent of your adjusted gross income."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene09_ScheduleAFloor',
  durationInSeconds: 26,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene09_ScheduleAFloor: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const yearOp = sp(15);
  const schedOp = sp(70);
  const arrowDraw = interpolate(frame, [120, 180], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const floorOp = sp(200);
  const formulaOp = sp(290);

  return (
    <AbsoluteFill style={{ backgroundColor: '#CBE4DE', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 30%, rgba(70,138,154,0.12), rgba(203,228,222,0) 55%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute', left: '60%', top: '5%', width: 500, height: 500,
        border: '2px solid #468A9A', opacity: 0.05, borderRadius: 60,
        transform: `rotate(${frame * 0.07}deg)`,
      }} />
      <div style={{
        position: 'absolute', left: '5%', bottom: '5%', width: 280, height: 280,
        backgroundColor: '#2C3333', opacity: 0.04, borderRadius: '50%',
      }} />

      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="g9" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M0 0 L50 50 M50 0 L0 50" stroke="#2C3333" strokeWidth="0.6" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#g9)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 41);
        const px = r * 1920;
        const py = (seededRandom(i + 311) * 1080 + frame * 0.4) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 4, height: 4, backgroundColor: '#468A9A',
            borderRadius: '50%', opacity: 0.3,
          }} />
        );
      })}

      {/* Top Year Tag */}
      <div style={{
        position: 'absolute', top: 70, left: '50%',
        transform: `translateX(-50%) scale(${yearOp})`,
        opacity: yearOp,
        display: 'flex', alignItems: 'center', gap: 20,
        padding: '12px 30px',
        backgroundColor: '#2C3333',
      }}>
        <div style={{
          width: 12, height: 12, backgroundColor: '#468A9A', borderRadius: '50%',
          animation: 'pulse 1s infinite',
        }} />
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 30, color: '#CBE4DE',
          letterSpacing: 6, fontWeight: 700,
        }}>STARTING // 2026</div>
      </div>

      {/* Schedule A Document */}
      <div style={{
        position: 'absolute', top: 200, left: 130, width: 700, height: 540,
        transform: `translateX(${(1 - schedOp) * -60}px) rotate(-2deg)`,
        opacity: schedOp,
        backgroundColor: '#CBE4DE',
        border: '4px solid #2C3333',
        padding: 30,
        boxShadow: '12px 12px 0 rgba(44,51,51,0.15)',
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 24, color: '#468A9A',
          letterSpacing: 3, fontWeight: 700, marginBottom: 10,
        }}>IRS FORM</div>
        <div style={{
          fontFamily: 'Mont Heavy', fontSize: 119, color: '#2C3333',
          fontWeight: 900, letterSpacing: -3, lineHeight: 1,
        }}>SCHEDULE A</div>
        <div style={{
          fontFamily: 'Inter', fontSize: 30, color: '#2C3333',
          fontWeight: 500, marginTop: 14, opacity: 0.7,
        }}>Itemized Deductions</div>

        <div style={{ marginTop: 40 }}>
          {['Charitable Contributions', 'Medical Expenses', 'SALT', 'Mortgage Interest'].map((row, i) => (
            <div key={i} style={{
              display: 'flex', justifyContent: 'space-between', alignItems: 'center',
              padding: '12px 0', borderBottom: '1px dashed #2C3333',
              fontFamily: 'JetBrains Mono', fontSize: 22, color: '#2C3333',
              fontWeight: 500,
            }}>
              <span>{row}</span>
              <span style={{ color: i === 0 ? '#468A9A' : '#2C3333', fontWeight: 700 }}>
                {i === 0 ? 'BLOCKED ↓' : '$ ___'}
              </span>
            </div>
          ))}
        </div>
      </div>

      {/* Arrow & Floor */}
      <svg width="200" height="500" style={{
        position: 'absolute', top: 280, left: 880,
        opacity: arrowDraw,
      }} viewBox="0 0 200 500">
        <path
          d={`M 20 50 Q 100 200 180 350 L 180 380 M 160 370 L 180 390 L 200 370`}
          stroke="#468A9A" strokeWidth="6" fill="none" strokeLinecap="round"
          strokeDasharray="600"
          strokeDashoffset={600 - arrowDraw * 600}
        />
      </svg>

      {/* Floor box */}
      <div style={{
        position: 'absolute', top: 230, right: 100, width: 720,
        transform: `translateX(${(1 - floorOp) * 60}px) scale(${0.92 + floorOp * 0.08})`,
        opacity: floorOp,
        backgroundColor: '#2C3333',
        padding: 40,
        border: '5px solid #468A9A',
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 24, color: '#468A9A',
          letterSpacing: 4, fontWeight: 700, marginBottom: 14,
        }}>MUST CLEAR FIRST</div>
        <div style={{
          fontFamily: 'Mont Heavy', fontSize: 102, color: '#CBE4DE',
          fontWeight: 900, letterSpacing: -2, lineHeight: 1,
        }}>THE FLOOR</div>
        <div style={{
          marginTop: 30, padding: '30px 20px',
          backgroundColor: '#CBE4DE',
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'Nexa Bold', fontSize: 153, color: '#2C3333',
            fontWeight: 900, letterSpacing: -5, lineHeight: 1,
          }}>0.5<span style={{ color: '#468A9A' }}>%</span></div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 30, color: '#2C3333',
            letterSpacing: 4, fontWeight: 700, marginTop: 10,
          }}>OF YOUR AGI</div>
        </div>
      </div>

      {/* Bottom Formula */}
      <div style={{
        position: 'absolute', bottom: 50, left: '50%',
        transform: `translateX(-50%) translateY(${(1 - formulaOp) * 30}px)`,
        opacity: formulaOp,
        padding: '18px 40px',
        backgroundColor: 'rgba(44,51,51,0.08)',
        border: '2px solid #468A9A',
        fontFamily: 'JetBrains Mono', fontSize: 38, color: '#2C3333',
        fontWeight: 700, letterSpacing: 2,
      }}>
        DEDUCTION = TOTAL GIFTS <span style={{ color: '#468A9A' }}>−</span> (AGI × 0.5%)
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 55%, rgba(44,51,51,0.30) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#2C3333', opacity: 0.3,
      }}>Failed Finances</div>
    </AbsoluteFill>
  );
};

export default Scene09_ScheduleAFloor;
```

```tsx
import React from 'react';
Audio: "That's the 0.5% AGI floor. Sounds tiny on paper — but let's actually do the math, because that's where the reality slaps you."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene10_RealitySlap',
  durationInSeconds: 27,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene10_RealitySlap: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const numScale = sp(10, { damping: 10, stiffness: 100 });
  const tinyOp = sp(80);
  const tinyCross = sp(140);
  const mathOp = sp(200);
  const slapScale = sp(310, { damping: 6, stiffness: 220 });
  const slapShake = frame > 320 && frame < 360 ? Math.sin(frame * 2) * 10 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#CBE4DE', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 40%, rgba(70,138,154,0.15), rgba(203,228,222,0) 60%)',
      }} />

      {/* Shapes */}
      <div style={{
        position: 'absolute', left: '50%', top: '50%',
        transform: `translate(-50%,-50%) rotate(${frame * 0.1}deg)`,
        width: 1000, height: 1000,
        border: '2px solid #468A9A', opacity: 0.04, borderRadius: '50%',
      }} />
      <div style={{
        position: 'absolute', left: '50%', top: '50%',
        transform: `translate(-50%,-50%) rotate(${-frame * 0.12}deg)`,
        width: 700, height: 700,
        border: '2px solid #2C3333', opacity: 0.05,
      }} />

      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="g10" width="40" height="40" patternUnits="userSpaceOnUse">
            <circle cx="20" cy="20" r="1.5" fill="#2C3333" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#g10)" />
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 5);
        const px = r * 1920;
        const py = (seededRandom(i + 555) * 1080 + Math.sin(frame * 0.025 + i) * 20) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 4, height: 4, backgroundColor: '#468A9A',
            borderRadius: '50%', opacity: 0.3,
          }} />
        );
      })}

      <div style={{ transform: `translate(${slapShake}px, ${slapShake * 0.3}px)`, width: '100%', height: '100%' }}>
        {/* Big number 0.5% */}
        <div style={{
          position: 'absolute', top: 100, left: '50%',
          transform: `translateX(-50%) scale(${numScale})`,
          opacity: Math.min(1, numScale),
          textAlign: 'center',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 28, color: '#468A9A',
            letterSpacing: 6, fontWeight: 700, marginBottom: 8,
          }}>THE NUMBER</div>
          <div style={{
            fontFamily: 'Nexa Bold', fontSize: 264, color: '#2C3333',
            fontWeight: 900, letterSpacing: -10, lineHeight: 1,
          }}>
            0.5<span style={{ color: '#468A9A' }}>%</span>
          </div>
          <div style={{
            fontFamily: 'JetBrains Mono', fontSize: 32, color: '#2C3333',
            letterSpacing: 4, fontWeight: 700, opacity: 0.7,
          }}>AGI FLOOR</div>
        </div>

        {/* Tiny on paper (crossed out) */}
        <div style={{
          position: 'absolute', top: 600, left: '50%',
          transform: `translateX(-50%)`,
          opacity: tinyOp,
        }}>
          <div style={{
            position: 'relative', display: 'inline-block',
            fontFamily: 'Mont Heavy', fontSize: 64, color: '#2C3333',
            fontWeight: 900, letterSpacing: -1, opacity: 0.5,
            padding: '0 20px',
          }}>
            "SOUNDS TINY ON PAPER..."
            <div style={{
              position: 'absolute', left: 0, right: 0, top: '50%', height: 6,
              backgroundColor: '#468A9A',
              transform: `scaleX(${tinyCross}) translateY(-50%) rotate(-2deg)`,
              transformOrigin: 'left',
            }} />
          </div>
        </div>

        {/* Math arrow */}
        <div style={{
          position: 'absolute', top: 700, left: '50%',
          transform: `translateX(-50%) scale(${mathOp})`,
          opacity: mathOp,
          display: 'flex', alignItems: 'center', gap: 30,
        }}>
          <svg width="60" height="60" viewBox="0 0 60 60">
            <path d="M30 8 L30 50 M14 36 L30 52 L46 36" stroke="#2C3333" strokeWidth="6" strokeLinecap="round" fill="none" />
          </svg>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 64, color: '#2C3333',
            fontWeight: 900, letterSpacing: -1,
          }}>
            BUT THE <span style={{ color: '#468A9A' }}>MATH</span>...
          </div>
        </div>

        {/* SLAP banner */}
        <div style={{
          position: 'absolute', bottom: 80, left: '50%',
          transform: `translateX(-50%) scale(${slapScale}) rotate(${(1 - slapScale) * -10}deg)`,
          opacity: slapScale,
          padding: '24px 60px',
          backgroundColor: '#2C3333',
          border: '6px solid #468A9A',
          display: 'flex', alignItems: 'center', gap: 24,
        }}>
          <svg width="60" height="60" viewBox="0 0 60 60">
            <path d="M15 10 L40 25 L25 28 L40 50 L20 50 L25 35 L10 30 Z" fill="#468A9A" />
          </svg>
          <div style={{
            fontFamily: 'Mont Heavy', fontSize: 76, color: '#CBE4DE',
            fontWeight: 900, letterSpacing: 2,
          }}>
            REALITY <span style={{ color: '#468A9A' }}>SLAPS</span> YOU
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, transparent 50%, rgba(44,51,51,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#2C3333', opacity: 0.3,
      }}>Failed Finances</div>
    </AbsoluteFill>
  );
};

export default Scene10_RealitySlap;
```

