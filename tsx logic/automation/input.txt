```tsx
import React from 'react';
Audio: "Un bien classé F décoté de 25 %, alors qu'une rénovation ciblée pourrait le faire remonter en C pour bien moins cher — c'est de la création de valeur pure."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene31: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  // Arrow from F to C animation
  const arrowProgress = interpolate(frame, [50, 130], [0, 1], { extrapolateRight: 'clamp', easing: Easing.bezier(0.25, 0.1, 0.25, 1) });
  
  // Decote bar shrink
  const decoteShrink = interpolate(frame, [140, 220], [1, 0.4], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  
  // Value rise
  const valueRise = interpolate(frame, [200, 290], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  const labels = ['F', 'E', 'D', 'C'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp, transform: `scale(${camScale})` }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 30% 20%, rgba(251,245,167,0.4) 0%, transparent 60%)' }} />

      {/* Shape Layers - large transparent geometric shapes */}
      <div style={{
        position: 'absolute', top: '-15%', right: '-10%', width: 700, height: 700,
        borderRadius: '50%', border: '3px solid #1E2022', opacity: 0.06,
        transform: `rotate(${frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-20%', left: '-15%', width: 900, height: 900,
        background: '#1E2022', opacity: 0.04, borderRadius: 80,
        transform: `rotate(${-frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '40%', left: '50%', width: 400, height: 400,
        border: '2px solid #1E2022', opacity: 0.05,
        transform: `translate(-50%, -50%) rotate(${frame * 0.3}deg)`,
      }} />

      {/* Motion texture - diagonal grid lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="100%" height="100%">
        <defs>
          <pattern id="grid31" width="80" height="80" patternUnits="userSpaceOnUse" patternTransform={`translate(${frame * 0.3}, 0)`}>
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#1E2022" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid31)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 30 }).map((_, i) => {
        const seed = i * 13;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 1) * 1080;
        const size = 3 + seededRandom(seed + 2) * 6;
        const drift = Math.sin(frame * 0.02 + i) * 25;
        return (
          <div key={i} style={{
            position: 'absolute', left: x + drift, top: y + Math.cos(frame * 0.03 + i) * 20,
            width: size, height: size, borderRadius: '50%',
            background: i % 3 === 0 ? '#FBF5A7' : '#1E2022',
            opacity: 0.35,
          }} />
        );
      })}

      {/* Top label badge */}
      <div style={{
        position: 'absolute', top: 70, left: '50%',
        transform: `translateX(-50%) translateY(${(1 - sp(0)) * -50}px)`,
        opacity: sp(0),
        background: '#1E2022', color: '#FBF5A7',
        padding: '14px 36px', borderRadius: 50,
        fontFamily: 'Space Mono, monospace', fontSize: 26, fontWeight: 700,
        letterSpacing: 3,
      }}>
        STRATÉGIE INVESTISSEUR
      </div>

      {/* Main Headline */}
      <div style={{
        position: 'absolute', top: 160, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(15),
        transform: `translateY(${(1 - sp(15)) * 30}px)`,
      }}>
        <div style={{
          fontFamily: 'Clash Display, Anton, sans-serif',
          fontSize: 108, fontWeight: 800, color: '#1E2022',
          lineHeight: 1, letterSpacing: -2,
        }}>
          CRÉATION DE
        </div>
        <div style={{
          fontFamily: 'Clash Display, Anton, sans-serif',
          fontSize: 136, fontWeight: 900, color: '#1E2022',
          lineHeight: 1, letterSpacing: -3,
          textShadow: '6px 6px 0 #FBF5A7',
        }}>
          VALEUR PURE
        </div>
      </div>

      {/* DPE Letters progression F -> C */}
      <div style={{
        position: 'absolute', top: 480, left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex', gap: 30, alignItems: 'center',
      }}>
        {labels.map((label, i) => {
          const reveal = sp(60 + i * 20);
          const isStart = i === 0;
          const isEnd = i === labels.length - 1;
          const pulse = isEnd ? 1 + Math.sin(frame * 0.15) * 0.05 : 1;
          
          return (
            <React.Fragment key={label}>
              <div style={{
                width: 130, height: 130,
                background: isStart ? '#1E2022' : isEnd ? '#FBF5A7' : 'rgba(30,32,34,0.15)',
                border: `5px solid #1E2022`,
                borderRadius: 24,
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                fontFamily: 'Anton, sans-serif',
                fontSize: 85,
                color: isStart ? '#FBF5A7' : '#1E2022',
                opacity: reveal,
                transform: `scale(${reveal * pulse}) ${isStart ? `rotate(${(1 - reveal) * -20}deg)` : ''}`,
                boxShadow: isEnd ? '0 0 40px rgba(30,32,34,0.4)' : 'none',
                position: 'relative',
              }}>
                {label}
                {isStart && (
                  <div style={{
                    position: 'absolute', top: -45, left: '50%',
                    transform: 'translateX(-50%)',
                    fontFamily: 'Montserrat, sans-serif',
                    fontSize: 22, fontWeight: 700, color: '#1E2022',
                    background: '#FBF5A7', padding: '4px 14px', borderRadius: 8,
                    opacity: reveal,
                    whiteSpace: 'nowrap',
                  }}>
                    AVANT
                  </div>
                )}
                {isEnd && (
                  <div style={{
                    position: 'absolute', top: -45, left: '50%',
                    transform: 'translateX(-50%)',
                    fontFamily: 'Montserrat, sans-serif',
                    fontSize: 22, fontWeight: 700, color: '#FBF5A7',
                    background: '#1E2022', padding: '4px 14px', borderRadius: 8,
                    opacity: reveal,
                    whiteSpace: 'nowrap',
                  }}>
                    APRÈS
                  </div>
                )}
              </div>
              {i < labels.length - 1 && (
                <svg width="50" height="40" style={{ opacity: sp(70 + i * 20) }}>
                  <path
                    d={`M 5 20 L ${5 + 40 * arrowProgress} 20`}
                    stroke="#1E2022" strokeWidth="5" strokeLinecap="round"
                  />
                  <path
                    d={`M ${35 * arrowProgress} 10 L ${45 * arrowProgress} 20 L ${35 * arrowProgress} 30`}
                    stroke="#1E2022" strokeWidth="5" strokeLinecap="round" strokeLinejoin="round" fill="none"
                    opacity={arrowProgress}
                  />
                </svg>
              )}
            </React.Fragment>
          );
        })}
      </div>

      {/* Comparison bars: Decote vs Renovation */}
      <div style={{
        position: 'absolute', bottom: 130, left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex', gap: 80, alignItems: 'flex-end',
        opacity: sp(140),
      }}>
        {/* Decote bar */}
        <div style={{ textAlign: 'center' }}>
          <div style={{
            fontFamily: 'Space Mono, monospace', fontSize: 38, fontWeight: 700,
            color: '#1E2022', marginBottom: 12,
          }}>
            -25%
          </div>
          <div style={{
            width: 140, height: 220 * decoteShrink,
            background: '#1E2022',
            transition: 'height 0.3s',
            borderRadius: '8px 8px 0 0',
            position: 'relative',
          }}>
            <div style={{
              position: 'absolute', bottom: -40, left: 0, right: 0,
              fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
              color: '#1E2022',
            }}>
              DÉCOTE
            </div>
          </div>
        </div>

        {/* VS */}
        <div style={{
          fontFamily: 'Anton, sans-serif', fontSize: 64,
          color: '#1E2022', alignSelf: 'center',
          opacity: sp(160),
          transform: `scale(${sp(160)})`,
        }}>
          VS
        </div>

        {/* Value recovered */}
        <div style={{ textAlign: 'center' }}>
          <div style={{
            fontFamily: 'Space Mono, monospace', fontSize: 38, fontWeight: 700,
            color: '#1E2022', marginBottom: 12,
          }}>
            +VALEUR
          </div>
          <div style={{
            width: 140, height: 80 + 180 * valueRise,
            background: '#FBF5A7',
            border: '4px solid #1E2022',
            borderRadius: '8px 8px 0 0',
            position: 'relative',
          }}>
            <svg style={{ position: 'absolute', top: 20, left: '50%', transform: 'translateX(-50%)' }} width="60" height="60" viewBox="0 0 60 60">
              <path d="M 10 50 L 25 30 L 35 40 L 50 15" stroke="#1E2022" strokeWidth="5" fill="none" strokeLinecap="round" strokeLinejoin="round"
                strokeDasharray="100" strokeDashoffset={100 - 100 * valueRise} />
              <polygon points="45,15 55,15 55,25" fill="#1E2022" opacity={valueRise} />
            </svg>
            <div style={{
              position: 'absolute', bottom: -40, left: 0, right: 0,
              fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
              color: '#1E2022',
            }}>
              RÉNOVATION
            </div>
          </div>
        </div>
      </div>

      {/* Side accent line */}
      <div style={{
        position: 'absolute', left: 0, top: '40%',
        width: 8, height: 200,
        background: '#1E2022',
        transform: `scaleY(${sp(30)})`,
        transformOrigin: 'top',
      }} />

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(30,32,34,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1E2022', opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene31;
```

```tsx
import React from 'react';
Audio: "Mais attention, ça ne s'improvise pas. Tout le monde n'est pas en mesure de mener ce genre d'opération. Se lancer sans préparation peut coûter très cher."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Warning pulse
  const warningPulse = 1 + Math.sin(frame * 0.18) * 0.04;
  
  // Screen shake at impact
  const shake = frame < 30 ? Math.sin(frame * 1.2) * (1 - frame / 30) * 4 : 0;

  // Camera tilt
  const camRotate = interpolate(frame, [0, durationInFrames], [-0.3, 0.3]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp, transform: `translate(${shake}px, ${shake}px) rotate(${camRotate}deg)` }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{ background: 'linear-gradient(135deg, rgba(251,245,167,0.3) 0%, transparent 50%, rgba(30,32,34,0.15) 100%)' }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: '10%', left: '-10%', width: 600, height: 600,
        background: '#1E2022', opacity: 0.05,
        clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-15%', right: '-10%', width: 700, height: 700,
        border: '4px solid #1E2022', opacity: 0.06,
        borderRadius: 40,
        transform: `rotate(${-frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute', top: '50%', left: '50%', width: 350, height: 350,
        borderRadius: '50%', border: '2px dashed #1E2022', opacity: 0.08,
        transform: `translate(-50%, -50%) rotate(${frame * 0.5}deg) scale(${1 + Math.sin(frame * 0.03) * 0.08})`,
      }} />

      {/* Motion texture - warning stripes */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
        <defs>
          <pattern id="stripes32" width="60" height="60" patternUnits="userSpaceOnUse" patternTransform={`translate(${frame * 0.4}, ${frame * 0.4})`}>
            <path d="M 0 60 L 60 0" stroke="#1E2022" strokeWidth="6" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#stripes32)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const seed = i * 17;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 1) * 1080;
        const drift = Math.sin(frame * 0.025 + i) * 30;
        return (
          <div key={i} style={{
            position: 'absolute', left: x + drift, top: y,
            width: 4, height: 4, borderRadius: '50%',
            background: '#1E2022', opacity: 0.3,
          }} />
        );
      })}

      {/* Warning Icon - top */}
      <div style={{
        position: 'absolute', top: 80, left: '50%',
        transform: `translateX(-50%) scale(${sp(0) * warningPulse})`,
      }}>
        <svg width="120" height="120" viewBox="0 0 120 120">
          <polygon points="60,10 110,100 10,100" fill="#1E2022" stroke="#1E2022" strokeWidth="4" strokeLinejoin="round" />
          <text x="60" y="85" textAnchor="middle" fontFamily="Anton, sans-serif" fontSize="60" fontWeight="900" fill="#FBF5A7">!</text>
        </polygon>
      </div>

      {/* Main Headline */}
      <div style={{
        position: 'absolute', top: 240, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(15),
      }}>
        <div style={{
          fontFamily: 'Bricolage Grotesque, sans-serif',
          fontSize: 44, fontWeight: 500, color: '#1E2022',
          letterSpacing: 8, marginBottom: 10,
          opacity: sp(10),
        }}>
          ATTENTION
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 153, fontWeight: 900, color: '#1E2022',
          lineHeight: 0.95, letterSpacing: -2,
          transform: `translateY(${(1 - sp(20)) * 40}px)`,
        }}>
          ÇA NE
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 195, fontWeight: 900,
          color: '#1E2022',
          background: '#FBF5A7',
          display: 'inline-block',
          padding: '0 40px',
          lineHeight: 1,
          letterSpacing: -4,
          transform: `scale(${sp(40)}) rotate(${(1 - sp(40)) * -3}deg)`,
          boxShadow: '12px 12px 0 #1E2022',
        }}>
          S'IMPROVISE PAS
        </div>
      </div>

      {/* Risk indicators row */}
      <div style={{
        position: 'absolute', bottom: 140, left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex', gap: 50, alignItems: 'center',
      }}>
        {[
          { icon: 'analyse', label: 'PRÉPARATION', delay: 80 },
          { icon: 'expert', label: 'EXPERTISE', delay: 100 },
          { icon: 'capital', label: 'CAPITAL', delay: 120 },
        ].map((item, i) => {
          const reveal = sp(item.delay);
          return (
            <div key={i} style={{
              opacity: reveal,
              transform: `translateY(${(1 - reveal) * 50}px) scale(${reveal})`,
              textAlign: 'center',
            }}>
              <div style={{
                width: 110, height: 110,
                background: '#1E2022',
                borderRadius: 20,
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                margin: '0 auto 14px',
                border: '4px solid #1E2022',
              }}>
                {item.icon === 'analyse' && (
                  <svg width="55" height="55" viewBox="0 0 55 55">
                    <circle cx="22" cy="22" r="14" stroke="#FBF5A7" strokeWidth="4" fill="none" />
                    <line x1="33" y1="33" x2="48" y2="48" stroke="#FBF5A7" strokeWidth="4" strokeLinecap="round" />
                  </svg>
                )}
                {item.icon === 'expert' && (
                  <svg width="55" height="55" viewBox="0 0 55 55">
                    <circle cx="27" cy="20" r="10" stroke="#FBF5A7" strokeWidth="4" fill="none" />
                    <path d="M 10 50 Q 27 35 44 50" stroke="#FBF5A7" strokeWidth="4" fill="none" strokeLinecap="round" />
                  </svg>
                )}
                {item.icon === 'capital' && (
                  <svg width="55" height="55" viewBox="0 0 55 55">
                    <rect x="12" y="18" width="32" height="22" stroke="#FBF5A7" strokeWidth="4" fill="none" rx="3" />
                    <circle cx="28" cy="29" r="5" stroke="#FBF5A7" strokeWidth="3" fill="none" />
                    <line x1="12" y1="24" x2="44" y2="24" stroke="#FBF5A7" strokeWidth="3" />
                  </svg>
                )}
              </div>
              <div style={{
                fontFamily: 'Montserrat, sans-serif',
                fontSize: 24, fontWeight: 700, color: '#1E2022',
                letterSpacing: 2,
              }}>
                {item.label}
              </div>
            </div>
          );
        })}
      </div>

      {/* Bottom caption */}
      <div style={{
        position: 'absolute', bottom: 50, left: '50%',
        transform: 'translateX(-50%)',
        fontFamily: 'Space Mono, monospace',
        fontSize: 28, fontWeight: 700, color: '#1E2022',
        background: '#1E2022', color: '#FBF5A7',
        padding: '10px 30px', borderRadius: 6,
        opacity: sp(150),
        letterSpacing: 2,
      }}>
        SANS PRÉPARATION = TRÈS CHER
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(30,32,34,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1E2022', opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene32;
```

```tsx
import React from 'react';
Audio: "Passons aux mains dans le cambouis. La première chose à faire, et elle est gratuite ou presque, c'est de connaître votre situation réelle avec le nouveau calcul."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene33: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [0, -20]);

  // Counter for "0€"
  const counterProgress = interpolate(frame, [80, 160], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const cost = Math.round((1 - counterProgress) * 250);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp, transform: `translateX(${camX}px)` }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 70% 80%, rgba(251,245,167,0.5) 0%, transparent 60%)' }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: '5%', right: '5%', width: 500, height: 500,
        border: '5px solid #1E2022', opacity: 0.06,
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '10%', left: '5%', width: 400, height: 400,
        background: '#1E2022', opacity: 0.05,
        borderRadius: '30% 70% 50% 50%',
        transform: `rotate(${-frame * 0.15}deg) scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />

      {/* Motion texture - dots grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.1 }} width="100%" height="100%">
        <defs>
          <pattern id="dots33" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#1E2022" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots33)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 28 }).map((_, i) => {
        const seed = i * 23;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 1) * 1080;
        const size = 2 + seededRandom(seed + 2) * 5;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x + Math.sin(frame * 0.03 + i) * 20,
            top: y + Math.cos(frame * 0.025 + i) * 25,
            width: size, height: size, borderRadius: '50%',
            background: '#FBF5A7', opacity: 0.6,
          }} />
        );
      })}

      {/* Step number badge */}
      <div style={{
        position: 'absolute', top: 100, left: 100,
        opacity: sp(0),
        transform: `scale(${sp(0)})`,
      }}>
        <div style={{
          width: 110, height: 110,
          background: '#1E2022', borderRadius: '50%',
          display: 'flex', alignItems: 'center', justifyContent: 'center',
          fontFamily: 'Anton, sans-serif', fontSize: 64, color: '#FBF5A7',
          border: '5px solid #1E2022',
        }}>
          01
        </div>
        <div style={{
          marginTop: 14,
          fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 700,
          color: '#1E2022', letterSpacing: 3,
        }}>
          ÉTAPE
        </div>
      </div>

      {/* Main Title */}
      <div style={{
        position: 'absolute', top: 90, left: 280, right: 100,
        opacity: sp(15),
        transform: `translateY(${(1 - sp(15)) * 30}px)`,
      }}>
        <div style={{
          fontFamily: 'Bricolage Grotesque, sans-serif',
          fontSize: 38, fontWeight: 500, color: '#1E2022',
          letterSpacing: 5, marginBottom: 8,
        }}>
          MAINS DANS LE CAMBOUIS
        </div>
        <div style={{
          fontFamily: 'Clash Display, Anton, sans-serif',
          fontSize: 119, fontWeight: 900, color: '#1E2022',
          lineHeight: 0.95, letterSpacing: -3,
        }}>
          CONNAÎTRE VOTRE
        </div>
        <div style={{
          fontFamily: 'Clash Display, Anton, sans-serif',
          fontSize: 153, fontWeight: 900, color: '#1E2022',
          lineHeight: 1, letterSpacing: -3,
          background: '#FBF5A7',
          display: 'inline-block', padding: '0 24px',
          transform: `translateX(${(1 - sp(35)) * -30}px)`,
        }}>
          SITUATION RÉELLE
        </div>
      </div>

      {/* Cost meter - large counter */}
      <div style={{
        position: 'absolute', bottom: 200, left: '50%',
        transform: 'translateX(-50%)',
        textAlign: 'center',
        opacity: sp(60),
      }}>
        <div style={{
          fontFamily: 'Montserrat, sans-serif', fontSize: 28, fontWeight: 600,
          color: '#1E2022', letterSpacing: 3, marginBottom: 12,
        }}>
          COÛT ESTIMÉ
        </div>
        <div style={{
          fontFamily: 'Space Mono, monospace', fontSize: 230, fontWeight: 700,
          color: '#1E2022', lineHeight: 1,
          background: '#1E2022', color: '#FBF5A7',
          padding: '20px 60px', borderRadius: 16,
          display: 'inline-block',
          boxShadow: '14px 14px 0 #FBF5A7',
        }}>
          {cost}€
        </div>
        <div style={{
          marginTop: 16,
          fontFamily: 'Bricolage Grotesque, sans-serif', fontSize: 42, fontWeight: 600,
          color: '#1E2022',
          opacity: counterProgress,
        }}>
          GRATUIT OU PRESQUE
        </div>
      </div>

      {/* Decorative elements - corner brackets */}
      {[
        { top: 40, left: 40, rotate: 0 },
        { top: 40, right: 40, rotate: 90 },
        { bottom: 40, left: 40, rotate: -90 },
        { bottom: 40, right: 40, rotate: 180 },
      ].map((pos, i) => (
        <div key={i} style={{
          position: 'absolute', ...pos,
          width: 60, height: 60,
          borderTop: '6px solid #1E2022',
          borderLeft: '6px solid #1E2022',
          transform: `rotate(${pos.rotate}deg)`,
          opacity: sp(10),
        }} />
      ))}

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(30,32,34,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1E2022', opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene33;
```

```tsx
import React from 'react';
Audio: "Beaucoup se basent sur un DPE réalisé il y a deux, trois, quatre ans. Le réflexe : vérifier sa validité et faire une simulation avec le nouveau coefficient."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene34: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom in slowly
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  // Timeline progress
  const timelineProgress = interpolate(frame, [60, 180], [0, 1], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Document scan effect
  const scanY = interpolate(frame, [120, 220], [0, 100], { extrapolateRight: 'clamp' });

  const years = ['2022', '2023', '2024', '2025'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp, transform: `scale(${camScale})` }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, rgba(251,245,167,0.4) 0%, transparent 50%)' }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: '-10%', right: '20%', width: 550, height: 550,
        borderRadius: '50%', background: '#1E2022', opacity: 0.04,
        transform: `scale(${1 + Math.sin(frame * 0.025) * 0.06})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-15%', left: '-5%', width: 600, height: 600,
        border: '5px solid #1E2022', opacity: 0.06,
        borderRadius: 60,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Motion texture - horizontal lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="hlines34" width="100" height="40" patternUnits="userSpaceOnUse" patternTransform={`translate(0, ${frame * 0.5})`}>
            <line x1="0" y1="20" x2="100" y2="20" stroke="#1E2022" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hlines34)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 26 }).map((_, i) => {
        const seed = i * 19;
        const x = seededRandom(seed) * 1920;
        const y = seededRandom(seed + 1) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x, top: y + Math.sin(frame * 0.04 + i) * 20,
            width: 5, height: 5, borderRadius: '50%',
            background: '#1E2022', opacity: 0.3,
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 70, left: '50%',
        transform: `translateX(-50%) translateY(${(1 - sp(0)) * -30}px)`,
        opacity: sp(0),
        fontFamily: 'Space Mono, monospace', fontSize: 24, fontWeight: 700,
        color: '#FBF5A7', background: '#1E2022',
        padding: '12px 32px', borderRadius: 40,
        letterSpacing: 4,
      }}>
        DPE OBSOLÈTE ?
      </div>

      {/* Headline */}
      <div style={{
        position: 'absolute', top: 150, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(15),
        transform: `translateY(${(1 - sp(15)) * 30}px)`,
      }}>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 119, fontWeight: 900, color: '#1E2022',
          lineHeight: 1, letterSpacing: -2,
        }}>
          VÉRIFIEZ LA
        </div>
        <div style={{
          fontFamily: 'Anton, sans-serif',
          fontSize: 153, fontWeight: 900, color: '#1E2022',
          lineHeight: 1, letterSpacing: -3,
          textShadow: '7px 7px 0 #FBF5A7',
        }}>
          VALIDITÉ
        </div>
      </div>

      {/* Timeline of years */}
      <div style={{
        position: 'absolute', bottom: 320, left: '50%',
        transform: 'translateX(-50%)',
        width: 1100,
      }}>
        {/* Line */}
        <div style={{
          position: 'absolute', top: 30, left: 60, right: 60, height: 6,
          background: 'rgba(30,32,34,0.2)', borderRadius: 3,
        }} />
        <div style={{
          position: 'absolute', top: 30, left: 60, height: 6,
          width: `${(1100 - 120) * timelineProgress}px`,
          background: '#1E2022', borderRadius: 3,
        }} />

        {/* Year dots */}
        <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}>
          {years.map((year, i) => {
            const dotReveal = sp(70 + i * 15);
            return (
              <div key={year} style={{ textAlign: 'center', opacity: dotReveal }}>
                <div style={{
                  width: 70, height: 70, borderRadius: '50%',
                  background: '#1E2022',
                  border: '5px solid #1E2022',
                  display: 'flex', alignItems: 'center', justifyContent: 'center',
                  margin: '0 auto',
                  transform: `scale(${dotReveal})`,
                }}>
                  <svg width="32" height="32" viewBox="0 0 32 32">
                    <path d="M 8 8 L 24 24 M 24 8 L 8 24" stroke="#FBF5A7" strokeWidth="4" strokeLinecap="round" />
                  </svg>
                </div>
                <div style={{
                  marginTop: 14,
                  fontFamily: 'Space Mono, monospace', fontSize: 36, fontWeight: 700,
                  color: '#1E2022',
                }}>
                  {year}
                </div>
                <div style={{
                  fontFamily: 'Montserrat, sans-serif', fontSize: 19, fontWeight: 600,
                  color: '#1E2022', opacity: 0.7,
                }}>
                  CADUC
                </div>
              </div>
            );
          })}
        </div>
      </div>

      {/* Document card with scan line */}
      <div style={{
        position: 'absolute', bottom: 60, left: '50%',
        transform: `translateX(-50%) scale(${sp(100)})`,
        opacity: sp(100),
      }}>
        <div style={{
          background: '#1E2022',
          color: '#FBF5A7',
          padding: '24px 50px',
          borderRadius: 12,
          fontFamily: 'Bricolage Grotesque, sans-serif',
          fontSize: 47, fontWeight: 700,
          letterSpacing: 1,
          position: 'relative',
          overflow: 'hidden',
          boxShadow: '10px 10px 0 #FBF5A7',
        }}>
          NOUVEAU COEFFICIENT → SIMULATION
          {/* Scan line */}
          <div style={{
            position: 'absolute', left: 0, right: 0,
            top: `${scanY}%`, height: 4,
            background: '#FBF5A7',
            boxShadow: '0 0 20px #FBF5A7',
          }} />
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(30,32,34,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1E2022', opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene34;
```

```tsx
import React from 'react';
Audio: "Prenons un exemple : un appartement de 50 m² chauffé à l'électricité, classé F, dans une ville où le mètre carré tourne autour de 3 000 euros."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Counter
  const priceCounter = Math.round(interpolate(frame, [120, 220], [0, 3000], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));
  const surfaceCounter = Math.round(interpolate(frame, [70, 140], [0, 50], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));

  // Camera orbit
  const camRotate = interpolate(frame, [0, durationInFrames], [-0.5, 0.5]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp, transform: `rotate(${camRotate}deg)` }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{ background: 'linear-gradient(45deg, rgba(251,245,167,0.4) 0%, transparent 60%)' }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: '20%', left: '-8%', width: 500, height: 500,
        background: '#1E2022', opacity: 0.05,
        clipPath: 'polygon(0% 0%, 100% 50%, 0% 100%)',
        transform: `rotate(${frame * 0.15}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '5%', right: '-10%', width: 650, height: 650,
        borderRadius: '50%', border: '4px solid #1E2022', opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />

      {/* Motion texture - blueprint grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.08 }} width="100%" height="100%">
        <defs>
          <pattern id="bp35" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 40 0 L 0 0 0 40" fill="none" stroke="#1E2022" strokeWidth="0.5" />
          </pattern>
          <pattern id="bp35big" width="200" height="200" patternUnits="userSpaceOnUse">
            <rect width="200" height="200" fill="url(#bp35)" />
            <path d="M 200 0 L 0 0 0 200" fill="none" stroke="#1E2022" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#bp35big)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = i * 29;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(seed) * 1920,
            top: seededRandom(seed + 1) * 1080,
            width: 4, height: 4, borderRadius: '50%',
            background: '#1E2022', opacity: 0.3,
            transform: `translate(${Math.sin(frame * 0.03 + i) * 20}px, ${Math.cos(frame * 0.025 + i) * 20}px)`,
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 60, left: '50%',
        transform: `translateX(-50%) scale(${sp(0)})`,
        background: '#1E2022', color: '#FBF5A7',
        padding: '12px 36px', borderRadius: 40,
        fontFamily: 'Space Mono, monospace', fontSize: 26, fontWeight: 700,
        letterSpacing: 4,
      }}>
        EXEMPLE CHIFFRÉ
      </div>

      {/* Headline */}
      <div style={{
        position: 'absolute', top: 150, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(15),
      }}>
        <div style={{
          fontFamily: 'Clash Display, Anton, sans-serif',
          fontSize: 153, fontWeight: 900, color: '#1E2022',
          lineHeight: 1, letterSpacing: -3,
        }}>
          CAS PRATIQUE
        </div>
      </div>

      {/* Three info cards */}
      <div style={{
        position: 'absolute', top: 380, left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex', gap: 40,
      }}>
        {/* Card 1: Surface */}
        <div style={{
          width: 360, height: 380,
          background: '#1E2022',
          borderRadius: 24,
          padding: 36,
          display: 'flex', flexDirection: 'column', justifyContent: 'space-between',
          opacity: sp(50),
          transform: `translateY(${(1 - sp(50)) * 60}px) rotate(${(1 - sp(50)) * -5}deg)`,
          boxShadow: '10px 10px 0 #1E2022',
        }}>
          <div style={{
            fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 700,
            color: '#FBF5A7', letterSpacing: 3,
          }}>
            SURFACE
          </div>
          <svg width="100" height="100" viewBox="0 0 100 100" style={{ margin: '0 auto' }}>
            <rect x="15" y="15" width="70" height="70" stroke="#FBF5A7" strokeWidth="5" fill="none" rx="6" />
            <line x1="15" y1="50" x2="85" y2="50" stroke="#FBF5A7" strokeWidth="2" strokeDasharray="5,3" />
            <line x1="50" y1="15" x2="50" y2="85" stroke="#FBF5A7" strokeWidth="2" strokeDasharray="5,3" />
          </svg>
          <div>
            <div style={{
              fontFamily: 'Space Mono, monospace', fontSize: 102, fontWeight: 700,
              color: '#FBF5A7', lineHeight: 1,
            }}>
              {surfaceCounter}
            </div>
            <div style={{
              fontFamily: 'Bricolage Grotesque, sans-serif', fontSize: 38, fontWeight: 600,
              color: '#F4CE14',
            }}>
              MÈTRES CARRÉS
            </div>
          </div>
        </div>

        {/* Card 2: DPE F */}
        <div style={{
          width: 360, height: 380,
          background: '#FBF5A7',
          border: '5px solid #1E2022',
          borderRadius: 24,
          padding: 36,
          display: 'flex', flexDirection: 'column', justifyContent: 'space-between',
          opacity: sp(70),
          transform: `translateY(${(1 - sp(70)) * 60}px) scale(${sp(70)})`,
          boxShadow: '10px 10px 0 #1E2022',
        }}>
          <div style={{
            fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 700,
            color: '#1E2022', letterSpacing: 3,
          }}>
            CLASSE DPE
          </div>
          <div style={{
            fontFamily: 'Anton, sans-serif', fontSize: 246, fontWeight: 900,
            color: '#1E2022', textAlign: 'center', lineHeight: 1,
          }}>
            F
          </div>
          <div style={{
            fontFamily: 'Bricolage Grotesque, sans-serif', fontSize: 28, fontWeight: 600,
            color: '#1E2022', textAlign: 'center',
            background: '#1E2022', color: '#FBF5A7',
            padding: '8px 16px', borderRadius: 8,
          }}>
            ÉLECTRIQUE
          </div>
        </div>

        {/* Card 3: Price */}
        <div style={{
          width: 360, height: 380,
          background: '#1E2022',
          borderRadius: 24,
          padding: 36,
          display: 'flex', flexDirection: 'column', justifyContent: 'space-between',
          opacity: sp(90),
          transform: `translateY(${(1 - sp(90)) * 60}px) rotate(${(1 - sp(90)) * 5}deg)`,
          boxShadow: '10px 10px 0 #1E2022',
        }}>
          <div style={{
            fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 700,
            color: '#FBF5A7', letterSpacing: 3,
          }}>
            PRIX AU M²
          </div>
          <svg width="100" height="100" viewBox="0 0 100 100" style={{ margin: '0 auto' }}>
            <circle cx="50" cy="50" r="35" stroke="#FBF5A7" strokeWidth="5" fill="none" />
            <text x="50" y="65" textAnchor="middle" fontFamily="Anton, sans-serif" fontSize="42" fontWeight="900" fill="#FBF5A7">€</text>
          </svg>
          <div>
            <div style={{
              fontFamily: 'Space Mono, monospace', fontSize: 85, fontWeight: 700,
              color: '#FBF5A7', lineHeight: 1,
            }}>
              {priceCounter.toLocaleString('fr-FR')}
            </div>
            <div style={{
              fontFamily: 'Bricolage Grotesque, sans-serif', fontSize: 36, fontWeight: 600,
              color: '#F4CE14',
            }}>
              EUROS / M²
            </div>
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(30,32,34,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1E2022', opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene35;
```

```tsx
import React from 'react';
Audio: "Avec le passage du coefficient de 2,3 à 1,9, ce même appartement peut remonter en E, voire frôler le D. Il sort de la zone de danger réglementaire."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene36: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Coefficient number morph 2.3 -> 1.9
  const coefMorph = interpolate(frame, [50, 130], [2.3, 1.9], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  
  // Letter F -> E -> D animation
  const letterProgress = interpolate(frame, [140, 240], [0, 2], { extrapolateRight: 'clamp', easing: Easing.bezier(0.34, 1.56, 0.64, 1) });
  const currentLetter = letterProgress < 1 ? 'F' : letterProgress < 1.8 ? 'E' : 'D';
  const letterScale = 1 + Math.sin(frame * 0.15) * 0.04;

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp, transform: `scale(${camScale})` }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 60%, rgba(251,245,167,0.5) 0%, transparent 60%)' }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: '-15%', left: '-15%', width: 700, height: 700,
        borderRadius: '50%', background: '#1E2022', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', top: '20%', right: '-10%', width: 500, height: 500,
        border: '5px solid #1E2022', opacity: 0.06,
        clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
        transform: `rotate(${frame * 0.2}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-10%', right: '20%', width: 400, height: 400,
        background: '#1E2022', opacity: 0.04, borderRadius: 30,
        transform: `rotate(${-frame * 0.15}deg)`,
      }} />

      {/* Motion texture - circular pulses */}
      {[0, 1, 2].map(i => (
        <div key={i} style={{
          position: 'absolute', top: '50%', left: '50%',
          width: 400 + i * 200, height: 400 + i * 200,
          marginLeft: -(200 + i * 100), marginTop: -(200 + i * 100),
          border: '2px solid #1E2022',
          borderRadius: '50%',
          opacity: 0.05 + Math.sin(frame * 0.04 + i) * 0.04,
        }} />
      ))}

      {/* Floating particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const seed = i * 31;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(seed) * 1920 + Math.sin(frame * 0.03 + i) * 25,
            top: seededRandom(seed + 1) * 1080,
            width: 5, height: 5, borderRadius: '50%',
            background: '#FBF5A7', opacity: 0.6,
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 60, left: '50%',
        transform: `translateX(-50%) scale(${sp(0)})`,
        background: '#1E2022', color: '#FBF5A7',
        padding: '12px 36px', borderRadius: 40,
        fontFamily: 'Space Mono, monospace', fontSize: 26, fontWeight: 700,
        letterSpacing: 4,
      }}>
        NOUVEAU COEFFICIENT
      </div>

      {/* Coefficient transformation */}
      <div style={{
        position: 'absolute', top: 180, left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex', alignItems: 'center', gap: 50,
        opacity: sp(20),
      }}>
        {/* Old */}
        <div style={{ textAlign: 'center' }}>
          <div style={{
            fontFamily: 'Montserrat, sans-serif', fontSize: 24, fontWeight: 600,
            color: '#1E2022', letterSpacing: 3, marginBottom: 8,
          }}>
            AVANT
          </div>
          <div style={{
            fontFamily: 'Space Mono, monospace', fontSize: 153, fontWeight: 700,
            color: '#1E2022', lineHeight: 1,
            textDecoration: frame > 100 ? 'line-through' : 'none',
            opacity: frame > 130 ? 0.4 : 1,
          }}>
            2,3
          </div>
        </div>

        {/* Arrow */}
        <svg width="120" height="60" style={{ opacity: sp(40) }}>
          <path d="M 10 30 L 100 30" stroke="#1E2022" strokeWidth="8" strokeLinecap="round" />
          <path d="M 85 15 L 110 30 L 85 45" stroke="#1E2022" strokeWidth="8" strokeLinecap="round" strokeLinejoin="round" fill="none" />
        </svg>

        {/* New */}
        <div style={{ textAlign: 'center' }}>
          <div style={{
            fontFamily: 'Montserrat, sans-serif', fontSize: 24, fontWeight: 600,
            color: '#1E2022', letterSpacing: 3, marginBottom: 8,
          }}>
            APRÈS
          </div>
          <div style={{
            fontFamily: 'Space Mono, monospace', fontSize: 187, fontWeight: 700,
            color: '#1E2022',
            background: '#FBF5A7',
            padding: '0 24px', borderRadius: 12,
            lineHeight: 1,
            boxShadow: '8px 8px 0 #1E2022',
            transform: `scale(${1 + Math.sin(frame * 0.1) * 0.02})`,
          }}>
            {coefMorph.toFixed(1).replace('.', ',')}
          </div>
        </div>
      </div>

      {/* Letter progression */}
      <div style={{
        position: 'absolute', bottom: 180, left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex', alignItems: 'center', gap: 40,
        opacity: sp(120),
      }}>
        {['F', 'E', 'D'].map((letter, i) => {
          const isActive = (i === 0 && letterProgress < 1) || 
                          (i === 1 && letterProgress >= 1 && letterProgress < 1.8) || 
                          (i === 2 && letterProgress >= 1.8);
          return (
            <React.Fragment key={letter}>
              <div style={{
                width: 130, height: 130,
                background: isActive ? '#1E2022' : 'rgba(30,32,34,0.15)',
                border: '5px solid #1E2022',
                borderRadius: 20,
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                fontFamily: 'Anton, sans-serif', fontSize: 85, fontWeight: 900,
                color: isActive ? '#FBF5A7' : '#1E2022',
                transform: isActive ? `scale(${letterScale})` : 'scale(1)',
                boxShadow: isActive ? '0 0 30px rgba(30,32,34,0.4)' : 'none',
              }}>
                {letter}
              </div>
              {i < 2 && (
                <div style={{
                  fontFamily: 'Anton, sans-serif', fontSize: 51,
                  color: '#1E2022',
                }}>
                  →
                </div>
              )}
            </React.Fragment>
          );
        })}
      </div>

      {/* Bottom banner - exit danger zone */}
      <div style={{
        position: 'absolute', bottom: 60, left: '50%',
        transform: `translateX(-50%) scale(${sp(200)})`,
        opacity: sp(200),
        background: '#1E2022', color: '#FBF5A7',
        padding: '16px 40px', borderRadius: 8,
        fontFamily: 'Anton, sans-serif', fontSize: 47, fontWeight: 900,
        letterSpacing: 3,
        boxShadow: '8px 8px 0 #FBF5A7',
      }}>
        SORTIE DE ZONE DE DANGER
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(30,32,34,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1E2022', opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene36;
```

```tsx
import React from 'react';
Audio: "Avant de paniquer et de signer un devis à 30 000 euros, faites d'abord recalculer votre diagnostic. Vous pourriez découvrir que le problème s'est résolu tout seul."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene37: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // 30K counter that gets crossed out
  const priceCount = Math.round(interpolate(frame, [40, 110], [0, 30000], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) }));
  const crossOut = interpolate(frame, [130, 170], [0, 1], { extrapolateRight: 'clamp' });
  
  // Stamp impact
  const stampScale = spring({ frame: Math.max(0, frame - 180), fps, config: { damping: 6, mass: 0.5, stiffness: 200 } });
  
  // Camera shake on stamp
  const shake = (frame > 180 && frame < 200) ? Math.sin(frame * 1.5) * (1 - (frame - 180) / 20) * 5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp, transform: `translate(${shake}px, ${shake}px)` }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{ background: 'linear-gradient(225deg, rgba(251,245,167,0.4) 0%, transparent 60%)' }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: '10%', right: '-10%', width: 600, height: 600,
        borderRadius: '50%', background: '#1E2022', opacity: 0.05,
      }} />
      <div style={{
        position: 'absolute', bottom: '-15%', left: '-10%', width: 700, height: 700,
        border: '5px solid #1E2022', opacity: 0.06,
        borderRadius: 50,
        transform: `rotate(${frame * 0.1}deg)`,
      }} />

      {/* Motion texture - cross hatch */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="cross37" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 0 0 L 40 40 M 40 0 L 0 40" stroke="#1E2022" strokeWidth="0.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#cross37)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 26 }).map((_, i) => {
        const seed = i * 37;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(seed) * 1920,
            top: seededRandom(seed + 1) * 1080 + Math.sin(frame * 0.03 + i) * 20,
            width: 4, height: 4, borderRadius: '50%',
            background: '#1E2022', opacity: 0.3,
          }} />
        );
      })}

      {/* Warning header */}
      <div style={{
        position: 'absolute', top: 70, left: '50%',
        transform: `translateX(-50%) scale(${sp(0)})`,
        background: '#1E2022', color: '#FBF5A7',
        padding: '14px 40px', borderRadius: 50,
        fontFamily: 'Anton, sans-serif', fontSize: 36, fontWeight: 900,
        letterSpacing: 4,
      }}>
        AVANT DE PANIQUER
      </div>

      {/* Big price display */}
      <div style={{
        position: 'absolute', top: 200, left: '50%',
        transform: 'translateX(-50%)',
        textAlign: 'center',
        opacity: sp(15),
      }}>
        <div style={{
          fontFamily: 'Bricolage Grotesque, sans-serif', fontSize: 38, fontWeight: 600,
          color: '#1E2022', letterSpacing: 3, marginBottom: 12,
        }}>
          DEVIS DE RÉNOVATION
        </div>
        <div style={{ position: 'relative', display: 'inline-block' }}>
          <div style={{
            fontFamily: 'Space Mono, monospace', fontSize: 263, fontWeight: 700,
            color: '#1E2022', lineHeight: 1,
            background: '#FBF5A7',
            padding: '0 40px', borderRadius: 16,
            boxShadow: '14px 14px 0 #1E2022',
          }}>
            {priceCount.toLocaleString('fr-FR')}€
          </div>
          {/* Cross out line */}
          <div style={{
            position: 'absolute', top: '50%', left: 0,
            width: `${crossOut * 100}%`, height: 14,
            background: '#1E2022',
            transform: 'translateY(-50%) rotate(-5deg)',
            transformOrigin: 'left',
            borderRadius: 7,
          }} />
        </div>
      </div>

      {/* Solution stamp */}
      <div style={{
        position: 'absolute', bottom: 160, left: '50%',
        transform: `translateX(-50%) scale(${stampScale}) rotate(${(1 - stampScale) * -15}deg)`,
        opacity: stampScale,
      }}>
        <div style={{
          background: '#1E2022',
          color: '#FBF5A7',
          padding: '24px 60px',
          borderRadius: 20,
          fontFamily: 'Anton, sans-serif',
          fontSize: 102, fontWeight: 900,
          letterSpacing: 2,
          border: '6px dashed #FBF5A7',
          boxShadow: '12px 12px 0 #FBF5A7',
        }}>
          RECALCULEZ D'ABORD
        </div>
      </div>

      {/* Sub text */}
      <div style={{
        position: 'absolute', bottom: 60, left: '50%',
        transform: 'translateX(-50%)',
        opacity: sp(200),
        fontFamily: 'Bricolage Grotesque, sans-serif',
        fontSize: 42, fontWeight: 600, color: '#1E2022',
        textAlign: 'center',
      }}>
        Le problème peut s'être résolu seul
      </div>

      {/* Side icons - calculator */}
      <div style={{
        position: 'absolute', top: 240, left: 120,
        opacity: sp(60),
        transform: `scale(${sp(60)}) rotate(${Math.sin(frame * 0.05) * 5}deg)`,
      }}>
        <svg width="120" height="140" viewBox="0 0 120 140">
          <rect x="10" y="10" width="100" height="120" rx="10" fill="#1E2022" />
          <rect x="20" y="20" width="80" height="25" rx="4" fill="#FBF5A7" />
          {[0, 1, 2].map(r => 
            [0, 1, 2].map(c => (
              <rect key={`${r}-${c}`} x={22 + c * 28} y={55 + r * 22} width="22" height="16" rx="3" fill="#FBF5A7" />
            ))
          )}
        </svg>
      </div>

      {/* Side icons - magnifying glass */}
      <div style={{
        position: 'absolute', top: 240, right: 120,
        opacity: sp(80),
        transform: `scale(${sp(80)}) rotate(${-Math.sin(frame * 0.05) * 5}deg)`,
      }}>
        <svg width="120" height="120" viewBox="0 0 120 120">
          <circle cx="48" cy="48" r="32" stroke="#1E2022" strokeWidth="8" fill="#FBF5A7" />
          <line x1="72" y1="72" x2="105" y2="105" stroke="#1E2022" strokeWidth="12" strokeLinecap="round" />
        </svg>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(30,32,34,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1E2022', opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene37;
```

```tsx
import React from 'react';
Audio: "Mais ce coup de pouce ne sauve pas tout le monde. Si votre bien est une vraie passoire, aucun ajustement ne le sortira de la catégorie G."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene38: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera slight zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1, 1.03]);

  // Heat leak animation - rising waves
  const wave1 = Math.sin(frame * 0.1) * 8;
  const wave2 = Math.sin(frame * 0.12 + 1) * 8;
  const wave3 = Math.sin(frame * 0.08 + 2) * 8;

  // G letter big pulse
  const gPulse = 1 + Math.sin(frame * 0.12) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp, transform: `scale(${camScale})` }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 100%, rgba(30,32,34,0.2) 0%, transparent 60%)' }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: '-10%', left: '20%', width: 600, height: 600,
        background: '#1E2022', opacity: 0.05,
        clipPath: 'polygon(50% 0%, 100% 100%, 0% 100%)',
        transform: `rotate(${frame * 0.1}deg)`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-15%', right: '-10%', width: 700, height: 700,
        borderRadius: '50%', border: '5px solid #1E2022', opacity: 0.06,
      }} />

      {/* Motion texture - vertical drift lines */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
        <defs>
          <pattern id="vlines38" width="60" height="100" patternUnits="userSpaceOnUse" patternTransform={`translate(0, ${-frame * 0.6})`}>
            <line x1="30" y1="0" x2="30" y2="100" stroke="#1E2022" strokeWidth="1" strokeDasharray="20,10" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#vlines38)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const seed = i * 41;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(seed) * 1920 + Math.sin(frame * 0.04 + i) * 25,
            top: seededRandom(seed + 1) * 1080,
            width: 5, height: 5, borderRadius: '50%',
            background: '#1E2022', opacity: 0.3,
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 60, left: '50%',
        transform: `translateX(-50%) scale(${sp(0)})`,
        background: '#1E2022', color: '#FBF5A7',
        padding: '14px 36px', borderRadius: 40,
        fontFamily: 'Space Mono, monospace', fontSize: 26, fontWeight: 700,
        letterSpacing: 4,
      }}>
        VRAIE PASSOIRE
      </div>

      {/* Headline */}
      <div style={{
        position: 'absolute', top: 160, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(15),
      }}>
        <div style={{
          fontFamily: 'Clash Display, Anton, sans-serif',
          fontSize: 119, fontWeight: 900, color: '#1E2022',
          lineHeight: 1, letterSpacing: -2,
        }}>
          AUCUN AJUSTEMENT
        </div>
        <div style={{
          fontFamily: 'Clash Display, Anton, sans-serif',
          fontSize: 153, fontWeight: 900, color: '#1E2022',
          lineHeight: 1, letterSpacing: -3,
          textShadow: '7px 7px 0 #FBF5A7',
        }}>
          NE LA SAUVERA
        </div>
      </div>

      {/* House with heat leaks visualization */}
      <div style={{
        position: 'absolute', bottom: 90, left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex', alignItems: 'flex-end', gap: 80,
        opacity: sp(60),
      }}>
        {/* House SVG */}
        <div style={{ position: 'relative' }}>
          <svg width="320" height="320" viewBox="0 0 320 320">
            {/* House body */}
            <rect x="50" y="140" width="220" height="160" fill="#1E2022" />
            {/* Roof */}
            <polygon points="30,140 160,40 290,140" fill="#1E2022" />
            {/* Door */}
            <rect x="140" y="220" width="40" height="80" fill="#F4CE14" />
            {/* Window */}
            <rect x="80" y="170" width="40" height="40" fill="#FBF5A7" stroke="#1E2022" strokeWidth="3" />
            <rect x="200" y="170" width="40" height="40" fill="#FBF5A7" stroke="#1E2022" strokeWidth="3" />
            
            {/* Heat leak waves - top */}
            <path d={`M 100 40 Q ${110 + wave1} 20 120 40 Q ${130 + wave2} 20 140 40`} stroke="#1E2022" strokeWidth="4" fill="none" strokeLinecap="round" opacity="0.7" />
            <path d={`M 180 40 Q ${190 + wave2} 20 200 40 Q ${210 + wave3} 20 220 40`} stroke="#1E2022" strokeWidth="4" fill="none" strokeLinecap="round" opacity="0.7" />
            
            {/* Heat waves left */}
            <path d={`M ${30 + wave1} 180 Q ${15 + wave2} 190 ${30 + wave3} 200 Q ${15 + wave1} 210 ${30 + wave2} 220`} stroke="#1E2022" strokeWidth="4" fill="none" strokeLinecap="round" opacity="0.6" />
            
            {/* Heat waves right */}
            <path d={`M ${290 + wave2} 180 Q ${305 + wave1} 190 ${290 + wave3} 200 Q ${305 + wave2} 210 ${290 + wave1} 220`} stroke="#1E2022" strokeWidth="4" fill="none" strokeLinecap="round" opacity="0.6" />
          </svg>
          <div style={{
            position: 'absolute', bottom: -30, left: 0, right: 0,
            textAlign: 'center',
            fontFamily: 'Montserrat, sans-serif', fontSize: 24, fontWeight: 700,
            color: '#1E2022', letterSpacing: 2,
          }}>
            PASSOIRE THERMIQUE
          </div>
        </div>

        {/* G letter giant */}
        <div style={{
          opacity: sp(100),
          transform: `scale(${sp(100) * gPulse})`,
        }}>
          <div style={{
            width: 280, height: 280,
            background: '#1E2022',
            border: '8px solid #1E2022',
            borderRadius: 30,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: 'Anton, sans-serif', fontSize: 230, fontWeight: 900,
            color: '#FBF5A7',
            boxShadow: '14px 14px 0 #FBF5A7',
            position: 'relative',
          }}>
            G
            {/* Lock icon on top */}
            <div style={{
              position: 'absolute', top: -30, right: -30,
              width: 80, height: 80,
              background: '#FBF5A7',
              border: '5px solid #1E2022',
              borderRadius: '50%',
              display: 'flex', alignItems: 'center', justifyContent: 'center',
              transform: `scale(${sp(140)})`,
            }}>
              <svg width="40" height="40" viewBox="0 0 40 40">
                <rect x="10" y="18" width="20" height="16" rx="2" fill="#1E2022" />
                <path d="M 14 18 V 12 Q 14 6 20 6 Q 26 6 26 12 V 18" stroke="#1E2022" strokeWidth="3" fill="none" />
              </svg>
            </div>
          </div>
          <div style={{
            marginTop: 18, textAlign: 'center',
            fontFamily: 'Anton, sans-serif', fontSize: 38, fontWeight: 900,
            color: '#1E2022', letterSpacing: 2,
          }}>
            VERROUILLÉ
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(30,32,34,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1E2022', opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene38;
```

```tsx
import React from 'react';
Audio: "Il faut alors calculer froidement le retour sur investissement d'une rénovation. La plupart se trompent : soit en surinvestissant, soit en ne faisant rien."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39',
  durationInSeconds: 18,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene39: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera pan
  const camX = interpolate(frame, [0, durationInFrames], [-10, 10]);

  // ROI Gauge fill
  const gaugeProgress = interpolate(frame, [60, 180], [0, 0.65], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp, transform: `translateX(${camX}px)` }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{ background: 'linear-gradient(180deg, transparent 0%, rgba(251,245,167,0.3) 100%)' }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: '10%', left: '5%', width: 500, height: 500,
        borderRadius: '50%', border: '5px solid #1E2022', opacity: 0.06,
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '5%', right: '8%', width: 450, height: 450,
        background: '#1E2022', opacity: 0.05, borderRadius: 60,
        transform: `rotate(${frame * 0.15}deg)`,
      }} />

      {/* Motion texture - chart grid */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.07 }} width="100%" height="100%">
        <defs>
          <pattern id="chart39" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 80 0 L 0 0 0 80" fill="none" stroke="#1E2022" strokeWidth="0.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#chart39)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const seed = i * 43;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(seed) * 1920,
            top: seededRandom(seed + 1) * 1080,
            width: 4, height: 4, borderRadius: '50%',
            background: '#FBF5A7', opacity: 0.5,
            transform: `translateY(${Math.sin(frame * 0.04 + i) * 20}px)`,
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 60, left: '50%',
        transform: `translateX(-50%) scale(${sp(0)})`,
        background: '#1E2022', color: '#FBF5A7',
        padding: '14px 36px', borderRadius: 40,
        fontFamily: 'Space Mono, monospace', fontSize: 26, fontWeight: 700,
        letterSpacing: 4,
      }}>
        CALCUL FROID
      </div>

      {/* Headline */}
      <div style={{
        position: 'absolute', top: 150, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(15),
      }}>
        <div style={{
          fontFamily: 'Clash Display, Anton, sans-serif',
          fontSize: 178, fontWeight: 900, color: '#1E2022',
          lineHeight: 1, letterSpacing: -3,
        }}>
          ROI RÉNOVATION
        </div>
      </div>

      {/* ROI Gauge in the middle */}
      <div style={{
        position: 'absolute', top: 400, left: '50%',
        transform: 'translateX(-50%)',
        opacity: sp(50),
      }}>
        <svg width="500" height="280" viewBox="0 0 500 280">
          {/* Gauge background */}
          <path d="M 50 230 A 200 200 0 0 1 450 230" stroke="rgba(30,32,34,0.15)" strokeWidth="40" fill="none" strokeLinecap="round" />
          {/* Gauge active */}
          <path 
            d="M 50 230 A 200 200 0 0 1 450 230" 
            stroke="#1E2022" strokeWidth="40" fill="none" strokeLinecap="round"
            strokeDasharray="628"
            strokeDashoffset={628 - 628 * gaugeProgress * 0.5}
          />
          {/* Needle */}
          <line 
            x1="250" y1="230" 
            x2={250 + Math.cos((Math.PI * (1 - gaugeProgress))) * 170} 
            y2={230 - Math.sin((Math.PI * (1 - gaugeProgress))) * 170}
            stroke="#FBF5A7" strokeWidth="10" strokeLinecap="round"
          />
          <circle cx="250" cy="230" r="20" fill="#1E2022" />
          <circle cx="250" cy="230" r="10" fill="#FBF5A7" />
          
          {/* Labels */}
          <text x="50" y="270" textAnchor="middle" fontFamily="Anton, sans-serif" fontSize="32" fill="#1E2022">PERTE</text>
          <text x="250" y="60" textAnchor="middle" fontFamily="Anton, sans-serif" fontSize="32" fill="#1E2022">ÉQUILIBRE</text>
          <text x="450" y="270" textAnchor="middle" fontFamily="Anton, sans-serif" fontSize="32" fill="#1E2022">GAIN</text>
        </svg>
      </div>

      {/* Two mistake cards left and right */}
      <div style={{
        position: 'absolute', bottom: 130, left: 100,
        opacity: sp(120),
        transform: `translateX(${(1 - sp(120)) * -50}px)`,
      }}>
        <div style={{
          width: 380, padding: 28,
          background: '#1E2022', color: '#FBF5A7',
          borderRadius: 20,
          boxShadow: '10px 10px 0 #FBF5A7',
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif', fontSize: 64, fontWeight: 900,
            color: '#F4CE14', lineHeight: 1,
          }}>
            ERREUR 1
          </div>
          <div style={{
            marginTop: 12,
            fontFamily: 'Bricolage Grotesque, sans-serif', fontSize: 36, fontWeight: 700,
          }}>
            SURINVESTIR
          </div>
          <div style={{
            marginTop: 8,
            fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 500,
            opacity: 0.8,
          }}>
            Travaux trop chers, jamais rentabilisés
          </div>
        </div>
      </div>

      <div style={{
        position: 'absolute', bottom: 130, right: 100,
        opacity: sp(140),
        transform: `translateX(${(1 - sp(140)) * 50}px)`,
      }}>
        <div style={{
          width: 380, padding: 28,
          background: '#FBF5A7',
          border: '5px solid #1E2022',
          borderRadius: 20,
          boxShadow: '10px 10px 0 #1E2022',
        }}>
          <div style={{
            fontFamily: 'Anton, sans-serif', fontSize: 64, fontWeight: 900,
            color: '#1E2022', lineHeight: 1,
          }}>
            ERREUR 2
          </div>
          <div style={{
            marginTop: 12,
            fontFamily: 'Bricolage Grotesque, sans-serif', fontSize: 36, fontWeight: 700,
            color: '#1E2022',
          }}>
            NE RIEN FAIRE
          </div>
          <div style={{
            marginTop: 8,
            fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 500,
            color: '#1E2022', opacity: 0.8,
          }}>
            Bien dévalorisé, interdit à la location
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(30,32,34,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1E2022', opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene39;
```

```tsx
import React from 'react';
Audio: "La bonne approche : raisonner par gestes prioritaires. L'isolation des combles est souvent le geste le plus rentable, un coût modéré pour un gain significatif."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40',
  durationInSeconds: 17,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene40: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1]);

  // Cost vs Gain bar animation
  const costBar = interpolate(frame, [80, 160], [0, 0.3], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });
  const gainBar = interpolate(frame, [110, 200], [0, 0.85], { extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic) });

  // Insulation lines drop animation
  const insulationY = interpolate(frame, [60, 130], [-50, 0], { extrapolateRight: 'clamp', easing: Easing.out(Easing.bounce) });

  return (
    <AbsoluteFill style={{ backgroundColor: '#F4CE14', overflow: 'hidden', opacity: exitOp, transform: `scale(${camScale})` }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{ background: 'radial-gradient(circle at 50% 30%, rgba(251,245,167,0.4) 0%, transparent 60%)' }} />

      {/* Shape Layers */}
      <div style={{
        position: 'absolute', top: '5%', right: '-10%', width: 600, height: 600,
        borderRadius: '50%', background: '#1E2022', opacity: 0.05,
        transform: `scale(${1 + Math.sin(frame * 0.025) * 0.05})`,
      }} />
      <div style={{
        position: 'absolute', bottom: '-10%', left: '-10%', width: 600, height: 600,
        border: '5px solid #1E2022', opacity: 0.06,
        transform: `rotate(${frame * 0.12}deg)`,
      }} />

      {/* Motion texture - hexagonal pattern */}
      <svg style={{ position: 'absolute', inset: 0, opacity: 0.06 }} width="100%" height="100%">
        <defs>
          <pattern id="hex40" width="60" height="52" patternUnits="userSpaceOnUse">
            <polygon points="30,2 55,15 55,40 30,52 5,40 5,15" fill="none" stroke="#1E2022" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hex40)" />
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 24 }).map((_, i) => {
        const seed = i * 47;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: seededRandom(seed) * 1920,
            top: seededRandom(seed + 1) * 1080 + Math.sin(frame * 0.04 + i) * 20,
            width: 5, height: 5, borderRadius: '50%',
            background: '#1E2022', opacity: 0.3,
          }} />
        );
      })}

      {/* Top label */}
      <div style={{
        position: 'absolute', top: 60, left: '50%',
        transform: `translateX(-50%) scale(${sp(0)})`,
        background: '#1E2022', color: '#FBF5A7',
        padding: '12px 36px', borderRadius: 40,
        fontFamily: 'Space Mono, monospace', fontSize: 26, fontWeight: 700,
        letterSpacing: 4,
      }}>
        GESTE N°1 PRIORITAIRE
      </div>

      {/* Headline */}
      <div style={{
        position: 'absolute', top: 145, left: 0, right: 0,
        textAlign: 'center',
        opacity: sp(15),
      }}>
        <div style={{
          fontFamily: 'Clash Display, Anton, sans-serif',
          fontSize: 119, fontWeight: 900, color: '#1E2022',
          lineHeight: 1, letterSpacing: -2,
        }}>
          ISOLATION
        </div>
        <div style={{
          fontFamily: 'Clash Display, Anton, sans-serif',
          fontSize: 178, fontWeight: 900, color: '#1E2022',
          lineHeight: 1, letterSpacing: -3,
          background: '#FBF5A7',
          display: 'inline-block', padding: '0 30px',
          boxShadow: '10px 10px 0 #1E2022',
        }}>
          DES COMBLES
        </div>
      </div>

      {/* House cross-section with insulation */}
      <div style={{
        position: 'absolute', bottom: 100, left: 140,
        opacity: sp(50),
      }}>
        <svg width="380" height="380" viewBox="0 0 380 380">
          {/* House outline */}
          <polygon points="190,40 30,180 30,360 350,360 350,180" stroke="#1E2022" strokeWidth="6" fill="#FBF5A7" />
          {/* Roof line */}
          <line x1="30" y1="180" x2="350" y2="180" stroke="#1E2022" strokeWidth="4" strokeDasharray="10,5" />
          
          {/* Insulation in attic */}
          <g transform={`translate(0, ${insulationY})`}>
            {[0, 1, 2, 3, 4, 5].map(i => (
              <path 
                key={i}
                d={`M ${50 + i * 50} 180 Q ${75 + i * 50} 160 ${100 + i * 50} 180`}
                stroke="#1E2022" strokeWidth="5" fill="none"
              />
            ))}
          </g>
          
          {/* Door */}
          <rect x="160" y="280" width="60" height="80" fill="#1E2022" />
          {/* Window */}
          <rect x="80" y="240" width="50" height="50" fill="#1E2022" />
          <rect x="250" y="240" width="50" height="50" fill="#1E2022" />
          
          {/* Heat saved arrows up - prevented */}
          {[100, 190, 280].map((x, i) => {
            const op = sp(140 + i * 10);
            return (
              <g key={x} opacity={op}>
                <circle cx={x} cy="90" r="20" fill="#1E2022" />
                <path d={`M ${x - 10} 95 L ${x - 10} 85 L ${x + 10} 85 L ${x + 10} 95 M ${x - 5} 85 L ${x + 5} 75`} stroke="#FBF5A7" strokeWidth="3" fill="none" strokeLinecap="round" />
                <text x={x} y={105} textAnchor="middle" fontFamily="Anton, sans-serif" fontSize="24" fill="#FBF5A7">X</text>
              </g>
            );
          })}
        </svg>
      </div>

      {/* Cost vs Gain comparison */}
      <div style={{
        position: 'absolute', bottom: 130, right: 140,
        width: 500,
      }}>
        {/* Cost row */}
        <div style={{ marginBottom: 30, opacity: sp(70) }}>
          <div style={{
            fontFamily: 'Montserrat, sans-serif', fontSize: 28, fontWeight: 700,
            color: '#1E2022', marginBottom: 8, letterSpacing: 2,
          }}>
            COÛT
          </div>
          <div style={{
            height: 60, background: 'rgba(30,32,34,0.15)', borderRadius: 10, overflow: 'hidden',
            border: '4px solid #1E2022',
          }}>
            <div style={{
              width: `${costBar * 100}%`, height: '100%',
              background: '#1E2022',
              display: 'flex', alignItems: 'center', justifyContent: 'flex-end',
              padding: '0 16px',
              fontFamily: 'Space Mono, monospace', fontSize: 30, fontWeight: 700,
              color: '#FBF5A7',
            }}>
              MODÉRÉ
            </div>
          </div>
        </div>

        {/* Gain row */}
        <div style={{ opacity: sp(100) }}>
          <div style={{
            fontFamily: 'Montserrat, sans-serif', fontSize: 28, fontWeight: 700,
            color: '#1E2022', marginBottom: 8, letterSpacing: 2,
          }}>
            GAIN ÉNERGÉTIQUE
          </div>
          <div style={{
            height: 60, background: 'rgba(30,32,34,0.15)', borderRadius: 10, overflow: 'hidden',
            border: '4px solid #1E2022',
          }}>
            <div style={{
              width: `${gainBar * 100}%`, height: '100%',
              background: '#FBF5A7',
              display: 'flex', alignItems: 'center', justifyContent: 'flex-end',
              padding: '0 16px',
              fontFamily: 'Space Mono, monospace', fontSize: 30, fontWeight: 700,
              color: '#1E2022',
            }}>
              SIGNIFICATIF
            </div>
          </div>
        </div>

        {/* ROI stamp */}
        <div style={{
          marginTop: 30,
          opacity: sp(180),
          transform: `scale(${sp(180)})`,
          background: '#1E2022', color: '#FBF5A7',
          padding: '16px 24px', borderRadius: 12,
          fontFamily: 'Anton, sans-serif', fontSize: 47, fontWeight: 900,
          textAlign: 'center', letterSpacing: 3,
          boxShadow: '8px 8px 0 #FBF5A7',
        }}>
          ROI MAXIMAL
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(30,32,34,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Montserrat, sans-serif', fontSize: 22, fontWeight: 600,
        color: '#1E2022', opacity: 0.5,
      }}>
        Matures Finance
      </div>
    </AbsoluteFill>
  );
};

export default Scene40;
```

