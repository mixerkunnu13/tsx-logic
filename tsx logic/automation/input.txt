```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'RoutineFuelTrap',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const RoutineFuelTrap: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    }
  );

  const cameraZoom = interpolate(
    frame,
    [0, durationInFrames],
    [1.02, 1.09],
    {
      easing: Easing.inOut(Easing.quad),
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    }
  );

  const fuelLevel = interpolate(frame, [95, 470], [0, 252], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const needle = interpolate(frame, [110, 420, 760], [-62, 44, 28], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const impactShake =
    frame > 405 && frame < 430
      ? Math.sin((frame - 405) * 2.8) * (1 - (frame - 405) / 25) * 12
      : 0;

  const nozzleEntry = sp(40, {damping: 10, stiffness: 150});
  const carEntry = sp(70, {damping: 14});
  const questionEntry = sp(310, {damping: 9, stiffness: 170});

  const particles = Array.from({length: 32}, (_, index) => {
    const x = seededRandom(index + 10) * 1920;
    const baseY = seededRandom(index + 80) * 1080;
    const size = 3 + seededRandom(index + 120) * 8;
    const speed = 0.18 + seededRandom(index + 160) * 0.35;
    const y = (baseY - frame * speed + 1300) % 1300;

    return (
      <div
        key={index}
        style={{
          position: 'absolute',
          left: x,
          top: y - 110,
          width: size,
          height: size,
          borderRadius: '50%',
          backgroundColor: index % 3 === 0 ? '#F14A00' : '#EEEEEE',
          opacity: 0.12 + seededRandom(index + 200) * 0.22,
          boxShadow: index % 4 === 0 ? '0 0 18px #F14A00' : 'none',
        }}
      />
    );
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#8E1616',
        overflow: 'hidden',
        fontFamily: "'Noto Sans Devanagari', 'Inter', sans-serif",
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at ${
            22 + Math.sin(frame * 0.012) * 8
          }% ${36 + Math.cos(frame * 0.01) * 7}%, #F14A0066 0%, transparent 31%),
          radial-gradient(circle at 85% 72%, #FF8A3D36 0%, transparent 34%),
          linear-gradient(132deg, #8E1616 0%, #641010 48%, #A41D18 100%)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.16,
          backgroundImage:
            'repeating-linear-gradient(118deg, transparent 0px, transparent 39px, #EEEEEE 40px, transparent 42px)',
          transform: `translateX(${(frame * 0.45) % 42}px)`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          width: 560,
          height: 560,
          border: '2px solid #EEEEEE',
          borderRadius: '50%',
          opacity: 0.055,
          left: -170,
          top: -190,
          transform: `rotate(${frame * 0.08}deg) scale(${
            1 + Math.sin(frame * 0.025) * 0.04
          })`,
        }}
      />

      <div
        style={{
          position: 'absolute',
          width: 720,
          height: 220,
          border: '2px solid #F14A00',
          opacity: 0.08,
          right: -210,
          bottom: 70,
          transform: `rotate(-18deg) translateX(${
            Math.sin(frame * 0.018) * 28
          }px)`,
        }}
      />

      {particles}

      <AbsoluteFill
        style={{
          opacity: exitOp,
          transform: `translateX(${impactShake}px) scale(${cameraZoom})`,
          transformOrigin: 'center center',
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 105,
            top: 84,
            display: 'flex',
            alignItems: 'center',
            gap: 18,
            opacity: interpolate(frame, [0, 30], [0, 1], {
              extrapolateRight: 'clamp',
            }),
          }}
        >
          <div
            style={{
              width: 64,
              height: 5,
              backgroundColor: '#F14A00',
              boxShadow: '0 0 22px #F14A00',
            }}
          />
          <div
            style={{
              color: '#EEEEEE',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 27,
              fontWeight: 700,
              letterSpacing: 5,
            }}
          >
            EVERYDAY ROUTINE
          </div>
        </div>

        <svg
          width="1920"
          height="1080"
          viewBox="0 0 1920 1080"
          style={{position: 'absolute', inset: 0}}
        >
          <defs>
            <linearGradient id="pumpBody" x1="0" y1="0" x2="1" y2="1">
              <stop offset="0" stopColor="#EEEEEE" />
              <stop offset="1" stopColor="#CFCFCF" />
            </linearGradient>
            <linearGradient id="fuelGlow" x1="0" y1="1" x2="0" y2="0">
              <stop offset="0" stopColor="#F14A00" />
              <stop offset="1" stopColor="#FF9B58" />
            </linearGradient>
            <filter id="orangeGlow">
              <feGaussianBlur stdDeviation="12" result="blur" />
              <feMerge>
                <feMergeNode in="blur" />
                <feMergeNode in="SourceGraphic" />
              </feMerge>
            </filter>
          </defs>

          <g
            transform={`translate(${260 - (1 - nozzleEntry) * 330} 0)`}
            opacity={nozzleEntry}
          >
            <rect
              x="120"
              y="260"
              width="300"
              height="590"
              rx="40"
              fill="url(#pumpBody)"
            />
            <rect
              x="155"
              y="305"
              width="230"
              height="175"
              rx="22"
              fill="#5A1010"
              stroke="#F14A00"
              strokeWidth="8"
            />
            <text
              x="270"
              y="380"
              textAnchor="middle"
              fill="#EEEEEE"
              fontFamily="Inter"
              fontSize="50"
              fontWeight="900"
            >
              FUEL
            </text>
            <text
              x="270"
              y="438"
              textAnchor="middle"
              fill="#F14A00"
              fontFamily="JetBrains Mono"
              fontSize="42"
              fontWeight="900"
            >
              FULL
            </text>
            <rect x="180" y="525" width="180" height="18" rx="9" fill="#F14A00" />
            <rect x="180" y="570" width="138" height="12" rx="6" fill="#8E1616" />
            <rect x="180" y="610" width="162" height="12" rx="6" fill="#8E1616" />
            <path
              d="M420 360 C600 330 520 665 700 642"
              fill="none"
              stroke="#EEEEEE"
              strokeWidth="28"
              strokeLinecap="round"
            />
            <path
              d="M420 360 C600 330 520 665 700 642"
              fill="none"
              stroke="#F14A00"
              strokeWidth="7"
              strokeLinecap="round"
              strokeDasharray="18 26"
              strokeDashoffset={-frame * 8}
            />
            <g
              transform={`translate(670 ${
                600 + Math.sin(frame * 0.08) * 5
              }) rotate(-8)`}
            >
              <rect width="150" height="58" rx="16" fill="#EEEEEE" />
              <path d="M130 12 L215 20 L215 43 L130 46 Z" fill="#F14A00" />
              <rect x="28" y="50" width="48" height="84" rx="16" fill="#EEEEEE" />
            </g>
          </g>

          <g
            transform={`translate(${(1 - carEntry) * 560} 0)`}
            opacity={carEntry}
          >
            <path
              d="M740 720 L810 570 Q850 510 960 510 L1240 510 Q1320 515 1380 590 L1465 720 Z"
              fill="#EEEEEE"
            />
            <path
              d="M900 548 L1020 548 L1020 650 L840 650 Z"
              fill="#6B1111"
            />
            <path
              d="M1055 548 L1225 548 Q1270 550 1310 620 L1328 650 L1055 650 Z"
              fill="#6B1111"
            />
            <rect
              x="720"
              y="670"
              width="790"
              height="170"
              rx="64"
              fill="#EEEEEE"
            />
            <rect
              x="1395"
              y="623"
              width="85"
              height="68"
              rx="18"
              fill="#F14A00"
            />
            <circle cx="900" cy="835" r="92" fill="#4B0D0D" />
            <circle cx="900" cy="835" r="42" fill="#F14A00" />
            <circle cx="1325" cy="835" r="92" fill="#4B0D0D" />
            <circle cx="1325" cy="835" r="42" fill="#F14A00" />
          </g>

          <g transform="translate(1130 360)">
            <circle
              cx="0"
              cy="0"
              r="182"
              fill="#671010"
              stroke="#EEEEEE"
              strokeWidth="15"
            />
            <path
              d="M-126 70 A145 145 0 0 1 126 70"
              fill="none"
              stroke="#F14A00"
              strokeWidth="32"
              strokeLinecap="round"
            />
            <g transform={`rotate(${needle})`}>
              <line
                x1="0"
                y1="20"
                x2="0"
                y2="-124"
                stroke="#EEEEEE"
                strokeWidth="18"
                strokeLinecap="round"
              />
            </g>
            <circle cx="0" cy="0" r="25" fill="#F14A00" />
            <text
              x="0"
              y="105"
              textAnchor="middle"
              fill="#EEEEEE"
              fontFamily="Inter"
              fontSize="34"
              fontWeight="800"
            >
              TANK LEVEL
            </text>
          </g>

          <g transform="translate(1540 495)">
            <rect
              x="-95"
              y="-285"
              width="190"
              height="285"
              rx="42"
              fill="#5C0E0E"
              stroke="#EEEEEE"
              strokeWidth="8"
            />
            <rect
              x="-76"
              y={-18 - fuelLevel}
              width="152"
              height={fuelLevel}
              rx="26"
              fill="url(#fuelGlow)"
              filter="url(#orangeGlow)"
            />
            <text
              x="0"
              y="60"
              textAnchor="middle"
              fill="#EEEEEE"
              fontSize="30"
              fontFamily="Inter"
              fontWeight="800"
            >
              FILLING
            </text>
          </g>
        </svg>

        <div
          style={{
            position: 'absolute',
            left: 675,
            bottom: 88,
            transform: `scale(${questionEntry})`,
            transformOrigin: 'center',
            padding: '18px 45px 22px',
            border: '3px solid #EEEEEE',
            backgroundColor: '#8E1616E8',
            boxShadow: '14px 14px 0 #F14A00',
          }}
        >
          <div
            style={{
              color: '#EEEEEE',
              fontSize: 102,
              lineHeight: 1,
              fontWeight: 900,
              letterSpacing: -4,
            }}
          >
            सिर्फ रूटीन काम?
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at center, transparent 47%, #3D08088F 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: 45,
          bottom: 30,
          color: '#EEEEEE',
          opacity: 0.3,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default RoutineFuelTrap;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'AnnualWalletLeak',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const AnnualWalletLeak: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    }
  );

  const cameraY = interpolate(frame, [0, durationInFrames], [-18, 25], {
    easing: Easing.inOut(Easing.quad),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const walletEntry = sp(35, {damping: 10, stiffness: 135});
  const scannerEntry = sp(120);
  const counterEntry = sp(285, {damping: 9, stiffness: 170});

  const counter = Math.round(
    interpolate(frame, [290, 700], [0, 50000], {
      easing: Easing.out(Easing.cubic),
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    })
  );

  const holePulse = 1 + Math.sin(frame * 0.12) * 0.08;
  const scannerY = interpolate(frame % 210, [0, 210], [265, 775]);
  const calendarRotation = frame * 0.08;

  const notes = Array.from({length: 18}, (_, index) => {
    const x = 750 + (seededRandom(index + 30) - 0.5) * 260;
    const cycle = (frame * (1.2 + seededRandom(index + 70)) + index * 61) % 440;
    const y = 570 + cycle;
    const rotation = frame * (0.6 + seededRandom(index + 100)) + index * 27;

    return (
      <div
        key={index}
        style={{
          position: 'absolute',
          left: x,
          top: y,
          width: 88,
          height: 42,
          border: '3px solid #EEEEEE',
          borderRadius: 7,
          backgroundColor: '#F14A00',
          color: '#EEEEEE',
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          fontFamily: "'JetBrains Mono', monospace",
          fontSize: 22,
          fontWeight: 900,
          opacity: interpolate(cycle, [0, 45, 350, 440], [0, 1, 1, 0]),
          transform: `rotate(${rotation}deg) scale(${
            0.82 + seededRandom(index + 150) * 0.28
          })`,
          boxShadow: '0 0 18px #F14A0066',
        }}
      >
        ₹
      </div>
    );
  });

  const dust = Array.from({length: 26}, (_, index) => {
    const x = seededRandom(index + 200) * 1920;
    const y =
      (seededRandom(index + 260) * 1080 + frame * (0.1 + index * 0.006)) %
      1080;

    return (
      <div
        key={index}
        style={{
          position: 'absolute',
          left: x,
          top: y,
          width: 4 + seededRandom(index + 300) * 7,
          height: 4 + seededRandom(index + 300) * 7,
          borderRadius: '50%',
          backgroundColor: index % 2 === 0 ? '#EEEEEE' : '#F14A00',
          opacity: 0.1 + seededRandom(index + 340) * 0.18,
        }}
      />
    );
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#8E1616',
        overflow: 'hidden',
        fontFamily: "'Noto Sans Devanagari', 'Inter', sans-serif",
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 48% ${
            34 + Math.sin(frame * 0.012) * 8
          }%, #F14A0052 0%, transparent 32%),
          linear-gradient(145deg, #A31D18 0%, #8E1616 46%, #551010 100%)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.12,
          backgroundImage:
            'linear-gradient(#EEEEEE 1px, transparent 1px), linear-gradient(90deg, #EEEEEE 1px, transparent 1px)',
          backgroundSize: '62px 62px',
          transform: `perspective(800px) rotateX(62deg) translateY(${
            470 + frame * 0.2
          }px) scale(1.8)`,
          transformOrigin: 'center bottom',
        }}
      />

      <div
        style={{
          position: 'absolute',
          left: -160,
          top: 120,
          width: 500,
          height: 500,
          borderRadius: '50%',
          border: '70px solid #F14A00',
          opacity: 0.045,
          transform: `rotate(${frame * 0.06}deg)`,
        }}
      />

      {dust}

      <AbsoluteFill
        style={{
          opacity: exitOp,
          transform: `translateY(${cameraY}px) scale(1.035)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 90,
            width: 510,
          }}
        >
          <div
            style={{
              color: '#F14A00',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 27,
              fontWeight: 800,
              letterSpacing: 5,
            }}
          >
            FINANCIAL X-RAY
          </div>
          <div
            style={{
              marginTop: 16,
              color: '#EEEEEE',
              fontSize: 75,
              lineHeight: 1.12,
              fontWeight: 900,
            }}
          >
            आपकी जेब में
            <br />
            छिपा हुआ सुराख
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 520,
            top: 245,
            width: 700,
            height: 455,
            transform: `scale(${walletEntry}) rotate(${
              Math.sin(frame * 0.035) * 0.7
            }deg)`,
            transformOrigin: 'center',
          }}
        >
          <div
            style={{
              position: 'absolute',
              inset: 0,
              borderRadius: 68,
              border: '9px solid #EEEEEE',
              background:
                'linear-gradient(145deg, #B72A20 0%, #741010 60%, #5A0C0C 100%)',
              boxShadow:
                '0 45px 90px #3B080877, inset 0 0 0 18px #F14A0022',
            }}
          />

          <div
            style={{
              position: 'absolute',
              right: -35,
              top: 105,
              width: 250,
              height: 180,
              border: '8px solid #EEEEEE',
              borderRadius: 38,
              backgroundColor: '#8E1616',
            }}
          >
            <div
              style={{
                position: 'absolute',
                left: 48,
                top: 64,
                width: 42,
                height: 42,
                borderRadius: '50%',
                backgroundColor: '#F14A00',
                boxShadow: '0 0 24px #F14A00',
              }}
            />
          </div>

          <div
            style={{
              position: 'absolute',
              left: 205,
              bottom: -43,
              width: 235,
              height: 112,
              borderRadius: '50%',
              transform: `scale(${holePulse})`,
              background:
                'radial-gradient(ellipse at center, #3A0808 0%, #5A0B0B 40%, #F14A00 72%, transparent 75%)',
              filter: 'drop-shadow(0 0 25px #F14A00)',
            }}
          />
        </div>

        {notes}

        <div
          style={{
            position: 'absolute',
            left: 420,
            top: scannerY,
            width: 930,
            height: 5,
            opacity: scannerEntry,
            backgroundColor: '#EEEEEE',
            boxShadow: '0 0 12px #EEEEEE, 0 0 45px #F14A00',
          }}
        />

        <div
          style={{
            position: 'absolute',
            right: 110,
            top: 145,
            width: 485,
            height: 485,
            transform: `scale(${counterEntry})`,
          }}
        >
          {Array.from({length: 12}, (_, index) => {
            const angle = index * 30 + calendarRotation;
            const monthActive = frame > 320 + index * 22;

            return (
              <div
                key={index}
                style={{
                  position: 'absolute',
                  left: 218,
                  top: 218,
                  width: 18,
                  height: 76,
                  borderRadius: 10,
                  backgroundColor: monthActive ? '#F14A00' : '#EEEEEE',
                  opacity: monthActive ? 1 : 0.2,
                  transformOrigin: '9px 24px',
                  transform: `rotate(${angle}deg) translateY(-205px)`,
                  boxShadow: monthActive ? '0 0 18px #F14A00' : 'none',
                }}
              />
            );
          })}

          <div
            style={{
              position: 'absolute',
              inset: 70,
              borderRadius: '50%',
              border: '4px solid #EEEEEE55',
              backgroundColor: '#681010E8',
              display: 'flex',
              flexDirection: 'column',
              justifyContent: 'center',
              alignItems: 'center',
              boxShadow: 'inset 0 0 70px #F14A0038',
            }}
          >
            <div
              style={{
                color: '#EEEEEE',
                fontFamily: "'JetBrains Mono', monospace",
                fontSize: 31,
                fontWeight: 700,
                letterSpacing: 4,
              }}
            >
              YEARLY LEAK
            </div>
            <div
              style={{
                color: '#F14A00',
                fontFamily: "'Inter', sans-serif",
                fontSize: 88,
                fontWeight: 950,
                lineHeight: 1.1,
                letterSpacing: -5,
                textShadow: '0 0 28px #F14A0077',
              }}
            >
              ₹{counter.toLocaleString('en-IN')}
            </div>
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 180,
            bottom: 115,
            display: 'flex',
            gap: 20,
            alignItems: 'center',
            opacity: interpolate(frame, [560, 620], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
          }}
        >
          <svg width="72" height="72" viewBox="0 0 72 72">
            <circle
              cx="36"
              cy="36"
              r="29"
              fill="none"
              stroke="#F14A00"
              strokeWidth="7"
            />
            <path
              d="M21 37 L31 47 L52 24"
              fill="none"
              stroke="#EEEEEE"
              strokeWidth="7"
              strokeLinecap="round"
              strokeLinejoin="round"
            />
          </svg>
          <div
            style={{
              color: '#EEEEEE',
              fontSize: 44,
              fontWeight: 850,
            }}
          >
            नुकसान जारी है
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at center, transparent 50%, #3A08089A 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: 45,
          bottom: 30,
          color: '#EEEEEE',
          opacity: 0.3,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default AnnualWalletLeak;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'E20HiddenPipeline',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const E20HiddenPipeline: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    }
  );

  const cameraX = interpolate(frame, [0, durationInFrames], [-30, 25], {
    easing: Easing.inOut(Easing.quad),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const tankEntry = sp(30, {damping: 10});
  const splitEntry = sp(155, {damping: 11});
  const revealEntry = sp(350, {damping: 9, stiffness: 160});
  const ethanolLevel = interpolate(frame, [120, 360], [0, 116], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const metricData = [
    {label: 'MILEAGE', x: 1070, icon: 'gauge', delay: 230},
    {label: 'ENGINE', x: 1335, icon: 'engine', delay: 275},
    {label: 'SALARY', x: 1600, icon: 'wallet', delay: 320},
  ];

  const particles = Array.from({length: 38}, (_, index) => {
    const x = seededRandom(index + 50) * 1920;
    const y =
      (seededRandom(index + 100) * 1080 - frame * (0.12 + index * 0.004) + 1200) %
      1200;

    return (
      <div
        key={index}
        style={{
          position: 'absolute',
          left: x,
          top: y - 80,
          width: 3 + seededRandom(index + 170) * 7,
          height: 3 + seededRandom(index + 170) * 7,
          borderRadius: index % 3 === 0 ? 1 : '50%',
          backgroundColor: index % 3 === 0 ? '#F14A00' : '#EEEEEE',
          opacity: 0.08 + seededRandom(index + 220) * 0.2,
        }}
      />
    );
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#8E1616',
        overflow: 'hidden',
        fontFamily: "'Noto Sans Devanagari', 'Inter', sans-serif",
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at ${
            38 + Math.sin(frame * 0.014) * 7
          }% 44%, #F14A0055 0%, transparent 30%),
          radial-gradient(circle at 86% 18%, #FF7B3936 0%, transparent 28%),
          linear-gradient(115deg, #5B0D0D 0%, #8E1616 48%, #A7221B 100%)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.11,
          backgroundImage:
            'repeating-linear-gradient(0deg, transparent 0px, transparent 47px, #EEEEEE 48px, transparent 50px)',
          transform: `translateY(${(frame * 0.35) % 50}px)`,
        }}
      />

      {particles}

      <AbsoluteFill
        style={{
          opacity: exitOp,
          transform: `translateX(${cameraX}px) scale(1.03)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 105,
            top: 80,
            display: 'flex',
            alignItems: 'center',
            gap: 20,
          }}
        >
          <div
            style={{
              width: 74,
              height: 8,
              backgroundColor: '#F14A00',
              boxShadow: '0 0 20px #F14A00',
            }}
          />
          <div
            style={{
              color: '#EEEEEE',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 27,
              fontWeight: 800,
              letterSpacing: 5,
            }}
          >
            HIDDEN FLOW NETWORK
          </div>
        </div>

        <svg
          width="1920"
          height="1080"
          viewBox="0 0 1920 1080"
          style={{position: 'absolute', inset: 0}}
        >
          <defs>
            <linearGradient id="glassTank" x1="0" y1="0" x2="1" y2="1">
              <stop offset="0" stopColor="#EEEEEE" stopOpacity="0.28" />
              <stop offset="1" stopColor="#EEEEEE" stopOpacity="0.07" />
            </linearGradient>
            <linearGradient id="e20Fill" x1="0" y1="1" x2="0" y2="0">
              <stop offset="0" stopColor="#F14A00" />
              <stop offset="1" stopColor="#FF9B63" />
            </linearGradient>
            <filter id="flowGlow">
              <feGaussianBlur stdDeviation="7" result="blur" />
              <feMerge>
                <feMergeNode in="blur" />
                <feMergeNode in="SourceGraphic" />
              </feMerge>
            </filter>
          </defs>

          <g
            opacity={tankEntry}
            transform={`translate(${(1 - tankEntry) * -290} 0)`}
          >
            <rect
              x="125"
              y="270"
              width="280"
              height="510"
              rx="38"
              fill="#EEEEEE"
            />
            <rect
              x="165"
              y="315"
              width="200"
              height="145"
              rx="22"
              fill="#681010"
              stroke="#F14A00"
              strokeWidth="8"
            />
            <text
              x="265"
              y="375"
              textAnchor="middle"
              fill="#EEEEEE"
              fontFamily="Inter"
              fontWeight="900"
              fontSize="48"
            >
              E-20
            </text>
            <text
              x="265"
              y="425"
              textAnchor="middle"
              fill="#F14A00"
              fontFamily="Inter"
              fontWeight="800"
              fontSize="27"
            >
              BLENDED PETROL
            </text>
            <rect x="180" y="520" width="170" height="18" rx="9" fill="#8E1616" />
            <rect x="180" y="565" width="125" height="12" rx="6" fill="#F14A00" />
            <rect x="180" y="605" width="150" height="12" rx="6" fill="#8E1616" />
          </g>

          <path
            d="M405 540 C515 540 530 495 610 495"
            fill="none"
            stroke="#EEEEEE"
            strokeWidth="38"
            strokeLinecap="round"
          />
          <path
            d="M405 540 C515 540 530 495 610 495"
            fill="none"
            stroke="#F14A00"
            strokeWidth="10"
            strokeDasharray="18 24"
            strokeDashoffset={-frame * 8}
            filter="url(#flowGlow)"
          />

          <g
            transform={`translate(0 ${(1 - tankEntry) * 160})`}
            opacity={tankEntry}
          >
            <rect
              x="610"
              y="245"
              width="330"
              height="505"
              rx="72"
              fill="url(#glassTank)"
              stroke="#EEEEEE"
              strokeWidth="9"
            />
            <rect
              x="635"
              y="270"
              width="280"
              height="455"
              rx="50"
              fill="#641010"
              opacity="0.82"
            />
            <rect
              x="635"
              y={725 - ethanolLevel}
              width="280"
              height={ethanolLevel}
              rx="30"
              fill="url(#e20Fill)"
            />
            <line
              x1="635"
              y1="634"
              x2="915"
              y2="634"
              stroke="#EEEEEE"
              strokeWidth="5"
              strokeDasharray="14 12"
            />
            <text
              x="775"
              y="610"
              textAnchor="middle"
              fill="#EEEEEE"
              fontFamily="JetBrains Mono"
              fontWeight="900"
              fontSize="35"
            >
              20%
            </text>
            <text
              x="775"
              y="835"
              textAnchor="middle"
              fill="#EEEEEE"
              fontFamily="Inter"
              fontWeight="900"
              fontSize="38"
            >
              ETHANOL MIX
            </text>
          </g>

          <g opacity={splitEntry}>
            <path
              d="M940 495 H1020 V345 H1070"
              fill="none"
              stroke="#EEEEEE"
              strokeWidth="22"
              strokeLinejoin="round"
            />
            <path
              d="M940 495 H1220 V495 H1335"
              fill="none"
              stroke="#EEEEEE"
              strokeWidth="22"
              strokeLinejoin="round"
            />
            <path
              d="M940 495 H1020 V650 H1600"
              fill="none"
              stroke="#EEEEEE"
              strokeWidth="22"
              strokeLinejoin="round"
            />

            <path
              d="M940 495 H1020 V345 H1070"
              fill="none"
              stroke="#F14A00"
              strokeWidth="7"
              strokeDasharray="15 24"
              strokeDashoffset={-frame * 9}
            />
            <path
              d="M940 495 H1220 V495 H1335"
              fill="none"
              stroke="#F14A00"
              strokeWidth="7"
              strokeDasharray="15 24"
              strokeDashoffset={-frame * 9}
            />
            <path
              d="M940 495 H1020 V650 H1600"
              fill="none"
              stroke="#F14A00"
              strokeWidth="7"
              strokeDasharray="15 24"
              strokeDashoffset={-frame * 9}
            />
          </g>

          {metricData.map((metric, index) => {
            const entry = sp(metric.delay, {damping: 10, stiffness: 145});
            const activeDrop = interpolate(
              frame,
              [metric.delay + 110, metric.delay + 360],
              [0, 1],
              {
                extrapolateLeft: 'clamp',
                extrapolateRight: 'clamp',
              }
            );

            return (
              <g
                key={metric.label}
                transform={`translate(${metric.x} ${
                  index === 0 ? 345 : index === 1 ? 495 : 650
                }) scale(${entry})`}
              >
                <circle
                  r="118"
                  fill="#6B1010"
                  stroke="#EEEEEE"
                  strokeWidth="8"
                />
                <circle
                  r="92"
                  fill="none"
                  stroke="#F14A00"
                  strokeWidth="14"
                  strokeDasharray={`${580 * (1 - activeDrop * 0.65)} 580`}
                  transform="rotate(-90)"
                />

                {metric.icon === 'gauge' && (
                  <>
                    <path
                      d="M-55 23 A62 62 0 0 1 55 23"
                      fill="none"
                      stroke="#EEEEEE"
                      strokeWidth="10"
                      strokeLinecap="round"
                    />
                    <line
                      x1="0"
                      y1="20"
                      x2={-32 - activeDrop * 20}
                      y2={-35 + activeDrop * 25}
                      stroke="#F14A00"
                      strokeWidth="10"
                      strokeLinecap="round"
                    />
                    <circle r="10" fill="#EEEEEE" />
                  </>
                )}

                {metric.icon === 'engine' && (
                  <>
                    <path
                      d="M-56 -36 H35 L59 -12 V45 H-50 V20 H-73 V-20 H-56 Z"
                      fill="none"
                      stroke="#EEEEEE"
                      strokeWidth="10"
                      strokeLinejoin="round"
                    />
                    <circle
                      cx="20"
                      cy="7"
                      r="20"
                      fill="none"
                      stroke="#F14A00"
                      strokeWidth="8"
                    />
                  </>
                )}

                {metric.icon === 'wallet' && (
                  <>
                    <rect
                      x="-66"
                      y="-45"
                      width="132"
                      height="94"
                      rx="17"
                      fill="none"
                      stroke="#EEEEEE"
                      strokeWidth="10"
                    />
                    <rect
                      x="17"
                      y="-16"
                      width="62"
                      height="45"
                      rx="12"
                      fill="#F14A00"
                    />
                    <circle cx="36" cy="6" r="6" fill="#EEEEEE" />
                  </>
                )}

                <text
                  x="0"
                  y="160"
                  textAnchor="middle"
                  fill="#EEEEEE"
                  fontFamily="Inter"
                  fontWeight="900"
                  fontSize="27"
                >
                  {metric.label}
                </text>
              </g>
            );
          })}
        </svg>

        <div
          style={{
            position: 'absolute',
            left: 1050,
            bottom: 105,
            transform: `scale(${revealEntry})`,
            transformOrigin: 'left center',
            padding: '20px 38px',
            borderLeft: '10px solid #F14A00',
            backgroundColor: '#641010D9',
            boxShadow: '18px 18px 0 #3D0808',
          }}
        >
          <div
            style={{
              color: '#EEEEEE',
              fontSize: 75,
              lineHeight: 1,
              fontWeight: 900,
            }}
          >
            असली खेल यहाँ है
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at center, transparent 48%, #3A080896 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: 45,
          bottom: 30,
          color: '#EEEEEE',
          opacity: 0.3,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default E20HiddenPipeline;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'TripleLegalDrain',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const TripleLegalDrain: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    }
  );

  const stampProgress = sp(330, {damping: 7, stiffness: 210});
  const hit = frame > 335 && frame < 360;
  const shake = hit
    ? Math.sin((frame - 335) * 3.5) * (1 - (frame - 335) / 25) * 18
    : 0;

  const cameraZoom = interpolate(frame, [0, durationInFrames], [1.01, 1.08], {
    easing: Easing.inOut(Easing.quad),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const systems = [
    {title: 'MILEAGE', value: '15 → 10', x: 390, delay: 45, icon: 'gauge'},
    {title: 'ENGINE LIFE', value: '100 → 62', x: 960, delay: 110, icon: 'engine'},
    {title: 'SALARY', value: '₹ → 0', x: 1530, delay: 175, icon: 'salary'},
  ];

  const debris = Array.from({length: 30}, (_, index) => {
    const angle = seededRandom(index + 20) * Math.PI * 2;
    const distance = interpolate(frame, [335, 430], [0, 450], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    });
    const opacity = interpolate(frame, [335, 380, 470], [0, 1, 0], {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    });

    return (
      <div
        key={index}
        style={{
          position: 'absolute',
          left: 960 + Math.cos(angle) * distance,
          top: 540 + Math.sin(angle) * distance * 0.55,
          width: 5 + seededRandom(index + 60) * 14,
          height: 3 + seededRandom(index + 100) * 8,
          backgroundColor: index % 2 === 0 ? '#F14A00' : '#EEEEEE',
          opacity,
          transform: `rotate(${frame * (1 + seededRandom(index + 140))}deg)`,
        }}
      />
    );
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#8E1616',
        overflow: 'hidden',
        fontFamily: "'Noto Sans Devanagari', 'Inter', sans-serif",
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 50% 42%, #F14A0048 0%, transparent 33%),
          linear-gradient(${118 + Math.sin(frame * 0.01) * 5}deg, #5C0D0D 0%, #8E1616 47%, #AF251D 100%)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.1,
          backgroundImage:
            'repeating-radial-gradient(circle at center, transparent 0px, transparent 48px, #EEEEEE 49px, transparent 51px)',
          transform: `scale(${1 + (frame % 180) / 900})`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: exitOp,
          transform: `translate(${shake}px, ${shake * 0.3}px) scale(${cameraZoom})`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 0,
            right: 0,
            top: 72,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              color: '#F14A00',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 26,
              fontWeight: 800,
              letterSpacing: 6,
            }}
          >
            THREE-WAY FINANCIAL DAMAGE
          </div>
          <div
            style={{
              marginTop: 14,
              color: '#EEEEEE',
              fontSize: 69,
              fontWeight: 900,
            }}
          >
            एक ही फ्यूल — तीन तरफ नुकसान
          </div>
        </div>

        <svg
          width="1920"
          height="1080"
          viewBox="0 0 1920 1080"
          style={{position: 'absolute', inset: 0}}
        >
          <defs>
            <filter id="dashboardGlow">
              <feGaussianBlur stdDeviation="8" result="blur" />
              <feMerge>
                <feMergeNode in="blur" />
                <feMergeNode in="SourceGraphic" />
              </feMerge>
            </filter>
          </defs>

          <path
            d="M390 475 H1530"
            fill="none"
            stroke="#EEEEEE"
            strokeWidth="12"
            strokeDasharray="22 24"
            opacity="0.38"
            strokeDashoffset={-frame * 5}
          />

          {systems.map((system, index) => {
            const entry = sp(system.delay, {
              damping: 10,
              stiffness: 145,
            });

            const damage = interpolate(
              frame,
              [system.delay + 130, system.delay + 430],
              [0, 1],
              {
                easing: Easing.inOut(Easing.cubic),
                extrapolateLeft: 'clamp',
                extrapolateRight: 'clamp',
              }
            );

            const needleAngle = -55 + damage * 110;

            return (
              <g
                key={system.title}
                transform={`translate(${system.x} 505) scale(${entry})`}
              >
                <circle
                  r="215"
                  fill="#641010"
                  stroke="#EEEEEE"
                  strokeWidth="9"
                />
                <circle
                  r="180"
                  fill="none"
                  stroke="#F14A00"
                  strokeWidth="18"
                  strokeDasharray={`${1130 * (1 - damage * 0.78)} 1130`}
                  transform="rotate(-90)"
                  filter="url(#dashboardGlow)"
                />
                <circle
                  r="142"
                  fill="#791313"
                  stroke="#EEEEEE"
                  strokeOpacity="0.2"
                  strokeWidth="3"
                />

                {system.icon === 'gauge' && (
                  <>
                    <path
                      d="M-83 32 A92 92 0 0 1 83 32"
                      fill="none"
                      stroke="#EEEEEE"
                      strokeWidth="15"
                      strokeLinecap="round"
                    />
                    <g transform={`rotate(${needleAngle})`}>
                      <line
                        x1="0"
                        y1="20"
                        x2="0"
                        y2="-86"
                        stroke="#F14A00"
                        strokeWidth="15"
                        strokeLinecap="round"
                      />
                    </g>
                    <circle r="18" fill="#EEEEEE" />
                  </>
                )}

                {system.icon === 'engine' && (
                  <>
                    <path
                      d="M-91 -56 H45 L86 -18 V63 H-68 V35 H-100 V-29 H-91 Z"
                      fill="none"
                      stroke="#EEEEEE"
                      strokeWidth="14"
                      strokeLinejoin="round"
                    />
                    <path
                      d={`M-15 5 L18 -28 L48 ${5 + damage * 30} L13 42 Z`}
                      fill="#F14A00"
                    />
                  </>
                )}

                {system.icon === 'salary' && (
                  <>
                    <rect
                      x="-97"
                      y="-64"
                      width="194"
                      height="128"
                      rx="25"
                      fill="none"
                      stroke="#EEEEEE"
                      strokeWidth="14"
                    />
                    <rect
                      x="-72"
                      y={-38 + damage * 78}
                      width="144"
                      height={76 * (1 - damage)}
                      rx="10"
                      fill="#F14A00"
                    />
                    <text
                      x="0"
                      y="19"
                      textAnchor="middle"
                      fill="#EEEEEE"
                      fontSize="66"
                      fontFamily="Inter"
                      fontWeight="900"
                    >
                      ₹
                    </text>
                  </>
                )}

                <text
                  x="0"
                  y="278"
                  textAnchor="middle"
                  fill="#EEEEEE"
                  fontFamily="Inter"
                  fontSize="29"
                  fontWeight="900"
                  letterSpacing="3"
                >
                  {system.title}
                </text>
                <text
                  x="0"
                  y="335"
                  textAnchor="middle"
                  fill="#F14A00"
                  fontFamily="JetBrains Mono"
                  fontSize="43"
                  fontWeight="900"
                >
                  {system.value}
                </text>
              </g>
            );
          })}
        </svg>

        <div
          style={{
            position: 'absolute',
            left: 585,
            top: 335,
            width: 750,
            height: 340,
            transform: `translateY(${(1 - stampProgress) * -520}px) scale(${
              0.8 + stampProgress * 0.2
            }) rotate(-7deg)`,
            transformOrigin: 'center',
            opacity: stampProgress,
            border: '18px solid #F14A00',
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            backgroundColor: '#8E1616EE',
            boxShadow: '0 0 0 8px #EEEEEE, 0 35px 75px #3B070799',
          }}
        >
          <div
            style={{
              color: '#EEEEEE',
              fontFamily: "'Inter', sans-serif",
              fontSize: 105,
              fontWeight: 950,
              letterSpacing: 4,
              textAlign: 'center',
              lineHeight: 0.95,
            }}
          >
            LEGAL
            <br />
            DRAIN
          </div>
        </div>

        {debris}

        <div
          style={{
            position: 'absolute',
            left: 730,
            bottom: 68,
            padding: '16px 38px',
            borderTop: '5px solid #F14A00',
            color: '#EEEEEE',
            fontSize: 42,
            fontWeight: 850,
            backgroundColor: '#5C0D0DDD',
            opacity: interpolate(frame, [450, 510], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
          }}
        >
          माइलेज + इंजन + सैलरी
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at center, transparent 49%, #3907079C 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: 45,
          bottom: 30,
          color: '#EEEEEE',
          opacity: 0.3,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default TripleLegalDrain;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'ProBuffetResearchPortal',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const ProBuffetResearchPortal: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    }
  );

  const brandEntry = sp(35, {damping: 9, stiffness: 150});
  const archiveEntry = sp(250, {damping: 11});
  const cameraPush = interpolate(frame, [0, durationInFrames], [0, 125], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const rings = Array.from({length: 7}, (_, index) => {
    const size = 270 + index * 135;
    const direction = index % 2 === 0 ? 1 : -1;

    return (
      <div
        key={index}
        style={{
          position: 'absolute',
          left: 960 - size / 2,
          top: 460 - size / 2,
          width: size,
          height: size,
          borderRadius: '50%',
          border: `${index % 3 === 0 ? 5 : 2}px solid ${
            index % 2 === 0 ? '#EEEEEE' : '#F14A00'
          }`,
          opacity: 0.06 + index * 0.018,
          transform: `rotate(${frame * 0.12 * direction + index * 17}deg) scale(${
            1 + Math.sin(frame * 0.02 + index) * 0.018
          })`,
        }}
      >
        {Array.from({length: 6}, (_, dotIndex) => (
          <div
            key={dotIndex}
            style={{
              position: 'absolute',
              left: '50%',
              top: -6,
              width: 12,
              height: 12,
              borderRadius: '50%',
              backgroundColor: '#F14A00',
              transformOrigin: `0 ${size / 2 + 6}px`,
              transform: `rotate(${dotIndex * 60}deg)`,
              boxShadow: '0 0 14px #F14A00',
            }}
          />
        ))}
      </div>
    );
  });

  const particles = Array.from({length: 34}, (_, index) => {
    const x = seededRandom(index + 80) * 1920;
    const y =
      (seededRandom(index + 120) * 1080 + frame * (0.1 + index * 0.003)) %
      1080;

    return (
      <div
        key={index}
        style={{
          position: 'absolute',
          left: x,
          top: y,
          width: 3 + seededRandom(index + 170) * 7,
          height: 3 + seededRandom(index + 170) * 7,
          backgroundColor: index % 2 === 0 ? '#F14A00' : '#EEEEEE',
          opacity: 0.08 + seededRandom(index + 220) * 0.18,
          transform: `rotate(${frame + index * 25}deg)`,
        }}
      />
    );
  });

  const cards = [
    {title: 'PRICE', icon: '₹', delay: 285},
    {title: 'POLICY', icon: '§', delay: 335},
    {title: 'POCKET', icon: '↘', delay: 385},
  ];

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#8E1616',
        overflow: 'hidden',
        fontFamily: "'Noto Sans Devanagari', 'Inter', sans-serif",
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 50% 42%, #F14A005E 0%, transparent 29%),
          radial-gradient(circle at 15% 83%, #FF80402E 0%, transparent 31%),
          linear-gradient(135deg, #5A0D0D 0%, #8E1616 52%, #AD221B 100%)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.11,
          backgroundImage:
            'repeating-linear-gradient(90deg, transparent 0px, transparent 89px, #EEEEEE 90px, transparent 92px)',
          transform: `translateX(${(frame * 0.22) % 92}px)`,
        }}
      />

      {rings}
      {particles}

      <AbsoluteFill
        style={{
          opacity: exitOp,
          transform: `perspective(1300px) translateZ(${cameraPush}px)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 670,
            top: 235,
            width: 580,
            height: 450,
            transform: `scale(${brandEntry})`,
            transformOrigin: 'center',
          }}
        >
          <div
            style={{
              position: 'absolute',
              left: 110,
              top: 35,
              width: 360,
              height: 360,
              borderRadius: '50%',
              border: '9px solid #EEEEEE',
              background:
                'radial-gradient(circle at 35% 30%, #F14A00 0%, #A9241C 38%, #5B0D0D 100%)',
              boxShadow:
                '0 0 0 20px #F14A002A, 0 0 100px #F14A0077, inset 0 0 70px #3D0808',
              display: 'flex',
              justifyContent: 'center',
              alignItems: 'center',
            }}
          >
            <svg width="220" height="220" viewBox="0 0 220 220">
              <path
                d="M45 165 V58 H114 Q169 58 169 105 Q169 151 114 151 H76"
                fill="none"
                stroke="#EEEEEE"
                strokeWidth="24"
                strokeLinecap="round"
                strokeLinejoin="round"
              />
              <path
                d="M48 109 H138"
                stroke="#EEEEEE"
                strokeWidth="22"
                strokeLinecap="round"
              />
              <circle cx="172" cy="52" r="17" fill="#F14A00" />
            </svg>
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 0,
            right: 0,
            top: 685,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              color: '#EEEEEE',
              fontSize: 94,
              fontWeight: 950,
              letterSpacing: -4,
              lineHeight: 1,
              transform: `translateY(${(1 - brandEntry) * 80}px)`,
              opacity: brandEntry,
            }}
          >
            ProBuffet HQ
          </div>
          <div
            style={{
              marginTop: 20,
              color: '#F14A00',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 28,
              fontWeight: 800,
              letterSpacing: 7,
              opacity: brandEntry,
            }}
          >
            MONEY • DATA • REALITY
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 85,
            top: 300,
            width: 430,
            opacity: interpolate(frame, [145, 220], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
          }}
        >
          <div
            style={{
              color: '#F14A00',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 26,
              fontWeight: 800,
              letterSpacing: 5,
            }}
          >
            HOST
          </div>
          <div
            style={{
              color: '#EEEEEE',
              fontSize: 82,
              fontWeight: 950,
              lineHeight: 1,
            }}
          >
            मैं
            <br />
            कुनाल हूँ
          </div>
          <div
            style={{
              marginTop: 24,
              width: 280,
              height: 7,
              backgroundColor: '#F14A00',
              boxShadow: '0 0 23px #F14A00',
            }}
          />
        </div>

        <div
          style={{
            position: 'absolute',
            right: 90,
            top: 215,
            width: 450,
            height: 610,
            opacity: archiveEntry,
            transform: `translateX(${(1 - archiveEntry) * 180}px)`,
          }}
        >
          <div
            style={{
              color: '#EEEEEE',
              fontSize: 48,
              lineHeight: 1.15,
              fontWeight: 900,
              marginBottom: 35,
            }}
          >
            पैसे से पहले
            <br />
            पूरा बैकग्राउंड
          </div>

          {cards.map((card, index) => {
            const entry = sp(card.delay, {damping: 10, stiffness: 145});

            return (
              <div
                key={card.title}
                style={{
                  height: 125,
                  marginBottom: 22,
                  border: '3px solid #EEEEEE',
                  borderLeft: '12px solid #F14A00',
                  backgroundColor: '#671010D9',
                  display: 'flex',
                  alignItems: 'center',
                  gap: 28,
                  padding: '0 28px',
                  transform: `translateX(${(1 - entry) * 180}px) rotate(${
                    Math.sin(frame * 0.018 + index) * 0.5
                  }deg)`,
                  opacity: entry,
                  boxShadow: '12px 12px 0 #4A0A0A',
                }}
              >
                <div
                  style={{
                    width: 68,
                    height: 68,
                    borderRadius: '50%',
                    backgroundColor: '#F14A00',
                    color: '#EEEEEE',
                    fontSize: 42,
                    fontFamily: 'Inter',
                    fontWeight: 900,
                    display: 'flex',
                    justifyContent: 'center',
                    alignItems: 'center',
                  }}
                >
                  {card.icon}
                </div>
                <div
                  style={{
                    color: '#EEEEEE',
                    fontFamily: "'JetBrains Mono', monospace",
                    fontSize: 32,
                    fontWeight: 900,
                    letterSpacing: 3,
                  }}
                >
                  {card.title}
                </div>
              </div>
            );
          })}
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at center, transparent 45%, #3907079B 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: 45,
          bottom: 30,
          color: '#EEEEEE',
          opacity: 0.3,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default ProBuffetResearchPortal;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'IndiaOilImport85',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const IndiaOilImport85: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    }
  );

  const mapEntry = sp(45, {damping: 10, stiffness: 145});
  const gaugeEntry = sp(180, {damping: 9, stiffness: 160});
  const importValue = Math.round(
    interpolate(frame, [185, 500], [0, 85], {
      easing: Easing.out(Easing.cubic),
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    })
  );

  const cameraZoom = interpolate(frame, [0, durationInFrames], [1.01, 1.07], {
    easing: Easing.inOut(Easing.quad),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const routeProgress = interpolate(frame, [110, 600], [0, 1], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const ships = Array.from({length: 5}, (_, index) => {
    const x =
      -240 +
      ((frame * (1.2 + index * 0.12) + index * 400) % 2250);
    const y = 700 + index * 42 + Math.sin(frame * 0.04 + index) * 8;

    return (
      <svg
        key={index}
        width="220"
        height="100"
        viewBox="0 0 220 100"
        style={{
          position: 'absolute',
          left: x,
          top: y,
          opacity: 0.25 + index * 0.1,
        }}
      >
        <path d="M18 45 H185 L156 82 H48 Z" fill="#EEEEEE" />
        <rect x="68" y="19" width="82" height="30" rx="4" fill="#F14A00" />
        <rect x="92" y="4" width="13" height="22" fill="#EEEEEE" />
        <circle cx="75" cy="82" r="7" fill="#8E1616" />
        <circle cx="126" cy="82" r="7" fill="#8E1616" />
      </svg>
    );
  });

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#8E1616',
        overflow: 'hidden',
        fontFamily: "'Noto Sans Devanagari', 'Inter', sans-serif",
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 58% 45%, #F14A004D 0%, transparent 32%),
          linear-gradient(126deg, #530C0C 0%, #8E1616 52%, #AE251D 100%)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.12,
          backgroundImage:
            'repeating-linear-gradient(0deg, transparent 0px, transparent 53px, #EEEEEE 54px, transparent 56px)',
          transform: `translateY(${(frame * 0.28) % 56}px)`,
        }}
      />

      {ships}

      <AbsoluteFill
        style={{
          opacity: exitOp,
          transform: `scale(${cameraZoom})`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 105,
            top: 80,
          }}
        >
          <div
            style={{
              color: '#F14A00',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 27,
              fontWeight: 800,
              letterSpacing: 5,
            }}
          >
            INDIA'S OIL DEPENDENCY
          </div>
          <div
            style={{
              marginTop: 14,
              color: '#EEEEEE',
              fontSize: 71,
              fontWeight: 900,
              lineHeight: 1.08,
            }}
          >
            हर 100 लीटर में
            <br />
            85 लीटर विदेश से
          </div>
        </div>

        <svg
          width="1920"
          height="1080"
          viewBox="0 0 1920 1080"
          style={{position: 'absolute', inset: 0}}
        >
          <defs>
            <filter id="mapGlow">
              <feGaussianBlur stdDeviation="12" result="blur" />
              <feMerge>
                <feMergeNode in="blur" />
                <feMergeNode in="SourceGraphic" />
              </feMerge>
            </filter>
            <linearGradient id="mapFill" x1="0" y1="0" x2="1" y2="1">
              <stop offset="0" stopColor="#EEEEEE" />
              <stop offset="1" stopColor="#D4D4D4" />
            </linearGradient>
          </defs>

          <g
            transform={`translate(670 160) scale(${0.76 * mapEntry})`}
            opacity={mapEntry}
          >
            <path
              d="M311 38 L370 88 L430 100 L467 153 L441 213 L487 262 L448 328 L412 374 L430 447 L382 501 L352 566 L314 639 L279 725 L246 817 L207 742 L176 651 L129 581 L107 504 L65 441 L91 383 L62 326 L105 282 L96 221 L148 185 L160 117 L224 101 L253 54 Z"
              fill="url(#mapFill)"
              stroke="#F14A00"
              strokeWidth="13"
              filter="url(#mapGlow)"
            />
            <path
              d="M154 182 L232 220 L293 189 L357 251 L314 312 L384 371 L333 453 L365 526 L303 614 L247 718"
              fill="none"
              stroke="#8E1616"
              strokeWidth="7"
              strokeDasharray="15 14"
              strokeDashoffset={-frame * 5}
              opacity="0.55"
            />

            {[
              [130, 325],
              [212, 220],
              [354, 268],
              [286, 470],
              [245, 670],
            ].map(([x, y], index) => (
              <g key={index} transform={`translate(${x} ${y})`}>
                <circle
                  r={18 + Math.sin(frame * 0.08 + index) * 3}
                  fill="#F14A00"
                />
                <circle
                  r={30 + Math.sin(frame * 0.08 + index) * 4}
                  fill="none"
                  stroke="#F14A00"
                  strokeWidth="5"
                  opacity="0.38"
                />
              </g>
            ))}
          </g>

          <g opacity={routeProgress}>
            <path
              d="M180 660 C390 500 520 540 730 620"
              fill="none"
              stroke="#EEEEEE"
              strokeWidth="8"
              strokeDasharray="18 23"
              strokeDashoffset={-frame * 8}
            />
            <path
              d="M250 780 C450 660 570 690 780 700"
              fill="none"
              stroke="#F14A00"
              strokeWidth="8"
              strokeDasharray="18 23"
              strokeDashoffset={-frame * 9}
            />
          </g>

          <g
            transform={`translate(1475 490) scale(${gaugeEntry})`}
            opacity={gaugeEntry}
          >
            <circle
              r="285"
              fill="#681010"
              stroke="#EEEEEE"
              strokeWidth="12"
            />
            <circle
              r="232"
              fill="none"
              stroke="#EEEEEE"
              strokeOpacity="0.16"
              strokeWidth="38"
            />
            <circle
              r="232"
              fill="none"
              stroke="#F14A00"
              strokeWidth="38"
              strokeLinecap="round"
              strokeDasharray={`${1458 * (importValue / 100)} 1458`}
              transform="rotate(-90)"
              filter="url(#mapGlow)"
            />
            <text
              x="0"
              y="14"
              textAnchor="middle"
              fill="#F14A00"
              fontFamily="Inter"
              fontSize="145"
              fontWeight="950"
            >
              {importValue}%
            </text>
            <text
              x="0"
              y="82"
              textAnchor="middle"
              fill="#EEEEEE"
              fontFamily="JetBrains Mono"
              fontSize="35"
              fontWeight="900"
              letterSpacing="5"
            >
              IMPORTED
            </text>
          </g>

          {Array.from({length: 10}, (_, index) => {
            const reveal = sp(270 + index * 18, {damping: 10});

            return (
              <g
                key={index}
                transform={`translate(${1180 + index * 62} 860) scale(${reveal})`}
              >
                <ellipse cx="0" cy="-45" rx="24" ry="10" fill="#F14A00" />
                <rect
                  x="-24"
                  y="-45"
                  width="48"
                  height="78"
                  fill={index < 8 ? '#F14A00' : '#EEEEEE'}
                />
                <ellipse
                  cx="0"
                  cy="33"
                  rx="24"
                  ry="10"
                  fill={index < 8 ? '#F14A00' : '#EEEEEE'}
                />
                <ellipse
                  cx="0"
                  cy="-45"
                  rx="24"
                  ry="10"
                  fill={index < 8 ? '#FF8D4C' : '#EEEEEE'}
                />
              </g>
            );
          })}
        </svg>

        <div
          style={{
            position: 'absolute',
            left: 105,
            bottom: 90,
            display: 'flex',
            alignItems: 'center',
            gap: 22,
            opacity: interpolate(frame, [500, 570], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
          }}
        >
          <svg width="75" height="75" viewBox="0 0 75 75">
            <path
              d="M38 7 C51 23 61 35 61 49 C61 63 51 70 38 70 C24 70 14 62 14 49 C14 35 25 22 38 7 Z"
              fill="#F14A00"
              stroke="#EEEEEE"
              strokeWidth="5"
            />
          </svg>
          <div
            style={{
              color: '#EEEEEE',
              fontSize: 42,
              fontWeight: 850,
            }}
          >
            विदेशी तेल पर भारी निर्भरता
          </div>
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at center, transparent 50%, #3907079E 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: 45,
          bottom: 30,
          color: '#EEEEEE',
          opacity: 0.3,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default IndiaOilImport85;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'DomesticFuelChain',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const DomesticFuelChain: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    }
  );

  const cameraX = interpolate(frame, [0, durationInFrames], [-26, 28], {
    easing: Easing.inOut(Easing.quad),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const nodes = [
    {x: 240, title: 'FARM', delay: 35, type: 'farm'},
    {x: 720, title: 'REFINERY', delay: 135, type: 'refinery'},
    {x: 1200, title: 'E-20 PUMP', delay: 235, type: 'pump'},
    {x: 1680, title: 'VEHICLE', delay: 335, type: 'car'},
  ];

  const pipeReveal = interpolate(frame, [90, 470], [0, 1], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const droplets = Array.from({length: 12}, (_, index) => {
    const progress = ((frame * 0.0042 + index / 12) % 1) * pipeReveal;
    const x = 315 + progress * 1290;
    const y = 575 + Math.sin(progress * Math.PI * 6) * 3;

    return (
      <div
        key={index}
        style={{
          position: 'absolute',
          left: x,
          top: y,
          width: 24,
          height: 24,
          borderRadius: '50% 50% 50% 10%',
          backgroundColor: '#F14A00',
          transform: 'rotate(45deg)',
          boxShadow: '0 0 20px #F14A00',
          opacity: pipeReveal,
        }}
      />
    );
  });

  const particles = Array.from({length: 30}, (_, index) => (
    <div
      key={index}
      style={{
        position: 'absolute',
        left: seededRandom(index + 90) * 1920,
        top:
          (seededRandom(index + 130) * 1080 -
            frame * (0.1 + seededRandom(index + 160) * 0.25) +
            1200) %
            1200 -
          80,
        width: 4 + seededRandom(index + 200) * 7,
        height: 4 + seededRandom(index + 200) * 7,
        borderRadius: '50%',
        backgroundColor: index % 3 === 0 ? '#F14A00' : '#EEEEEE',
        opacity: 0.08 + seededRandom(index + 240) * 0.18,
      }}
    />
  ));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#8E1616',
        overflow: 'hidden',
        fontFamily: "'Noto Sans Devanagari', 'Inter', sans-serif",
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at ${
            20 + (frame / durationInFrames) * 65
          }% 55%, #F14A0052 0%, transparent 25%),
          linear-gradient(125deg, #5A0C0C 0%, #8E1616 48%, #AB231C 100%)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.1,
          backgroundImage:
            'repeating-linear-gradient(135deg, transparent 0px, transparent 46px, #EEEEEE 47px, transparent 49px)',
          transform: `translate(${(frame * 0.25) % 49}px, ${
            (frame * 0.18) % 49
          }px)`,
        }}
      />

      {particles}

      <AbsoluteFill
        style={{
          opacity: exitOp,
          transform: `translateX(${cameraX}px) scale(1.025)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 105,
            top: 75,
          }}
        >
          <div
            style={{
              color: '#F14A00',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 27,
              fontWeight: 800,
              letterSpacing: 5,
            }}
          >
            DOMESTIC ALTERNATIVE
          </div>
          <div
            style={{
              marginTop: 14,
              color: '#EEEEEE',
              fontSize: 72,
              fontWeight: 900,
            }}
          >
            खेत से फ्यूल टैंक तक
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 300,
            top: 585,
            width: 1320 * pipeReveal,
            height: 36,
            borderRadius: 18,
            backgroundColor: '#EEEEEE',
            boxShadow: '0 0 22px #EEEEEE44',
          }}
        />

        <div
          style={{
            position: 'absolute',
            left: 310,
            top: 596,
            width: 1300 * pipeReveal,
            height: 14,
            borderRadius: 7,
            background:
              'repeating-linear-gradient(90deg, #F14A00 0px, #F14A00 20px, transparent 20px, transparent 38px)',
            backgroundPositionX: -frame * 8,
          }}
        />

        {droplets}

        <svg
          width="1920"
          height="1080"
          viewBox="0 0 1920 1080"
          style={{position: 'absolute', inset: 0}}
        >
          {nodes.map((node) => {
            const entry = sp(node.delay, {damping: 9, stiffness: 155});

            return (
              <g
                key={node.title}
                transform={`translate(${node.x} 520) scale(${entry})`}
              >
                <circle
                  r="154"
                  fill="#681010"
                  stroke="#EEEEEE"
                  strokeWidth="9"
                />
                <circle
                  r={132 + Math.sin(frame * 0.04 + node.x) * 4}
                  fill="none"
                  stroke="#F14A00"
                  strokeWidth="5"
                  strokeDasharray="24 18"
                  transform={`rotate(${frame * 0.1})`}
                />

                {node.type === 'farm' && (
                  <>
                    <path
                      d="M-80 62 H80 V98 H-80 Z"
                      fill="#F14A00"
                    />
                    {[-60, -20, 20, 60].map((x, index) => (
                      <g key={index} transform={`translate(${x} 10)`}>
                        <path
                          d="M0 56 C-5 20 3 -18 0 -65"
                          fill="none"
                          stroke="#EEEEEE"
                          strokeWidth="10"
                          strokeLinecap="round"
                        />
                        <path
                          d="M-2 -10 C-36 -28 -37 -50 -5 -42"
                          fill="none"
                          stroke="#F14A00"
                          strokeWidth="10"
                          strokeLinecap="round"
                        />
                        <path
                          d="M2 12 C35 -5 40 -26 8 -22"
                          fill="none"
                          stroke="#F14A00"
                          strokeWidth="10"
                          strokeLinecap="round"
                        />
                      </g>
                    ))}
                  </>
                )}

                {node.type === 'refinery' && (
                  <>
                    <rect x="-80" y="-20" width="160" height="105" fill="#EEEEEE" />
                    <path
                      d="M-75 -20 L-25 -70 L25 -20 L78 -72 V-20"
                      fill="none"
                      stroke="#F14A00"
                      strokeWidth="13"
                      strokeLinejoin="round"
                    />
                    <rect x="-52" y="-105" width="28" height="85" fill="#EEEEEE" />
                    <rect x="32" y="-125" width="28" height="105" fill="#EEEEEE" />
                    <circle
                      cx="47"
                      cy="-150"
                      r={14 + Math.sin(frame * 0.08) * 4}
                      fill="#F14A00"
                      opacity="0.7"
                    />
                  </>
                )}

                {node.type === 'pump' && (
                  <>
                    <rect
                      x="-72"
                      y="-92"
                      width="125"
                      height="178"
                      rx="18"
                      fill="#EEEEEE"
                    />
                    <rect
                      x="-49"
                      y="-65"
                      width="79"
                      height="57"
                      rx="8"
                      fill="#8E1616"
                    />
                    <text
                      x="-9"
                      y="-26"
                      textAnchor="middle"
                      fill="#F14A00"
                      fontFamily="Inter"
                      fontSize="27"
                      fontWeight="900"
                    >
                      E20
                    </text>
                    <path
                      d="M53 -38 C105 -30 68 68 112 72"
                      fill="none"
                      stroke="#EEEEEE"
                      strokeWidth="13"
                      strokeLinecap="round"
                    />
                    <rect
                      x="92"
                      y="59"
                      width="45"
                      height="23"
                      rx="6"
                      fill="#F14A00"
                    />
                  </>
                )}

                {node.type === 'car' && (
                  <>
                    <path
                      d="M-101 24 L-72 -38 Q-58 -67 -15 -67 H42 Q74 -62 98 -17 L112 24 Z"
                      fill="#EEEEEE"
                    />
                    <rect
                      x="-114"
                      y="10"
                      width="228"
                      height="76"
                      rx="28"
                      fill="#EEEEEE"
                    />
                    <path
                      d="M-56 -42 H-14 V0 H-77 Z"
                      fill="#8E1616"
                    />
                    <path
                      d="M2 -42 H40 Q65 -38 82 0 H2 Z"
                      fill="#8E1616"
                    />
                    <circle cx="-65" cy="82" r="30" fill="#F14A00" />
                    <circle cx="69" cy="82" r="30" fill="#F14A00" />
                  </>
                )}

                <text
                  x="0"
                  y="222"
                  textAnchor="middle"
                  fill="#EEEEEE"
                  fontFamily="JetBrains Mono"
                  fontSize="28"
                  fontWeight="900"
                  letterSpacing="3"
                >
                  {node.title}
                </text>
              </g>
            );
          })}
        </svg>

        <div
          style={{
            position: 'absolute',
            left: 610,
            bottom: 78,
            padding: '18px 48px',
            border: '4px solid #EEEEEE',
            borderBottom: '13px solid #F14A00',
            backgroundColor: '#641010E8',
            color: '#EEEEEE',
            fontSize: 61,
            fontWeight: 900,
            transform: `scale(${sp(470, {damping: 9, stiffness: 165})})`,
            transformOrigin: 'center',
          }}
        >
          यहीं से आया ETHANOL
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at center, transparent 49%, #3907079E 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: 45,
          bottom: 30,
          color: '#EEEEEE',
          opacity: 0.3,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default DomesticFuelChain;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'CropToEthanolLab',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const CropToEthanolLab: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    }
  );

  const machineEntry = sp(160, {damping: 10, stiffness: 145});
  const dropletEntry = sp(390, {damping: 8, stiffness: 170});
  const cameraTilt = interpolate(frame, [0, durationInFrames], [-1.2, 1.2], {
    easing: Easing.inOut(Easing.quad),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const crops = [
    {type: 'sugarcane', label: 'गन्ना', delay: 35, x: 230},
    {type: 'corn', label: 'मक्का', delay: 80, x: 490},
    {type: 'grain', label: 'अनाज', delay: 125, x: 750},
  ];

  const molecules = Array.from({length: 16}, (_, index) => {
    const progress = ((frame * 0.005 + index / 16) % 1);
    const x = 900 + progress * 430;
    const y = 500 + Math.sin(progress * Math.PI * 5 + index) * 80;

    return (
      <div
        key={index}
        style={{
          position: 'absolute',
          left: x,
          top: y,
          width: 18 + seededRandom(index + 20) * 17,
          height: 18 + seededRandom(index + 20) * 17,
          borderRadius: '50%',
          border: '4px solid #EEEEEE',
          backgroundColor: index % 3 === 0 ? '#F14A00' : '#8E1616',
          opacity: machineEntry,
          boxShadow: index % 3 === 0 ? '0 0 18px #F14A00' : 'none',
        }}
      />
    );
  });

  const particles = Array.from({length: 28}, (_, index) => (
    <div
      key={index}
      style={{
        position: 'absolute',
        left: seededRandom(index + 100) * 1920,
        top:
          (seededRandom(index + 150) * 1080 -
            frame * (0.1 + seededRandom(index + 190) * 0.25) +
            1200) %
            1200 -
          80,
        width: 3 + seededRandom(index + 230) * 7,
        height: 3 + seededRandom(index + 230) * 7,
        borderRadius: '50%',
        backgroundColor: index % 2 === 0 ? '#EEEEEE' : '#F14A00',
        opacity: 0.08 + seededRandom(index + 270) * 0.18,
      }}
    />
  ));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#8E1616',
        overflow: 'hidden',
        fontFamily: "'Noto Sans Devanagari', 'Inter', sans-serif",
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 72% 49%, #F14A005C 0%, transparent 28%),
          radial-gradient(circle at 18% 25%, #FF834135 0%, transparent 28%),
          linear-gradient(135deg, #5B0D0D 0%, #8E1616 50%, #A9211A 100%)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.11,
          backgroundImage:
            'repeating-linear-gradient(90deg, transparent 0px, transparent 69px, #EEEEEE 70px, transparent 72px)',
          transform: `translateX(${(frame * 0.3) % 72}px)`,
        }}
      />

      {particles}

      <AbsoluteFill
        style={{
          opacity: exitOp,
          transform: `perspective(1300px) rotateY(${cameraTilt}deg) scale(1.025)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 105,
            top: 72,
          }}
        >
          <div
            style={{
              color: '#F14A00',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 27,
              fontWeight: 800,
              letterSpacing: 5,
            }}
          >
            BIOFUEL ASSEMBLY
          </div>
          <div
            style={{
              marginTop: 14,
              color: '#EEEEEE',
              fontSize: 69,
              fontWeight: 900,
            }}
          >
            फसलें बदलती हैं फ्यूल में
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            left: 115,
            top: 705,
            width: 990,
            height: 46,
            borderRadius: 24,
            border: '7px solid #EEEEEE',
            background:
              'repeating-linear-gradient(90deg, #F14A00 0px, #F14A00 42px, #641010 42px, #641010 84px)',
            backgroundPositionX: -frame * 7,
            boxShadow: '0 18px 40px #3B080888',
          }}
        />

        <svg
          width="1920"
          height="1080"
          viewBox="0 0 1920 1080"
          style={{position: 'absolute', inset: 0}}
        >
          {crops.map((crop) => {
            const entry = sp(crop.delay, {damping: 9, stiffness: 150});
            const bob = Math.sin(frame * 0.045 + crop.x) * 5;

            return (
              <g
                key={crop.type}
                transform={`translate(${crop.x} ${535 + bob}) scale(${entry})`}
              >
                <circle
                  r="132"
                  fill="#681010"
                  stroke="#EEEEEE"
                  strokeWidth="8"
                />

                {crop.type === 'sugarcane' && (
                  <>
                    {[-45, 0, 45].map((x, index) => (
                      <g key={index} transform={`translate(${x} 0)`}>
                        <path
                          d="M0 82 C-4 35 5 -10 0 -88"
                          fill="none"
                          stroke="#EEEEEE"
                          strokeWidth="15"
                          strokeLinecap="round"
                        />
                        <path
                          d="M-2 -27 C-46 -49 -48 -70 -6 -59"
                          fill="none"
                          stroke="#F14A00"
                          strokeWidth="12"
                          strokeLinecap="round"
                        />
                        <path
                          d="M3 6 C44 -14 49 -39 9 -29"
                          fill="none"
                          stroke="#F14A00"
                          strokeWidth="12"
                          strokeLinecap="round"
                        />
                      </g>
                    ))}
                  </>
                )}

                {crop.type === 'corn' && (
                  <>
                    <ellipse
                      cx="0"
                      cy="-4"
                      rx="53"
                      ry="94"
                      fill="#F14A00"
                      stroke="#EEEEEE"
                      strokeWidth="9"
                    />
                    {[-30, 0, 30].map((x) =>
                      [-48, -15, 18, 50].map((y) => (
                        <circle
                          key={`${x}-${y}`}
                          cx={x}
                          cy={y}
                          r="9"
                          fill="#EEEEEE"
                          opacity="0.72"
                        />
                      ))
                    )}
                    <path
                      d="M-48 71 C-103 34 -104 -32 -42 -54"
                      fill="none"
                      stroke="#EEEEEE"
                      strokeWidth="15"
                    />
                    <path
                      d="M47 72 C102 29 99 -35 40 -57"
                      fill="none"
                      stroke="#EEEEEE"
                      strokeWidth="15"
                    />
                  </>
                )}

                {crop.type === 'grain' && (
                  <>
                    <path
                      d="M0 92 V-95"
                      stroke="#EEEEEE"
                      strokeWidth="12"
                      strokeLinecap="round"
                    />
                    {[-65, -35, -5, 25, 55].map((y, index) => (
                      <g key={index}>
                        <ellipse
                          cx="-27"
                          cy={y}
                          rx="28"
                          ry="14"
                          fill="#F14A00"
                          transform={`rotate(${index % 2 === 0 ? -30 : -18} -27 ${y})`}
                        />
                        <ellipse
                          cx="27"
                          cy={y - 13}
                          rx="28"
                          ry="14"
                          fill="#EEEEEE"
                          transform={`rotate(${index % 2 === 0 ? 30 : 18} 27 ${
                            y - 13
                          })`}
                        />
                      </g>
                    ))}
                  </>
                )}

                <text
                  x="0"
                  y="190"
                  textAnchor="middle"
                  fill="#EEEEEE"
                  fontFamily="Noto Sans Devanagari"
                  fontWeight="900"
                  fontSize="37"
                >
                  {crop.label}
                </text>
              </g>
            );
          })}

          <g
            transform={`translate(1060 520) scale(${machineEntry})`}
            opacity={machineEntry}
          >
            <path
              d="M-150 -150 H85 L150 -78 V135 H-150 Z"
              fill="#EEEEEE"
            />
            <rect
              x="-115"
              y="-110"
              width="190"
              height="190"
              rx="30"
              fill="#681010"
            />
            <circle
              cx="-20"
              cy="-15"
              r={64 + Math.sin(frame * 0.08) * 4}
              fill="none"
              stroke="#F14A00"
              strokeWidth="14"
              strokeDasharray="24 14"
              transform={`rotate(${frame * 0.8} -20 -15)`}
            />
            <circle cx="-20" cy="-15" r="20" fill="#EEEEEE" />
            <path
              d="M84 -63 H160 V70 H90"
              fill="none"
              stroke="#F14A00"
              strokeWidth="25"
              strokeLinejoin="round"
            />
            <text
              x="-20"
              y="125"
              textAnchor="middle"
              fill="#8E1616"
              fontFamily="JetBrains Mono"
              fontWeight="900"
              fontSize="28"
            >
              FERMENT
            </text>
          </g>

          <g
            transform={`translate(1535 505) scale(${dropletEntry})`}
            opacity={dropletEntry}
          >
            <path
              d="M0 -210 C115 -65 175 20 175 112 C175 220 98 282 0 282 C-98 282 -175 220 -175 112 C-175 20 -115 -65 0 -210 Z"
              fill="#F14A00"
              stroke="#EEEEEE"
              strokeWidth="12"
            />
            <path
              d="M-72 75 C-55 18 -12 -16 45 -32"
              fill="none"
              stroke="#EEEEEE"
              strokeWidth="18"
              strokeLinecap="round"
              opacity="0.72"
            />
            <text
              x="0"
              y="100"
              textAnchor="middle"
              fill="#EEEEEE"
              fontFamily="Inter"
              fontWeight="950"
              fontSize="82"
            >
              E20
            </text>
            <text
              x="0"
              y="155"
              textAnchor="middle"
              fill="#EEEEEE"
              fontFamily="JetBrains Mono"
              fontWeight="900"
              fontSize="28"
            >
              ETHANOL
            </text>
          </g>
        </svg>

        {molecules}

        <div
          style={{
            position: 'absolute',
            right: 145,
            bottom: 80,
            padding: '16px 35px',
            borderLeft: '9px solid #F14A00',
            backgroundColor: '#621010DF',
            color: '#EEEEEE',
            fontSize: 45,
            fontWeight: 850,
            opacity: interpolate(frame, [500, 570], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
          }}
        >
          देश में उगाई गई फसलों से
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at center, transparent 49%, #3907079E 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: 45,
          bottom: 30,
          color: '#EEEEEE',
          opacity: 0.3,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default CropToEthanolLab;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'MacroBenefitBlueprint',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const MacroBenefitBlueprint: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    }
  );

  const cameraPan = interpolate(frame, [0, durationInFrames], [-20, 26], {
    easing: Easing.inOut(Easing.quad),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const cards = [
    {
      x: 360,
      title: 'IMPORT BILL',
      hindi: 'कम',
      delay: 50,
      type: 'down',
    },
    {
      x: 960,
      title: 'FARMER INCOME',
      hindi: 'नया स्रोत',
      delay: 145,
      type: 'up',
    },
    {
      x: 1560,
      title: 'POLLUTION',
      hindi: 'कम',
      delay: 240,
      type: 'cloud',
    },
  ];

  const sealEntry = sp(410, {damping: 8, stiffness: 175});
  const blueprintScan = interpolate(frame % 220, [0, 220], [125, 930]);

  const particles = Array.from({length: 32}, (_, index) => (
    <div
      key={index}
      style={{
        position: 'absolute',
        left: seededRandom(index + 20) * 1920,
        top:
          (seededRandom(index + 70) * 1080 +
            frame * (0.08 + seededRandom(index + 110) * 0.15)) %
          1080,
        width: 3 + seededRandom(index + 150) * 7,
        height: 3 + seededRandom(index + 150) * 7,
        borderRadius: '50%',
        backgroundColor: index % 3 === 0 ? '#F14A00' : '#EEEEEE',
        opacity: 0.07 + seededRandom(index + 200) * 0.17,
      }}
    />
  ));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#8E1616',
        overflow: 'hidden',
        fontFamily: "'Noto Sans Devanagari', 'Inter', sans-serif",
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 50% 50%, #F14A003F 0%, transparent 39%),
          linear-gradient(132deg, #5B0C0C 0%, #8E1616 50%, #A9211B 100%)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.13,
          backgroundImage:
            'linear-gradient(#EEEEEE 1px, transparent 1px), linear-gradient(90deg, #EEEEEE 1px, transparent 1px)',
          backgroundSize: '48px 48px',
          transform: `translate(${(frame * 0.16) % 48}px, ${
            (frame * 0.1) % 48
          }px)`,
        }}
      />

      {particles}

      <div
        style={{
          position: 'absolute',
          left: 80,
          right: 80,
          top: blueprintScan,
          height: 4,
          backgroundColor: '#EEEEEE',
          boxShadow: '0 0 18px #EEEEEE, 0 0 42px #F14A00',
          opacity: 0.3,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: exitOp,
          transform: `translateX(${cameraPan}px) scale(1.02)`,
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 0,
            right: 0,
            top: 70,
            textAlign: 'center',
          }}
        >
          <div
            style={{
              color: '#F14A00',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 26,
              fontWeight: 800,
              letterSpacing: 6,
            }}
          >
            THE OFFICIAL PROMISE
          </div>
          <div
            style={{
              marginTop: 14,
              color: '#EEEEEE',
              fontSize: 72,
              fontWeight: 900,
            }}
          >
            कागज़ पर शानदार योजना
          </div>
        </div>

        <svg
          width="1920"
          height="1080"
          viewBox="0 0 1920 1080"
          style={{position: 'absolute', inset: 0}}
        >
          <path
            d="M360 505 H1560"
            fill="none"
            stroke="#EEEEEE"
            strokeWidth="7"
            strokeDasharray="20 19"
            strokeDashoffset={-frame * 4}
            opacity="0.35"
          />

          {cards.map((card, index) => {
            const entry = sp(card.delay, {damping: 9, stiffness: 150});
            const pulse = 1 + Math.sin(frame * 0.05 + index * 1.7) * 0.025;

            return (
              <g
                key={card.title}
                transform={`translate(${card.x} 520) scale(${entry * pulse})`}
              >
                <rect
                  x="-225"
                  y="-250"
                  width="450"
                  height="510"
                  rx="48"
                  fill="#651010"
                  stroke="#EEEEEE"
                  strokeWidth="8"
                />
                <rect
                  x="-195"
                  y="-220"
                  width="390"
                  height="45"
                  rx="20"
                  fill="#F14A00"
                  opacity="0.28"
                />

                {card.type === 'down' && (
                  <>
                    <rect
                      x="-102"
                      y="-90"
                      width="204"
                      height="142"
                      rx="24"
                      fill="none"
                      stroke="#EEEEEE"
                      strokeWidth="14"
                    />
                    <text
                      x="0"
                      y="4"
                      textAnchor="middle"
                      fill="#EEEEEE"
                      fontFamily="Inter"
                      fontSize="79"
                      fontWeight="900"
                    >
                      ₹
                    </text>
                    <path
                      d="M0 83 V163 M-39 127 L0 167 L39 127"
                      fill="none"
                      stroke="#F14A00"
                      strokeWidth="17"
                      strokeLinecap="round"
                      strokeLinejoin="round"
                    />
                  </>
                )}

                {card.type === 'up' && (
                  <>
                    <path
                      d="M-115 75 H115 V137 H-115 Z"
                      fill="#F14A00"
                    />
                    {[-76, -25, 25, 76].map((x, i) => (
                      <g key={i} transform={`translate(${x} 0)`}>
                        <path
                          d="M0 71 V-93"
                          stroke="#EEEEEE"
                          strokeWidth="12"
                          strokeLinecap="round"
                        />
                        <path
                          d="M0 -37 C-32 -50 -37 -70 -5 -65"
                          fill="none"
                          stroke="#F14A00"
                          strokeWidth="11"
                        />
                        <path
                          d="M0 -4 C31 -19 36 -40 5 -33"
                          fill="none"
                          stroke="#F14A00"
                          strokeWidth="11"
                        />
                      </g>
                    ))}
                    <path
                      d="M0 -125 V-177 M-35 -143 L0 -180 L35 -143"
                      fill="none"
                      stroke="#F14A00"
                      strokeWidth="15"
                      strokeLinecap="round"
                      strokeLinejoin="round"
                    />
                  </>
                )}

                {card.type === 'cloud' && (
                  <>
                    <path
                      d="M-112 38 C-135 -32 -66 -75 -15 -49 C10 -114 116 -94 115 -23 C175 -10 169 76 103 82 H-88 C-154 82 -165 8 -112 38 Z"
                      fill="#EEEEEE"
                    />
                    {[-60, 0, 60].map((x, i) => (
                      <path
                        key={i}
                        d={`M${x} 119 V${167 - i * 12}`}
                        stroke="#F14A00"
                        strokeWidth="15"
                        strokeLinecap="round"
                        opacity={0.8 - i * 0.18}
                      />
                    ))}
                    <path
                      d="M-98 -137 H99"
                      stroke="#F14A00"
                      strokeWidth="15"
                      strokeLinecap="round"
                      transform={`scale(${1 - Math.sin(frame * 0.04) * 0.08} 1)`}
                    />
                  </>
                )}

                <text
                  x="0"
                  y="203"
                  textAnchor="middle"
                  fill="#EEEEEE"
                  fontFamily="JetBrains Mono"
                  fontSize="27"
                  fontWeight="900"
                  letterSpacing="2"
                >
                  {card.title}
                </text>
                <text
                  x="0"
                  y="250"
                  textAnchor="middle"
                  fill="#F14A00"
                  fontFamily="Noto Sans Devanagari"
                  fontSize="37"
                  fontWeight="900"
                >
                  {card.hindi}
                </text>
              </g>
            );
          })}
        </svg>

        <div
          style={{
            position: 'absolute',
            left: 695,
            bottom: 55,
            width: 530,
            height: 150,
            border: '9px double #F14A00',
            borderRadius: 75,
            backgroundColor: '#8E1616EE',
            color: '#EEEEEE',
            display: 'flex',
            justifyContent: 'center',
            alignItems: 'center',
            fontSize: 48,
            fontWeight: 950,
            transform: `scale(${sealEntry}) rotate(-3deg)`,
            transformOrigin: 'center',
            boxShadow: '0 0 45px #F14A0055',
          }}
        >
          MACRO BENEFITS
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at center, transparent 50%, #3907079F 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: 45,
          bottom: 30,
          color: '#EEEEEE',
          opacity: 0.3,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default MacroBenefitBlueprint;
```

```tsx
import React from 'react';
import {
  useCurrentFrame,
  useVideoConfig,
  interpolate,
  Easing,
  spring,
  AbsoluteFill,
} from 'remotion';

export const compositionConfig = {
  id: 'MacroToMicroReality',
  durationInSeconds: 30,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const MacroToMicroReality: React.FC = () => {
  const frame = useCurrentFrame();
  const {fps, durationInFrames} = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({
      frame: Math.max(0, frame - delay),
      fps,
      config: {
        damping: 12,
        mass: 0.4,
        stiffness: 120,
        ...config,
      },
    });

  const exitOp = interpolate(
    frame,
    [durationInFrames - 15, durationInFrames],
    [1, 0],
    {
      extrapolateLeft: 'clamp',
      extrapolateRight: 'clamp',
    }
  );

  const macroEntry = sp(25, {damping: 10, stiffness: 145});
  const microEntry = sp(305, {damping: 9, stiffness: 160});

  const microReveal = interpolate(frame, [260, 510], [0, 100], {
    easing: Easing.inOut(Easing.cubic),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const cameraZoom = interpolate(frame, [0, 260, 600, durationInFrames], [
    1.02, 1.08, 1.15, 1.2,
  ], {
    easing: Easing.inOut(Easing.quad),
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const walletDrain = interpolate(frame, [430, 750], [1, 0.18], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const needle = interpolate(frame, [420, 740], [45, -55], {
    extrapolateLeft: 'clamp',
    extrapolateRight: 'clamp',
  });

  const gears = [
    {x: 360, y: 400, r: 175, teeth: 12, speed: 0.22},
    {x: 655, y: 570, r: 128, teeth: 10, speed: -0.3},
    {x: 930, y: 345, r: 205, teeth: 14, speed: 0.16},
  ];

  const particles = Array.from({length: 36}, (_, index) => (
    <div
      key={index}
      style={{
        position: 'absolute',
        left: seededRandom(index + 30) * 1920,
        top:
          (seededRandom(index + 70) * 1080 -
            frame * (0.1 + seededRandom(index + 110) * 0.22) +
            1200) %
            1200 -
          70,
        width: 3 + seededRandom(index + 150) * 7,
        height: 3 + seededRandom(index + 150) * 7,
        borderRadius: index % 3 === 0 ? 1 : '50%',
        backgroundColor: index % 2 === 0 ? '#EEEEEE' : '#F14A00',
        opacity: 0.08 + seededRandom(index + 190) * 0.18,
      }}
    />
  ));

  return (
    <AbsoluteFill
      style={{
        backgroundColor: '#8E1616',
        overflow: 'hidden',
        fontFamily: "'Noto Sans Devanagari', 'Inter', sans-serif",
      }}
    >
      {/* Gradient + Shape Layers + Texture + Particles + Content + Character + Vignette + Watermark */}

      <AbsoluteFill
        style={{
          background: `radial-gradient(circle at 45% 42%, #F14A0054 0%, transparent 31%),
          radial-gradient(circle at 80% 72%, #FF854035 0%, transparent 29%),
          linear-gradient(133deg, #580C0C 0%, #8E1616 50%, #A7221B 100%)`,
        }}
      />

      <AbsoluteFill
        style={{
          opacity: 0.1,
          backgroundImage:
            'repeating-radial-gradient(circle at center, transparent 0px, transparent 54px, #EEEEEE 55px, transparent 57px)',
          transform: `scale(${1 + frame / 7000})`,
        }}
      />

      {particles}

      <AbsoluteFill
        style={{
          opacity: exitOp,
          transform: `scale(${cameraZoom})`,
          transformOrigin: '72% 58%',
        }}
      >
        <div
          style={{
            position: 'absolute',
            left: 105,
            top: 75,
            zIndex: 20,
          }}
        >
          <div
            style={{
              color: '#F14A00',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 27,
              fontWeight: 800,
              letterSpacing: 5,
            }}
          >
            POLICY VS POCKET
          </div>
          <div
            style={{
              marginTop: 14,
              color: '#EEEEEE',
              fontSize: 69,
              fontWeight: 900,
            }}
          >
            असली समस्या नीचे छिपी है
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            inset: 0,
            opacity: macroEntry,
          }}
        >
          <svg
            width="1920"
            height="1080"
            viewBox="0 0 1920 1080"
            style={{position: 'absolute', inset: 0}}
          >
            {gears.map((gear, gearIndex) => (
              <g
                key={gearIndex}
                transform={`translate(${gear.x} ${gear.y}) rotate(${
                  frame * gear.speed
                })`}
              >
                {Array.from({length: gear.teeth}, (_, index) => (
                  <rect
                    key={index}
                    x="-22"
                    y={-gear.r - 31}
                    width="44"
                    height="65"
                    rx="8"
                    fill={gearIndex % 2 === 0 ? '#EEEEEE' : '#F14A00'}
                    transform={`rotate(${(360 / gear.teeth) * index})`}
                  />
                ))}
                <circle
                  r={gear.r}
                  fill="#681010"
                  stroke={gearIndex % 2 === 0 ? '#EEEEEE' : '#F14A00'}
                  strokeWidth="18"
                />
                <circle
                  r={gear.r * 0.48}
                  fill="#8E1616"
                  stroke="#EEEEEE"
                  strokeWidth="12"
                />
                <circle r={gear.r * 0.16} fill="#F14A00" />
              </g>
            ))}

            <g transform="translate(1205 460)">
              <circle
                r="285"
                fill="#671010"
                stroke="#EEEEEE"
                strokeWidth="11"
              />
              <ellipse
                rx="225"
                ry="88"
                fill="none"
                stroke="#F14A00"
                strokeWidth="11"
                transform={`rotate(${frame * 0.15})`}
              />
              <ellipse
                rx="225"
                ry="88"
                fill="none"
                stroke="#EEEEEE"
                strokeWidth="7"
                transform={`rotate(${60 + frame * 0.12})`}
                opacity="0.5"
              />
              <ellipse
                rx="225"
                ry="88"
                fill="none"
                stroke="#EEEEEE"
                strokeWidth="7"
                transform={`rotate(${-60 + frame * 0.12})`}
                opacity="0.5"
              />
              <circle r="58" fill="#F14A00" />
              <text
                x="0"
                y="13"
                textAnchor="middle"
                fill="#EEEEEE"
                fontFamily="Inter"
                fontSize="38"
                fontWeight="900"
              >
                GOVT
              </text>
            </g>
          </svg>

          <div
            style={{
              position: 'absolute',
              left: 250,
              bottom: 90,
              color: '#EEEEEE',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 49,
              fontWeight: 900,
              letterSpacing: 5,
            }}
          >
            MACRO LEVEL
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            inset: 0,
            clipPath: `circle(${microReveal}% at 73% 62%)`,
            background:
              'radial-gradient(circle at 73% 62%, #B82C20 0%, #8E1616 45%, #580C0C 100%)',
            opacity: microEntry,
          }}
        >
          <AbsoluteFill
            style={{
              opacity: 0.12,
              backgroundImage:
                'linear-gradient(#EEEEEE 1px, transparent 1px), linear-gradient(90deg, #EEEEEE 1px, transparent 1px)',
              backgroundSize: '55px 55px',
              transform: `translate(${(frame * 0.22) % 55}px, ${
                (frame * 0.15) % 55
              }px)`,
            }}
          />

          <svg
            width="1920"
            height="1080"
            viewBox="0 0 1920 1080"
            style={{position: 'absolute', inset: 0}}
          >
            <g transform="translate(770 580)">
              <path
                d="M-280 62 L-220 -65 Q-198 -115 -100 -115 H145 Q216 -107 274 -10 L310 62 Z"
                fill="#EEEEEE"
              />
              <rect
                x="-305"
                y="35"
                width="640"
                height="170"
                rx="58"
                fill="#EEEEEE"
              />
              <path
                d="M-176 -72 H-55 V25 H-222 Z"
                fill="#8E1616"
              />
              <path
                d="M-17 -72 H135 Q185 -66 228 25 H-17 Z"
                fill="#8E1616"
              />
              <circle cx="-185" cy="202" r="78" fill="#4C0A0A" />
              <circle cx="-185" cy="202" r="34" fill="#F14A00" />
              <circle cx="215" cy="202" r="78" fill="#4C0A0A" />
              <circle cx="215" cy="202" r="34" fill="#F14A00" />

              <g transform="translate(-5 -160)">
                <path
                  d="M-105 27 A120 120 0 0 1 105 27"
                  fill="none"
                  stroke="#F14A00"
                  strokeWidth="20"
                  strokeLinecap="round"
                />
                <g transform={`rotate(${needle})`}>
                  <line
                    x1="0"
                    y1="17"
                    x2="0"
                    y2="-94"
                    stroke="#EEEEEE"
                    strokeWidth="15"
                    strokeLinecap="round"
                  />
                </g>
                <circle r="17" fill="#EEEEEE" />
              </g>
            </g>

            <g transform="translate(1450 575)">
              <rect
                x="-220"
                y="-160"
                width="440"
                height="315"
                rx="52"
                fill="#651010"
                stroke="#EEEEEE"
                strokeWidth="11"
              />
              <rect
                x="52"
                y="-60"
                width="205"
                height="122"
                rx="28"
                fill="#8E1616"
                stroke="#EEEEEE"
                strokeWidth="9"
              />
              <circle cx="102" cy="0" r="18" fill="#F14A00" />

              <rect
                x="-165"
                y={-97 + (1 - walletDrain) * 150}
                width="260"
                height={175 * walletDrain}
                rx="22"
                fill="#F14A00"
                opacity="0.92"
              />

              <text
                x="-40"
                y="34"
                textAnchor="middle"
                fill="#EEEEEE"
                fontFamily="Inter"
                fontSize="100"
                fontWeight="950"
              >
                ₹
              </text>

              {Array.from({length: 8}, (_, index) => {
                const cycle = (frame * 1.35 + index * 54) % 350;

                return (
                  <g
                    key={index}
                    transform={`translate(${
                      -100 + seededRandom(index + 100) * 180
                    } ${170 + cycle}) rotate(${frame + index * 30})`}
                    opacity={interpolate(cycle, [0, 45, 280, 350], [0, 1, 1, 0])}
                  >
                    <rect
                      x="-35"
                      y="-17"
                      width="70"
                      height="34"
                      rx="5"
                      fill="#F14A00"
                      stroke="#EEEEEE"
                      strokeWidth="4"
                    />
                    <text
                      x="0"
                      y="10"
                      textAnchor="middle"
                      fill="#EEEEEE"
                      fontFamily="Inter"
                      fontSize="23"
                      fontWeight="900"
                    >
                      ₹
                    </text>
                  </g>
                );
              })}
            </g>
          </svg>

          <div
            style={{
              position: 'absolute',
              left: 665,
              bottom: 85,
              color: '#F14A00',
              fontFamily: "'JetBrains Mono', monospace",
              fontSize: 53,
              fontWeight: 950,
              letterSpacing: 6,
            }}
          >
            MICRO LEVEL
          </div>
        </div>

        <div
          style={{
            position: 'absolute',
            right: 95,
            top: 160,
            width: 510,
            padding: '22px 35px',
            borderLeft: '11px solid #F14A00',
            backgroundColor: '#5E0D0DE8',
            color: '#EEEEEE',
            fontSize: 48,
            fontWeight: 900,
            lineHeight: 1.16,
            opacity: interpolate(frame, [520, 590], [0, 1], {
              extrapolateLeft: 'clamp',
              extrapolateRight: 'clamp',
            }),
            boxShadow: '15px 15px 0 #3D0808',
          }}
        >
          असली कीमत
          <br />
          आम आदमी देता है
        </div>
      </AbsoluteFill>

      <AbsoluteFill
        style={{
          background:
            'radial-gradient(circle at center, transparent 49%, #390707A2 100%)',
          pointerEvents: 'none',
        }}
      />

      <div
        style={{
          position: 'absolute',
          right: 45,
          bottom: 30,
          color: '#EEEEEE',
          opacity: 0.3,
          fontFamily: 'Inter, sans-serif',
          fontSize: 22,
          fontWeight: 600,
        }}
      >
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default MacroToMicroReality;
```

