```tsx
import React from 'react';
Audio: "The catch is that the county assessor does not do this for you. They don't check your age and automatically apply it."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene31_AssessorCatch',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene31_AssessorCatch: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Camera zoom in
  const camScale = interpolate(frame, [0, 300], [1, 1.05], { extrapolateRight: 'clamp' });

  // Stamp animation
  const stampDrop = sp(30, { damping: 8, stiffness: 200 });
  const stampRotate = interpolate(stampDrop, [0, 1], [-25, -8]);

  // Word reveals
  const word1 = sp(15);
  const word2 = sp(40);
  const word3 = sp(70);

  // Document slide
  const docSlide = sp(90);
  const docX = interpolate(docSlide, [0, 1], [-200, 0]);

  // X mark draw
  const xDraw = interpolate(frame, [120, 150], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Shake on stamp
  const shake = frame > 30 && frame < 50 ? Math.sin(frame * 2) * (50 - frame) * 0.3 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#E3FDFD', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(254,236,65,0.15) 0%, transparent 60%)',
      }} />

      {/* Shape Layers - drifting geometric shapes */}
      {[...Array(6)].map((_, i) => {
        const r = seededRandom(i + 1);
        const r2 = seededRandom(i + 100);
        const size = 200 + r * 300;
        const drift = Math.sin(frame * 0.008 + i) * 30;
        return (
          <div key={`shape-${i}`} style={{
            position: 'absolute',
            left: `${r * 100}%`,
            top: `${r2 * 100}%`,
            width: size,
            height: size,
            borderRadius: i % 2 === 0 ? '50%' : '0%',
            border: `2px solid #FEEC41`,
            opacity: 0.06,
            transform: `translateY(${drift}px) rotate(${frame * 0.1 + i * 60}deg)`,
          }} />
        );
      })}

      {/* Motion texture - grid lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {[...Array(20)].map((_, i) => (
          <line key={`v-${i}`} x1={i * 96} y1="0" x2={i * 96} y2="1080" stroke="#000000" strokeWidth="1" />
        ))}
        {[...Array(12)].map((_, i) => (
          <line key={`h-${i}`} x1="0" y1={i * 90} x2="1920" y2={i * 90} stroke="#000000" strokeWidth="1" />
        ))}
      </svg>

      {/* Floating particles */}
      {[...Array(25)].map((_, i) => {
        const r = seededRandom(i + 50);
        const r2 = seededRandom(i + 200);
        const py = (r2 * 1080 + frame * (0.3 + r * 0.8)) % 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute',
            left: r * 1920,
            top: py,
            width: 4 + r * 6,
            height: 4 + r * 6,
            borderRadius: '50%',
            backgroundColor: '#FEEC41',
            opacity: 0.4,
          }} />
        );
      })}

      {/* Main content */}
      <AbsoluteFill style={{
        transform: `scale(${camScale}) translateX(${shake}px)`,
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
        padding: '60px',
      }}>
        {/* Top label */}
        <div style={{
          opacity: word1,
          transform: `translateY(${(1 - word1) * 30}px)`,
          backgroundColor: '#000000',
          color: '#FEEC41',
          padding: '14px 36px',
          fontFamily: 'Inter, Poppins',
          fontWeight: 800,
          fontSize: 32,
          letterSpacing: 4,
          marginBottom: 40,
        }}>
          THE CATCH
        </div>

        {/* Document with X stamp */}
        <div style={{
          position: 'relative',
          transform: `translateX(${docX}px)`,
          opacity: docSlide,
          marginBottom: 50,
        }}>
          <div style={{
            width: 480,
            height: 280,
            backgroundColor: '#F5F5F5',
            border: '6px solid #000000',
            padding: '30px',
            boxShadow: '12px 12px 0 #000000',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 24,
              fontWeight: 700,
              color: '#000000',
              marginBottom: 16,
            }}>
              COUNTY ASSESSOR
            </div>
            <div style={{ height: 4, backgroundColor: '#000000', width: '60%', marginBottom: 20 }} />
            <div style={{ height: 8, backgroundColor: '#9E9E9E', width: '90%', marginBottom: 10, opacity: 0.4 }} />
            <div style={{ height: 8, backgroundColor: '#9E9E9E', width: '75%', marginBottom: 10, opacity: 0.4 }} />
            <div style={{ height: 8, backgroundColor: '#9E9E9E', width: '85%', marginBottom: 10, opacity: 0.4 }} />
            <div style={{ height: 8, backgroundColor: '#9E9E9E', width: '50%', opacity: 0.4 }} />
          </div>

          {/* Red X stamp */}
          <div style={{
            position: 'absolute',
            top: -30,
            right: -60,
            transform: `scale(${stampDrop}) rotate(${stampRotate}deg)`,
            opacity: stampDrop,
          }}>
            <svg width="200" height="200" viewBox="0 0 200 200">
              <circle cx="100" cy="100" r="85" fill="none" stroke="#ff0000" strokeWidth="10" />
              <circle cx="100" cy="100" r="70" fill="none" stroke="#ff0000" strokeWidth="4" />
              <path
                d="M 55 55 L 145 145"
                stroke="#ff0000"
                strokeWidth="14"
                strokeLinecap="round"
                strokeDasharray="130"
                strokeDashoffset={(1 - xDraw) * 130}
              />
              <path
                d="M 145 55 L 55 145"
                stroke="#ff0000"
                strokeWidth="14"
                strokeLinecap="round"
                strokeDasharray="130"
                strokeDashoffset={(1 - xDraw) * 130}
              />
              <text x="100" y="180" textAnchor="middle" fontFamily="Inter" fontWeight="900" fontSize="20" fill="#ff0000">DENIED</text>
            </svg>
          </div>
        </div>

        {/* Main message */}
        <div style={{
          opacity: word2,
          transform: `translateY(${(1 - word2) * 40}px)`,
          fontFamily: 'Inter, Poppins',
          fontSize: 76,
          fontWeight: 900,
          color: '#000000',
          textAlign: 'center',
          lineHeight: 1.05,
          letterSpacing: -2,
          marginBottom: 20,
        }}>
          THEY WON'T DO IT <span style={{ backgroundColor: '#FEEC41', padding: '4px 16px' }}>FOR YOU</span>
        </div>

        {/* Subtext */}
        <div style={{
          opacity: word3,
          transform: `translateY(${(1 - word3) * 20}px)`,
          fontFamily: 'Inter',
          fontSize: 34,
          fontWeight: 600,
          color: '#000000',
          opacity: word3 * 0.75,
          textAlign: 'center',
        }}>
          No age check → No auto-apply
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene31_AssessorCatch;
```

```tsx
import React from 'react';
Audio: "You have to walk into the county clerk's office or go to their website and file the application yourself."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene32_TwoPaths',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene32_TwoPaths: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const title = sp(10);
  const leftCard = sp(35, { damping: 10 });
  const rightCard = sp(55, { damping: 10 });
  const orBadge = sp(85, { damping: 6, stiffness: 180 });
  const bottomLine = sp(120);

  const camPan = interpolate(frame, [0, 330], [-15, 15], { extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#E3FDFD', overflow: 'hidden', opacity: exitOp }}>
      {/* Diagonal gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(254,236,65,0.18) 0%, transparent 50%, rgba(0,0,0,0.05) 100%)',
      }} />

      {/* Shape Layers - triangles */}
      {[...Array(5)].map((_, i) => {
        const r = seededRandom(i + 10);
        const r2 = seededRandom(i + 110);
        return (
          <div key={`tri-${i}`} style={{
            position: 'absolute',
            left: `${r * 100}%`,
            top: `${r2 * 100}%`,
            width: 0,
            height: 0,
            borderLeft: `${100 + r * 100}px solid transparent`,
            borderRight: `${100 + r * 100}px solid transparent`,
            borderBottom: `${180 + r * 100}px solid #FEEC41`,
            opacity: 0.05,
            transform: `rotate(${frame * 0.15 + i * 72}deg)`,
          }} />
        );
      })}

      {/* Diagonal line texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.04 }}>
        {[...Array(40)].map((_, i) => (
          <line key={i} x1={-200 + i * 80} y1="0" x2={i * 80} y2="1080" stroke="#000000" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {[...Array(20)].map((_, i) => {
        const r = seededRandom(i + 60);
        const r2 = seededRandom(i + 160);
        const px = (r * 1920 + frame * 0.5) % 1920;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute',
            left: px,
            top: r2 * 1080,
            width: 5,
            height: 5,
            borderRadius: '50%',
            backgroundColor: '#000000',
            opacity: 0.2,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute',
        top: 90,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: title,
        transform: `translateY(${(1 - title) * -40}px) translateX(${camPan}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 38,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 6,
          opacity: 0.6,
          marginBottom: 10,
        }}>
          YOU MUST FILE IT YOURSELF
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 96,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -3,
        }}>
          TWO <span style={{ backgroundColor: '#FEEC41', padding: '4px 24px' }}>WAYS</span> TO ACT
        </div>
      </div>

      {/* Cards Container */}
      <div style={{
        position: 'absolute',
        top: 380,
        left: 0,
        right: 0,
        display: 'flex',
        justifyContent: 'center',
        gap: 80,
        transform: `translateX(${camPan * 0.5}px)`,
      }}>
        {/* LEFT CARD - Office */}
        <div style={{
          width: 480,
          opacity: leftCard,
          transform: `translateY(${(1 - leftCard) * 60}px) scale(${leftCard})`,
        }}>
          <div style={{
            backgroundColor: '#000000',
            padding: 50,
            borderRadius: 16,
            boxShadow: '10px 10px 0 #FEEC41',
          }}>
            {/* Building Icon */}
            <svg width="120" height="120" viewBox="0 0 120 120" style={{ display: 'block', margin: '0 auto 24px' }}>
              <rect x="15" y="40" width="90" height="70" fill="#FEEC41" />
              <polygon points="10,40 60,15 110,40" fill="#FEEC41" />
              <rect x="25" y="55" width="15" height="20" fill="#000000" />
              <rect x="52" y="55" width="15" height="20" fill="#000000" />
              <rect x="80" y="55" width="15" height="20" fill="#000000" />
              <rect x="50" y="85" width="20" height="25" fill="#000000" />
              <polygon points="50,40 60,30 70,40" fill="#000000" />
            </svg>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 700,
              color: '#FEEC41',
              textAlign: 'center',
              letterSpacing: 3,
              marginBottom: 8,
            }}>
              OPTION 1
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 46,
              fontWeight: 900,
              color: '#FEEC41',
              textAlign: 'center',
              lineHeight: 1.1,
            }}>
              VISIT CLERK'S OFFICE
            </div>
          </div>
        </div>

        {/* RIGHT CARD - Website */}
        <div style={{
          width: 480,
          opacity: rightCard,
          transform: `translateY(${(1 - rightCard) * 60}px) scale(${rightCard})`,
        }}>
          <div style={{
            backgroundColor: '#FEEC41',
            padding: 50,
            borderRadius: 16,
            border: '6px solid #000000',
            boxShadow: '10px 10px 0 #000000',
          }}>
            {/* Computer Icon */}
            <svg width="120" height="120" viewBox="0 0 120 120" style={{ display: 'block', margin: '0 auto 24px' }}>
              <rect x="10" y="20" width="100" height="65" rx="4" fill="#000000" />
              <rect x="18" y="28" width="84" height="49" fill="#E3FDFD" />
              <rect x="40" y="85" width="40" height="8" fill="#000000" />
              <rect x="30" y="93" width="60" height="6" rx="2" fill="#000000" />
              <text x="60" y="58" textAnchor="middle" fontFamily="JetBrains Mono" fontWeight="900" fontSize="18" fill="#000000">www</text>
            </svg>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 700,
              color: '#000000',
              textAlign: 'center',
              letterSpacing: 3,
              marginBottom: 8,
            }}>
              OPTION 2
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 46,
              fontWeight: 900,
              color: '#000000',
              textAlign: 'center',
              lineHeight: 1.1,
            }}>
              FILE ONLINE
            </div>
          </div>
        </div>
      </div>

      {/* OR Badge */}
      <div style={{
        position: 'absolute',
        top: 580,
        left: '50%',
        transform: `translateX(-50%) scale(${orBadge}) rotate(${orBadge * 360}deg)`,
        opacity: orBadge,
        width: 100,
        height: 100,
        borderRadius: '50%',
        backgroundColor: '#ff0000',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontFamily: 'Inter',
        fontSize: 32,
        fontWeight: 900,
        color: '#F5F5F5',
        border: '4px solid #000000',
        zIndex: 10,
      }}>
        OR
      </div>

      {/* Bottom strip */}
      <div style={{
        position: 'absolute',
        bottom: 100,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: bottomLine,
        transform: `translateY(${(1 - bottomLine) * 30}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#000000',
          color: '#FEEC41',
          padding: '20px 50px',
          fontFamily: 'Inter',
          fontSize: 38,
          fontWeight: 800,
          letterSpacing: 1,
        }}>
          → THE APPLICATION IS ON YOU ←
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 60%, rgba(0,0,0,0.2) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene32_TwoPaths;
```

```tsx
import React from 'react';
Audio: "If you haven't done this since you turned 65, you are overpaying right now. Shield number two is the property tax freeze."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene33_Shield2Reveal',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene33_Shield2Reveal: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Phase 1 — Overpaying warning (0-90)
  const warn1 = sp(10);
  const warn2 = sp(35);
  const moneyFly = interpolate(frame, [50, 120], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Phase 2 — Shield 2 reveal (90+)
  const transitionWipe = interpolate(frame, [85, 110], [0, 100], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const shieldDrop = sp(115, { damping: 8, stiffness: 140 });
  const shieldText = sp(140);
  const freezeWord = sp(170);

  // Ice/freeze pulse
  const freezePulse = 1 + Math.sin(frame * 0.15) * 0.03;

  return (
    <AbsoluteFill style={{ backgroundColor: '#E3FDFD', overflow: 'hidden', opacity: exitOp }}>
      {/* Phase 1 Background */}
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at center, rgba(255,0,0,0.08) 0%, transparent 70%)',
        opacity: 1 - transitionWipe / 100,
      }} />

      {/* Shape layers */}
      {[...Array(7)].map((_, i) => {
        const r = seededRandom(i + 20);
        const r2 = seededRandom(i + 120);
        const breathe = 1 + Math.sin(frame * 0.02 + i) * 0.1;
        return (
          <div key={`shape-${i}`} style={{
            position: 'absolute',
            left: `${r * 100}%`,
            top: `${r2 * 100}%`,
            width: 250,
            height: 250,
            borderRadius: i % 3 === 0 ? '50%' : i % 3 === 1 ? '0%' : '20%',
            backgroundColor: '#FEEC41',
            opacity: 0.05,
            transform: `scale(${breathe}) rotate(${frame * 0.08 + i * 50}deg)`,
          }} />
        );
      })}

      {/* Particles */}
      {[...Array(30)].map((_, i) => {
        const r = seededRandom(i + 80);
        const r2 = seededRandom(i + 180);
        const py = (r2 * 1080 - frame * (0.4 + r * 0.6)) % 1080;
        const fixedY = py < 0 ? py + 1080 : py;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute',
            left: r * 1920,
            top: fixedY,
            width: 6,
            height: 6,
            borderRadius: '50%',
            backgroundColor: frame < 100 ? '#ff0000' : '#FEEC41',
            opacity: 0.3,
          }} />
        );
      })}

      {/* ===== PHASE 1: Overpaying ===== */}
      <AbsoluteFill style={{
        opacity: Math.max(0, 1 - transitionWipe / 50),
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
        <div style={{
          opacity: warn1,
          transform: `translateY(${(1 - warn1) * -30}px)`,
          backgroundColor: '#ff0000',
          color: '#F5F5F5',
          padding: '14px 40px',
          fontFamily: 'Inter',
          fontSize: 36,
          fontWeight: 900,
          letterSpacing: 4,
          marginBottom: 30,
        }}>
          WARNING
        </div>
        <div style={{
          opacity: warn1,
          fontFamily: 'Inter, Poppins',
          fontSize: 110,
          fontWeight: 900,
          color: '#000000',
          textAlign: 'center',
          lineHeight: 1,
          letterSpacing: -3,
        }}>
          YOU'RE <span style={{ color: '#ff0000' }}>OVERPAYING</span>
        </div>
        <div style={{
          opacity: warn2,
          transform: `translateY(${(1 - warn2) * 30}px)`,
          fontFamily: 'Inter',
          fontSize: 44,
          fontWeight: 700,
          color: '#000000',
          marginTop: 20,
        }}>
          Right. This. Moment.
        </div>

        {/* Money flying away */}
        {[...Array(8)].map((_, i) => {
          const delay = i * 8;
          const localFrame = Math.max(0, frame - 60 - delay);
          const fly = Math.min(1, localFrame / 40);
          const startX = 960 + (i - 4) * 40;
          const endX = startX + (i % 2 === 0 ? -800 : 800);
          const x = interpolate(fly, [0, 1], [startX, endX]);
          const y = 600 + Math.sin(fly * Math.PI) * -200 + fly * 100;
          return (
            <div key={`bill-${i}`} style={{
              position: 'absolute',
              left: x - 40,
              top: y,
              width: 80,
              height: 40,
              backgroundColor: '#FEEC41',
              border: '2px solid #000000',
              opacity: (1 - fly) * (frame > 60 ? 1 : 0),
              transform: `rotate(${fly * 360}deg)`,
              fontFamily: 'Inter',
              fontSize: 24,
              fontWeight: 900,
              color: '#000000',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
            }}>
              $
            </div>
          );
        })}
      </AbsoluteFill>

      {/* Transition Wipe */}
      <div style={{
        position: 'absolute',
        top: 0,
        left: 0,
        width: '100%',
        height: '100%',
        backgroundColor: '#000000',
        clipPath: `polygon(0 0, ${transitionWipe}% 0, ${transitionWipe - 10}% 100%, 0 100%)`,
      }} />

      {/* ===== PHASE 2: Shield 2 Reveal ===== */}
      <AbsoluteFill style={{
        opacity: Math.max(0, (transitionWipe - 50) / 50),
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
        {/* Shield Number Badge */}
        <div style={{
          transform: `scale(${shieldDrop * freezePulse})`,
          opacity: shieldDrop,
          marginBottom: 30,
        }}>
          <div style={{
            display: 'flex',
            alignItems: 'center',
            gap: 24,
            backgroundColor: '#FEEC41',
            padding: '20px 50px',
            border: '6px solid #000000',
            boxShadow: '8px 8px 0 #000000',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 32,
              fontWeight: 800,
              color: '#000000',
              letterSpacing: 6,
            }}>
              SHIELD
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 100,
              fontWeight: 900,
              color: '#000000',
              lineHeight: 1,
            }}>
              02
            </div>
          </div>
        </div>

        {/* Shield Title */}
        <div style={{
          opacity: shieldText,
          transform: `translateY(${(1 - shieldText) * 30}px)`,
          fontFamily: 'Inter, Poppins',
          fontSize: 120,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -4,
          lineHeight: 1,
          textAlign: 'center',
        }}>
          PROPERTY TAX
        </div>

        {/* FREEZE Big Word */}
        <div style={{
          opacity: freezeWord,
          transform: `scale(${freezeWord}) translateY(${(1 - freezeWord) * 30}px)`,
          marginTop: 10,
          fontFamily: 'Inter, Poppins',
          fontSize: 180,
          fontWeight: 900,
          color: '#FEEC41',
          letterSpacing: -6,
          lineHeight: 1,
          textAlign: 'center',
          textShadow: '8px 8px 0 #000000',
        }}>
          FREEZE
        </div>

        {/* Snowflake/ice marks around FREEZE */}
        {freezeWord > 0.5 && [...Array(6)].map((_, i) => {
          const angle = (i / 6) * Math.PI * 2;
          const radius = 380 + Math.sin(frame * 0.1 + i) * 20;
          return (
            <svg key={`flake-${i}`} width="40" height="40" style={{
              position: 'absolute',
              left: 960 + Math.cos(angle) * radius - 20,
              top: 680 + Math.sin(angle) * radius * 0.4 - 20,
              opacity: freezeWord * 0.6,
              transform: `rotate(${frame * 0.5 + i * 60}deg)`,
            }}>
              <path d="M 20 0 L 20 40 M 0 20 L 40 20 M 6 6 L 34 34 M 34 6 L 6 34" stroke="#000000" strokeWidth="3" strokeLinecap="round" />
            </svg>
          );
        })}
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene33_Shield2Reveal;
```

```tsx
import React from 'react';
Audio: "This is the one that saves seniors from being priced out of their own neighborhoods. Let's say you've lived in your house for 30 years."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene34_PricedOut',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene34_PricedOut: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headline = sp(10);
  const houseAppear = sp(35, { damping: 10 });
  const yearsCounter = interpolate(frame, [50, 110], [0, 30], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const treasureBox = sp(130);
  const bottomTag = sp(180);

  // House sway
  const houseSway = Math.sin(frame * 0.04) * 1.5;
  const camTilt = Math.sin(frame * 0.02) * 0.5;

  return (
    <AbsoluteFill style={{ backgroundColor: '#E3FDFD', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(254,236,65,0.2) 0%, transparent 60%)',
      }} />

      {/* Shape rectangles */}
      {[...Array(6)].map((_, i) => {
        const r = seededRandom(i + 30);
        const r2 = seededRandom(i + 130);
        return (
          <div key={`shape-${i}`} style={{
            position: 'absolute',
            left: `${r * 100}%`,
            top: `${r2 * 100}%`,
            width: 300,
            height: 80,
            backgroundColor: '#000000',
            opacity: 0.04,
            transform: `rotate(${-20 + r * 40}deg) translateY(${Math.sin(frame * 0.01 + i) * 20}px)`,
          }} />
        );
      })}

      {/* Dotted texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.08 }}>
        {[...Array(30)].map((_, i) => (
          [...Array(20)].map((_, j) => (
            <circle key={`d-${i}-${j}`} cx={i * 70} cy={j * 60} r="2" fill="#000000" />
          ))
        ))}
      </svg>

      {/* Particles */}
      {[...Array(20)].map((_, i) => {
        const r = seededRandom(i + 90);
        const r2 = seededRandom(i + 190);
        const py = (r2 * 1080 + frame * (0.3 + r)) % 1080;
        return (
          <div key={`p-${i}`} style={{
            position: 'absolute',
            left: r * 1920,
            top: py,
            width: 5,
            height: 5,
            backgroundColor: '#FEEC41',
            opacity: 0.5,
          }} />
        );
      })}

      {/* Headline */}
      <div style={{
        position: 'absolute',
        top: 80,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: headline,
        transform: `translateY(${(1 - headline) * -30}px) rotate(${camTilt}deg)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 96,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -3,
          lineHeight: 1,
        }}>
          PRICED OUT of your <span style={{ backgroundColor: '#ff0000', color: '#F5F5F5', padding: '4px 20px' }}>OWN STREET</span>
        </div>
      </div>

      {/* Center House Illustration */}
      <div style={{
        position: 'absolute',
        top: 320,
        left: '50%',
        transform: `translateX(-50%) scale(${houseAppear}) translateY(${houseSway}px)`,
        opacity: houseAppear,
      }}>
        <svg width="500" height="380" viewBox="0 0 500 380">
          {/* Roof */}
          <polygon points="50,180 250,40 450,180" fill="#000000" />
          {/* Chimney */}
          <rect x="330" y="80" width="40" height="80" fill="#000000" />
          {/* House body */}
          <rect x="80" y="180" width="340" height="180" fill="#FEEC41" stroke="#000000" strokeWidth="6" />
          {/* Door */}
          <rect x="220" y="240" width="60" height="120" fill="#000000" />
          <circle cx="270" cy="305" r="3" fill="#FEEC41" />
          {/* Windows */}
          <rect x="110" y="220" width="70" height="70" fill="#E3FDFD" stroke="#000000" strokeWidth="4" />
          <line x1="145" y1="220" x2="145" y2="290" stroke="#000000" strokeWidth="3" />
          <line x1="110" y1="255" x2="180" y2="255" stroke="#000000" strokeWidth="3" />

          <rect x="320" y="220" width="70" height="70" fill="#E3FDFD" stroke="#000000" strokeWidth="4" />
          <line x1="355" y1="220" x2="355" y2="290" stroke="#000000" strokeWidth="3" />
          <line x1="320" y1="255" x2="390" y2="255" stroke="#000000" strokeWidth="3" />

          {/* Heart on house */}
          <path d="M 250 130 C 240 115, 220 115, 220 135 C 220 150, 250 170, 250 170 C 250 170, 280 150, 280 135 C 280 115, 260 115, 250 130 Z" fill="#ff0000" />
        </svg>
      </div>

      {/* Years Counter */}
      <div style={{
        position: 'absolute',
        top: 380,
        left: 200,
        opacity: Math.min(1, (frame - 40) / 20),
        transform: `scale(${1 + Math.sin(frame * 0.1) * 0.05})`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 26,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 3,
          marginBottom: 8,
        }}>
          YEARS LIVED HERE
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 240,
          fontWeight: 900,
          color: '#FEEC41',
          lineHeight: 1,
          letterSpacing: -10,
          textShadow: '8px 8px 0 #000000',
        }}>
          {Math.floor(yearsCounter)}
        </div>
      </div>

      {/* Memory bubbles on right */}
      <div style={{
        position: 'absolute',
        top: 380,
        right: 180,
        opacity: treasureBox,
        transform: `translateY(${(1 - treasureBox) * 40}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 22,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 3,
          marginBottom: 16,
        }}>
          BUILT HERE
        </div>
        {['MEMORIES', 'FAMILY', 'COMMUNITY', 'LIFE'].map((word, i) => (
          <div key={word} style={{
            backgroundColor: i % 2 === 0 ? '#000000' : '#FEEC41',
            color: i % 2 === 0 ? '#FEEC41' : '#000000',
            border: '4px solid #000000',
            padding: '14px 28px',
            marginBottom: 12,
            fontFamily: 'Inter',
            fontSize: 38,
            fontWeight: 900,
            letterSpacing: 1,
            transform: `translateX(${(1 - sp(140 + i * 15)) * 60}px)`,
            opacity: sp(140 + i * 15),
          }}>
            {word}
          </div>
        ))}
      </div>

      {/* Bottom tag */}
      <div style={{
        position: 'absolute',
        bottom: 90,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: bottomTag,
        transform: `translateY(${(1 - bottomTag) * 30}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#000000',
          color: '#FEEC41',
          padding: '18px 50px',
          fontFamily: 'Inter',
          fontSize: 36,
          fontWeight: 800,
        }}>
          The Freeze keeps you HERE
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene34_PricedOut;
```

```tsx
import React from 'react';
Audio: "Suddenly, young buyers are moving in, flipping houses, and driving up the property values on your street."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene35_FlippingStreet',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene35_FlippingStreet: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const title = sp(8);
  const street = sp(28);

  // Arrow upward
  const arrowGrow = interpolate(frame, [60, 220], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Price counter
  const priceVal = interpolate(frame, [60, 220], [250000, 580000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const flipLabel = sp(180);

  return (
    <AbsoluteFill style={{ backgroundColor: '#E3FDFD', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, transparent 50%, rgba(0,0,0,0.08) 100%)',
      }} />

      {/* Shape layer */}
      {[...Array(5)].map((_, i) => {
        const r = seededRandom(i + 40);
        const r2 = seededRandom(i + 140);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r * 100}%`,
            top: `${r2 * 100}%`,
            width: 350,
            height: 350,
            border: '3px solid #FEEC41',
            opacity: 0.08,
            transform: `rotate(${frame * 0.1 + i * 70}deg)`,
          }} />
        );
      })}

      {/* Particles - upward arrows */}
      {[...Array(15)].map((_, i) => {
        const r = seededRandom(i + 100);
        const py = (1080 - frame * (1 + r * 2)) % 1080;
        const fixedY = py < 0 ? py + 1080 : py;
        return (
          <div key={`up-${i}`} style={{
            position: 'absolute',
            left: r * 1920,
            top: fixedY,
            color: '#FEEC41',
            opacity: 0.3,
            fontSize: 24,
            fontWeight: 900,
            fontFamily: 'Inter',
          }}>
            ▲
          </div>
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute',
        top: 70,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: title,
        transform: `translateY(${(1 - title) * -30}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#FEEC41',
          color: '#000000',
          padding: '14px 36px',
          fontFamily: 'Inter',
          fontSize: 32,
          fontWeight: 800,
          letterSpacing: 5,
          marginBottom: 16,
          border: '4px solid #000000',
        }}>
          NEIGHBORHOOD INVASION
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 96,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -3,
          lineHeight: 1,
        }}>
          FLIPPERS DRIVE PRICES <span style={{ color: '#ff0000' }}>UP</span>
        </div>
      </div>

      {/* Street of houses */}
      <div style={{
        position: 'absolute',
        bottom: 280,
        left: 0,
        right: 0,
        display: 'flex',
        justifyContent: 'center',
        gap: 30,
        opacity: street,
        transform: `translateY(${(1 - street) * 50}px)`,
      }}>
        {[0, 1, 2, 3, 4].map((i) => {
          const localDelay = 60 + i * 25;
          const flipped = sp(localDelay);
          const isYou = i === 2;
          return (
            <div key={i} style={{
              position: 'relative',
              transform: `translateY(${Math.sin(frame * 0.04 + i) * 3}px)`,
            }}>
              <svg width="200" height="220" viewBox="0 0 200 220">
                <polygon points="20,90 100,20 180,90" fill={isYou ? '#000000' : interpolate(flipped, [0, 1], [0, 1]) === 1 ? '#FEEC41' : '#000000'} />
                <rect x="35" y="90" width="130" height="110" fill={isYou ? '#FEEC41' : flipped > 0.5 ? '#000000' : '#9E9E9E'} stroke="#000000" strokeWidth="4" />
                <rect x="85" y="130" width="30" height="70" fill={isYou ? '#000000' : '#FEEC41'} />
                <rect x="50" y="110" width="22" height="22" fill="#E3FDFD" stroke="#000000" strokeWidth="2" />
                <rect x="128" y="110" width="22" height="22" fill="#E3FDFD" stroke="#000000" strokeWidth="2" />
              </svg>
              {!isYou && flipped > 0.5 && (
                <div style={{
                  position: 'absolute',
                  top: -10,
                  left: '50%',
                  transform: `translateX(-50%) rotate(${-10 + Math.sin(frame * 0.2) * 5}deg)`,
                  backgroundColor: '#ff0000',
                  color: '#F5F5F5',
                  padding: '4px 12px',
                  fontFamily: 'Inter',
                  fontSize: 18,
                  fontWeight: 900,
                  letterSpacing: 1,
                  opacity: (flipped - 0.5) * 2,
                }}>
                  SOLD
                </div>
              )}
              {isYou && (
                <div style={{
                  position: 'absolute',
                  bottom: -40,
                  left: '50%',
                  transform: 'translateX(-50%)',
                  backgroundColor: '#000000',
                  color: '#FEEC41',
                  padding: '6px 16px',
                  fontFamily: 'Inter',
                  fontSize: 22,
                  fontWeight: 900,
                  letterSpacing: 2,
                }}>
                  YOU
                </div>
              )}
            </div>
          );
        })}
      </div>

      {/* Price arrow */}
      <div style={{
        position: 'absolute',
        bottom: 100,
        left: 200,
        opacity: arrowGrow > 0 ? 1 : 0,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 22,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 3,
          marginBottom: 6,
        }}>
          STREET AVG VALUE
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 110,
          fontWeight: 900,
          color: '#ff0000',
          lineHeight: 1,
          letterSpacing: -3,
        }}>
          ${Math.floor(priceVal).toLocaleString()}
        </div>
      </div>

      {/* Big up arrow */}
      <svg width="200" height="500" style={{
        position: 'absolute',
        bottom: 100,
        right: 200,
        opacity: arrowGrow,
      }}>
        <defs>
          <linearGradient id="arrowGrad" x1="0%" y1="100%" x2="0%" y2="0%">
            <stop offset="0%" stopColor="#FEEC41" />
            <stop offset="100%" stopColor="#ff0000" />
          </linearGradient>
        </defs>
        <rect x="75" y={500 - arrowGrow * 400} width="50" height={arrowGrow * 350} fill="url(#arrowGrad)" stroke="#000000" strokeWidth="4" />
        <polygon
          points={`100,${500 - arrowGrow * 400 - 50} 30,${500 - arrowGrow * 400} 170,${500 - arrowGrow * 400}`}
          fill="#ff0000"
          stroke="#000000"
          strokeWidth="4"
        />
      </svg>

      {/* Flip label */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        left: '50%',
        transform: `translateX(-50%) translateY(${(1 - flipLabel) * 30}px) rotate(${-2 + Math.sin(frame * 0.1) * 1}deg)`,
        opacity: flipLabel,
        backgroundColor: '#FEEC41',
        color: '#000000',
        padding: '12px 30px',
        fontFamily: 'Inter',
        fontSize: 28,
        fontWeight: 900,
        letterSpacing: 2,
        border: '4px solid #000000',
      }}>
        FLIP → FLIP → FLIP → YOUR TAX BILL
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene35_FlippingStreet;
```

```tsx
import React from 'react';
Audio: "Your county assessor reassesses your home and your taxes jump by 2,000 dollars. You're on a fixed income. You can't afford that."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene36_TaxJump',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene36_TaxJump: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headline = sp(10);
  const barOld = sp(40);
  const barNewGrow = interpolate(frame, [90, 160], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const jumpLabel = sp(165, { damping: 6, stiffness: 200 });
  const fixedIncome = sp(220);
  const cantAfford = sp(260);

  // Shake on jump
  const shake = frame > 155 && frame < 175 ? Math.sin(frame * 3) * (175 - frame) * 0.5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#E3FDFD', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 100%, rgba(255,0,0,0.12) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      {[...Array(5)].map((_, i) => {
        const r = seededRandom(i + 50);
        const r2 = seededRandom(i + 150);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r * 100}%`,
            top: `${r2 * 100}%`,
            width: 280,
            height: 280,
            borderRadius: '50%',
            backgroundColor: '#000000',
            opacity: 0.03,
            transform: `scale(${1 + Math.sin(frame * 0.03 + i) * 0.1})`,
          }} />
        );
      })}

      {/* Grid background */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {[...Array(15)].map((_, i) => (
          <line key={i} x1="0" y1={i * 72} x2="1920" y2={i * 72} stroke="#000000" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {[...Array(20)].map((_, i) => {
        const r = seededRandom(i + 110);
        const r2 = seededRandom(i + 210);
        const py = (r2 * 1080 + frame * 0.6) % 1080;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920,
            top: py,
            width: 5,
            height: 5,
            backgroundColor: '#FEEC41',
            opacity: 0.4,
          }} />
        );
      })}

      {/* Headline */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: headline,
        transform: `translate(${shake}px, ${(1 - headline) * -30}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 28,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 6,
          marginBottom: 12,
          opacity: 0.7,
        }}>
          REASSESSMENT IMPACT
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 102,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -3,
          lineHeight: 1,
        }}>
          TAX BILL <span style={{ color: '#ff0000' }}>JUMPS</span>
        </div>
      </div>

      {/* Bar Chart */}
      <div style={{
        position: 'absolute',
        bottom: 220,
        left: '50%',
        transform: `translateX(-50%) translateX(${shake}px)`,
        display: 'flex',
        alignItems: 'flex-end',
        gap: 80,
        height: 480,
      }}>
        {/* OLD BAR */}
        <div style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          opacity: barOld,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 28,
            fontWeight: 700,
            color: '#000000',
            marginBottom: 12,
            letterSpacing: 2,
          }}>
            BEFORE
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 64,
            fontWeight: 900,
            color: '#000000',
            marginBottom: 12,
          }}>
            $4,500
          </div>
          <div style={{
            width: 220,
            height: barOld * 240,
            backgroundColor: '#000000',
            border: '6px solid #000000',
          }} />
          <div style={{
            width: 260,
            height: 8,
            backgroundColor: '#000000',
            marginTop: 4,
          }} />
        </div>

        {/* Arrow */}
        <div style={{
          fontSize: 100,
          color: '#ff0000',
          fontWeight: 900,
          alignSelf: 'center',
          opacity: barNewGrow,
          transform: `scale(${1 + barNewGrow * 0.3})`,
        }}>
          →
        </div>

        {/* NEW BAR */}
        <div style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          opacity: barNewGrow > 0 ? 1 : 0,
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 28,
            fontWeight: 700,
            color: '#ff0000',
            marginBottom: 12,
            letterSpacing: 2,
          }}>
            AFTER
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 76,
            fontWeight: 900,
            color: '#ff0000',
            marginBottom: 12,
          }}>
            $6,500
          </div>
          <div style={{
            width: 220,
            height: barNewGrow * 380,
            background: 'linear-gradient(180deg, #ff0000 0%, #FEEC41 100%)',
            border: '6px solid #000000',
          }} />
          <div style={{
            width: 260,
            height: 8,
            backgroundColor: '#000000',
            marginTop: 4,
          }} />
        </div>

        {/* +$2000 floating label */}
        <div style={{
          position: 'absolute',
          right: -120,
          top: 30,
          opacity: jumpLabel,
          transform: `scale(${jumpLabel}) rotate(${-8 + Math.sin(frame * 0.15) * 3}deg)`,
        }}>
          <div style={{
            backgroundColor: '#ff0000',
            color: '#F5F5F5',
            padding: '20px 30px',
            fontFamily: 'Inter, Poppins',
            fontSize: 64,
            fontWeight: 900,
            border: '6px solid #000000',
            boxShadow: '8px 8px 0 #000000',
            letterSpacing: -2,
          }}>
            +$2,000
          </div>
        </div>
      </div>

      {/* Bottom strip */}
      <div style={{
        position: 'absolute',
        bottom: 80,
        left: 0,
        right: 0,
        textAlign: 'center',
      }}>
        <div style={{
          display: 'inline-flex',
          alignItems: 'center',
          gap: 24,
          opacity: fixedIncome,
          transform: `translateY(${(1 - fixedIncome) * 20}px)`,
        }}>
          <div style={{
            backgroundColor: '#000000',
            color: '#FEEC41',
            padding: '14px 28px',
            fontFamily: 'Inter',
            fontSize: 32,
            fontWeight: 800,
            letterSpacing: 2,
          }}>
            FIXED INCOME
          </div>
          <div style={{
            fontSize: 48,
            color: '#000000',
            fontWeight: 900,
          }}>
            +
          </div>
          <div style={{
            backgroundColor: '#ff0000',
            color: '#F5F5F5',
            padding: '14px 28px',
            fontFamily: 'Inter',
            fontSize: 32,
            fontWeight: 800,
            letterSpacing: 2,
            opacity: cantAfford,
          }}>
            CAN'T AFFORD IT
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene36_TaxJump;
```

```tsx
import React from 'react';
Audio: "In dozens of counties and states, once you reach age 65, you can apply to freeze your home's assessed value at its current level."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene37_Age65Freeze',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene37_Age65Freeze: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleIn = sp(10);
  const ageRing = sp(35, { damping: 10 });
  const ageNumber = sp(60, { damping: 6, stiffness: 180 });
  const arrowDown = sp(110);
  const freezeBox = sp(140, { damping: 8 });
  const lockIcon = sp(180);

  // 65 pulse
  const agePulse = 1 + Math.sin(frame * 0.1) * 0.04;

  return (
    <AbsoluteFill style={{ backgroundColor: '#E3FDFD', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, rgba(254,236,65,0.18) 0%, transparent 60%)',
      }} />

      {/* Shape layers - concentric circles */}
      {[...Array(4)].map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          width: 600 + i * 200,
          height: 600 + i * 200,
          borderRadius: '50%',
          border: `2px solid #FEEC41`,
          opacity: 0.08,
          transform: `translate(-50%, -50%) scale(${1 + Math.sin(frame * 0.02 + i) * 0.03})`,
        }} />
      ))}

      {/* Particles */}
      {[...Array(25)].map((_, i) => {
        const r = seededRandom(i + 120);
        const r2 = seededRandom(i + 220);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920,
            top: (r2 * 1080 + frame * (0.3 + r * 0.7)) % 1080,
            width: 5,
            height: 5,
            borderRadius: '50%',
            backgroundColor: '#000000',
            opacity: 0.2,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute',
        top: 70,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: titleIn,
        transform: `translateY(${(1 - titleIn) * -30}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 26,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 6,
          marginBottom: 12,
          opacity: 0.7,
        }}>
          AVAILABLE IN DOZENS OF STATES
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 90,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -3,
          lineHeight: 1,
        }}>
          THE MAGIC NUMBER IS
        </div>
      </div>

      {/* Big 65 ring */}
      <div style={{
        position: 'absolute',
        top: 280,
        left: '50%',
        transform: `translateX(-50%) scale(${ageRing * agePulse})`,
        opacity: ageRing,
      }}>
        <div style={{
          width: 360,
          height: 360,
          borderRadius: '50%',
          backgroundColor: '#FEEC41',
          border: '12px solid #000000',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'center',
          position: 'relative',
          boxShadow: '12px 12px 0 #000000',
        }}>
          <div style={{
            transform: `scale(${ageNumber})`,
            fontFamily: 'Inter, Poppins',
            fontSize: 280,
            fontWeight: 900,
            color: '#000000',
            lineHeight: 1,
            letterSpacing: -10,
          }}>
            65
          </div>
          <div style={{
            position: 'absolute',
            bottom: -30,
            left: '50%',
            transform: 'translateX(-50%)',
            backgroundColor: '#000000',
            color: '#FEEC41',
            padding: '8px 24px',
            fontFamily: 'Inter',
            fontSize: 26,
            fontWeight: 900,
            letterSpacing: 3,
          }}>
            AGE TRIGGER
          </div>
        </div>
      </div>

      {/* Down arrow */}
      <div style={{
        position: 'absolute',
        top: 680,
        left: '50%',
        transform: `translateX(-50%) scale(${arrowDown})`,
        opacity: arrowDown,
      }}>
        <svg width="60" height="80" viewBox="0 0 60 80">
          <path d="M 30 0 L 30 60 M 10 40 L 30 60 L 50 40" stroke="#000000" strokeWidth="8" fill="none" strokeLinecap="round" strokeLinejoin="round" />
        </svg>
      </div>

      {/* Freeze action box */}
      <div style={{
        position: 'absolute',
        bottom: 90,
        left: '50%',
        transform: `translateX(-50%) scale(${freezeBox})`,
        opacity: freezeBox,
        backgroundColor: '#000000',
        padding: '24px 60px',
        border: '6px solid #FEEC41',
        boxShadow: '10px 10px 0 #FEEC41',
        display: 'flex',
        alignItems: 'center',
        gap: 30,
      }}>
        {/* Lock SVG */}
        <svg width="80" height="80" viewBox="0 0 80 80" style={{ opacity: lockIcon, transform: `scale(${lockIcon})` }}>
          <rect x="15" y="35" width="50" height="40" rx="4" fill="#FEEC41" stroke="#FEEC41" strokeWidth="2" />
          <path d="M 22 35 L 22 22 C 22 12, 58 12, 58 22 L 58 35" stroke="#FEEC41" strokeWidth="6" fill="none" />
          <circle cx="40" cy="52" r="6" fill="#000000" />
          <rect x="38" y="52" width="4" height="12" fill="#000000" />
        </svg>
        <div>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 22,
            fontWeight: 700,
            color: '#FEEC41',
            letterSpacing: 4,
            marginBottom: 4,
            opacity: 0.7,
          }}>
            APPLY TO
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 64,
            fontWeight: 900,
            color: '#FEEC41',
            letterSpacing: -2,
            lineHeight: 1,
          }}>
            FREEZE YOUR VALUE
          </div>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene37_Age65Freeze;
```

```tsx
import React from 'react';
Audio: "If your home is assessed at $300,000 today and you file the freeze, it stays at $300,000 — even if the house next door sells for $500,000."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene38_FrozenVsNeighbor',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene38_FrozenVsNeighbor: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const title = sp(8);
  const leftHouse = sp(30);
  const rightHouse = sp(60);
  const leftPriceLock = sp(110);
  const rightPriceRise = interpolate(frame, [130, 230], [300000, 500000], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const vsBadge = sp(180, { damping: 6, stiffness: 200 });

  return (
    <AbsoluteFill style={{ backgroundColor: '#E3FDFD', overflow: 'hidden', opacity: exitOp }}>
      {/* Split background */}
      <AbsoluteFill style={{
        background: 'linear-gradient(90deg, rgba(254,236,65,0.1) 0%, rgba(254,236,65,0.1) 50%, rgba(255,0,0,0.08) 50%, rgba(255,0,0,0.08) 100%)',
      }} />

      {/* Shape layer */}
      {[...Array(6)].map((_, i) => {
        const r = seededRandom(i + 60);
        const r2 = seededRandom(i + 160);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r * 100}%`,
            top: `${r2 * 100}%`,
            width: 250,
            height: 250,
            border: '2px solid #000000',
            opacity: 0.04,
            transform: `rotate(${frame * 0.1 + i * 60}deg)`,
          }} />
        );
      })}

      {/* Particles */}
      {[...Array(20)].map((_, i) => {
        const r = seededRandom(i + 130);
        const r2 = seededRandom(i + 230);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920,
            top: (r2 * 1080 + frame * 0.5) % 1080,
            width: 4,
            height: 4,
            backgroundColor: '#000000',
            opacity: 0.25,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: title,
        transform: `translateY(${(1 - title) * -30}px)`,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 80,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -2,
          lineHeight: 1,
        }}>
          YOU vs THE NEIGHBOR
        </div>
      </div>

      {/* LEFT - YOUR HOUSE (FROZEN) */}
      <div style={{
        position: 'absolute',
        left: 180,
        top: 280,
        opacity: leftHouse,
        transform: `translateY(${(1 - leftHouse) * 50}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 26,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 4,
          marginBottom: 16,
        }}>
          YOUR HOME
        </div>

        {/* House SVG with ice overlay */}
        <div style={{ position: 'relative', marginBottom: 24 }}>
          <svg width="380" height="280" viewBox="0 0 380 280">
            <polygon points="20,120 190,30 360,120" fill="#000000" />
            <rect x="50,120 320 160" fill="#FEEC41" />
            <rect x="50" y="120" width="280" height="160" fill="#FEEC41" stroke="#000000" strokeWidth="6" />
            <rect x="160" y="180" width="60" height="100" fill="#000000" />
            <rect x="80" y="150" width="50" height="50" fill="#E3FDFD" stroke="#000000" strokeWidth="4" />
            <rect x="250" y="150" width="50" height="50" fill="#E3FDFD" stroke="#000000" strokeWidth="4" />
          </svg>
          {/* Ice/frozen overlay */}
          {leftPriceLock > 0.3 && (
            <div style={{
              position: 'absolute',
              top: 0,
              left: 0,
              width: '100%',
              height: '100%',
              background: 'linear-gradient(180deg, rgba(227,253,253,0.7) 0%, rgba(227,253,253,0.3) 100%)',
              opacity: leftPriceLock,
            }} />
          )}
          {/* Frozen label */}
          <div style={{
            position: 'absolute',
            top: 10,
            right: 10,
            backgroundColor: '#000000',
            color: '#FEEC41',
            padding: '6px 14px',
            fontFamily: 'Inter',
            fontSize: 22,
            fontWeight: 900,
            letterSpacing: 2,
            opacity: leftPriceLock,
            transform: `rotate(${-8 + Math.sin(frame * 0.1) * 2}deg)`,
          }}>
            FROZEN
          </div>
        </div>

        {/* Price */}
        <div style={{
          backgroundColor: '#FEEC41',
          border: '6px solid #000000',
          padding: '20px 30px',
          textAlign: 'center',
          boxShadow: '8px 8px 0 #000000',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 20,
            fontWeight: 700,
            color: '#000000',
            letterSpacing: 3,
            marginBottom: 4,
          }}>
            TAXED VALUE
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 64,
            fontWeight: 900,
            color: '#000000',
            letterSpacing: -2,
          }}>
            $300,000
          </div>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 20,
            fontWeight: 700,
            color: '#000000',
            letterSpacing: 2,
            marginTop: 4,
          }}>
            LOCKED FOREVER
          </div>
        </div>
      </div>

      {/* RIGHT - NEIGHBOR'S HOUSE */}
      <div style={{
        position: 'absolute',
        right: 180,
        top: 280,
        opacity: rightHouse,
        transform: `translateY(${(1 - rightHouse) * 50}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 26,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 4,
          marginBottom: 16,
        }}>
          NEIGHBOR'S HOME
        </div>

        <div style={{ position: 'relative', marginBottom: 24 }}>
          <svg width="380" height="280" viewBox="0 0 380 280">
            <polygon points="20,120 190,30 360,120" fill="#000000" />
            <rect x="50" y="120" width="280" height="160" fill="#ff0000" stroke="#000000" strokeWidth="6" />
            <rect x="160" y="180" width="60" height="100" fill="#000000" />
            <rect x="80" y="150" width="50" height="50" fill="#FEEC41" stroke="#000000" strokeWidth="4" />
            <rect x="250" y="150" width="50" height="50" fill="#FEEC41" stroke="#000000" strokeWidth="4" />
          </svg>
          {/* Sold sticker */}
          <div style={{
            position: 'absolute',
            top: 10,
            right: 10,
            backgroundColor: '#ff0000',
            color: '#F5F5F5',
            padding: '6px 14px',
            fontFamily: 'Inter',
            fontSize: 22,
            fontWeight: 900,
            letterSpacing: 2,
            transform: `rotate(${8 + Math.sin(frame * 0.1) * 2}deg)`,
          }}>
            SOLD
          </div>
        </div>

        {/* Price climbing */}
        <div style={{
          backgroundColor: '#ff0000',
          border: '6px solid #000000',
          padding: '20px 30px',
          textAlign: 'center',
          boxShadow: '8px 8px 0 #000000',
        }}>
          <div style={{
            fontFamily: 'JetBrains Mono',
            fontSize: 20,
            fontWeight: 700,
            color: '#F5F5F5',
            letterSpacing: 3,
            marginBottom: 4,
          }}>
            SOLD FOR
          </div>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 64,
            fontWeight: 900,
            color: '#F5F5F5',
            letterSpacing: -2,
          }}>
            ${Math.floor(rightPriceRise).toLocaleString()}
          </div>
          <div style={{
            fontFamily: 'Inter',
            fontSize: 20,
            fontWeight: 700,
            color: '#F5F5F5',
            letterSpacing: 2,
            marginTop: 4,
          }}>
            DOESN'T MATTER TO YOU
          </div>
        </div>
      </div>

      {/* VS in middle */}
      <div style={{
        position: 'absolute',
        top: 450,
        left: '50%',
        transform: `translateX(-50%) scale(${vsBadge}) rotate(${vsBadge * -10}deg)`,
        opacity: vsBadge,
        width: 140,
        height: 140,
        borderRadius: '50%',
        backgroundColor: '#000000',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontFamily: 'Inter, Poppins',
        fontSize: 56,
        fontWeight: 900,
        color: '#FEEC41',
        border: '6px solid #FEEC41',
        zIndex: 10,
      }}>
        VS
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 60%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene38_FrozenVsNeighbor;
```

```tsx
import React from 'react';
Audio: "Your property taxes are locked in. They cannot go up based on neighborhood gentrification or a hot real estate market."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene39_LockedIn',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene39_LockedIn: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const lockSpring = sp(20, { damping: 8, stiffness: 160 });
  const lockClick = frame > 50 && frame < 65 ? Math.sin(frame * 1.5) * 8 : 0;
  const taxesWord = sp(70);
  const lockedWord = sp(100);

  // Block 1 — gentrification
  const blockGent = sp(160);
  // Block 2 — hot market
  const blockMarket = sp(200);

  const bigX1 = interpolate(frame, [180, 210], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const bigX2 = interpolate(frame, [220, 250], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  return (
    <AbsoluteFill style={{ backgroundColor: '#E3FDFD', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at 50% 30%, rgba(254,236,65,0.2) 0%, transparent 60%)',
      }} />

      {/* Shape layers */}
      {[...Array(5)].map((_, i) => {
        const r = seededRandom(i + 70);
        const r2 = seededRandom(i + 170);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${r * 100}%`,
            top: `${r2 * 100}%`,
            width: 220,
            height: 220,
            backgroundColor: '#FEEC41',
            opacity: 0.05,
            transform: `rotate(${frame * 0.05 + i * 70}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.1})`,
          }} />
        );
      })}

      {/* Crosshatch texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {[...Array(30)].map((_, i) => (
          <line key={`x1-${i}`} x1={-200 + i * 80} y1="0" x2={i * 80} y2="1080" stroke="#000000" strokeWidth="1" />
        ))}
        {[...Array(30)].map((_, i) => (
          <line key={`x2-${i}`} x1={i * 80} y1="0" x2={-200 + i * 80} y2="1080" stroke="#000000" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {[...Array(20)].map((_, i) => {
        const r = seededRandom(i + 140);
        const r2 = seededRandom(i + 240);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920,
            top: (r2 * 1080 + frame * 0.4) % 1080,
            width: 5,
            height: 5,
            borderRadius: '50%',
            backgroundColor: '#FEEC41',
            opacity: 0.4,
          }} />
        );
      })}

      {/* Big Lock */}
      <div style={{
        position: 'absolute',
        top: 80,
        left: '50%',
        transform: `translateX(-50%) scale(${lockSpring}) translateY(${lockClick}px)`,
        opacity: lockSpring,
      }}>
        <svg width="280" height="320" viewBox="0 0 280 320">
          <rect x="40" y="130" width="200" height="170" rx="8" fill="#000000" />
          <rect x="40" y="130" width="200" height="170" rx="8" fill="#FEEC41" stroke="#000000" strokeWidth="8" />
          <path d="M 70 130 L 70 70 C 70 25, 210 25, 210 70 L 210 130" stroke="#000000" strokeWidth="18" fill="none" strokeLinecap="round" />
          <circle cx="140" cy="200" r="22" fill="#000000" />
          <rect x="135" y="200" width="10" height="50" fill="#000000" />
        </svg>
      </div>

      {/* TAXES LOCKED IN headline */}
      <div style={{
        position: 'absolute',
        top: 430,
        left: 0,
        right: 0,
        textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 90,
          fontWeight: 900,
          color: '#000000',
          opacity: taxesWord,
          transform: `translateY(${(1 - taxesWord) * 30}px)`,
          letterSpacing: -3,
          lineHeight: 1,
          display: 'inline-block',
        }}>
          PROPERTY TAXES
        </div>
        <div style={{
          marginTop: 12,
          fontFamily: 'Inter, Poppins',
          fontSize: 160,
          fontWeight: 900,
          color: '#000000',
          opacity: lockedWord,
          transform: `scale(${lockedWord}) translateY(${(1 - lockedWord) * 30}px)`,
          letterSpacing: -6,
          lineHeight: 1,
          background: '#FEEC41',
          padding: '0 30px',
          display: 'inline-block',
        }}>
          LOCKED IN
        </div>
      </div>

      {/* Bottom row - blocked threats */}
      <div style={{
        position: 'absolute',
        bottom: 100,
        left: 0,
        right: 0,
        display: 'flex',
        justifyContent: 'center',
        gap: 60,
      }}>
        {/* Block 1 - Gentrification */}
        <div style={{
          opacity: blockGent,
          transform: `translateY(${(1 - blockGent) * 40}px)`,
          position: 'relative',
        }}>
          <div style={{
            backgroundColor: '#000000',
            padding: '20px 40px',
            border: '6px solid #FEEC41',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 800,
              color: '#FEEC41',
              letterSpacing: 3,
              textAlign: 'center',
              opacity: 0.6,
              marginBottom: 4,
            }}>
              CANNOT TOUCH
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 44,
              fontWeight: 900,
              color: '#FEEC41',
              letterSpacing: -1,
              textAlign: 'center',
            }}>
              GENTRIFICATION
            </div>
          </div>
          {/* Big X over it */}
          <svg width="100%" height="100%" viewBox="0 0 400 120" style={{
            position: 'absolute',
            top: 0,
            left: 0,
            opacity: bigX1,
          }}>
            <line x1="20" y1="20" x2="380" y2="100" stroke="#ff0000" strokeWidth="10" strokeLinecap="round" strokeDasharray="400" strokeDashoffset={(1 - bigX1) * 400} />
            <line x1="380" y1="20" x2="20" y2="100" stroke="#ff0000" strokeWidth="10" strokeLinecap="round" strokeDasharray="400" strokeDashoffset={(1 - bigX1) * 400} />
          </svg>
        </div>

        {/* Block 2 - Hot Market */}
        <div style={{
          opacity: blockMarket,
          transform: `translateY(${(1 - blockMarket) * 40}px)`,
          position: 'relative',
        }}>
          <div style={{
            backgroundColor: '#000000',
            padding: '20px 40px',
            border: '6px solid #FEEC41',
          }}>
            <div style={{
              fontFamily: 'Inter',
              fontSize: 28,
              fontWeight: 800,
              color: '#FEEC41',
              letterSpacing: 3,
              textAlign: 'center',
              opacity: 0.6,
              marginBottom: 4,
            }}>
              CANNOT TOUCH
            </div>
            <div style={{
              fontFamily: 'Inter, Poppins',
              fontSize: 44,
              fontWeight: 900,
              color: '#FEEC41',
              letterSpacing: -1,
              textAlign: 'center',
            }}>
              HOT MARKET
            </div>
          </div>
          <svg width="100%" height="100%" viewBox="0 0 400 120" style={{
            position: 'absolute',
            top: 0,
            left: 0,
            opacity: bigX2,
          }}>
            <line x1="20" y1="20" x2="380" y2="100" stroke="#ff0000" strokeWidth="10" strokeLinecap="round" strokeDasharray="400" strokeDashoffset={(1 - bigX2) * 400} />
            <line x1="380" y1="20" x2="20" y2="100" stroke="#ff0000" strokeWidth="10" strokeLinecap="round" strokeDasharray="400" strokeDashoffset={(1 - bigX2) * 400} />
          </svg>
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene39_LockedIn;
```

```tsx
import React from 'react';
Audio: "Some states allow the frozen value to increase by a tiny capped amount — like 2 percent — but that's nothing compared to 10–15% spikes we've seen."

import React from 'react';
import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene40_TinyCapVsSpike',
  durationInSeconds: 10,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene40_TinyCapVsSpike: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const title = sp(10);
  const smallBar = interpolate(frame, [40, 100], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const smallNum = interpolate(frame, [40, 100], [0, 2], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const bigBar = interpolate(frame, [120, 210], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const bigNum = interpolate(frame, [120, 210], [0, 15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const verdict = sp(230);

  return (
    <AbsoluteFill style={{ backgroundColor: '#E3FDFD', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(254,236,65,0.12) 0%, transparent 50%, rgba(255,0,0,0.08) 100%)',
      }} />

      {/* Shape rings */}
      {[...Array(4)].map((_, i) => (
        <div key={i} style={{
          position: 'absolute',
          left: '50%',
          top: '50%',
          width: 700 + i * 200,
          height: 700 + i * 200,
          borderRadius: '50%',
          border: '2px solid #000000',
          opacity: 0.04,
          transform: `translate(-50%, -50%) rotate(${frame * 0.05 + i * 30}deg)`,
        }} />
      ))}

      {/* Particles */}
      {[...Array(20)].map((_, i) => {
        const r = seededRandom(i + 150);
        const r2 = seededRandom(i + 250);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1920,
            top: (r2 * 1080 + frame * 0.4) % 1080,
            width: 4,
            height: 4,
            backgroundColor: '#000000',
            opacity: 0.2,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        position: 'absolute',
        top: 60,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: title,
        transform: `translateY(${(1 - title) * -30}px)`,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono',
          fontSize: 28,
          fontWeight: 700,
          color: '#000000',
          letterSpacing: 6,
          marginBottom: 12,
          opacity: 0.7,
        }}>
          THE TINY CATCH
        </div>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 96,
          fontWeight: 900,
          color: '#000000',
          letterSpacing: -3,
          lineHeight: 1,
        }}>
          CAPPED vs CHAOS
        </div>
      </div>

      {/* Bar chart container */}
      <div style={{
        position: 'absolute',
        top: 290,
        left: '50%',
        transform: 'translateX(-50%)',
        display: 'flex',
        alignItems: 'flex-end',
        gap: 200,
        height: 540,
      }}>
        {/* Small bar - 2% */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 86,
            fontWeight: 900,
            color: '#FEEC41',
            opacity: smallBar,
            lineHeight: 1,
            letterSpacing: -3,
            marginBottom: 12,
            textShadow: '4px 4px 0 #000000',
          }}>
            {smallNum.toFixed(1)}%
          </div>
          <div style={{
            width: 200,
            height: smallBar * 80,
            backgroundColor: '#FEEC41',
            border: '6px solid #000000',
            boxShadow: '6px 6px 0 #000000',
          }} />
          <div style={{
            marginTop: 20,
            backgroundColor: '#000000',
            color: '#FEEC41',
            padding: '10px 20px',
            fontFamily: 'Inter',
            fontSize: 28,
            fontWeight: 900,
            letterSpacing: 2,
            opacity: smallBar,
          }}>
            FROZEN CAP
          </div>
          <div style={{
            marginTop: 8,
            fontFamily: 'Inter',
            fontSize: 22,
            fontWeight: 700,
            color: '#000000',
            opacity: smallBar * 0.7,
            letterSpacing: 1,
          }}>
            barely moves
          </div>
        </div>

        {/* Big bar - 15% */}
        <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
          <div style={{
            fontFamily: 'Inter, Poppins',
            fontSize: 86,
            fontWeight: 900,
            color: '#ff0000',
            opacity: bigBar > 0 ? 1 : 0,
            lineHeight: 1,
            letterSpacing: -3,
            marginBottom: 12,
            textShadow: '4px 4px 0 #000000',
          }}>
            {bigNum.toFixed(0)}%
          </div>
          <div style={{
            width: 200,
            height: bigBar * 440,
            background: 'linear-gradient(180deg, #ff0000 0%, #FEEC41 100%)',
            border: '6px solid #000000',
            boxShadow: '6px 6px 0 #000000',
            position: 'relative',
          }}>
            {/* Spike spikes */}
            {bigBar > 0.5 && [...Array(4)].map((_, i) => (
              <div key={i} style={{
                position: 'absolute',
                top: i * 100,
                left: -15,
                width: 0,
                height: 0,
                borderTop: '15px solid transparent',
                borderBottom: '15px solid transparent',
                borderRight: '20px solid #ff0000',
              }} />
            ))}
          </div>
          <div style={{
            marginTop: 20,
            backgroundColor: '#ff0000',
            color: '#F5F5F5',
            padding: '10px 20px',
            fontFamily: 'Inter',
            fontSize: 28,
            fontWeight: 900,
            letterSpacing: 2,
            opacity: bigBar,
          }}>
            MARKET SPIKE
          </div>
          <div style={{
            marginTop: 8,
            fontFamily: 'Inter',
            fontSize: 22,
            fontWeight: 700,
            color: '#000000',
            opacity: bigBar * 0.7,
            letterSpacing: 1,
          }}>
            without freeze
          </div>
        </div>
      </div>

      {/* Verdict */}
      <div style={{
        position: 'absolute',
        bottom: 60,
        left: 0,
        right: 0,
        textAlign: 'center',
        opacity: verdict,
        transform: `translateY(${(1 - verdict) * 30}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          backgroundColor: '#000000',
          color: '#FEEC41',
          padding: '16px 40px',
          fontFamily: 'Inter, Poppins',
          fontSize: 44,
          fontWeight: 900,
          letterSpacing: 1,
        }}>
          2% is NOTHING compared to 15%
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(0,0,0,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute',
        bottom: 30,
        right: 45,
        fontFamily: 'Inter',
        fontSize: 22,
        fontWeight: 600,
        color: '#000000',
        opacity: 0.4,
      }}>
        Senior Fund USA
      </div>
    </AbsoluteFill>
  );
};

export default Scene40_TinyCapVsSpike;
```

