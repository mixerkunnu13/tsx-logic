```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene41_AdvisorSecret',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene41_AdvisorSecret: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.06]);
  const lockOpen = sp(30);
  const lockRotate = interpolate(lockOpen, [0, 1], [0, -35]);
  const secretReveal = sp(60);
  const advisorMaskOp = interpolate(sp(90), [0, 1], [1, 0.15]);
  const subscribeGlow = Math.sin(frame * 0.12) * 0.3 + 0.7;
  const lineGrow = interpolate(sp(120), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(31,21,12,0.08) 0%, transparent 60%), radial-gradient(circle at 70% 80%, rgba(8,6,22,0.06) 0%, transparent 55%)',
        transform: `scale(${camZoom})`,
      }} />

      {/* Drifting transparent shapes */}
      {[0,1,2,3,4].map(i => {
        const s = seededRandom(i + 10);
        const s2 = seededRandom(i + 20);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 90}%`,
            top: `${s2 * 80}%`,
            width: 280 + s * 200,
            height: 280 + s * 200,
            borderRadius: i % 2 === 0 ? '50%' : '20%',
            border: '2px solid rgba(31,21,12,0.06)',
            transform: `rotate(${frame * 0.1 + i * 40}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.05})`,
          }} />
        );
      })}

      {/* Grid texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid41" width="60" height="60" patternUnits="userSpaceOnUse">
            <path d="M 60 0 L 0 0 0 60" fill="none" stroke="#1F150C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid41)" />
      </svg>

      {/* Floating particles */}
      {Array.from({length: 30}).map((_, i) => {
        const px = seededRandom(i + 50) * 1920;
        const py = (seededRandom(i + 60) * 1080 + frame * 0.5) % 1080;
        return <div key={i} style={{
          position: 'absolute', left: px, top: py,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: '#1F150C', opacity: 0.25,
        }} />;
      })}

      {/* Main content */}
      <div style={{
        position: 'absolute', top: 100, left: 120, right: 120,
        display: 'flex', flexDirection: 'column', gap: 30,
      }}>
        <div style={{
          fontFamily: 'Inter, Poppins',
          fontSize: 38, fontWeight: 700,
          color: '#080616', letterSpacing: 4,
          opacity: sp(10),
          transform: `translateX(${interpolate(sp(10),[0,1],[-50,0])}px)`,
        }}>
          ◆ THE HIDDEN LAYER
        </div>

        <div style={{
          fontFamily: 'Yantra One',
          fontSize: 145, fontWeight: 900,
          color: '#1F150C', lineHeight: 1.05,
          opacity: sp(25),
          transform: `translateY(${interpolate(sp(25),[0,1],[40,0])}px)`,
        }}>
          जो advisor भी<br/>
          <span style={{ color: '#ff0000' }}>छुपाता</span> है
        </div>
      </div>

      {/* Vault / Lock visual */}
      <div style={{
        position: 'absolute', right: 180, top: 380,
        width: 380, height: 380,
        opacity: sp(45),
        transform: `scale(${sp(45)})`,
      }}>
        <svg width="380" height="380" viewBox="0 0 380 380">
          <circle cx="190" cy="190" r="170" fill="#1F150C" />
          <circle cx="190" cy="190" r="170" fill="none" stroke="#FFC107" strokeWidth="4" />
          <circle cx="190" cy="190" r="140" fill="none" stroke="#FFC107" strokeWidth="2" strokeDasharray="6 4" 
            style={{ transformOrigin: '190px 190px', transform: `rotate(${frame * 2}deg)` }} />
          {/* Lock shackle */}
          <path d="M 130 170 L 130 130 Q 130 80 190 80 Q 250 80 250 130 L 250 170"
            fill="none" stroke="#FFC107" strokeWidth="14" strokeLinecap="round"
            style={{ transformOrigin: '190px 170px', transform: `rotate(${lockRotate}deg) translateY(${lockRotate * -0.3}px)` }} />
          <rect x="115" y="170" width="150" height="120" rx="12" fill="#FFC107" />
          <circle cx="190" cy="220" r="14" fill="#1F150C" />
          <rect x="186" y="225" width="8" height="30" fill="#1F150C" />
          {/* Secret text inside */}
          <text x="190" y="340" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="700" fill="#1F150C" opacity={secretReveal}>
            FINANCIAL SECRETS
          </text>
        </svg>
      </div>

      {/* Advisor silhouette being unmasked */}
      <div style={{
        position: 'absolute', left: 160, bottom: 200,
        opacity: sp(75),
      }}>
        <svg width="220" height="280" viewBox="0 0 220 280">
          <circle cx="110" cy="70" r="50" fill="#080616" opacity={advisorMaskOp} />
          <rect x="50" y="120" width="120" height="160" rx="20" fill="#080616" opacity={advisorMaskOp} />
          {/* Mask falling */}
          <path d="M 70 60 L 150 60 L 145 90 Q 110 100 75 90 Z" fill="#1F150C"
            transform={`translate(0, ${interpolate(sp(90),[0,1],[0,180])}) rotate(${interpolate(sp(90),[0,1],[0,45])} 110 75)`} />
          <circle cx="90" cy="70" r="6" fill="#DFF1F1" opacity={interpolate(sp(95),[0,1],[0,1])} />
          <circle cx="130" cy="70" r="6" fill="#DFF1F1" opacity={interpolate(sp(95),[0,1],[0,1])} />
        </svg>
        <div style={{
          fontFamily: 'Inter', fontSize: 26, fontWeight: 700,
          color: '#ff0000', marginTop: 12, textAlign: 'center',
          opacity: interpolate(sp(100),[0,1],[0,1]),
        }}>
          UNMASKED
        </div>
      </div>

      {/* Subscribe CTA bar */}
      <div style={{
        position: 'absolute', bottom: 100, left: 120, right: 120,
        height: 90, borderRadius: 16,
        backgroundColor: '#ff0000',
        boxShadow: `0 0 ${40 * subscribeGlow}px rgba(255,0,0,${subscribeGlow})`,
        display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 30,
        opacity: sp(110),
        transform: `scale(${sp(110, { damping: 8 })})`,
      }}>
        <svg width="50" height="50" viewBox="0 0 24 24" fill="#F5F5F5">
          <path d="M21.582 6.186a2.506 2.506 0 0 0-1.768-1.768C18.254 4 12 4 12 4s-6.254 0-7.814.418A2.506 2.506 0 0 0 2.418 6.186C2 7.746 2 12 2 12s0 4.254.418 5.814a2.506 2.506 0 0 0 1.768 1.768C5.746 20 12 20 12 20s6.254 0 7.814-.418a2.506 2.506 0 0 0 1.768-1.768C22 16.254 22 12 22 12s0-4.254-.418-5.814zM10 15V9l5.2 3-5.2 3z"/>
        </svg>
        <div style={{
          fontFamily: 'Inter', fontSize: 44, fontWeight: 900,
          color: '#F5F5F5', letterSpacing: 2,
        }}>
          SUBSCRIBE → ProBuffet HQ
        </div>
      </div>

      {/* Connector line */}
      <svg style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: '100%', pointerEvents: 'none' }}>
        <line x1="780" y1="450" x2={780 + 380 * lineGrow} y2="500"
          stroke="#1F150C" strokeWidth="3" strokeDasharray="8 6" opacity="0.5" />
      </svg>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(31,21,12,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene41_AdvisorSecret;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene42_BalloonInflation',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene42_BalloonInflation: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Balloon growing
  const balloonScale = interpolate(frame, [40, 200], [0.3, 1.4], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const balloonY = Math.sin(frame * 0.04) * 8;
  const inflationPercent = Math.floor(interpolate(frame, [40, 200], [0, 15], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // Cost counter
  const costValue = Math.floor(interpolate(frame, [60, 220], [20, 47], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // 3 action checkbox stagger
  const check1 = sp(280);
  const check2 = sp(330);
  const check3 = sp(380);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(255,193,7,0.06) 0%, transparent 50%, rgba(31,21,12,0.06) 100%)',
      }} />

      {/* Shape layers */}
      {[0,1,2,3].map(i => (
        <div key={i} style={{
          position: 'absolute',
          left: `${seededRandom(i+100) * 85}%`,
          top: `${seededRandom(i+200) * 75}%`,
          width: 300, height: 300,
          borderRadius: '50%',
          border: '2px solid rgba(31,21,12,0.05)',
          transform: `scale(${1 + Math.sin(frame * 0.03 + i) * 0.08})`,
        }} />
      ))}

      {/* Diagonal lines texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="lines42" width="80" height="80" patternUnits="userSpaceOnUse">
            <path d="M 0 80 L 80 0" stroke="#080616" strokeWidth="1.5" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#lines42)" />
      </svg>

      {/* Floating particles */}
      {Array.from({length: 25}).map((_, i) => {
        const px = seededRandom(i + 300) * 1920;
        const py = (seededRandom(i + 400) * 1080 - frame * 0.6 + 1080) % 1080;
        return <div key={i} style={{
          position: 'absolute', left: px, top: py,
          width: 5, height: 5, borderRadius: '50%',
          backgroundColor: '#FFC107', opacity: 0.4,
        }} />;
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 80, left: 100, right: 100,
        opacity: sp(10),
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 32, fontWeight: 700,
          color: '#ff0000', letterSpacing: 5,
        }}>
          ▲ THE BALLOON EFFECT
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 110, fontWeight: 900,
          color: '#1F150C', lineHeight: 1.05, marginTop: 16,
        }}>
          Costs <span style={{ color: '#ff0000' }}>BALLOON</span> कैसे होती हैं
        </div>
      </div>

      {/* Balloon visual */}
      <div style={{
        position: 'absolute', left: 220, top: 360,
        transform: `translateY(${balloonY}px) scale(${balloonScale})`,
        transformOrigin: 'bottom center',
      }}>
        <svg width="400" height="500" viewBox="0 0 400 500">
          <ellipse cx="200" cy="200" rx="180" ry="200" fill="#ff0000" />
          <ellipse cx="160" cy="150" rx="40" ry="60" fill="#F5F5F5" opacity="0.3" />
          <path d="M 180 395 L 220 395 L 215 415 L 185 415 Z" fill="#1F150C" />
          <path d="M 200 415 Q 180 460 200 500" stroke="#1F150C" strokeWidth="3" fill="none" />
          <text x="200" y="210" textAnchor="middle" fontFamily="Yantra One" fontSize="80" fontWeight="900" fill="#F5F5F5">
            ₹{costValue}L
          </text>
          <text x="200" y="260" textAnchor="middle" fontFamily="Inter" fontSize="26" fontWeight="600" fill="#F5F5F5">
            Loan Amount
          </text>
        </svg>
      </div>

      {/* Inflation gauge */}
      <div style={{
        position: 'absolute', right: 120, top: 360,
        width: 540,
        opacity: sp(50),
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 28, fontWeight: 700,
          color: '#1F150C', marginBottom: 14, letterSpacing: 3,
        }}>
          ANNUAL INFLATION
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 200, fontWeight: 900,
          color: '#ff0000', lineHeight: 1,
        }}>
          {inflationPercent}%
        </div>
        <div style={{
          width: '100%', height: 24, borderRadius: 12,
          backgroundColor: '#1F150C', overflow: 'hidden', marginTop: 20,
        }}>
          <div style={{
            width: `${(inflationPercent / 15) * 100}%`,
            height: '100%',
            background: 'linear-gradient(90deg, #FFC107, #ff0000)',
          }} />
        </div>
        <div style={{
          fontFamily: 'Noto Sans Devanagari', fontSize: 30, fontWeight: 600,
          color: '#080616', marginTop: 24, lineHeight: 1.3,
        }}>
          हर साल आपकी पढ़ाई की लागत<br/>
          चुपचाप बढ़ती जा रही है
        </div>
      </div>

      {/* Action checklist */}
      <div style={{
        position: 'absolute', bottom: 60, left: 100, right: 100,
        display: 'flex', gap: 24,
      }}>
        {[
          { d: check1, label: 'Loan Terms पढ़ो', icon: '01' },
          { d: check2, label: 'Moratorium Interest Check', icon: '02' },
          { d: check3, label: 'Subsidy Apply', icon: '03' },
        ].map((item, i) => (
          <div key={i} style={{
            flex: 1, height: 130, borderRadius: 14,
            backgroundColor: '#1F150C',
            padding: '20px 26px',
            display: 'flex', flexDirection: 'column', justifyContent: 'center', gap: 8,
            opacity: item.d,
            transform: `translateY(${interpolate(item.d, [0,1], [40, 0])}px)`,
            borderLeft: '6px solid #FFC107',
          }}>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700,
              color: '#FFC107',
            }}>
              {item.icon} ✓
            </div>
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 32, fontWeight: 700,
              color: '#F5F5F5',
            }}>
              {item.label}
            </div>
          </div>
        ))}
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(31,21,12,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene42_BalloonInflation;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene43_InfoAsymmetry',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene43_InfoAsymmetry: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Pen signing animation
  const penProgress = interpolate(frame, [30, 90], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const stampDrop = sp(100, { damping: 8, stiffness: 200 });
  const stampScale = interpolate(stampDrop, [0, 1], [3, 1]);

  // Split reveal
  const splitProgress = sp(150);
  const leftSide = interpolate(splitProgress, [0, 1], [-100, 0]);
  const rightSide = interpolate(splitProgress, [0, 1], [100, 0]);

  // Camera tilt
  const camTilt = interpolate(frame, [0, durationInFrames], [0, 1.5]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp, transform: `rotate(${camTilt}deg)` }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(ellipse at top, rgba(255,0,0,0.06) 0%, transparent 60%), linear-gradient(180deg, transparent 50%, rgba(31,21,12,0.08) 100%)',
      }} />

      {/* Shape decor */}
      <div style={{
        position: 'absolute', left: -150, top: -100,
        width: 500, height: 500, borderRadius: '50%',
        backgroundColor: 'rgba(255,193,7,0.05)',
        transform: `scale(${1 + Math.sin(frame * 0.02) * 0.1})`,
      }} />
      <div style={{
        position: 'absolute', right: -200, bottom: -150,
        width: 600, height: 600, borderRadius: '30%',
        backgroundColor: 'rgba(8,6,22,0.04)',
        transform: `rotate(${frame * 0.15}deg)`,
      }} />

      {/* Circle texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.05 }}>
        <defs>
          <pattern id="dots43" width="50" height="50" patternUnits="userSpaceOnUse">
            <circle cx="25" cy="25" r="2" fill="#080616" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#dots43)" />
      </svg>

      {/* Particles */}
      {Array.from({length: 28}).map((_, i) => {
        const px = (seededRandom(i+500) * 1920 + frame * 0.3) % 1920;
        const py = seededRandom(i+600) * 1080;
        return <div key={i} style={{
          position: 'absolute', left: px, top: py,
          width: 3, height: 3, borderRadius: '50%',
          backgroundColor: '#1F150C', opacity: 0.3,
        }} />;
      })}

      {/* Top: warning headline */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0, textAlign: 'center',
        opacity: sp(5),
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 28, fontWeight: 800,
          color: '#ff0000', letterSpacing: 6, marginBottom: 10,
        }}>
          ⚠ STOP — BLIND SIGNING
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 95, fontWeight: 900,
          color: '#1F150C', lineHeight: 1.05,
        }}>
          Information <span style={{ color: '#ff0000' }}>Asymmetry</span> का खेल
        </div>
      </div>

      {/* Center document with pen */}
      <div style={{
        position: 'absolute', top: 320, left: '50%',
        transform: `translateX(-50%)`,
        width: 480, height: 280,
        opacity: interpolate(sp(20), [0,1], [0,1]),
      }}>
        <div style={{
          width: '100%', height: '100%',
          backgroundColor: '#F5F5F5',
          borderRadius: 8,
          padding: 28,
          boxShadow: '0 20px 50px rgba(31,21,12,0.25)',
          border: '2px solid #1F150C',
          position: 'relative',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 18, fontWeight: 700, color: '#080616', letterSpacing: 2 }}>
            LOAN AGREEMENT
          </div>
          <div style={{ marginTop: 14, display: 'flex', flexDirection: 'column', gap: 10 }}>
            {[0.95, 0.85, 0.9, 0.8, 0.7].map((w, i) => (
              <div key={i} style={{
                width: `${w * 100}%`, height: 8,
                backgroundColor: '#9E9E9E', opacity: 0.4, borderRadius: 2,
              }} />
            ))}
          </div>
          {/* Signature line */}
          <div style={{
            position: 'absolute', bottom: 50, left: 28, right: 28,
            borderBottom: '2px solid #1F150C',
          }}>
            <svg width="100%" height="40" style={{ position: 'absolute', top: -30 }}>
              <path d={`M 20 25 Q 80 5 140 20 T 280 15`}
                fill="none" stroke="#ff0000" strokeWidth="3"
                strokeDasharray="400"
                strokeDashoffset={400 - 400 * penProgress} />
            </svg>
          </div>
          {/* Stamp */}
          <div style={{
            position: 'absolute', right: 30, bottom: 30,
            transform: `scale(${stampScale}) rotate(-15deg)`,
            opacity: stampDrop,
          }}>
            <div style={{
              border: '4px solid #ff0000', borderRadius: 8,
              padding: '6px 16px',
              fontFamily: 'Inter', fontSize: 22, fontWeight: 900,
              color: '#ff0000', letterSpacing: 3,
            }}>
              TRAPPED
            </div>
          </div>
        </div>
      </div>

      {/* Bottom split: TWO CATEGORIES */}
      <div style={{
        position: 'absolute', bottom: 60, left: 80, right: 80,
        display: 'flex', gap: 30, height: 220,
      }}>
        {/* LEFT - जो नहीं जानता */}
        <div style={{
          flex: 1,
          backgroundColor: '#080616',
          borderRadius: 16,
          padding: '24px 32px',
          transform: `translateX(${leftSide}%)`,
          opacity: splitProgress,
          display: 'flex', flexDirection: 'column', justifyContent: 'space-between',
          borderLeft: '8px solid #ff0000',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 24, fontWeight: 800, color: '#ff0000', letterSpacing: 3 }}>
            ✗ CATEGORY A
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 64, fontWeight: 900, color: '#F5F5F5', lineHeight: 1 }}>
            जो नहीं जानता
          </div>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 36, fontWeight: 700, color: '#ff0000' }}>
            → वो चुकाता है
          </div>
        </div>

        {/* RIGHT - जो जानता है */}
        <div style={{
          flex: 1,
          backgroundColor: '#1F150C',
          borderRadius: 16,
          padding: '24px 32px',
          transform: `translateX(${rightSide}%)`,
          opacity: splitProgress,
          display: 'flex', flexDirection: 'column', justifyContent: 'space-between',
          borderLeft: '8px solid #FFC107',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 24, fontWeight: 800, color: '#FFC107', letterSpacing: 3 }}>
            ✓ CATEGORY B
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 64, fontWeight: 900, color: '#F5F5F5', lineHeight: 1 }}>
            जो जानता है
          </div>
          <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 36, fontWeight: 700, color: '#FFC107' }}>
            → वो plan करता है
          </div>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(31,21,12,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene43_InfoAsymmetry;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene44_IndianFlavour',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene44_IndianFlavour: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Layer peel
  const peelProgress = interpolate(frame, [20, 80], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // VS battle
  const usaSlide = sp(120);
  const indiaSlide = sp(140);

  // Depth bars
  const usaDepth = interpolate(sp(180), [0, 1], [0, 1]);
  const indiaDepth = interpolate(sp(220), [0, 1], [0, 1]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(31,21,12,0.08) 0%, transparent 40%, rgba(255,0,0,0.06) 100%)',
      }} />

      {/* Shape layers */}
      {[0,1,2,3,4,5].map(i => {
        const s = seededRandom(i + 700);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: `${s * 90}%`,
            top: `${seededRandom(i+800) * 80}%`,
            width: 200 + s * 180,
            height: 200 + s * 180,
            border: `2px solid rgba(8,6,22,${0.04 + s * 0.04})`,
            borderRadius: i % 3 === 0 ? '50%' : (i % 3 === 1 ? '20%' : '0'),
            transform: `rotate(${frame * 0.08 + i * 30}deg)`,
          }} />
        );
      })}

      {/* Hex grid texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.07 }}>
        <defs>
          <pattern id="hex44" width="60" height="52" patternUnits="userSpaceOnUse">
            <polygon points="30,2 58,17 58,37 30,52 2,37 2,17" fill="none" stroke="#1F150C" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hex44)" />
      </svg>

      {/* Particles */}
      {Array.from({length: 32}).map((_, i) => {
        const px = seededRandom(i + 900) * 1920;
        const py = (seededRandom(i + 1000) * 1080 + frame * 0.4) % 1080;
        return <div key={i} style={{
          position: 'absolute', left: px, top: py,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: i % 2 === 0 ? '#FFC107' : '#ff0000', opacity: 0.4,
        }} />;
      })}

      {/* Layer peel animation - top header */}
      <div style={{
        position: 'absolute', top: 70, left: 0, right: 0, textAlign: 'center',
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 30, fontWeight: 800,
          color: '#ff0000', letterSpacing: 6,
          opacity: sp(5),
        }}>
          ◢ UNCOVERING ANOTHER LAYER
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 130, fontWeight: 900,
          color: '#1F150C', lineHeight: 1.05, marginTop: 18,
          opacity: peelProgress,
          transform: `translateY(${interpolate(peelProgress,[0,1],[60,0])}px)`,
        }}>
          भारतीय रंग का<br/><span style={{ color: '#ff0000' }}>अनोखा दर्द</span>
        </div>
      </div>

      {/* USA vs INDIA comparison */}
      <div style={{
        position: 'absolute', bottom: 100, left: 100, right: 100,
        display: 'flex', gap: 40,
      }}>
        {/* USA card */}
        <div style={{
          flex: 1, height: 340,
          backgroundColor: '#F5F5F5',
          border: '3px solid #080616',
          borderRadius: 18,
          padding: 30,
          transform: `translateX(${interpolate(usaSlide,[0,1],[-120, 0])}%)`,
          opacity: usaSlide,
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 36, fontWeight: 900, color: '#080616', letterSpacing: 4 }}>
            🇺🇸 USA / UK MODEL
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 56, fontWeight: 900, color: '#1F150C', marginTop: 18, lineHeight: 1.1 }}>
            कर्ज सिर्फ<br/>छात्र का
          </div>
          {/* Depth indicator */}
          <div style={{ marginTop: 20 }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 18, fontWeight: 600, color: '#9E9E9E', letterSpacing: 2 }}>
              PAIN DEPTH
            </div>
            <div style={{ height: 24, backgroundColor: '#1F150C', borderRadius: 12, marginTop: 8, overflow: 'hidden' }}>
              <div style={{
                width: `${usaDepth * 35}%`, height: '100%',
                background: 'linear-gradient(90deg, #FFC107, #ff0000)',
              }} />
            </div>
          </div>
        </div>

        {/* VS divider */}
        <div style={{
          alignSelf: 'center',
          fontFamily: 'Yantra One', fontSize: 100, fontWeight: 900,
          color: '#080616',
          transform: `scale(${sp(160, { damping: 6 })})`,
        }}>
          VS
        </div>

        {/* INDIA card */}
        <div style={{
          flex: 1, height: 340,
          backgroundColor: '#1F150C',
          borderRadius: 18,
          padding: 30,
          transform: `translateX(${interpolate(indiaSlide,[0,1],[120, 0])}%)`,
          opacity: indiaSlide,
          borderLeft: '8px solid #ff0000',
        }}>
          <div style={{ fontFamily: 'Inter', fontSize: 36, fontWeight: 900, color: '#FFC107', letterSpacing: 4 }}>
            🇮🇳 INDIA MODEL
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 56, fontWeight: 900, color: '#F5F5F5', marginTop: 18, lineHeight: 1.1 }}>
            पारिवारिक<br/>दायित्व
          </div>
          <div style={{ marginTop: 20 }}>
            <div style={{ fontFamily: 'JetBrains Mono', fontSize: 18, fontWeight: 600, color: '#FFC107', letterSpacing: 2 }}>
              PAIN DEPTH
            </div>
            <div style={{ height: 24, backgroundColor: '#080616', borderRadius: 12, marginTop: 8, overflow: 'hidden' }}>
              <div style={{
                width: `${indiaDepth * 100}%`, height: '100%',
                background: 'linear-gradient(90deg, #ff0000, #FFC107, #ff0000)',
              }} />
            </div>
          </div>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(31,21,12,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene44_IndianFlavour;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene45_FamilyCollateral',
  durationInSeconds: 27,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene45_FamilyCollateral: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Three collateral cards stagger
  const card1 = sp(120);
  const card2 = sp(180);
  const card3 = sp(240);

  // Pulsing center node
  const pulse = Math.sin(frame * 0.1) * 0.05 + 1;

  // Vulnerability bar
  const vulnGrow = interpolate(sp(450), [0, 1], [0, 92]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, rgba(255,193,7,0.08) 0%, transparent 60%)',
      }} />

      {/* Concentric circles texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        {[100, 200, 300, 400, 500, 600].map(r => (
          <circle key={r} cx="960" cy="540" r={r}
            fill="none" stroke="#1F150C" strokeWidth="1"
            strokeDasharray="4 4"
            style={{ transformOrigin: '960px 540px', transform: `rotate(${frame * 0.1}deg)` }}
          />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({length: 35}).map((_, i) => {
        const px = (seededRandom(i+1100) * 1920 + Math.sin(frame * 0.02 + i) * 30) % 1920;
        const py = (seededRandom(i+1200) * 1080 + frame * 0.3) % 1080;
        return <div key={i} style={{
          position: 'absolute', left: px, top: py,
          width: 3, height: 3, borderRadius: '50%',
          backgroundColor: '#080616', opacity: 0.3,
        }} />;
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 60, left: 100, right: 100, textAlign: 'center',
        opacity: sp(10),
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 30, fontWeight: 800,
          color: '#ff0000', letterSpacing: 5,
        }}>
          ◆ THE FAMILY COLLATERAL TRAP
        </div>
        <div style={{
          fontFamily: 'Yantra One', fontSize: 95, fontWeight: 900,
          color: '#1F150C', marginTop: 12, lineHeight: 1.05,
        }}>
          एक loan, <span style={{ color: '#ff0000' }}>पूरा परिवार</span> दाँव पर
        </div>
      </div>

      {/* Center: Student node */}
      <div style={{
        position: 'absolute', left: '50%', top: '46%',
        transform: `translate(-50%, -50%) scale(${pulse})`,
      }}>
        <svg width="200" height="200" viewBox="0 0 200 200">
          <circle cx="100" cy="100" r="90" fill="#080616" />
          <circle cx="100" cy="100" r="90" fill="none" stroke="#FFC107" strokeWidth="3" />
          <circle cx="100" cy="100" r="78" fill="none" stroke="#FFC107" strokeWidth="1" strokeDasharray="4 4" 
            style={{ transformOrigin: '100px 100px', transform: `rotate(${frame * 2}deg)` }} />
          <text x="100" y="92" textAnchor="middle" fontFamily="Yantra One" fontSize="42" fontWeight="900" fill="#FFC107">
            STUDENT
          </text>
          <text x="100" y="125" textAnchor="middle" fontFamily="Inter" fontSize="20" fontWeight="700" fill="#F5F5F5">
            LOAN ₹20L
          </text>
        </svg>
      </div>

      {/* Connecting lines from center to cards */}
      <svg style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: '100%', pointerEvents: 'none' }}>
        {[
          { x: 320, y: 780, op: card1 },
          { x: 960, y: 880, op: card2 },
          { x: 1600, y: 780, op: card3 },
        ].map((p, i) => (
          <line key={i} x1="960" y1="600" x2={p.x} y2={p.y}
            stroke="#ff0000" strokeWidth="3" strokeDasharray="8 6" opacity={p.op * 0.6} />
        ))}
      </svg>

      {/* Three collateral cards */}
      <div style={{
        position: 'absolute', bottom: 100, left: 80, right: 80,
        display: 'flex', justifyContent: 'space-between', gap: 30,
      }}>
        {/* पिता की जमीन */}
        <div style={{
          width: 480, height: 280,
          backgroundColor: '#1F150C',
          borderRadius: 16,
          padding: 28,
          opacity: card1,
          transform: `translateY(${interpolate(card1,[0,1],[60,0])}px) scale(${card1})`,
          borderTop: '6px solid #FFC107',
        }}>
          <svg width="60" height="60" viewBox="0 0 60 60">
            <path d="M 10 50 L 30 15 L 50 50 Z" fill="#FFC107" />
            <rect x="22" y="38" width="16" height="12" fill="#1F150C" />
          </svg>
          <div style={{ fontFamily: 'Inter', fontSize: 20, fontWeight: 700, color: '#FFC107', letterSpacing: 3, marginTop: 12 }}>
            COLLATERAL 01
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 56, fontWeight: 900, color: '#F5F5F5', marginTop: 10, lineHeight: 1 }}>
            पिता की<br/>जमीन
          </div>
        </div>

        {/* माँ की FD */}
        <div style={{
          width: 480, height: 280,
          backgroundColor: '#080616',
          borderRadius: 16,
          padding: 28,
          opacity: card2,
          transform: `translateY(${interpolate(card2,[0,1],[60,0])}px) scale(${card2})`,
          borderTop: '6px solid #ff0000',
        }}>
          <svg width="60" height="60" viewBox="0 0 60 60">
            <rect x="8" y="14" width="44" height="32" rx="3" fill="#ff0000" />
            <text x="30" y="36" textAnchor="middle" fontFamily="Inter" fontSize="16" fontWeight="900" fill="#F5F5F5">FD</text>
          </svg>
          <div style={{ fontFamily: 'Inter', fontSize: 20, fontWeight: 700, color: '#ff0000', letterSpacing: 3, marginTop: 12 }}>
            COLLATERAL 02
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 56, fontWeight: 900, color: '#F5F5F5', marginTop: 10, lineHeight: 1 }}>
            माँ की<br/>FD
          </div>
        </div>

        {/* दादाजी की प्रॉपर्टी */}
        <div style={{
          width: 480, height: 280,
          backgroundColor: '#1F150C',
          borderRadius: 16,
          padding: 28,
          opacity: card3,
          transform: `translateY(${interpolate(card3,[0,1],[60,0])}px) scale(${card3})`,
          borderTop: '6px solid #FFC107',
        }}>
          <svg width="60" height="60" viewBox="0 0 60 60">
            <rect x="8" y="22" width="44" height="28" fill="#FFC107" />
            <path d="M 5 22 L 30 6 L 55 22 Z" fill="#FFC107" />
            <rect x="24" y="32" width="12" height="18" fill="#1F150C" />
          </svg>
          <div style={{ fontFamily: 'Inter', fontSize: 20, fontWeight: 700, color: '#FFC107', letterSpacing: 3, marginTop: 12 }}>
            COLLATERAL 03
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 56, fontWeight: 900, color: '#F5F5F5', marginTop: 10, lineHeight: 1 }}>
            दादाजी की<br/>प्रॉपर्टी
          </div>
        </div>
      </div>

      {/* Vulnerability meter at top right */}
      <div style={{
        position: 'absolute', top: 280, right: 80, width: 380,
        opacity: sp(420),
      }}>
        <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#1F150C', letterSpacing: 3 }}>
          FAMILY VULNERABILITY
        </div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 110, fontWeight: 900, color: '#ff0000', lineHeight: 1 }}>
          {Math.floor(vulnGrow)}%
        </div>
        <div style={{ height: 18, backgroundColor: '#1F150C', borderRadius: 9, overflow: 'hidden', marginTop: 8 }}>
          <div style={{
            width: `${vulnGrow}%`, height: '100%',
            background: 'linear-gradient(90deg, #FFC107, #ff0000)',
          }} />
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(31,21,12,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene45_FamilyCollateral;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene46_NPAGrowth',
  durationInSeconds: 11,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene46_NPAGrowth: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Chart line draw
  const chartProgress = interpolate(frame, [40, 200], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  
  // Counter values
  const val2015 = interpolate(frame, [60, 140], [0, 7.6], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const val2024 = interpolate(frame, [180, 280], [0, 12.3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Chart points
  const chartW = 1100;
  const chartH = 400;
  const chartX = 350;
  const chartY = 380;

  // X-axis years
  const years = ['2015', '2017', '2019', '2021', '2024'];
  const values = [7.6, 8.4, 9.5, 11.0, 12.3];

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, rgba(255,0,0,0.05) 0%, transparent 60%)',
      }} />

      {/* Shape decor */}
      {[0,1,2].map(i => (
        <div key={i} style={{
          position: 'absolute',
          left: `${10 + i * 35}%`, top: `${5 + i * 20}%`,
          width: 220, height: 220, borderRadius: '50%',
          border: '2px solid rgba(255,0,0,0.06)',
          transform: `scale(${1 + Math.sin(frame * 0.04 + i) * 0.06})`,
        }} />
      ))}

      {/* Grid */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.08 }}>
        <defs>
          <pattern id="grid46" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 40 0 L 0 0 0 40" fill="none" stroke="#080616" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid46)" />
      </svg>

      {/* Particles */}
      {Array.from({length: 22}).map((_, i) => {
        const px = seededRandom(i + 1300) * 1920;
        const py = (seededRandom(i + 1400) * 1080 - frame * 0.5 + 1080) % 1080;
        return <div key={i} style={{
          position: 'absolute', left: px, top: py,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: '#ff0000', opacity: 0.4,
        }} />;
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 60, left: 100,
        opacity: sp(5),
      }}>
        <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 800, color: '#ff0000', letterSpacing: 5 }}>
          ◢ RBI + IBA DATA
        </div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 95, fontWeight: 900, color: '#1F150C', marginTop: 10, lineHeight: 1.05 }}>
          Education Loan <span style={{ color: '#ff0000' }}>NPA</span>
        </div>
      </div>

      {/* Chart */}
      <svg width="100%" height="100%" style={{ position: 'absolute' }}>
        {/* Y-axis grid lines */}
        {[0, 4, 8, 12, 16].map((v, i) => {
          const y = chartY + chartH - (v / 16) * chartH;
          return (
            <g key={i}>
              <line x1={chartX} y1={y} x2={chartX + chartW} y2={y}
                stroke="#1F150C" strokeWidth="1" strokeDasharray="4 4" opacity="0.2" />
              <text x={chartX - 20} y={y + 8} textAnchor="end" fontFamily="JetBrains Mono" fontSize="22" fontWeight="600" fill="#1F150C">
                {v}%
              </text>
            </g>
          );
        })}

        {/* X-axis labels */}
        {years.map((yr, i) => {
          const x = chartX + (i / (years.length - 1)) * chartW;
          return (
            <text key={i} x={x} y={chartY + chartH + 40}
              textAnchor="middle" fontFamily="JetBrains Mono" fontSize="24" fontWeight="700" fill="#080616">
              {yr}
            </text>
          );
        })}

        {/* Area fill */}
        <path d={`M ${chartX} ${chartY + chartH} ${values.map((v, i) => {
          const x = chartX + (i / (values.length - 1)) * chartW;
          const y = chartY + chartH - (v / 16) * chartH;
          return `L ${x} ${y}`;
        }).join(' ')} L ${chartX + chartW} ${chartY + chartH} Z`}
          fill="url(#redGrad46)" opacity={chartProgress * 0.4} />

        <defs>
          <linearGradient id="redGrad46" x1="0" y1="0" x2="0" y2="1">
            <stop offset="0%" stopColor="#ff0000" stopOpacity="0.7" />
            <stop offset="100%" stopColor="#ff0000" stopOpacity="0" />
          </linearGradient>
        </defs>

        {/* Line */}
        <path d={`M ${values.map((v, i) => {
          const x = chartX + (i / (values.length - 1)) * chartW;
          const y = chartY + chartH - (v / 16) * chartH;
          return `${i === 0 ? '' : 'L '}${x} ${y}`;
        }).join(' ')}`}
          fill="none" stroke="#ff0000" strokeWidth="6" strokeLinecap="round"
          strokeDasharray="2000"
          strokeDashoffset={2000 - 2000 * chartProgress} />

        {/* Data points */}
        {values.map((v, i) => {
          const x = chartX + (i / (values.length - 1)) * chartW;
          const y = chartY + chartH - (v / 16) * chartH;
          const visible = chartProgress > i / values.length;
          return (
            <g key={i}>
              <circle cx={x} cy={y} r={visible ? 12 : 0} fill="#1F150C" />
              <circle cx={x} cy={y} r={visible ? 7 : 0} fill="#ff0000" />
            </g>
          );
        })}
      </svg>

      {/* 2015 Callout */}
      <div style={{
        position: 'absolute', left: 280, top: 720,
        opacity: sp(50),
      }}>
        <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#9E9E9E', letterSpacing: 2 }}>
          2015
        </div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 78, fontWeight: 900, color: '#1F150C', lineHeight: 1 }}>
          {val2015.toFixed(1)}%
        </div>
      </div>

      {/* Arrow */}
      <div style={{
        position: 'absolute', left: 720, top: 770,
        fontFamily: 'JetBrains Mono', fontSize: 60, fontWeight: 900,
        color: '#ff0000',
        opacity: sp(160),
      }}>
        →
      </div>

      {/* 2024 Callout */}
      <div style={{
        position: 'absolute', right: 220, top: 700,
        opacity: sp(170),
        textAlign: 'right',
      }}>
        <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#ff0000', letterSpacing: 2 }}>
          2024
        </div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 110, fontWeight: 900, color: '#ff0000', lineHeight: 1 }}>
          {val2024.toFixed(1)}%
        </div>
        <div style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 700, color: '#1F150C', marginTop: 4 }}>
          DANGER ZONE
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(31,21,12,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene46_NPAGrowth;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene47_StructuralAlarm',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene47_StructuralAlarm: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // 10 figures grid - 1 turns red
  const familiesReveal = (i: number) => sp(60 + i * 8);

  // Alarm pulse
  const alarmPulse = Math.sin(frame * 0.25) * 0.5 + 0.5;
  const screenShake = frame > 280 && frame < 320 ? Math.sin(frame * 1.5) * 4 : 0;

  return (
    <AbsoluteFill style={{ 
      backgroundColor: '#DFF1F1', 
      overflow: 'hidden', 
      opacity: exitOp,
      transform: `translateX(${screenShake}px)`,
    }}>
      <AbsoluteFill style={{
        background: `radial-gradient(circle at 50% 50%, rgba(255,0,0,${0.06 + alarmPulse * 0.08}) 0%, transparent 60%)`,
      }} />

      {/* Concentric rings */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.1 }}>
        {[150, 300, 450, 600, 750].map((r, i) => (
          <circle key={r} cx="960" cy="540" r={r + alarmPulse * 20}
            fill="none" stroke="#ff0000" strokeWidth="1.5"
            opacity={0.4 - i * 0.06} />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({length: 25}).map((_, i) => {
        const px = (seededRandom(i + 1500) * 1920 + Math.cos(frame * 0.02 + i) * 20);
        const py = (seededRandom(i + 1600) * 1080 + frame * 0.4) % 1080;
        return <div key={i} style={{
          position: 'absolute', left: px, top: py,
          width: 3, height: 3, borderRadius: '50%',
          backgroundColor: '#1F150C', opacity: 0.3,
        }} />;
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 60, left: 100, right: 100,
        opacity: sp(5),
      }}>
        <div style={{ fontFamily: 'Inter', fontSize: 30, fontWeight: 800, color: '#ff0000', letterSpacing: 5 }}>
          ◢ STRUCTURAL ALARM
        </div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 88, fontWeight: 900, color: '#1F150C', marginTop: 10, lineHeight: 1.05 }}>
          हर <span style={{ color: '#ff0000' }}>10 में से 1</span> परिवार<br/>लोन नहीं चुका पा रहा
        </div>
      </div>

      {/* 10 family figures - 1 red */}
      <div style={{
        position: 'absolute', top: 360, left: 100, right: 100,
        display: 'grid', gridTemplateColumns: 'repeat(10, 1fr)', gap: 16,
      }}>
        {Array.from({length: 10}).map((_, i) => {
          const isRed = i === 7;
          const op = familiesReveal(i);
          return (
            <div key={i} style={{
              opacity: op,
              transform: `scale(${op}) translateY(${interpolate(op,[0,1],[40,0])}px)`,
            }}>
              <svg viewBox="0 0 100 140" width="100%">
                {/* Head */}
                <circle cx="50" cy="32" r="22"
                  fill={isRed ? '#ff0000' : '#1F150C'} />
                {/* Body */}
                <path d="M 18 130 L 18 80 Q 18 60 50 60 Q 82 60 82 80 L 82 130 Z"
                  fill={isRed ? '#ff0000' : '#1F150C'} />
                {isRed && (
                  <>
                    <circle cx="50" cy="32" r="32" fill="none" stroke="#ff0000" strokeWidth="2"
                      opacity={alarmPulse} />
                    <text x="50" y="38" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#F5F5F5">!</text>
                  </>
                )}
              </svg>
            </div>
          );
        })}
      </div>

      {/* Bottom statement box */}
      <div style={{
        position: 'absolute', bottom: 80, left: 100, right: 100,
        backgroundColor: '#1F150C',
        borderRadius: 18,
        padding: '36px 48px',
        opacity: sp(260),
        transform: `translateY(${interpolate(sp(260),[0,1],[50,0])}px)`,
        borderLeft: `10px solid #ff0000`,
        boxShadow: `0 0 ${30 + alarmPulse * 30}px rgba(255,0,0,${0.3 + alarmPulse * 0.3})`,
        display: 'flex', alignItems: 'center', gap: 36,
      }}>
        {/* Alarm icon */}
        <svg width="120" height="120" viewBox="0 0 120 120">
          <path d="M 60 20 Q 30 20 25 70 L 20 95 L 100 95 L 95 70 Q 90 20 60 20 Z"
            fill="#ff0000" />
          <rect x="50" y="95" width="20" height="8" fill="#ff0000" />
          <circle cx="60" cy="105" r="5" fill="#ff0000" />
          <line x1="22" y1="40" x2="10" y2="30" stroke="#FFC107" strokeWidth="4" strokeLinecap="round" opacity={alarmPulse} />
          <line x1="98" y1="40" x2="110" y2="30" stroke="#FFC107" strokeWidth="4" strokeLinecap="round" opacity={alarmPulse} />
        </svg>

        <div style={{ flex: 1 }}>
          <div style={{ fontFamily: 'Inter', fontSize: 26, fontWeight: 700, color: '#FFC107', letterSpacing: 4 }}>
            ये FAILURE RATE नहीं है
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 76, fontWeight: 900, color: '#F5F5F5', marginTop: 8, lineHeight: 1 }}>
            ये एक<br/><span style={{ color: '#ff0000' }}>STRUCTURAL ALARM</span> है
          </div>
        </div>

        {/* % indicator */}
        <div style={{ textAlign: 'right' }}>
          <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#9E9E9E', letterSpacing: 3 }}>
            DEFAULT
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 120, fontWeight: 900, color: '#ff0000', lineHeight: 1 }}>
            12%
          </div>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 45%, rgba(31,21,12,0.28) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene47_StructuralAlarm;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene48_CommentCTA',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene48_CommentCTA: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Phone vibrate
  const phoneVibrate = frame > 60 && frame < 180 ? Math.sin(frame * 1.8) * 6 : 0;
  const phoneTilt = frame > 60 && frame < 180 ? Math.sin(frame * 0.4) * 3 : 0;

  // Comment bubbles
  const bubble1 = sp(180);
  const bubble2 = sp(230);
  const bubble3 = sp(280);

  // Pulse arrow
  const arrowPulse = Math.sin(frame * 0.2) * 8;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 25% 50%, rgba(255,193,7,0.08) 0%, transparent 60%)',
      }} />

      {/* Drifting shapes */}
      {[0,1,2,3].map(i => (
        <div key={i} style={{
          position: 'absolute',
          left: `${seededRandom(i+1700) * 85}%`,
          top: `${seededRandom(i+1800) * 80}%`,
          width: 260, height: 260, borderRadius: '50%',
          border: '2px solid rgba(31,21,12,0.06)',
          transform: `rotate(${frame * 0.1 + i * 90}deg) scale(${1 + Math.sin(frame * 0.025 + i) * 0.08})`,
        }} />
      ))}

      {/* Wave texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="wave48" width="100" height="20" patternUnits="userSpaceOnUse">
            <path d="M 0 10 Q 25 0 50 10 T 100 10" fill="none" stroke="#080616" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#wave48)" />
      </svg>

      {/* Particles */}
      {Array.from({length: 22}).map((_, i) => {
        const px = seededRandom(i + 1900) * 1920;
        const py = (seededRandom(i + 2000) * 1080 - frame * 0.4 + 1080) % 1080;
        return <div key={i} style={{
          position: 'absolute', left: px, top: py,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: '#FFC107', opacity: 0.4,
        }} />;
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 80, left: 100, right: 100,
        opacity: sp(5),
      }}>
        <div style={{ fontFamily: 'Inter', fontSize: 30, fontWeight: 800, color: '#ff0000', letterSpacing: 5 }}>
          ◣ YOUR STORY MATTERS
        </div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 100, fontWeight: 900, color: '#1F150C', marginTop: 12, lineHeight: 1.05 }}>
          क्या आपने झेला है<br/><span style={{ color: '#ff0000' }}>Bank का प्रेशर?</span>
        </div>
      </div>

      {/* Phone with EMI missed notification */}
      <div style={{
        position: 'absolute', left: 200, top: 450,
        transform: `translateX(${phoneVibrate}px) rotate(${phoneTilt}deg) scale(${sp(40)})`,
        opacity: sp(40),
      }}>
        <svg width="320" height="500" viewBox="0 0 320 500">
          <rect x="20" y="20" width="280" height="460" rx="40" fill="#1F150C" />
          <rect x="40" y="60" width="240" height="380" rx="8" fill="#080616" />
          <circle cx="160" cy="38" r="6" fill="#9E9E9E" />
          {/* Notification card */}
          <rect x="55" y="100" width="210" height="120" rx="12" fill="#ff0000" />
          <text x="70" y="130" fontFamily="Inter" fontSize="14" fontWeight="700" fill="#F5F5F5">
            BANK ALERT
          </text>
          <text x="70" y="160" fontFamily="Inter" fontSize="22" fontWeight="900" fill="#F5F5F5">
            EMI MISSED
          </text>
          <text x="70" y="185" fontFamily="Inter" fontSize="14" fontWeight="600" fill="#FFC107">
            Penalty + Late Fee
          </text>
          <text x="70" y="205" fontFamily="Inter" fontSize="14" fontWeight="600" fill="#FFC107">
            Recovery initiated
          </text>
          {/* Second notification */}
          <rect x="55" y="240" width="210" height="80" rx="12" fill="#2D2D2D" />
          <text x="70" y="265" fontFamily="Inter" fontSize="13" fontWeight="700" fill="#9E9E9E">
            UNKNOWN CALLER
          </text>
          <text x="70" y="290" fontFamily="Inter" fontSize="18" fontWeight="800" fill="#F5F5F5">
            Recovery Agent
          </text>
          <text x="70" y="310" fontFamily="Inter" fontSize="12" fontWeight="600" fill="#ff0000">
            ● Calling...
          </text>
        </svg>
      </div>

      {/* YouTube comment box */}
      <div style={{
        position: 'absolute', right: 100, top: 450,
        width: 640,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 26, fontWeight: 700,
          color: '#080616', letterSpacing: 3, marginBottom: 16,
          opacity: sp(150),
        }}>
          ▼ COMMENT BELOW
        </div>

        {/* Comment 1 */}
        <div style={{
          backgroundColor: '#1F150C',
          borderRadius: 14,
          padding: '18px 24px',
          marginBottom: 12,
          opacity: bubble1,
          transform: `translateX(${interpolate(bubble1,[0,1],[80,0])}px)`,
          display: 'flex', gap: 16, alignItems: 'center',
        }}>
          <div style={{ width: 50, height: 50, borderRadius: '50%', backgroundColor: '#FFC107', flexShrink: 0,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: 'Inter', fontWeight: 900, fontSize: 22, color: '#1F150C' }}>R</div>
          <div>
            <div style={{ fontFamily: 'Inter', fontSize: 18, fontWeight: 700, color: '#FFC107' }}>@Rahul_S</div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 26, fontWeight: 600, color: '#F5F5F5' }}>
              मेरे साथ ये हुआ था...
            </div>
          </div>
        </div>

        {/* Comment 2 */}
        <div style={{
          backgroundColor: '#080616',
          borderRadius: 14,
          padding: '18px 24px',
          marginBottom: 12,
          opacity: bubble2,
          transform: `translateX(${interpolate(bubble2,[0,1],[80,0])}px)`,
          display: 'flex', gap: 16, alignItems: 'center',
        }}>
          <div style={{ width: 50, height: 50, borderRadius: '50%', backgroundColor: '#ff0000', flexShrink: 0,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: 'Inter', fontWeight: 900, fontSize: 22, color: '#F5F5F5' }}>P</div>
          <div>
            <div style={{ fontFamily: 'Inter', fontSize: 18, fontWeight: 700, color: '#ff0000' }}>@Priya_K</div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 26, fontWeight: 600, color: '#F5F5F5' }}>
              Recovery agents ने 3 बार call किया
            </div>
          </div>
        </div>

        {/* Comment 3 */}
        <div style={{
          backgroundColor: '#1F150C',
          borderRadius: 14,
          padding: '18px 24px',
          opacity: bubble3,
          transform: `translateX(${interpolate(bubble3,[0,1],[80,0])}px)`,
          display: 'flex', gap: 16, alignItems: 'center',
        }}>
          <div style={{ width: 50, height: 50, borderRadius: '50%', backgroundColor: '#FFC107', flexShrink: 0,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            fontFamily: 'Inter', fontWeight: 900, fontSize: 22, color: '#1F150C' }}>A</div>
          <div>
            <div style={{ fontFamily: 'Inter', fontSize: 18, fontWeight: 700, color: '#FFC107' }}>@Amit_R</div>
            <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 26, fontWeight: 600, color: '#F5F5F5' }}>
              Credit score टूट गया था
            </div>
          </div>
        </div>

        {/* Arrow down */}
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 48, fontWeight: 900,
          color: '#ff0000', textAlign: 'center', marginTop: 20,
          transform: `translateY(${arrowPulse}px)`,
          opacity: sp(300),
        }}>
          ↓ ↓ ↓
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(31,21,12,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene48_CommentCTA;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene49_OutstandingVsAccounts',
  durationInSeconds: 28,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene49_OutstandingVsAccounts: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Bar growth
  const outstanding2014 = interpolate(frame, [60, 160], [0, 52327], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const outstanding2025 = interpolate(frame, [200, 320], [0, 137474], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const accounts2014 = interpolate(frame, [360, 440], [0, 23.36], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const accounts2025 = interpolate(frame, [470, 550], [0, 20.63], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  // Insight reveal
  const insightOp = sp(620);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, rgba(255,193,7,0.06) 0%, transparent 50%, rgba(255,0,0,0.06) 100%)',
      }} />

      {/* Shape decor */}
      <div style={{
        position: 'absolute', top: -100, left: -100,
        width: 400, height: 400, borderRadius: '50%',
        backgroundColor: 'rgba(31,21,12,0.04)',
      }} />

      {/* Grid texture */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="grid49" width="50" height="50" patternUnits="userSpaceOnUse">
            <path d="M 50 0 L 0 0 0 50" fill="none" stroke="#080616" strokeWidth="1" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#grid49)" />
      </svg>

      {/* Particles */}
      {Array.from({length: 25}).map((_, i) => {
        const px = (seededRandom(i+2100) * 1920);
        const py = (seededRandom(i+2200) * 1080 + frame * 0.3) % 1080;
        return <div key={i} style={{
          position: 'absolute', left: px, top: py,
          width: 4, height: 4, borderRadius: '50%',
          backgroundColor: i % 2 === 0 ? '#FFC107' : '#1F150C', opacity: 0.3,
        }} />;
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 50, left: 100, right: 100,
        opacity: sp(5),
      }}>
        <div style={{ fontFamily: 'Inter', fontSize: 28, fontWeight: 800, color: '#ff0000', letterSpacing: 4 }}>
          ◢ 2014 vs 2025 — THE PARADOX
        </div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 80, fontWeight: 900, color: '#1F150C', marginTop: 8, lineHeight: 1 }}>
          कम लोग, <span style={{ color: '#ff0000' }}>ज्यादा बोझ</span>
        </div>
      </div>

      {/* LEFT: Outstanding Amount comparison */}
      <div style={{
        position: 'absolute', left: 80, top: 240, width: 880,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 26, fontWeight: 800,
          color: '#080616', letterSpacing: 3, marginBottom: 22,
        }}>
          ▲ TOTAL OUTSTANDING (₹ Crore)
        </div>

        {/* 2014 Bar */}
        <div style={{ marginBottom: 28 }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 8 }}>
            <span style={{ fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700, color: '#1F150C' }}>2014</span>
            <span style={{ fontFamily: 'Yantra One', fontSize: 44, fontWeight: 900, color: '#1F150C' }}>
              ₹{Math.floor(outstanding2014).toLocaleString('en-IN')} Cr
            </span>
          </div>
          <div style={{
            height: 60,
            width: `${(outstanding2014 / 137474) * 100}%`,
            backgroundColor: '#9E9E9E',
            borderRadius: 8,
            transition: 'width 0.3s',
          }} />
        </div>

        {/* 2025 Bar */}
        <div>
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 8 }}>
            <span style={{ fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700, color: '#ff0000' }}>2025</span>
            <span style={{ fontFamily: 'Yantra One', fontSize: 56, fontWeight: 900, color: '#ff0000' }}>
              ₹{Math.floor(outstanding2025).toLocaleString('en-IN')} Cr
            </span>
          </div>
          <div style={{
            height: 60,
            width: `${(outstanding2025 / 137474) * 100}%`,
            background: 'linear-gradient(90deg, #ff0000, #FFC107)',
            borderRadius: 8,
          }} />
        </div>

        <div style={{
          marginTop: 20, fontFamily: 'Inter', fontSize: 32, fontWeight: 800,
          color: '#ff0000', opacity: sp(340),
        }}>
          ↑ 2.6× INCREASE
        </div>
      </div>

      {/* RIGHT: Active Accounts (downward) */}
      <div style={{
        position: 'absolute', right: 80, top: 240, width: 880,
      }}>
        <div style={{
          fontFamily: 'Inter', fontSize: 26, fontWeight: 800,
          color: '#080616', letterSpacing: 3, marginBottom: 22,
        }}>
          ▼ ACTIVE LOAN ACCOUNTS (Lakhs)
        </div>

        {/* 2014 */}
        <div style={{ marginBottom: 28 }}>
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 8 }}>
            <span style={{ fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700, color: '#1F150C' }}>2014</span>
            <span style={{ fontFamily: 'Yantra One', fontSize: 56, fontWeight: 900, color: '#1F150C' }}>
              {accounts2014.toFixed(2)} L
            </span>
          </div>
          <div style={{
            height: 60,
            width: `${(accounts2014 / 25) * 100}%`,
            backgroundColor: '#FFC107',
            borderRadius: 8,
          }} />
        </div>

        {/* 2025 */}
        <div>
          <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 8 }}>
            <span style={{ fontFamily: 'JetBrains Mono', fontSize: 26, fontWeight: 700, color: '#ff0000' }}>2025</span>
            <span style={{ fontFamily: 'Yantra One', fontSize: 50, fontWeight: 900, color: '#ff0000' }}>
              {accounts2025.toFixed(2)} L
            </span>
          </div>
          <div style={{
            height: 60,
            width: `${(accounts2025 / 25) * 100}%`,
            background: 'linear-gradient(90deg, #9E9E9E, #ff0000)',
            borderRadius: 8,
          }} />
        </div>

        <div style={{
          marginTop: 20, fontFamily: 'Inter', fontSize: 32, fontWeight: 800,
          color: '#ff0000', opacity: sp(570),
        }}>
          ↓ ACCESSIBILITY घटी
        </div>
      </div>

      {/* Bottom insight */}
      <div style={{
        position: 'absolute', bottom: 60, left: 80, right: 80,
        backgroundColor: '#1F150C',
        borderRadius: 18,
        padding: '28px 40px',
        opacity: insightOp,
        transform: `translateY(${interpolate(insightOp,[0,1],[40,0])}px)`,
        borderLeft: '10px solid #FFC107',
        display: 'flex', alignItems: 'center', gap: 32,
      }}>
        <div style={{
          fontFamily: 'JetBrains Mono', fontSize: 70, fontWeight: 900,
          color: '#FFC107', flexShrink: 0,
        }}>
          ⚡
        </div>
        <div>
          <div style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#FFC107', letterSpacing: 3 }}>
            CRITICAL INSIGHT
          </div>
          <div style={{ fontFamily: 'Yantra One', fontSize: 52, fontWeight: 900, color: '#F5F5F5', marginTop: 4, lineHeight: 1.1 }}>
            Average loan size बढ़ रही है — शिक्षा महंगी, accessibility घट रही
          </div>
        </div>
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 55%, rgba(31,21,12,0.22) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene49_OutstandingVsAccounts;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'Scene410_4PercentReality',
  durationInSeconds: 19,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const Scene410_4PercentReality: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  // Donut chart
  const donutProgress = interpolate(frame, [40, 180], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const percentCount = Math.floor(interpolate(frame, [40, 180], [0, 4], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' }));

  // 100 dots grid
  const dotsReveal = (i: number) => sp(220 + i * 2);

  // Source labels
  const sourceFamily = sp(480);
  const sourceInformal = sp(520);
  const sourceSacrifice = sp(560);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 50%, rgba(255,0,0,0.08) 0%, transparent 55%)',
      }} />

      {/* Shape layers */}
      {[0,1,2,3].map(i => (
        <div key={i} style={{
          position: 'absolute',
          left: `${seededRandom(i+2300) * 85}%`,
          top: `${seededRandom(i+2400) * 80}%`,
          width: 280, height: 280, borderRadius: i % 2 === 0 ? '50%' : '20%',
          border: '2px solid rgba(31,21,12,0.05)',
          transform: `rotate(${frame * 0.08 + i * 50}deg)`,
        }} />
      ))}

      {/* Diagonal lines */}
      <svg width="100%" height="100%" style={{ position: 'absolute', opacity: 0.06 }}>
        <defs>
          <pattern id="diag410" width="70" height="70" patternUnits="userSpaceOnUse">
            <path d="M 0 70 L 70 0" stroke="#080616" strokeWidth="1.2" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#diag410)" />
      </svg>

      {/* Particles */}
      {Array.from({length: 24}).map((_, i) => {
        const px = seededRandom(i+2500) * 1920;
        const py = (seededRandom(i+2600) * 1080 + frame * 0.4) % 1080;
        return <div key={i} style={{
          position: 'absolute', left: px, top: py,
          width: 3, height: 3, borderRadius: '50%',
          backgroundColor: '#ff0000', opacity: 0.35,
        }} />;
      })}

      {/* Title */}
      <div style={{
        position: 'absolute', top: 60, left: 100, right: 100,
        opacity: sp(5),
      }}>
        <div style={{ fontFamily: 'Inter', fontSize: 30, fontWeight: 800, color: '#ff0000', letterSpacing: 5 }}>
          ◣ THE SHOCKING TRUTH
        </div>
        <div style={{ fontFamily: 'Yantra One', fontSize: 88, fontWeight: 900, color: '#1F150C', marginTop: 8, lineHeight: 1.05 }}>
          सिर्फ <span style={{ color: '#ff0000' }}>4%</span> को मिलता है Bank Loan
        </div>
      </div>

      {/* LEFT: Donut chart */}
      <div style={{
        position: 'absolute', left: 140, top: 320,
      }}>
        <svg width="500" height="500" viewBox="0 0 500 500">
          <circle cx="250" cy="250" r="180" fill="none" stroke="#1F150C" strokeWidth="60" opacity="0.2" />
          {/* 4% slice */}
          <circle cx="250" cy="250" r="180" fill="none" stroke="#ff0000" strokeWidth="60"
            strokeDasharray={`${1130.97 * 0.04 * donutProgress} ${1130.97}`}
            strokeLinecap="butt"
            transform="rotate(-90 250 250)" />
          {/* 96% slice */}
          <circle cx="250" cy="250" r="180" fill="none" stroke="#1F150C" strokeWidth="60"
            strokeDasharray={`${1130.97 * 0.96 * donutProgress} ${1130.97}`}
            strokeDashoffset={`${-1130.97 * 0.04 * donutProgress}`}
            strokeLinecap="butt"
            transform="rotate(-90 250 250)" />
          {/* Center text */}
          <text x="250" y="240" textAnchor="middle" fontFamily="Yantra One" fontSize="120" fontWeight="900" fill="#ff0000">
            {percentCount}%
          </text>
          <text x="250" y="290" textAnchor="middle" fontFamily="Inter" fontSize="22" fontWeight="700" fill="#080616">
            BANK FUNDED
          </text>
        </svg>
      </div>

      {/* RIGHT: 100 dots showing 96 vs 4 */}
      <div style={{
        position: 'absolute', right: 100, top: 320, width: 620,
      }}>
        <div style={{ fontFamily: 'Inter', fontSize: 24, fontWeight: 800, color: '#080616', letterSpacing: 3, marginBottom: 16 }}>
          ▼ 100 STUDENTS
        </div>
        <div style={{
          display: 'grid', gridTemplateColumns: 'repeat(10, 1fr)', gap: 10,
        }}>
          {Array.from({length: 100}).map((_, i) => {
            const isBank = i < 4;
            const op = dotsReveal(i);
            return (
              <div key={i} style={{
                width: 48, height: 48, borderRadius: 8,
                backgroundColor: isBank ? '#FFC107' : '#1F150C',
                opacity: op,
                transform: `scale(${op})`,
                display: 'flex', alignItems: 'center', justifyContent: 'center',
              }}>
                <svg viewBox="0 0 30 40" width="22" height="28">
                  <circle cx="15" cy="10" r="7" fill={isBank ? '#1F150C' : '#9E9E9E'} />
                  <path d="M 4 38 L 4 24 Q 4 18 15 18 Q 26 18 26 24 L 26 38 Z" fill={isBank ? '#1F150C' : '#9E9E9E'} />
                </svg>
              </div>
            );
          })}
        </div>
        <div style={{
          display: 'flex', gap: 24, marginTop: 18,
          opacity: sp(440),
        }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
            <div style={{ width: 20, height: 20, backgroundColor: '#FFC107', borderRadius: 4 }} />
            <span style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#080616' }}>4 — Bank Loan</span>
          </div>
          <div style={{ display: 'flex', alignItems: 'center', gap: 10 }}>
            <div style={{ width: 20, height: 20, backgroundColor: '#1F150C', borderRadius: 4 }} />
            <span style={{ fontFamily: 'Inter', fontSize: 22, fontWeight: 700, color: '#080616' }}>96 — Family/Other</span>
          </div>
        </div>
      </div>

      {/* Bottom: 3 source pills */}
      <div style={{
        position: 'absolute', bottom: 60, left: 100, right: 100,
        display: 'flex', gap: 24, justifyContent: 'center',
      }}>
        {[
          { d: sourceFamily, label: 'परिवार की बचत', sub: 'Family Savings' },
          { d: sourceInformal, label: 'Informal Borrowing', sub: 'उधार रिश्तेदारों से' },
          { d: sourceSacrifice, label: 'व्यक्तिगत बलिदान', sub: 'Personal Sacrifice' },
        ].map((item, i) => (
          <div key={i} style={{
            flex: 1, maxWidth: 540,
            backgroundColor: '#080616',
            borderRadius: 14,
            padding: '20px 28px',
            opacity: item.d,
            transform: `translateY(${interpolate(item.d,[0,1],[40,0])}px) scale(${item.d})`,
            borderTop: '5px solid #ff0000',
            textAlign: 'center',
          }}>
            <div style={{ fontFamily: 'Yantra One', fontSize: 38, fontWeight: 900, color: '#F5F5F5', lineHeight: 1 }}>
              {item.label}
            </div>
            <div style={{ fontFamily: 'Inter', fontSize: 18, fontWeight: 600, color: '#FFC107', marginTop: 8, letterSpacing: 2 }}>
              {item.sub}
            </div>
          </div>
        ))}
      </div>

      <AbsoluteFill style={{
        background: 'radial-gradient(circle at center, transparent 50%, rgba(31,21,12,0.25) 100%)',
        pointerEvents: 'none',
      }} />

      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#9E9E9E', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default Scene410_4PercentReality;
```

