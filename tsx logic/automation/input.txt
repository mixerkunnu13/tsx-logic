```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'KhamoshDashakIntro',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const KhamoshDashakIntro: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const labelOp = sp(10);
  const numberScale = sp(25);
  const titleOp = sp(55);
  const underlineWidth = interpolate(sp(85), [0, 1], [0, 720]);
  const subOp = sp(110);

  // Camera zoom
  const camScale = interpolate(frame, [0, durationInFrames], [1.02, 1.08]);

  // Shake on number reveal
  const shake = frame > 25 && frame < 40 ? Math.sin(frame * 2) * (40 - frame) * 0.5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFDE42', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient overlay */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 30% 40%, rgba(223,241,241,0.45) 0%, transparent 60%)',
      }} />

      {/* Shape Layer — large drifting circles */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 1);
        const x = r * 1920;
        const y = seededRandom(i + 10) * 1080;
        const size = 280 + r * 240;
        const drift = Math.sin(frame * 0.012 + i) * 30;
        return (
          <div key={i} style={{
            position: 'absolute',
            left: x + drift, top: y + Math.cos(frame * 0.01 + i) * 25,
            width: size, height: size,
            borderRadius: '50%',
            background: '#1F150C',
            opacity: 0.04,
            transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.05})`,
          }} />
        );
      })}

      {/* Motion texture — vertical lines */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.07 }}>
        {Array.from({ length: 30 }).map((_, i) => (
          <line key={i} x1={i * 64} y1="0" x2={i * 64} y2="1080" stroke="#1F150C" strokeWidth="1" />
        ))}
      </svg>

      {/* Floating particles */}
      {Array.from({ length: 25 }).map((_, i) => {
        const r = seededRandom(i + 50);
        const px = (r * 1920 + frame * (0.3 + r)) % 1920;
        const py = seededRandom(i + 70) * 1080 + Math.sin(frame * 0.03 + i) * 20;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py,
            width: 6, height: 6, borderRadius: '50%',
            background: '#080616', opacity: 0.25,
          }} />
        );
      })}

      {/* Main Content */}
      <AbsoluteFill style={{
        transform: `scale(${camScale}) translateX(${shake}px)`,
        display: 'flex', alignItems: 'center', justifyContent: 'center',
      }}>
        <div style={{ textAlign: 'center', padding: 60 }}>
          {/* Top Label */}
          <div style={{
            opacity: labelOp,
            fontFamily: 'Inter, Poppins',
            fontSize: 36,
            letterSpacing: 10,
            color: '#1F150C',
            fontWeight: 700,
            marginBottom: 30,
            transform: `translateY(${(1 - labelOp) * -20}px)`,
          }}>
            FUNDAMENTAL · 03
          </div>

          {/* Giant Number */}
          <div style={{
            fontSize: 280,
            fontFamily: 'Yantra One',
            color: '#080616',
            fontWeight: 900,
            lineHeight: 0.85,
            transform: `scale(${numberScale})`,
            marginBottom: 10,
          }}>
            03
          </div>

          {/* Title Hindi */}
          <div style={{
            opacity: titleOp,
            fontFamily: 'Yantra One',
            fontSize: 170,
            color: '#1F150C',
            fontWeight: 800,
            lineHeight: 1,
            transform: `translateY(${(1 - titleOp) * 30}px)`,
            marginTop: 20,
          }}>
            ख़ामोश दशक
          </div>

          {/* Underline */}
          <div style={{
            width: underlineWidth,
            height: 8,
            background: '#080616',
            margin: '30px auto',
            borderRadius: 4,
          }} />

          {/* Subtext */}
          <div style={{
            opacity: subOp,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 50,
            color: '#1F150C',
            fontWeight: 500,
            transform: `translateY(${(1 - subOp) * 20}px)`,
          }}>
            जहाँ 90% लोग टूट जाते हैं
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(31,21,12,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default KhamoshDashakIntro;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'SecretChapter',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const SecretChapter: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const bookOp = sp(15);
  const line1Op = sp(50);
  const line2Op = sp(90);
  const line3Op = sp(130);
  const stampOp = sp(180);
  const stampRot = interpolate(sp(180), [0, 1], [-30, -12]);

  const camPan = interpolate(frame, [0, durationInFrames], [0, -40]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Soft gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #DFF1F1 0%, #FFDE42 120%)',
        opacity: 0.5,
      }} />

      {/* Shape layer — triangles */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 5);
        const x = 200 + r * 1500;
        const y = 100 + seededRandom(i + 15) * 800;
        const rot = frame * 0.15 + i * 60;
        return (
          <svg key={i} width="400" height="400" style={{
            position: 'absolute', left: x, top: y,
            opacity: 0.05,
            transform: `rotate(${rot}deg) scale(${1 + Math.sin(frame * 0.02 + i) * 0.08})`,
          }}>
            <polygon points="200,40 360,340 40,340" fill="#080616" />
          </svg>
        );
      })}

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 14 }).map((_, i) => (
          <line key={`h${i}`} x1="0" y1={i * 80} x2="1920" y2={i * 80} stroke="#1F150C" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 100);
        const px = (r * 1920 + frame * 0.5) % 1920;
        const py = seededRandom(i + 200) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.04 + i) * 15,
            width: 5, height: 5, borderRadius: '50%',
            background: '#1F150C', opacity: 0.2,
          }} />
        );
      })}

      {/* Main Content */}
      <AbsoluteFill style={{
        transform: `translateY(${camPan}px)`,
        display: 'flex', alignItems: 'center', justifyContent: 'center',
        padding: 100,
      }}>
        <div style={{ width: '100%', maxWidth: 1500 }}>
          {/* Book / chapter visual */}
          <div style={{
            opacity: bookOp,
            transform: `scale(${bookOp}) translateY(${(1 - bookOp) * 30}px)`,
            background: '#FFDE42',
            border: '6px solid #1F150C',
            padding: '40px 60px',
            borderRadius: 18,
            marginBottom: 50,
            boxShadow: '14px 14px 0 #1F150C',
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 32, letterSpacing: 8,
              color: '#1F150C', fontWeight: 700, marginBottom: 14,
            }}>
              CHAPTER · UNSPOKEN
            </div>
            <div style={{
              fontFamily: 'Yantra One', fontSize: 110,
              color: '#080616', fontWeight: 900, lineHeight: 1,
            }}>
              हर अमीर की कहानी
            </div>
          </div>

          {/* Lines */}
          <div style={{
            opacity: line1Op,
            transform: `translateX(${(1 - line1Op) * -40}px)`,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 52, color: '#1F150C', fontWeight: 600,
            marginBottom: 22,
          }}>
            एक दौर ऐसा आता है —
          </div>

          <div style={{
            opacity: line2Op,
            transform: `translateX(${(1 - line2Op) * -40}px)`,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 62, color: '#080616', fontWeight: 800,
            marginBottom: 22,
            background: '#FFDE42',
            display: 'inline-block',
            padding: '8px 24px',
            borderRadius: 8,
          }}>
            जिसके बारे में वो खुलकर बात नहीं करता
          </div>

          <div style={{
            opacity: line3Op,
            transform: `translateX(${(1 - line3Op) * -40}px)`,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 48, color: '#1F150C', fontWeight: 500,
            fontStyle: 'italic',
            marginTop: 18,
          }}>
            क्योंकि सुनकर लोग डर जाते हैं
          </div>
        </div>

        {/* Stamp */}
        <div style={{
          position: 'absolute', right: 120, top: 140,
          opacity: stampOp * 0.85,
          transform: `rotate(${stampRot}deg) scale(${0.5 + stampOp * 0.5})`,
          border: '8px solid #080616',
          padding: '20px 40px',
          fontFamily: 'Inter', fontSize: 48, fontWeight: 900,
          color: '#080616',
          letterSpacing: 6,
        }}>
          CONFIDENTIAL
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(8,6,22,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default SecretChapter;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'TenYearGrind',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const TenYearGrind: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headOp = sp(10);
  const yearsOp = sp(40);
  const pillars = [80, 110, 140, 170];
  const pillarSps = pillars.map((d) => sp(d));

  const camTilt = interpolate(frame, [0, durationInFrames], [-0.5, 0.5]);

  const verbs = ['मेहनत', 'बचत', 'निवेश', 'संयम'];

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFDE42', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #FFDE42 0%, #DFF1F1 100%)',
        opacity: 0.6,
      }} />

      {/* Shape layer — rectangles */}
      {[0, 1, 2].map((i) => {
        const r = seededRandom(i + 20);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1500, top: seededRandom(i + 30) * 800,
            width: 500, height: 500,
            background: '#1F150C',
            opacity: 0.04,
            transform: `rotate(${frame * 0.1 + i * 30}deg)`,
          }} />
        );
      })}

      {/* Diagonal lines texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 20 }).map((_, i) => (
          <line key={i} x1={-200 + i * 150} y1="0" x2={i * 150} y2="1080" stroke="#1F150C" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 300);
        const px = (r * 1920 + frame * 0.4) % 1920;
        const py = seededRandom(i + 400) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.05 + i) * 18,
            width: 6, height: 6, borderRadius: '50%',
            background: '#080616', opacity: 0.2,
          }} />
        );
      })}

      {/* Main */}
      <AbsoluteFill style={{
        transform: `rotate(${camTilt}deg)`,
        display: 'flex', flexDirection: 'column', alignItems: 'center',
        justifyContent: 'center', padding: 80,
      }}>
        {/* Headline */}
        <div style={{
          opacity: headOp,
          transform: `translateY(${(1 - headOp) * -30}px)`,
          fontFamily: 'Yantra One',
          fontSize: 110, color: '#1F150C', fontWeight: 800,
          marginBottom: 30, textAlign: 'center', lineHeight: 1,
        }}>
          पहला दस साल का दौर
        </div>

        {/* 10 YEARS giant */}
        <div style={{
          opacity: yearsOp,
          transform: `scale(${yearsOp})`,
          fontFamily: 'Yantra One',
          fontSize: 240, color: '#080616', fontWeight: 900,
          lineHeight: 0.9, marginBottom: 50,
          textShadow: '8px 8px 0 #FFDE42',
        }}>
          10 <span style={{ fontSize: 130 }}>YEARS</span>
        </div>

        {/* Four Pillars */}
        <div style={{ display: 'flex', gap: 30, justifyContent: 'center' }}>
          {verbs.map((v, i) => {
            const o = pillarSps[i];
            return (
              <div key={i} style={{
                opacity: o,
                transform: `translateY(${(1 - o) * 40}px) scale(${o})`,
                width: 320, height: 200,
                background: i % 2 === 0 ? '#1F150C' : '#080616',
                color: '#FFDE42',
                display: 'flex', alignItems: 'center', justifyContent: 'center',
                fontFamily: 'Yantra One', fontSize: 72, fontWeight: 800,
                borderRadius: 14,
                boxShadow: '10px 10px 0 rgba(31,21,12,0.3)',
              }}>
                {v}
              </div>
            );
          })}
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(8,6,22,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default TenYearGrind;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'PortfolioShock',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const PortfolioShock: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const cardOp = sp(15);
  const titleOp = sp(40);
  const amountVal = interpolate(sp(70), [0, 1], [0, 2300000]);
  const amountOp = sp(70);
  const sighOp = sp(130);
  const shake = frame > 70 && frame < 95 ? Math.sin(frame * 3) * 6 : 0;

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.05]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 70% 30%, rgba(255,222,66,0.4) 0%, transparent 60%)',
      }} />

      {/* Shapes */}
      {[0, 1, 2, 3].map((i) => {
        const r = seededRandom(i + 40);
        return (
          <div key={i} style={{
            position: 'absolute',
            left: r * 1700, top: seededRandom(i + 50) * 900,
            width: 350, height: 350,
            borderRadius: '50%',
            border: '3px solid #1F150C',
            opacity: 0.06,
            transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.1})`,
          }} />
        );
      })}

      {/* Texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 16 }).map((_, i) => (
          <circle key={i} cx={i * 130 + 60} cy={540} r="3" fill="#1F150C" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 500);
        const px = (r * 1920 + frame * 0.6) % 1920;
        const py = seededRandom(i + 600) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.cos(frame * 0.04 + i) * 20,
            width: 5, height: 5, borderRadius: '50%',
            background: '#080616', opacity: 0.2,
          }} />
        );
      })}

      {/* Main */}
      <AbsoluteFill style={{
        transform: `scale(${camZoom}) translateX(${shake}px)`,
        display: 'flex', alignItems: 'center', justifyContent: 'center', padding: 80,
      }}>
        {/* Phone / Portfolio Card */}
        <div style={{
          opacity: cardOp,
          transform: `scale(${cardOp})`,
          width: 980,
          background: '#FFDE42',
          border: '8px solid #1F150C',
          borderRadius: 32,
          padding: 60,
          boxShadow: '18px 18px 0 #1F150C',
        }}>
          {/* Top bar */}
          <div style={{
            display: 'flex', justifyContent: 'space-between', alignItems: 'center',
            paddingBottom: 24, borderBottom: '3px solid #1F150C', marginBottom: 30,
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 28, fontWeight: 700,
              color: '#1F150C', letterSpacing: 4,
            }}>
              MY PORTFOLIO
            </div>
            <div style={{
              fontFamily: 'JetBrains Mono', fontSize: 26,
              color: '#1F150C',
            }}>
              10 YRS
            </div>
          </div>

          {/* Title */}
          <div style={{
            opacity: titleOp,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 42, color: '#1F150C', fontWeight: 500,
            marginBottom: 18,
          }}>
            कुल जमा
          </div>

          {/* Amount */}
          <div style={{
            opacity: amountOp,
            fontFamily: 'JetBrains Mono',
            fontSize: 160, color: '#080616', fontWeight: 900,
            lineHeight: 1, marginBottom: 20,
          }}>
            ₹{Math.floor(amountVal).toLocaleString('en-IN')}
          </div>

          {/* Sub */}
          <div style={{
            opacity: amountOp,
            fontFamily: 'Noto Sans Devanagari',
            fontSize: 38, color: '#1F150C', fontWeight: 600,
          }}>
            ~ 23 लाख रुपये
          </div>
        </div>
      </AbsoluteFill>

      {/* Reaction text */}
      <div style={{
        position: 'absolute', bottom: 90, left: 0, right: 0,
        textAlign: 'center', opacity: sighOp,
        transform: `translateY(${(1 - sighOp) * 30}px)`,
      }}>
        <div style={{
          display: 'inline-block',
          background: '#1F150C',
          color: '#FFDE42',
          fontFamily: 'Yantra One',
          fontSize: 70, fontWeight: 800,
          padding: '18px 50px',
          borderRadius: 14,
        }}>
          "यार… बस इतना सा?"
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 50%, rgba(8,6,22,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default PortfolioShock;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'SamePlaceVsInsta',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const SamePlaceVsInsta: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const leftOp = sp(15);
  const rightOp = sp(70);
  const item1 = sp(35);
  const item2 = sp(50);
  const item3 = sp(65);

  const reel1 = sp(90);
  const reel2 = sp(105);
  const reel3 = sp(120);

  const dividerH = interpolate(sp(10), [0, 1], [0, 980]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFDE42', overflow: 'hidden', opacity: exitOp }}>
      {/* Split halves */}
      <div style={{
        position: 'absolute', left: 0, top: 0, width: '50%', height: '100%',
        background: '#FFDE42',
      }} />
      <div style={{
        position: 'absolute', right: 0, top: 0, width: '50%', height: '100%',
        background: '#DFF1F1',
      }} />

      {/* Center divider */}
      <div style={{
        position: 'absolute', left: '50%', top: 50,
        width: 8, height: dividerH,
        background: '#080616',
        transform: 'translateX(-50%)',
      }} />

      {/* Shape layer */}
      {[0, 1, 2].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 60) * 1700,
          top: seededRandom(i + 70) * 900,
          width: 300, height: 300, borderRadius: '50%',
          background: '#1F150C', opacity: 0.04,
          transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.1})`,
        }} />
      ))}

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 700);
        const px = (r * 1920 + frame * 0.4) % 1920;
        const py = seededRandom(i + 800) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.04 + i) * 15,
            width: 5, height: 5, borderRadius: '50%',
            background: '#080616', opacity: 0.18,
          }} />
        );
      })}

      {/* LEFT SIDE — YOU */}
      <div style={{
        position: 'absolute', left: 0, top: 0, width: '50%', height: '100%',
        padding: 80, display: 'flex', flexDirection: 'column', justifyContent: 'center',
      }}>
        <div style={{
          opacity: leftOp,
          fontFamily: 'Inter', fontSize: 30, letterSpacing: 8,
          color: '#1F150C', fontWeight: 700, marginBottom: 18,
        }}>
          आपकी ज़िंदगी
        </div>
        <div style={{
          opacity: leftOp,
          fontFamily: 'Yantra One', fontSize: 90,
          color: '#1F150C', fontWeight: 800, lineHeight: 1, marginBottom: 50,
        }}>
          वही… वही… वही…
        </div>

        {[
          { o: item1, t: 'वही पुराना फ़्लैट' },
          { o: item2, t: 'वही पुरानी गाड़ी' },
          { o: item3, t: 'वही ज़िंदगी' },
        ].map((row, i) => (
          <div key={i} style={{
            opacity: row.o,
            transform: `translateX(${(1 - row.o) * -30}px)`,
            display: 'flex', alignItems: 'center',
            marginBottom: 22,
          }}>
            <div style={{
              width: 18, height: 18,
              background: '#080616', borderRadius: 4, marginRight: 22,
            }} />
            <div style={{
              fontFamily: 'Noto Sans Devanagari', fontSize: 50,
              color: '#1F150C', fontWeight: 600,
            }}>
              {row.t}
            </div>
          </div>
        ))}
      </div>

      {/* RIGHT SIDE — INSTAGRAM */}
      <div style={{
        position: 'absolute', right: 0, top: 0, width: '50%', height: '100%',
        padding: 80, display: 'flex', flexDirection: 'column', justifyContent: 'center',
      }}>
        <div style={{
          opacity: rightOp,
          fontFamily: 'Inter', fontSize: 30, letterSpacing: 8,
          color: '#080616', fontWeight: 700, marginBottom: 18,
        }}>
          INSTAGRAM FEED
        </div>
        <div style={{
          opacity: rightOp,
          fontFamily: 'Yantra One', fontSize: 90,
          color: '#080616', fontWeight: 800, lineHeight: 1, marginBottom: 40,
        }}>
          सब उड़ रहे हैं
        </div>

        {/* Reel cards */}
        {[
          { o: reel1, place: 'DUBAI', sub: 'Burj Khalifa · 7★ stay' },
          { o: reel2, place: 'SWITZERLAND', sub: 'Snow reels · Jungfrau' },
          { o: reel3, place: 'BALI', sub: 'Villa · Private pool' },
        ].map((r, i) => (
          <div key={i} style={{
            opacity: r.o,
            transform: `translateX(${(1 - r.o) * 40}px) rotate(${(i - 1) * 1.5}deg)`,
            background: '#FFDE42',
            border: '5px solid #080616',
            borderRadius: 14,
            padding: '18px 30px',
            marginBottom: 18,
            boxShadow: '8px 8px 0 #080616',
          }}>
            <div style={{
              fontFamily: 'Inter', fontSize: 42, fontWeight: 900,
              color: '#080616', letterSpacing: 3,
            }}>
              {r.place}
            </div>
            <div style={{
              fontFamily: 'Inter', fontSize: 24, fontWeight: 500,
              color: '#1F150C', marginTop: 4,
            }}>
              {r.sub}
            </div>
          </div>
        ))}
      </div>

      {/* VS Badge center */}
      <div style={{
        position: 'absolute', left: '50%', top: '50%',
        transform: `translate(-50%, -50%) scale(${sp(45)})`,
        width: 120, height: 120, borderRadius: '50%',
        background: '#080616', color: '#FFDE42',
        display: 'flex', alignItems: 'center', justifyContent: 'center',
        fontFamily: 'Yantra One', fontSize: 50, fontWeight: 900,
        border: '6px solid #FFDE42',
        boxShadow: '0 0 0 6px #080616',
      }}>
        VS
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(8,6,22,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default SamePlaceVsInsta;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'NinetyPercentBreak',
  durationInSeconds: 13,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const NinetyPercentBreak: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const percentVal = interpolate(sp(15), [0, 1], [0, 90]);
  const percentOp = sp(15);
  const labelOp = sp(70);
  const arrowOp = sp(100);
  const trap1 = sp(120);
  const trap2 = sp(140);
  const trap3 = sp(160);

  const shake = frame > 100 && frame < 130 ? Math.sin(frame * 3) * 5 : 0;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFDE42', overflow: 'hidden', opacity: exitOp }}>
      {/* Background grad */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 50%, #FFDE42 0%, #DFF1F1 110%)',
      }} />

      {/* Shapes */}
      {[0, 1, 2].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 80) * 1700, top: seededRandom(i + 90) * 900,
          width: 380, height: 380, borderRadius: '50%',
          background: '#1F150C', opacity: 0.04,
          transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.08})`,
        }} />
      ))}

      {/* Grid texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.05 }}>
        {Array.from({ length: 24 }).map((_, i) => (
          <line key={i} x1="0" y1={i * 50} x2="1920" y2={i * 50} stroke="#1F150C" strokeWidth="1" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 20 }).map((_, i) => {
        const r = seededRandom(i + 900);
        const px = (r * 1920 + frame * 0.5) % 1920;
        const py = seededRandom(i + 1000) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.04 + i) * 15,
            width: 5, height: 5, borderRadius: '50%',
            background: '#080616', opacity: 0.2,
          }} />
        );
      })}

      {/* Main */}
      <AbsoluteFill style={{
        display: 'flex', alignItems: 'center', justifyContent: 'center',
        padding: 80, transform: `translateX(${shake}px)`,
      }}>
        <div style={{ display: 'flex', alignItems: 'center', gap: 70, width: '100%', justifyContent: 'center' }}>
          
          {/* Left — 90% */}
          <div style={{ textAlign: 'center' }}>
            <div style={{
              opacity: percentOp,
              fontFamily: 'Yantra One',
              fontSize: 420,
              color: '#080616', fontWeight: 900,
              lineHeight: 0.9,
              textShadow: '12px 12px 0 #1F150C',
            }}>
              {Math.floor(percentVal)}%
            </div>
            <div style={{
              opacity: labelOp,
              fontFamily: 'Noto Sans Devanagari',
              fontSize: 56, color: '#1F150C', fontWeight: 700,
              marginTop: 10,
            }}>
              यहीं टूट जाते हैं
            </div>
          </div>

          {/* Arrow */}
          <div style={{
            opacity: arrowOp,
            transform: `scale(${arrowOp})`,
          }}>
            <svg width="160" height="120">
              <path d="M10 60 L140 60 M100 20 L140 60 L100 100"
                stroke="#1F150C" strokeWidth="10" fill="none"
                strokeLinecap="round" strokeLinejoin="round" />
            </svg>
          </div>

          {/* Right — Traps */}
          <div style={{ display: 'flex', flexDirection: 'column', gap: 22 }}>
            {[
              { o: trap1, t: 'SIP बंद', sub: 'मेहनत छोड़ दी' },
              { o: trap2, t: 'CRYPTO में जंप', sub: 'लालच का रास्ता' },
              { o: trap3, t: 'F&O में हाथ', sub: 'जुआ शुरू' },
            ].map((row, i) => (
              <div key={i} style={{
                opacity: row.o,
                transform: `translateX(${(1 - row.o) * 50}px)`,
                background: '#1F150C',
                color: '#FFDE42',
                padding: '22px 36px',
                borderRadius: 12,
                width: 460,
                boxShadow: '8px 8px 0 #080616',
              }}>
                <div style={{ fontFamily: 'Yantra One', fontSize: 56, fontWeight: 800 }}>
                  {row.t}
                </div>
                <div style={{ fontFamily: 'Noto Sans Devanagari', fontSize: 28, opacity: 0.85, marginTop: 4 }}>
                  {row.sub}
                </div>
              </div>
            ))}
          </div>
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(8,6,22,0.35) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default NinetyPercentBreak;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'SIPMathSetup',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const SIPMathSetup: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const headOp = sp(10);
  const card1Op = sp(45);
  const card2Op = sp(80);
  const card3Op = sp(115);
  const noteOp = sp(160);

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(135deg, #DFF1F1 0%, #FFDE42 130%)',
        opacity: 0.6,
      }} />

      {/* Shape — large rotating squares */}
      {[0, 1, 2].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 110) * 1600, top: seededRandom(i + 120) * 800,
          width: 400, height: 400,
          border: '4px solid #1F150C',
          opacity: 0.05,
          transform: `rotate(${frame * 0.2 + i * 45}deg)`,
        }} />
      ))}

      {/* Dot texture */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {Array.from({ length: 12 }).map((_, r) =>
          Array.from({ length: 24 }).map((_, c) => (
            <circle key={`${r}-${c}`} cx={c * 85 + 20} cy={r * 95 + 30} r="2.5" fill="#1F150C" />
          ))
        )}
      </svg>

      {/* Particles */}
      {Array.from({ length: 18 }).map((_, i) => {
        const r = seededRandom(i + 1100);
        const px = (r * 1920 + frame * 0.45) % 1920;
        const py = seededRandom(i + 1200) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.04 + i) * 15,
            width: 5, height: 5, borderRadius: '50%',
            background: '#080616', opacity: 0.2,
          }} />
        );
      })}

      {/* Main */}
      <AbsoluteFill style={{
        display: 'flex', flexDirection: 'column', alignItems: 'center',
        justifyContent: 'center', padding: 70,
      }}>
        <div style={{
          opacity: headOp,
          transform: `translateY(${(1 - headOp) * -30}px)`,
          fontFamily: 'Yantra One',
          fontSize: 100, color: '#080616', fontWeight: 900,
          marginBottom: 50, textAlign: 'center', lineHeight: 1,
        }}>
          SIP का असली गणित
        </div>

        {/* Three input cards */}
        <div style={{ display: 'flex', gap: 36 }}>
          {[
            { o: card1Op, label: 'मासिक SIP', value: '₹10,000', sub: 'हर महीने' },
            { o: card2Op, label: 'रिटर्न', value: '12%', sub: 'औसत सालाना' },
            { o: card3Op, label: 'समय', value: '40 साल', sub: 'धैर्य' },
          ].map((c, i) => (
            <div key={i} style={{
              opacity: c.o,
              transform: `translateY(${(1 - c.o) * 40}px) scale(${c.o})`,
              background: i === 1 ? '#1F150C' : '#FFDE42',
              color: i === 1 ? '#FFDE42' : '#1F150C',
              border: '6px solid #1F150C',
              borderRadius: 18,
              padding: '40px 50px',
              width: 380,
              textAlign: 'center',
              boxShadow: '12px 12px 0 #1F150C',
            }}>
              <div style={{
                fontFamily: 'Inter', fontSize: 26,
                letterSpacing: 5, fontWeight: 700, marginBottom: 16,
                opacity: 0.8,
              }}>
                {c.label}
              </div>
              <div style={{
                fontFamily: 'Yantra One', fontSize: 100,
                fontWeight: 900, lineHeight: 0.95, marginBottom: 10,
              }}>
                {c.value}
              </div>
              <div style={{
                fontFamily: 'Noto Sans Devanagari', fontSize: 30, fontWeight: 500,
              }}>
                {c.sub}
              </div>
            </div>
          ))}
        </div>

        {/* Note */}
        <div style={{
          opacity: noteOp,
          marginTop: 60,
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 44, color: '#080616', fontWeight: 700,
          background: '#FFDE42',
          padding: '14px 34px',
          borderRadius: 10,
          border: '3px solid #1F150C',
        }}>
          ⚙ कंपाउंडिंग का खेल — पीछे की तरफ़
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(8,6,22,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default SIPMathSetup;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'SIPGrowthChart',
  durationInSeconds: 15,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const SIPGrowthChart: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = sp(10);
  const axisOp = sp(30);

  // 8 bars
  const data = [
    { y: 5,  v: 8,    label: '8L' },
    { y: 10, v: 23,   label: '23L' },
    { y: 15, v: 50,   label: '50L' },
    { y: 20, v: 100,  label: '1Cr' },
    { y: 25, v: 190,  label: '1.9Cr' },
    { y: 30, v: 350,  label: '3.5Cr' },
    { y: 35, v: 650,  label: '6.5Cr' },
    { y: 40, v: 1150, label: '11.5Cr' },
  ];

  const maxV = 1150;
  const chartH = 620;
  const chartW = 1500;
  const baseY = 800;
  const baseX = 280;
  const barW = chartW / data.length - 14;

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFDE42', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #FFDE42 0%, #DFF1F1 100%)',
        opacity: 0.5,
      }} />

      {/* Shapes */}
      {[0, 1, 2].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 130) * 1600, top: seededRandom(i + 140) * 800,
          width: 350, height: 350, borderRadius: '50%',
          background: '#1F150C', opacity: 0.04,
          transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.08})`,
        }} />
      ))}

      {/* Particles */}
      {Array.from({ length: 16 }).map((_, i) => {
        const r = seededRandom(i + 1300);
        const px = (r * 1920 + frame * 0.4) % 1920;
        const py = seededRandom(i + 1400) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.cos(frame * 0.05 + i) * 15,
            width: 5, height: 5, borderRadius: '50%',
            background: '#080616', opacity: 0.18,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        opacity: titleOp,
        position: 'absolute', top: 60, left: 0, right: 0,
        textAlign: 'center',
        fontFamily: 'Yantra One', fontSize: 75, color: '#080616', fontWeight: 900,
      }}>
        ₹10,000 / month → 40 साल का सफ़र
      </div>

      {/* Axis */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: axisOp }}>
        {/* X axis */}
        <line x1={baseX} y1={baseY} x2={baseX + chartW} y2={baseY}
          stroke="#1F150C" strokeWidth="4" />
        {/* Y axis */}
        <line x1={baseX} y1={baseY} x2={baseX} y2={baseY - chartH}
          stroke="#1F150C" strokeWidth="4" />

        {/* Bars */}
        {data.map((d, i) => {
          const barDelay = 40 + i * 14;
          const grow = sp(barDelay);
          const h = (d.v / maxV) * chartH * grow;
          const x = baseX + i * (chartW / data.length) + 18;
          const y = baseY - h;
          const isLast = i === data.length - 1;
          return (
            <g key={i}>
              <rect x={x} y={y} width={barW} height={h}
                fill={isLast ? '#080616' : '#1F150C'}
                rx="6" />
              {/* Label on top */}
              {grow > 0.6 && (
                <text x={x + barW / 2} y={y - 18}
                  fill="#080616"
                  fontFamily="JetBrains Mono"
                  fontSize="30"
                  fontWeight="900"
                  textAnchor="middle">
                  ₹{d.label}
                </text>
              )}
              {/* Year label */}
              <text x={x + barW / 2} y={baseY + 45}
                fill="#1F150C"
                fontFamily="Inter"
                fontSize="28"
                fontWeight="700"
                textAnchor="middle">
                {d.y}Y
              </text>
            </g>
          );
        })}

        {/* Highlight curve hint */}
        <text x={baseX + chartW - 180} y={baseY - chartH + 50}
          fill="#080616" fontFamily="Yantra One"
          fontSize="60" fontWeight="900"
          opacity={sp(180)}>
          🚀
        </text>
      </svg>

      {/* Bottom insight */}
      <div style={{
        position: 'absolute', bottom: 60, left: 0, right: 0,
        textAlign: 'center', opacity: sp(200),
      }}>
        <div style={{
          display: 'inline-block',
          background: '#1F150C',
          color: '#FFDE42',
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 42, fontWeight: 700,
          padding: '14px 36px',
          borderRadius: 10,
        }}>
          आख़िरी 10 साल = ₹5 करोड़ की छलाँग
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 60%, rgba(8,6,22,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default SIPGrowthChart;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'BambooMetaphor',
  durationInSeconds: 16,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const BambooMetaphor: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const titleOp = sp(10);
  const groundOp = sp(35);
  const rootGrow = sp(55, { damping: 14 });
  const yearsLabelOp = sp(90);

  // Bamboo grows after frame ~150
  const bambooGrow = interpolate(frame, [150, 220], [0, 1], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp', easing: Easing.out(Easing.cubic),
  });

  const explosionOp = interpolate(frame, [145, 165], [0, 1], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });
  const explosionScale = interpolate(frame, [145, 200], [0, 3], { extrapolateLeft: 'clamp', extrapolateRight: 'clamp' });

  const tagOp = sp(240);

  const groundY = 780;
  const bambooMaxH = 680;

  return (
    <AbsoluteFill style={{ backgroundColor: '#DFF1F1', overflow: 'hidden', opacity: exitOp }}>
      {/* Sky gradient */}
      <AbsoluteFill style={{
        background: 'linear-gradient(180deg, #DFF1F1 0%, #FFDE42 100%)',
      }} />

      {/* Shapes — clouds */}
      {[0, 1, 2].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: 200 + i * 600 + Math.sin(frame * 0.01 + i) * 30,
          top: 120 + i * 60,
          width: 280, height: 80,
          borderRadius: 60,
          background: '#1F150C', opacity: 0.05,
        }} />
      ))}

      {/* Particles */}
      {Array.from({ length: 15 }).map((_, i) => {
        const r = seededRandom(i + 1500);
        const px = (r * 1920 + frame * 0.3) % 1920;
        const py = seededRandom(i + 1600) * 700;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.04 + i) * 12,
            width: 6, height: 6, borderRadius: '50%',
            background: '#080616', opacity: 0.18,
          }} />
        );
      })}

      {/* Title */}
      <div style={{
        opacity: titleOp,
        position: 'absolute', top: 50, left: 0, right: 0,
        textAlign: 'center',
        fontFamily: 'Yantra One', fontSize: 80, color: '#080616', fontWeight: 900,
      }}>
        बाँस का पेड़ = कंपाउंडिंग
      </div>

      {/* Ground line */}
      <div style={{
        position: 'absolute', top: groundY, left: 0, right: 0,
        height: 6, background: '#1F150C', opacity: groundOp,
      }} />

      {/* Underground roots area */}
      <div style={{
        position: 'absolute', top: groundY + 6, left: 0, right: 0,
        height: 1080 - groundY, background: '#1F150C', opacity: 0.08,
      }} />

      {/* Roots SVG (years 1-4) */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: rootGrow }}>
        <g transform="translate(960, 780)">
          {[0, 1, 2, 3, 4, 5, 6].map((i) => {
            const angle = -90 + (i - 3) * 22;
            const len = 80 + i * 12;
            const rad = (angle * Math.PI) / 180;
            const x2 = Math.cos(rad) * len * rootGrow;
            const y2 = -Math.sin(rad) * len * rootGrow + 60;
            return (
              <line key={i} x1="0" y1="0" x2={x2} y2={y2}
                stroke="#1F150C" strokeWidth="4" strokeLinecap="round" />
            );
          })}
          {/* small roots */}
          {[0, 1, 2, 3, 4].map((i) => (
            <line key={`r${i}`} x1={(i - 2) * 40} y1="100"
              x2={(i - 2) * 60} y2={180 * rootGrow}
              stroke="#1F150C" strokeWidth="3" />
          ))}
        </g>
      </svg>

      {/* Year markers */}
      <div style={{
        opacity: yearsLabelOp,
        position: 'absolute', top: groundY + 60, left: 200, right: 200,
        display: 'flex', justifyContent: 'space-between',
      }}>
        {['Y1', 'Y2', 'Y3', 'Y4', 'Y5'].map((y, i) => (
          <div key={i} style={{
            fontFamily: 'JetBrains Mono', fontSize: 32, fontWeight: 800,
            color: '#1F150C',
            background: i === 4 ? '#080616' : 'transparent',
            color: i === 4 ? '#FFDE42' : '#1F150C',
            padding: i === 4 ? '6px 16px' : 0,
            borderRadius: 8,
          }}>
            {y}
          </div>
        ))}
      </div>

      {/* Explosion burst at Y5 */}
      <div style={{
        position: 'absolute', left: 960 - 80, top: groundY - 80,
        width: 160, height: 160,
        opacity: explosionOp,
        transform: `scale(${explosionScale})`,
        borderRadius: '50%',
        background: 'radial-gradient(circle, #FFDE42 0%, transparent 70%)',
      }} />

      {/* Bamboo shoot */}
      <div style={{
        position: 'absolute', left: 960 - 22, top: groundY - bambooMaxH * bambooGrow,
        width: 44, height: bambooMaxH * bambooGrow,
        background: '#1F150C',
        borderRadius: '8px 8px 0 0',
      }}>
        {/* Bamboo segments */}
        {[0, 1, 2, 3, 4, 5].map((i) => (
          <div key={i} style={{
            position: 'absolute',
            top: `${(i + 1) * 14}%`,
            left: -6, right: -6,
            height: 4, background: '#080616',
          }} />
        ))}
      </div>

      {/* Leaves at top */}
      {bambooGrow > 0.7 && (
        <svg width="200" height="120" style={{
          position: 'absolute',
          left: 960 - 100, top: groundY - bambooMaxH * bambooGrow - 60,
          opacity: (bambooGrow - 0.7) / 0.3,
        }}>
          <ellipse cx="60" cy="60" rx="55" ry="14" fill="#1F150C" transform="rotate(-25 60 60)" />
          <ellipse cx="140" cy="60" rx="55" ry="14" fill="#1F150C" transform="rotate(25 140 60)" />
          <ellipse cx="100" cy="30" rx="55" ry="14" fill="#080616" />
        </svg>
      )}

      {/* Tag — 90 ft / 6 weeks */}
      <div style={{
        opacity: tagOp,
        position: 'absolute', right: 120, top: 300,
        transform: `translateY(${(1 - tagOp) * 30}px)`,
        background: '#080616', color: '#FFDE42',
        padding: '20px 36px',
        borderRadius: 14,
        fontFamily: 'Yantra One', fontSize: 56, fontWeight: 900,
        boxShadow: '10px 10px 0 #1F150C',
      }}>
        90 FT<br/>
        <span style={{ fontSize: 32, fontFamily: 'Inter', fontWeight: 700 }}>in 6 weeks</span>
      </div>

      {/* Bottom message */}
      <div style={{
        opacity: tagOp,
        position: 'absolute', bottom: 70, left: 0, right: 0,
        textAlign: 'center',
      }}>
        <div style={{
          display: 'inline-block',
          background: '#FFDE42',
          border: '4px solid #1F150C',
          padding: '14px 36px',
          borderRadius: 10,
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 44, color: '#1F150C', fontWeight: 800,
        }}>
          4 साल जड़ें बनती हैं — फिर विस्फोट
        </div>
      </div>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(8,6,22,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default BambooMetaphor;
```

```tsx
import React from 'react';

import { useCurrentFrame, useVideoConfig, interpolate, Easing, spring, AbsoluteFill } from 'remotion';

export const compositionConfig = {
  id: 'YouAreNotFailing',
  durationInSeconds: 14,
  fps: 30,
  width: 1920,
  height: 1080,
};

const seededRandom = (seed: number): number => {
  const x = Math.sin(seed * 9999) * 10000;
  return x - Math.floor(x);
};

const YouAreNotFailing: React.FC = () => {
  const frame = useCurrentFrame();
  const { fps, durationInFrames } = useVideoConfig();

  const sp = (delay: number, config?: object) =>
    spring({ frame: Math.max(0, frame - delay), fps, config: { damping: 12, mass: 0.4, stiffness: 120, ...config } });

  const exitOp = interpolate(frame, [durationInFrames - 15, durationInFrames], [1, 0], {
    extrapolateLeft: 'clamp', extrapolateRight: 'clamp',
  });

  const strikeOp = sp(15);
  const strikeWidth = interpolate(sp(40), [0, 1], [0, 900]);
  const replaceOp = sp(70);
  const quoteOp = sp(120);
  const equationOp = sp(170);

  const camZoom = interpolate(frame, [0, durationInFrames], [1, 1.04]);

  return (
    <AbsoluteFill style={{ backgroundColor: '#FFDE42', overflow: 'hidden', opacity: exitOp }}>
      {/* Gradient */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle at 50% 60%, #FFDE42 0%, #DFF1F1 110%)',
      }} />

      {/* Shapes */}
      {[0, 1, 2, 3].map((i) => (
        <div key={i} style={{
          position: 'absolute',
          left: seededRandom(i + 160) * 1700, top: seededRandom(i + 170) * 900,
          width: 320, height: 320, borderRadius: '50%',
          background: '#1F150C', opacity: 0.04,
          transform: `scale(${1 + Math.sin(frame * 0.02 + i) * 0.1})`,
        }} />
      ))}

      {/* Texture — concentric arcs */}
      <svg width="1920" height="1080" style={{ position: 'absolute', opacity: 0.06 }}>
        {[0, 1, 2, 3].map((i) => (
          <circle key={i} cx="960" cy="540" r={200 + i * 120}
            stroke="#1F150C" strokeWidth="1.5" fill="none" />
        ))}
      </svg>

      {/* Particles */}
      {Array.from({ length: 22 }).map((_, i) => {
        const r = seededRandom(i + 1700);
        const px = (r * 1920 + frame * 0.5) % 1920;
        const py = seededRandom(i + 1800) * 1080;
        return (
          <div key={i} style={{
            position: 'absolute', left: px, top: py + Math.sin(frame * 0.04 + i) * 18,
            width: 5, height: 5, borderRadius: '50%',
            background: '#080616', opacity: 0.2,
          }} />
        );
      })}

      {/* Main */}
      <AbsoluteFill style={{
        transform: `scale(${camZoom})`,
        display: 'flex', flexDirection: 'column', alignItems: 'center',
        justifyContent: 'center', padding: 80,
      }}>
        {/* Strikethrough sentence */}
        <div style={{
          opacity: strikeOp,
          position: 'relative',
          marginBottom: 40,
        }}>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 95,
            color: '#1F150C', fontWeight: 700, opacity: 0.55,
            lineHeight: 1,
          }}>
            "आप फ़ेल हो रहे हैं"
          </div>
          {/* Strike line */}
          <div style={{
            position: 'absolute', top: '50%', left: '50%',
            transform: `translate(-50%, -50%)`,
            width: strikeWidth, height: 8,
            background: '#080616',
            borderRadius: 4,
          }} />
        </div>

        {/* Replacement headline */}
        <div style={{
          opacity: replaceOp,
          transform: `translateY(${(1 - replaceOp) * 30}px) scale(${0.9 + replaceOp * 0.1})`,
          fontFamily: 'Yantra One', fontSize: 150,
          color: '#080616', fontWeight: 900, lineHeight: 0.95,
          textAlign: 'center',
          background: '#FFDE42',
          padding: '20px 50px',
          borderRadius: 18,
          border: '6px solid #1F150C',
          boxShadow: '14px 14px 0 #1F150C',
          marginBottom: 50,
        }}>
          आप जड़ें<br/>जमा रहे हैं
        </div>

        {/* Equation */}
        <div style={{
          opacity: equationOp,
          display: 'flex', alignItems: 'center', gap: 26,
          marginBottom: 36,
        }}>
          <div style={{
            background: '#1F150C', color: '#FFDE42',
            padding: '18px 30px', borderRadius: 12,
            fontFamily: 'Yantra One', fontSize: 52, fontWeight: 800,
          }}>
            पहले 10 साल
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 70, color: '#080616', fontWeight: 900,
          }}>
            =
          </div>
          <div style={{
            background: '#DFF1F1', color: '#080616',
            padding: '18px 30px', borderRadius: 12,
            fontFamily: 'Yantra One', fontSize: 52, fontWeight: 800,
            border: '4px solid #1F150C',
          }}>
            फ़ीस
          </div>
          <div style={{
            fontFamily: 'Yantra One', fontSize: 70, color: '#080616', fontWeight: 900,
          }}>
            →
          </div>
          <div style={{
            background: '#FFDE42', color: '#1F150C',
            padding: '18px 30px', borderRadius: 12,
            fontFamily: 'Yantra One', fontSize: 52, fontWeight: 800,
            border: '4px solid #080616',
          }}>
            फिर कमाई
          </div>
        </div>

        {/* Quote */}
        <div style={{
          opacity: quoteOp,
          fontFamily: 'Noto Sans Devanagari',
          fontSize: 42, color: '#1F150C', fontWeight: 600,
          fontStyle: 'italic',
          textAlign: 'center',
          maxWidth: 1300,
          lineHeight: 1.3,
        }}>
          "जो सातवें साल में छोड़ देते हैं,<br/>उन्हें पता ही नहीं — वो मंज़िल के कितने क़रीब थे"
        </div>
      </AbsoluteFill>

      {/* Vignette */}
      <AbsoluteFill style={{
        background: 'radial-gradient(circle, transparent 55%, rgba(8,6,22,0.3) 100%)',
        pointerEvents: 'none',
      }} />

      {/* Watermark */}
      <div style={{
        position: 'absolute', bottom: 30, right: 45,
        fontFamily: 'Inter', fontSize: 22, fontWeight: 600,
        color: '#1F150C', opacity: 0.3,
      }}>
        ProBuffet HQ
      </div>
    </AbsoluteFill>
  );
};

export default YouAreNotFailing;
```

